# @retikz/inspect

Optional, host-independent inspection capabilities for retikz compile outputs.

The root entry provides Inspector definitions, registries, runtime selection, diagnostics, planes, and the compile driver. Import `@retikz/inspect/render` explicitly to map a plane to `@retikz/render` readonly layers, or `@retikz/inspect/react` for optional React authoring wrappers and the Layout compile driver.
