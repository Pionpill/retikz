# @retikz/diagram-vanilla

`@retikz/diagram-vanilla` is the framework-free authoring adapter reserved for
`@retikz/diagram`.

The package shell is initialized, but its public API is intentionally empty
until the Diagram roadmap and ADR define the shared authoring contract.

This package is ESM-only and requires Node.js 24 or newer.
本包仅发布 ES modules，要求 Node.js 24 或更高版本。
