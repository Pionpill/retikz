import type { GraphThemeStyleDefinition } from './types';

/** 定义一个可注册的 Graph Theme style */
export const defineGraphThemeStyle = (definition: GraphThemeStyleDefinition): GraphThemeStyleDefinition => definition;
