import type { FlowThemeStyleDefinition } from './types';

/** 定义一个可注册的 Flow Theme style */
export const defineFlowThemeStyle = (definition: FlowThemeStyleDefinition): FlowThemeStyleDefinition => definition;
