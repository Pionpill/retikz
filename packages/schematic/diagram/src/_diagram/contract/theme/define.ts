import type { DiagramThemeStyleDefinition } from './types';

/** 定义一个可注册的 Diagram Theme style */
export const defineDiagramThemeStyle = (definition: DiagramThemeStyleDefinition): DiagramThemeStyleDefinition =>
  definition;
