# @retikz/data

`@retikz/data` is the shared data layer for retikz visualization packages.

It owns JSON-serializable data models, field resolution, transform contracts, transform registries, and pure data pipeline helpers used by `@retikz/plot` and future viz packages.
