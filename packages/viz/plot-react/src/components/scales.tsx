import type { InputPositionScaleType, InputScale, InputScaleDimension } from '@retikz/plot-vanilla';
import type { FC } from 'react';

export type ScaleDimension = InputScaleDimension;
export type PositionScaleType = InputPositionScaleType;
export type ScaleProps = InputScale;

/** 位置比例尺声明组件 */
export const Scale: FC<ScaleProps> = () => null;
