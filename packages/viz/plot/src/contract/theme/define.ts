import type { PlotThemeStyleDefinition } from './types';

/** 定义一个 Plot Theme style resolver */
export const definePlotThemeStyle = (definition: PlotThemeStyleDefinition): PlotThemeStyleDefinition => definition;
