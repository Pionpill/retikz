# @retikz/chart-react

React authoring for canonical Retikz Chart presentation and typed recipes.

The package exports `Chart`, `ScatterChart`, `BubbleChart`,
`ConnectedScatterChart`, the four headless presentation markers, and
`ChartThemeProvider` for Chart-owned named Theme definitions.
