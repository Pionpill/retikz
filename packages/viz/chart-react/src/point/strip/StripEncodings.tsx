import type { IRStripChartEncodings } from '@retikz/chart/point/strip';
import type { FC } from 'react';

/** Strip 字段映射声明属性 */
export type StripEncodingsProps = IRStripChartEncodings;

/** 声明 Strip 精确字段映射 */
export const StripEncodings: FC<StripEncodingsProps> = () => null;
StripEncodings.displayName = 'StripEncodings';
