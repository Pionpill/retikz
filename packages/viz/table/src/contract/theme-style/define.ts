import type { TableThemeStyleDefinition } from './types';

/** 定义一个 Table Theme style resolver */
export const defineTableThemeStyle = (definition: TableThemeStyleDefinition): TableThemeStyleDefinition => definition;
