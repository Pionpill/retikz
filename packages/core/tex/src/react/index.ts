// @retikz/tex/react —— LaTeX 公式的 React 便捷接入：useLowerMath（MathJax startup → core lowerMath 注入）
// 仅此子入口依赖 React（optional peer）；主入口 @retikz/tex 仍为无 React 的纯引擎
export { useLowerMath } from './useLowerMath';
