# 基础用法

面向已完成方案快速开始的读者，固定入门。最小示例先于 controls，选择完成本能力最常见任务所需的最少配置；组件页链接安装入口，不重复 npm 命令。

接入展示真实公开 import、初始化或挂载及结果，不用空 JSX 或配置对象代替闭环。相关属性精选本页的常用成员，不把全部 props 搬到入门页。

数值计算 API 说明单位、坐标空间、参数与距离的区别及退化结果；正确调用所需精度和边界保留在本页，推导与执行过程链接原理页。复杂度注明前提，不用对象引用相等证明数学等价。
