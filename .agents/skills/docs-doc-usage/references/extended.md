# 扩展用法

这里的扩展是组合或配置已有公开能力，不是新增 Definition。先链接前置基础/专题，说明组合后多解决了什么问题，再给完整接入与可观察结果。

章节按组合任务组织，交代各参与能力的职责、输入引用和相互约束；只解释本页实际使用的高级选项。单纯填写内置定义、开放参数或使用现有 registry 项仍归此类用法。

必须按公共规则提供本章相关属性；不能因“扩展用法”而漏掉。定义并注册新能力链接自定义页，深入缓存、编译分支和执行阶段链接实现原理。
