# 安装

通过如下指令安装 `@retikz/core` 包(也可以使用 pnpm/yarn):

```bash
npm install @retikz/core
```

`@retikz/core` 有一个前置依赖：`react`，请确保你的项目中已经安装了对应的包:

```bash
npm install react react-dom
```

<Alert type="warning">目前，@retikz/core 基于 react19 开发，在正式版发布前会兼容 react18。更低的版本暂时不考虑支持。</Alert>
