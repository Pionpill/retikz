---
title: Contour
description: An arbitrary closed vertex ring as a node shape — give it a ring of points and get a connectable, roundable, auto-centered contour node.
---

`contour` is a built-in Node shape that takes an **arbitrary closed vertex ring**: give it a ring of points (`params.points`) and the node's boundary is the closed contour through those points. It runs on the same contour engine as `polygon` / `star`; the only difference is where the vertices come from — `polygon` derives a regular polygon from a side count, while `contour` takes the points you give directly. See [Shapes](/kernel/components/shapes) for the full definition.

One thing sets it apart from the other shapes: it is **a Node shape only**, with no sugar drawing component. To draw an arbitrary closed outline, use [`<Path>`](/kernel/components/draw/path) with `line` steps; `contour` solves a different problem — making that arbitrary contour **still a connectable node** (other lines can target it by id and snap to its boundary).

## As a node shape

Set `shape={{ type: 'contour', params: { points } }}` on `<Node>` and the node is bounded by the closed contour through those points.

### Usage

```tsx
import { Node } from '@retikz/react';
```

```tsx
<Node
  shape={{
    type: 'contour',
    params: {
      points: [[-30, 44], [30, 30], [30, -44], [-30, -44]],
      cornerRadius: 8,
    },
  }}
  fill="steelblue"
/>
```

`points` is a ring of **local-frame vertices** (at least 3, implicitly closed, with straight edges). You **do not need to pre-center** them — core normalizes by the points' AABB center and aligns the node's `position` to the geometric center of the contour. The two nodes below use the same shape, but the right one's points are all shifted by a fixed offset: after auto-centering they render identically.

<ComponentPreview name="node-contour" size="sm" />

`cornerRadius` rounds **every vertex** uniformly (user units); omitted / `0` = sharp corners, clamped per corner to the largest non-self-intersecting radius — reusing the same fillet engine as `polygon` / `roundedCorners`.

### Connectable

The key value of `contour` is that it is **still a Node**: another `<Path>` / `<Draw>` can target it by id, and the endpoint snaps via `boundaryPoint` to the exact intersection on the contour edge (ray ∩ contour), not the bounding box.

<ComponentPreview name="contour-connect" size="sm" />

<ComponentAlert title="Anchors fall back to the AABB" description="Named / compass anchors (north / center …) have no meaningful direction on a curved block, so contour falls them back to the circumscribing AABB rectangle; directional connection (boundaryPoint) always hits the contour exactly. For an exact edge hit, point a line at it rather than reading a named anchor." />

### API Reference

| `shape` | Boundary | Parameters |
| --- | --- | --- |
| `{ type: 'contour', params }` | The closed contour through `points` (auto-centered on the AABB center) | `points` (≥3) / `cornerRadius?` |

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `points` | `Array<[number, number]>` (≥3) | — | Closed local-frame vertex ring (any origin; core auto-centers on the AABB center), implicitly closed, straight edges |
| `cornerRadius` | `number` (≥0) | — | Uniform per-vertex fillet radius (user units); omitted / `0` = sharp corners, clamped per corner to the largest non-self-intersecting radius |

## How it works

> Optional for everyday use; revisit when you want to understand auto-centering or anchor fallback.

### Auto-centering

`points` may be given at any local origin. Internally core subtracts the points' **AABB center** from every vertex before projecting to world space, so the geometric center of the contour lands on the node's `position`. Whether you draw the points near the origin or far off to one side, the result is the same — the "uncentered" footgun the schema cannot guard against simply does not exist.

### Anchor fallback, exact boundaryPoint

`contour` is a geometry-driven curved block; compass directions (`north` / `east` …) have no stable meaning, so `anchor` provides no named anchors and compile falls back to the circumscribing AABB rectangle. Directional connection goes through `boundaryPoint` — it computes the exact ray ∩ contour intersection for **any** vertex ring (convex / concave / filleted), and is the reliable way to connect to a contour node.

## Related

- [Node](/kernel/components/node/overview): full props for a node
- [Polygon](../polygon): regular polygons (same contour engine, vertices derived from a side count)
- [Path](/kernel/components/draw/path): draw an arbitrary contour line (not a node)
- [Shapes](/kernel/components/shapes): Shapes overview and full definition
