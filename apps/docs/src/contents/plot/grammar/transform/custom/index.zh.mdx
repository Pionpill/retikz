---
title: 自定义变换
description: 自定义变换把执行函数留在运行时：IR 只保存自定义 kind 与 JSON 配置，字段收集与 apply 由 defineTransform 提供。
---

内置变换之外，可以用 `defineTransform` 注册自己的数据处理器。它和坐标系的 `defineCoordinate`、比例尺的 `defineScale` 同一套范式：写进 spec 的仍然只是 JSON operation，例如 `{ kind: 'double-x', field: 'x', as: 'x2' }`；真正的 schema、字段收集和执行函数放在 runtime definition 里，再通过 `<Plot transformDefinitions={[...]}>` 注入。

自定义 operation 仍是纯 JSON、可序列化、可由 AI 生成；`inputFields` / `outputFields` / `apply` 这类函数只在运行时参与编译，不进 IR。

## 双倍 x 变换

下面的 `double-x` 变换把 `field` 字段翻倍写进 `as` 字段。它在 `transformDefinitions` 里注册，spec 用 `<Transform kind="double-x" field="x" as="x2" />` 引用。

<ComponentPreview name="transform-custom" sourceFiles={['transform-custom.data.ts']} />

`x2` 没有写进 `model`，因为它是 `double-x` 产生的派生字段。`outputFields` 会把它登记为 transform 输出，strict model 只校验真正来自输入数据的 `x` 和 `y`。

## 定义契约

| 字段 | 作用 |
| --- | --- |
| `schema` | 校验 `{ kind, ...config }` operation。`kind` 必须是非内置的 `z.literal(...)`，其它字段保持 JSON 可序列化。 |
| `inputFields` | 返回这个 transform 会读取的源字段；这些字段会进入数据模型和字段解析流程。 |
| `outputFields` | 返回这个 transform 会产出的派生字段；被下游 mark 或 transform 消费时，不需要提前写进 `model`。 |
| `apply` | 按顺序执行 transform，返回下一步要消费的行集；必须确定性，避免渲染和 locator 结果不一致。 |

`apply` 的第三个参数是 transform context。保行数 transform 通常 `{ ...row, newField }` 就能保留行级来源；改行数 transform 如果输出行代表一组源行，必须用 `context.groupProvenance(out, members)` 给输出行挂上 `sourceIndices`。如果 transform 生成了没有对应源行的新数据，provenance 会自然降级为只有 `transformedIndex`。

<ComponentAlert title="apply 必须确定性" description="渲染和 locator 命中各会调用一次 apply；若 apply 带随机或依赖外部可变状态，两次结果不一致会导致点击命中错位。需要抖动等随机效果时，用固定 seed 的确定性算法。" />
