---
title: Roadmap
description: "retikz's current and near-future roadmap: v0.2 rc validation, v0.3 renderer/runtime split, v0.4 incremental-rendering candidates, and v1+ domain packages."
---

The roadmap is a **goal list, not a promise** — it adjusts with community feedback and maintainer bandwidth. Concrete releases land by version in the [Changelog](/about/releases/changelog).

## Now

**v0.2.0-rc.1** — release-candidate validation.

| Item | Status |
| --- | --- |
| v0.2 core capabilities | ✅ Complete |
| Public API | ✅ Frozen |
| Installation / docs / release notes | ⏳ In rc validation |

## Short term: v0.2.0 stable

Turn the rc line into the stable release.

| Goal | Content |
| --- | --- |
| API freeze | No new components, props, IR fields, or public exports |
| Installation acceptance | npm package, type exports, examples, and docs links all work |
| Release notes | Changelog, versioning notes, and migration hints stay aligned |

## Mid term: v0.3 renderer / runtime

Split Scene output and runtime responsibilities into clearer layers.

| Goal | Content |
| --- | --- |
| `@retikz/svg` | Scene / resources → SVG |
| `@retikz/canvas` | Scene → Canvas 2D |
| `@retikz/vanilla` | Framework-free mounting and SSR entry points |
| `@retikz/react` | Unified `renderer="svg"` / `renderer="canvas"` entry |
| Hydration | Stable ids + event-binding semantics |
| Plot support | Lowering, coordinate scopes, layers, locator / anchor interfaces |

## Long term

| Topic | Where |
| --- | --- |
| v0.4 | Incremental rendering, Progressive IR, AI step updates, view-layer experiments |
| v1+ | Domain packages such as `plot` / `graph` / `flow`, plus the broader ecosystem |

## Not on the roadmap

To keep scope honest, things we are **not** doing:

- Built-in LaTeX math layout
- Visual editor
- Full TikZ library-loading semantics
- Complete calc expression language
- Complex decorations such as `snake` / `coil`
- Canvas implemented by piping through SVG
