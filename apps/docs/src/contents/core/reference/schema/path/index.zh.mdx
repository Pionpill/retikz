---
title: Path
description: 笔画外壳、step 动作序列与 step 目标点
---

`path` 是一条笔画的外壳容器；它的 `children` 字段是一个 `Step` 动作有序数组，渲染器顺序处理它们产出 SVG path 命令。

## Path

<ZodSchema name="PathSchema" descriptions={{
  type: '判别字段，标记此子节点为 path',
  stroke: '描边颜色，任意 CSS color，默认 currentColor',
  strokeWidth: '描边宽度（用户单位），默认 1',
  strokeDasharray: 'SVG stroke-dasharray 虚线模式（如 "4 2"），空 = 实线',
  arrow: '路径箭头方向：省略 / `none` 无箭头；`->` 末端；`<-` 起端；`<->` 双端',
  arrowShape: '箭头形状，默认 `normal`（实心三角）；其它 `open` / `stealth` / `diamond` / `circle`',
  fill: '闭合区域填充色；省略 = 无填充；与 `cycle` step 配合产生封闭形状',
  fillRule: '自交 / 嵌套子路径填充规则；`nonzero`（默认）方向缠绕，`evenodd` 交错',
  lineCap: '描边端点形状（SVG `stroke-linecap`）：`butt` 默认 / `round` / `square`',
  lineJoin: '描边拐角形状（SVG `stroke-linejoin`）：`miter` 默认 / `round` / `bevel`',
  thickness: '语义化描边厚度预设（TikZ `ultra thin` … `ultra thick`）；显式 strokeWidth 优先',
  opacity: '整路径不透明度 0..1，同时作用于描边与填充',
  fillOpacity: '填充不透明度 0..1，只影响闭合区域',
  drawOpacity: '描边不透明度 0..1（TikZ `draw opacity`），只影响描边',
  children: 'step 动作序列；首项通常应是 `move`',
}} />

## Step

`StepSchema` 是判别联合 —— `Path.children` 是 Step 数组。`kind` 字段选变体：

<ZodSchema name="StepSchema" />

### Move

<ZodSchema name="MoveStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`move` —— 不画线，仅移动光标到目标点（SVG path "M"）',
  to: 'move 动作的目标点',
}} />

### Line

<ZodSchema name="LineStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`line` —— 从光标到目标点画一条直线（SVG path "L"）',
  to: '直线段的目标点',
  label: '边标注：附在本段上的文字标签',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

### Fold

<ZodSchema name="FoldStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`step`（折角段，从光标到目标点经一个直角中间点；TikZ `-|`/`|-`）',
  via: '折角方向：`-|` 先横后竖；`|-` 先竖后横',
  to: '折线段的目标点',
  label: '边标注：附在本段上的文字标签（按 position 落在对应支段）',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

### Cycle

<ZodSchema name="CycleStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`cycle` —— 闭合回最近一次 move 的目标点（TikZ `cycle` / SVG path "Z"）',
}} />

### Curve

<ZodSchema name="CurveStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`curve` —— 二次贝塞尔，从光标到目标点带 1 个控制点（SVG path "Q"）',
  to: '曲线终点',
  control: '二次贝塞尔单控制点',
  label: '边标注：附在本段上的文字标签',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

### Cubic

<ZodSchema name="CubicStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`cubic` —— 三次贝塞尔，从光标到目标点带 2 个控制点（SVG path "C"）',
  to: '曲线终点',
  control1: '第一个控制点（影响起点切线）',
  control2: '第二个控制点（影响终点切线）',
  label: '边标注：附在本段上的文字标签',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

### Bend

<ZodSchema name="BendStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`bend` —— 简记弧形：按方向 + 角度生成贝塞尔近似（TikZ `to[bend left=N]` / `to[bend right=N]`）',
  to: '弧形段的目标点',
  bendDirection: '弧形偏转方向（相对 from→to 弦的视觉左 / 右）',
  bendAngle: '弯曲角度（°），默认 30',
  label: '边标注：附在本段上的文字标签',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

### Arc

<ZodSchema name="ArcStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`arc` —— 以光标为圆心，按起末角度 + 半径绘制弧段（TikZ `arc[start angle=…, end angle=…, radius=…]`）；笔停在弧端点而非圆心',
  startAngle: '弧起始角（°），逆时针从 +x 轴起（数学约定；SVG y-down 视觉为顺时针）',
  endAngle: '弧终止角（°）；扫向由 startAngle 与 endAngle 大小关系隐式决定',
  radius: '弧半径（用户单位）',
  label: '边标注：附在本段上的文字标签',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

### CirclePath

<ZodSchema name="CirclePathStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`circlePath` —— 以光标为圆心、给定半径的整圆（TikZ `circle[radius=…]`），笔画结束后回到圆心',
  radius: '圆半径（用户单位）',
  label: '边标注：附在本段上的文字标签',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

### EllipsePath

<ZodSchema name="EllipsePathStepSchema" descriptions={{
  type: '判别字段，标记本节点为 path step',
  kind: '动作种类：`ellipsePath` —— 以光标为圆心、给定 x/y 半径的整椭圆（TikZ `ellipse[x radius=…, y radius=…]`），笔画结束后回到中心',
  radiusX: 'x 轴半径（半长轴或半短轴）',
  radiusY: 'y 轴半径（半长轴或半短轴）',
  label: '边标注：附在本段上的文字标签',
  'label.text': '标签文字内容（单行；多行需 \\n）',
  'label.position': '沿段位置：`midway` / `near-start` / `near-end`，默认 midway',
  'label.side': '相对段方向的偏移：above/below/left/right 沿法线偏移；sloped 沿切线旋转（无法线偏移），默认 above',
}} />

## Target

`TargetSchema` 是四种形态的 union —— 笛卡尔 / 极坐标 / 节点 id / 相对偏移：

<ZodSchema name="TargetSchema" />

### Rel

<ZodSchema name="RelTargetSchema" descriptions={{
  rel: '相对偏移 (dx, dy)；不更新光标位置（对应 TikZ `(+x, +y)` 语法）',
}} />

### RelAccumulate

<ZodSchema name="RelAccumulateTargetSchema" descriptions={{
  relAccumulate: '累积相对偏移 (dx, dy)；**会**更新光标位置（对应 TikZ `(++x, ++y)` 语法）',
}} />
