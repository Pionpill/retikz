---
title: Node
description: 矩形节点 + 可选文本标签——retikz 最常用的图元，path / draw 通过 id 引用它。
---

`<Node>` 是有命名、有位置的最基本图元。其它路径用它的 `id` 引用，连线时自动贴边界。

## 用法

```tsx
import { Node } from '@retikz/react';
```

```tsx
<Node id="a" position={[0, 0]}>
  Label
</Node>
```

## 例子

### 基础

<ComponentPreview name="node-basic" />

### 样式

`fill` / `stroke` / `strokeWidth` / `padding` / `fontSize` 控制单个节点观感：

<ComponentPreview name="node-styled" />

### 旋转

`rotate` 绕节点中心旋转；连线会跟着新边界点贴：

<ComponentPreview name="node-rotated" />

## API 参考

| 属性 | 类型 | 默认值 | 描述 |
| --- | --- | --- | --- |
| `id` | `string` | — | 唯一标识；被 `<Path>` / `<Draw>` 用来引用 |
| `position` | `[x, y] \| PolarPosition` | — | 节点中心位置（笛卡尔或极坐标） |
| `children` / `text` | `string` | — | 节点内可见文字；二选一，`text` 优先 |
| `rotate` | `number` | `0` | 绕中心旋转角度（度，正值顺时针） |
| `fill` | `string` | — | 背景色（任意 CSS 颜色） |
| `stroke` | `string` | `currentColor` | 边框色 |
| `strokeWidth` | `number` | `1` | 边框宽度（user units） |
| `padding` | `number` | — | 文字到 border 的内边距 |
| `margin` | `number` | `0` | border 到 path 附着点的外边距；**不影响 border 位置**，影响连线起止 |
| `fontSize` | `number` | — | 文字字号（user units） |

## margin 与 padding 的区别

| | 影响什么 | 视觉表现 |
| --- | --- | --- |
| `padding` | 文字到 border 的距离 | border 变大 |
| `margin` | border 到 path 端点的距离 | border 不变；连线提前 margin 停 |

`margin` 用来在多节点流程图里给连线"留呼吸空间"——不让线贴死 border。
