---
title: Overview
description: Meta-information beyond the docs body — release history, versioning policy, roadmap. The docs teach how to use; the reference tells when to upgrade, where to, and what to expect.
---

The retikz reference is the meta-layer around the docs: how versions are scheduled, what shipped, and what is planned.

## What's inside

| Page | Content |
| --- | --- |
| [Changelog](/reference/releases/changelog) | Per-version release notes |
| [Versioning](/reference/releases/versioning) | SemVer + pre-release channels + deprecation |
| [Roadmap](/reference/releases/roadmap) | Now / short / mid / long term goals |

## Current versions

| Package | Version | Notes |
| --- | --- | --- |
| `@retikz/core` | `0.1.0-alpha.0` | IR + scene compiler, zero React |
| `@retikz/react` | `0.1.0-alpha.0` | React adapter |

APIs may still shift during alpha; SemVer guarantees take effect once `0.1.0` ships stable. See [Versioning](/reference/releases/versioning).

## Feedback

retikz is in v0.1 alpha — **feedback on the API and IR schema is wanted**, especially naming, prop shape, and IR field semantics, all of which benefit from converging early. The docs site itself is part of the alpha — flag gaps or inconsistencies as you go.
