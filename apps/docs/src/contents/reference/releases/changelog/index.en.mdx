---
title: Changelog
description: retikz release history grouped by MINOR version, newest first. Date on the left, affected packages and changes on the right.
---

<Update label="May 2026" tags={["@retikz/core", "@retikz/react", "docs"]}>

## v0.1.0-alpha.2

Node visual P1 lands in full: multi-line text, font object, axis-specific seps, color / opacity / stroke style / size constraints / scaling. Backed by 3 ADRs and +47 new test cases.

> **BREAKING**: `<Node fontSize={n}>` is now `<Node font={{ size: n }}>`; the IR `NodeSchema.fontSize` field is removed. No deprecation window during alpha (see ADR-0001).

### `@retikz/core`

- **Node font as nested object**: `font: { family?, size?, weight?, style? }` replaces the `fontSize` scalar — ADR-0001
- **Node multi-line text**: `text: string | Array<string | LineSpec>` with per-line `fill` / `opacity` / `font` overrides; complementary `align` (left / center / right) + `lineHeight` (user units, defaults to `font.size × 1.2`) — ADR-0002
- **Node axis-specific seps**: `innerXSep` / `innerYSep` / `outerSep`, mirroring TikZ `inner xsep` / `inner ysep` / `outer sep`; `padding` / `margin` are kept as symmetric aliases (axis-specific fields take precedence) — ADR-0003
- **Node colors and opacity**: `textColor` / `opacity` (whole-node) / `fillOpacity` / `drawOpacity`
- **Node stroke styles**: `dashed` (`4 2` preset) / `dotted` (`1 2` preset) / `dashArray` (explicit SVG `stroke-dasharray`); priority `dashArray` > `dashed` > `dotted`
- **Node size constraints**: `roundedCorners` (rectangle corner radius) / `minimumWidth` / `minimumHeight` + symmetric alias `minimumSize`
- **Node scaling**: `scale` / `xScale` / `yScale`; uniform scale takes the font with it, non-uniform falls back to `min(sx, sy)` to preserve glyph shape
- **TextPrim upgrade**: `lines: Array<TextLine>` replaces `content: string`; per-line `fontSize` / `fontFamily` / `fontWeight` / `fontStyle` / `fill` / `opacity` overrides

### `@retikz/react`

- New sugar `<Text>` component for per-line overrides inside `<Node>`: `<Node><Text fill="red" font={{ weight: 'bold' }}>L1</Text>L2</Node>`
- `<Node>` children accept multi-line text directly: `\n` strings, arrays, and mixed `<Text>` children all work
- Builder / unbuilder forward every new field; derived types `IRFont` / `IRLineSpec` / `NodeTextAlign` are re-exported from core
- Renderer emits `<text>` with one `<tspan>` per line, deriving the first-line `dy` from the block `baseline` for vertical alignment

### `docs`

- Node docs split into `overview` / `text` sub-pages: overview covers shape / font / multi-line / color / stroke / size / scale / sep in 8 sections; text sub-page dives into `<Text>` per-line overrides
- New demos: `node-font` / `node-multiline` / `node-multiline-shapes` / `node-styled-lines` / `node-sep` / `node-color-opacity` / `node-stroke-style` / `node-size-constraints`
- ComponentPreview on mobile: drag + touch (`<lg` enabled by default), reduced height / padding, Maximize entry hidden
- `notes/adr/0001`–`0003` (font object / multi-line text / inner sep) decisions captured

## v0.1.0-alpha.1

P0 + P1 essentials for flow charts / UML / state machines land in one shot: 4 node shapes, path folds / closing / multi-end arrows / region fills, explicit anchor references. Backed by 4 ADRs and ~180 test cases.

### `@retikz/core`

- **Step kind grows to 4**: `'move'` / `'line'` / `'step'` (fold, TikZ `(A) -| (B)` style) / `'cycle'` (close to start) — ADR-0001
- **Path-level visuals**: `arrow` (4 directions) + `arrowShape` (7 shapes: normal / open / stealth / diamond / openDiamond / circle / openCircle, hollow shapes auto-shrink the path) + `fill` / `fillRule` — ADR-0002
- **Node shape polymorphism**: `rectangle` / `circle` / `ellipse` / `diamond` with multimorphic `boundaryPoint`; the outer shape circumscribes "text + padding" — ADR-0003
- **Target string syntax**: `'A'` (auto-clip) / `'A.<anchor>'` (named anchor) / `'A.<deg>'` (angle border) — ADR-0004
- **Sugar way DSL upgrade**: `DrawWay` keyword constants (cycle / hv / vh) + infix fold operators `'-|'` / `'|-'` + cycle keyword
- **Path clip algorithm refactor**: per-segment clip from node centers — avoids lines passing through intermediate nodes (matches TikZ semantics)
- **New type utility**: `ValueOf<T extends object> = T[keyof T]`, standardizes `as const` object + derived-union enum pattern
- **New geometry modules**: `circle` / `ellipse` / `diamond`, each with `boundaryPoint` / `anchor` / `contains`

### `@retikz/react`

- Path / Draw / Step / Node forward all new IR fields; `DrawWay` is fully interchangeable with IR string literals
- Tikz container injects SVG `<defs><marker>` on demand (one per arrow shape used, `useId()` keeps ids unique across multiple instances)
- Renderer adds `'ellipse'` primitive case (shared by circle / ellipse nodes)
- Render pipeline now supports GroupPrim to wrap multi-sub-path paths — for multi-node arrowed paths, markers attach only to the very first / last segment

### `docs`

- Node page adds shape polymorphism section + a 4-shape-around-hub demo
- Path / Draw overview pages: independent sections + demos for fold / cycle / arrow / arrowShape / fill
- Step page: all 4 kinds overview + per-kind demos + styling / fold / cycle sections
- Anchors concept page rewritten: three-form Target string syntax table + 4-shape anchor cross-reference
- 4 new ADRs (`notes/adr/0001`–`0004`); AGENTS.md adds "const + ValueOf for enum-like unions" rule

## v0.1.0-alpha.0

First npm release. retikz v0.1 redesign baseline with IR at the center — the foundation for upcoming alpha iterations.

### `@retikz/core`

- **IR + scene compiler**: framework-agnostic intermediate representation and compiler, zero React and zero DOM
- **Persistence**: internal React state → serializable IR JSON, cross-platform and directly readable / writable by LLMs

### `@retikz/react`

- **Architecture split**: moved from a single React component library to a thin React adapter; core is decoupled from React
- **Kernel + Sugar JSX layers**: Kernel maps 1:1 to IR nodes; Sugar compiles down to Kernel
- peerDep React 18+

### `docs`

- v0.1 docs site: bilingual (zh / en) + co-located demos + live IR view

</Update>

<Update label="Apr 2025" tags={["retikz"]}>

## v0.0

First public series, iterating from `rc.0` through `rc.3`. This series shipped a single React package — no IR, no cross-framework architecture.

**Added**
- Arrow type: `Circle`
- Vertical way types: `|-|`, `-|-`

**Changed**
- React 18 compatibility

**Fixed**
- Fixed converted way bug
- Fixed `ScopeContext` propagation
- Various TypeScript errors and warnings
- Non-functional prop forwarding regressions

**Removed**
- `react/jsx-runtime` dependency dropped from build output

</Update>
