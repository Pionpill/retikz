---
title: Axes
description: Preserve origin, direction, arrows, ticks, and text for number lines or reference axes as JSON-safe Axes IR that lowers to Core Paths and Nodes.
---

`<Axes>` is a Standard Tier 2 component for static mathematical illustrations. It preserves a number line or Cartesian reference axes as one persistable configuration, then deterministically lowers that configuration to Core `Path` and `Node` primitives.

## Usage

```tsx
import { Layout } from '@retikz/react';
import { Axes } from '@retikz/standard-react';
```

```tsx
<Layout width={280} height={170}>
  <Axes origin={[140, 90]} extent={{ x: 110, y: 60 }} />
</Layout>
```

The React entry contributes `AxesDefinition` locally to the current `Layout`. For persistence or non-React use, create complete IR through `@retikz/standard` and explicitly provide the definition to Core compile options.

| Scenario         | Public entry point                                                       |
| ---------------- | ------------------------------------------------------------------------ |
| React            | `Axes` from `@retikz/standard-react`                                     |
| Vanilla          | `axes` and `AxesVanillaAdapter` from `@retikz/standard-vanilla`          |
| IR / persistence | `AxesSchema`, `createAxes`, and `AxesDefinition` from `@retikz/standard` |

See [Get Started](/standard/get-start) for complete Vanilla mounting and persistence flows. Use [Plot axis](/viz/plot/guide/axis) instead when the task includes a data domain, scale, automatic ticks, formatters, or label collision handling.

## Examples

### Basic axes

`extent` is the only required semantic field. It determines how far each axis extends from the origin in its positive and negative directions:

- A number shorthand uses the same length on both sides, such as `x: 110`
- `{ negative, positive }` configures each side separately and allows either side to be `0`
- Positive x points visually right and positive y points visually up; this direction mapping only affects primitives generated by Axes

The canonical example below combines an asymmetric y extent, ticks on both x half-axes, positive-only y ticks, static numeric text, and a lightweight grid.

<ComponentPreview files="axes-basic" size="sm" />

Optional configuration answers four separate questions: `x` / `y` control each line, its ticks, and its label; `grid` adds lightweight regular lines; `originLabel` emits one text item at the intersection; and `origin` moves the complete reference axes.

### Common variants

These four side-by-side cases compare positive-only half-axes, a two-ended number line whose ticks extend upward only, asymmetric extents, and several ticks that remain after hiding the baseline. Use `x: false` or `y: false` for a single axis; `line: false` hides only that axis baseline.

<ComponentPreview files="axes-variants" size="sm" />

Axis labels, tick labels, and the origin label all accept Core `IRTextBlock`. Plain strings work directly. Formula strings such as `'$x_1$'` or `'$\\pi/2$'` require the host to inject [TeX support](/kernel/packages/tex) through `lowerTex`.

### Controls

The panel covers the Axes origin, extents, grid, both axis lines, arrows, ticks, tick labels, axis labels, and the origin label, including an independent endpoint gap for each axis. Array fields use one representative negative/positive tick pair, while open arrow names use Core built-in shape options; the camera remains fixed.

<ComponentPreview
  files="axes-playground"
  size="md"
  caption="Adjust per-axis tick distribution and extension side, arrow appearance, text styles, or the grid lattice offset and see the same Axes IR lower to Core primitives."
/>

## How it works

<ComponentAlert
  type="tip"
  title="Optional for everyday use"
  description="You only need this lowering model when reading Axes IR directly, aligning other Core primitives, or debugging compile diagnostics."
/>

Axes IR never becomes a renderer-specific primitive. Once the current figure loads `AxesDefinition`, lowering normalizes positive and negative extents, maps the y direction, enumerates grid lines and ticks, and assembles ordinary Core IR in a fixed order.

The figure shows only the transformation owned by Axes. Its responsibility ends once the output reaches `Core IR[]`.

<ComponentPreview files="axes-lowering" hideCode size="xs" />

The y-axis path always runs from its negative endpoint to its positive endpoint. A positive value `v` maps to screen position `originY - v`, so the default positive arrow consistently points up. This does not create a new global coordinate system: adjacent `<Node position>` and `<Step to>` values still use Core y-down coordinates.

Axes `grid` only emits ordinary lines clipped to the same `origin` and `extent`; `offset` shifts the lattice origin in mathematical x/y directions, while dashes and other appearance fields reuse Path style. It does not depend on `GridDefinition`. Use standalone [Grid](/standard/composite/grid) for major lines, borders, and direction visibility.

A handwritten `IRAxes` without a loaded definition is never silently interpreted: Core emits `COMPOSITE_NOT_REGISTERED` and skips it. Renderers only receive the lowered `Path` and text `Node` primitives, so they never need to recognize Axes.

<SourceLinks
  sources={[
    {
      label: 'Axes schema and public contract',
      path: 'packages/library/standard/src/composites/presentation/axes/schema.ts',
    },
    {
      label: 'Axes direction mapping and lowering',
      path: 'packages/library/standard/src/composites/presentation/axes/lower.ts',
    },
  ]}
/>

## API Reference

`AxesProps` is a public alias of `AxesInput`, and the Vanilla adapter accepts that same `AxesInput`. `createAxes` validates it and returns `IRAxes` with schema defaults applied. `Position` in the tables below comes from `@retikz/math`.

### `AxesInput`

| Prop           | Type                                                                                                                                                                                                                                                                                                 | Default                                                                                              | Description                                                                                                                                                                     |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `origin?`      | `Position`                                                                                                                                                                                                                                                                                           | `[0, 0]`                                                                                             | Shared screen-space origin for axes, grid, ticks, and text                                                                                                                      |
| `extent`       | `AxesInput['extent']`                                                                                                                                                                                                                                                                                | —                                                                                                    | Positive and negative drawing lengths for both axes                                                                                                                             |
| `grid?`        | `AxesInput['grid']`                                                                                                                                                                                                                                                                                  | —                                                                                                    | Lightweight regular grid clipped to the same extents                                                                                                                            |
| `x?`           | `AxesInput['x']`<br />`false` \| `Exclude<AxesInput['x'], false \| undefined>`<br />- `line?:` `Exclude<AxesInput['x'], false \| undefined>['line']`<br />- `ticks?:` `Exclude<AxesInput['x'], false \| undefined>['ticks']`<br />- `label?:` `Exclude<AxesInput['x'], false \| undefined>['label']` | `{ line: { arrows: 'positive' }, label: 'x' }`<br />- `{ arrows: 'positive' }`<br />- —<br />- `'x'` | Horizontal axis; `false` removes all its artifacts<br />- Baseline and endpoint arrows<br />- Ticks and optional static text<br />- Axis-name text or hiding configuration      |
| `y?`           | `AxesInput['y']`<br />`false` \| `Exclude<AxesInput['y'], false \| undefined>`<br />- `line?:` `Exclude<AxesInput['y'], false \| undefined>['line']`<br />- `ticks?:` `Exclude<AxesInput['y'], false \| undefined>['ticks']`<br />- `label?:` `Exclude<AxesInput['y'], false \| undefined>['label']` | `{ line: { arrows: 'positive' }, label: 'y' }`<br />- `{ arrows: 'positive' }`<br />- —<br />- `'y'` | Vertical axis; positive values point visually up<br />- Baseline and endpoint arrows<br />- Ticks and optional static text<br />- Axis-name text or hiding configuration        |
| `originLabel?` | `AxesInput['originLabel']`<br />`false` \| `IRTextBlock` \| `Exclude<AxesInput['originLabel'], false \| IRTextBlock \| undefined>`<br />- `text:` `IRTextBlock`<br />- `offset?:` `number`<br />- `style?:` `IRLabelVisualStyle`                                                                     | `false`<br />- —<br />- `10`<br />- —                                                                | One static label placed down-left from the origin<br />- Static origin-label text<br />- Nonnegative down-left offset of its center<br />- Core text style for the origin label |

### `AxesInput['extent']`

| Prop | Type                                                                                                                                            | Default             | Description                                                                                                                                          |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `x`  | `AxesInput['extent']['x']`<br />`number` \| `Exclude<AxesInput['extent']['x'], number>`<br />- `negative:` `number`<br />- `positive:` `number` | —<br />- —<br />- — | A number is symmetric; an object sets left/right lengths, with at least one > 0<br />- Drawing length to the left<br />- Drawing length to the right |
| `y`  | `AxesInput['extent']['y']`<br />`number` \| `Exclude<AxesInput['extent']['y'], number>`<br />- `negative:` `number`<br />- `positive:` `number` | —<br />- —<br />- — | A number is symmetric; an object sets down/up lengths, with at least one > 0<br />- Drawing length downward<br />- Drawing length upward             |

### `AxesInput['grid']`

The `offset` default applies only when the `grid` object is provided.

| Prop          | Type                                                                                                                                                                              | Default             | Description                                                                                                                                                                                |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `spacing`     | `Exclude<AxesInput['grid'], undefined>['spacing']`<br />`number` \| `Exclude<Exclude<AxesInput['grid'], undefined>['spacing'], number>`<br />- `x:` `number`<br />- `y:` `number` | —<br />- —<br />- — | Positive spacing; a number applies one interval to both directions, while an object configures each axis<br />- x spacing between vertical lines<br />- y spacing between horizontal lines |
| `offset?`     | `Position`                                                                                                                                                                        | `[0, 0]`            | Axis-local mathematical offset for lattice alignment                                                                                                                                       |
| `style?`      | `IRStandardPathStrokeStyle`                                                                                                                                                       | —                   | Shared Path stroke fields for both grid directions                                                                                                                                         |
| `vertical?`   | `IRStandardPathStrokeStyle`                                                                                                                                                       | —                   | Stroke fields overriding the shared vertical-line style                                                                                                                                    |
| `horizontal?` | `IRStandardPathStrokeStyle`                                                                                                                                                       | —                   | Stroke fields overriding the shared horizontal-line style                                                                                                                                  |

### `AxesInput['x']` / `AxesInput['y']`

| Prop     | Type                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Default                                                                       | Description                                                                                                                                                                                                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `line?`  | `Exclude<AxesInput['x'], false \| undefined>['line']`<br />`false` \| `Exclude<Exclude<AxesInput['x'], false \| undefined>['line'], false \| undefined>`<br />- `arrows?:` <ApiValues name="AxesArrowMode" /><br />- `arrowDetail?:` `IRArrowDetail`<br />- `style?:` `IRStandardPathStrokeStyle`                                                                                                                                                                                                                                                   | `{ arrows: 'positive' }`<br />- `'positive'`<br />- —<br />- —                | Baseline and endpoint arrows; `false` preserves ticks and text<br />- Arrow on the negative end, positive end, both ends, or neither<br />- Core arrow appearance; `start` / `end` map to negative/positive ends<br />- Path stroke fields for this baseline                                                             |
| `ticks?` | `Exclude<AxesInput['x'], false \| undefined>['ticks']`<br />`false` \| `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>`<br />- `source:` `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source']`<br />- `side?:` <ApiValues name="AxesTickSide" /><br />- `endpointGap?:` `number`<br />- `length?:` `number`<br />- `style?:` `IRStandardPathStrokeStyle`<br />- `labels?:` `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels']` | —<br />- —<br />- `'both'`<br />- `6`<br />- `6`<br />- —<br />- —            | Ticks and optional static text for this axis<br />- Regular-spacing or explicit-value source<br />- Perpendicular extension side<br />- Endpoint exclusion distance<br />- Total tick-segment length distributed across the selected side<br />- Path stroke fields for tick segments<br />- Optional static tick labels |
| `label?` | `Exclude<AxesInput['x'], false \| undefined>['label']`<br />`false` \| `IRTextBlock` \| `Exclude<Exclude<AxesInput['x'], false \| undefined>['label'], false \| IRTextBlock \| undefined>`<br />- `text:` `IRTextBlock`<br />- `end?:` <ApiValues name="AxesLabelEnd" /><br />- `offset?:` `number`<br />- `style?:` `IRLabelVisualStyle`                                                                                                                                                                                                           | `'x'` for x<br />`'y'` for y<br />- —<br />- `'positive'`<br />- `8`<br />- — | Axis-name shorthand, full text object, or `false` to hide it<br />- Static axis-label text<br />- Places the text beyond the positive or negative end<br />- Nonnegative distance from endpoint to text center<br />- Core text style for the axis label                                                                 |

### `x.line` / `y.line`

| Prop           | Type                               | Default      | Description                                                          |
| -------------- | ---------------------------------- | ------------ | -------------------------------------------------------------------- |
| `arrows?`      | <ApiValues name="AxesArrowMode" /> | `'positive'` | Arrow on the negative end, positive end, both ends, or neither       |
| `arrowDetail?` | `IRArrowDetail`                    | —            | Core arrow appearance; `start` / `end` map to negative/positive ends |
| `style?`       | `IRStandardPathStrokeStyle`        | —            | Path stroke fields for this baseline                                 |

### `x.ticks` / `y.ticks`

| Prop           | Type                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Default                                               | Description                                                                                                                                                                                                                                                                                                                |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `source`       | `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source']`<br />`Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'spacing' }>` \| `Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'values' }>`<br />- `kind:` `'spacing'`<br />- `spacing:` `number`<br />- `extent?:` `AxesTickExtentValue`<br />- `kind:` `'values'`<br />- `values:` `Array<number>` | —<br />- —<br />- —<br />- `'both'`<br />- —<br />- — | Regular spacing or explicit values; both branches are listed below<br />- Discriminator for the regular-spacing branch<br />- Positive distance between adjacent ticks<br />- Half-axes on which ticks are emitted<br />- Discriminator for the explicit-values branch<br />- Strictly increasing nonzero signed positions |
| `side?`        | <ApiValues name="AxesTickSide" />                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `'both'`                                              | Perpendicular extension side; x positive is above, y positive is right                                                                                                                                                                                                                                                     |
| `endpointGap?` | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `6`                                                   | Omits ticks closer than this to either endpoint; `0` disables filtering                                                                                                                                                                                                                                                    |
| `length?`      | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `6`                                                   | Total tick-segment length distributed across the selected side                                                                                                                                                                                                                                                             |
| `style?`       | `IRStandardPathStrokeStyle`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                                                     | Path stroke fields for tick segments                                                                                                                                                                                                                                                                                       |
| `labels?`      | `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels']`<br />`false` \| `Exclude<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels'], false \| undefined>`<br />- `entries:` `Exclude<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels'], false \| undefined>['entries']`<br />- `offset?:` `number`<br />- `style?:` `IRLabelVisualStyle`                                                     | omitted (no labels)<br />- —<br />- `4`<br />- —      | Labels emitted ticks only; formatter functions are not accepted<br />- Non-empty list of static labels<br />- Nonnegative distance from tick endpoint to text center<br />- Shared Core text style for this label set                                                                                                      |

### `ticks.source`

Both union branches are JSON objects. The public closed set for `kind` is <ApiValues name="AxesTickSourceKind" />.

| Branch          | Type                                                                                                                                                                                                                      | Default                                            | Description                                                                                                                                                                                                   |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Regular spacing | `Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'spacing' }>`<br />- `kind:` `'spacing'`<br />- `spacing:` `number`<br />- `extent?:` `AxesTickExtentValue` | `extent: 'both'`<br />- —<br />- —<br />- `'both'` | Enumerates positive spacing from the origin on selected sides<br />- Discriminator for the regular-spacing branch<br />- Positive distance between adjacent ticks<br />- Half-axes on which ticks are emitted |
| Explicit values | `Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'values' }>`<br />- `kind:` `'values'`<br />- `values:` `Array<number>`                                     | —<br />- —<br />- —                                | Strictly increasing nonzero signed values inside the extent<br />- Discriminator for the explicit-values branch<br />- Nonzero signed positions inside this axis extent                                       |

The spacing branch takes its `extent` values from <ApiValues name="AxesTickExtent" />.

### `ticks.labels`

The `offset` default is applied only when a labels object is provided. Omitting `labels` emits no text.

| Prop      | Type                                                                                                                                                                                                          | Default             | Description                                                                                                                  |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `entries` | `Exclude<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels'], false \| undefined>['entries']`<br />- `[number].value:` `number`<br />- `[number].text:` `IRTextBlock` | —<br />- —<br />- — | Non-empty static list; every entry must match an emitted tick<br />- Tick value to annotate<br />- Corresponding static text |
| `offset?` | `number`                                                                                                                                                                                                      | `4`                 | Nonnegative distance from tick endpoint to text center                                                                       |
| `style?`  | `IRLabelVisualStyle`                                                                                                                                                                                          | —                   | Shared Core text style for this label set                                                                                    |

### `x.label` / `y.label`

| Prop      | Type                              | Default      | Description                                         |
| --------- | --------------------------------- | ------------ | --------------------------------------------------- |
| `text`    | `IRTextBlock`                     | —            | Static axis-label text                              |
| `end?`    | <ApiValues name="AxesLabelEnd" /> | `'positive'` | Places the text beyond the positive or negative end |
| `offset?` | `number`                          | `8`          | Nonnegative distance from endpoint to text center   |
| `style?`  | `IRLabelVisualStyle`              | —            | Core text style for the axis label                  |

### `originLabel`

The `offset` default is applied only when an `originLabel` object is provided.

| Prop      | Type                 | Default | Description                                |
| --------- | -------------------- | ------- | ------------------------------------------ |
| `text`    | `IRTextBlock`        | —       | Static origin-label text                   |
| `offset?` | `number`             | `10`    | Nonnegative down-left offset of its center |
| `style?`  | `IRLabelVisualStyle` | —       | Core text style for the origin label       |

## Related

- [Grid](/standard/composite/grid): major lines, borders, and a fuller regular-grid contract
- [Tier 2 Composites](/kernel/concepts/design/composite): how Core registers and lowers high-level IR
- [Path](/kernel/components/draw/path): the axis, grid-line, and tick primitives emitted by Axes
- [TeX](/kernel/packages/tex): LaTeX formulas in axis and tick labels
- [Plot axis](/viz/plot/guide/axis): data scales, automatic ticks, formatters, and guide layout
