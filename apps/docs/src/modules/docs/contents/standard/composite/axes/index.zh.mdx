---
title: 坐标轴
description: 用 JSON-safe Axes 保留数轴与坐标参考轴的原点、方向、箭头、刻度和文字意图，并下沉为 Core Path 与 Node。
---

`<Axes>` 是面向静态数学插图的 Standard Tier 2 组件。它把数轴或笛卡尔参考轴保留为一份可持久化配置，再确定性下沉为 Core `Path` 与 `Node`。

## 用法

```tsx
import { Layout } from '@retikz/react';
import { Axes } from '@retikz/standard-react';
```

```tsx
<Layout width={280} height={170}>
  <Axes origin={[140, 90]} extent={{ x: 110, y: 60 }} />
</Layout>
```

React 入口会在当前 `Layout` 局部贡献 `AxesDefinition`。持久化或非 React 场景可从 `@retikz/standard` 创建完整 IR，并在 Core compile options 中显式传入 definition。

| 场景        | 公开入口                                                           |
| ----------- | ------------------------------------------------------------------ |
| React       | `Axes`（`@retikz/standard-react`）                                 |
| Vanilla     | `axes`、`AxesVanillaAdapter`（`@retikz/standard-vanilla`）         |
| IR / 持久化 | `AxesSchema`、`createAxes`、`AxesDefinition`（`@retikz/standard`） |

完整的 Vanilla 挂载与持久化加载流程统一见[快速开始](/standard/get-start)。当需求包含数据 domain、scale、自动 tick、formatter 或标签避让时，应改用 [Plot axis](/viz/plot/guide/axis)。

## 例子

### 基础坐标轴

`extent` 是唯一必填的语义字段，分别决定 x/y 轴从原点向正、负方向延伸多远：

- 数字 shorthand 表示两侧等长，例如 `x: 110`
- `{ negative, positive }` 分别设置两侧长度，并允许其中一侧为 `0`
- x 正值视觉向右，y 正值视觉向上；这套方向换算只作用于 Axes 自己生成的图元

下面的 canonical 示例使用非对称 y 范围、双向 x 刻度、仅正向 y 刻度、静态数字文字和轻量网格。

<ComponentPreview files="axes-basic" size="sm" />

其余配置分别回答四类问题：`x` / `y` 决定每条轴的线、刻度和轴名，`grid` 增加轻量规则格线，`originLabel` 只在交点生成一次文字，`origin` 则移动整组坐标参考。

### 常见变体

四个并列场景展示仅正半轴、刻度只向上伸出的双向数轴、非对称范围，以及隐藏轴线但保留多组刻度和文字。单轴模式使用 `x: false` 或 `y: false`，而 `line: false` 只隐藏该轴的基线。

<ComponentPreview files="axes-variants" size="sm" />

轴名、刻度文字和原点文字都接受 Core `IRTextBlock`。普通字符串可直接使用；`'$x_1$'`、`'$\\pi/2$'` 等公式写法需要宿主通过 `lowerTex` 注入 [TeX 能力](/kernel/packages/tex)。

### 控制面板

面板覆盖 Axes 的坐标原点、范围、网格、两条轴线、箭头、刻度、刻度文字、轴名和原点文字，并可分别调整 x/y 刻度的端点留白。数组字段使用一对代表性正负刻度，开放箭头名使用 Core 内置形状选项；预览视野保持固定。

<ComponentPreview
  files="axes-playground"
  size="md"
  caption="分别调整 x/y 刻度分布与伸出侧、箭头外观、文字样式或网格起算偏移，观察同一 Axes IR 如何下沉为 Core 图元。"
/>

## 技术原理

<ComponentAlert
  type="tip"
  title="上手阶段可跳过本节"
  description="只有在直接读写 Axes IR、对齐其它 Core 图元或排查编译诊断时才需要理解 lowering。"
/>

Axes IR 不会成为 renderer 专有图元。当前图加载 `AxesDefinition` 后，lowering 才会规范化正负范围、换算 y 方向、枚举网格与刻度，并按固定顺序组装普通 Core IR。

下图只展示 Axes 自己负责的转换链。输出到达 `Core IR[]` 后，它的职责就结束了。

<ComponentPreview files="axes-lowering" hideCode size="xs" />

y 轴路径始终从负端走向正端；正值 `v` 映射到屏幕位置 `originY - v`，所以默认正向箭头稳定朝上。这不是新的全局坐标系，旁边的 `<Node position>` 与 `<Step to>` 仍使用 Core 的 y-down 坐标。

Axes 的 `grid` 只生成裁切在相同 `origin`、`extent` 内的普通格线；`offset` 按数学 x/y 方向调整格线起算位置，虚线等外观继续复用 Path style。它不依赖 `GridDefinition`，主线、边框和方向关闭继续使用单独的 [Grid](/standard/composite/grid)。

未加载 definition 的手写 `IRAxes` 不会被静默解释：Core 会发出 `COMPOSITE_NOT_REGISTERED` 并跳过它。renderer 只看到 lowering 后的 `Path` 与文字 `Node`，不需要识别 Axes。

<SourceLinks
  sources={[
    { label: 'Axes schema 与公开契约', path: 'packages/library/standard/src/composites/presentation/axes/schema.ts' },
    { label: 'Axes 方向换算与 lowering', path: 'packages/library/standard/src/composites/presentation/axes/lower.ts' },
  ]}
/>

## API 参考

`AxesProps` 是 `AxesInput` 的公开别名，Vanilla adapter 也接收同一个 `AxesInput`；`createAxes` 校验输入并返回应用了 schema 默认值的 `IRAxes`。下表中的 `Position` 来自 `@retikz/math`。

### `AxesInput`

| 属性           | 类型                                                                                                                                                                                                                                                                                                 | 默认值                                                                                               | 描述                                                                                                               |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `origin?`      | `Position`                                                                                                                                                                                                                                                                                           | `[0, 0]`                                                                                             | 轴、格线、刻度和文字的共同屏幕坐标原点                                                                             |
| `extent`       | `AxesInput['extent']`                                                                                                                                                                                                                                                                                | —                                                                                                    | 两轴正负方向的绘图长度                                                                                             |
| `grid?`        | `AxesInput['grid']`                                                                                                                                                                                                                                                                                  | —                                                                                                    | 裁切在相同范围内的轻量规则网格                                                                                     |
| `x?`           | `AxesInput['x']`<br />`false` \| `Exclude<AxesInput['x'], false \| undefined>`<br />- `line?:` `Exclude<AxesInput['x'], false \| undefined>['line']`<br />- `ticks?:` `Exclude<AxesInput['x'], false \| undefined>['ticks']`<br />- `label?:` `Exclude<AxesInput['x'], false \| undefined>['label']` | `{ line: { arrows: 'positive' }, label: 'x' }`<br />- `{ arrows: 'positive' }`<br />- —<br />- `'x'` | 水平轴；`false` 关闭该轴全部产物<br />- 基线与端点箭头<br />- 刻度与可选静态文字<br />- 轴名文字或隐藏配置         |
| `y?`           | `AxesInput['y']`<br />`false` \| `Exclude<AxesInput['y'], false \| undefined>`<br />- `line?:` `Exclude<AxesInput['y'], false \| undefined>['line']`<br />- `ticks?:` `Exclude<AxesInput['y'], false \| undefined>['ticks']`<br />- `label?:` `Exclude<AxesInput['y'], false \| undefined>['label']` | `{ line: { arrows: 'positive' }, label: 'y' }`<br />- `{ arrows: 'positive' }`<br />- —<br />- `'y'` | 垂直轴；正值视觉向上<br />- 基线与端点箭头<br />- 刻度与可选静态文字<br />- 轴名文字或隐藏配置                     |
| `originLabel?` | `AxesInput['originLabel']`<br />`false` \| `IRTextBlock` \| `Exclude<AxesInput['originLabel'], false \| IRTextBlock \| undefined>`<br />- `text:` `IRTextBlock`<br />- `offset?:` `number`<br />- `style?:` `IRLabelVisualStyle`                                                                     | `false`<br />- —<br />- `10`<br />- —                                                                | 原点左下方的单一静态文字<br />- 静态原点文字<br />- 文字中心向左下方移动的非负距离<br />- 原点文字的 Core 文字样式 |

### `AxesInput['extent']`

| 属性 | 类型                                                                                                                                            | 默认值              | 描述                                                                                                     |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------- |
| `x`  | `AxesInput['extent']['x']`<br />`number` \| `Exclude<AxesInput['extent']['x'], number>`<br />- `negative:` `number`<br />- `positive:` `number` | —<br />- —<br />- — | 数字表示两侧等长；对象分别设置左右长度，且至少一侧必须为正值<br />- 向左的绘图长度<br />- 向右的绘图长度 |
| `y`  | `AxesInput['extent']['y']`<br />`number` \| `Exclude<AxesInput['extent']['y'], number>`<br />- `negative:` `number`<br />- `positive:` `number` | —<br />- —<br />- — | 数字表示两侧等长；对象分别设置下上长度，且至少一侧必须为正值<br />- 向下的绘图长度<br />- 向上的绘图长度 |

### `AxesInput['grid']`

只有提供 `grid` 对象时，`offset` 的默认值才生效。

| 属性          | 类型                                                                                                                                                                              | 默认值              | 描述                                                                                               |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------- |
| `spacing`     | `Exclude<AxesInput['grid'], undefined>['spacing']`<br />`number` \| `Exclude<Exclude<AxesInput['grid'], undefined>['spacing'], number>`<br />- `x:` `number`<br />- `y:` `number` | —<br />- —<br />- — | 正格距；数字对两方向使用同一间距，对象则分轴设置<br />- 竖直格线的 x 间距<br />- 水平格线的 y 间距 |
| `offset?`     | `Position`                                                                                                                                                                        | `[0, 0]`            | 轴局部数学方向的格线起算偏移                                                                       |
| `style?`      | `IRStandardPathStrokeStyle`                                                                                                                                                       | —                   | 两个方向共享的 Path 描边样式                                                                       |
| `vertical?`   | `IRStandardPathStrokeStyle`                                                                                                                                                       | —                   | 覆盖共享样式的竖直格线描边字段                                                                     |
| `horizontal?` | `IRStandardPathStrokeStyle`                                                                                                                                                       | —                   | 覆盖共享样式的水平格线描边字段                                                                     |

### `AxesInput['x']` / `AxesInput['y']`

| 属性     | 类型                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 默认值                                                                          | 描述                                                                                                                                                                                                         |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `line?`  | `Exclude<AxesInput['x'], false \| undefined>['line']`<br />`false` \| `Exclude<Exclude<AxesInput['x'], false \| undefined>['line'], false \| undefined>`<br />- `arrows?:` <ApiValues name="AxesArrowMode" /><br />- `arrowDetail?:` `IRArrowDetail`<br />- `style?:` `IRStandardPathStrokeStyle`                                                                                                                                                                                                                                                   | `{ arrows: 'positive' }`<br />- `'positive'`<br />- —<br />- —                  | 基线及端点箭头；`false` 不影响该轴的刻度与文字<br />- 负端、正端、两端或不生成箭头<br />- Core 箭头外观；`start` / `end` 对应负 / 正端<br />- 本轴基线的 Path 描边样式                                       |
| `ticks?` | `Exclude<AxesInput['x'], false \| undefined>['ticks']`<br />`false` \| `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>`<br />- `source:` `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source']`<br />- `side?:` <ApiValues name="AxesTickSide" /><br />- `endpointGap?:` `number`<br />- `length?:` `number`<br />- `style?:` `IRStandardPathStrokeStyle`<br />- `labels?:` `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels']` | —<br />- —<br />- `'both'`<br />- `6`<br />- `6`<br />- —<br />- —              | 本轴的刻度和可选静态文字<br />- 规则间距或显式位置来源<br />- 刻度相对轴线的伸出侧<br />- 端点附近不生成刻度的距离<br />- 分配在所选侧上的刻度线段总长<br />- 刻度线的 Path 描边样式<br />- 可选静态刻度文字 |
| `label?` | `Exclude<AxesInput['x'], false \| undefined>['label']`<br />`false` \| `IRTextBlock` \| `Exclude<Exclude<AxesInput['x'], false \| undefined>['label'], false \| IRTextBlock \| undefined>`<br />- `text:` `IRTextBlock`<br />- `end?:` <ApiValues name="AxesLabelEnd" /><br />- `offset?:` `number`<br />- `style?:` `IRLabelVisualStyle`                                                                                                                                                                                                           | x 轴为 `'x'`<br />y 轴为 `'y'`<br />- —<br />- `'positive'`<br />- `8`<br />- — | 轴名 shorthand、完整文字对象，或 `false` 隐藏轴名<br />- 静态轴名文字<br />- 文字放置在正端或负端之外<br />- 文字中心到所选端点的非负距离<br />- 轴名的 Core 文字样式                                        |

### `x.line` / `y.line`

| 属性           | 类型                               | 默认值       | 描述                                         |
| -------------- | ---------------------------------- | ------------ | -------------------------------------------- |
| `arrows?`      | <ApiValues name="AxesArrowMode" /> | `'positive'` | 负端、正端、两端或不生成箭头                 |
| `arrowDetail?` | `IRArrowDetail`                    | —            | Core 箭头外观；`start` / `end` 对应负 / 正端 |
| `style?`       | `IRStandardPathStrokeStyle`        | —            | 本轴基线的 Path 描边样式                     |

### `x.ticks` / `y.ticks`

| 属性           | 类型                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 默认值                                                | 描述                                                                                                                                                                         |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `source`       | `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source']`<br />`Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'spacing' }>` \| `Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'values' }>`<br />- `kind:` `'spacing'`<br />- `spacing:` `number`<br />- `extent?:` `AxesTickExtentValue`<br />- `kind:` `'values'`<br />- `values:` `Array<number>` | —<br />- —<br />- —<br />- `'both'`<br />- —<br />- — | 规则间距或显式位置；两个分支见下表<br />- 规则间距分支的判别值<br />- 相邻刻度的正间距<br />- 生成刻度的半轴范围<br />- 显式位置分支的判别值<br />- 严格递增的非零有符号位置 |
| `side?`        | <ApiValues name="AxesTickSide" />                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `'both'`                                              | 刻度相对轴线的伸出侧；x 正侧向上、y 正侧向右                                                                                                                                 |
| `endpointGap?` | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `6`                                                   | 距任一端点小于该值的刻度不生成；`0` 关闭过滤                                                                                                                                 |
| `length?`      | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `6`                                                   | 分配在所选侧上的刻度线段总长                                                                                                                                                 |
| `style?`       | `IRStandardPathStrokeStyle`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                                                     | 刻度线的 Path 描边样式                                                                                                                                                       |
| `labels?`      | `Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels']`<br />`false` \| `Exclude<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels'], false \| undefined>`<br />- `entries:` `Exclude<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels'], false \| undefined>['entries']`<br />- `offset?:` `number`<br />- `style?:` `IRLabelVisualStyle`                                                     | 省略（不生成文字）<br />- —<br />- `4`<br />- —       | 只标注已生成的 tick；不接受 formatter 函数<br />- 非空静态文字列表<br />- 从刻度端点到文字中心的非负间距<br />- 这组刻度文字共享的 Core 文字样式                             |

### `ticks.source`

两个 union 分支都是 JSON 对象，`kind` 的公开闭合集合由 <ApiValues name="AxesTickSourceKind" /> 提供。

| 分支     | 类型                                                                                                                                                                                                                      | 默认值                                             | 描述                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 规则间距 | `Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'spacing' }>`<br />- `kind:` `'spacing'`<br />- `spacing:` `number`<br />- `extent?:` `AxesTickExtentValue` | `extent: 'both'`<br />- —<br />- —<br />- `'both'` | 从原点按正间距枚举，可限制到一个半轴<br />- 规则间距分支的判别值<br />- 相邻刻度的正间距<br />- 生成刻度的半轴范围 |
| 显式位置 | `Extract<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['source'], { kind: 'values' }>`<br />- `kind:` `'values'`<br />- `values:` `Array<number>`                                     | —<br />- —<br />- —                                | 严格递增、非零且位于本轴范围内的有符号值<br />- 显式位置分支的判别值<br />- 本轴范围内的非零有符号位置             |

规则间距分支的 `extent` 取值来自 <ApiValues name="AxesTickExtent" />。

### `ticks.labels`

只有提供 labels 对象时，`offset` 才默认使用 `4`；省略整个 `labels` 不会生成文字。

| 属性      | 类型                                                                                                                                                                                                          | 默认值              | 描述                                                                                |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------- |
| `entries` | `Exclude<Exclude<Exclude<AxesInput['x'], false \| undefined>['ticks'], false \| undefined>['labels'], false \| undefined>['entries']`<br />- `[number].value:` `number`<br />- `[number].text:` `IRTextBlock` | —<br />- —<br />- — | 非空静态文字列表；每项都必须命中 tick<br />- 要标注的 tick 值<br />- 对应的静态文字 |
| `offset?` | `number`                                                                                                                                                                                                      | `4`                 | 从刻度端点到文字中心的非负间距                                                      |
| `style?`  | `IRLabelVisualStyle`                                                                                                                                                                                          | —                   | 这组刻度文字共享的 Core 文字样式                                                    |

### `x.label` / `y.label`

| 属性      | 类型                              | 默认值       | 描述                         |
| --------- | --------------------------------- | ------------ | ---------------------------- |
| `text`    | `IRTextBlock`                     | —            | 静态轴名文字                 |
| `end?`    | <ApiValues name="AxesLabelEnd" /> | `'positive'` | 文字放置在正端或负端之外     |
| `offset?` | `number`                          | `8`          | 文字中心到所选端点的非负距离 |
| `style?`  | `IRLabelVisualStyle`              | —            | 轴名的 Core 文字样式         |

### `originLabel`

只有提供 `originLabel` 对象时，`offset` 才默认使用 `10`。

| 属性      | 类型                 | 默认值 | 描述                           |
| --------- | -------------------- | ------ | ------------------------------ |
| `text`    | `IRTextBlock`        | —      | 静态原点文字                   |
| `offset?` | `number`             | `10`   | 文字中心向左下方移动的非负距离 |
| `style?`  | `IRLabelVisualStyle` | —      | 原点文字的 Core 文字样式       |

## 相关

- [Grid](/standard/composite/grid)：主线、边框和更完整的规则网格
- [Tier 2 Composites](/kernel/concepts/design/composite)：Core 如何注册和下沉高层 IR
- [Path](/kernel/components/draw/path)：Axes 下沉后的轴线、格线与刻度
- [TeX](/kernel/packages/tex)：在轴名和刻度文字中使用 LaTeX 公式
- [Plot axis](/viz/plot/guide/axis)：数据 scale、自动 tick、formatter 与 guide layout
