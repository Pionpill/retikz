---
title: Grid
description: Describe regular guide lines, major lines, and borders as JSON-safe Tier 2 Grid IR that lowers to Core Paths when loaded.
---

`<Grid>` is an optional Standard Tier 2 component. It retains bounds, spacing, alignment, major lines, and borders as compact JSON fields; it lowers to Core `Path` only when the current figure loads `GridDefinition`.

## Usage

```tsx
import { Layout } from '@retikz/react';
import { Grid } from '@retikz/standard-react';
```

```tsx
<Layout width={280} height={170}>
  <Grid bounds={{ min: [20, 20], max: [260, 150] }} spacing={20} lines={{ style: { stroke: 'lightgray' } }} />
</Layout>
```

The React entry contributes `GridDefinition` locally to the current `Layout`. For persistence or non-React use, create IR through `createGrid` from `@retikz/standard` and explicitly provide `GridDefinition` to Core compile options.

| Scenario         | Public entry point                                                       |
| ---------------- | ------------------------------------------------------------------------ |
| React            | `Grid` from `@retikz/standard-react`                                     |
| Vanilla          | `grid` and `GridVanillaAdapter` from `@retikz/standard-vanilla`          |
| IR / persistence | `GridSchema`, `createGrid`, and `GridDefinition` from `@retikz/standard` |

See [Get Started](/standard/get-start) for complete Vanilla mounting and persistence flows. This page focuses on Grid-specific fields and semantics.

## Examples

### Basic grid

Give normalized `bounds` and uniform `spacing` to produce background guides.

<ComponentPreview files="grid-basic" size="sm" />

### Directional grids

`spacing` can differ per axis; `lines.vertical` and `lines.horizontal` choose visible directions. `major.style` shallowly overrides ordinary line style.

<ComponentPreview files="grid-directions" size="sm" />

### Alignment, boundaries, and border

`origin` is the lattice alignment base; `includeBoundary` only inserts missing range edges; the border can render behind and extend the lines outward.

<ComponentPreview files="grid-border" size="sm" />

## How it works

<ComponentAlert
  type="tip"
  title="Optional for everyday use"
  description="You only need this lowering model when working with Grid IR directly or debugging compile diagnostics."
/>

Grid IR does not become a renderer primitive. Lowering enumerates x/y lattice points from `origin`, identifies major lines by origin-relative lattice index, then emits ordinary `Path` nodes for Core's existing compiler and renderers.

A handwritten `IRGrid` without a loaded definition is never silently interpreted: Core emits `COMPOSITE_NOT_REGISTERED` and skips it. Standard therefore remains dynamically loaded by capability without changing compilation for other figures.

<SourceLinks
  sources={[
    { label: 'Grid schema and contract', path: 'packages/library/standard/src/composites/grid/schema.ts' },
    { label: 'Grid lowering', path: 'packages/library/standard/src/composites/grid/lower.ts' },
  ]}
/>

## API Reference

| Prop      | Type                                                                                                                               | Default                 | Description                                                               |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------- |
| `bounds`  | `{ min: Position; max: Position }`                                                                                                 | —                       | Strict two-dimensional bounds; `min` must be less than `max` on both axes |
| `spacing` | `number \| { x: number; y: number }`                                                                                               | —                       | Uniform or axis-specific positive spacing                                 |
| `origin`  | `Position`                                                                                                                         | `bounds.min`            | Lattice alignment and major-line counting base                            |
| `lines`   | `{ vertical?: boolean;`<br />`horizontal?: boolean;`<br />`includeBoundary?: boolean;`<br />`style?: IRStandardPathStrokeStyle }`  | both directions visible | Ordinary-line directions, boundary insertion, and style                   |
| `major`   | `{ every: number;`<br />`offset?: number;`<br />`style?: IRStandardPathStrokeStyle }`                                              | —                       | Major-line interval, index offset, and shallow style override             |
| `border`  | `{ padding?: number;`<br />`order?: 'behind' \| 'front';`<br />`extendLines?: boolean;`<br />`style?: IRStandardPathBorderStyle }` | —                       | Optional border outward padding, order, line extension, and style         |

## Related

- [Tier 2 Composites](/kernel/concepts/design/composite): how Core registers and lowers high-level IR
- [Path](/kernel/components/draw/path): the Core path primitive emitted by Grid lowering
