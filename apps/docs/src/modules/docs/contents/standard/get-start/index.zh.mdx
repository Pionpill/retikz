---
title: 快速开始
description: 分别用 React、Vanilla plain spec 和持久化 JSON 加载一个 Standard Grid，并理解 Definition 的注入位置。
---

本页用同一个 Grid 展示三条入口。React 与 Vanilla 适合在代码中创作；持久化路径适合从数据库、文件或 `localStorage` 恢复 JSON 配置。

## React

安装 React 入口及其宿主：

<PackageManagerInstall packages="@retikz/standard-react @retikz/react react react-dom" />

`<Grid>` 会在当前 `Layout` 局部加载 `GridDefinition`，不需要手动注册。

```tsx
import { Layout } from '@retikz/react';
import { Grid } from '@retikz/standard-react';
import { createRoot } from 'react-dom/client';

const container = document.querySelector('#diagram');
if (container === null) throw new Error('Missing #diagram container');

createRoot(container).render(
  <Layout width={280} height={170}>
    <Grid bounds={{ min: [20, 20], max: [260, 150] }} spacing={20} lines={{ style: { stroke: 'lightgray' } }} />
  </Layout>,
);
```

## Vanilla

安装 Vanilla 作者入口与 Kernel runtime：

<PackageManagerInstall packages="@retikz/standard-vanilla @retikz/vanilla" />

`grid()` 创建 plain spec embed；`GridVanillaAdapter` 在规范化当前 figure 时贡献相同的 `GridDefinition`。

```ts
import type { VanillaFigureSpec } from '@retikz/vanilla';

import { grid, GridVanillaAdapter } from '@retikz/standard-vanilla';
import { mountSvg } from '@retikz/vanilla';

const figure = {
  type: 'figure',
  version: 1,
  viewBox: { x: 0, y: 0, width: 280, height: 170 },
  children: [
    grid('paper-grid', {
      bounds: { min: [20, 20], max: [260, 150] },
      spacing: 20,
      lines: { style: { stroke: 'lightgray' } },
    }),
  ],
} satisfies VanillaFigureSpec;

const container = document.querySelector('#diagram');
if (container === null) throw new Error('Missing #diagram container');

mountSvg(container, figure, {
  adapters: [GridVanillaAdapter],
  output: { width: 280, height: 170 },
});
```

只加载 Grid 时保留这个部分数组；需要当前 Standard 的全部 Vanilla embeds 时，可从同一根入口导入并传入 `StandardVanillaAdapters`。

## 从持久化 JSON 加载

直接读写 IR 时，安装 Standard 真源、Core schema 与所选 runtime：

<PackageManagerInstall packages="@retikz/standard @retikz/core @retikz/vanilla" />

持久化完整 `IRGrid`，而不是只保存 `GridInput`。完整 IR 自带 `namespace` 与 `type`，因此宿主和工具可以识别它是什么能力；Definition 包含函数，不能进入 JSON，必须在加载时由宿主注入。

```ts
import { SceneSchema } from '@retikz/core';
import { createGrid, GridDefinition, GridSchema } from '@retikz/standard';
import { mountSvg } from '@retikz/vanilla';

const storageKey = 'retikz:grid';

localStorage.setItem(
  storageKey,
  JSON.stringify(
    createGrid({
      bounds: { min: [20, 20], max: [260, 150] },
      spacing: 20,
      lines: { style: { stroke: 'lightgray' } },
    }),
  ),
);

const raw = localStorage.getItem(storageKey);
if (raw === null) throw new Error('Missing persisted Grid');

const storedGrid = GridSchema.parse(JSON.parse(raw));
const scene = SceneSchema.parse({
  type: 'scene',
  version: 1,
  viewBox: { x: 0, y: 0, width: 280, height: 170 },
  children: [storedGrid],
});
const gridCompile = { composites: [GridDefinition] };

const container = document.querySelector('#diagram');
if (container === null) throw new Error('Missing #diagram container');

mountSvg(container, scene, {
  compile: gridCompile,
  output: { width: 280, height: 170 },
});
```

如果 `GridDefinition` 没有传入，Core 会产生 `COMPOSITE_NOT_REGISTERED` 诊断并跳过 Grid；其它已注册或 Core 原生 child 仍会继续编译。需要多个 Standard composite 时，直接把对应 Definition 放入同一个 `composites` 数组

## 下一步：用布局容器替代手写坐标

布局 JSX 使用 `FlexLayout`、`GridLayout`、`OverlayLayout` 与直属 `LayoutItem`。三者共享一次 `standard.layout` contribution；直接 IR 可注入 `[FlexLayoutDefinition, GridLayoutDefinition, OverlayLayoutDefinition]`，Vanilla 可传 `StandardLayoutVanillaAdapters`

```tsx
import { Layout, Node } from '@retikz/react';
import { FlexLayout, LayoutItem } from '@retikz/standard-react';

<Layout width={320} height={120}>
  <FlexLayout columnGap={8}>
    <LayoutItem kind="flex" itemKey="label" grow={1}>
      <Node position={[0, 0]} text="Label" />
    </LayoutItem>
    <LayoutItem kind="flex" itemKey="value" shrink={0}>
      <Node position={[0, 0]} text="42%" />
    </LayoutItem>
  </FlexLayout>
</Layout>;
```

## 接下来

| 页面                                                           | 继续了解什么                                 |
| -------------------------------------------------------------- | -------------------------------------------- |
| [网格](/standard/composite/grid)                               | 格距、主线、边界和边框                       |
| [坐标轴](/standard/composite/axes)                             | 箭头、刻度、轴名和分方向网格                 |
| [分组框](/standard/composite/frame)                            | 自动 bounds、间距、边框和标签                |
| [图例](/standard/composite/legend)                             | 离散样本、连续 ramp、probe 与 typed artifact |
| [布局容器](/standard/layout)                                   | Flex、Grid、Overlay 与 typed artifacts       |
| [直接 Definition 装载](/standard/extension/capability-loading) | Definition 选择、Core 注入与宿主边界         |
