---
title: 标准组件
description: Standard 提供可选、可动态加载、以横向能力扩展 Core 的 Tier 2 组件。
---

Standard 不向 Core 增加默认图元或渲染语义。每个组件拥有自己的 JSON-safe 高层 IR，并经已加载的 composite definition 下沉为 Core 图元。

## 组件

<LinkedCard href="/standard/components/grid">
  <span className="font-semibold">Grid</span>
  <span className="mt-1 text-center text-sm text-muted-foreground">规则笛卡尔网格、主线和边框</span>
</LinkedCard>

<LinkedCard href="/standard/components/axes">
  <span className="font-semibold">坐标轴</span>
  <span className="mt-1 text-center text-sm text-muted-foreground">带箭头、刻度、轴名和可选网格的参考平面</span>
</LinkedCard>
