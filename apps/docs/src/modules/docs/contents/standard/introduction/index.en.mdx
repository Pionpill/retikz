---
title: Introduction
description: Learn how the Library extends Core horizontally with optional, dynamically loaded Tier 2 capabilities and which entry point each package owns.
---

The Library group contains official drawing capabilities that should not enter minimal Core but remain reusable across domains. Components such as Grid, Axes, and Frame express common structures through compact high-level configuration, then lower to existing Core `Path`, `Node`, and `Scope` primitives.

## Boundary with Core

The Library extends Core horizontally; it is not another drawing kernel. Each Tier 2 capability owns a JSON-safe schema and lowering logic without introducing parallel base IR, geometry foundations, or renderer semantics.

As a result:

- Kernel installation, compilation, and rendering stay unchanged when the Library is unused.
- Capabilities load per figure. Definitions are not registered globally and never enter persisted JSON.
- Library IR still lowers through Core compile and uses the existing SVG, Canvas, and other renderers.

## Three Packages

| Package                    | Responsibility                                      | Use it when                                        |
| -------------------------- | --------------------------------------------------- | -------------------------------------------------- |
| `@retikz/standard`         | Schemas, factories, Definitions, and lowering       | Validating or persisting IR and compiling directly |
| `@retikz/standard-react`   | React DSL such as `<Grid>`, `<Axes>`, and `<Frame>` | Authoring inside `@retikz/react` `Layout`          |
| `@retikz/standard-vanilla` | Plain-spec helpers and Vanilla Tier 2 adapters      | Authoring in browsers or scripts without React     |

All three packages share the same schemas and lowering from `@retikz/standard`. React and Vanilla only connect their authoring surfaces to those capabilities; they do not duplicate semantics.

## Loading Model

| Scenario     | How the capability is loaded                                                             |
| ------------ | ---------------------------------------------------------------------------------------- |
| React        | The component contributes its Definition locally to the current `Layout`                 |
| Vanilla      | A helper creates an embed and the host supplies its Adapter through `adapters`           |
| Persisted IR | A schema validates JSON and the host injects its Definition through `compile.composites` |

See [Get Started](/standard/get-start) for all three complete paths, or browse [Composites](/standard/composite) to choose a capability.
