{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## Effect presets

### BlinkOptions

<p>
  <ApiSourceLink label={'BlinkOptions'} path={'packages/library/extension/src/animation/presets.ts'} startLine={95}>
    Blink options: minimum opacity and iteration count, defaulting to infinite
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member        | Type                             | Default | Description                                                                            |
| ------------- | -------------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`      | `number`                         | —       | Delay before the first iteration, in milliseconds                                      |
| `dim?`        | `number`                         | —       | Minimum opacity during flashing; defaults to 0                                         |
| `duration?`   | `number`                         | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`     | `IRAnimationTrack['easing']`     | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `iterations?` | `IRAnimationTrack['iterations']` | —       | Flash iteration count; defaults to infinite                                            |
| `trigger?`    | `IRAnimationTrack['trigger']`    | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type BlinkOptions = AnimationPresetOptions & {
  dim?: number;
  iterations?: IRAnimationTrack['iterations'];
};
```

</DocTab>

</DocTabs>

### FlashOptions

<p>
  <ApiSourceLink label={'FlashOptions'} path={'packages/library/extension/src/animation/presets.ts'} startLine={75}>
    Flash options: minimum opacity and flash count
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member        | Type                             | Default | Description                                                                            |
| ------------- | -------------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`      | `number`                         | —       | Delay before the first iteration, in milliseconds                                      |
| `dim?`        | `number`                         | —       | Minimum opacity during flashing; defaults to 0                                         |
| `duration?`   | `number`                         | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`     | `IRAnimationTrack['easing']`     | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `iterations?` | `IRAnimationTrack['iterations']` | —       | Flash count; defaults to 2                                                             |
| `trigger?`    | `IRAnimationTrack['trigger']`    | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type FlashOptions = AnimationPresetOptions & {
  dim?: number;
  iterations?: IRAnimationTrack['iterations'];
};
```

</DocTab>

</DocTabs>

### GrowUpOptions

<p>
  <ApiSourceLink label={'GrowUpOptions'} path={'packages/library/extension/src/animation/presets.ts'} startLine={19}>
    GrowUp options: origin, defaulting to bottom center for bars growing from a baseline
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                          | Default | Description                                                                            |
| ----------- | ----------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`    | `number`                      | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                      | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`  | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `origin?`   | `IRAnimationOrigin`           | —       | Scale origin; defaults to bottom center                                                |
| `trigger?`  | `IRAnimationTrack['trigger']` | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type GrowUpOptions = AnimationPresetOptions & {
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### PulseOptions

<p>
  <ApiSourceLink label={'PulseOptions'} path={'packages/library/extension/src/animation/presets.ts'} startLine={36}>
    Pulse options: peak scale and origin
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                          | Default | Description                                                                            |
| ----------- | ----------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`    | `number`                      | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                      | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`  | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `origin?`   | `IRAnimationOrigin`           | —       | Scale origin; defaults to the geometry center                                          |
| `peak?`     | `number`                      | —       | Peak scale; defaults to 1.1                                                            |
| `trigger?`  | `IRAnimationTrack['trigger']` | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type PulseOptions = AnimationPresetOptions & {
  peak?: number;
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### SpinOptions

<p>
  <ApiSourceLink label={'SpinOptions'} path={'packages/library/extension/src/animation/presets.ts'} startLine={57}>
    Spin options: rotation origin
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                          | Default | Description                                                                            |
| ----------- | ----------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`    | `number`                      | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                      | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`  | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `origin?`   | `IRAnimationOrigin`           | —       | Rotation origin; defaults to the geometry center                                       |
| `trigger?`  | `IRAnimationTrack['trigger']` | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type SpinOptions = AnimationPresetOptions & {
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### WiggleOptions

<p>
  <ApiSourceLink label={'WiggleOptions'} path={'packages/library/extension/src/animation/presets.ts'} startLine={115}>
    Wiggle options: angular amplitude in degrees, origin, and iteration count
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member        | Type                             | Default | Description                                                                            |
| ------------- | -------------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `angle?`      | `number`                         | —       | Angular amplitude in degrees; defaults to 5                                            |
| `delay?`      | `number`                         | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?`   | `number`                         | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`     | `IRAnimationTrack['easing']`     | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `iterations?` | `IRAnimationTrack['iterations']` | —       | Wiggle count; defaults to 3                                                            |
| `origin?`     | `IRAnimationOrigin`              | —       | Rotation origin; defaults to the geometry center                                       |
| `trigger?`    | `IRAnimationTrack['trigger']`    | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type WiggleOptions = AnimationPresetOptions & {
  angle?: number;
  origin?: IRAnimationOrigin;
  iterations?: IRAnimationTrack['iterations'];
};
```

</DocTab>

</DocTabs>

### blink

<p>
  <ApiSourceLink label={'blink'} path={'packages/library/extension/src/animation/presets.ts'} startLine={103}>
    Continuous flashing: opacity 1 to dim to 1 in an infinite loop; blink is the infinite form of flash
  </ApiSourceLink>
</p>

```ts
export declare const blink: (opts: BlinkOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type           | Description |
| --------- | -------------- | ----------- |
| `opts`    | `BlinkOptions` | —           |

### flash

<p>
  <ApiSourceLink label={'flash'} path={'packages/library/extension/src/animation/presets.ts'} startLine={83}>
    Flash emphasis: opacity 1 to dim to 1, twice by default; ends at base full visibility
  </ApiSourceLink>
</p>

```ts
export declare const flash: (opts: FlashOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type           | Description |
| --------- | -------------- | ----------- |
| `opts`    | `FlashOptions` | —           |

### grow

<p>
  <ApiSourceLink label={'grow'} path={'packages/library/extension/src/animation/presets.ts'} startLine={16}>
    Grow from zero: scaleIn with from set to 0
  </ApiSourceLink>
</p>

```ts
export declare const grow: (opts: Omit<ScaleInOptions, 'from'>) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type                           | Description |
| --------- | ------------------------------ | ----------- |
| `opts`    | `Omit<ScaleInOptions, 'from'>` | —           |

### growUp

<p>
  <ApiSourceLink label={'growUp'} path={'packages/library/extension/src/animation/presets.ts'} startLine={25}>
    Grow from the baseline: scaleY from 0 to 1 around the bottom edge
  </ApiSourceLink>
</p>

```ts
export declare const growUp: (opts: GrowUpOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type            | Description |
| --------- | --------------- | ----------- |
| `opts`    | `GrowUpOptions` | —           |

### pulse

<p>
  <ApiSourceLink label={'pulse'} path={'packages/library/extension/src/animation/presets.ts'} startLine={44}>
    Pulse: scale from 1 to peak and back to 1 in an infinite loop
  </ApiSourceLink>
</p>

```ts
export declare const pulse: (opts: PulseOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type           | Description |
| --------- | -------------- | ----------- |
| `opts`    | `PulseOptions` | —           |

### spin

<p>
  <ApiSourceLink label={'spin'} path={'packages/library/extension/src/animation/presets.ts'} startLine={63}>
    Spin: rotate from 0 to 360 in an infinite, constant-speed loop
  </ApiSourceLink>
</p>

```ts
export declare const spin: (opts: SpinOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type          | Description |
| --------- | ------------- | ----------- |
| `opts`    | `SpinOptions` | —           |

### wiggle

<p>
  <ApiSourceLink label={'wiggle'} path={'packages/library/extension/src/animation/presets.ts'} startLine={125}>
    Wiggle emphasis: rotate through 0, positive amplitude, negative amplitude, positive amplitude, and 0; ends at base
    rotation
  </ApiSourceLink>
</p>

```ts
export declare const wiggle: (opts: WiggleOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type            | Description |
| --------- | --------------- | ----------- |
| `opts`    | `WiggleOptions` | —           |
