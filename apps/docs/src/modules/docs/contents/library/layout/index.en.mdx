---
title: Layout
description: Use the independent Layout package family to compose reusable, domain-neutral arrangements with FlexLayout, GridLayout, and OverlayLayout.
---

Layout extracts child arrangement from hand-authored coordinates. Flex, Grid, and Overlay cover linear distribution, two-dimensional tracks, and stacking within one region, and they compose recursively. Higher-level packages such as Standard, Notation, and Plot can reuse them while retaining ownership of their domain semantics.

Given the same available space and child measurement, layout solving is deterministic and independent of a specific renderer. Text dimensions still depend on the host measurer, fonts, and font-loading state.

The example below uses Flex for lines, Grid for columns, and Overlay for a badge in the right-hand cell to show how all three containers compose as ordinary children.

<ComponentPreview
  files="layout-nested"
  hideCode
  size="md"
  caption="The inspection overlay is separate from the real figure: it does not expand layout bounds or participate in hit testing."
/>

This example uses `LayoutInspectLayout` from `@retikz/layout-react/inspect` to install the layout Inspectors, then selects individual occurrences with `InspectFlexLayout`, `InspectGridLayout`, and `InspectOverlayLayout`. Use `LayoutInspectScope request={false}` as a hard barrier when one authored subtree must not inherit inspection requests. The Layout root does not statically load the optional inspection capability.

## Responsibilities

| Component       | Core model                     | Best suited for                                |
| --------------- | ------------------------------ | ---------------------------------------------- |
| `FlexLayout`    | One axis, grow/shrink, wrap    | Legend entries, label strips, toolbars         |
| `GridLayout`    | 2D tracks, spans, auto placing | Panels, matrices, guide regions, structured UI |
| `OverlayLayout` | Alignment or local positioning | Badges, overlays, annotations, stacked marks   |

Containers own generic size distribution, spacing, alignment, overflow, and observable results. Data channels, chart semantics, table rules, and algorithmic layout remain with their domain packages.

## Pages

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/library/layout/get-start">
    <span className="font-semibold">Get Started</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">React, Vanilla, and persisted IR paths</span>
  </LinkedCard>
  <LinkedCard href="/library/layout/flex-layout">
    <span className="font-semibold">FlexLayout</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      One-dimensional distribution, wrapping, and baselines
    </span>
  </LinkedCard>
  <LinkedCard href="/library/layout/grid-layout">
    <span className="font-semibold">GridLayout</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Two-dimensional tracks, spans, and auto placement
    </span>
  </LinkedCard>
  <LinkedCard href="/library/layout/overlay-layout">
    <span className="font-semibold">OverlayLayout</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Alignment, positioning, and stable paint order
    </span>
  </LinkedCard>
  <LinkedCard href="/library/layout/reference">
    <span className="font-semibold">API Reference</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Input contracts, artifact contracts, and runtime wiring
    </span>
  </LinkedCard>
  <LinkedCard href="/library/layout/changelog">
    <span className="font-semibold">Changelog</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Version history for the Layout package family
    </span>
  </LinkedCard>
</div>
