---
title: 快速开始
description: 安装 Layout，用 React 或 Vanilla 将两个图形排成一行。
---

选择与你的项目一致的宿主入口，完成下面的最小例子。所有发布包使用 ES modules；Node.js 环境要求 24 或更高版本。

## React

<PackageManagerInstall packages="@retikz/layout-react @retikz/react react react-dom" />

在已有 React 应用中渲染下面的 `Example` 组件。`react` / `react-dom` 是宿主对等依赖，版本至少为 18。

```tsx
import type { FC } from 'react';
import { Layout, Node } from '@retikz/react';
import { FlexLayout, LayoutItem } from '@retikz/layout-react';

export const Example: FC = () => (
  <Layout>
    <FlexLayout gap={{ column: 12, row: 0 }}>
      <LayoutItem kind="flex">
        <Node shape="circle" position={[0, 0]} layout={{ minimumSize: 32 }} />
      </LayoutItem>
      <LayoutItem kind="flex">
        <Node position={[0, 0]} layout={{ minimumSize: 32 }} />
      </LayoutItem>
    </FlexLayout>
  </Layout>
);
```

结果是一个圆形和一个矩形沿水平方向排列，间隔 12 个用户单位。每个 `LayoutItem` 包含一个图形；它们虽然都从局部 `[0, 0]` 开始，最终位置由 Flex 容器分配。

将上面的组件保存为 `Example.tsx`，在已有 React 应用入口挂载（HTML 中准备 `<div id="root"></div>`）：

```tsx
import { createRoot } from 'react-dom/client';
import { Example } from './Example';

const root = document.getElementById('root');
if (root) createRoot(root).render(<Example />);
```

根 `Layout` 默认输出 SVG，本例无需配置 renderer。尺寸和间距使用相同的图形用户单位；默认原尺寸显示时，1 单位对应 1 CSS 像素。

## Vanilla

<PackageManagerInstall packages="@retikz/layout-vanilla @retikz/vanilla" />

```ts
import { flexLayout, FlexLayoutInputEmbedAdapter } from '@retikz/layout-vanilla';
import { node, renderToSvgString, scene } from '@retikz/vanilla';

const input = scene({
  children: [
    flexLayout({
      gap: { column: 12, row: 0 },
      children: [
        {
          kind: 'flex',
          child: node({ id: 'circle', shape: 'circle', position: [0, 0], layout: { minimumSize: 32 } }),
        },
        {
          kind: 'flex',
          child: node({ id: 'box', position: [0, 0], layout: { minimumSize: 32 } }),
        },
      ],
    }),
  ],
});
const svg = renderToSvgString(input, { adapters: [FlexLayoutInputEmbedAdapter] });
console.log(svg);
```

`svg` 是可保存为 `.svg` 文件或交给页面宿主展示的完整 SVG 字符串。helper 创建普通输入，`adapters` 让 Vanilla 识别对应的嵌入项并收集其能力依赖；省略 adapter 会使输入无法解析。

`flexLayout` 的第一个参数 `'row'` 是容器标识符，不是方向配置。两端默认方向都是水平行（`direction: 'row'`）。`LayoutItem kind="flex"` 选择 Flex 子项约束，每项包含一个可绘制子图形；`minimumSize: 32` 同时约束最小宽和最小高。未指定 `shape` 的 Node 默认是矩形。

## 接下来

修改 `gap.column` 调整间距；需要分配剩余空间时，为子项设置 `grow` 并给容器指定尺寸。继续阅读[弹性布局](/library/layout/flex-layout)。二维区域分配用[网格布局](/library/layout/grid-layout)，同区域叠放用[叠加布局](/library/layout/overlay-layout)。

版本变化见[更新日志](/library/layout/changelog)。
