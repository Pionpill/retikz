{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

### React 属性与组合标记

#### MapEntryProps

<p>
  <ApiSourceLink
    label={'MapEntryProps'}
    path={'packages/library/standard-react/src/container/map/MapMarkers.tsx'}
    startLine={7}
  >
    Map 的一条键值记录，包含一个 MapKey 和一个 MapValue
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型        | 默认值 | 说明 |
| ---------- | ----------- | ------ | ---- |
| `children` | `ReactNode` | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MapEntryProps = {
  children: ReactNode;
};
```

</DocTab>

</DocTabs>

#### MapKeyProps

<p>
  <ApiSourceLink
    label={'MapKeyProps'}
    path={'packages/library/standard-react/src/container/map/MapMarkers.tsx'}
    startLine={9}
  >
    Map 键单元格的文本或 drawable 输入
  </ApiSourceLink>
</p>

```ts
export type MapKeyProps = CellMarkerProps;
```

#### MapProps

<p>
  <ApiSourceLink label={'MapProps'} path={'packages/library/standard-react/src/container/map/Map.tsx'} startLine={14}>
    Map 的 React authoring 属性；键值角色覆盖统一位于 style.key/value 与 layout.key/value
  </ApiSourceLink>
</p>

```ts
export type MapProps = Omit<IRMap, 'namespace' | 'type' | 'entries' | 'data' | 'dataObjectDisplay'> &
  (
    | {
        data: NonNullable<IRMap['data']>;
        entries?: never;
        children?: never;
        dataObjectDisplay?: IRMap['dataObjectDisplay'];
      }
    | {
        data?: never;
        entries: Array<{
          key: string | CellProps;
          value: string | CellProps;
        }>;
        children?: never;
        dataObjectDisplay?: never;
      }
    | {
        data?: never;
        entries?: never;
        children?: ReactNode;
        dataObjectDisplay?: never;
      }
  );
```

#### MapValueProps

<p>
  <ApiSourceLink
    label={'MapValueProps'}
    path={'packages/library/standard-react/src/container/map/MapMarkers.tsx'}
    startLine={11}
  >
    Map 值单元格的文本或 drawable 输入
  </ApiSourceLink>
</p>

```ts
export type MapValueProps = CellMarkerProps;
```

#### MapEntry

<p>
  <ApiSourceLink
    label={'MapEntry'}
    path={'packages/library/standard-react/src/container/map/MapMarkers.tsx'}
    startLine={14}
  >
    Map 的直属记录声明
  </ApiSourceLink>
</p>

```ts
export declare const MapEntry: FC<MapEntryProps>;
```

#### MapKey

<p>
  <ApiSourceLink
    label={'MapKey'}
    path={'packages/library/standard-react/src/container/map/MapMarkers.tsx'}
    startLine={16}
  >
    MapEntry 的键槽位
  </ApiSourceLink>
</p>

```ts
export declare const MapKey: FC<MapKeyProps>;
```

#### MapValue

<p>
  <ApiSourceLink
    label={'MapValue'}
    path={'packages/library/standard-react/src/container/map/MapMarkers.tsx'}
    startLine={18}
  >
    MapEntry 的值槽位
  </ApiSourceLink>
</p>

```ts
export declare const MapValue: FC<MapValueProps>;
```

### Vanilla 入口

#### InputMap

<p>
  <ApiSourceLink label={'InputMap'} path={'packages/library/standard-vanilla/src/container/map.ts'} startLine={10}>
    Map 的 Vanilla authoring 输入；键值角色覆盖统一位于 style.key/value 与 layout.key/value
  </ApiSourceLink>
</p>

```ts
export type InputMap = Omit<IRMap, 'namespace' | 'type' | 'entries' | 'data' | 'dataObjectDisplay'> &
  (
    | {
        entries: Array<{
          key: string | InputCell;
          value: string | InputCell;
        }>;
        data?: never;
        dataObjectDisplay?: never;
      }
    | {
        data: NonNullable<IRMap['data']>;
        entries?: never;
        dataObjectDisplay?: IRMap['dataObjectDisplay'];
      }
  );
```

#### MapInputEmbedAdapter

<p>
  <ApiSourceLink
    label={'MapInputEmbedAdapter'}
    path={'packages/library/standard-vanilla/src/container/map.ts'}
    startLine={17}
  >
    将 Map 输入与嵌套内容交给根级 traversal
  </ApiSourceLink>
</p>

```ts
export declare const MapInputEmbedAdapter: InputEmbedAdapter<InputMap>;
```

#### map

<p>
  <ApiSourceLink label={'map'} path={'packages/library/standard-vanilla/src/container/map.ts'} startLine={50}>
    创建 Map embed；显式 input.id 同时用作领域与 embed 身份
  </ApiSourceLink>
</p>

```ts
export declare const map: (input: InputMap) => InputEmbed<InputMap>;
```

##### 参数

| 参数    | 类型       | 说明 |
| ------- | ---------- | ---- |
| `input` | `InputMap` | —    |

### IR 与定义

#### IRMap

<p>
  <ApiSourceLink label={'IRMap'} path={'packages/library/standard/src/container/map/schema/types.ts'} startLine={8}>
    稀疏 Map Source，展示键允许重复
  </ApiSourceLink>
</p>

```ts
export type IRMap = Omit<input<typeof MapSchema>, keyof IRScopeProps | 'entries' | 'data' | 'dataObjectDisplay'> &
  Omit<IRScopeProps, 'style'> & {
    style?: input<typeof MapSchema>['style'];
  } & (
    | {
        entries: Array<{
          key: string | IRCell;
          value: string | IRCell;
        }>;
        data?: never;
        dataObjectDisplay?: never;
      }
    | {
        data: NonNullable<input<typeof MapSchema>['data']>;
        entries?: never;
        dataObjectDisplay?: input<typeof MapSchema>['dataObjectDisplay'];
      }
  );
```

#### MapDefinition

<p>
  <ApiSourceLink
    label={'MapDefinition'}
    path={'packages/library/standard/src/container/map/definition.ts'}
    startLine={8}
  >
    Standard Map 的布局感知 Definition
  </ApiSourceLink>
</p>

```ts
export const MapDefinition: LayoutCompositeDefinition<IRMap, 'standard', 'map'> = defineComposite({
  namespace: 'standard',
  type: 'map',
  schema: MapSchema,
  compile: compileMap,
});
```

#### MapProvider

<p>
  <ApiSourceLink label={'MapProvider'} path={'packages/library/standard/src/container/map/provider.ts'} startLine={6}>
    Map 与单元格 lower target 的按需依赖声明
  </ApiSourceLink>
</p>

```ts
export declare const MapProvider: CoreDependencyProvider;
```

#### createMap

<p>
  <ApiSourceLink label={'createMap'} path={'packages/library/standard/src/container/map/factory.ts'} startLine={3}>
    保留稀疏 Source 的类型化工厂
  </ApiSourceLink>
</p>

```ts
export declare const createMap: (input: IRMap) => IRMap;
```

##### 参数

| 参数    | 类型    | 说明 |
| ------- | ------- | ---- |
| `input` | `IRMap` | —    |
