{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## 效果预设

### BlinkOptions

<p>
  <ApiSourceLink label={'BlinkOptions'} path={'packages/library/standard/src/animation/presets.ts'} startLine={95}>
    `blink` 选项：谷值不透明度 + 闪烁次数（缺省无限）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员          | 类型                             | 默认值 | 说明                                                              |
| ------------- | -------------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`      | `number`                         | —      | 首次迭代前延迟（毫秒）                                            |
| `dim?`        | `number`                         | —      | 闪烁谷值不透明度；缺省 0                                          |
| `duration?`   | `number`                         | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`     | `IRAnimationTrack['easing']`     | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `iterations?` | `IRAnimationTrack['iterations']` | —      | 闪烁次数；缺省 'infinite'                                         |
| `trigger?`    | `IRAnimationTrack['trigger']`    | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BlinkOptions = AnimationPresetOptions & {
  dim?: number;
  iterations?: IRAnimationTrack['iterations'];
};
```

</DocTab>

</DocTabs>

### FlashOptions

<p>
  <ApiSourceLink label={'FlashOptions'} path={'packages/library/standard/src/animation/presets.ts'} startLine={75}>
    `flash` 选项：谷值不透明度 + 闪烁次数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员          | 类型                             | 默认值 | 说明                                                              |
| ------------- | -------------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`      | `number`                         | —      | 首次迭代前延迟（毫秒）                                            |
| `dim?`        | `number`                         | —      | 闪烁谷值不透明度；缺省 0                                          |
| `duration?`   | `number`                         | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`     | `IRAnimationTrack['easing']`     | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `iterations?` | `IRAnimationTrack['iterations']` | —      | 闪烁次数；缺省 2                                                  |
| `trigger?`    | `IRAnimationTrack['trigger']`    | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type FlashOptions = AnimationPresetOptions & {
  dim?: number;
  iterations?: IRAnimationTrack['iterations'];
};
```

</DocTab>

</DocTabs>

### GrowUpOptions

<p>
  <ApiSourceLink label={'GrowUpOptions'} path={'packages/library/standard/src/animation/presets.ts'} startLine={19}>
    `growUp` 选项：支点 `origin`（缺省底边中点，柱状图从基线长出）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                          | 默认值 | 说明                                                              |
| ----------- | ----------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`    | `number`                      | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                      | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`  | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `origin?`   | `IRAnimationOrigin`           | —      | 缩放支点；缺省 'bottom'（底边中点）                               |
| `trigger?`  | `IRAnimationTrack['trigger']` | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type GrowUpOptions = AnimationPresetOptions & {
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### PulseOptions

<p>
  <ApiSourceLink label={'PulseOptions'} path={'packages/library/standard/src/animation/presets.ts'} startLine={36}>
    `pulse` 选项：峰值缩放 + 支点
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                          | 默认值 | 说明                                                              |
| ----------- | ----------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`    | `number`                      | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                      | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`  | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `origin?`   | `IRAnimationOrigin`           | —      | 缩放支点（缺省几何中心）                                          |
| `peak?`     | `number`                      | —      | 峰值缩放；缺省 1.1                                                |
| `trigger?`  | `IRAnimationTrack['trigger']` | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PulseOptions = AnimationPresetOptions & {
  peak?: number;
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### SpinOptions

<p>
  <ApiSourceLink label={'SpinOptions'} path={'packages/library/standard/src/animation/presets.ts'} startLine={57}>
    `spin` 选项：旋转支点
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                          | 默认值 | 说明                                                              |
| ----------- | ----------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`    | `number`                      | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                      | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`  | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `origin?`   | `IRAnimationOrigin`           | —      | 旋转支点（缺省几何中心）                                          |
| `trigger?`  | `IRAnimationTrack['trigger']` | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type SpinOptions = AnimationPresetOptions & {
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### WiggleOptions

<p>
  <ApiSourceLink label={'WiggleOptions'} path={'packages/library/standard/src/animation/presets.ts'} startLine={115}>
    `wiggle` 选项：抖动幅度（度）+ 支点 + 抖动次数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员          | 类型                             | 默认值 | 说明                                                              |
| ------------- | -------------------------------- | ------ | ----------------------------------------------------------------- |
| `angle?`      | `number`                         | —      | 抖动幅度（度）；缺省 5                                            |
| `delay?`      | `number`                         | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?`   | `number`                         | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`     | `IRAnimationTrack['easing']`     | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `iterations?` | `IRAnimationTrack['iterations']` | —      | 抖动次数；缺省 3                                                  |
| `origin?`     | `IRAnimationOrigin`              | —      | 旋转支点（缺省几何中心）                                          |
| `trigger?`    | `IRAnimationTrack['trigger']`    | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type WiggleOptions = AnimationPresetOptions & {
  angle?: number;
  origin?: IRAnimationOrigin;
  iterations?: IRAnimationTrack['iterations'];
};
```

</DocTab>

</DocTabs>

### blink

<p>
  <ApiSourceLink label={'blink'} path={'packages/library/standard/src/animation/presets.ts'} startLine={103}>
    持续闪烁：`opacity` 1→dim→1 无限循环（blink = 无限版 flash）
  </ApiSourceLink>
</p>

```ts
export declare const blink: (opts: BlinkOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型           | 说明 |
| ------ | -------------- | ---- |
| `opts` | `BlinkOptions` | —    |

### flash

<p>
  <ApiSourceLink label={'flash'} path={'packages/library/standard/src/animation/presets.ts'} startLine={83}>
    闪一下强调：`opacity` 1→dim→1，默认闪 2 次（末帧 = base = 完整可见）
  </ApiSourceLink>
</p>

```ts
export declare const flash: (opts: FlashOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型           | 说明 |
| ------ | -------------- | ---- |
| `opts` | `FlashOptions` | —    |

### grow

<p>
  <ApiSourceLink label={'grow'} path={'packages/library/standard/src/animation/presets.ts'} startLine={16}>
    从无到有放大：`scaleIn` 的 `from: 0` 别名
  </ApiSourceLink>
</p>

```ts
export declare const grow: (opts: Omit<ScaleInOptions, 'from'>) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型                           | 说明 |
| ------ | ------------------------------ | ---- |
| `opts` | `Omit<ScaleInOptions, 'from'>` | —    |

### growUp

<p>
  <ApiSourceLink label={'growUp'} path={'packages/library/standard/src/animation/presets.ts'} startLine={25}>
    从基线长出：`scaleY` 0→1，支点底边（柱状图入场）
  </ApiSourceLink>
</p>

```ts
export declare const growUp: (opts: GrowUpOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型            | 说明 |
| ------ | --------------- | ---- |
| `opts` | `GrowUpOptions` | —    |

### pulse

<p>
  <ApiSourceLink label={'pulse'} path={'packages/library/standard/src/animation/presets.ts'} startLine={44}>
    脉冲：`scale` 1→peak→1 无限循环（强调 / 心跳）
  </ApiSourceLink>
</p>

```ts
export declare const pulse: (opts: PulseOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型           | 说明 |
| ------ | -------------- | ---- |
| `opts` | `PulseOptions` | —    |

### spin

<p>
  <ApiSourceLink label={'spin'} path={'packages/library/standard/src/animation/presets.ts'} startLine={63}>
    旋转：`rotate` 0→360 无限循环、匀速（loader）
  </ApiSourceLink>
</p>

```ts
export declare const spin: (opts: SpinOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型          | 说明 |
| ------ | ------------- | ---- |
| `opts` | `SpinOptions` | —    |

### wiggle

<p>
  <ApiSourceLink label={'wiggle'} path={'packages/library/standard/src/animation/presets.ts'} startLine={125}>
    抖动强调：`rotate` 0→+a→−a→+a→0 来回摆（末帧 = base 不旋转）
  </ApiSourceLink>
</p>

```ts
export declare const wiggle: (opts: WiggleOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型            | 说明 |
| ------ | --------------- | ---- |
| `opts` | `WiggleOptions` | —    |
