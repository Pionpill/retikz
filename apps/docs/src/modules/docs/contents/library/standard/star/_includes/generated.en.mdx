{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## React entry

### StarProps

<p>
  <ApiSourceLink label={'StarProps'} path={'packages/library/standard-react/src/shape/Star.tsx'} startLine={9}>
    React Star authoring input and host events
  </ApiSourceLink>
</p>

```ts
export type StarProps = InputStar &
  HydrationEventProps & {
    id?: string;
  };
```

### Star

<p>
  <ApiSourceLink label={'Star'} path={'packages/library/standard-react/src/shape/Star.tsx'} startLine={12}>
    Declare a Standard Star through its Vanilla adapter
  </ApiSourceLink>
</p>

```ts
export declare const Star: {
  (props: P, context?: any): ReactNode;
};
```

## Vanilla inputs and adapters

### InputStar

<p>
  <ApiSourceLink label={'InputStar'} path={'packages/library/standard-vanilla/src/shape/star.ts'} startLine={8}>
    Star Vanilla authoring input
  </ApiSourceLink>
</p>

```ts
export type InputStar = InputShape<IRStar>;
```

### StarInputEmbedAdapter

<p>
  <ApiSourceLink
    label={'StarInputEmbedAdapter'}
    path={'packages/library/standard-vanilla/src/shape/star.ts'}
    startLine={10}
  >
    Normalize Star authoring input into persistent Standard intent
  </ApiSourceLink>
</p>

```ts
export declare const StarInputEmbedAdapter: InputEmbedAdapter<InputStar & { id?: string }>;
```

## Source and capability assembly

### IRStar

<p>
  <ApiSourceLink label={'IRStar'} path={'packages/library/standard/src/shape/star/types.ts'} startLine={6}>
    Persistent Star shape
  </ApiSourceLink>
</p>

```ts
export type IRStar = ShapeSource<ZodInput<typeof StarSchema>>;
```

### StarDefinition

<p>
  <ApiSourceLink label={'StarDefinition'} path={'packages/library/standard/src/shape/star/definition.ts'} startLine={7}>
    Star Core composite registration
  </ApiSourceLink>
</p>

```ts
export const StarDefinition = defineComposite({
  namespace: 'standard',
  type: 'star',
  schema: StarSchema,
  expand: (source: IRStar) => ({ children: [lowerStar(source)] }),
});
```

### StarProvider

<p>
  <ApiSourceLink label={'StarProvider'} path={'packages/library/standard/src/shape/star/provider.ts'} startLine={5}>
    Star on-demand dependency contribution
  </ApiSourceLink>
</p>

```ts
export const StarProvider: CoreDependencyProvider = Object.freeze({
  key: Object.freeze({ capability: 'composite', namespace: 'standard', type: 'star' }),
  dependencies: Object.freeze([]),
  datasets: Object.freeze({}),
  makeDefinition: () => StarDefinition,
});
```

### createStar

<p>
  <ApiSourceLink label={'createStar'} path={'packages/library/standard/src/shape/star/factory.ts'} startLine={4}>
    Create a Star Source preserving geometry intent
  </ApiSourceLink>
</p>

```ts
export declare const createStar: (input: ShapeProperties<IRStar>) => IRStar;
```

#### Parameters

| Parameter | Type                      | Description |
| --------- | ------------------------- | ----------- |
| `input`   | `ShapeProperties<IRStar>` | —           |
