{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## React 入口

### ArcProps

<p>
  <ApiSourceLink label={'ArcProps'} path={'packages/library/standard-react/src/shape/Arc.tsx'} startLine={9}>
    React Arc 的作者输入与宿主事件
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员              | 类型                                  | 默认值 | 说明                                                                                         |
| ----------------- | ------------------------------------- | ------ | -------------------------------------------------------------------------------------------- |
| `animations?`     | `InputArc['animations']`              | —      | —                                                                                            |
| `arrow?`          | `InputPathArrowDirectionValue`        | —      | 路径级箭头方向                                                                               |
| `arrowDetail?`    | `IRArrowDetail`                       | —      | 箭头的顶层默认与端点覆盖配置                                                                 |
| `arrowPlacement?` | `InputPathArrowPlacement`             | —      | 箭头端点的共享与逐端放置配置                                                                 |
| `center`          | `InputTarget`                         | —      | —                                                                                            |
| `close?`          | `"chord" \| "open" \| "sector"`       | —      | —                                                                                            |
| `endAngle?`       | `number`                              | —      | —                                                                                            |
| `id?`             | `string`                              | —      | —                                                                                            |
| `label?`          | `InputArc['label']`                   | —      | —                                                                                            |
| `marks?`          | `InputArc['marks']`                   | —      | —                                                                                            |
| `meta?`           | `Record<string, JsonValue>`           | —      | —                                                                                            |
| `onClick?`        | `HydrationHandler`                    | —      | 单击该图元（DOM `click`）                                                                    |
| `onDoubleClick?`  | `HydrationHandler`                    | —      | 双击该图元（DOM `dblclick`）                                                                 |
| `onPointerDown?`  | `HydrationHandler`                    | —      | 指针在该图元上按下（DOM `pointerdown`）                                                      |
| `onPointerEnter?` | `HydrationHandler`                    | —      | 指针进入该图元（由 `pointermove` + 命中 id 状态机合成，跨子元素不重复触发）                  |
| `onPointerLeave?` | `HydrationHandler`                    | —      | 指针离开该图元（由 `pointermove` + 命中 id 状态机合成）                                      |
| `onPointerMove?`  | `HydrationHandler`                    | —      | 指针在该图元上移动（DOM `pointermove`）                                                      |
| `onPointerUp?`    | `HydrationHandler`                    | —      | 指针在该图元上抬起（DOM `pointerup`）                                                        |
| `onRightClick?`   | `HydrationHandler`                    | —      | 右键该图元（DOM `contextmenu`）；默认不抑制浏览器菜单，handler 自行 `event.preventDefault()` |
| `onWheel?`        | `HydrationHandler`                    | —      | 在该图元上滚轮（DOM `wheel`）                                                                |
| `radius`          | `number \| { x: number; y: number; }` | —      | —                                                                                            |
| `rotate?`         | `number`                              | —      | —                                                                                            |
| `roundedCorners?` | `number`                              | —      | —                                                                                            |
| `scale?`          | `number \| { x: number; y: number; }` | —      | —                                                                                            |
| `startAngle?`     | `number`                              | —      | —                                                                                            |
| `style?`          | `InputArc['style']`                   | —      | —                                                                                            |
| `sweepAngle?`     | `number`                              | —      | —                                                                                            |
| `thickness?`      | `PathThicknessValue`                  | —      | 路径描边宽度语法糖                                                                           |
| `zIndex?`         | `number`                              | —      | —                                                                                            |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ArcProps = InputArc &
  HydrationEventProps & {
    id?: string;
  };
```

</DocTab>

</DocTabs>

### SectorProps

<p>
  <ApiSourceLink label={'SectorProps'} path={'packages/library/standard-react/src/shape/Sector.tsx'} startLine={9}>
    React Sector 的作者输入与宿主事件
  </ApiSourceLink>
</p>

```ts
export type SectorProps = InputSector &
  HydrationEventProps & {
    id?: string;
  };
```

### Arc

<p>
  <ApiSourceLink label={'Arc'} path={'packages/library/standard-react/src/shape/Arc.tsx'} startLine={12}>
    通过 Vanilla adapter 声明 Standard Arc
  </ApiSourceLink>
</p>

```ts
export declare const Arc: {
  (props: P, context?: any): ReactNode;
};
```

### Sector

<p>
  <ApiSourceLink label={'Sector'} path={'packages/library/standard-react/src/shape/Sector.tsx'} startLine={12}>
    通过 Vanilla adapter 声明 Standard Sector
  </ApiSourceLink>
</p>

```ts
export declare const Sector: {
  (props: P, context?: any): ReactNode;
};
```

## Vanilla 输入与适配器

### InputArc

<p>
  <ApiSourceLink label={'InputArc'} path={'packages/library/standard-vanilla/src/shape/arc.ts'} startLine={8}>
    Arc 的 Vanilla 作者输入
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员              | 类型                                  | 默认值 | 说明                         |
| ----------------- | ------------------------------------- | ------ | ---------------------------- |
| `animations?`     | `…`                                   | —      | —                            |
| `arrow?`          | `InputPathArrowDirectionValue`        | —      | 路径级箭头方向               |
| `arrowDetail?`    | `IRArrowDetail`                       | —      | 箭头的顶层默认与端点覆盖配置 |
| `arrowPlacement?` | `InputPathArrowPlacement`             | —      | 箭头端点的共享与逐端放置配置 |
| `center`          | `InputTarget`                         | —      | —                            |
| `close?`          | `"chord" \| "open" \| "sector"`       | —      | —                            |
| `endAngle?`       | `number`                              | —      | —                            |
| `label?`          | `…`                                   | —      | —                            |
| `marks?`          | `…`                                   | —      | —                            |
| `meta?`           | `Record<string, JsonValue>`           | —      | —                            |
| `radius`          | `number \| { x: number; y: number; }` | —      | —                            |
| `rotate?`         | `number`                              | —      | —                            |
| `roundedCorners?` | `number`                              | —      | —                            |
| `scale?`          | `number \| { x: number; y: number; }` | —      | —                            |
| `startAngle?`     | `number`                              | —      | —                            |
| `style?`          | `…`                                   | —      | —                            |
| `sweepAngle?`     | `number`                              | —      | —                            |
| `thickness?`      | `PathThicknessValue`                  | —      | 路径描边宽度语法糖           |
| `zIndex?`         | `number`                              | —      | —                            |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type InputArc = InputShape<IRArc>;
```

</DocTab>

</DocTabs>

### InputSector

<p>
  <ApiSourceLink label={'InputSector'} path={'packages/library/standard-vanilla/src/shape/sector.ts'} startLine={8}>
    Sector 的 Vanilla 作者输入
  </ApiSourceLink>
</p>

```ts
export type InputSector = InputShape<IRSector>;
```

### ArcInputEmbedAdapter

<p>
  <ApiSourceLink
    label={'ArcInputEmbedAdapter'}
    path={'packages/library/standard-vanilla/src/shape/arc.ts'}
    startLine={10}
  >
    将 Arc 作者输入归一为持久化的 Standard 意图
  </ApiSourceLink>
</p>

```ts
export declare const ArcInputEmbedAdapter: InputEmbedAdapter<InputArc & { id?: string }>;
```

### SectorInputEmbedAdapter

<p>
  <ApiSourceLink
    label={'SectorInputEmbedAdapter'}
    path={'packages/library/standard-vanilla/src/shape/sector.ts'}
    startLine={10}
  >
    将 Sector 作者输入归一为持久化的 Standard 意图
  </ApiSourceLink>
</p>

```ts
export declare const SectorInputEmbedAdapter: InputEmbedAdapter<InputSector & { id?: string }>;
```

## Source 与能力装配

### IRArc

<p>
  <ApiSourceLink label={'IRArc'} path={'packages/library/standard/src/composites/shape/arc/types.ts'} startLine={6}>
    持久化的 Arc 形状
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员              | 类型                                  | 默认值 | 说明 |
| ----------------- | ------------------------------------- | ------ | ---- |
| `animations?`     | `…`                                   | —      | —    |
| `center`          | `…`                                   | —      | —    |
| `close?`          | `"chord" \| "open" \| "sector"`       | —      | —    |
| `endAngle?`       | `number`                              | —      | —    |
| `id?`             | `string`                              | —      | —    |
| `label?`          | `…`                                   | —      | —    |
| `marks?`          | `…`                                   | —      | —    |
| `meta?`           | `Record<string, JsonValue>`           | —      | —    |
| `namespace`       | `"standard"`                          | —      | —    |
| `radius`          | `number \| { x: number; y: number; }` | —      | —    |
| `rotate?`         | `number`                              | —      | —    |
| `roundedCorners?` | `number`                              | —      | —    |
| `scale?`          | `number \| { x: number; y: number; }` | —      | —    |
| `startAngle?`     | `number`                              | —      | —    |
| `style?`          | `…`                                   | —      | —    |
| `sweepAngle?`     | `number`                              | —      | —    |
| `type`            | `"arc"`                               | —      | —    |
| `zIndex?`         | `number`                              | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRArc = ShapeSource<ZodInput<typeof ArcSchema>>;
```

</DocTab>

</DocTabs>

### IRSector

<p>
  <ApiSourceLink
    label={'IRSector'}
    path={'packages/library/standard/src/composites/shape/sector/types.ts'}
    startLine={6}
  >
    持久化的 Sector 形状
  </ApiSourceLink>
</p>

```ts
export type IRSector = ShapeSource<ZodInput<typeof SectorSchema>>;
```

### ArcDefinition

<p>
  <ApiSourceLink
    label={'ArcDefinition'}
    path={'packages/library/standard/src/composites/shape/arc/definition.ts'}
    startLine={7}
  >
    Arc 的 Core composite 注册项
  </ApiSourceLink>
</p>

```ts
export const ArcDefinition = defineComposite({
  namespace: 'standard',
  type: 'arc',
  schema: ArcSchema,
  expand: (source: IRArc) => ({ children: [lowerArc(source)] }),
});
```

### ArcProvider

<p>
  <ApiSourceLink
    label={'ArcProvider'}
    path={'packages/library/standard/src/composites/shape/arc/provider.ts'}
    startLine={5}
  >
    Arc 的按需依赖贡献
  </ApiSourceLink>
</p>

```ts
export const ArcProvider: CoreDependencyProvider = Object.freeze({
  key: Object.freeze({ capability: 'composite', namespace: 'standard', type: 'arc' }),
  dependencies: Object.freeze([]),
  datasets: Object.freeze({}),
  makeDefinition: () => ArcDefinition,
});
```

### SectorDefinition

<p>
  <ApiSourceLink
    label={'SectorDefinition'}
    path={'packages/library/standard/src/composites/shape/sector/definition.ts'}
    startLine={7}
  >
    Sector 的 Core composite 注册项
  </ApiSourceLink>
</p>

```ts
export const SectorDefinition = defineComposite({
  namespace: 'standard',
  type: 'sector',
  schema: SectorSchema,
  expand: (source: IRSector) => ({ children: [lowerSector(source)] }),
});
```

### SectorProvider

<p>
  <ApiSourceLink
    label={'SectorProvider'}
    path={'packages/library/standard/src/composites/shape/sector/provider.ts'}
    startLine={5}
  >
    Sector 的按需依赖贡献
  </ApiSourceLink>
</p>

```ts
export const SectorProvider: CoreDependencyProvider = Object.freeze({
  key: Object.freeze({ capability: 'composite', namespace: 'standard', type: 'sector' }),
  dependencies: Object.freeze([]),
  datasets: Object.freeze({}),
  makeDefinition: () => SectorDefinition,
});
```

### createArc

<p>
  <ApiSourceLink
    label={'createArc'}
    path={'packages/library/standard/src/composites/shape/arc/factory.ts'}
    startLine={4}
  >
    创建保留几何意图的 Arc Source
  </ApiSourceLink>
</p>

```ts
export declare const createArc: (input: ShapeProperties<IRArc>) => { animations?: Array<{ delay?: number; direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse'; duration: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> | [number, number, number, number]; fill?: 'none' | 'forwards' | 'backwards' | 'both'; iterations?: number | 'infinite'; keyframes: Array<{ at: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> | [number, number, number, number]; value: JsonValue }>; origin?: 'center' | 'top' | 'right' | 'bottom' | 'left' | 'top-right' | 'top-left' | 'bottom-right' | 'bottom-left' | [number, number]; property: OpenString<ValueOf<{ Fill: 'fill'; Opacity: 'opacity'; PathDraw: 'pathDraw'; Rotate: 'rotate'; Scale: 'scale'; ScaleX: 'scaleX'; ScaleY: 'scaleY'; Stroke: 'stroke'; StrokeWidth: 'strokeWidth'; TranslateX: 'translateX'; TranslateY: 'translateY'; ViewBox: 'viewBox' }>>; trigger?: 'load' | 'visible' | 'manual' | { onEvent: string } }>; center: [number, number] | SharedPolarPosition | IRBetweenPosition | { anchor?: number | OpenString<ValueOf<{ Bottom: 'bottom'; BottomLeft: 'bottom-left'; BottomRight: 'bottom-right'; Center: 'center'; Left: 'left'; Right: 'right'; Top: 'top'; TopLeft: 'top-left'; TopRight: 'top-right' }>> | { fraction: number; side: 'top' | 'right' | 'bottom' | 'left' }; boundary?: OpenString<ValueOf<{ Circle: 'circle'; Diamond: 'diamond'; Ellipse: 'ellipse'; Polygon: 'polygon'; Rectangle: 'rectangle'; Self: 'shape' }>> | { params?: Record<string, JsonValue>; type: OpenString<ValueOf<{ Circle: 'circle'; Diamond: 'diamond'; Ellipse: 'ellipse'; Polygon: 'polygon'; Rectangle: 'rectangle' }>> }; id: string; offset?: [number, number] } | { relative: [number, number] } | { relativeAccumulate: [number, number] } | { of: string | [number, number] | SharedPolarPosition; offset: [number, number] }; close?: 'chord' | 'open' | 'sector'; endAngle?: number; id?: string; label?: { distance?: number; font?: { family?: string; size?: string | number; style?: 'normal' | 'italic' | 'oblique'; weight?: number | 'normal' | 'bold' }; gap?: number; interrupt?: boolean; opacity?: number; placement?: 'outside' | 'inside'; position?: number | 'at-start' | 'very-near-start' | 'near-start' | 'midway' | 'near-end' | 'very-near-end' | 'at-end'; side?: 'top' | 'right' | 'bottom' | 'left'; sloped?: boolean; text: string | Array<string | { fill?: string | number; font?: { family?: string; size?: string | number; style?: 'normal' | 'italic' | 'oblique'; weight?: number | 'normal' | 'bold' }; opacity?: number; text: string } | { runs: Array<{ fill?: ... | ... | ...; font?: ... | ...; opacity?: ... | ...; text: string } | { displayMode?: ... | ... | ...; fill?: ... | ... | ...; opacity?: ... | ...; tex: string }> }>; textColor?: string | number }; marks?: Array<{ endpointOverlap?: number; mark: { color?: string | number; fill?: string | number; kind: 'arrow'; length?: number; lineWidth?: number; opacity?: number; scale?: number; shape?: OpenString<ValueOf<{ Circle: 'circle'; Normal: 'normal'; Open: 'open'; OpenCircle: 'openCircle'; OpenStealth: 'openStealth'; Stealth: 'stealth' }>>; width?: number }; pos: number }>; meta?: Record<string, JsonValue>; namespace: 'standard'; radius: number | { x: number; y: number }; rotate?: number; roundedCorners?: number; scale?: number | { x: number; y: number }; startAngle?: number; style?: { blendMode?: 'normal' | 'color' | 'multiply' | 'screen' | 'overlay' | 'darken' | 'lighten' | 'color-dodge' | 'color-burn' | 'hard-light' | 'soft-light' | 'difference' | 'exclusion' | 'hue' | 'saturation' | 'luminosity'; color?: string; dashOffset?: number; dashPattern?: Array<number>; fill?: string | number | { angle?: number; kind: 'linearGradient'; stops: Array<{ color: string; offset: number; opacity?: number }> } | { center?: [number, number]; kind: 'radialGradient'; radius?: number; stops: Array<{ color: string; offset: number; opacity?: number }> } | { angle?: number; center?: [number, number]; kind: 'conicGradient'; stops: Array<{ color: string; offset: number; opacity?: number }> } | { background?: string; color?: string; dashed?: boolean; dashOffset?: number; dashPattern?: Array<number>; dotted?: boolean; horizontalStyle?: { color?: string; dashed?: boolean; dashOffset?: number; dashPattern?: Array<number>; dotted?: boolean; lineCap?: 'butt' | 'round' | 'square'; lineJoin?: 'round' | 'miter' | 'bevel'; lineWidth?: number }; kind: 'pattern'; lineCap?: 'butt' | 'round' | 'square'; lineJoin?: 'round' | 'miter' | 'bevel'; lineStyleCycle?: { overrides: Array<{ index: number; style: { color?: ... | ...; dashed?: ... | ... | ...; dashOffset?: ... | ...; dashPattern?: ... | ...; dotted?: ... | ... | ...; lineCap?: ... | ... | ... | ...; lineJoin?: ... | ... | ... | ...; lineWidth?: ... | ... } }>; period: number }; lineWidth?: number; rotation?: number; shape: OpenString<ValueOf<{ Dots: 'dots'; Grid: 'grid'; Lines: 'lines' }>>; size?: number; verticalStyle?: { color?: string; dashed?: boolean; dashOffset?: number; dashPattern?: Array<number>; dotted?: boolean; lineCap?: 'butt' | 'round' | 'square'; lineJoin?: 'round' | 'miter' | 'bevel'; lineWidth?: number } } | { fit?: 'fill' | 'contain' | 'cover'; href: string; kind: 'image' }; fillOpacity?: number; fillRule?: 'nonzero' | 'evenodd'; lineCap?: 'butt' | 'round' | 'square'; lineJoin?: 'round' | 'miter' | 'bevel'; opacity?: number; shadow?: 'none' | 'sm' | 'lg' | 'xl' | '2xl' | 'md' | { blur?: number; color?: string; offsetX?: number; offsetY?: number; opacity?: number; preset?: 'none' | 'sm' | 'lg' | 'xl' | '2xl' | 'md' }; stroke?: string | number | { angle?: number; kind: 'linearGradient'; stops: Array<{ color: string; offset: number; opacity?: number }> } | { center?: [number, number]; kind: 'radialGradient'; radius?: number; stops: Array<{ color: string; offset: number; opacity?: number }> } | { angle?: number; center?: [number, number]; kind: 'conicGradient'; stops: Array<{ color: string; offset: number; opacity?: number }> } | { background?: string; color?: string; dashed?: boolean; dashOffset?: number; dashPattern?: Array<number>; dotted?: boolean; horizontalStyle?: { color?: string; dashed?: boolean; dashOffset?: number; dashPattern?: Array<number>; dotted?: boolean; lineCap?: 'butt' | 'round' | 'square'; lineJoin?: 'round' | 'miter' | 'bevel'; lineWidth?: number }; kind: 'pattern'; lineCap?: 'butt' | 'round' | 'square'; lineJoin?: 'round' | 'miter' | 'bevel'; lineStyleCycle?: { overrides: Array<{ index: number; style: { color?: ... | ...; dashed?: ... | ... | ...; dashOffset?: ... | ...; dashPattern?: ... | ...; dotted?: ... | ... | ...; lineCap?: ... | ... | ... | ...; lineJoin?: ... | ... | ... | ...; lineWidth?: ... | ... } }>; period: number }; lineWidth?: number; rotation?: number; shape: OpenString<ValueOf<{ Dots: 'dots'; Grid: 'grid'; Lines: 'lines' }>>; size?: number; verticalStyle?: { color?: string; dashed?: boolean; dashOffset?: number; dashPattern?: Array<number>; dotted?: boolean; lineCap?: 'butt' | 'round' | 'square'; lineJoin?: 'round' | 'miter' | 'bevel'; lineWidth?: number } } | { fit?: 'fill' | 'contain' | 'cover'; href: string; kind: 'image' }; strokeOpacity?: number; strokeWidth?: number }; sweepAngle?: number; type: 'arc'; zIndex?: number };
```

#### 参数

| 参数    | 类型                     | 说明 |
| ------- | ------------------------ | ---- |
| `input` | `ShapeProperties<IRArc>` | —    |

### createSector

<p>
  <ApiSourceLink
    label={'createSector'}
    path={'packages/library/standard/src/composites/shape/sector/factory.ts'}
    startLine={4}
  >
    创建保留几何意图的 Sector Source
  </ApiSourceLink>
</p>

```ts
export declare const createSector: (input: ShapeProperties<IRSector>) => IRSector;
```

#### 参数

| 参数    | 类型                        | 说明 |
| ------- | --------------------------- | ---- |
| `input` | `ShapeProperties<IRSector>` | —    |
