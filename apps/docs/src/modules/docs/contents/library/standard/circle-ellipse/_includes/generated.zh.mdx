{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## React 入口

### CircleProps

<p>
  <ApiSourceLink label={'CircleProps'} path={'packages/library/standard-react/src/shape/Circle.tsx'} startLine={9}>
    React Circle 的作者输入与宿主事件
  </ApiSourceLink>
</p>

```ts
export type CircleProps = InputCircle &
  HydrationEventProps & {
    id?: string;
  };
```

### EllipseProps

<p>
  <ApiSourceLink label={'EllipseProps'} path={'packages/library/standard-react/src/shape/Ellipse.tsx'} startLine={9}>
    React Ellipse 的作者输入与宿主事件
  </ApiSourceLink>
</p>

```ts
export type EllipseProps = InputEllipse &
  HydrationEventProps & {
    id?: string;
  };
```

### Circle

<p>
  <ApiSourceLink label={'Circle'} path={'packages/library/standard-react/src/shape/Circle.tsx'} startLine={12}>
    通过 Vanilla adapter 声明 Standard Circle
  </ApiSourceLink>
</p>

```ts
export declare const Circle: {
  (props: P, context?: any): ReactNode;
};
```

### Ellipse

<p>
  <ApiSourceLink label={'Ellipse'} path={'packages/library/standard-react/src/shape/Ellipse.tsx'} startLine={12}>
    通过 Vanilla adapter 声明 Standard Ellipse
  </ApiSourceLink>
</p>

```ts
export declare const Ellipse: {
  (props: P, context?: any): ReactNode;
};
```

## Vanilla 输入与适配器

### InputCircle

<p>
  <ApiSourceLink label={'InputCircle'} path={'packages/library/standard-vanilla/src/shape/circle.ts'} startLine={8}>
    Circle 的 Vanilla 作者输入
  </ApiSourceLink>
</p>

```ts
export type InputCircle = InputShape<IRCircle>;
```

### InputEllipse

<p>
  <ApiSourceLink label={'InputEllipse'} path={'packages/library/standard-vanilla/src/shape/ellipse.ts'} startLine={8}>
    Ellipse 的 Vanilla 作者输入
  </ApiSourceLink>
</p>

```ts
export type InputEllipse = InputShape<IREllipse>;
```

### CircleInputEmbedAdapter

<p>
  <ApiSourceLink
    label={'CircleInputEmbedAdapter'}
    path={'packages/library/standard-vanilla/src/shape/circle.ts'}
    startLine={10}
  >
    将 Circle 作者输入归一为持久化的 Standard 意图
  </ApiSourceLink>
</p>

```ts
export declare const CircleInputEmbedAdapter: InputEmbedAdapter<InputCircle & { id?: string }>;
```

### EllipseInputEmbedAdapter

<p>
  <ApiSourceLink
    label={'EllipseInputEmbedAdapter'}
    path={'packages/library/standard-vanilla/src/shape/ellipse.ts'}
    startLine={10}
  >
    将 Ellipse 作者输入归一为持久化的 Standard 意图
  </ApiSourceLink>
</p>

```ts
export declare const EllipseInputEmbedAdapter: InputEmbedAdapter<InputEllipse & { id?: string }>;
```

## Source 与能力装配

### IRCircle

<p>
  <ApiSourceLink label={'IRCircle'} path={'packages/library/standard/src/shape/circle/types.ts'} startLine={6}>
    持久化的 Circle 形状
  </ApiSourceLink>
</p>

```ts
export type IRCircle = ShapeSource<ZodInput<typeof CircleSchema>>;
```

### IREllipse

<p>
  <ApiSourceLink label={'IREllipse'} path={'packages/library/standard/src/shape/ellipse/types.ts'} startLine={6}>
    持久化的 Ellipse 形状
  </ApiSourceLink>
</p>

```ts
export type IREllipse = ShapeSource<ZodInput<typeof EllipseSchema>>;
```

### CircleDefinition

<p>
  <ApiSourceLink
    label={'CircleDefinition'}
    path={'packages/library/standard/src/shape/circle/definition.ts'}
    startLine={7}
  >
    Circle 的 Core composite 注册项
  </ApiSourceLink>
</p>

```ts
export const CircleDefinition = defineComposite({
  namespace: 'standard',
  type: 'circle',
  schema: CircleSchema,
  expand: (source: IRCircle) => ({ children: [lowerCircle(source)] }),
});
```

### CircleProvider

<p>
  <ApiSourceLink label={'CircleProvider'} path={'packages/library/standard/src/shape/circle/provider.ts'} startLine={5}>
    Circle 的按需依赖贡献
  </ApiSourceLink>
</p>

```ts
export const CircleProvider: CoreDependencyProvider = Object.freeze({
  key: Object.freeze({ capability: 'composite', namespace: 'standard', type: 'circle' }),
  dependencies: Object.freeze([]),
  datasets: Object.freeze({}),
  makeDefinition: () => CircleDefinition,
});
```

### EllipseDefinition

<p>
  <ApiSourceLink
    label={'EllipseDefinition'}
    path={'packages/library/standard/src/shape/ellipse/definition.ts'}
    startLine={7}
  >
    Ellipse 的 Core composite 注册项
  </ApiSourceLink>
</p>

```ts
export const EllipseDefinition = defineComposite({
  namespace: 'standard',
  type: 'ellipse',
  schema: EllipseSchema,
  expand: (source: IREllipse) => ({ children: [lowerEllipse(source)] }),
});
```

### EllipseProvider

<p>
  <ApiSourceLink
    label={'EllipseProvider'}
    path={'packages/library/standard/src/shape/ellipse/provider.ts'}
    startLine={5}
  >
    Ellipse 的按需依赖贡献
  </ApiSourceLink>
</p>

```ts
export const EllipseProvider: CoreDependencyProvider = Object.freeze({
  key: Object.freeze({ capability: 'composite', namespace: 'standard', type: 'ellipse' }),
  dependencies: Object.freeze([]),
  datasets: Object.freeze({}),
  makeDefinition: () => EllipseDefinition,
});
```

### createCircle

<p>
  <ApiSourceLink label={'createCircle'} path={'packages/library/standard/src/shape/circle/factory.ts'} startLine={4}>
    创建保留几何意图的 Circle Source
  </ApiSourceLink>
</p>

```ts
export declare const createCircle: (input: ShapeProperties<IRCircle>) => IRCircle;
```

#### 参数

| 参数    | 类型                        | 说明 |
| ------- | --------------------------- | ---- |
| `input` | `ShapeProperties<IRCircle>` | —    |

### createEllipse

<p>
  <ApiSourceLink label={'createEllipse'} path={'packages/library/standard/src/shape/ellipse/factory.ts'} startLine={4}>
    创建保留几何意图的 Ellipse Source
  </ApiSourceLink>
</p>

```ts
export declare const createEllipse: (input: ShapeProperties<IREllipse>) => IREllipse;
```

#### 参数

| 参数    | 类型                         | 说明 |
| ------- | ---------------------------- | ---- |
| `input` | `ShapeProperties<IREllipse>` | —    |
