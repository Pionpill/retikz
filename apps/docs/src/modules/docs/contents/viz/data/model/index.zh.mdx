---
title: 数据模型
description: 数据模型把外部数据行接到稳定的字段契约上，生成供数据变换与不同消费模块复用的规范化行。
---

数据模型负责回答四个问题：配置里引用的字段是什么、外部数据怎样接入这些字段、遇到坏值时怎样诊断、内置能力不够时怎样扩展。真实数据行始终在运行时传入，不写进可序列化配置；同一份字段契约因此可以复用到不同数据源。

## 整体流程

<ComponentPreview files="data-model-pipeline" size="sm" hideCode />

图中的实线表示数据主链：外部行经规范化后，才交给数据变换与消费模块。字段契约是规范化阶段的输入，负责提供逻辑字段名、类型、解析格式与分类顺序；它描述数据，不包含数据行，也不绑定某一种消费模块。

## 职责一览

| 主题       | 负责什么                                     | 何时阅读                               |
| ---------- | -------------------------------------------- | -------------------------------------- |
| 字段契约   | 声明逻辑字段、测量类型、自动推断与分类顺序   | 要让配置稳定、自描述或控制分类域时     |
| 数据接入   | 对齐真实字段路径，解析存储格式并生成规范化行 | 要换接口、接嵌套数据或处理非默认格式时 |
| 校验与诊断 | 决定非法值是跳过、抽样检查还是全量报错       | 排查空图、建立数据准入或测试契约时     |
| 扩展机制   | 注册字段格式与运行时字段解析                 | 内置接入能力无法表达存储格式时         |

数据模型负责到生成规范化行为止：数据怎样排序、汇总或筛选，由[数据变换](/viz/data/transform)负责；结果怎样进入具体视图，由消费模块决定。在 Plot 应用中，字段进入位置、颜色或尺寸后的映射由 [Plot 比例尺](/viz/plot/scale)负责。

## 章节内容

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/viz/data/model/contract">
    <span className="font-semibold">字段契约</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">声明字段类型、部分契约、自动推断与分类顺序。</span>
  </LinkedCard>
  <LinkedCard href="/viz/data/model/intake">
    <span className="font-semibold">数据接入</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">对齐字段路径、解析格式并生成规范化行。</span>
  </LinkedCard>
  <LinkedCard href="/viz/data/model/validation">
    <span className="font-semibold">校验与诊断</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">选择非法值策略，并把空图转成可定位的错误。</span>
  </LinkedCard>
  <LinkedCard href="/viz/data/model/extensions">
    <span className="font-semibold">扩展机制</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">注册字段格式或提供运行时字段解析。</span>
  </LinkedCard>
</div>
