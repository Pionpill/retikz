---
title: Color Scales
description: Color scales map categorical, continuous, and midpoint-centered data to discrete colors, continuous ramps, or binned colors.
---

A color scale answers which color a data value should use. The scale establishes the data-to-color mapping; `Legend` then explains that mapping to the reader. Adding a legend does not change the scale type or palette.

| Data and task                              | Recommended scale                     | Legend form  |
| ------------------------------------------ | ------------------------------------- | ------------ |
| Categorical field                          | `ordinal`                             | swatches     |
| Continuous / temporal field                | `sequential`                          | ramp         |
| Continuous number with a meaningful center | `diverging`                           | ramp         |
| Continuous quantity that needs bins        | `quantize` / `threshold` / `quantile` | bin swatches |

## Categorical color: ordinal

Bind `color` to a categorical field and `<Plot>` derives `ordinal`, assigning one discrete color per category. A categorical color field also splits path data into multiple series when no explicit series is present.

<ComponentPreview
  size="md"
  files={['scale-ordinal', 'scale-ordinal.data.ts']}
  caption="Switch discrete palettes and show or hide the legend to inspect the separate responsibilities of color mapping and legend explanation."
/>

`ordinal` does not interpolate between categories. For stable domain colors, provide an explicit `domain` and same-order `range` in the full spec. With an inferred domain, category order comes from the data.

## Continuous color: sequential and diverging

`sequential` uses one low-to-high ramp for a continuous quantity. `diverging` uses a three-value domain for the low end, center, and high end, which suits profit/loss, deviation, temperature delta, and other data centered on a reference value.

<ComponentPreview
  size="md"
  files={['scale-continuous', 'scale-continuous.data.ts']}
  caption="Switch between sequential and diverging, choose a scheme, and move the midpoint. The same -50 to 50 values reveal the mapping difference immediately."
/>

Both scale types accept serializable named schemes. Common sequential schemes include `viridis`, `blues`, `greens`, and `magma`; common diverging schemes include `rdbu`, `brbg`, and `spectral`. Use `range` when custom colors should replace the scheme.

Continuous color works only on marks colored independently per datum, such as point, interval, rect, and sector. A path or area is colored as one mark, so it cannot bind a continuous color field that changes along the path.

<ComponentAlert
  title="Auto-derivation needs a field contract"
  description="React derives sequential only when the model declares the color field as continuous or temporal; an unknown field type still follows ordinal behavior. Diverging must be declared explicitly through a full IRPlotSpec."
/>

## Discretization: quantize / threshold / quantile

A discretization scale still consumes continuous data, but returns only a finite set of colors. The three types differ in how they choose boundaries:

| Type        | Boundaries                                       | Suited task                                  |
| ----------- | ------------------------------------------------ | -------------------------------------------- |
| `quantize`  | split an explicit domain into `count` equal bins | equal numeric intervals, graded statistics   |
| `threshold` | use domain-specific `breakpoints`                | alert lines, policy lines, rating standards  |
| `quantile`  | derive quantiles with roughly equal sample count | skewed distributions, reduced outlier impact |

<ComponentPreview
  size="md"
  files={['scale-discretization', 'scale-discretization.data.ts']}
  caption="Switch among quantize, threshold, and quantile. Tune the bin count, business thresholds, and palette to compare how the same skewed data falls into different color bins."
/>

Configuration constraints:

- `threshold.breakpoints` must be strictly ascending; bin count is one more than the breakpoint count, and an explicit `range` must match
- `count` defaults to `5` for `quantize` / `quantile`; when only `range` is given, its length becomes the bin count
- when both `count` and `range` are present, their lengths must agree
- `quantile` derives boundaries from the actual data and does not use an explicit numeric domain

## How it works

React auto-derivation covers two common paths:

- a categorical color field derives `ordinal`
- a color field declared as `continuous` or `temporal` derives `sequential`

Configure `diverging`, the three discretization scales, and explicit scheme / range / domain options in `IRPlotSpec.scales`, then reference the scale name from the mark's color field binding. `<Legend channel="color" />` or a full-spec legend guide only displays the resolved mapping.

Auto-derivation and explicit configuration are only two entry points: both are resolved by the same scale definition and resolver path, rather than creating separate color semantics.

<ComponentPreview files="scale-color-flow" size="xs" hideCode />

The resolver derives the domain, bin boundaries, and scheme / range for the selected scale type, producing one resolved scale with its color mapping and required boundary metadata. Marks use it to color data, while Legend reads that same result to generate a ramp or bin swatches, so adding a legend does not change the mapping.

<SourceLinks
  sources={[
    {
      label: 'React derives a color scale from field type',
      path: 'packages/viz/plot-react/src/components/build-plot-spec.ts',
      startLine: 1657,
      endLine: 1670,
    },
    {
      label: 'Resolve ordinal, sequential, and diverging color',
      path: 'packages/viz/plot/src/providers/scale/features/color.ts',
      startLine: 320,
      endLine: 390,
    },
    {
      label: 'Resolve quantize, threshold, and quantile color',
      path: 'packages/viz/plot/src/providers/scale/features/color.ts',
      startLine: 393,
      endLine: 449,
    },
  ]}
/>

## API Reference

Every color scale in a full spec needs a `name`, which a mark's color field binding references through `scale`. Auto-derivation covers only `ordinal` and `sequential`; other types and explicit domain / scheme / range options belong in `IRPlotSpec.scales`.

| `type`       | Input and boundaries                         | Palette and defaults                              |
| ------------ | -------------------------------------------- | ------------------------------------------------- |
| `ordinal`    | categorical `domain`, inferred in order      | discrete `range`; Plot palette when omitted       |
| `sequential` | two-value `domain`, inferred from data       | `scheme` defaults to `viridis`; two-color `range` |
| `diverging`  | three-value low / mid / high `domain`        | `scheme` defaults to `rdbu`; three-color `range`  |
| `quantize`   | two-value `domain`; `count` defaults `5`     | sampled scheme or explicit discrete `range`       |
| `threshold`  | required ascending `breakpoints`             | one more color than breakpoints                   |
| `quantile`   | data-derived quantiles; `count` defaults `5` | sampled scheme or explicit discrete `range`       |

An explicit `range` overrides `scheme`. When `quantize` / `quantile` provide both `count` and `range`, their lengths must agree. See [Plot IR](/viz/reference/plot-ir) for the complete schemas and custom scale operations.

<SourceLinks
  sources={[
    {
      label: 'Ordinal color scale schema',
      path: 'packages/viz/plot/src/schemas/scale/schema.ts',
      startLine: 111,
      endLine: 129,
    },
    {
      label: 'Sequential and diverging schemas',
      path: 'packages/viz/plot/src/schemas/scale/schema.ts',
      startLine: 282,
      endLine: 338,
    },
    {
      label: 'Discretization schemas',
      path: 'packages/viz/plot/src/schemas/scale/schema.ts',
      startLine: 340,
      endLine: 431,
    },
  ]}
/>

## Related

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/viz/plot/scale/position">
    <span className="font-semibold">Positional Scales</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Choose positional mappings for numeric, temporal, and categorical fields
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/plot/guide/legend">
    <span className="font-semibold">Legend</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Explain a color mapping with swatches or a ramp
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/data/model/contract">
    <span className="font-semibold">Field Contract</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Declare categorical, continuous, and temporal field types
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/plot/scale/custom-scale">
    <span className="font-semibold">Custom scales</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Register a scale definition for domain-specific mappings
    </span>
  </LinkedCard>
</div>
