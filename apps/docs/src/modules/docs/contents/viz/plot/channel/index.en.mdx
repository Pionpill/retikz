---
title: Channels
description: 'Channels connect fields or fixed values to visual results such as position, color, and size.'
---

A channel describes two things: where a value comes from and which visual result it controls. For example, `x="gdp"` uses the `gdp` field for position, while `color="region"` uses the `region` field for color.

## Responsibilities

| Topic             | Problem it solves                                        | Reach for it when                                                        |
| ----------------- | -------------------------------------------------------- | ------------------------------------------------------------------------ |
| Channel binding   | How fields or fixed values are written on mark props     | You want to confirm what `x="date"`, `color="region"`, or `y={80}` means |
| Built-in channels | Which options control position, color, size, text, paths | You want to choose channels such as `color`, `size`, or `strokeWidth`    |

## In This Section

For a first read, start with how fields and constants bind, then look up built-in channels by visual goal.

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/viz/plot/channel/binding">
    <span className="font-semibold">Channel Binding</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Bind fields or fixed values to position, color, size, and related channels
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/plot/channel/builtin">
    <span className="font-semibold">Built-In Channels</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Look up channels for position, color, size, text, and paths
    </span>
  </LinkedCard>
</div>

To add a project-specific visual channel, continue to [Custom Channels](/viz/plot/extend/channel).

Channels decide which value to read; [Scales](/viz/plot/scale) decide how values map, and [Coordinates](/viz/plot/coordinate) decide how positional roles project.

## How It Works (Optional On A First Read)

React mark props are assembled into PlotSpec, a serializable drawing configuration. Position channels are interpreted by the coordinate system. Other channels use a shared channel registry to find their handling rules, then resolve into visible properties or legend information.

<ComponentPreview files="channel-flow" size="lg" hideCode />

PlotSpec stores field or constant bindings, not functions. Built-ins and custom channels injected through `channelDefinitions` share the same registration, resolution, and delivery path, so extending channels does not require another configuration format.

<SourceLinks
  sources={[
    {
      label: 'Position bindings are read through coordinate roles',
      path: 'packages/viz/plot/src/pipeline/expand/frame/resolve.ts',
      startLine: 244,
      endLine: 256,
    },
    {
      label: 'Built-in and custom channels merge into one registry',
      path: 'packages/viz/plot/src/providers/channel/registry.ts',
      startLine: 77,
      endLine: 114,
    },
    {
      label: 'Bindings resolve and dispatch by channel type',
      path: 'packages/viz/plot/src/providers/channel/registry.ts',
      startLine: 120,
      endLine: 190,
    },
  ]}
/>
