---
title: Legend
description: 'Visualize non-positional channels (color / size / opacity / shape) as a legend with <Legend>; channel picks which channel, form follows the scale type.'
---

`<Legend>` is the visual counterpart of a scale. An axis (`<Axis>`) explains the two positional channels x / y; a legend explains the rest — the non-positional channels `color`, `size`, `opacity`, and `shape`. Both are chart guides, side by side.

`channel` picks which channel the legend explains. You don't pick its form — it follows the type of the scale bound to that channel: categorical color → one swatch per category; continuous color → a smooth ramp + ticks; binned color → one block per bin + range labels; size → a few representative circles (graduated symbols).

For a legend to explain a channel, that channel must first have a data-driven scale. So bind a field to the channel on `<PointMark>` (`color="region"` / `size="pop"`), then add `<Legend channel="..." />`. Continuous fields (continuous color ramps, size gradients) also need `model` to declare the field type, otherwise they are treated as categorical.

`<Legend>` and `<Axis>` are independent: adding `<Legend>` only controls the legend, while axes come from the `<Axis>` you write explicitly (`<Plot>` draws none on its own).

## Usage

```tsx
import { Plot, PointMark, Legend } from '@retikz/plot-react';
```

```tsx
<Plot data={cities}>
  <PointMark x="lng" y="lat" color="region" size="pop" />
  <Legend channel="color" />
  <Legend channel="size" position="bottom" />
</Plot>
```

## Examples

### Categorical color swatches

`color` bound to a categorical field → an ordinal color scale, one swatch per category. The x / y axes come from explicit `<Axis>`.

<ComponentPreview files={['legend-color-swatch', 'legend.data.ts']} />

<p className="text-sm text-muted-foreground">One swatch per region; the legend sits in the reserved band on the right; x / y axes drawn by explicit `<Axis>`.</p>

### Continuous color ramp

`color` bound to a continuous numeric field (declared `continuous` via `model`) → a sequential color scale, drawn as a smooth ramp + ticks. Use `ticks` for tick source and `tickLabels.format` for display format.

<ComponentPreview files={['legend-color-ramp', 'legend.data.ts']} />

<p className="text-sm text-muted-foreground">Continuous values form a smooth ramp with a few tick values beside it.</p>

### Size graduated symbols

`size` bound to a continuous field → radius via a square-root scale; the legend draws a few representative sizes as circles + values (graduated symbols). `position="bottom"` puts the legend at the bottom.

<ComponentPreview files={['legend-size', 'legend.data.ts']} />

<p className="text-sm text-muted-foreground">
  A few representative sizes map size to value; the legend sits at the bottom.
</p>

### Theme and local style

Legend defaults come from `Plot.theme.legend` and `Plot.theme.palette`. A single `<Legend>` can still override its own layout and text tokens with `style`; semantic fields such as `channel`, `position`, `orient`, `ticks`, and `tickLabels.format` stay on the root `<Legend>`.

```tsx
<Plot
  theme={{
    palette: { sequential: 'magma' },
    legend: { rampLength: 120, label: { textColor: '#475569' } },
  }}
>
  <Legend
    channel="color"
    ticks={{ count: 4 }}
    tickLabels={{ format: '~s' }}
    style={{ title: { font: { weight: 600 } } }}
  />
</Plot>
```

### Layer Order

Legends are drawn above axes, marks, and plot-level labels by default, with zIndex `500`. Use `layer={{ zIndex }}` on `<Legend>` to move the whole legend scope up or down; it does not change the order of entries inside the legend.

## API

| Prop         | Type                                        | Default       | Description                                                                                             |
| ------------ | ------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------- |
| `channel`    | `'color' \| 'size' \| 'opacity' \| 'shape'` | —             | Which non-positional channel the legend explains; form follows the bound scale type                     |
| `scale`      | `string?`                                   | —             | Disambiguating scale name when one channel is driven by several scales; omit = the channel's only scale |
| `title`      | `string \| TextBlock?`                      | none          | Legend title content; accepts the same text-block form as core text nodes                               |
| `position`   | `'right' \| 'left' \| 'top' \| 'bottom'`    | `'right'`     | Which edge the legend sits on (a band is reserved there)                                                |
| `orient`     | `'vertical' \| 'horizontal'`                | by `position` | Item layout direction; left/right default to vertical, top/bottom to horizontal                         |
| `ticks`      | `{ count?; values? }`                       | scale default | Tick source for continuous ramps; explicit `values` win over `count`; ignored for discrete swatches     |
| `tickLabels` | `false \| { format? }`                      | `true`        | Hide labels with `false`, or format ramp tick labels with a declarative format string                   |
| `style`      | `LegendGuideStyle?`                         | theme legend  | Local legend visual tokens: swatch / ramp size, gaps, and title / label text style                      |

## Related

- [Axis](/viz/plot/guide/axis) — the guide for the x / y positional channels
- [Scale](/viz/plot/scale) — the legend form follows the channel's scale type
- [Point](/viz/plot/mark/point) — binding the color / size / shape channels
