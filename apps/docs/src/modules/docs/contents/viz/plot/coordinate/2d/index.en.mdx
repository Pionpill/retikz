---
title: 2D Coordinates
description: '2D coordinates project x and y position channels onto Cartesian or polar planes while point, line, and area marks reuse the same data bindings.'
---

2D coordinates decide how two position channels land on a plane. Data fields first bind to `x` / `y` and pass through position scales; the coordinate then projects them into horizontal / vertical positions or angle / radius. Switching marks does not change those bindings.

## Cartesian 2D Coordinates

Cartesian coordinates map `x` horizontally and `y` vertically, making them suitable for comparing categories and values along shared straight axes. The playground fixes the coordinate and six data rows while letting you switch marks, adjust plot-area margins, and toggle the y grid:

- Points use `<PointMark>` to show the position of each row.
- Lines use an open `<PathMark>` to connect those positions by `order`.
- Areas use `<IntervalMark>` to occupy each category band and extend upward from the `y = 0` x-axis baseline.

<ComponentPreview
  files={['coordinate-cartesian', 'coordinate-2d.data.ts']}
  size="lg"
  caption="After switching point, line, and area marks, adjust all four margins and the y grid to see how the same x / y bindings adapt to the plot area."
/>

## Polar 2D Coordinates

Polar coordinates map `x` to angular positions around the center and `y` to radial distance. They reuse the same data and marks, but their projected relationships change:

- Points spread around the angle, with values determining their radii.
- Lines use a closed `<PathMark>` that connects the final point back to the first.
- Areas still use `<IntervalMark>`, producing radial intervals separated by category.

The playground also controls `innerRadius`, the start angle, and the sweep angle. Sweep angle is a teaching parameter in the panel; the coordinate receives `endAngle = startAngle + sweepAngle`.

<ComponentPreview
  files={['coordinate-polar', 'coordinate-2d.data.ts']}
  size="lg"
  caption="After switching point, line, and area marks, adjust the inner radius and angle range to see how polar projection changes the same position bindings."
/>

<Comparison target="tikz" title="TikZ Comparison">
TikZ typically distinguishes Cartesian and polar coordinates with the PGFPlots `axis` and `polaraxis` environments. retikz puts the projection on `<Plot coordinate>`, allowing the same mark to keep its original field bindings.
</Comparison>

## Further Reading

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/viz/plot/channel">
    <span className="font-semibold">Channels</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">Understand how x and y bind data fields</span>
  </LinkedCard>
  <LinkedCard href="/viz/plot/mark">
    <span className="font-semibold">Marks</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Continue with PointMark, PathMark, and IntervalMark
    </span>
  </LinkedCard>
</div>
