---
title: Extending
description: Extend the drawing grammar by registering long-tail channels, scales, coordinates, and marks into the same plot lowering flow.
---

This group covers how to **extend** Plot's drawing grammar. The built-ins cover common charts; when you need a new channel, scale, coordinate projection, or mark, runtime-injected definitions reuse the same parsing, validation, and lowering flow as built-in plot capabilities.

Data formats, field parsing, and data transforms belong to [Data Flow](/viz/data): custom field formats are documented under [Data Model](/viz/data/model), and custom transform / reducer / selector definitions are documented under [Data Transforms](/viz/data/transform). Plot can consume those data capabilities, but they are no longer part of this drawing-grammar extension group.

> Extension definitions are host capabilities and do not enter Plot IR directly. The IR stores only serializable types, names, field bindings, and JSON config; functions, projections, evaluators, and lowerers are injected through `<Plot>` props or vanilla options.

## What each part covers

| Topic              | Extends                                                   | Reach for it when                                                                        |
| ------------------ | --------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Custom Channels    | Position / mark / scope / node / path channel definitions | You need a new `encoding.channels.<name>` that lands data on a custom visual surface     |
| Custom Marks       | Mark definitions                                          | You need a new geometry layer that projects rows through the frame and assembles core IR |
| Custom Scales      | Scale definitions and named color schemes                 | You need brand ramps, domain mappings, or value mappings beyond the built-in scales      |
| Custom Coordinates | Coordinate definitions                                    | You need new position roles, projections, custom axes, or special spaces                 |

## Three Boundaries

- **Data and drawing are separate layers**: real rows, parsers, and data transforms belong to Data Flow; PlotSpec records drawing-related field bindings and serializable config.
- **Built-ins and custom definitions share one mechanism**: custom definitions do not use private patch points; they register, parse, validate, and consume through the same path as built-ins.
- **Lowering still returns to core**: custom coordinates and marks can decide how geometry is generated, but the final output should still be core Node / Path / Coordinate / Scope primitives.

## In This Section

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/viz/grammar/extend/channel">
    <span className="font-semibold">Custom Channels</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Declare new channel definitions and reuse the existing binding and delivery path.
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/grammar/extend/mark">
    <span className="font-semibold">Custom Marks</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Lower rows into custom core IR assemblies with mark definitions.
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/grammar/extend/scale">
    <span className="font-semibold">Custom Scales</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Inject position / channel scale definitions or add named color schemes.
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/grammar/extend/coordinate">
    <span className="font-semibold">Custom Coordinates</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Keep projections, roles, and custom axes in runtime coordinate definitions.
    </span>
  </LinkedCard>
</div>
