---
title: PointMark
description: PointMark 是最简单的图元：它在坐标系定位后绘制一个 Node，可作为散点、气泡、形状标记或文本点的底层能力。
---

`<PointMark>` 是最简单的图元：它先让每行数据在当前坐标系里得到一个位置，再在这个位置绘制一个 core `Node`。这个 Node 可以是默认圆点、形状标记、气泡、文本，或带标签的组合形态；散点图、气泡图和文本点都可以看作它的上层封装。

绑哪些位置通道由[坐标系](/viz/grammar/coordinate)决定：一维用 `x`，二维用 `x` / `y`，三元（ternary2D）用 `x` / `y` / `z`。这些属性在组件层都是可选的；当前坐标系会在编译期校验需要哪些维度。

## 用法

```tsx
import { Plot, PointMark } from '@retikz/plot-react';
```

```tsx
<Plot data={cities}>
  <PointMark x="lng" y="lat" />
</Plot>
```

PointMark 常和 PathMark 叠加——同一份数据先画 `<PathMark>` 再叠 `<PointMark>` 标出每个观测点。

## 表现层

PointMark 是底层点 mark：它只负责把每行数据投影成一个坐标位置，并把样式、文本等通道写到这个位置对应的 Node 上。表现层看到的“散点图”“气泡图”“文本点”不是不同的底层 mark，而是同一个 PointMark 在不同通道配置下的显示形态。

这也是 PointMark 通常不需要按坐标系单独适配的原因：坐标系只负责把位置角色投影成屏幕点，表现层再决定这个点画成什么。

| 表现形态 | 关键属性             | 说明                                                           |
| -------- | -------------------- | -------------------------------------------------------------- |
| 散点     | `x` / `y`            | 默认形态，每行数据绘制一个点；`color` / `shape` 继续装饰这组点 |
| 气泡图   | `size`               | 点半径由数值字段驱动，位置投影不变                             |
| 文本点   | `text` / `dx` / `dy` | 单独使用 `text` 时绘制文本 Node，可理解为表现层的 TextMark     |

## 例子

### 位置属性

`x` / `y` 决定每行数据的锚点。左侧是笛卡尔二维坐标系，右侧是极坐标二维坐标系；同一组字段在不同坐标系下会被解释成不同的几何位置。

<ComponentPreview name="point-position" sourceFiles={['point-api.data.ts']} />

### 图元局部变换

`transform` 可以直接写在当前 PointMark 上。下面的灰点仍然使用原始行，彩色气泡层先按 `region` 聚合出 `totalOrders`，再把变换后的字段绑定到 `y` / `size`。

<ComponentPreview name="point-transform" sourceFiles={['point-transform.data.ts']} />

### 样式属性

`color` / `size` / `opacity` / `shape` / `stroke` / `strokeWidth` 都可以作为点的内置通道。它们只改变点的可见形态，不改变点的坐标位置。

`stroke`、`strokeWidth`、`opacity`、`fillOpacity`、`rotate` 等 Node 样式都支持字段绑定。组件层的字符串 / 数字是语法糖：如果有同名数据字段，字符串会变成 `{ kind: 'field', value: FieldName }`；如果没有字段但能作为合法常量使用，就变成 `{ kind: 'constant', value }`；否则会跳过并给出 warning。需要消除歧义时可以直接写显式形式。

<ComponentPreview name="point-style" sourceFiles={['point-api.data.ts']} />

### 填充与描边

`fill` / `stroke` 除纯色外也可以接 `IRPaintSpec`。常量样式会下沉到 core `Node` 的默认样式；如果需要按数据字段映射颜色，继续使用 `color` 或字段绑定形式。

<ComponentPreview name="point-paint" sourceFiles={['point-api.data.ts']} />

### 文本属性

点上的文字优先使用 `label`：它会把文本挂到每个散点对应的 core `Node.label` 上，因此一层 `PointMark` 就能同时画点和显示标签。`labelPosition`、`labelDistance`、`labelPin` 对齐 core Node label 的方位、距离和引线能力。`text` 是另一种模式：它把点本身替换成无边框文本 Node，适合只画文字点而不画圆点的场景；`dx` / `dy` 用来做屏幕方向的微调。

<ComponentPreview name="point-text" sourceFiles={['point-api.data.ts']} />

### 不同坐标系

下面只看位置角色如何被坐标系解释：PointMark 自身不变，变的是 `x` / `y` / `z` 被投影到直线、圆环或三角形中的方式。

#### 一维：直线和极坐标

一维坐标系只消费 `x`。在直线坐标里它落到一条轴上，在极坐标里同一组值落到圆环上。

<ComponentPreview name="point-coordinate-1d" sourceFiles={['point-coordinates.data.ts']} />

#### 三元：三元图

三元坐标系消费 `x` / `y` / `z` 三个分量，并把每行数据归一化后投影到三角形内。

<ComponentPreview name="point-coordinate-ternary" sourceFiles={['point-coordinates.data.ts']} />

## 技术原理

`PointMark` 是一个内置注册图元。React 里的 `<PointMark>` 本身不渲染 DOM，也不直接画点；`<Plot>` 会在构造 `PlotSpec` 时把它收集成 `type: 'point'` 的 mark IR，并记录 `x` / `y` / `z` 等字段通道。

编译时，`lowerMark` 按 `mark.type` 查询 `MARK_REGISTRY`，`PointMark` 命中 `PlotMark.Point` 对应的 `lowerPoint`。`lowerPoint` 对每行数据取出位置通道值，再交给当前坐标系的 `frame.projectRoles(...)` 投影成屏幕点，最后下沉为 core `Node`。

这意味着散点最终复用 [@retikz/core 的 Node](/kernel/components/node/overview) 实现；`PointMark` 只负责从数据和坐标系计算位置，节点的形状、尺寸、填充、文本等视觉细节仍由 core Node 处理。

注册部分只需要看 `PointMark` 对应的注册行，以及真正执行下沉的 `lowerPoint`：

```ts
[PlotMark.Point]: { type: PlotMark.Point, lower: lowerPoint },

const lowerPoint = (mark: Mark, rows: Array<ExternalRow>, frame: CoordinateFrame, channels: MarkChannels, markProvenance: MarkProvenance | undefined): IRChild | null => {
  if (mark.type !== PlotMark.Point) return null;
  const { colorOf, defaultColor = DEFAULT_FILL, sizeOf, opacityOf, shapeOf, strokeOf, strokeWidthOf, labelOf } = channels;
  const isText = mark.encoding.text !== undefined;
  const dx = mark.dx ?? 0;
  const dy = mark.dy ?? 0;
  const placed: Array<{ color: string | undefined; node: IRNode }> = [];
  for (let transformedIndex = 0; transformedIndex < rows.length; transformedIndex++) {
    const row = rows[transformedIndex];
    if (isText) {
      // 文本点：投影同 point（roleValues + projectRoles，坐标系无关）；内容缺失跳过；dx/dy 锚点像素微调
      const point = frame.projectRoles(roleValues(mark, row, frame));
      if (!point) continue;
      const text = labelOf?.(row);
      if (text === undefined) continue;
      const position: [number, number] = dx === 0 && dy === 0 ? point : [point[0] + dx, point[1] + dy];
      const base: IRNode = { type: 'node', position, text };
      if (mark.zIndex !== undefined) base.zIndex = mark.zIndex;
      placed.push({ color: colorOf?.(row), node: decorateDatum(base, row, transformedIndex, mark.type, markProvenance, undefined) });
      continue;
    }
    // 散点：锚点与 locator 共享同一 datumAnchor（point → frame.projectRoles），杜绝两套投影漂移
    const point = datumAnchor(mark, row, frame);
    if (!point) continue;
    const base: IRNode = { type: 'node', position: point };
    if (mark.zIndex !== undefined) base.zIndex = mark.zIndex;
    const radius = sizeOf?.(row);
    if (radius !== undefined) base.minimumSize = radius * Math.SQRT2;
    const opacity = opacityOf?.(row);
    if (opacity !== undefined) base.opacity = opacity;
    const shape = shapeOf?.(row);
    if (shape !== undefined) base.shape = shape;
    const stroke = strokeOf?.(row);
    if (stroke !== undefined) base.stroke = stroke;
    const strokeWidth = strokeWidthOf?.(row);
    if (strokeWidth !== undefined) base.strokeWidth = strokeWidth;
    const node = attachDatumLabel(decorateDatum(base, row, transformedIndex, mark.type, markProvenance, undefined), mark, row, labelOf);
    placed.push({ color: colorOf?.(row), node });
  }
  if (placed.length === 0) return null;
  const layer: IRScope = !colorOf
    ? {
        type: 'scope',
        nodeDefault: isText ? textStyle(typeof mark.fill === 'string' ? mark.fill : defaultColor, mark) : pointStyle(mark.fill ?? defaultColor, mark),
        children: placed.map(p => p.node),
      }
    : colorGroupedScope(placed, fill => (isText ? textStyle(fill, mark) : pointStyle(fill, mark)));
  return attachMarkLayer(layer, mark, markProvenance);
};
```

这段代码的执行顺序很直接：

1. 先确认当前 mark 是 `PlotMark.Point`，否则不处理。
2. 每行数据通过 `datumAnchor` 或 `frame.projectRoles(...)` 投影到屏幕坐标；投影失败的行直接跳过。
3. 普通散点生成 core `Node`，并把 `size`、`opacity`、`shape`、`stroke`、`strokeWidth` 等通道或常量 Node 样式写到该 Node / `nodeDefault` 上；带 `label` 的点会填充 `Node.label`，文本点则生成文本 Node。
4. 最后按是否有 `color` 通道决定分层方式：没有颜色编码时共用一层 `nodeDefault`，有颜色编码时按颜色拆分子 `Scope`，再挂上图层元信息。

| 特性          | 说明                                                                                                   |
| ------------- | ------------------------------------------------------------------------------------------------------ |
| 坐标系无关    | 点只关心“位置角色”，不关心这些角色在 cartesian、polar、ternary 或 custom 坐标系里怎么解释              |
| 每行一个 Node | 普通散点会生成一个 datum Node；无效位置投影不到屏幕点时跳过该行                                        |
| 样式分层      | 无颜色编码时通过外层 `nodeDefault` 共享默认样式；有 `color` 时按颜色分组，减少重复 IR                  |
| 点专属通道    | `size` / `opacity` / `shape` / `stroke` / `strokeWidth` 只作用在 `PointMark`，会写到每个 datum Node 上 |
| 文本变体      | 设置 `text` 后不再画圆点，而是生成无边框文本 Node，并复用同一套位置投影                                |

## API

`PointMark` 的 API 按位置、专属、节点、文本、标签、通用和自定义分组：位置字段决定投影位置，专属字段由 PointMark 自己消费，节点字段下沉到 core `Node`。表里的 `FieldName` 表示数据字段名或字段路径，例如 `region` / `user.age`。

样式绑定统一使用 `MarkValueType<T>`：`{ kind: 'field', value: FieldName } | { kind: 'constant', value: T }`。需要指定 scale 时使用 `MarkValueType<T> & { scale?: string }`。组件层的 `FieldName`、`number`、`string` 等单值写法只是语法糖，最终都会落到这个统一结构。

| 分类   | 属性                                                  | 类型                                                                                                                                                                                                                                                          | 默认值                         | 描述                                                                           |
| ------ | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------ |
| 位置   | `x` / `y` / `z`                                       | `FieldName?`                                                                                                                                                                                                                                                  | —                              | 坐标角色字段；必填角色由当前坐标系校验                                         |
| 专属   | `color`                                               | `FieldName`<br />`PointColorStyle?`                                                                                                                                                                                                                           | —                              | mark 语义颜色；字段绑定会生成颜色通道和图例                                    |
|        | `fill`                                                | `FieldName`<br />`IRPaintSpec`<br />`PointFillStyle?`                                                                                                                                                                                                         | `currentColor`                 | 普通点填充；有 `color` 通道时由颜色接管                                        |
|        | `stroke`                                              | `FieldName`<br />`IRPaintSpec`<br />`PointStrokeStyle?`                                                                                                                                                                                                       | —                              | 普通点描边样式；字段绑定仍走颜色 scale                                         |
| 节点   | `size`                                                | `FieldName`<br />`number`<br />`PointSizeStyle?`                                                                                                                                                                                                              | —                              | 仅作用于普通点；字段绑定经 sqrt scale 映射为半径                               |
|        | `shape`                                               | `FieldName`<br />`NodeShapeChannelValue`<br />`PointShapeStyle?`                                                                                                                                                                                              | —                              | 仅作用于普通点；分类字段映射到 point shape                                     |
|        | `textColor`                                           | `FieldName`<br />`PointColorStyle?`                                                                                                                                                                                                                           | —                              | 仅作用于文本点的文字颜色                                                       |
|        | `opacity` / `fillOpacity`<br />`drawOpacity`          | `MarkValueProp<number>`<br />`PointOpacityStyle`                                                                                                                                                                                                              | —                              | 整体、填充和描边透明度                                                         |
|        | `strokeWidth`                                         | `FieldName`<br />`number`<br />`PointStrokeWidthStyle?`                                                                                                                                                                                                       | —                              | 普通点或 cell 的描边宽度                                                       |
|        | `rotate` / `zIndex`                                   | `MarkValueProp<number>`<br />`PointNumberStyle`<br />`PointZIndexStyle`                                                                                                                                                                                       | —                              | 旋转角度与绘制顺序                                                             |
|        | `padding` / `minimumSize`                             | `MarkValueProp<number>`<br />`PointNonnegativeNumberStyle`                                                                                                                                                                                                    | —                              | Node 内边距与最小视觉尺寸                                                      |
|        | `minimumSize`                      | `MarkValueProp<number>`<br />`PointNonnegativeNumberStyle`                                                                                                                                                                                                    | —                              | 最小视觉尺寸                                                                |
|        | `align`<br />`lineHeight` / `maxTextWidth`            | `MarkValueProp<NodeTextAlignValue>`<br />`NodeTextAlignStyle`<br />`MarkValueProp<number>`<br />`NodePositiveNumberStyle`                                                                                                                                     | —<br />—                       | 文本 Node 排版通道                                                             |
|        | `cornerRadius`<br />`scale`     | `MarkValueProp<number>`<br />`PointNonnegativeNumberStyle`<br />`MarkValueProp<number>`<br />`NodePositiveNumberStyle`                                                                                                                                        | —<br />—                       | Node 圆角与缩放通道                                                            |
|        | `padding` / `margin`                                                                                                                                                                                                         | `MarkValueProp<number \| IRBoxSpacing>`<br />`NodeBoxSpacingStyle`                                                                                                                                                                                            | —                              | Node 间距通道                                                                  |
|        | `dashed` / `dotted`<br />`dashPattern`                | `MarkValueProp<boolean>`<br />`NodeBooleanStyle`<br />`MarkValueProp<Array<number>>`<br />`NodeDashPatternStyle`                                                                                                                                              | —<br />—                       | Node 描边虚线通道                                                              |
|        | `font`<br />`boundary`<br />`shadow`<br />`blendMode` | `MarkValueProp<IRFont>`<br />`NodeFontStyle`<br />`MarkValueProp<IRBoundary>`<br />`NodeBoundaryStyle`<br />`MarkValueProp<ShadowPresetValue>`<br />`MarkValueProp<IRDropShadow>`<br />`ShadowStyle`<br />`MarkValueProp<BlendModeValue>`<br />`BlendModeStyle` | —<br />—<br />—<br />—         | 字体、边界、阴影和混合模式                                                     |
| 文本   | `text`                                                | `FieldName?`                                                                                                                                                                                                                                                  | —                              | 绑定文本内容字段；设置后点本身会变成文本 Node                                  |
|        | `displayFormat`                                       | `string?`                                                                                                                                                                                                                                                     | —                              | 设置 `text` 的展示格式                                                         |
|        | `dx` / `dy`                                           | `number?`                                                                                                                                                                                                                                                     | `0`                            | 文本点相对锚点的屏幕微调                                                       |
| 标签   | `label`                                               | `FieldName?`                                                                                                                                                                                                                                                  | —                              | 绑定宿主 `Node.label` 文本字段；保留点本身                                     |
|        | `labelDisplayFormat`                                  | `string?`                                                                                                                                                                                                                                                     | —                              | 设置 `label` 的展示格式                                                        |
|        | `labelPosition`<br />`labelDistance`<br />`labelPin`  | `string / number`<br />`number`<br />`boolean / LabelPin`                                                                                                                                                                                                     | `top`<br />`12`<br />`false` | 标签方位、距离和引线                                                           |
|        | `labelTextColor`<br />`labelOpacity`<br />`labelFont` | `string?`<br />`number?`<br />`IRFont?`                                                                                                                                                                                                                       | —<br />—<br />—                | 标签文字颜色、透明度和字体                                                     |
|        | `labelRotate`<br />`labelKeepUpright`                 | `string / number?`<br />`boolean?`                                                                                                                                                                                                                            | —<br />—                       | 标签旋转和正向保持                                                             |
|        | `resolveLabel`                                        | `(row) => string?`                                                                                                                                                                                                                                            | —                              | 运行时生成标签；不进 IR，需配 `id`                                             |
| 通用   | `id`                                                  | `string?`                                                                                                                                                                                                                                                     | —                              | 可选 mark 句柄，用于图层 id / 运行时标签解析                                   |
|        | `transform`                                           | `Array<Transform>?`                                                                                                                                                                                                                                           | —                              | 当前图元局部的数据变换；在 plot 级 transform 之后执行，只影响这一层看到的 rows |
| 自定义 | `channels`                                            | `Record<string, Channel>`                                                                                                                                                                                                                                     | —                              | 自定义通道入口；交给注册的 channel definition 解析                             |

## 相关

- [PathMark](/viz/grammar/mark/path) —— 叠在散点下连成趋势线
- [坐标系](/viz/grammar/coordinate) —— rug（cartesian1D）/ 环形（polar1D）/ 三元散点（ternary2D）用 PointMark 的写法
- [比例尺](/viz/grammar/scale) —— size 用的平方根 scale 与连续 scale 家族
