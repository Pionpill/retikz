---
title: Scatter
description: Compare two continuous variables with points, and distinguish categories through color and shape encodings.
family: scatter-points
usage: distribution
---

<ShowcaseGallery
  examples={[
    {
      id: 'scatter-fertility-work',
      title: 'Categorical encoding',
      description: 'Compare fertility and female labor-force participation from a real World Bank country cross-section, and control whether color and shape distinguish income groups.',
      preview: {
        files: ['scatter-fertility-work', 'scatter-fertility-work.data.ts'],
        controls: { name: 'scatter-fertility-work' },
        controlPanelDefaultOpen: false,
        size: 'xxxl',
        caption: 'Toggle color and shape encodings independently to compare one channel, both channels, or no categorical encoding.',
      },
    },
    {
      id: 'scatter-penguins-facet-jitter',
      title: 'Faceting and jitter',
      description: 'Use `column` and `facet` encodings for three shared-scale panels, with deterministic horizontal jitter declared in the rich `x` mapping.',
      preview: {
        files: ['scatter-penguins-facet-jitter', 'scatter-penguins-facet-jitter.data.ts'],
        controls: { name: 'scatter-penguins-facet-jitter' },
        controlPanelDefaultOpen: false,
        size: 'xxxl',
        caption: 'The three species use separate panels with shared x and y scales; the x mapping uses jitter to separate overlapping points.',
      },
    },
    {
      id: 'scatter-world-cup-shots',
      title: 'Spatial scatter',
      description: 'Use `ScatterChart` for shot positions in StatsBomb coordinates, with outcome driving point color.',
      preview: {
        files: ['scatter-world-cup-shots', 'scatter-world-cup-shots.data.ts'],
        controls: { name: 'scatter-world-cup-shots' },
        controlPanelDefaultOpen: false,
        size: 'xxxl',
        caption: 'Point positions preserve the spatial meaning of shots, while outcome drives color and readable point styling.',
      },
    },
  ]}
>
This API section lists only Scatter-private declarations. `ChartData`, `ChartLayout`, `ChartExtension`, presentation, and shared root wiring are not repeated; see the [shared Chart API](/viz/chart).

## `ScatterChart`

`ScatterChart` fixes the Point family and Scatter recipe identity. It has no Scatter-private root prop: field mappings, constant properties, and marks use the three declarations below, while the root retains only shared Chart wiring and `children`.

## `ScatterEncodings`

`ScatterEncodingsProps` exactly reuses schema-derived `IRScatterChartEncodings`. Vanilla input and JSON Source keep the same encoding payload; React authors it as a separate declaration.

| Slot             | Supported Scatter mappings                                                            | Scatter-specific constraint                                                                       |
| ---------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `x`              | `string`, direct, aggregate, derived (`bin`, `normalize`, `jitter`, custom transform) | Required; `jitter` can target only x; `bin` cannot be combined with a facet                       |
| `y`              | `string`, direct, aggregate, derived (`bin`, `normalize`, `jitter`, custom transform) | Required; `jitter` can target only y; `bin` cannot be combined with a facet                       |
| `color`          | `string`, direct, aggregate                                                           | Optional; binds Scatter's color consumer and accepts a compatible categorical or continuous scale |
| `size`           | `string`, direct, aggregate, derived (`normalize`, custom transform)                  | Optional; output must be continuous, and named scales currently accept only `sqrt`                |
| `opacity`        | `string`, direct, aggregate, derived (`normalize`, custom transform)                  | Optional; output must be continuous, and named scales currently accept only `linear`              |
| `shape`          | `string`, direct                                                                      | Optional; binds categorical shape and does not accept a named scale                               |
| `row` / `column` | `string`, `IRPlotPartitionDimension`, `Array<IRPlotPartitionDimension>`               | Optional; either dimension creates a facet, and an array expresses ordered facet hierarchy        |
| `facet`          | `IRPlotFacetOptions`                                                                  | Optional; configures `empty`, `header`, `resolve`, and `spacing`; requires `row` or `column`      |

Scatter rejects `series`, `detail`, `order`, `text`, and ordinary slot arrays without a defined consumer. Field types and scale compatibility are handled by the existing Data / Plot contracts and are not expanded here.

### Facet

`row` / `column` / `facet` are Scatter's facet mappings. `facet` cannot be used without `row` or `column`, and x/y `bin` mappings are rejected when a facet is present.

```tsx
<ScatterEncodings
  x="billLengthMm"
  y="flipperLengthMm"
  column={{ field: 'species', order: ['Adelie', 'Chinstrap', 'Gentoo'] }}
  facet={{ header: { column: true }, spacing: { panelGap: 20 } }}
/>
```

`row` / `column` partition panels; they do not choose a mark type. Scatter continues to use its point mark. Plot owns facet panel arrangement, scale resolution, and location, so Source does not repeat derived panel ids or coordinates.

## `ScatterProperties`

`ScatterProperties` declares recipe-level constants and does not accept field mappings; use `ScatterEncodings` for data-driven visual changes. `ScatterPropertiesProps` fields are grouped below.

| Fields                                                                                      | Values                                                   |
| ------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `color`, `textColor`, `fill`, `stroke`, `shape`                                             | Colors, paints, or an extensible shape name              |
| `size`, `minimumSize`, `scale`, `rotate`, `dx`, `dy`, `padding`, `margin`, `cornerRadius`   | Finite numbers or the corresponding JSON-safe structures |
| `strokeWidth`, `fillOpacity`, `strokeOpacity`, `opacity`, `dashed`, `dotted`, `dashPattern` | Stroke, opacity, and dash constants                      |
| `zIndex`, `align`, `lineHeight`, `maxTextWidth`, `font`, `label`                            | Drawing order, text, and label configuration             |
| `boundary`, `shadow`, `blendMode`                                                           | Boundary, shadow, and blend mode                         |

## `ScatterMark`

`ScatterMark` is Scatter's direct-child mark API. `ScatterMarkProps` is the props type exported by `@retikz/chart-react/point`; the component identity is fixed to Scatter, so callers do not provide `kind`. Shared Point mark nested fields are not repeated here.

| `ScatterMarkProps` property | Type                             | Default | Scatter-specific behavior                                                                                                                                               |
| --------------------------- | -------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `override?`                 | `boolean`                        | `false` | When `true`, replaces the implicit primary `scatter` mark in place; if no target exists, it appends and reports `CHART_MARK_OVERRIDE_TARGET_NOT_FOUND` through `onWarn` |
| `encodings?`                | `ScatterMarkProps['encodings']`  | —       | Overrides inherited point field mappings from the current Scatter recipe; available fields follow the shared Point mark encoding contract                               |
| `properties?`               | `ScatterMarkProps['properties']` | —       | Overrides inherited point styling from the current Scatter recipe; available fields follow the shared Point properties contract                                         |

## Related reading

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/viz/chart/model/authoring">
    <span className="font-semibold">Authoring &amp; Resolution</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Reference the React, Vanilla, schema, and recipe pipeline.
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/chart/model/plot">
    <span className="font-semibold">Plot Integration</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Reference independent Plot fragments and owner boundaries.
    </span>
  </LinkedCard>
</div>

</ShowcaseGallery>
