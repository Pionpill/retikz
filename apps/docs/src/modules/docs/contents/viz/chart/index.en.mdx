---
title: Chart
description: Chart organizes concrete graphic types on top of Plot and adds chart-wide title, source, canvas, and theme capabilities.
---

Chart represents a complete graphic: the root `type` is a discoverable family, while nested `recipe.chartType` selects the concrete recipe. Chart also adds title, source, canvas, and Theme behavior. Plot continues to own data, marks, channels, scales, coordinates, guides, and transforms. Chart does not introduce a second graphics grammar.

| Layer | Owns                                                                      | Does not own                                      |
| ----- | ------------------------------------------------------------------------- | ------------------------------------------------- |
| Chart | family, chartType, Chart Source, presentation, canvas, and Theme          | A replacement for Plot marks and encoding grammar |
| Plot  | data, transforms, marks, channels, scales, coordinates, axes, and legends | Product semantics such as Scatter types           |

## A minimal Point Chart

Concrete graphics use typed Chart entries. This `ScatterChart` produces Source with `type: 'point'` and `recipe.chartType: 'scatter'`; `encodings` bind fields and `properties` contain constants.

```tsx
import { ScatterChart } from '@retikz/chart-react/point/scatter';

const rows = [
  { x: 1, y: 2 },
  { x: 2, y: 3 },
  { x: 3, y: 5 },
];

<ScatterChart
  data={rows}
  encodings={{ x: 'x', y: 'y' }}
  properties={{ size: 8 }}
  title="Relationship between two variables"
  source="Example data"
  layout={{ width: 640, height: 360 }}
/>;
```

## React hosting and embedding

Every concrete Chart component provides Layout capability by default. Rendering `<ScatterChart>` directly creates exactly one `Layout` / Scene host. Inside an existing `<Layout>`, Chart contributes only the Chart-to-Plot-to-Core Scope/composite, inherits the outer effective Theme, and does not create a second SVG.

`width`, `height`, CSS, renderer/runtime, animation, and compile callbacks are all `ChartHostProps` and are valid only for standalone use. An embedded Chart fails loudly when it owns any of those fields, including an explicit `undefined`; move them to the outer `<Layout>`. Chart Source `layout` remains the separate border-box contract and is never populated or overwritten by host `width` / `height`; Chart `id` remains optional.

When you already have JSON Source that matches an exact schema, the application
should select the concrete entry from `type` and `recipe.chartType`. Chart does
not provide a generic `<Chart source={...}>` component that dynamically loads
every family. To compose axes, legends, or an independent Plot mark, write a
Source `plotExtension` fragment or use Plot directly.

<SourceLinks
  sources={[
    {
      label: 'Shared Chart entry',
      path: 'packages/viz/chart-react/src/_chart/index.ts',
      startLine: 1,
      endLine: 11,
    },
    {
      label: 'Point Chart shared mark components',
      path: 'packages/viz/chart-react/src/point/shared/marks.tsx',
      startLine: 4,
      endLine: 14,
    },
    {
      label: 'Standalone and embedded Chart host-field boundary',
      path: 'packages/viz/chart-react/src/shared/host.ts',
      startLine: 30,
      endLine: 44,
    },
    {
      label: 'Concrete Chart components share one Layout / InputEmbed entry',
      path: 'packages/viz/chart-react/src/point/shared/component.tsx',
      startLine: 102,
      endLine: 130,
    },
  ]}
/>

## Pages

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/viz/chart/points">
    <span className="font-semibold">Scatter &amp; Points</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">Choose the available concrete Scatter type.</span>
  </LinkedCard>
  <LinkedCard href="/viz/chart/model">
    <span className="font-semibold">Chart Model</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Understand family, chartType, marks, presentation, and Plot ownership.
    </span>
  </LinkedCard>
</div>
