---
title: 契约 - 表格
description: 查询 TableSpec 根、Cell payload、当前固定布局与 alpha.2 轨道尺寸原语的 JSON schema。
---

基础表格契约定义所有表型共享的顶层身份、Cell 内容和布局字段，并提供 manual / custom 精确根变体。detail 专有的数据引用与列结构见[明细表契约](/viz/table/reference/contract-detail)。

## TableSpec 根

`IRTableSpec` 是 `IRDetailTableSpec | IRManualTableSpec | IRCustomTableSpec` 的聚合类型。三个变体共享 `namespace + type`，再分别约束 `data` 与 `structure`。

| 字段        | 类型                                | 默认值         | 描述                                  |
| ----------- | ----------------------------------- | -------------- | ------------------------------------- |
| `namespace` | `'table'`                           | —              | Tier 2 composite namespace            |
| `type`      | <ApiValues name="TableComposite" /> | —              | 顶层 Table node type                  |
| `id?`       | `string`                            | —              | 可选稳定 id                           |
| `layout?`   | `IRTableLayout`                     | 固定轨道默认值 | 列宽、正文/列头行高与行列 gap         |
| `meta?`     | `IRJsonObject`                      | —              | lowering 原样保留的 JSON-safe 元数据  |
| `data?`     | `IRDataReference`                   | —              | detail 必需、manual 禁止、custom 可选 |
| `structure` | `IRTableStructureOperation`         | —              | detail、manual 或 custom Structure    |

### TableSpecSchema

<ZodSchema
  name="TableSpecSchema"
  description="Table 顶层规格的聚合 schema，联合 detail、manual 与 custom 三个精确根变体。"
/>

只生成一种表格的 LLM 或工具应持有精确 schema；同一存储、协议或 pipeline 接收多种表格时，再使用 `TableSpecSchema` / `IRTableSpec`。

### ManualTableSpecSchema

<ZodSchema
  name="ManualTableSpecSchema"
  description="显式网格 spec：直接保存行、列与 Cell，不接受外部 data reference。"
  descriptions={{
    namespace: 'Table Tier 2 composite 的固定命名空间，用于把节点路由到 Table composite definition。',
    type: '顶层 Table 规格的 composite 类型判别值。',
    id: '可选稳定 Table id；省略时由 lowering 生成根 Scope id。',
    layout: '固定轨道布局配置；省略字段使用 pipeline 默认值。',
    meta: '由 lowering 原样保留的 JSON-safe 元数据。',
    data: 'manual Table 不接受外部数据引用。',
    structure: 'manual Table 的显式行、列与 Cell 结构。',
    'structure.kind': '显式 manual Structure 的判别值。',
    'structure.rows': '显式声明的行数。',
    'structure.columns': '显式声明的列数。',
    'structure.rowKinds': '每一行的可选语义类型；省略时所有行均按正文行处理。',
    'structure.cells': '按声明行列范围内的地址保存的显式 Cell。',
  }}
/>

### CustomTableSpecSchema

<ZodSchema
  name="CustomTableSpecSchema"
  description="自定义结构 spec：通过 registry 解析 structure kind，并可选引用外部数据。"
  descriptions={{
    namespace: 'Table Tier 2 composite 的固定命名空间，用于把节点路由到 Table composite definition。',
    type: '顶层 Table 规格的 composite 类型判别值。',
    id: '可选稳定 Table id；省略时由 lowering 生成根 Scope id。',
    layout: '固定轨道布局配置；省略字段使用 pipeline 默认值。',
    meta: '由 lowering 原样保留的 JSON-safe 元数据。',
    data: '可选外部数据引用，运行时会把对应 dataset 暴露给自定义 Structure definition。',
    'data.reference': '外部 dataset 名称；实际数据值不写入 IR。',
    'data.model': '可选字段声明；字段名必须唯一。',
    structure: '由 registry 解析的自定义 Table Structure operation。',
    'structure.kind': '自定义 Table Structure provider 的名称。',
  }}
/>

## Cell

`IRTableCell` 是 manual Structure 中可直接持久化的 Cell。地址从 `0` 开始；省略 `id`、`location` 或 `roles` 时，运行时会从地址和所在行语义派生。

### TableCellSchema

<ZodSchema
  name="TableCellSchema"
  description="显式 Cell：包含零基地址、value/content payload，以及可选身份与语义角色。"
  descriptions={{
    id: '可选稳定 Cell id；省略时根据 Cell 地址派生。',
    address: 'manual Structure 中的零基 Cell 地址。',
    'address.row': 'Structure 输出中的零基行索引。',
    'address.column': 'Structure 输出中的零基列索引。',
    payload: 'Cell 的标量值或直接 Core/Tier 2 内容。',
    location: 'Cell 所在的可选语义区域；省略时根据所在行类型派生。',
    roles: 'Cell 承担的可选非空语义角色；省略时根据所在行类型派生。',
  }}
/>

### TableCellPayloadSchema

<ZodSchema
  name="TableCellPayloadSchema"
  description="Cell 内容联合：value 交给 presentation provider，content 直接保存 JSON-safe Core/Tier 2 child。"
/>

`{ kind: 'value', value: 'Score' }` 中的 `kind: 'value'` 表示“标量值呈现”分支；它不是 JavaScript 字符串类型的别名。直接放置 Node、Path 或 Tier 2 child 时使用 `kind: 'content'`。

## Layout

### TableLayoutSchema

<ZodSchema
  name="TableLayoutSchema"
  description="v0.1 固定轨道布局：统一列宽、正文/列头行高，以及非负的行列间距。"
  descriptions={{
    columnWidth: '统一列宽；省略时使用 120。',
    rowHeight: '统一正文行高；省略时使用 32。',
    headerHeight: '统一列头行高；省略时跟随最终正文行高。',
    columnGap: '相邻列之间的非负间距；省略时使用 0。',
    rowGap: '相邻行之间的非负间距；省略时使用 0。',
  }}
/>

省略字段时，`columnWidth` 为 `120`、`rowHeight` 为 `32`、`headerHeight` 跟随正文行高，行列 gap 为 `0`。Cell 内容不会反向改变轨道尺寸。

## 轨道尺寸原语

alpha.2 公开以下独立尺寸原语，用于表达固定尺寸、内容自然尺寸、剩余空间份额和带上下界的轨道。它们目前尚未接入 Table 根布局：`TableLayoutSchema` 仍只接受上面的 `columnWidth`、`rowHeight`、`headerHeight`、`columnGap` 与 `rowGap`。根 schema、layout pipeline 与 manifest 会在后续迁移中原子切换；现阶段不要把这些对象写入 `TableSpec.layout`。

轨道尺寸的判别值为 <ApiValues name="TableTrackSizeKind" />。

### TableFixedTrackSizeSchema

<ZodSchema
  name="TableFixedTrackSizeSchema"
  description="显式固定轨道尺寸；允许 0 表达折叠轨道。"
  descriptions={{
    kind: '固定轨道尺寸的判别值。',
    value: '有限非负的固定尺寸。',
  }}
/>

### TableAutoTrackSizeSchema

<ZodSchema
  name="TableAutoTrackSizeSchema"
  description="从 canonical 内容贡献确定自然尺寸的轨道。"
  descriptions={{
    kind: '内容自然尺寸轨道的判别值。',
  }}
/>

### TableFractionTrackSizeSchema

<ZodSchema
  name="TableFractionTrackSizeSchema"
  description="在有限轴约束下按权重分配剩余空间的轨道。"
  descriptions={{
    kind: '弹性份额轨道的判别值。',
    weight: '有限正权重；省略时运行时使用 1。',
  }}
/>

### TableMinmaxTrackSizeSchema

<ZodSchema
  name="TableMinmaxTrackSizeSchema"
  description="由下界和上界变体约束的轨道尺寸。"
  descriptions={{
    kind: '带上下界轨道的判别值。',
    min: '固定或内容自然下界。',
    'min.kind': '下界尺寸的判别值。',
    'min.value': '固定下界的有限非负尺寸。',
    max: '固定、内容自然或弹性份额上界。',
    'max.kind': '上界尺寸的判别值。',
    'max.value': '固定上界的有限非负尺寸。',
    'max.weight': '弹性上界的有限正权重；省略时运行时使用 1。',
  }}
/>

当上下界都是 fixed 时，`min.value` 不能大于 `max.value`。`min` 不接受 fraction；`max` 接受 fixed、auto 或 fraction。

### TableTrackSizeSchema

<ZodSchema name="TableTrackSizeSchema" description="fixed、auto、fraction 与 minmax 四种轨道尺寸的判别联合。" />

### TableTrackOverrideSchema

<ZodSchema
  name="TableTrackOverrideSchema"
  description="通过 canonical 零基索引覆盖单个轨道的尺寸。"
  descriptions={{
    index: '要覆盖的 canonical 零基轨道索引。',
    size: '替换该索引轴默认值的轨道尺寸。',
  }}
/>

### TableTrackOverridesSchema

<ZodSchema name="TableTrackOverridesSchema" description="canonical 索引必须唯一的稀疏轨道尺寸覆盖列表。" />

列表允许只声明需要覆盖的索引，不要求复制其余轨道。同一列表中的重复索引会被 schema 拒绝；相对于实际行列数量的越界检查由后续 layout resolve 执行。

## 相关

- [明细表契约](/viz/table/reference/contract-detail)：data reference、columns 与 detail Structure
- [运行时](/viz/table/reference/runtime)：这些 JSON 规格如何构造、下沉和渲染
- [表格模型](/viz/table/model)：Structure、Cell、Presentation 与 Layout 的关系
