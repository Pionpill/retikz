---
title: Tables
description: Start with detail tables and turn data into readable, persistable tables that lower into Kernel rendering.
---

Table is an independent Tier 2 capability. It owns row-and-column structure, Cell semantics, deterministic layout, and lowering, while [Data](/viz/data) owns records, field models, and data transforms. A table is not another chart type layered over Plot, and each table type does not need a duplicated low-level grammar.

The current release exposes one business-facing path: use `DetailTable` to map one record to one row. `ManualTable` is the low-level entry for declaring a Cell grid directly, so it is documented under the shared model and API reference instead of appearing as a second business table type.

## Where to start

| Goal                                              | Start here                            |
| ------------------------------------------------- | ------------------------------------- |
| Show lists, ledgers, or record-per-row data       | [Detail Table](/viz/table/detail)     |
| Understand Structure, Cells, layout, and lowering | [Table Model](/viz/table/model)       |
| Look up complete specs or React and Vanilla APIs  | [API Reference](/viz/table/reference) |

When pivot, matrix, or other table types arrive, they will sit beside Detail Table under table types. Shared Cell, Presentation, Layout, rule, and lowering concepts will remain in one table model.

## Pages

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-3">
  <LinkedCard href="/viz/table/detail">
    <span className="font-semibold">Detail Table</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Declare fields and headers, then map external records into rows.
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/table/model">
    <span className="font-semibold">Table Model</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Learn the structure, Cell, presentation, layout, and lowering pipeline shared by table types.
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/table/reference">
    <span className="font-semibold">API Reference</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Look up TableSpec, React, Vanilla, lowering, and manifest contracts.
    </span>
  </LinkedCard>
</div>
