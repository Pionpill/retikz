---
title: 表格
description: 从明细表开始，将数据组织成可阅读、可持久化并能接入 Kernel 渲染的表格。
---

Table 是独立的 Tier 2 能力：它负责行列结构、Cell 语义、确定性布局和 lowering，而[数据](/viz/data)负责记录、字段模型与数据变换。表格不是 Plot 上再包一层的“图表类型”，也不需要为每种表型复制一套底层语法。

当前版本只开放一条面向业务使用的路径：用 `DetailTable` 把一条记录映射为一行。`ManualTable` 是直接声明 Cell 网格的底层入口，放在共享模型和 API 参考中说明，不作为第二种业务表型。

## 如何阅读

| 你想做什么                          | 从哪里开始                       |
| ----------------------------------- | -------------------------------- |
| 展示名单、流水或逐行记录            | [明细表](/viz/table/detail)      |
| 理解 Structure、Cell、布局和下沉    | [表格模型](/viz/table/model)     |
| 查询完整 spec、React 或 Vanilla API | [API 参考](/viz/table/reference) |

未来增加交叉表、矩阵表等表型时，它们会与明细表并列进入“表格类型”；共享的 Cell、Presentation、Layout、规则和 lowering 仍留在同一套表格模型中。

## 章节内容

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-3">
  <LinkedCard href="/viz/table/detail">
    <span className="font-semibold">明细表</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">声明字段和列头，将外部记录映射为表格行。</span>
  </LinkedCard>
  <LinkedCard href="/viz/table/model">
    <span className="font-semibold">表格模型</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      理解不同表型共享的结构、Cell、呈现、布局与下沉管线。
    </span>
  </LinkedCard>
  <LinkedCard href="/viz/table/reference">
    <span className="font-semibold">API 参考</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      查询 TableSpec、React、Vanilla、lowering 与 manifest 契约。
    </span>
  </LinkedCard>
</div>
