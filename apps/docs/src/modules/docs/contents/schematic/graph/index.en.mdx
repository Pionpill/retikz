---
title: Graph
description: Choose reusable diagram elements from one entry point, then express local structure and relationships with logic shells, Entity, and Relation.
---

Graph is the unified entry point for reusable elements in the Schematic domain. Its first set covers common logic links: `Container` owns a reusable shell and regions; `Entity` uses `role` for terminals, stages, decisions, and junctions; `Relation` expresses relationships.

Graph owns each element's diagram meaning and drawing contract. It does not own a `GraphModel`, automatic layout, obstacle avoidance, hit testing, or interaction state. Place units with Core positions or Layout containers, then use stable `id` values for explicit targets.

## Responsibilities at a glance

| Member      | Owns                                                          | Does not own                            |
| ----------- | ------------------------------------------------------------- | --------------------------------------- |
| `Container` | A shell, header, ordered sections, and reusable region layout | Graph topology or automatic placement   |
| `Entity`    | Neutral shape, content, sizing, and semantic role             | Domain-specific state machines          |
| `Relation`  | One Core Path route and an optional final-step label          | Automatic routing or obstacle avoidance |

All members lower to existing Core primitives and can be authored through React, direct JSON/IR, or Vanilla embeds. Definitions are injected per figure; they never enter persisted JSON.

## Content groups

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/schematic/graph/base">
    <span className="font-semibold">Base Units</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">Entity and Relation</span>
  </LinkedCard>
  <LinkedCard href="/schematic/graph/frame">
    <span className="font-semibold">Composite Units</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">Container and future reusable compositions</span>
  </LinkedCard>
  <LinkedCard href="/schematic/graph/api-reference">
    <span className="font-semibold">Graph API Reference</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Schemas, factories, Definitions, and adapter identity
    </span>
  </LinkedCard>
</div>

## Related

- [Layout containers](/library/layout): generic Flex, Grid, and Overlay arrangement
- [Schematic introduction](/schematic/introduction): the boundary between Graph and Diagram capabilities
- [Core paths](/kernel/components/draw/overview): the primitive output used by Relation
