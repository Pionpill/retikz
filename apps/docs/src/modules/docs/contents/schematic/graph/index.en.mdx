---
title: Graph
description: Compose an optional Graph Scope, Block, Group, Entity, Relation, and arbitrary Core content.
---

Graph is an optional context shell. It reuses the complete Core `Scope` surface and adds `graphDefaults` for visible Entity, Relation, Group, and Block descendants, plus conditional `graphRules` for Entity and Relation. Automatic layout, routing algorithms, and rendering remain outside Graph.

Block, Group, Entity, and Relation are independent Tier 2 composites that can appear directly in Layout, Scope, or any other Core-child position. Block models code and engineering objects through a fixed Header, Section, Row, and Cell structure. Graph maintains neither a member collection nor an identity index; Core namespace owns references and duplicate-id diagnostics.

`Graph.style` sets cascading drawing styles, and `Graph.defaults` sets named descendant defaults. Standalone and embedded Graph use the same contract. `style` does not accept host CSS; apply CSS to an outer DOM element or `Layout.style`.

## Two host modes

As a top-level React component, Graph creates one Scene host like Layout, Plot, or Table. It does not need an outer Layout:

```tsx
<Graph>
  <Entity id="service" role="activity" position={[120, 80]}>
    Service
  </Entity>
</Graph>
```

When an outer Layout already exists, Graph lowers to one Scope that affects only local content and creates no second SVG or Canvas:

```tsx
<Layout>
  <Graph transforms={[{ kind: 'translate', x: 80, y: 40 }]}>
    <Entity role="activity" position={[120, 80]}>
      Local content
    </Entity>
  </Graph>
</Layout>
```

Fields such as `theme`, `defaults.node`, `defaults.path`, `defaults.label`, `defaults.arrow`, `defaults.reset`, `transforms`, `placement`, `zIndex`, and `clip` belong directly to `IRGraph` and retain Core Scope semantics. `width`, `height`, renderer, runtime, and DOM props belong only to the standalone Scene host; move them to the outer Layout when Graph is embedded.

## Responsibilities

| Member     | Responsibility                                                        | Does not own                                 |
| ---------- | --------------------------------------------------------------------- | -------------------------------------------- |
| Graph      | Core Scope surface and local Graph defaults and rules                 | Member indexes, layout, or routing           |
| Block      | Structured objects, local Flex layout, and addressable Section / Row  | Port models, execution, or automatic routing |
| Group      | Visible arbitrary-content boundary, caption, and boundary labels      | Automatic layout, routing, or avoidance      |
| Entity     | Node semantics and independent lowering to one Core Node              | Generated ids or arbitrary drawing children  |
| Relation   | Core NodeTarget endpoints, relation semantics, and Core Path lowering | Graph membership checks or automatic routing |
| Core child | Free author-ordered composition with Entity and Relation              | Graph-local semantic rules                   |

## Chapters

<LinkedSections
  items={[
    {
      title: 'Block',
      description: 'Structured code and engineering objects with local connection targets',
      url: '/schematic/graph/block',
    },
    {
      title: 'Group',
      description: 'Nestable shells, captions, and Core boundary labels',
      url: '/schematic/graph/group',
    },
    {
      title: 'Entity',
      description: 'Independent node semantics, text, placement, and extensions',
      url: '/schematic/graph/entity',
    },
    {
      title: 'Relation',
      description: 'General references, direction, routes, and extensions',
      url: '/schematic/graph/relation',
    },
    {
      title: 'Graph API Reference',
      description: 'Source schemas, Scope surface, and Graph-family adapters',
      url: '/schematic/graph/api-reference',
    },
  ]}
/>
