---
title: Entity
description: Store an optional identity, open semantics, and Core Node instance fields in one record.
---

Entity expresses what a node means and independently lowers to one ordinary Core Node. One Source record stores an optional `id`, `role`, optional `kind`, `predicate`, and non-structural Core Node fields such as `text`, `position`, `style.fill`, and `style.dashed`; it is not an arbitrary child host. The role selects structure, Theme supplies appearance defaults, and explicit instance fields take final precedence. Graph is only an optional local context host. Entity `style.color` remains the string master; numeric `style.fill`, `style.stroke`, and text-derived colors resolve in Core only after the final instance master is known

## Responsibilities

| Topic       | Responsibility                                       | Reach for it when                 |
| ----------- | ---------------------------------------------------- | --------------------------------- |
| Basic usage | Built-in roles, text, position, and size             | Creating your first Entity        |
| Extensions  | Custom roles, kinds, predicates, and Theme selectors | Building a domain visual language |

## Chapters

<LinkedSections
  items={[
    {
      title: 'Basic usage',
      description: 'Seven built-in roles and direct Node fields',
      url: '/schematic/graph/entity/basic',
    },
    {
      title: 'Extensions',
      description: 'Definitions, registries, injection, and failures',
      url: '/schematic/graph/entity/extension',
    },
  ]}
/>
