---
title: Entity Basic Usage
description: Author node semantics, text, and placement in one concise record at any Core-child position.
---

`Entity` is a semantic node with an optional Core identity. Semantics, text, and Core Node-compatible placement live directly in one record, which independently lowers to one ordinary Core `Node`. Omitting `id` draws the node without registering a Core target that references can resolve

## Usage

```tsx
import { Entity } from '@retikz/graph-react';
import { Layout } from '@retikz/react';
```

```tsx
<Layout width={360} height={140}>
  <Entity id="task" role="activity" position={[180, 70]}>
    Process order
  </Entity>
</Layout>
```

`Entity` can appear directly in Layout, Scope, Graph, or any other Core-child position. Its children accept only strings, numbers, Fragments, or Core `<Text>` and write the direct `text` field. Keep drawing content as siblings rather than Entity children

## Examples

The seven built-in roles define only upper-level semantics and a base shape recipe. Graph has **no built-in Entity kinds or predicates**: a `kind` or `predicate.name` can be used only after it is registered in the current provider assembly. Every kind and params object listed below is an extension example, not a built-in value

Theme owns the complete Entity visual language. Each demo below can precisely control instance color and text, while the page-level Theme selector switches the overall style among Academic, Vibrant, Clean, and other choices

Neutral is also the concise baseline fully inherited by Graph Clean. `stroke: 1` draws the complete final static master color, `fill: 0.08` composites that color at 8% weight over the current mode backdrop into an opaque fill, and `textColor: 'contrast'` then resolves body text to black or white from the final fill. The `0.08` value is a contextual-color weight, not `fillOpacity`

### participant: an active subject

`participant` represents a subject that actively participates, owns responsibility, or provides a capability. Typical objects include people, teams, organizations, external systems, and services. Use `activity` for work being performed and `resource` for an object that is merely consumed or stored

- **Built-in kinds / predicates:** none
- **Registrable kind examples:** `participant.person`, `participant.team`, and `participant.system`; these are examples of globally unique keys only
- **Controllable predicate params example:** an extension could declare `{ capacity: number, region: string }` through `paramsSchema`, but built-in `participant` accepts no predefined params

<ComponentPreview
  files="entity-participant"
  size="sm"
  caption="Control the participant outline, Graph Theme primary color, and text."
/>

### activity: work and action

`activity` represents work, an action, or a transformation that happens or must be performed. Typical objects include tasks, processing steps, subprocesses, and system operations. A persistent condition is a `state`; an instantaneous occurrence is an `event`

- **Built-in kinds / predicates:** none
- **Registrable kind examples:** `activity.task`, `activity.subprocess`, and `activity.operation`
- **Controllable predicate params example:** an extension could declare `{ retryable: boolean, slaMinutes: number }`; its `paramsSchema` owns the exact fields, defaults, and ranges

<ComponentPreview
  files="entity-activity"
  size="sm"
  caption="Inspect the activity's default solid outline, then control its precise color and text."
/>

### event: a point in time

`event` represents an occurrence, boundary, or lifecycle point in time. Typical objects include start, end, timeout, message arrival, and failure events. It does not represent ongoing work or a condition that persists

- **Built-in kinds / predicates:** none
- **Registrable kind examples:** `event.start`, `event.end`, `event.timer`, and `event.message`
- **Controllable predicate params example:** an extension could declare `{ interrupting: boolean, channel: string }`; Graph stores only JSON-safe params accepted by that schema

<ComponentPreview
  files="entity-event"
  size="sm"
  caption="Control the event outline, Graph Theme primary color, and text."
/>

### state: a persistent condition

`state` represents a condition that persists for an object or system. Typical objects include pending, active, suspended, and failed states. The action that changes a state is an `activity`; the instant of transition may be modeled as an `event`

- **Built-in kinds / predicates:** none
- **Registrable kind examples:** `state.pending`, `state.active`, and `state.failed`
- **Controllable predicate params example:** an extension could declare `{ severity: 'low' | 'high', terminal: boolean }`, with the enum and value domain validated by `paramsSchema`

<ComponentPreview
  files="entity-state"
  size="sm"
  caption="Control the state outline, Graph Theme primary color, and text."
/>

### gateway: a semantic control point

`gateway` represents a split, merge, or synchronization point with explicit control semantics. Typical objects include exclusive choices, parallel splits, parallel joins, and multi-way synchronization. Do not use it merely to obtain a diamond shape or in place of a participant or business resource

- **Built-in kinds / predicates:** none
- **Registrable kind examples:** `gateway.exclusive`, `gateway.parallel`, and `gateway.inclusive`
- **Controllable predicate params example:** an extension could declare `{ mode: 'split' | 'merge', branches: number }` and map params to emphasis without replacing the gateway structure recipe

<ComponentPreview
  files="entity-gateway"
  size="sm"
  caption="Control the gateway outline, Graph Theme primary color, and text."
/>

### resource: an object that is used or stored

`resource` represents an object that is used, produced, transferred, or stored. Typical objects include documents, databases, queues, datasets, and artifacts. A system that actively participates and provides a capability is usually a `participant`. Its default contour closes two half-elliptic caps with straight sides, so the top does not form a complete ellipse; symmetric vertical padding keeps the text vertically centered

- **Built-in kinds / predicates:** none
- **Registrable kind examples:** `resource.document`, `resource.database`, and `resource.queue`
- **Controllable predicate params example:** an extension could declare `{ mutable: boolean, replicas: number }` and map validated params to sparse appearance tokens in its predicate resolver

<ComponentPreview
  files="entity-resource"
  size="sm"
  caption="Inspect the resource's half-elliptic caps, then control the Graph Theme primary color and text."
/>

### concept: an abstract knowledge object

`concept` represents an abstract knowledge object rather than a concrete runtime instance. Typical objects include classes, claims, topics, domain concepts, and rules. Use `activity` for executable work and `participant` for an active connectable subject

- **Built-in kinds / predicates:** none
- **Registrable kind examples:** `concept.class`, `concept.claim`, and `concept.topic`
- **Controllable predicate params example:** an extension could declare `{ confidence: number, source: string }`; those params remain the predicate's domain contract rather than fixed `concept` fields

<ComponentPreview
  files="entity-concept"
  size="sm"
  caption="Control the concept outline, Graph Theme primary color, and text."
/>

### Style adjustments

The combined playground switches among all seven built-in roles and directly adjusts Core Node-compatible fill, stroke, opacity, and text styles. The role changes Entity semantics and its base shape recipe; these explicit style fields override the corresponding Theme defaults

<ComponentPreview
  files="entity-style"
  size="sm"
  caption="Switch roles, then adjust the fill, stroke, stroke width, dash, opacity, and text color independently."
/>

## How it works

<ComponentAlert
  type="tip"
  title="Optional for everyday use"
  description="Revisit when persisting Graph data, integrating Diagram, or diagnosing compile input."
/>

An Entity Source record stores authored fields only. `id`, `text`, and `position` may all be omitted without materializing empty wrappers, generated ids, or defaults. An Entity without `id` is still drawn from its role but registers no target in the Core namespace. At compile time, the role supplies shape, boundary, padding, corner radius, and base minimum size. Theme supplies appearance defaults, while explicit non-structural Node fields such as `color`, `fill`, `dashed`, `dashPattern`, and `textColor` take final precedence

An explicit static `color` drives both numeric default stroke and fill channels. Compilation fails loudly when a non-static master such as `currentColor` must still resolve either numeric color channel. Use a static `color`, or provide both a string stroke and an opaque static fill, so Graph never guesses the host CSS color or backdrop

Entity performs its own registry lookup and lowers to an ordinary Core `Node`. Core, Scene, and renderers do not interpret Entity roles. Graph only adds local Theme context when present; it is not a compile prerequisite

## API Reference

| Prop         | Type                                      | Default | Description                                                     |
| ------------ | ----------------------------------------- | ------- | --------------------------------------------------------------- |
| `id?`        | `string`                                  | —       | Optional Core target id; omission registers no reference target |
| `role`       | `string`                                  | —       | Registered open semantic role key                               |
| `kind?`      | `string`                                  | —       | Registered stable subtype within the role                       |
| `predicate?` | `{ name: string; params?: IRJsonObject }` | —       | Registered predicate and its JSON-safe parameters               |
| `position?`  | `IRPosition`                              | —       | Directly reuses the Core Node position language                 |
| `text?`      | `IRNode['text']`                          | —       | Node-compatible text                                            |
| `children?`  | Node-compatible text                      | —       | React text entry; it cannot host arbitrary drawing children     |
| `meta?`      | `IRJsonObject`                            | —       | Metadata ignored by Graph semantics                             |

## Related

- [Entity Extensions](/schematic/graph/entity/extension): register roles, kinds, predicates, and Theme styles
- [Relation Basic Usage](/schematic/graph/relation/basic): connect any named targets through Core NodeTarget
- [Graph API Reference](/schematic/graph/api-reference): inspect minimal Source records and Theme schemas
