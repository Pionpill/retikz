---
title: Effects
description: Add shadows, blending, patterns, and animation to existing primitives such as Node and Path, then register custom effects when needed.
---

Effects attach to existing primitives such as Node and Path. Shadows add depth, blending controls how layers combine, patterns provide reusable fills, and animation describes properties changing over time. They keep the same primitive model.

Start with shadows, blending, animation, and patterns for everyday work. Use the final extension guides only when built-in patterns or animation channels cannot express the result you need.

In the figure, solid arrows follow primitives through compile, the Scene, and the renderer. The dashed arrows mark different injection stages: pattern definitions produce Scene resources during compile, while easing and property registries consume Scene tracks during rendering.

<ComponentPreview files="effects-family" size="sm" hideCode />

## Responsibilities

| Capability       | Type            | Responsibility               | Use it when                 |
| ---------------- | --------------- | ---------------------------- | --------------------------- |
| Shadow           | Shared prop     | Add a drop shadow            | Showing depth or elevation  |
| Blend            | Shared prop     | Compose with painted content | Creating overlays           |
| Animation        | Track model     | Describe tracks and playback | Adding motion or feedback   |
| Patterns         | Fill spec       | Reuse lines, dots, or grids  | Distinguishing regions      |
| Custom Patterns  | Extension guide | Register repeating fills     | Built-ins cannot express it |
| Custom Animation | Extension guide | Register easing or channels  | Built-ins cannot express it |

## Chapters

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/kernel/components/effects/shadow">
    <span className="font-semibold">Shadow</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">Offsets, blur, color, and render bounds</span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/effects/blend">
    <span className="font-semibold">Blend</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">How primitives compose with their background</span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/effects/animation">
    <span className="font-semibold">Animation</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Tracks, triggers, controls, and static fallback
    </span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/effects/pattern">
    <span className="font-semibold">Patterns</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Built-in lines, dots, grids, and instance options
    </span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/effects/custom-pattern">
    <span className="font-semibold">Custom Patterns</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Register repeating fills beyond the built-ins
    </span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/effects/custom-animation">
    <span className="font-semibold">Custom Animation</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Register easing curves and animation property channels
    </span>
  </LinkedCard>
</div>
