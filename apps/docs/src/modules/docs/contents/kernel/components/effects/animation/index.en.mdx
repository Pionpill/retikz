---
title: Animation
description: Animate primitives and the camera with presets or keyframe tracks, including triggers, controls, snapshots, and reduced-motion fallback.
---

Animation transitions a static figure **from one look to another over time**: fade in, draw a line, scale up, spin, shift color, pan the camera. Call a **preset factory** (`fadeIn()` / `spin()` …) for common effects, or write an `IRAnimationTrack` when you need exact control.

## Usage

```tsx
import { fadeIn, scaleIn } from '@retikz/core';
import { Node } from '@retikz/react';
```

```tsx
<Node position={[0, 0]} animations={[fadeIn(), scaleIn()]}>
  hi
</Node>
```

## Examples

`animations` attaches to `<Node>` / `<Path>` / `<Scope>` (element animation) and `<Layout>` (camera). An element can carry several: different channels can combine, but tracks on the same property are not automatically composited. Merge conflicting values into one track and preserve the intended array order for transform channels. Apart from the `loop` wrapper and the `stagger` choreographer, every preset accepts the common options `{ duration, delay, easing, trigger }`; some add their own (`scaleIn.from` / `slideIn.axis,offset` / `growUp.origin` / `pulse.peak` …).

### Animations and controls

| Animation    | Family        | Channel       | What it does                          |
| ------------ | ------------- | ------------- | ------------------------------------- |
| `fadeIn`     | intro         | opacity       | fade in (0→1)                         |
| `drawOn`     | intro         | pathDraw      | stroke draws along the path           |
| `scaleIn`    | intro         | scale         | pop in from slightly small (0.8→1)    |
| `grow`       | intro         | scale         | grow from nothing (0→1)               |
| `growUp`     | intro         | scaleY        | grow from baseline (bars)             |
| `slideIn`    | intro         | translateX/Y  | slide in from an offset               |
| `colorShift` | emphasis      | fill / stroke | color transition (oklch lerp)         |
| `flash`      | emphasis      | opacity       | flash once (2× by default)            |
| `blink`      | emphasis      | opacity       | continuous blink (infinite)           |
| `wiggle`     | emphasis      | rotate        | rock left/right (3× by default)       |
| `pulse`      | loop          | scale         | pulsing scale (infinite)              |
| `spin`       | loop          | rotate        | steady rotation (infinite, loader)    |
| `loop`       | loop          | any           | turn any track into an infinite loop  |
| `cameraTo`   | camera        | viewBox       | pan / zoom the camera (on `<Layout>`) |
| `stagger`    | choreography  | —             | add increasing delay across a group   |
| `snapshotAt` | static output | —             | bake a static frame at a given time   |

### Hand-written keyframes

When a preset does not cover the timing you need, write a serializable `IRAnimationTrack` directly. This three-keyframe track moves the node up and back to its settled position:

<ComponentPreview files="handwritten-track" size="sm" />

`at` is a normalized position from 0 to 1, and `value` is an absolute value for the selected channel. The last frame returns to `0`, so completed playback and disabled animation share the same settled state. An easing on a keyframe controls the following segment.

### Intro

Intro animations **play once on load**, bringing an element from a transparent / offset / shrunk state to its final look. They are element animations attached to a `<Node>` / `<Path>` / `<Scope>`'s `animations`.

<ComponentPreview
  files="entrance"
  size="md"
  caption="All in one: fadeIn / scaleIn / grow / slideIn nodes, two growUp bars, and one drawOn path."
/>

When `origin` is omitted, rects and ellipses can derive their geometric center. Path, Text, and Group primitives fall back to the backend's default pivot because their center cannot be derived. Set `origin` explicitly when an animation must behave consistently across primitive kinds.

| Animation | Behavior                        | Specific options               |
| --------- | ------------------------------- | ------------------------------ |
| `fadeIn`  | opacity 0→1                     | —                              |
| `scaleIn` | scale `from`→1                  | `from: 0.8`<br />`origin`      |
| `grow`    | scale 0→1                       | `origin`                       |
| `growUp`  | scaleY 0→1 from the baseline    | `origin: 'bottom'`             |
| `slideIn` | translate `offset`→0            | `axis: 'x'`<br />`offset: -20` |
| `drawOn`  | pathDraw 0→1 reveals the stroke | stroked `<Path>` / shapes only |

### Emphasis & Loop

Emphasis animations **draw attention** — shift color, flash, rock; loop animations **play continuously** — pulse, spin, or wrap any track into an infinite loop. They are element animations. Emphasis often uses three frames `0 → 0.5 → 1` (with the last frame back at base, so one pass leaves the element unchanged); `flash` repeats twice by default, `wiggle` three times, while `blink` / `pulse` / `spin` use `iterations: 'infinite'` to run forever.

<ComponentPreview
  files="emphasis"
  size="md"
  caption="flash / blink / wiggle / pulse nodes, plus a spin bar, a loop slide, and a colorShift shape."
/>

| Animation    | Behavior                          | Specific options                              |
| ------------ | --------------------------------- | --------------------------------------------- |
| `colorShift` | fill / stroke transition in oklch | `from` + `to` required<br />`channel: 'fill'` |
| `flash`      | opacity 1→dim→1                   | `dim: 0`<br />`iterations: 2`                 |
| `blink`      | continuous flash                  | `dim: 0`<br />`iterations: 'infinite'`        |
| `wiggle`     | rotate back and forth             | `angle: 5`<br />`iterations: 3`<br />`origin` |
| `pulse`      | scale 1→peak→1                    | `peak: 1.1`<br />`origin`                     |
| `spin`       | rotate 0→360                      | `origin`                                      |
| `loop`       | any track, looping forever        | `direction`<br />`iterations: 'infinite'`     |

### Camera & Stagger

Camera and stagger are the two animations that act **on the whole, not on a single element's look**: `cameraTo` animates the scene's viewbox, while `stagger` adjusts the start times of a group of animations.

`cameraTo({ from, to })`: animates the scene's viewbox `[x, y, w, h]` for pan / zoom. Attach it to `<Layout animations>` (the camera belongs to the scene root, not an element); both endpoints are explicit. It defaults to `duration` 800ms and `easing` `ease-in-out`, both overridable like the other common options. It expands to a single `viewBox`-channel track.

<ComponentPreview
  files="camera"
  size="md"
  caption="The camera pushes from the full view into the bottom-right corner."
/>

`stagger(tracks, stepMs, startMs = 0)`: **rewrites each track's `delay` to `startMs + i×stepMs`** (overriding any existing `delay`) for a one-by-one entrance. The third argument `startMs` defaults to 0 and is the group's start offset. It choreographs the start times of element animations; it doesn't change any single animation's shape.

<ComponentPreview files="stagger" size="sm" caption="Three nodes with the same fadeIn, entering at 0 / 150 / 300ms." />

### Timing playground

The static examples above already cover entrances, emphasis, loops, camera motion, and choreography. Finish with a fixed `scaleIn` structure and vary only the starting scale, duration, delay, easing, and transform origin to compare the shared timing options.

<ComponentPreview
  files="animation-playground"
  size="sm"
  caption="Changing a value replays the animation; the preview toolbar can replay the current combination again."
/>

## How it works

<ComponentAlert
  type="tip"
  title="Optional for everyday use"
  description="Revisit when you want to understand track compilation, backend playback, or why snapshots and fallback agree."
/>

### How a track reaches rendering

A preset and a hand-written track both end as the same `AnimationTrack`. A preset only supplies a common property channel, keyframes, and timing defaults; its result is still serializable JSON with no callbacks, DOM objects, or playback state.

After a track is attached to an element or `<Layout>`, compile only validates its responsibility boundary and preserves valid data: `viewBox` belongs to the Scene root as a camera track, while every other channel belongs to an element. An out-of-place track emits a warning and is dropped, so a renderer does not need to decide that ownership again.

The figure stays within the animation boundary: presets and hand-written data converge on one track, enter the renderer-agnostic Scene, then branch into live playback or a static snapshot.

<ComponentPreview files="animation-pipeline" size="lg" hideCode />

React DSL, direct IR, and Vanilla specs therefore share one animation model. A backend chooses how to play it; it does not reinterpret a preset as a second model.

<SourceLinks
  sources={[
    {
      label: 'Presets produce JSON tracks',
      path: 'packages/kernel/core/src/parsers/animation.ts',
      startLine: 17,
      endLine: 36,
    },
    {
      label: 'Element and camera track boundaries',
      path: 'packages/kernel/core/src/compile/orchestration/animation.ts',
      startLine: 20,
      endLine: 42,
    },
    {
      label: 'Scene-root camera track emission',
      path: 'packages/kernel/core/src/compile/compile.ts',
      startLine: 22,
      endLine: 41,
    },
  ]}
/>

### How time becomes a frame

A keyframe's `at` is a normalized position in `[0, 1]`; `duration` maps that position to milliseconds. At any instant, evaluation subtracts `delay`, selects the iteration and direction, applies `fill` outside the active interval, locates the neighboring keyframes, applies segment easing, and finally interpolates by value type.

The two renderers use different live carriers while consuming the same track:

- SVG expands `load` tracks into inline CSS `@keyframes`, so static HTML can autoplay with zero JavaScript; `visible`, `manual`, and event tracks become descriptors that the runtime connects to WAAPI
- Canvas advances one Scene-level rAF clock and calls the shared evaluator every frame, then applies the result to primitive properties or canvas transforms before redrawing
- `snapshotAt` starts no clock; it samples that same evaluator at one time and emits static attributes and transforms

<SourceLinks
  sources={[
    {
      label: 'Shared absolute-time track evaluation',
      path: 'packages/kernel/render/src/animation/evaluate.ts',
      startLine: 92,
      endLine: 155,
    },
    {
      label: 'SVG CSS and WAAPI routing',
      path: 'packages/kernel/render/src/svg/animation/keyframes.ts',
      startLine: 121,
      endLine: 178,
    },
    {
      label: 'Canvas frame evaluation and application',
      path: 'packages/kernel/render/src/canvas/animate.ts',
      startLine: 68,
      endLine: 106,
    },
  ]}
/>

### Triggers and control

`trigger` decides **when a track becomes active**; it does not change the keyframes or timing. The default is `load`:

| trigger                | Behavior                                | Backend support                                                        |
| ---------------------- | --------------------------------------- | ---------------------------------------------------------------------- |
| `load` (default)       | plays on render                         | SVG (inline CSS, zero-JS SSR) / Canvas (starts rAF)                    |
| `visible`              | plays when in viewport                  | SVG (IntersectionObserver); Canvas (id bbox intersecting the viewport) |
| `manual`               | no autoplay, driven by a runtime handle | SVG / Canvas                                                           |
| `{ onEvent: 'click' }` | plays when the event fires              | SVG / Canvas (hydration hit activates the id)                          |

Pass a ref to `<Layout animationRef={ref}>` and after mount you get `AnimationControls` (`play` / `pause` / `seek`) to drive playback from your own buttons. The `manual` track below does not autoplay; `seek(600)` moves directly to the midpoint of its 1200ms duration:

<ComponentPreview files="manual-controls" size="sm" controls={{ animation: true }} />

`animationRef` / vanilla `view.animation` is the Scene-level clock handle: SVG controls the diagram's interactive WAAPI handle, while Canvas controls the shared rAF clock. Inside a handler, use hydration's [`context.animation`](/kernel/packages/render/hydration#the-handler-context) to control the hit id; both backends expose per-id controls there.

The playback switch has three states. React and Vanilla use different shapes but the same priority:

| Intent        | React             | Vanilla                         |
| ------------- | ----------------- | ------------------------------- |
| Follow system | omit `animate`    | omit `animation.enabled`        |
| Force on      | `animate={true}`  | `animation: { enabled: true }`  |
| Force off     | `animate={false}` | `animation: { enabled: false }` |

<ComponentAlert
  type="warn"
  title="Explicit on overrides the system preference"
  description="animate={true} and animation.enabled: true still play under prefers-reduced-motion. Most apps should omit the switch to follow the system, and force animation only after an explicit user choice or when motion is essential to the experience."
/>

<SourceLinks
  sources={[
    {
      label: 'Playback switch and autoplay detection',
      path: 'packages/kernel/render/src/animation/runtime.ts',
      startLine: 124,
      endLine: 154,
    },
    {
      label: 'SVG interactive triggers and controls',
      path: 'packages/kernel/render/src/animation/retained/binding.ts',
      startLine: 117,
      endLine: 167,
    },
    {
      label: 'Canvas shared clock and imperative handle',
      path: 'packages/kernel/render/src/runtime/canvas-renderer.ts',
      startLine: 915,
      endLine: 970,
    },
  ]}
/>

### Static frames and fallback

Pass `<Layout snapshotAt={ms}>` (or `renderToSvgString(ir, { animation: { snapshotAt } })` / `mountSvg(…, { animation: { snapshotAt } })`) to render **one frame frozen at that time**. It starts no playback and emits no animation rules; it only bakes autoplay-track values into static attributes or transforms. This is useful for SSR poster frames, thumbnails, and social-card previews.

<ComponentPreview files="snapshot" size="xs" />

```tsx
// grow (0→1, 300ms) frozen at 150ms ≈ half size
<Layout snapshotAt={150}>
  <Node position={[0, 0]} animations={[grow({ duration: 300 })]}>
    150ms
  </Node>
</Layout>
```

`snapshotAt` has priority over the playback switch, but samples only `load` or omitted triggers. `manual`, `visible`, and event tracks stay at base because a poster represents "loaded, before any interaction." Without a snapshot, explicitly disabling animation, following a reduced-motion preference, or lacking backend animation support also renders the base figure without throwing or dropping it.

The default `fill="forwards"` keeps the last frame after playback. Built-in intro and return-to-base presets end at common base values, and `spin` at 360° is equivalent to 0°. `colorShift` and `cameraTo` cannot read an element or camera base, so you provide their destinations explicitly. `AnimationTrackSchema` documents "the last frame should be settled" as an authoring convention rather than comparing it against the base; hand-written tracks and explicit destinations must land on the intended resting state when they need graceful fallback.

<ComponentAlert
  title="Ending at rest is an authoring convention"
  description="Built-in intro and return-to-base presets use common base values; hand-written tracks and explicit to values still need your guarantee. Otherwise live playback ends at the last frame while disabled animation shows the base, and the two results can differ."
/>

<SourceLinks
  sources={[
    {
      label: 'SVG snapshot baking and interactive-track filtering',
      path: 'packages/kernel/render/src/svg/animation/keyframes.ts',
      startLine: 219,
      endLine: 295,
    },
    {
      label: 'Canvas snapshot priority and static rendering',
      path: 'packages/kernel/react/src/render/canvas/CanvasHost.tsx',
      startLine: 198,
      endLine: 230,
    },
    {
      label: 'Settled-last-frame data contract',
      path: 'packages/kernel/core/src/schemas/animation/schema.ts',
      startLine: 51,
      endLine: 86,
    },
  ]}
/>

## API Reference

### Component props

| Prop           | Type                          | Default     | Description                                               |
| -------------- | ----------------------------- | ----------- | --------------------------------------------------------- |
| `animations`   | `Array<IRAnimationTrack>`     | —           | Tracks attached to a Node, Path, Scope, or Layout         |
| `animate`      | `boolean`                     | `undefined` | Layout playback switch; omitted follows system preference |
| `snapshotAt`   | `number`                      | —           | Layout static-snapshot time in milliseconds               |
| `animationRef` | `LayoutProps['animationRef']` | —           | Layout's scene-level playback control handle              |

### AnimationTrack

| Prop         | Type                                                                                   | Default                          | Description                                                                             |
| ------------ | -------------------------------------------------------------------------------------- | -------------------------------- | --------------------------------------------------------------------------------------- |
| `property`   | <ApiValues name="AnimationProperty" /> \| `string`                                     | —                                | Property channel; `viewBox` is Layout-only                                              |
| `keyframes`  | `Array<IRKeyframe>`                                                                    | —                                | Absolute-value keyframes sorted by `at`                                                 |
| `duration`   | `number`                                                                               | —                                | Duration of one iteration in milliseconds                                               |
| `delay`      | `number`                                                                               | `0`                              | Delay before the first iteration in milliseconds                                        |
| `easing`     | <ApiValues name="AnimationEasing" /> \| `string` \| `[number, number, number, number]` | `'linear'`                       | Track easing; a keyframe can override the next segment                                  |
| `iterations` | `number` \| `'infinite'`                                                               | `1`                              | Playback count                                                                          |
| `direction`  | <ApiValues name="AnimationDirection" />                                                | `'normal'`                       | Direction of each iteration                                                             |
| `fill`       | <ApiValues name="AnimationFill" />                                                     | `'forwards'`                     | Value used outside the active interval                                                  |
| `trigger`    | <ApiValues name="AnimationTrigger" /> \| `IRAnimationTrigger`                          | `'load'`                         | Playback trigger                                                                        |
| `origin`     | `IRAnimationOrigin`                                                                    | derived center / backend default | Pivot for scale, scaleX/Y, and rotate; set explicitly for consistency across primitives |

## Related

- [Shadow](/kernel/components/effects/shadow): the element-level `shadow`
- [Blend](/kernel/components/effects/blend): the element-level `blendMode`
- [Custom Animation](/kernel/components/effects/custom-animation): register easing curves and Canvas property channels
