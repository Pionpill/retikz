---
title: Way
description: 查询 Draw 的 way 数组如何表达目标、折角、闭合与相对坐标，以及它们如何展开为有序 Step。
---

`way` 是 `<Draw>` 的核心：target 决定路径经过哪里，折角与曲线算子决定相邻 target 如何连接，`DrawWay.Cycle` 闭合路径，相对坐标则改变 target 的求值基准。

可以把 `way` 看成一个很小的 path 语言：target 产生点，`'-|'` / `'|-'` 改写前后两个点之间的段，`{ label }` 修饰下一段，`{ curve }` / `{ cubic }` / `{ bend }` 消耗下一项生成曲线，`{ arc }` / `{ circle }` / `{ ellipse }` 围绕上一点画形状而不消耗下一项。

## 用法

```tsx
import { DrawWay } from '@retikz/core';
import { Draw } from '@retikz/react';
```

```tsx
<Draw way={['a', DrawWay.Hv, 'b']} />
```

## 例子

### Target 形态

同一条 `way` 可以混合节点 id、笛卡尔坐标、极坐标和对象形态 target。

<ComponentPreview files="draw-way-types" size="sm" />

### 单轴连接

`{ horizontalTo }` 只连接到目标 x，`{ verticalTo }` 只连接到目标 y；两者各自产生一个 `axis-line` Step，不像 `-\|` / `\|-` 那样发出两段。方向按当前 Scope 的局部坐标轴解释。

<ComponentPreview files="axis-line" size="sm" />

算子值只接受笛卡尔坐标、NodeTarget 或对应字符串 shorthand。relative、polar、offset、between，以及同时含多个 Way operator 的对象会由 `parseWay` 直接拒绝。

### 折角

切换四种折角方向，观察蓝色路径经过一个或两个正交转折点。选择 `-\|-` / `\|-\|` 后可用 `fraction` 在 `0..1` 内移动中间腿，默认 `0.5`；灰色 dotted 直线只标出两个端点的直接连接。

<Comparison target="tikz" title="折角写法">

`'-|'` / `'|-'` 沿用 TikZ path 的 `(A) -| (B)` / `(A) |- (B)` 两段写法。`'-|-'` / `'|-|'` 是流程图常见的三段扩展；裸 token 使用中点，`{ via: '-|-', fraction: 0.3 }` 可配置中间腿位置。

</Comparison>

<ComponentPreview files="way-fold" size="sm" />

### 闭合

切换路径状态，观察“闭合路径”如何用 `DrawWay.Cycle` 的实线回边取代 dotted 缺口，并形成可填充区域。

<ComponentPreview files={['way-cycle', 'way-cycle.data.ts']} size="sm" />

### 相对坐标

`+` 的第二段仍以起点为偏移基准，`++` 则从第一段终点继续偏移。调整二维偏移，dotted 直角辅助线会标出第二段实际使用的基准。

<ComponentPreview files={['way-relative', 'way-relative.data.ts']} size="sm" />

## 技术原理

<ComponentAlert
  type="tip"
  title="上手阶段可跳过本节"
  description="需要排查算子顺序、label 挂载或相对坐标累积时再回来读。"
/>

本节只放大 `way` 到 `<Path>` / `<Step>` 的局部展开；完整的 JSX → IR（中间表示） → Scene 编译管线见[架构](/kernel/concepts/design/principles)。

### WayItem 如何变成 Step

`<Draw>` 从左到右扫描 `way`。首项必须是 target，并固定产出 `move`；后续 item 先按角色分类，再按各自的消费规则产出等价 Step。这里的“消费”是指当前规则会从数组中取走哪些 item。

<ComponentPreview files="way-parser-flow" size="sm" hideCode />

| 类别                              | 扫描如何前进           | 产出                          |
| --------------------------------- | ---------------------- | ----------------------------- |
| target                            | 消费当前项             | 首项 `move`，其余 `line`      |
| horizontalTo / verticalTo         | 消费当前项             | 一个 `axis-line` Step         |
| 两段 / 三段 fold、curve / bend 等 | 消费算子 + 下一 target | 一个对应的折角或曲线 Step     |
| arc / circle / ellipse            | 只消费当前算子         | 一个形状 Step；下一项仍会扫描 |
| `{ label }`                       | 暂存到下一段           | 自身不产出 Step               |
| `DrawWay.Cycle`                   | 消费当前项             | `cycle`，且不能挂 label       |

因此，折角和曲线算子后面必须紧跟 target；形状算子不会吞掉下一项。`{ label }` 则先进入一个待挂状态，由下一个真正产出路径段的 item 消费，所以它要写在 target 或段算子之前。连续 label、末尾 label，以及给 cycle 挂 label 都会被拒绝。

最后，`<Draw>` 把 `way` 与描边、填充和箭头等视觉属性交给 `<Path>`；Vanilla 再把它归一化为 Step 序列。它只是更紧凑的语法糖（Sugar），不引入另一套路径语义。

<SourceLinks
  sources={[
    {
      label: 'WayItem 分类与相邻 target 校验',
      path: 'packages/kernel/core/src/parse/way.ts',
      startLine: 240,
      endLine: 333,
    },
    {
      label: 'way 按序归约与 label 状态',
      path: 'packages/kernel/core/src/parse/way.ts',
      startLine: 450,
      endLine: 511,
    },
    {
      label: 'Draw 将输入透传给 Path',
      path: 'packages/kernel/react/src/sugar/path/Draw.tsx',
      startLine: 50,
      endLine: 54,
    },
  ]}
/>

### 相对坐标在 Path 编译时求值

`parseWay` 只把 `'+10,0'` / `'++10,0'` 和对应对象写法归一为 `relative` / `relativeAccumulate` target，不会提前算成绝对坐标。Path 编译逐段处理时，两者都以上一个有效终点 `prevEnd` 为基准；首项没有基准时回退到 `[0, 0]`。

`Relative` 只借用 `prevEnd` 算出本次终点，不替换这个基准；`Accumulate` 会把结果写回 `prevEnd`，所以下一个相对项从新位置继续。若当前处在变换后的 [Scope](/kernel/components/layout/scope) 中，偏移会先在该 Scope 的局部坐标中相加，再投影回全局。

<SourceLinks
  sources={[
    {
      label: '对象相对坐标归一',
      path: 'packages/kernel/core/src/parse/way.ts',
      startLine: 229,
      endLine: 240,
    },
    {
      label: '字符串相对坐标归一',
      path: 'packages/kernel/core/src/parse/target-sugar.ts',
      startLine: 10,
      endLine: 29,
    },
    {
      label: '相对坐标求值与 prevEnd 更新',
      path: 'packages/kernel/core/src/compile/path/host/relative.ts',
      startLine: 77,
      endLine: 95,
    },
  ]}
/>

## API 参考

### WayItem

| 形态               | 例                                                                                              | 含义                                                       |
| ------------------ | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| 节点 id            | `'a'`                                                                                           | 引用同一张图内的 Node；端点自动贴 border                   |
| 显式锚点           | `{ id: 'a', anchor: 'top' }` 或 `'a.top'`                                                       | 锁定命名 anchor / 角度 / 边上比例点；对象形态是 IR 主契约  |
| 笛卡尔             | `[120, 0]`                                                                                      | 直接给坐标                                                 |
| 极坐标             | `{ origin: 'a', angle: 60, radius: 80 }`                                                        | 极坐标，详见 [定位](/kernel/concepts/basic/position)       |
| 偏移定位           | `{ of: 'a', offset: [10, 5] }`                                                                  | 基准 + `(dx, dy)`，与 `<Node position>` 的 offset 形态一致 |
| 比例定位           | `{ between: [{ id: 'a' }, { id: 'b' }], fraction: 0.5 }`                                        | A 到 B 连线上的比例点                                      |
| Relative           | `{ position: [10, 0], type: DrawWay.Relative }` 或 `'+10,0'`                                    | 基于前一 step 终点偏移，不更新 prevEnd                     |
| Accumulate         | `{ position: [10, 0], type: DrawWay.Accumulate }` 或 `'++10,0'`                                 | 基于前一 step 终点偏移，并更新 prevEnd                     |
| 单轴连接算子       | `{ horizontalTo: 'a' }` / `{ verticalTo: [120, 0] }`                                            | 只沿当前 host 的一个局部轴连接目标                         |
| 两段折角算子       | `'-\|'` / `'\|-'`                                                                               | 把上一项到下一项改成两段折角                               |
| 三段折角算子       | `'-\|-'` / `'\|-\|'` / `{ via: '-\|-', fraction: 0.3 }`                                         | 三段正交连接；对象形式 strict，fraction 范围 `0..1`        |
| 闭合               | `DrawWay.Cycle`                                                                                 | 闭合回 way 起点                                            |
| 曲线 / 形状 / 标注 | `{ curve }` / `{ cubic }` / `{ bend }` / `{ arc }` / `{ circle }` / `{ ellipse }` / `{ label }` | 详见 [Draw](/kernel/components/draw/overview)              |

### DrawWay

| 常量                 | 等价裸字面量 | 含义                           |
| -------------------- | ------------ | ------------------------------ |
| `DrawWay.Cycle`      | 仅常量       | 闭合关键字；避免与节点 id 撞车 |
| `DrawWay.Hv`         | `'-\|'`      | 先水平后垂直                   |
| `DrawWay.Vh`         | `'\|-'`      | 先垂直后水平                   |
| `DrawWay.Hvh`        | `'-\|-'`     | 水平 → 垂直 → 水平             |
| `DrawWay.Vhv`        | `'\|-\|'`    | 垂直 → 水平 → 垂直             |
| `DrawWay.Relative`   | 仅常量       | 非累积相对偏移                 |
| `DrawWay.Accumulate` | 仅常量       | 累积相对偏移                   |

## 相关

- [Draw](/kernel/components/draw/overview)
- [Step](/kernel/components/draw/step)
