---
title: Step
description: Step 是 Path 内的单个 Kernel 动作，覆盖直线、折角、曲线、圆弧、闭合、矩形与边标注。
---

## 用法

```tsx
import { Path, Step } from '@retikz/react';
```

```tsx
<Path>
  <Step kind="move" to="a" />
  <Step kind="line" to="b" />
</Path>
```

当你需要逐段控制 Kernel 路径动作时使用 `<Step>`：例如多次 `move`、混合直线 / 折角 / 曲线 / 圆弧，或排查 `<Draw>` 展开后的结构。日常连线优先用 [Draw](/kernel/components/draw/overview)。

## 例子

### 基础

下面这个 path 用一次 `move` + `line` 画 A→B，再 `move` 抬笔后 `line` 画一条独立的线：

<ComponentPreview files="step-basic" size="xs" />

### 折角（fold）

`kind="fold"` 在游标与目标点之间插一个直角拐点。流程图常用：

<ComponentPreview files="step-fold" size="sm" />

### 闭合（cycle）

`kind="cycle"` 把当前子路径闭合回最近一次 `move` 的起点，对应 SVG path 的 `Z`。`cycle` 没有 `to` 字段（目标隐式 = 起点）。比手动 `line` 回起点少写一个端点：

<ComponentPreview files="step-cycle" size="sm" />

### 偏移定位（offset）

`to` 也接受 **`{ of, offset }`** 形态——基准 + `(dx, dy)` 任意偏移。`of` 三态：节点 id、笛卡尔字面值、PolarPosition（递归极坐标）；和 `<Node position>` 的偏移定位完全对称：

<ComponentPreview files="step-offset-target" size="sm" />

### 相对坐标（relative / relativeAccumulate）

`to` 还可以是相对偏移对象：`{ relative: [dx, dy] }` 与 `{ relativeAccumulate: [dx, dy] }` 都基于**前一 step 的终点**（`prevEnd`，无前段时回退到 `[0, 0]`）解析；区别只是**是否推进 prevEnd**：

- `{ relative }`：多段链式偏移都基于**同一**锚点，**不**更新 prevEnd
- `{ relativeAccumulate }`：每段累积，prevEnd 跟着推进

也可写作 sugar 字符串 `'+dx,dy'` / `'++dx,dy'`（`<Step to="+1,0" />`），React adapter 会就地转成对应 IR 对象。下例两条路径 step 序列字面相同，仅差 target 形态——实线（relative）走出 L 形，虚线（relativeAccumulate）走出阶梯：

<ComponentPreview files="step-relative" size="sm" />

### 矩形（rectangle）

`kind="rectangle"` 用 `from` 和 `to` 作为两个对角点画一个轴对齐矩形；可选 `cornerRadius` 做圆角。它不像 `line` 那样依赖当前游标。日常画矩形更推荐用 [Rectangle](/kernel/components/shapes/rectangle) sugar，写底层 IR 或需要和其它 step 混排时再直接用这个 kind。

<ComponentPreview files="step-rectangle" size="sm" />

### 二次贝塞尔（curve）

`kind="curve"` 用一个控制点画二次贝塞尔曲线，对应 SVG path 的 `Q`。贝塞尔曲线不要求经过控制点；控制点更像一只手柄，用来把起点到终点之间的线段拉弯。控制点离直线越远，弯曲越明显。当前控制点使用笛卡尔 `[x, y]`：

<ComponentPreview files="step-curve" size="xs" />

### 三次贝塞尔（cubic）

`kind="cubic"` 用两个控制点画三次贝塞尔曲线，对应 SVG path 的 `C`。`control1` 影响曲线离开起点时的方向，`control2` 影响曲线靠近终点时的方向；两端方向可分别控制，因此常用于 S 形曲线、复杂连接：

<ComponentPreview files="step-cubic" size="xs" />

### 弧形简记（bend）

`kind="bend"` 是弧形连线的简记——只说明从起点看向终点时向左还是向右弯，以及弯曲角度；编译期会自动计算控制点，并展开为三次贝塞尔。`bendDirection` 是相对 from→to 路径方向的视觉左 / 右；`bendAngle` 缺省 30：

<ComponentPreview files="step-bend" size="md" />

### 过点平滑曲线（smooth）

`kind="smooth"` 画一条**穿过一串点**的光滑曲线——画数据折线、流线、自由曲线时不必手算每段贝塞尔控制点。它不用 `to`，改用 `points`：当前**游标是隐式的第一个 knot**，曲线从游标起、依次穿过 `points` 里每个点，游标终于末点（所以前面要先有一个 `move` 或别的 step 设游标，否则该 path 被跳过并发 `PATH_TOO_SHORT` 警告，与 `line` / `curve` 作首段一致）。编译期用 centripetal Catmull-Rom 转成三次贝塞尔链。下例同一组点，蓝色是 `smooth`、灰色是逐点 `line` 折线：

<ComponentPreview files="step-smooth" size="sm" />

`tension` 调曲线松紧：缺省 1 是标准 centripetal Catmull-Rom，`<1` 把曲线拉得更紧（更接近折线），`>1` 更鼓。无论怎么调，曲线始终穿过每个点，变的只是各段切线长度：

<ComponentPreview files="step-smooth-tension" size="sm" />

<ComponentAlert
  title="vanilla 写法"
  description="vanilla 暂未提供 way 文法的 smooth token；用 vanilla 时把 smooth 作为 IR 对象放进 path children：{ type: 'step', kind: 'smooth', points: [...], tension }。下方代码视图的 Vanilla 标签即此形态。"
/>

### 弧段（arc）

`kind="arc"` 按起末角度 + 半径画一段圆弧——默认以**游标为圆心**，也可用 `center` 显式指定圆心（脱离游标独立定位）。半径二选一：`radius` 画圆弧，`radius={{ x, y }}` 画椭圆弧。它描述的是“从哪个角度扫到哪个角度的弧”，不需要 `to`，常用于角度标注、圆周片段或几何辅助线。角度沿 retikz polar 约定：0° 朝 +x，90° 视觉朝下；画完画笔停在弧的终点：

<ComponentPreview files="step-arc" size="sm" />

### 整圆（circlePath）

`kind="circlePath"` 以**游标为圆心**，按 `radius` 画圆。不给角度时画一个完整闭合的圆——画完画笔回到圆心，常用于画端点圆点、圆形节点边框等场景。也可给 `startAngle` + `endAngle` 画**部分圆**（半圆 / 弧段），用 `closed` 选闭合方式：`'chord'`（弦，默认）/ `'sector'`（连圆心成扇形）/ `'open'`（不闭合的纯弧）：

<ComponentPreview files="step-circle" size="xs" />

### 整椭圆（ellipsePath）

`kind="ellipsePath"` 以**游标为圆心**，按 `radius={{ x, y }}` 画椭圆。不给角度时画一个完整闭合的椭圆——画完画笔回到圆心，`radius.x === radius.y` 时退化为圆。同 circlePath，也可给 `startAngle` + `endAngle` 画**部分椭圆**，`closed` 选闭合方式（`'chord'` 默认 / `'sector'` / `'open'`）：

<ComponentPreview files="step-ellipse" size="sm" />

<Comparison target="tikz" title="Step 曲线语法">

| retikz                                     | TikZ 对照                                              |
| ------------------------------------------ | ------------------------------------------------------ |
| `kind="curve"`                             | `.. controls (B) ..`                                   |
| `kind="cubic"`                             | `.. controls (B) and (C) ..`                           |
| `kind="bend"`                              | `to[bend left=N]` / `to[bend right=N]`                 |
| `kind="smooth"`                            | `plot[smooth, tension=…] coordinates {…}`              |
| `outAngle` / `inAngle` / `looseness`       | `out=` / `in=` / `looseness=`                          |
| `kind="arc"`                               | `arc[start angle=…, end angle=…, radius=…]`            |
| `kind="circlePath"` / `kind="ellipsePath"` | `circle[radius=…]` / `ellipse[x radius=…, y radius=…]` |

</Comparison>

### 边标注（label）

给 `<Step>` 挂一个 `label` 就能在该段画线的中部 / 起点 / 终点附近放一段文字。`label` 是个对象：

- `text`：必填，标签文字（单行）
- `position`：`'at-start' | 'very-near-start' | 'near-start' | 'midway' | 'near-end' | 'very-near-end' | 'at-end'` 七个 keyword，或 0..1 的数值 t（任意精细位置），缺省 `'midway'`（t=0.5）
- `side`：`'top'` / `'bottom'` / `'left'` / `'right'`，缺省 `'top'`。也接受 compass 别名 `north` / `south` / `east` / `west`；`sloped: true` 让标签沿段切线方向旋转
- `textColor`：标签文字色；缺省按 **scope `labelDefault` → 宿主 path 主色 `color` → currentColor** 回退（跟主色不跟 stroke）
- `opacity`：标签独立透明度 0..1；与所属 path 的 opacity 相乘（元素内轴；跨 scope 不复合）
- `font`：标签字体覆盖（family / size / weight / style），逐字段回退 `labelDefault` → 渲染端默认

两种等价写法——prop 形态 `<Step label={{...}} />`，或 sugar 形态 `<Step><EdgeLabel>...</EdgeLabel></Step>`（需 `import { EdgeLabel } from '@retikz/react'`）；**两者并存时 prop 优先**。`'move'` / `'cycle'` / `'rectangle'` 不挂 label——move 不画线、cycle 是闭合标记、rectangle 无 label 字段，挂上去会被静默忽略：

<ComponentPreview files="step-label" />

#### 标签颜色与字体

边标注颜色跟随宿主 path 的**主色 `color`**——`<Path color>` 时标注自动同色；只设 `stroke`（不设 `color`）标注不跟，保持默认色。也可用 `textColor` 显式覆盖，`font` / `opacity` 做小一号 / 淡化的辅助标注：

<ComponentPreview files="step-label-color" size="sm" />

scope `labelDefault` 给整组标注统一默认（见 [Scope 样式](/kernel/components/layout/scope)）；继承顺序：label 显式 > `labelDefault` > 宿主 path 主色 > currentColor。

#### label.position：7 个 keyword + 任意数值 t

`position` 接受 7 个 keyword 或 0..1 的数值 t，keyword 是 t 的语义糖：

| keyword             | 对应 t  | 说明           |
| ------------------- | ------- | -------------- |
| `'at-start'`        | `0`     | 段起点         |
| `'very-near-start'` | `0.125` | 起点附近       |
| `'near-start'`      | `0.25`  | 起点 1/4 处    |
| `'midway'`          | `0.5`   | 段中点（缺省） |
| `'near-end'`        | `0.75`  | 终点 1/4 处    |
| `'very-near-end'`   | `0.875` | 终点附近       |
| `'at-end'`          | `1`     | 段终点         |

`position={0.3}` 这种任意数值可以精确指定段上位置。**数值超出 `[0, 1]` 区间在 schema 阶段直接拒绝，不在编译阶段 clamp**（`position={-0.1}` / `position={1.2}` 均会让 IR 校验失败）。

### 样式

`<Step>` 自身没有 stroke 类视觉属性——样式都挂在父 `<Path>` 上，整条 Path 的所有 Step 共享同一套 `stroke` / `strokeWidth` / `dashPattern` / `dashOffset`：

<ComponentPreview files="step-styling" size="sm" />

<Comparison target="tikz" title="Step 基础路径语法">

| retikz                                    | TikZ 对照                             |
| ----------------------------------------- | ------------------------------------- |
| `kind="fold"` + `via="-\|"` / `"\|-"`     | `(A) -\| (B)` / `(A) \|- (B)`         |
| `kind="cycle"`                            | `cycle`                               |
| `{ of, offset }` target                   | calc library 的 `($(of) + (dx, dy)$)` |
| `{ relative }` / `{ relativeAccumulate }` | `(+x, +y)` / `(++x, ++y)`             |
| `kind="rectangle"`                        | `(a) rectangle (b)`                   |

</Comparison>

## 技术原理

<ComponentAlert
  type="tip"
  title="上手阶段可跳过本节"
  description="想排查首段为何不画线、相对坐标从哪里起算，或标签为何没有落在视觉中点时，再回来读。"
/>

### 从标记到有序 Step

`<Step>` 是返回 `null` 的标记组件，本身不创建 DOM 或图形。父 `<Path>` 按 JSX 顺序收集这些标记，把字符串 target、缺省 `kind` 和 `<EdgeLabel>` child 归一成可序列化的 `IRStep`；显式 `label` prop 比 child 优先。

除单个自包含的 `rectangle` 外，一条 Path 至少需要两个 Step。首段不是 `move` 时，React 适配层会把它改成 `move`：有 `to` 就保留该目标，没有 `to` 就回退到 `[0, 0]`。因此首段写成 `line` 只会定位游标，不会画出这条线。

<SourceLinks
  sources={[
    {
      label: 'Step 标记组件',
      path: 'packages/kernel/react/src/kernel/components/Step.tsx',
      startLine: 235,
      endLine: 240,
    },
    {
      label: 'Path 收集 Step 与标签优先级',
      path: 'packages/kernel/react/src/kernel/adapter/builder.ts',
      startLine: 294,
      endLine: 367,
    },
    {
      label: '首段与最短序列规范化',
      path: 'packages/kernel/react/src/kernel/adapter/builder.ts',
      startLine: 525,
      endLine: 539,
    },
  ]}
/>

### 游标驱动的几何编译

进入默认 `kind="stroke"` 的 core 编译后，Step 不会各自独立渲染。编译器先把相对 target 归一为当前 scope 的坐标，再按声明顺序推进游标、解析节点引用与边界连接，最后把每个 `kind` 降级为统一的路径命令和边标注几何：

<ComponentPreview files="step-compile-flow" size="xs" hideCode />

| Step 家族                                                        | 编译行为                                                   | 用户可见结果                                             |
| ---------------------------------------------------------------- | ---------------------------------------------------------- | -------------------------------------------------------- |
| `move` / `cycle`                                                 | 维护游标与最近一次子路径起点                               | `move` 不画线；`cycle` 闭合回最近一次 `move`             |
| `line` / `curve` / `cubic`<br />`fold` / `bend`                  | 解析前后目标并按节点边界裁切<br />再发射直线或 Bezier 命令 | 节点 id 自动接到边界；`fold` / `bend` 最终仍是普通路径段 |
| `smooth` / `generator`<br />`arc` / `circlePath` / `ellipsePath` | 生成一组路径命令，并记录画完后的真实笔位                   | 下一段从生成结果的终点续接；整圆 / 整椭圆画完回到圆心    |
| `rectangle`                                                      | 独立解析 `from` / `to`，不要求前置游标                     | 可以作为 Path 中唯一的 Step                              |

这套顺序让 JSX、直接写 IR 和 `<Draw>` sugar 最终复用同一条几何编译路径；renderer 只消费降级后的命令，不需要理解十三种 Step 写法。

<SourceLinks
  sources={[
    {
      label: '相对目标归一化',
      path: 'packages/kernel/core/src/compile/path/host/relative.ts',
      startLine: 11,
      endLine: 108,
    },
    {
      label: '目标归一化与逐段编译主循环',
      path: 'packages/kernel/core/src/compile/path/stroke/emit.ts',
      startLine: 54,
      endLine: 147,
    },
    {
      label: '普通连线 Step 的几何降级',
      path: 'packages/kernel/core/src/compile/path/stroke/segments.ts',
      startLine: 41,
      endLine: 125,
    },
    {
      label: '生成器、闭合与矩形 Step 的几何降级',
      path: 'packages/kernel/core/src/compile/path/stroke/shapes.ts',
      startLine: 85,
      endLine: 220,
    },
    {
      label: '圆弧、圆、椭圆与 smooth 的几何降级',
      path: 'packages/kernel/core/src/compile/path/stroke/shapes.ts',
      startLine: 223,
      endLine: 445,
    },
  ]}
/>

### t 在每种 step kind 上的几何含义

`label.position` 会先换算成 `0..1` 的参数 t。编译器再向当前 kind 的几何采样器索取该处的坐标与切线：`side` 根据坐标偏移，`sloped` 根据切线旋转。因此 t 表示的是“该 kind 的参数进度”，不一定是视觉弧长进度。

| Step kind             | t 的参数化方式                                             | `t=0.5` 的含义                        |
| --------------------- | ---------------------------------------------------------- | ------------------------------------- |
| `'line'`              | 归一化弧长                                                 | 直线中点                              |
| `'fold'`              | 两段折线各占一半 `t`，与各段长度无关                       | `t=0.5` 正好落在拐角                  |
| `'curve'` / `'cubic'` | Bezier 参数，**不是弧长**                                  | 通常不是视觉中点                      |
| `'bend'`              | 降级成 cubic 后使用 Bezier 参数                            | 与 cubic 相同                         |
| `'smooth'`            | 每个生成的 cubic 等分 t 区间<br />区间内再使用 Bezier 参数 | 取决于生成段数，不按整条曲线弧长      |
| `'generator'`         | 当前按进入 step 时的游标到最终笔位做直线插值               | 两端点的中点，不跟随中间命令          |
| `'arc'`               | 在 `startAngle..endAngle` 间线性插值                       | 起末角的中间角度                      |
| `'circlePath'`        | 整圆按角度 0→360；0 在 +x                                  | 180°（-x）                            |
| `'ellipsePath'`       | 同 circlePath，按参数角而非等弧长采样                      | 180°（-x）；任意 `t` 不按弧长均匀分布 |

整圆和椭圆的角度递增遵循屏幕坐标，因此 90° 位于视觉下方。

<SourceLinks
  sources={[
    {
      label: 'label.position 到 t 的映射',
      path: 'packages/kernel/core/src/compile/path/host/label.ts',
      startLine: 50,
      endLine: 69,
    },
    {
      label: '按 Step 几何采样边标注',
      path: 'packages/kernel/core/src/compile/path/stroke/sampling.ts',
      startLine: 41,
      endLine: 79,
    },
    {
      label: '边标注偏移与切线旋转',
      path: 'packages/kernel/core/src/compile/path/host/label.ts',
      startLine: 75,
      endLine: 166,
    },
  ]}
/>

## API 参考

| 属性           | 类型                                                                                                                                                        | 默认值   | 描述                                                                                                                                                                                                                                                                                                                                   |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `kind`         | `'move' \| 'line' \| 'fold' \| 'cycle' \| 'curve' \| 'cubic' \| 'bend' \| 'smooth' \| 'generator' \| 'arc' \| 'circlePath' \| 'ellipsePath' \| 'rectangle'` | `'line'` | 见下方"十三种取值"表                                                                                                                                                                                                                                                                                                                   |
| `via`          | `'-\|' \| '\|-'`                                                                                                                                            | —        | 仅 `kind="fold"` 必填：`-\|` 先水平后垂直；`\|-` 先垂直后水平                                                                                                                                                                                                                                                                          |
| `from`         | `IRTarget`                                                                                                                                                  | —        | 仅 `kind="rectangle"` 必填：矩形一个角点                                                                                                                                                                                                                                                                                               |
| `to`           | `IRTarget`                                                                                                                                                  | —        | `kind="cycle"` / `'arc'` / `'circlePath'` / `'ellipsePath'` 时不需要；`kind="rectangle"` 与其它会推进游标的 kind 必填——目标点：节点 id、笛卡尔 `[x, y]`、极坐标对象、偏移定位对象 `{ of, offset }`、比例定位 `{ between, fraction }`、相对偏移对象 `{ relative }` / `{ relativeAccumulate }`，或 sugar 字符串 `'+dx,dy'` / `'++dx,dy'` |
| `name`         | `string`                                                                                                                                                    | —        | 仅 `kind="generator"` 必填：内置或 `<Layout pathGenerators>` 注册的路径生成器名称                                                                                                                                                                                                                                                      |
| `params`       | `IRJsonObject`                                                                                                                                              | —        | 仅 `kind="generator"` 必填：传给路径生成器的 JSON-safe 参数；如内置 `parabola` 使用 `{ control }`                                                                                                                                                                                                                                      |
| `cornerRadius` | `number`                                                                                                                                                    | —        | 仅 `kind="rectangle"` 可选：矩形圆角半径                                                                                                                                                                                                                                                                                               |

### kind 的十三种取值

| kind            | 行为                                              | 底层路径类比    |
| --------------- | ------------------------------------------------- | --------------- |
| `'move'`        | 移动游标，**不画线**                              | path "M"        |
| `'line'`        | 从当前游标画直线到目标点                          | path "L"        |
| `'fold'`        | 经一个直角拐点折到目标点                          | 两段 path "L"   |
| `'cycle'`       | 闭合回最近一次 move 起点（无 `to`）               | path "Z"        |
| `'curve'`       | 二次贝塞尔（一个控制点）                          | path "Q"        |
| `'cubic'`       | 三次贝塞尔（两个控制点）                          | path "C"        |
| `'bend'`        | 弧形简记（方向 + 角度），编译期退到 cubic         | path "C"        |
| `'smooth'`      | 过游标 + `points` 的平滑曲线，编译期退到 cubic 链 | path "C"×N      |
| `'generator'`   | 调用内置或注册的路径生成器                        | provider 产命令 |
| `'arc'`         | 以游标为圆心，按起末角度 + 半径画弧               | path "A"        |
| `'circlePath'`  | 以游标为圆心画整圆，画完回到圆心                  | path "A"×2      |
| `'ellipsePath'` | 以游标为圆心画整椭圆，画完回到圆心                | path "A"×2      |
| `'rectangle'`   | 用 `from` 和 `to` 作为两对角画矩形                | 独立矩形子路径  |

### via 的两种取值

仅 `kind="fold"` 时生效，决定折角中间点的位置。设当前游标 `(px, py)`，目标 `(tx, ty)`：

| via     | 中间点     | 走向                         |
| ------- | ---------- | ---------------------------- |
| `'-\|'` | `(tx, py)` | 先水平到 `tx`，再垂直到 `ty` |
| `'\|-'` | `(px, ty)` | 先垂直到 `ty`，再水平到 `tx` |

### to 的形态

| 形态                          | 例                                                                                        | 行为                                                                                                       |
| ----------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 节点 id                       | `'a'`                                                                                     | 自动贴 Node 边界（参看 [图元关系](/kernel/concepts/core/primitive-relations)）                             |
| 节点 + 连接面                 | `{ id: 'a', boundary: 'circle' }`                                                         | 单边覆盖目标节点 [连接面](/kernel/components/node/overview#连接面-boundary)（贴边形状），不改节点占位      |
| 笛卡尔                        | `[60, 0]`                                                                                 | 直接坐标                                                                                                   |
| 极坐标                        | `{ origin: 'a', angle: 30, radius: 50 }`                                                  | 极坐标（参看 [定位](/kernel/concepts/basic/position)）                                                     |
| 偏移定位                      | `{ of: 'a', offset: [10, 5] }`                                                            | 基准 + `(dx, dy)`；`of` 三态——节点 id / 笛卡尔字面值 / PolarPosition                                       |
| 比例定位                      | `{ between: [{ id: 'a' }, { id: 'b' }], fraction: 0.5 }`                                  | A 到 B 连线上的比例点；端点必须是可独立解析的 absolute target（`{ id, anchor? }` 形态，或笛卡尔 / 极坐标） |
| 比例定位（显式锚点）          | `{ between: [{ id: 'a', anchor: 'right' }, { id: 'b', anchor: 'left' }], fraction: 0.5 }` | 端点可带显式 `anchor`，从锚点而非自动贴边解析                                                              |
| 相对偏移 (relative)           | `{ relative: [10, 0] }` 或 `'+10,0'`                                                      | 基于前一 step 终点 + `[dx, dy]`；**不**更新 prevEnd                                                        |
| 相对偏移 (relativeAccumulate) | `{ relativeAccumulate: [10, 0] }` 或 `'++10,0'`                                           | 基于前一 step 终点 + `[dx, dy]`；**更新** prevEnd                                                          |

### 曲线与圆弧

| 属性            | 类型                                                                                                                                                        | 默认值   | 描述                                                                                                                                                                                                      |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `kind`          | `'move' \| 'line' \| 'fold' \| 'cycle' \| 'curve' \| 'cubic' \| 'bend' \| 'smooth' \| 'generator' \| 'arc' \| 'circlePath' \| 'ellipsePath' \| 'rectangle'` | `'line'` | 见下方"十三种取值"表                                                                                                                                                                                      |
| `control`       | `[number, number]`                                                                                                                                          | —        | 仅 `kind="curve"` 必填：二次贝塞尔的单个控制点                                                                                                                                                            |
| `control1`      | `[number, number]`                                                                                                                                          | —        | 仅 `kind="cubic"` 必填：三次贝塞尔的第一控制点（影响起点切线）                                                                                                                                            |
| `control2`      | `[number, number]`                                                                                                                                          | —        | 仅 `kind="cubic"` 必填：三次贝塞尔的第二控制点（影响终点切线）                                                                                                                                            |
| `points`        | `Array<IRTarget>`（≥1）                                                                                                                                     | —        | 仅 `kind="smooth"` 必填：游标之后依次穿过的点（顺序敏感）。游标是隐式首 knot，单点 = 一段曲线，游标终于末点。元素形态同 `to`                                                                              |
| `tension`       | `number`（>0）                                                                                                                                              | `1`      | 仅 `kind="smooth"` 可选：切线长度乘子；缺省 1（标准 centripetal Catmull-Rom），`<1` 更紧、`>1` 更鼓                                                                                                       |
| `bendDirection` | `'left' \| 'right'`                                                                                                                                         | —        | 仅 `kind="bend"` 可选：相对 from→to 路径方向的视觉左 / 右；配 `bendAngle` 画对称弯。**给定 `outAngle` / `inAngle` 时它们优先**，此项被忽略                                                                |
| `bendAngle`     | `number`                                                                                                                                                    | `30`     | 仅 `kind="bend"` 可选：弯角度（度），限 (-180, 180)                                                                                                                                                       |
| `outAngle`      | `number`                                                                                                                                                    | —        | 仅 `kind="bend"` 可选：起点出射角（度）；配 `inAngle` 画非对称曲线 / 自环，优先于 `bendDirection` / `bendAngle`                                                                                           |
| `inAngle`       | `number`                                                                                                                                                    | —        | 仅 `kind="bend"` 可选：终点入射角（度）；与 `outAngle` 配套                                                                                                                                               |
| `looseness`     | `number`                                                                                                                                                    | `≈1`     | 仅 `kind="bend"` 可选：控制点距离的松紧因子，越大曲线越松；`from == to` 时同时缩放默认自环大小                                                                                                            |
| `startAngle`    | `number`                                                                                                                                                    | —        | `kind="arc"` 必填；`'circlePath'` / `'ellipsePath'` 可选（与 `endAngle` 配套画部分圆 / 椭圆）：起始角度（度，0° 朝 +x，90° 视觉朝下）                                                                     |
| `endAngle`      | `number`                                                                                                                                                    | —        | `kind="arc"` 必填；`'circlePath'` / `'ellipsePath'` 可选：终止角度（度），扫向由起末角度推断                                                                                                              |
| `radius`        | `number \| { x: number; y: number }`                                                                                                                        | —        | `kind="arc"`（圆弧）/ `'circlePath'` 时给：圆 / 弧半径。arc 上 `radius` 与 `radius: { x, y }` **二选一**（互斥）；circlePath 必填                                                                         |
| `center`        | `IRTarget`                                                                                                                                                  | 游标     | 仅 `kind="arc"` 可选：显式弧心，缺省取游标（前一 step 终点）；显式给定可让弧脱离游标独立定位（`<Sector>` 借此画正确扇形）                                                                                 |
| `closed`        | `'closed' \| 'chord' \| 'open' \| 'sector'`                                                                                                                 | 见下     | 仅 `kind="circlePath"` / `'ellipsePath'` 可选：闭合方式。无角度时 `'closed'`（整圆 / 整椭圆，默认）；有角度时 `'chord'`（两弧端连弦，默认）/ `'sector'`（两弧端连到圆心再闭合）/ `'open'`（不闭合的纯弧） |

### 边标注

| 属性    | 类型          | 默认值 | 描述                                                                                                                                                                                         |
| ------- | ------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `label` | `IRStepLabel` | —      | 边标注（`'move'` / `'cycle'` / `'rectangle'` 外的 kind 通用）：`{ text, position?, side?, textColor?, opacity?, font? }`；等价 sugar 写法 `<Step><EdgeLabel>…</EdgeLabel></Step>`，prop 优先 |

### label 的字段

`label` 是单个对象，对所有可挂 label 的 kind 透明：

| 字段        | 类型                                     | 默认       | 描述                                                                                                                                                                                      |
| ----------- | ---------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `text`      | `string`                                 | —          | 必填，标签文字（单行）                                                                                                                                                                    |
| `position`  | 7 keyword 之一 或 `number`（0..1）       | `'midway'` | 段上归一化位置 t——keyword 是 t 的语义糖（at-start=0 / very-near-start=0.125 / near-start=0.25 / midway=0.5 / near-end=0.75 / very-near-end=0.875 / at-end=1）；超出 [0, 1] 由 schema 拒绝 |
| `side`      | `'top' \| 'bottom' \| 'left' \| 'right'` | `'top'`    | 边标注相对段方向的侧；也接受 compass 别名 `north` / `south` / `east` / `west`                                                                                                             |
| `sloped`    | `boolean`                                | `false`    | 是否按段切线方向旋转；为 true 时仍可配合 `side` 控制偏移侧                                                                                                                                |
| `textColor` | `string`                                 | —          | 标签文字色；缺省回退 scope `labelDefault` → 宿主 path 主色 `color` → currentColor（跟主色不跟 stroke）                                                                                    |
| `opacity`   | `number`                                 | —          | 标签独立透明度 0..1；与所属 path opacity 相乘（跨 scope 不复合）                                                                                                                          |
| `font`      | `object`                                 | —          | 标签字体覆盖（family / size / weight / style）；逐字段回退 `labelDefault` → 渲染端默认                                                                                                    |

不同 step kind 上 t 的几何解释与 `smooth` / `generator` 边界见上方 "[t 在每种 step kind 上的几何含义](#t-在每种-step-kind-上的几何含义)" 表。

## 相关

- [Path](/kernel/components/draw/path)：`<Step>` 所在的 Kernel 路径容器
- [Draw Way](/kernel/components/draw/way)：日常使用的 Sugar way 写法
- [Draw](/kernel/components/draw/overview)：曲线、圆 / 椭圆和边标注的 Sugar 写法
