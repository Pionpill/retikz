{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/react`

### LayoutExtensions

<p>
  <ApiSourceLink
    label={'LayoutExtensions'}
    path={'packages/kernel/react/src/kernel/runtime/Layout.tsx'}
    startLine={143}
  >
    Register custom shapes, arrows, clips, and other drawing extensions for Layout
  </ApiSourceLink>
</p>

Pass these options through the extensions prop of Layout. Each field accepts an array of definitions; these runtime definitions are excluded from persisted scene data.

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member                     | Type                                     | Default                   | Description                      |
| -------------------------- | ---------------------------------------- | ------------------------- | -------------------------------- |
| `readonly arrows?`         | `ReadonlyArray<ArrowDefinition>`         | `BUILTIN_ARROWS`          | Arrow definitions                |
| `readonly boundaries?`     | `ReadonlyArray<BoundaryDefinition>`      | `BUILTIN_BOUNDARIES`      | Connection boundary definitions  |
| `readonly clips?`          | `ReadonlyArray<ClipDefinition>`          | `BUILTIN_CLIPS`           | Clip definitions                 |
| `readonly composites?`     | `ReadonlyArray<AnyCompositeDefinition>`  | `BUILTIN_COMPOSITES`      | Tier 2 composite expansion logic |
| `readonly pathGenerators?` | `ReadonlyArray<PathGeneratorDefinition>` | `BUILTIN_PATH_GENERATORS` | Path generator definitions       |
| `readonly pathKinds?`      | `ReadonlyArray<AnyPathKindDefinition>`   | `BUILTIN_PATH_KINDS`      | Path kind definitions            |
| `readonly patterns?`       | `ReadonlyArray<PatternDefinition>`       | `BUILTIN_PATTERNS`        | Pattern definitions              |
| `readonly shapes?`         | `ReadonlyArray<ShapeDefinition>`         | `BUILTIN_SHAPES`          | Shape definitions                |
| `readonly themeStyles?`    | `ReadonlyArray<ThemeStyleDefinition>`    | `ThemeStylesContext`      | Core Theme style definitions     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutExtensions = Readonly<{
  shapes?: ReadonlyArray<ShapeDefinition>;
  boundaries?: ReadonlyArray<BoundaryDefinition>;
  clips?: ReadonlyArray<ClipDefinition>;
  arrows?: ReadonlyArray<ArrowDefinition>;
  patterns?: ReadonlyArray<PatternDefinition>;
  pathGenerators?: ReadonlyArray<PathGeneratorDefinition>;
  pathKinds?: ReadonlyArray<AnyPathKindDefinition>;
  composites?: ReadonlyArray<AnyCompositeDefinition>;
  themeStyles?: ReadonlyArray<ThemeStyleDefinition>;
}>;
```

</DocTab>

</DocTabs>

### LayoutProps

<p>
  <ApiSourceLink label={'LayoutProps'} path={'packages/kernel/react/src/kernel/runtime/Layout.tsx'} startLine={192}>
    Public props for React Layout
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<DocSteps>

<DocStep title="Drawing input and defaults">

| Member          | Type                                      | Default | Description                                                                                                                                            |
| --------------- | ----------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `children?`     | `ReactNode`                               | —       | Kernel or Sugar JSX children                                                                                                                           |
| `ir?`           | `IRScene`                                 | —       | Persisted Source IR, used instead of children                                                                                                          |
| `theme?`        | `IRScene['theme']`                        | —       | Theme written to the Scene root and inherited by descendant Composites                                                                                 |
| `rootScope?`    | `Pick<InputScope, 'style' \| 'defaults'>` | —       | Implicit root Scope overrides for JSX children; complete IR takes precedence, while host CSS style applies independently                               |
| `nodeDistance?` | `number`                                  | `24`    | Default relative node distance in drawing units; applies when position uses direction/of without distance                                              |
| `fontSize?`     | `number`                                  | `16`    | Default font size in drawing units; used when font.size is omitted and as the root size for presets and rem, without overriding explicit numeric sizes |

</DocStep>

<DocStep title="Host and rendering">

| Member       | Type                | Default | Description                                                                                                                                      |
| ------------ | ------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `width?`     | `number \| string`  | —       | CSS width of SVG or Canvas; defaults to content width, with a single numeric axis deriving the other from the content aspect ratio               |
| `height?`    | `number \| string`  | —       | CSS height of SVG or Canvas; defaults to content height, while CSS strings use browser layout                                                    |
| `viewBox?`   | `IRViewBox`         | —       | Explicit viewport                                                                                                                                |
| `className?` | `string`            | —       | Host className                                                                                                                                   |
| `style?`     | `CSSProperties`     | —       | Host inline styles                                                                                                                               |
| `renderer?`  | `'svg' \| 'canvas'` | `'svg'` | Rendering backend; an explicit value takes precedence, otherwise inherits the Renderer context and falls back to SVG when no context is provided |
| `idPrefix?`  | `string`            | —       | SVG resource id prefix                                                                                                                           |
| `handlers?`  | `HydrationHandlers` | —       | Hydration handler registry for IR input                                                                                                          |

</DocStep>

<DocStep title="Extension registration">

| Member        | Type               | Default | Description                                                                   |
| ------------- | ------------------ | ------- | ----------------------------------------------------------------------------- |
| `extensions?` | `LayoutExtensions` | —       | Runtime extensions grouped by capability, using the Core compilation contract |
| `lowerTex?`   | `LowerTex`         | —       | Formula lowering capability                                                   |

</DocStep>

<DocStep title="Animation">

| Member                 | Type                              | Default | Description                                      |
| ---------------------- | --------------------------------- | ------- | ------------------------------------------------ |
| `animations?`          | `ReadonlyArray<IRAnimationTrack>` | —       | Scene-root animations                            |
| `animate?`             | `boolean`                         | —       | Whether to play animations                       |
| `snapshotAt?`          | `number`                          | —       | Time at which to sample a static animation frame |
| `animationRef?`        | `Ref<AnimationControls \| null>`  | —       | Animation controller ref                         |
| `easings?`             | `EasingRegistry`                  | —       | Easing registry                                  |
| `animationProperties?` | `AnimationPropertyRegistry`       | —       | Animation property registry                      |

</DocStep>

<DocStep title="Execution and results">

| Member             | Type                                                  | Default | Description                                                                                                     |
| ------------------ | ----------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------- |
| `runtime?`         | `LayoutRuntimeOptions`                                | —       | Retained or static processing mode                                                                              |
| `authoring?`       | `unknown`                                             | —       | JSX input metadata for a custom compileDriver; ignored when ir is supplied and excluded from persisted Scene IR |
| `compileDriver?`   | `VanillaCompileDriver`                                | —       | Domain-neutral Vanilla compile driver                                                                           |
| `artifacts?`       | `CompileArtifactOptions`                              | —       | Requested compilation artifacts                                                                                 |
| `onArtifacts?`     | `(artifacts: ReadonlyArray<CompileArtifact>) => void` | —       | Notification after artifacts are committed successfully                                                         |
| `onCompileResult?` | `(result: CompileResult) => void`                     | —       | Notification of the complete Core compilation result                                                            |

</DocStep>

</DocSteps>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutProps = {
  rootScope?: Pick<InputScope, 'style' | 'defaults'>;
  ir?: IRScene;
  theme?: IRScene['theme'];
  children?: ReactNode;
  authoring?: unknown;
  compileDriver?: VanillaCompileDriver;
  handlers?: HydrationHandlers;
  runtime?: LayoutRuntimeOptions;
  width?: number | string;
  height?: number | string;
  viewBox?: IRViewBox;
  className?: string;
  style?: CSSProperties;
  renderer?: 'svg' | 'canvas';
  animate?: boolean;
  snapshotAt?: number;
  animationRef?: Ref<AnimationControls | null>;
  animations?: ReadonlyArray<IRAnimationTrack>;
  easings?: EasingRegistry;
  animationProperties?: AnimationPropertyRegistry;
  idPrefix?: string;
  nodeDistance?: number;
  fontSize?: number;
  extensions?: LayoutExtensions;
  lowerTex?: LowerTex;
  artifacts?: CompileArtifactOptions;
  onArtifacts?: (artifacts: ReadonlyArray<CompileArtifact>) => void;
  onCompileResult?: (result: CompileResult) => void;
};
```

</DocTab>

</DocTabs>

### LayoutRetainedRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutRetainedRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={14}
  >
    Retained Runtime configuration for React Layout
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member                     | Type                                      | Default                      | Description                                                                                     |
| -------------------------- | ----------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------- |
| `readonly mode?`           | `typeof LayoutRuntimeMode.Retained`       | `LayoutRuntimeMode.Retained` | Creates a retained Runtime Session                                                              |
| `readonly onDiagnostic?`   | `(diagnostic: RuntimeDiagnostic) => void` | —                            | Receives structured Runtime diagnostics in the queue order of the Vanilla processing controller |
| `readonly updateStrategy?` | `RuntimeUpdateStrategyValue`              | `RuntimeUpdateStrategy.Auto` | Program update strategy                                                                         |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutRetainedRuntimeOptions = Readonly<{
  mode?: typeof LayoutRuntimeMode.Retained;
  updateStrategy?: RuntimeUpdateStrategyValue;
  onDiagnostic?: (diagnostic: RuntimeDiagnostic) => void;
}>;
```

</DocTab>

</DocTabs>

### LayoutRuntimeModeValue

<p>
  <ApiSourceLink
    label={'LayoutRuntimeModeValue'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={11}
  >
    React Layout host execution mode value
  </ApiSourceLink>
</p>

```ts
export type LayoutRuntimeModeValue = ValueOf<typeof LayoutRuntimeMode>;
```

#### Expanded type

```ts
export type LayoutRuntimeModeValue = 'retained' | 'static';
```

### LayoutRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={40}
  >
    Discriminated Runtime configuration for React Layout
  </ApiSourceLink>
</p>

```ts
export type LayoutRuntimeOptions = LayoutRetainedRuntimeOptions | LayoutStaticRuntimeOptions;
```

#### Expanded type

```ts
export type LayoutRuntimeOptions =
  | {
      readonly mode?: typeof LayoutRuntimeMode.Retained | undefined;
      readonly onDiagnostic?: ((diagnostic: RuntimeDiagnostic) => void) | undefined;
      readonly updateStrategy?: RuntimeUpdateStrategyValue | undefined;
    }
  | {
      readonly mode: typeof LayoutRuntimeMode.Static;
      readonly updateStrategy?: never | undefined;
      readonly onDiagnostic?: never | undefined;
    };
```

### LayoutStaticRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutStaticRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={30}
  >
    Static Runtime configuration for React Layout
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member                     | Type                              | Default | Description                                                                                  |
| -------------------------- | --------------------------------- | ------- | -------------------------------------------------------------------------------------------- |
| `readonly mode`            | `typeof LayoutRuntimeMode.Static` | —       | Compiles and materializes the complete result directly, without creating a Runtime Session   |
| `readonly onDiagnostic?`   | `never`                           | —       | Static mode creates no Runtime session and therefore emits no structured Runtime diagnostics |
| `readonly updateStrategy?` | `never`                           | —       | Static mode does not support a Program update strategy                                       |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutStaticRuntimeOptions = Readonly<{
  mode: typeof LayoutRuntimeMode.Static;
  updateStrategy?: never;
  onDiagnostic?: never;
}>;
```

</DocTab>

</DocTabs>

### Layout

<p>
  <ApiSourceLink label={'Layout'} path={'packages/kernel/react/src/kernel/runtime/Layout.tsx'} startLine={347}>
    React Layout converts JSX to Vanilla Input, then hosts the Vanilla processing result
  </ApiSourceLink>
</p>

React does not create Core Programs, Runtime sessions, or retained renderers; Vanilla owns all processing state

```ts
export declare const Layout: FC<LayoutProps>;
```

### LayoutRuntimeMode

<p>
  <ApiSourceLink
    label={'LayoutRuntimeMode'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={5}
  >
    React Layout host execution modes
  </ApiSourceLink>
</p>

```ts
export const LayoutRuntimeMode = Object.freeze({
  Retained: 'retained',
  Static: 'static',
} as const);
```
