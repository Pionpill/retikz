{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/react`

### Layout / LayoutProps

- `Layout`: <ApiSourceLink label={"Layout"} path={"packages/kernel/react/src/kernel/runtime/Layout.tsx"} startLine={362}>Render JSX drawings or scene IR as SVG or Canvas, with updates and animation</ApiSourceLink>
- `LayoutProps`: <ApiSourceLink label={"LayoutProps"} path={"packages/kernel/react/src/kernel/runtime/Layout.tsx"} startLine={192}>Public props for React Layout</ApiSourceLink>

Declare drawings through children or pass a complete scene through ir; ir takes precedence when both are supplied. Subsequent updates use retained mode by default; runtime can select static mode for full compilation.

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[10%] [&_th:nth-child(2)]:w-[20%] [&_th:nth-child(3)]:w-[24%] [&_th:nth-child(4)]:w-[14%]">

| Group      | Member                 | Type                                                  | Default                       | Description                                                                                                                                                                              |
| ---------- | ---------------------- | ----------------------------------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Main       | `children?`            | `ReactNode`                                           | —                             | Drawing content declared with JSX                                                                                                                                                        |
|            | `ir?`                  | `IRScene`                                             | —                             | Persisted Source IR, used instead of children                                                                                                                                            |
| Layout     | `nodeDistance?`        | `number`                                              | `24`                          | Default relative node distance in drawing units; applies when position uses direction/of without distance                                                                                |
| Style      | `fontSize?`            | `number`                                              | `16`                          | Default font size in drawing units; used when font.size is omitted and as the root size for presets and rem, without overriding explicit numeric sizes                                   |
|            | `rootScope?`           | `Pick<InputScope, 'style' \| 'defaults'>`             | —                             | Implicit root Scope overrides for JSX children; complete IR takes precedence, while host CSS style applies independently                                                                 |
|            | `theme?`               | `IRScene['theme']`                                    | —                             | Theme written to the Scene root and inherited by descendant Composites                                                                                                                   |
| Rendering  | `className?`           | `string`                                              | —                             | Host className                                                                                                                                                                           |
|            | `height?`              | `number \| string`                                    | `computeDisplaySize`          | CSS height of SVG or Canvas; derived from content dimensions when omitted. A single numeric dimension determines the other from the content aspect ratio; CSS strings use browser layout |
|            | `idPrefix?`            | `string`                                              | `useId`                       | SVG resource ID prefix; generated from React useId when omitted                                                                                                                          |
|            | `renderer?`            | `'svg' \| 'canvas'`                                   | `'svg'`                       | Rendering backend; an explicit value takes precedence, otherwise inherits the Renderer context and falls back to SVG when no context is provided                                         |
|            | `style?`               | `CSSProperties`                                       | —                             | Host inline styles                                                                                                                                                                       |
|            | `viewBox?`             | `IRViewBox`                                           | —                             | Explicit viewport in drawing coordinates; overrides ir.viewBox. When omitted, uses the scene viewport or derives it from content if the scene has none                                   |
|            | `width?`               | `number \| string`                                    | `computeDisplaySize`          | CSS width of SVG or Canvas; derived from content dimensions when omitted. A single numeric dimension determines the other from the content aspect ratio; CSS strings use browser layout  |
| Callbacks  | `handlers?`            | `HydrationHandlers`                                   | —                             | Event handlers used when ir is supplied directly                                                                                                                                         |
| Animation  | `animate?`             | `boolean`                                             | `resolveAnimationEnabled`     | Whether to play animations; the animation-mode context takes precedence, otherwise an unspecified value follows the system reduced-motion preference                                     |
|            | `animationProperties?` | `AnimationPropertyRegistry`                           | —                             | Animatable property registry                                                                                                                                                             |
|            | `animationRef?`        | `Ref<AnimationControls \| null>`                      | —                             | Animation controller ref                                                                                                                                                                 |
|            | `animations?`          | `ReadonlyArray<IRAnimationTrack>`                     | —                             | Scene-root animations                                                                                                                                                                    |
|            | `easings?`             | `EasingRegistry`                                      | —                             | Animation easing registry                                                                                                                                                                |
|            | `snapshotAt?`          | `number`                                              | —                             | Animation sampling time in milliseconds; freezes the drawing at that time without playback and takes precedence over animate                                                             |
| Runtime    | `authoring?`           | `unknown`                                             | —                             | JSX input metadata for a custom compileDriver; ignored when ir is supplied and excluded from persisted Scene IR                                                                          |
|            | `runtime?`             | `LayoutRuntimeOptions`                                | `captureLayoutRuntimeOptions` | Select retained incremental updates or static full compilation; defaults to retained                                                                                                     |
| Output     | `artifacts?`           | `CompileArtifactOptions`                              | —                             | Requested compilation artifacts                                                                                                                                                          |
|            | `onArtifacts?`         | `(artifacts: ReadonlyArray<CompileArtifact>) => void` | —                             | Notification after compilation artifacts are committed successfully                                                                                                                      |
|            | `onCompileResult?`     | `(result: CompileResult) => void`                     | —                             | Notification of the complete Core compilation result                                                                                                                                     |
| Extensions | `compileDriver?`       | `VanillaCompileDriver`                                | —                             | Compile driver that processes authoring input                                                                                                                                            |
|            | `extensions?`          | `LayoutExtensions`                                    | —                             | Runtime extensions grouped by capability, using the Core compilation contract                                                                                                            |
|            | `lowerTex?`            | `LowerTex`                                            | —                             | Formula lowering capability                                                                                                                                                              |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const Layout: FC<LayoutProps>;

export type LayoutProps = {
  rootScope?: Pick<InputScope, 'style' | 'defaults'>;
  ir?: IRScene;
  theme?: IRScene['theme'];
  children?: ReactNode;
  authoring?: unknown;
  compileDriver?: VanillaCompileDriver;
  handlers?: HydrationHandlers;
  runtime?: LayoutRuntimeOptions;
  width?: number | string;
  height?: number | string;
  viewBox?: IRViewBox;
  className?: string;
  style?: CSSProperties;
  renderer?: 'svg' | 'canvas';
  animate?: boolean;
  snapshotAt?: number;
  animationRef?: Ref<AnimationControls | null>;
  animations?: ReadonlyArray<IRAnimationTrack>;
  easings?: EasingRegistry;
  animationProperties?: AnimationPropertyRegistry;
  idPrefix?: string;
  nodeDistance?: number;
  fontSize?: number;
  extensions?: LayoutExtensions;
  lowerTex?: LowerTex;
  artifacts?: CompileArtifactOptions;
  onArtifacts?: (artifacts: ReadonlyArray<CompileArtifact>) => void;
  onCompileResult?: (result: CompileResult) => void;
};
```

</DocTab>

</DocTabs>

### LayoutExtensions

<p>
  <ApiSourceLink
    label={'LayoutExtensions'}
    path={'packages/kernel/react/src/kernel/runtime/Layout.tsx'}
    startLine={143}
  >
    Register custom shapes, arrows, clips, and other drawing extensions for Layout
  </ApiSourceLink>
</p>

Pass these options through the extensions prop of Layout. Each field accepts an array of definitions; these runtime definitions are excluded from persisted scene data.

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member                     | Type                                     | Default                   | Description                      |
| -------------------------- | ---------------------------------------- | ------------------------- | -------------------------------- |
| `readonly arrows?`         | `ReadonlyArray<ArrowDefinition>`         | `BUILTIN_ARROWS`          | Arrow definitions                |
| `readonly boundaries?`     | `ReadonlyArray<BoundaryDefinition>`      | `BUILTIN_BOUNDARIES`      | Connection boundary definitions  |
| `readonly clips?`          | `ReadonlyArray<ClipDefinition>`          | `BUILTIN_CLIPS`           | Clip definitions                 |
| `readonly composites?`     | `ReadonlyArray<AnyCompositeDefinition>`  | `BUILTIN_COMPOSITES`      | Tier 2 composite expansion logic |
| `readonly pathGenerators?` | `ReadonlyArray<PathGeneratorDefinition>` | `BUILTIN_PATH_GENERATORS` | Path generator definitions       |
| `readonly pathKinds?`      | `ReadonlyArray<AnyPathKindDefinition>`   | `BUILTIN_PATH_KINDS`      | Path kind definitions            |
| `readonly patterns?`       | `ReadonlyArray<PatternDefinition>`       | `BUILTIN_PATTERNS`        | Pattern definitions              |
| `readonly shapes?`         | `ReadonlyArray<ShapeDefinition>`         | `BUILTIN_SHAPES`          | Shape definitions                |
| `readonly themeStyles?`    | `ReadonlyArray<ThemeStyleDefinition>`    | `ThemeStylesContext`      | Theme style definitions          |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutExtensions = Readonly<{
  shapes?: ReadonlyArray<ShapeDefinition>;
  boundaries?: ReadonlyArray<BoundaryDefinition>;
  clips?: ReadonlyArray<ClipDefinition>;
  arrows?: ReadonlyArray<ArrowDefinition>;
  patterns?: ReadonlyArray<PatternDefinition>;
  pathGenerators?: ReadonlyArray<PathGeneratorDefinition>;
  pathKinds?: ReadonlyArray<AnyPathKindDefinition>;
  composites?: ReadonlyArray<AnyCompositeDefinition>;
  themeStyles?: ReadonlyArray<ThemeStyleDefinition>;
}>;
```

</DocTab>

</DocTabs>

### LayoutRetainedRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutRetainedRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={14}
  >
    Keep execution state for subsequent drawing updates; used when runtime or mode is omitted
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member                     | Type                                      | Default                      | Description                                                                                                                              |
| -------------------------- | ----------------------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `readonly mode?`           | `typeof LayoutRuntimeMode.Retained`       | `LayoutRuntimeMode.Retained` | Creates a retained Runtime Session                                                                                                       |
| `readonly onDiagnostic?`   | `(diagnostic: RuntimeDiagnostic) => void` | —                            | Receive structured diagnostics from drawing updates in the order they occur; failed updates preserve the last successful drawing         |
| `readonly updateStrategy?` | `RuntimeUpdateStrategyValue`              | `RuntimeUpdateStrategy.Auto` | Update strategy; auto selects incremental or full execution based on changes and executor capabilities, while full forces full execution |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutRetainedRuntimeOptions = Readonly<{
  mode?: typeof LayoutRuntimeMode.Retained;
  updateStrategy?: RuntimeUpdateStrategyValue;
  onDiagnostic?: (diagnostic: RuntimeDiagnostic) => void;
}>;
```

</DocTab>

</DocTabs>

### LayoutRuntimeModeValue

<p>
  <ApiSourceLink
    label={'LayoutRuntimeModeValue'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={11}
  >
    React Layout host execution mode value
  </ApiSourceLink>
</p>

<DocTabs defaultValue="expanded">

<DocTab value="expanded" label="Expanded type">

```ts
export type LayoutRuntimeModeValue = 'retained' | 'static';
```

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutRuntimeModeValue = ValueOf<typeof LayoutRuntimeMode>;
```

</DocTab>

</DocTabs>

### LayoutRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={40}
  >
    Select how drawings update through mode; retained supports an update strategy and diagnostics callback, while static
    accepts neither option
  </ApiSourceLink>
</p>

<DocTabs defaultValue="retained">

<DocTab value="retained" label="Members · Retained">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member                     | Type                                      | Default                      | Description                                                                                                                              |
| -------------------------- | ----------------------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `readonly mode?`           | `typeof LayoutRuntimeMode.Retained`       | `LayoutRuntimeMode.Retained` | Creates a retained Runtime Session                                                                                                       |
| `readonly onDiagnostic?`   | `(diagnostic: RuntimeDiagnostic) => void` | —                            | Receive structured diagnostics from drawing updates in the order they occur; failed updates preserve the last successful drawing         |
| `readonly updateStrategy?` | `RuntimeUpdateStrategyValue`              | `RuntimeUpdateStrategy.Auto` | Update strategy; auto selects incremental or full execution based on changes and executor capabilities, while full forces full execution |

</div>

</DocTab>

<DocTab value="static" label="Members · Static">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member                     | Type                              | Default | Description                                                                                  |
| -------------------------- | --------------------------------- | ------- | -------------------------------------------------------------------------------------------- |
| `readonly mode`            | `typeof LayoutRuntimeMode.Static` | —       | Compiles and materializes the complete result directly, without creating a Runtime Session   |
| `readonly onDiagnostic?`   | `never`                           | —       | Static mode creates no Runtime session and therefore emits no structured Runtime diagnostics |
| `readonly updateStrategy?` | `never`                           | —       | Static mode does not support a Program update strategy                                       |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutRuntimeOptions = LayoutRetainedRuntimeOptions | LayoutStaticRuntimeOptions;
```

</DocTab>

</DocTabs>

### LayoutStaticRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutStaticRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={30}
  >
    Compile the complete drawing whenever input changes, without retaining incremental execution state
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member                     | Type                              | Default | Description                                                                                  |
| -------------------------- | --------------------------------- | ------- | -------------------------------------------------------------------------------------------- |
| `readonly mode`            | `typeof LayoutRuntimeMode.Static` | —       | Compiles and materializes the complete result directly, without creating a Runtime Session   |
| `readonly onDiagnostic?`   | `never`                           | —       | Static mode creates no Runtime session and therefore emits no structured Runtime diagnostics |
| `readonly updateStrategy?` | `never`                           | —       | Static mode does not support a Program update strategy                                       |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LayoutStaticRuntimeOptions = Readonly<{
  mode: typeof LayoutRuntimeMode.Static;
  updateStrategy?: never;
  onDiagnostic?: never;
}>;
```

</DocTab>

</DocTabs>

### LayoutRuntimeMode

<p>
  <ApiSourceLink
    label={'LayoutRuntimeMode'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={5}
  >
    React Layout host execution modes
  </ApiSourceLink>
</p>

```ts
export const LayoutRuntimeMode = Object.freeze({
  Retained: 'retained',
  Static: 'static',
} as const);
```
