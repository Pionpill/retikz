{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/react`

### Layout / LayoutProps

- `Layout`：<ApiSourceLink label={"Layout"} path={"packages/kernel/react/src/kernel/runtime/Layout.tsx"} startLine={362}>将 JSX 图形或场景 IR 渲染为 SVG 或 Canvas，并接入更新与动画</ApiSourceLink>
- `LayoutProps`：<ApiSourceLink label={"LayoutProps"} path={"packages/kernel/react/src/kernel/runtime/Layout.tsx"} startLine={192}>React Layout 的公开属性</ApiSourceLink>

通过 children 声明图形，或通过 ir 传入完整场景；同时提供时使用 ir。默认使用 retained 模式处理后续更新，可通过 runtime 切换为 static 完整编译模式

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[10%] [&_th:nth-child(2)]:w-[20%] [&_th:nth-child(3)]:w-[24%] [&_th:nth-child(4)]:w-[14%]">

| 分组   | 成员                   | 类型                                                  | 默认值                        | 说明                                                                                                     |
| ------ | ---------------------- | ----------------------------------------------------- | ----------------------------- | -------------------------------------------------------------------------------------------------------- |
| 主要   | `children?`            | `ReactNode`                                           | —                             | 通过 JSX 声明的图形内容                                                                                  |
|        | `ir?`                  | `IRScene`                                             | —                             | 直接传入持久化 Source IR，与 children 二选一                                                             |
| 布局   | `nodeDistance?`        | `number`                                              | `24`                          | 节点相对定位的默认距离，单位为绘图单位；position 使用 direction/of 且省略 distance 时生效                |
| 样式   | `fontSize?`            | `number`                                              | `16`                          | 默认字号，单位为绘图单位；font.size 缺省时使用，同时作为字号预设与 rem 的根字号，不覆盖显式数字字号      |
|        | `rootScope?`           | `Pick<InputScope, 'style' \| 'defaults'>`             | —                             | JSX 子图的隐式根 Scope 覆盖；完整 ir 优先，style 宿主 CSS 独立生效                                       |
|        | `theme?`               | `IRScene['theme']`                                    | —                             | 写入 Scene 根并由后代 Composite 继承的 Theme                                                             |
| 渲染   | `className?`           | `string`                                              | —                             | 宿主 className                                                                                           |
|        | `height?`              | `number \| string`                                    | `computeDisplaySize`          | SVG 或 Canvas CSS 高度；缺省按内容尺寸计算，单轴数值尺寸按内容比例补齐另一轴，CSS 字符串尺寸由浏览器排版 |
|        | `idPrefix?`            | `string`                                              | `useId`                       | SVG 资源 id 前缀；省略时由 React useId 生成                                                              |
|        | `renderer?`            | `'svg' \| 'canvas'`                                   | `'svg'`                       | 渲染后端；显式值优先，否则继承 Renderer 上下文，未提供上下文时使用 SVG                                   |
|        | `style?`               | `CSSProperties`                                       | —                             | 宿主内联样式                                                                                             |
|        | `viewBox?`             | `IRViewBox`                                           | —                             | 显式视框，使用绘图坐标；优先于 ir.viewBox，省略时沿用场景视框，场景未指定时按内容计算                    |
|        | `width?`               | `number \| string`                                    | `computeDisplaySize`          | SVG 或 Canvas CSS 宽度；缺省按内容尺寸计算，单轴数值尺寸按内容比例补齐另一轴，CSS 字符串尺寸由浏览器排版 |
| 回调   | `handlers?`            | `HydrationHandlers`                                   | —                             | 直接传入 ir 时使用的事件处理函数表                                                                       |
| 动画   | `animate?`             | `boolean`                                             | `resolveAnimationEnabled`     | 是否播放动画；由动画模式上下文优先决定，未指定时遵循系统减少动态效果偏好                                 |
|        | `animationProperties?` | `AnimationPropertyRegistry`                           | —                             | 可动画属性注册表                                                                                         |
|        | `animationRef?`        | `Ref<AnimationControls \| null>`                      | —                             | 动画控制器出口                                                                                           |
|        | `animations?`          | `ReadonlyArray<IRAnimationTrack>`                     | —                             | Scene 根动画                                                                                             |
|        | `easings?`             | `EasingRegistry`                                      | —                             | 动画缓动函数注册表                                                                                       |
|        | `snapshotAt?`          | `number`                                              | —                             | 动画采样时刻，单位为毫秒；指定后定格在该时刻，不播放动画，优先于 animate                                 |
| 运行时 | `authoring?`           | `unknown`                                             | —                             | 供自定义 compileDriver 消费的 JSX 输入元数据；传入 ir 时忽略，不写入持久化 Scene IR                      |
|        | `runtime?`             | `LayoutRuntimeOptions`                                | `captureLayoutRuntimeOptions` | 选择 retained 增量更新或 static 完整编译模式；省略时使用 retained                                        |
| 输出   | `artifacts?`           | `CompileArtifactOptions`                              | —                             | 请求生成的编译附加产物                                                                                   |
|        | `onArtifacts?`         | `(artifacts: ReadonlyArray<CompileArtifact>) => void` | —                             | 编译附加产物成功提交后的通知                                                                             |
|        | `onCompileResult?`     | `(result: CompileResult) => void`                     | —                             | Core 完整编译结果通知                                                                                    |
| 扩展   | `compileDriver?`       | `VanillaCompileDriver`                                | —                             | 处理作者输入的编译驱动                                                                                   |
|        | `extensions?`          | `LayoutExtensions`                                    | —                             | 按能力分类的运行时扩展注册，复用 Core 编译契约                                                           |
|        | `lowerTex?`            | `LowerTex`                                            | —                             | 公式下沉能力                                                                                             |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const Layout: FC<LayoutProps>;

export type LayoutProps = {
  rootScope?: Pick<InputScope, 'style' | 'defaults'>;
  ir?: IRScene;
  theme?: IRScene['theme'];
  children?: ReactNode;
  authoring?: unknown;
  compileDriver?: VanillaCompileDriver;
  handlers?: HydrationHandlers;
  runtime?: LayoutRuntimeOptions;
  width?: number | string;
  height?: number | string;
  viewBox?: IRViewBox;
  className?: string;
  style?: CSSProperties;
  renderer?: 'svg' | 'canvas';
  animate?: boolean;
  snapshotAt?: number;
  animationRef?: Ref<AnimationControls | null>;
  animations?: ReadonlyArray<IRAnimationTrack>;
  easings?: EasingRegistry;
  animationProperties?: AnimationPropertyRegistry;
  idPrefix?: string;
  nodeDistance?: number;
  fontSize?: number;
  extensions?: LayoutExtensions;
  lowerTex?: LowerTex;
  artifacts?: CompileArtifactOptions;
  onArtifacts?: (artifacts: ReadonlyArray<CompileArtifact>) => void;
  onCompileResult?: (result: CompileResult) => void;
};
```

</DocTab>

</DocTabs>

### LayoutExtensions

<p>
  <ApiSourceLink
    label={'LayoutExtensions'}
    path={'packages/kernel/react/src/kernel/runtime/Layout.tsx'}
    startLine={143}
  >
    为 Layout 注册自定义形状、箭头、裁剪及其他绘图扩展
  </ApiSourceLink>
</p>

通过 Layout 的 extensions 属性传入，各字段接收对应的定义数组；这些运行时定义不写入可持久化的场景数据

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员                       | 类型                                     | 默认值                    | 说明                      |
| -------------------------- | ---------------------------------------- | ------------------------- | ------------------------- |
| `readonly arrows?`         | `ReadonlyArray<ArrowDefinition>`         | `BUILTIN_ARROWS`          | 箭头定义                  |
| `readonly boundaries?`     | `ReadonlyArray<BoundaryDefinition>`      | `BUILTIN_BOUNDARIES`      | 连接表面定义              |
| `readonly clips?`          | `ReadonlyArray<ClipDefinition>`          | `BUILTIN_CLIPS`           | 裁剪定义                  |
| `readonly composites?`     | `ReadonlyArray<AnyCompositeDefinition>`  | `BUILTIN_COMPOSITES`      | Tier 2 composite 展开逻辑 |
| `readonly pathGenerators?` | `ReadonlyArray<PathGeneratorDefinition>` | `BUILTIN_PATH_GENERATORS` | 路径生成器定义            |
| `readonly pathKinds?`      | `ReadonlyArray<AnyPathKindDefinition>`   | `BUILTIN_PATH_KINDS`      | 路径种类定义              |
| `readonly patterns?`       | `ReadonlyArray<PatternDefinition>`       | `BUILTIN_PATTERNS`        | 图案定义                  |
| `readonly shapes?`         | `ReadonlyArray<ShapeDefinition>`         | `BUILTIN_SHAPES`          | 形状定义                  |
| `readonly themeStyles?`    | `ReadonlyArray<ThemeStyleDefinition>`    | `ThemeStylesContext`      | 主题样式定义              |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LayoutExtensions = Readonly<{
  shapes?: ReadonlyArray<ShapeDefinition>;
  boundaries?: ReadonlyArray<BoundaryDefinition>;
  clips?: ReadonlyArray<ClipDefinition>;
  arrows?: ReadonlyArray<ArrowDefinition>;
  patterns?: ReadonlyArray<PatternDefinition>;
  pathGenerators?: ReadonlyArray<PathGeneratorDefinition>;
  pathKinds?: ReadonlyArray<AnyPathKindDefinition>;
  composites?: ReadonlyArray<AnyCompositeDefinition>;
  themeStyles?: ReadonlyArray<ThemeStyleDefinition>;
}>;
```

</DocTab>

</DocTabs>

### LayoutRetainedRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutRetainedRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={14}
  >
    保留执行状态以处理后续图形更新；省略 runtime 或 mode 时使用此模式
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员                       | 类型                                      | 默认值                       | 说明                                                                     |
| -------------------------- | ----------------------------------------- | ---------------------------- | ------------------------------------------------------------------------ |
| `readonly mode?`           | `typeof LayoutRuntimeMode.Retained`       | `LayoutRuntimeMode.Retained` | 创建保留式 Runtime Session                                               |
| `readonly onDiagnostic?`   | `(diagnostic: RuntimeDiagnostic) => void` | —                            | 接收图形更新的结构化诊断，按产生顺序通知；更新失败时保留最后一次成功画面 |
| `readonly updateStrategy?` | `RuntimeUpdateStrategyValue`              | `RuntimeUpdateStrategy.Auto` | 更新策略；auto 按变更和执行器能力选择增量或完整执行，full 强制完整执行   |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LayoutRetainedRuntimeOptions = Readonly<{
  mode?: typeof LayoutRuntimeMode.Retained;
  updateStrategy?: RuntimeUpdateStrategyValue;
  onDiagnostic?: (diagnostic: RuntimeDiagnostic) => void;
}>;
```

</DocTab>

</DocTabs>

### LayoutRuntimeModeValue

<p>
  <ApiSourceLink
    label={'LayoutRuntimeModeValue'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={11}
  >
    React Layout 宿主执行模式取值
  </ApiSourceLink>
</p>

<DocTabs defaultValue="expanded">

<DocTab value="expanded" label="展开类型">

```ts
export type LayoutRuntimeModeValue = 'retained' | 'static';
```

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LayoutRuntimeModeValue = ValueOf<typeof LayoutRuntimeMode>;
```

</DocTab>

</DocTabs>

### LayoutRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={40}
  >
    通过 mode 选择图形更新方式；retained 支持更新策略和诊断回调，static 不接受这两个选项
  </ApiSourceLink>
</p>

<DocTabs defaultValue="retained">

<DocTab value="retained" label="属性 · 保留模式">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员                       | 类型                                      | 默认值                       | 说明                                                                     |
| -------------------------- | ----------------------------------------- | ---------------------------- | ------------------------------------------------------------------------ |
| `readonly mode?`           | `typeof LayoutRuntimeMode.Retained`       | `LayoutRuntimeMode.Retained` | 创建保留式 Runtime Session                                               |
| `readonly onDiagnostic?`   | `(diagnostic: RuntimeDiagnostic) => void` | —                            | 接收图形更新的结构化诊断，按产生顺序通知；更新失败时保留最后一次成功画面 |
| `readonly updateStrategy?` | `RuntimeUpdateStrategyValue`              | `RuntimeUpdateStrategy.Auto` | 更新策略；auto 按变更和执行器能力选择增量或完整执行，full 强制完整执行   |

</div>

</DocTab>

<DocTab value="static" label="属性 · 静态模式">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员                       | 类型                              | 默认值 | 说明                                                         |
| -------------------------- | --------------------------------- | ------ | ------------------------------------------------------------ |
| `readonly mode`            | `typeof LayoutRuntimeMode.Static` | —      | 不创建 Runtime Session，直接完整编译与物化                   |
| `readonly onDiagnostic?`   | `never`                           | —      | static 不创建 Runtime session，因此不产生 Runtime 结构化诊断 |
| `readonly updateStrategy?` | `never`                           | —      | static 不支持 Program 更新策略                               |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LayoutRuntimeOptions = LayoutRetainedRuntimeOptions | LayoutStaticRuntimeOptions;
```

</DocTab>

</DocTabs>

### LayoutStaticRuntimeOptions

<p>
  <ApiSourceLink
    label={'LayoutStaticRuntimeOptions'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={30}
  >
    每次输入变化时完整编译图形，不保留增量执行状态
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员                       | 类型                              | 默认值 | 说明                                                         |
| -------------------------- | --------------------------------- | ------ | ------------------------------------------------------------ |
| `readonly mode`            | `typeof LayoutRuntimeMode.Static` | —      | 不创建 Runtime Session，直接完整编译与物化                   |
| `readonly onDiagnostic?`   | `never`                           | —      | static 不创建 Runtime session，因此不产生 Runtime 结构化诊断 |
| `readonly updateStrategy?` | `never`                           | —      | static 不支持 Program 更新策略                               |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LayoutStaticRuntimeOptions = Readonly<{
  mode: typeof LayoutRuntimeMode.Static;
  updateStrategy?: never;
  onDiagnostic?: never;
}>;
```

</DocTab>

</DocTabs>

### LayoutRuntimeMode

<p>
  <ApiSourceLink
    label={'LayoutRuntimeMode'}
    path={'packages/kernel/react/src/kernel/runtime/runtime-options.ts'}
    startLine={5}
  >
    React Layout 的宿主执行模式
  </ApiSourceLink>
</p>

```ts
export const LayoutRuntimeMode = Object.freeze({
  Retained: 'retained',
  Static: 'static',
} as const);
```
