{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/react`

### Path / PathProps

- `Path`：<ApiSourceLink label={"Path"} path={"packages/kernel/react/src/kernel/components/Path.tsx"} startLine={82}>Path 用一组 `<Step>` 声明路径</ApiSourceLink>
- `PathProps`：<ApiSourceLink label={"PathProps"} path={"packages/kernel/react/src/kernel/components/Path.tsx"} startLine={9}>用步骤或路径简写声明路径，并设置样式、箭头与交互</ApiSourceLink>

本组件自身不渲染 DOM；最终路径由 `<Layout>` 根据 step 序列、样式、箭头和标记输出

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable grouped>

| 分组   | 成员              | 类型                          | 默认值     | 说明                                                                                                                                                                                            |
| ------ | ----------------- | ----------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 主要   | `children?`       | `ReactNode`                   | —          | 应当全部是 `<Step />`                                                                                                                                                                           |
|        | `id?`             | `IRPath['id']`                | —          | 路径 id；其他 path / position 通过这个 id 引用本路径，也作为水合挂点供事件 handler 绑定                                                                                                         |
|        | `kind?`           | `IRPath['kind']`              | `'stroke'` | 路径种类，省略时使用内置 stroke；自定义种类通过 Layout.extensions.pathKinds 注册                                                                                                                |
|        | `kindOptions?`    | `IRPath['kindOptions']`       | —          | 所选路径种类的 JSON 参数；内置 stroke 不接受此字段                                                                                                                                              |
|        | `label?`          | `IRPath['label']`             | —          | 附着于整条路径的一个或多个几何标签                                                                                                                                                              |
|        | `way?`            | `WayDSL`                      | —          | TikZ 风格路径走向简写，由 Vanilla 统一解析为步骤                                                                                                                                                |
| 几何   | `arrow?`          | `InputPath['arrow']`          | `'none'`   | 路径级箭头方向<br />`'->'` 终点 / `'<-'` 起点 / `'<->'` 两端；省略或 `'none'` 无箭头                                                                                                            |
|        | `arrowDetail?`    | `InputPath['arrowDetail']`    | —          | 箭头详细配置<br />顶层默认 + 可选 `start` / `end` 子对象逐字段 merge override。空心 shape<br />（open / openStealth / openDiamond / openCircle）上 `fill` silent no-op                          |
|        | `arrowPlacement?` | `InputPath['arrowPlacement']` | —          | 箭头端点放置配置<br />`overlap` 为实际端点共享值，`start` / `end` 可逐端覆盖；比例从默认位置插值到最终视觉后缘与逻辑端点对齐的位置                                                              |
|        | `marks?`          | `IRPath['marks']`             | —          | 沿路径在归一化位置放标记（首批仅箭头）<br />每个 `{ pos, mark }`：`pos∈[0,1]`，`mark.kind:'arrow'` + 视觉子集（shape 为已注册箭头名，方向随路径切线）                                           |
|        | `rotate?`         | `IRPath['rotate']`            | —          | 整条 path 旋转（度，绕包围盒中心，正向 = 屏幕 y-down 视觉顺时针）<br />等价把 path 包一层绕其包围盒中心旋转的 Scope；端点先在当前 scope resolve 再整体旋转                                      |
|        | `roundedCorners?` | `IRPath['roundedCorners']`    | —          | 折线拐角几何圆角半径（TikZ `rounded corners=`）<br />对每个 line↔line 接缝插切圆弧、改路径几何（区别于 lineJoin 仅描边）；curve / arc / bezier / fold 接缝保持尖；按相邻段长 clamp；省略 = 尖角 |
|        | `scale?`          | `IRPath['scale']`             | —          | 整条 path 缩放（绕包围盒中心）：number 等比，或 `{ x, y }` 非等比                                                                                                                               |
| 样式   | `style?`          | `InputPath['style']`          | —          | 实例视觉覆盖，逐字段覆盖继承默认值                                                                                                                                                              |
|        | `thickness?`      | `PathThicknessValue`          | —          | 语义 stroke 档位糖（TikZ `ultra thin` … `ultra thick`）；构造 IR 时解析为 `strokeWidth`，显式 `strokeWidth` 始终优先                                                                            |
|        | `zIndex?`         | `IRPath['zIndex']`            | `0`        | 显式栈序：大者在上；缺省 0 = 声明顺序；同值稳定保序；只在同层子节点间生效                                                                                                                       |
| 回调   | `onClick?`        | `HydrationHandler`            | —          | 单击该图元（DOM `click`）                                                                                                                                                                       |
|        | `onDoubleClick?`  | `HydrationHandler`            | —          | 双击该图元（DOM `dblclick`）                                                                                                                                                                    |
|        | `onPointerDown?`  | `HydrationHandler`            | —          | 指针在该图元上按下（DOM `pointerdown`）                                                                                                                                                         |
|        | `onPointerEnter?` | `HydrationHandler`            | —          | 指针进入该图元（由 `pointermove` + 命中 id 状态机合成，跨子元素不重复触发）                                                                                                                     |
|        | `onPointerLeave?` | `HydrationHandler`            | —          | 指针离开该图元（由 `pointermove` + 命中 id 状态机合成）                                                                                                                                         |
|        | `onPointerMove?`  | `HydrationHandler`            | —          | 指针在该图元上移动（DOM `pointermove`）                                                                                                                                                         |
|        | `onPointerUp?`    | `HydrationHandler`            | —          | 指针在该图元上抬起（DOM `pointerup`）                                                                                                                                                           |
|        | `onRightClick?`   | `HydrationHandler`            | —          | 右键该图元（DOM `contextmenu`）；默认不抑制浏览器菜单，handler 自行 `event.preventDefault()`                                                                                                    |
|        | `onWheel?`        | `HydrationHandler`            | —          | 在该图元上滚轮（DOM `wheel`）                                                                                                                                                                   |
| 动画   | `animations?`     | `IRPath['animations']`        | —          | 路径级时间轴动画；渲染端播放或降级为静态，不参与布局                                                                                                                                            |
| 运行时 | `authoring?`      | `unknown`                     | —          | 可选 compile driver 自行解释的 runtime-only authoring 载荷，不进入 Core IR                                                                                                                      |
|        | `meta?`           | `IRPath['meta']`              | —          | 用户自定义元数据；可在事件 / 水合上下文中读取，不参与布局。须为 JSON 可序列化对象                                                                                                               |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const Path: FC<PathProps>;

export type PathProps = HydrationEventProps & {
  style?: InputPath['style'];
  authoring?: unknown;
  kind?: IRPath['kind'];
  kindOptions?: IRPath['kindOptions'];
  label?: IRPath['label'];
  id?: IRPath['id'];
  meta?: IRPath['meta'];
  animations?: IRPath['animations'];
  roundedCorners?: IRPath['roundedCorners'];
  thickness?: PathThicknessValue;
  arrow?: InputPath['arrow'];
  arrowDetail?: InputPath['arrowDetail'];
  arrowPlacement?: InputPath['arrowPlacement'];
  zIndex?: IRPath['zIndex'];
  rotate?: IRPath['rotate'];
  scale?: IRPath['scale'];
  marks?: IRPath['marks'];
  way?: WayDSL;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### Draw / DrawProps

- `Draw`：<ApiSourceLink label={"Draw"} path={"packages/kernel/react/src/sugar/path/Draw.tsx"} startLine={32}>Sugar 组件，将路径 grammar 原样调度给 Vanilla</ApiSourceLink>
- `DrawProps`：<ApiSourceLink label={"DrawProps"} path={"packages/kernel/react/src/sugar/path/Draw.tsx"} startLine={9}>Draw 的作者侧属性</ApiSourceLink>

React 只负责 JSX sugar；`normalizePath` 是 `way` 的唯一 parser 调度位置

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员                       | 类型                          | 默认值 | 说明                               |
| -------------------------- | ----------------------------- | ------ | ---------------------------------- |
| `readonly arrow?`          | `PathProps['arrow']`          | —      | 路径级箭头方向                     |
| `readonly arrowDetail?`    | `PathProps['arrowDetail']`    | —      | 箭头详细配置                       |
| `readonly arrowPlacement?` | `PathProps['arrowPlacement']` | —      | 箭头端点放置配置                   |
| `readonly roundedCorners?` | `IRPath['roundedCorners']`    | —      | 折线拐角几何圆角半径               |
| `readonly style?`          | `InputPath['style']`          | —      | 实例视觉覆盖，逐字段覆盖继承默认值 |
| `readonly thickness?`      | `PathThicknessValue`          | —      | 语义 stroke 档位糖                 |
| `readonly way`             | `WayDSL`                      | —      | TikZ 风格的路径走向简写            |
| `readonly zIndex?`         | `IRPath['zIndex']`            | —      | 同层 stack 顺序                    |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const Draw: FC<DrawProps>;

export type DrawProps = Readonly<{
  style?: InputPath['style'];
  way: WayDSL;
  roundedCorners?: IRPath['roundedCorners'];
  thickness?: PathThicknessValue;
  arrow?: PathProps['arrow'];
  arrowDetail?: PathProps['arrowDetail'];
  arrowPlacement?: PathProps['arrowPlacement'];
  zIndex?: IRPath['zIndex'];
}>;
```

</DocTab>

</DocTabs>

### Step / StepProps

- `Step`：<ApiSourceLink label={"Step"} path={"packages/kernel/react/src/kernel/components/Step.tsx"} startLine={282}>Step 是 DSL 标记组件——本身不渲染</ApiSourceLink>
- `StepProps`：<ApiSourceLink label={"StepProps"} path={"packages/kernel/react/src/kernel/components/Step.tsx"} startLine={262}>查看源码</ApiSourceLink>

声明路径中的一个动作，例如移动、连线、曲线、圆弧、闭合或生成器片段

十四种 kind：'move' / 'line'（默认） / 'axis-line'（单轴投影连接） / 'fold'（折角） / 'cycle'（闭合） / 'curve'（二次贝塞尔） / 'cubic'（三次贝塞尔） / 'bend'（弧形简记） / 'arc'（圆 / 椭圆弧段） / 'circlePath'（整圆 / 部分圆） / 'ellipsePath'（整椭圆 / 部分椭圆） / 'rectangle'（矩形） / 'smooth'（过点平滑曲线） / 'generator'（内置或注册路径生成器）。除 'move' / 'cycle' / 'rectangle' 外均可挂 `label?: IRStepLabel`，等价于 sugar `<EdgeLabel>` child（prop 优先）；'smooth' 用 `points` 而非 `to`，'generator' 用 `name` + JSON-safe `params`。每个 kind 有对应 named type export，便于 wrapper / forwardRef / `Pick<>` 派生

```ts
export declare const Step: FC<StepProps>;

export type StepProps =
  | MoveStepProps
  | LineStepProps
  | AxisLineStepProps
  | FoldStepProps
  | CycleStepProps
  | CurveStepProps
  | CubicStepProps
  | BendStepProps
  | ArcStepProps
  | CirclePathStepProps
  | EllipsePathStepProps
  | RectangleStepProps
  | SmoothStepProps
  | GeneratorStepProps;
```

### ArcStepProps

<p>
  <ApiSourceLink label={'ArcStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={157}>
    Arc action：按起末角度绘制圆弧 / 椭圆弧段；圆心缺省取游标，可显式指定（TikZ `arc[start angle=…, end angle=…,
    radius=…]`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员         | 类型             | 默认值 | 说明                                                                                |
| ------------ | ---------------- | ------ | ----------------------------------------------------------------------------------- |
| `center?`    | `DslTarget`      | —      | 显式圆心；缺省取游标（上一 step anchor）                                            |
| `children?`  | `ReactNode`      | —      | sugar 形态                                                                          |
| `endAngle`   | `number`         | —      | 终止角度（度），sweep 方向由 startAngle vs endAngle 决定                            |
| `kind`       | `'arc'`          | —      | 弧段 step 鉴别字面量                                                                |
| `label?`     | `InputStepLabel` | —      | 边标注                                                                              |
| `radius`     | `IRStepRadius`   | —      | 弧半径；number 表示正圆，&#123; x, y &#125; 表示椭圆                                |
| `startAngle` | `number`         | —      | 起始角度（度，0° = +x、90° = +y screen-down；与 ArcStep / Node label 角度约定一致） |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ArcStepProps = {
  kind: 'arc';
  startAngle: number;
  endAngle: number;
  radius: IRStepRadius;
  center?: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### AxisLineStepProps

<p>
  <ApiSourceLink
    label={'AxisLineStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={42}
  >
    Axis-line action：把目标投影到当前 host 的一个局部轴并画单段直线
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员        | 类型                         | 默认值 | 说明                                                          |
| ----------- | ---------------------------- | ------ | ------------------------------------------------------------- |
| `axis`      | `'horizontal' \| 'vertical'` | —      | 保持的当前 host 局部轴                                        |
| `children?` | `ReactNode`                  | —      | sugar 形态                                                    |
| `kind`      | `'axis-line'`                | —      | 单轴连接 step 鉴别字面量                                      |
| `label?`    | `InputStepLabel`             | —      | 边标注                                                        |
| `to`        | `IRAxisLineTarget \| string` | —      | 投影目标，仅支持笛卡尔坐标、NodeTarget 或对应字符串 shorthand |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type AxisLineStepProps = {
  kind: 'axis-line';
  axis: 'horizontal' | 'vertical';
  to: IRAxisLineTarget | string;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### BendStepProps

<p>
  <ApiSourceLink label={'BendStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={129}>
    Bend action：弧形简记，自动算控制点生成 cubic（TikZ `to[bend left=N]` / `to[out=…, in=…]`）
  </ApiSourceLink>
</p>

支持对称弯与非对称弯 / 自环两种模式；同给时 out/in 优先（编译层）。
三者全省时默认 left 对称弯。`from == to`（同节点 / 同坐标）配合 out/in 画自环

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员             | 类型                | 默认值   | 说明                                                                                                    |
| ---------------- | ------------------- | -------- | ------------------------------------------------------------------------------------------------------- |
| `bendAngle?`     | `number`            | `30`     | 对称弯模式的弯角度（度），缺省 30                                                                       |
| `bendDirection?` | `'left' \| 'right'` | `'left'` | 对称弯模式的弯向：'left' / 'right'（视觉左右，相对 from→to）；可选，与 out/in 互补                      |
| `children?`      | `ReactNode`         | —        | sugar 形态                                                                                              |
| `inAngle?`       | `number`            | —        | 非对称弯 / 自环模式的入射角（度，TikZ `in=`）；与 outAngle 一起编译成 cubic                             |
| `kind`           | `'bend'`            | —        | 弧形简记 step 鉴别字面量                                                                                |
| `label?`         | `InputStepLabel`    | —        | 边标注                                                                                                  |
| `looseness?`     | `number`            | —        | 非对称弯 / 自环模式的曲线松紧系数（TikZ `looseness=`，控制控制点距离），缺省约 1；也缩放自环默认大小    |
| `outAngle?`      | `number`            | —        | 非对称弯 / 自环模式的出射角（度，TikZ `out=`）；与 inAngle 一起编译成 cubic，给定时优先于 bendDirection |
| `to`             | `DslTarget`         | —        | 终点                                                                                                    |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BendStepProps = {
  kind: 'bend';
  bendDirection?: 'left' | 'right';
  bendAngle?: number;
  outAngle?: number;
  inAngle?: number;
  looseness?: number;
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CirclePathStepProps

<p>
  <ApiSourceLink
    label={'CirclePathStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={175}
  >
    CirclePath action：以游标为圆心绘制圆；无角度=整圆（画完回圆心），带角度=部分圆（TikZ `circle[radius=…]`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员          | 类型                                        | 默认值 | 说明                                                                                        |
| ------------- | ------------------------------------------- | ------ | ------------------------------------------------------------------------------------------- |
| `children?`   | `ReactNode`                                 | —      | sugar 形态                                                                                  |
| `closed?`     | `'closed' \| 'chord' \| 'open' \| 'sector'` | —      | 闭合模式：无角度=closed（整圆）；带角度=chord（弦，默认）/ sector（连回中心）/ open（纯弧） |
| `endAngle?`   | `number`                                    | —      | 部分圆终止角（度）                                                                          |
| `kind`        | `'circlePath'`                              | —      | 整圆 step 鉴别字面量                                                                        |
| `label?`      | `InputStepLabel`                            | —      | 边标注                                                                                      |
| `radius`      | `number`                                    | —      | 圆半径（user units）                                                                        |
| `startAngle?` | `number`                                    | —      | 部分圆起始角（度）；与 endAngle 同给才生效                                                  |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CirclePathStepProps = {
  kind: 'circlePath';
  radius: number;
  startAngle?: number;
  endAngle?: number;
  closed?: 'closed' | 'chord' | 'open' | 'sector';
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CubicStepProps

<p>
  <ApiSourceLink label={'CubicStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={109}>
    Cubic action：三次贝塞尔，两个控制点（TikZ `.. controls (B) and (C) ..`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员        | 类型             | 默认值 | 说明                       |
| ----------- | ---------------- | ------ | -------------------------- |
| `children?` | `ReactNode`      | —      | sugar 形态                 |
| `control1`  | `IRControlPoint` | —      | 第一控制点（影响起点切线） |
| `control2`  | `IRControlPoint` | —      | 第二控制点（影响终点切线） |
| `kind`      | `'cubic'`        | —      | 三次贝塞尔 step 鉴别字面量 |
| `label?`    | `InputStepLabel` | —      | 边标注                     |
| `to`        | `DslTarget`      | —      | 曲线终点                   |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CubicStepProps = {
  kind: 'cubic';
  control1: IRControlPoint;
  control2: IRControlPoint;
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CurveStepProps

<p>
  <ApiSourceLink label={'CurveStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={95}>
    Curve action：二次贝塞尔，一个控制点（TikZ `.. controls (B) ..`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员        | 类型             | 默认值 | 说明                            |
| ----------- | ---------------- | ------ | ------------------------------- |
| `children?` | `ReactNode`      | —      | sugar 形态                      |
| `control`   | `IRControlPoint` | —      | 控制点（仅支持笛卡尔 `[x, y]`） |
| `kind`      | `'curve'`        | —      | 二次贝塞尔 step 鉴别字面量      |
| `label?`    | `InputStepLabel` | —      | 边标注                          |
| `to`        | `DslTarget`      | —      | 曲线终点                        |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CurveStepProps = {
  kind: 'curve';
  control: IRControlPoint;
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CycleStepProps

<p>
  <ApiSourceLink label={'CycleStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={89}>
    Cycle action：把当前子路径闭合回最近一次 move 起点（TikZ `cycle`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员   | 类型      | 默认值 | 说明                                                                             |
| ------ | --------- | ------ | -------------------------------------------------------------------------------- |
| `kind` | `'cycle'` | —      | 闭合 step 鉴别字面量；无 `to` / `label` / `children`——不可挂边标注、不消耗目标点 |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CycleStepProps = {
  kind: 'cycle';
};
```

</DocTab>

</DocTabs>

### EllipsePathStepProps

<p>
  <ApiSourceLink
    label={'EllipsePathStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={193}
  >
    EllipsePath action：以游标为圆心绘制椭圆；无角度=整椭圆（画完回圆心），带角度=部分椭圆（TikZ `ellipse[x radius=…, y
    radius=…]`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员          | 类型                                        | 默认值 | 说明                                                                              |
| ------------- | ------------------------------------------- | ------ | --------------------------------------------------------------------------------- |
| `children?`   | `ReactNode`                                 | —      | sugar 形态                                                                        |
| `closed?`     | `'closed' \| 'chord' \| 'open' \| 'sector'` | —      | 闭合模式：无角度=closed（整椭圆）；带角度=chord（默认）/ sector（连回中心）/ open |
| `endAngle?`   | `number`                                    | —      | 部分椭圆终止角（度）                                                              |
| `kind`        | `'ellipsePath'`                             | —      | 整椭圆 step 鉴别字面量                                                            |
| `label?`      | `InputStepLabel`                            | —      | 边标注                                                                            |
| `radius`      | `IRStepAnisotropicRadius`                   | —      | 椭圆的 x、y 轴半径                                                                |
| `startAngle?` | `number`                                    | —      | 部分椭圆起始角（度）；与 endAngle 同给才生效                                      |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type EllipsePathStepProps = {
  kind: 'ellipsePath';
  radius: IRStepAnisotropicRadius;
  startAngle?: number;
  endAngle?: number;
  closed?: 'closed' | 'chord' | 'open' | 'sector';
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### FoldStepProps

<p>
  <ApiSourceLink label={'FoldStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={71}>
    Fold action：两段或三段正交折线
  </ApiSourceLink>
</p>

`-|` / `|-` 使用一个转折点；`-|-` / `|-|` 使用两个转折点，并可用归一化 `fraction` 调整中间腿位置

<DocTabs defaultValue="two-segments">

<DocTab value="two-segments" label="属性 · 两段折线">

<ApiTable>

| 成员        | 类型             | 默认值 | 说明                 |
| ----------- | ---------------- | ------ | -------------------- |
| `children?` | `ReactNode`      | —      | sugar 形态           |
| `kind`      | `'fold'`         | —      | 折角 step 鉴别字面量 |
| `label?`    | `InputStepLabel` | —      | 边标注               |
| `to`        | `DslTarget`      | —      | 折角终点             |
| `via`       | `'-\|' \| '\|-'` | —      | 两段折角走向         |

</ApiTable>

</DocTab>

<DocTab value="three-segments" label="属性 · 三段折线">

<ApiTable>

| 成员        | 类型                | 默认值 | 说明                 |
| ----------- | ------------------- | ------ | -------------------- |
| `children?` | `ReactNode`         | —      | sugar 形态           |
| `fraction?` | `number`            | `0.5`  | 中间腿的归一化位置   |
| `kind`      | `'fold'`            | —      | 折角 step 鉴别字面量 |
| `label?`    | `InputStepLabel`    | —      | 边标注               |
| `to`        | `DslTarget`         | —      | 折角终点             |
| `via`       | `'-\|-' \| '\|-\|'` | —      | 三段折角走向         |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type FoldStepProps = FoldStepBaseProps &
  (
    | {
        via: '-|' | '|-';
      }
    | {
        via: '-|-' | '|-|';
        fraction?: number;
      }
  );
```

</DocTab>

</DocTabs>

### GeneratorStepProps

<p>
  <ApiSourceLink
    label={'GeneratorStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={244}
  >
    Generator action：调用内置或运行时注册的 path generator 生成一段低层路径命令
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员        | 类型                      | 默认值 | 说明                                                                            |
| ----------- | ------------------------- | ------ | ------------------------------------------------------------------------------- |
| `children?` | `ReactNode`               | —      | sugar 形态                                                                      |
| `kind`      | `'generator'`             | —      | 生成器 step 鉴别字面量                                                          |
| `label?`    | `InputStepLabel`          | —      | 边标注                                                                          |
| `name`      | `IRGeneratorStep['name']` | —      | path generator 名称；内置名或 `<Layout extensions={{ pathGenerators }}>` 注册名 |
| `params`    | `JsonObject`              | —      | JSON-safe 参数对象；目标引用需写在 generator 的 `targetParams` 顶层 key 上      |
| `to?`       | `DslTarget`               | —      | 可选终点，会作为 generator context 的 `to` 传入                                 |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type GeneratorStepProps = {
  kind: 'generator';
  name: IRGeneratorStep['name'];
  to?: DslTarget;
  params: JsonObject;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### LineStepProps

<p>
  <ApiSourceLink label={'LineStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={30}>
    Line action：从当前游标到目标点画直线（TikZ `(A) -- (B)`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员        | 类型             | 默认值 | 说明                                                                          |
| ----------- | ---------------- | ------ | ----------------------------------------------------------------------------- |
| `children?` | `ReactNode`      | —      | sugar 形态：`<Step><EdgeLabel>...</EdgeLabel></Step>`；其它 children 静默忽略 |
| `kind?`     | `'line'`         | —      | 直线 step 鉴别字面量；省略时默认 'line'                                       |
| `label?`    | `InputStepLabel` | —      | 边标注，等价于 sugar `<EdgeLabel>` child                                      |
| `to`        | `DslTarget`      | —      | 直线终点                                                                      |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LineStepProps = {
  kind?: 'line';
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### MoveStepProps

<p>
  <ApiSourceLink label={'MoveStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={22}>
    Move action：移动游标但不绘制（TikZ `(A)`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员   | 类型        | 默认值 | 说明                 |
| ------ | ----------- | ------ | -------------------- |
| `kind` | `'move'`    | —      | 移动 step 鉴别字面量 |
| `to`   | `DslTarget` | —      | 移动目标点           |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MoveStepProps = {
  kind: 'move';
  to: DslTarget;
};
```

</DocTab>

</DocTabs>

### RectangleStepProps

<p>
  <ApiSourceLink
    label={'RectangleStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={211}
  >
    Rectangle action：两对角定义的轴对齐矩形（可圆角）；编译为 path 命令（TikZ `(a) rectangle (b)`）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员            | 类型          | 默认值 | 说明                                               |
| --------------- | ------------- | ------ | -------------------------------------------------- |
| `cornerRadius?` | `number`      | —      | 四角同圆角半径；缺省直角，compile clamp 到边长一半 |
| `from`          | `DslTarget`   | —      | 一角                                               |
| `kind`          | `'rectangle'` | —      | 矩形 step 鉴别字面量                               |
| `to`            | `DslTarget`   | —      | 对角（顺序无关）                                   |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type RectangleStepProps = {
  kind: 'rectangle';
  from: DslTarget;
  to: DslTarget;
  cornerRadius?: number;
};
```

</DocTab>

</DocTabs>

### SmoothStepProps

<p>
  <ApiSourceLink
    label={'SmoothStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={227}
  >
    Smooth action：过当前游标 + `points` 的平滑曲线（TikZ `plot[smooth]` / Hobby 风格）
  </ApiSourceLink>
</p>

游标为隐式首 knot，曲线依次穿过 `points` 每个点；编译期经 centripetal Catmull-Rom 转成 cubic 链。
需前置 step 设游标；游标终于 `points` 末项。`tension` 缺省 1（标准 centripetal CR），&lt;1 更紧、>1 更鼓

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员        | 类型               | 默认值 | 说明                                                            |
| ----------- | ------------------ | ------ | --------------------------------------------------------------- |
| `children?` | `ReactNode`        | —      | sugar 形态                                                      |
| `kind`      | `'smooth'`         | —      | 平滑曲线 step 鉴别字面量                                        |
| `label?`    | `InputStepLabel`   | —      | 边标注，沿生成 cubic 按贝塞尔参数定位                           |
| `points`    | `Array<DslTarget>` | —      | 游标之后依次穿过的点（顺序敏感）；单点 = 一段曲线，游标终于末点 |
| `tension?`  | `number`           | `1`    | 切线长度乘子（TikZ `tension`）；缺省 1，&lt;1 更紧、>1 更鼓     |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type SmoothStepProps = {
  kind: 'smooth';
  points: Array<DslTarget>;
  tension?: number;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

## `@retikz/vanilla`

### path / InputPath

- `path`：<ApiSourceLink label={"path"} path={"packages/kernel/vanilla/src/normalize/helpers.ts"} startLine={30}>创建作者侧路径输入，身份由配置中的 id 声明</ApiSourceLink>
- `InputPath`：<ApiSourceLink label={"InputPath"} path={"packages/kernel/vanilla/src/normalize/path/types.ts"} startLine={163}>作者侧路径输入</ApiSourceLink>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable grouped>

| 分组   | 成员              | 类型                                  | 默认值 | 说明                                                                                                                                                                                    |
| ------ | ----------------- | ------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 主要   | `children?`       | `ReadonlyArray<InputStep>`            | —      | 路径步骤序列；与 way 互斥                                                                                                                                                               |
|        | `id?`             | `string`                              | —      | 可选稳定 id：供其它元素 anchor 引用，也是水合事件绑定的挂点（带 id 才能给 path 绑 onClick 等 handler）                                                                                  |
|        | `kind?`           | `string & {} \| "stroke"`             | —      | Path kind provider 名；省略时使用内置 `stroke`                                                                                                                                          |
|        | `kindOptions?`    | `Record<string, JsonValue>`           | —      | 由选定 Path kind provider 消费的 JSON-safe 参数对象；完整 schema 在后续解析                                                                                                             |
|        | `label?`          | `InputPath["label"]`                  | —      | 附着在 Path 或其它 path-like 宿主上的几何标签                                                                                                                                           |
|        | `type?`           | `'path'`                              | —      | 无法由 authoring 字段唯一识别时显式指定路径类别                                                                                                                                         |
|        | `way?`            | `WayDSL`                              | —      | TikZ 风格的路径走向简写                                                                                                                                                                 |
| 几何   | `arrow?`          | `InputPathArrowDirectionValue`        | —      | 路径级箭头方向                                                                                                                                                                          |
|        | `arrowDetail?`    | `IRArrowDetail`                       | —      | 箭头的顶层默认与端点覆盖配置                                                                                                                                                            |
|        | `arrowPlacement?` | `InputPathArrowPlacement`             | —      | 箭头端点的共享与逐端放置配置                                                                                                                                                            |
|        | `marks?`          | `InputPath["marks"]`                  | —      | 沿路径放方向标记：每项 `{ pos, endpointOverlap?, mark }`，`pos∈[0,1]`、`mark.kind:'arrow'` + 已注册箭头名，朝向随该点切线；endpointOverlap 只适用于内置 Stroke Path 实际选中的端点 mark |
|        | `rotate?`         | `number`                              | —      | 整条 path 旋转角（度，绕包围盒中心）                                                                                                                                                    |
|        | `roundedCorners?` | `number`                              | —      | 线段相交处的几何圆角半径；与只改变描边外观的 `lineJoin` 不同                                                                                                                            |
|        | `scale?`          | `number \| { x: number; y: number; }` | —      | 整条 path 缩放（数字等比，或 `{ x, y }` 非等比，绕包围盒中心）                                                                                                                          |
| 样式   | `style?`          | `InputPath["style"]`                  | —      | 当前实例的视觉覆盖，按字段继承与覆盖                                                                                                                                                    |
|        | `thickness?`      | `PathThicknessValue`                  | —      | 路径描边宽度语法糖                                                                                                                                                                      |
|        | `zIndex?`         | `number`                              | —      | 显式栈序：大者在上；缺省 0、同值保持声明顺序；只在同层子节点间生效                                                                                                                      |
| 动画   | `animations?`     | `InputPath["animations"]`             | —      | 声明式动画 track 数组（关键帧 + 时长 / 缓动 / 触发器）；用法与 preset 见动画组件文档                                                                                                    |
| 运行时 | `authoring?`      | `unknown`                             | —      | 可选编译驱动自行解释的运行时载荷，不进入 Core IR                                                                                                                                        |
|        | `meta?`           | `Record<string, JsonValue>`           | —      | provenance 元数据：原样透传进本路径 emit 的 Scene 图元，renderer 忽略、不参与布局 / 样式；典型由 Tier 2 lowering 注入（标记来自哪个 datum / series / layer），须为 JSON 对象            |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const path: (config: Omit<InputPath, 'type'>) => InputPath;

export type InputPath = InputPathBase & {
  type?: 'path';
};
```

</DocTab>

</DocTabs>

### InputStep

<p>
  <ApiSourceLink label={'InputStep'} path={'packages/kernel/vanilla/src/normalize/path/types.ts'} startLine={128}>
    作者侧路径步骤
  </ApiSourceLink>
</p>

```ts
export type InputStep =
  | InputMoveStep
  | InputLineStep
  | InputAxisLineStep
  | InputFoldStep
  | Extract<
      IRStep,
      {
        kind: 'cycle';
      }
    >
  | InputCurveStep
  | InputCubicStep
  | InputBendStep
  | InputCirclePathStep
  | InputEllipsePathStep
  | InputArcStep
  | InputRectangleStep
  | InputSmoothStep
  | InputGeneratorStep;
```

## `@retikz/core`

### ArrowDefinition

<p>
  <ApiSourceLink label={'ArrowDefinition'} path={'packages/kernel/core/src/contract/arrow/types.ts'} startLine={22}>
    可注册的 arrow 定义
  </ApiSourceLink>
</p>

描述箭头 marker 的尺寸、接触点和几何生成能力；定义本身不进入 IR

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员             | 类型                                                   | 默认值                       | 说明                                                                               |
| ---------------- | ------------------------------------------------------ | ---------------------------- | ---------------------------------------------------------------------------------- |
| `backX`          | `number`                                               | —                            | marker 基础几何沿箭头轴向承诺的包络后缘，必须不晚于线接触点                        |
| `baseSize?`      | `number`                                               | `10`                         | marker 局部基准边长（viewBox `0 0 baseSize baseSize`，refY = baseSize/2）；缺省 10 |
| `defaultLength?` | `number`                                               | `8`                          | 默认箭头长度（length fallback）；缺省 8                                            |
| `defaultWidth?`  | `number`                                               | `8`                          | 默认箭头宽度（width fallback）；缺省 8                                             |
| `emit`           | `(ctx: ArrowEmitContext) => Iterable<MarkerPrimitive>` | —                            | 局部坐标 marker 几何（renderer-agnostic）；adapter 把产物嵌进 `<marker>`           |
| `hollow?`        | `boolean`                                              | `false`                      | 空心标志：true 时由描边表达外轮廓，并按 lineWidth 修正接触点                       |
| `lineContactX`   | `number`                                               | —                            | 线接触点静态 base，决定 path shrink 与 marker refX                                 |
| `name`           | `string`                                               | —                            | arrow 名称，由 IR `marks[].mark.shape` 引用；作者侧使用 `arrowDetail.shape`        |
| `outerInset?`    | `number`                                               | `hollow ? lineWidth / 2 : 0` | 外轮廓补偿量（marker 局部坐标）；缺省时空心箭头用 lineWidth/2，实心箭头用 0        |
| `tipX?`          | `number`                                               | `baseSize`                   | 尖端 x（shrink 用）；缺省 = baseSize                                               |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ArrowDefinition = {
  name: string;
  baseSize?: number;
  hollow?: boolean;
  backX: number;
  lineContactX: number;
  outerInset?: number;
  tipX?: number;
  defaultLength?: number;
  defaultWidth?: number;
  emit: (ctx: ArrowEmitContext) => Iterable<MarkerPrimitive>;
};
```

</DocTab>

</DocTabs>

### ArrowEmitContext

<p>
  <ApiSourceLink label={'ArrowEmitContext'} path={'packages/kernel/core/src/contract/arrow/types.ts'} startLine={7}>
    arrow emit 的运行时上下文
  </ApiSourceLink>
</p>

提供已解析的颜色、描边粗细和取整函数，供定义生成 marker 几何

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员        | 类型                    | 默认值 | 说明                                                                       |
| ----------- | ----------------------- | ------ | -------------------------------------------------------------------------- |
| `fill`      | `MarkerFill`            | —      | 填充颜色（实心箭头主导色；空心箭头会按 `hollow` 处理后传入）               |
| `lineWidth` | `number`                | —      | 描边粗细（marker 局部坐标，user units）；空心箭头据此画外轮廓              |
| `round`     | `(n: number) => number` | —      | 精度取整函数（与 compile/render 同一 round，保几何一致）                   |
| `stroke`    | `MarkerFill`            | —      | 描边颜色（无 override 时 = `{ kind: 'contextStroke' }`，继承 path stroke） |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ArrowEmitContext = {
  stroke: MarkerFill;
  fill: MarkerFill;
  lineWidth: number;
  round: (n: number) => number;
};
```

</DocTab>

</DocTabs>

### PathGeneratorDefinition

<p>
  <ApiSourceLink
    label={'PathGeneratorDefinition'}
    path={'packages/kernel/core/src/contract/path-generator/types.ts'}
    startLine={31}
  >
    可注册的 path generator 定义
  </ApiSourceLink>
</p>

描述 JSON 参数、可解析 target 参数和命令生成能力；定义本身不进入 IR

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员            | 类型                                                        | 默认值 | 说明                                                                                              |
| --------------- | ----------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------- |
| `generate`      | `(ctx: PathGeneratorGenerateContext) => Array<PathCommand>` | —      | 根据上下文生成低层 path 命令<br />返回命令使用与上下文一致的局部坐标；可返回 `move` 形成 sub-path |
| `name`          | `string`                                                    | —      | generator 名称，由 generator step 的 `name` 引用                                                  |
| `paramsSchema`  | `ZodType<JsonObject>`                                       | —      | 实例参数 schema<br />解析结果必须是 JSON object                                                   |
| `targetParams?` | `Array<string>`                                             | `[]`   | 需要解析为当前 Path 局部坐标的 params 顶层 key                                                    |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PathGeneratorDefinition = {
  name: string;
  paramsSchema: ZodType<JsonObject>;
  targetParams?: Array<string>;
  generate: (ctx: PathGeneratorGenerateContext) => Array<PathCommand>;
};
```

</DocTab>

</DocTabs>

### PathGeneratorGenerateContext

<p>
  <ApiSourceLink
    label={'PathGeneratorGenerateContext'}
    path={'packages/kernel/core/src/contract/path-generator/types.ts'}
    startLine={11}
  >
    path generator 的运行时上下文
  </ApiSourceLink>
</p>

坐标均位于当前 Path 的局部坐标系，返回的命令由所属 GroupPrim 统一应用 scope transform

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员              | 类型                       | 默认值 | 说明                                                      |
| ----------------- | -------------------------- | ------ | --------------------------------------------------------- |
| `from`            | `Position`                 | —      | 当前游标局部坐标（上一段终点 / sub-path 起点）            |
| `params`          | `Record<string, unknown>`  | —      | paramsSchema 校验后的参数对象                             |
| `resolvedTargets` | `Record<string, Position>` | —      | targetParams 顶层 key 到当前 Path 局部坐标的解析结果      |
| `round`           | `(n: number) => number`    | —      | 精度取整函数，与 compile/render 使用同一 round            |
| `to?`             | `Position`                 | —      | step.to resolve 后的局部坐标<br />缺省表示 step 未给 `to` |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PathGeneratorGenerateContext = {
  from: Position;
  to?: Position;
  params: Record<string, unknown>;
  resolvedTargets: Record<string, Position>;
  round: (n: number) => number;
};
```

</DocTab>

</DocTabs>

### PathKindCompileContext

<p>
  <ApiSourceLink
    label={'PathKindCompileContext'}
    path={'packages/kernel/core/src/contract/path-kind/types.ts'}
    startLine={107}
  >
    path kind 编译上下文
  </ApiSourceLink>
</p>

自定义 kind 可以完全接管输出，也可以调用回调复用标准描边逻辑

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员                  | 类型                                                                                          | 默认值 | 说明                                                                                |
| --------------------- | --------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------- |
| `TPath` (泛型)        | `extends IRPathBase = IRPathBase`                                                             | —      | 经当前路径定义的 schema 解析后的路径类型，决定 path 字段的结构；默认使用 IRPathBase |
| `TOwnerOutput` (泛型) | `extends JsonValue = never`                                                                   | —      | 通过 ownerOutput.publish 发布的 JSON 产物类型；默认 never 表示不发布所属者产物      |
| `appearance`          | `ResolvedPathKindAppearance`                                                                  | —      | 已解析的 renderer-neutral 宿主外观                                                  |
| `emitHostLabels`      | `(input: PathKindLabelInput) => ReadonlyArray<ScenePrimitive>`                                | —      | 编译共享宿主标签，并支持 kind 提供边界偏移                                          |
| `emitStroke`          | `EmitStroke`                                                                                  | —      | 复用 core 标准描边编译逻辑；不传 path 时使用当前 `path`                             |
| `materializePath`     | `(input?: Readonly<{`<br />`children?: ReadonlyArray<IRStep>;`<br />`}>) => MaterializedPath` | —      | 物化选定 steps，不应用 marker、dash、fill 或 kind-specific geometry                 |
| `ownerOutput`         | `CompileOwnerOutputPublisher<TOwnerOutput>`                                                   | —      | 当前 Path kind 的最终所属者产物 publisher                                           |
| `path`                | `TPath`                                                                                       | —      | 经该 definition 完整 schema 解析后的 path subject                                   |
| `round`               | `(value: number) => number`                                                                   | —      | 与本次 compile 一致的取整函数                                                       |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PathKindCompileContext<TPath extends IRPathBase = IRPathBase, TOwnerOutput extends JsonValue = never> = {
  path: TPath;
  ownerOutput: CompileOwnerOutputPublisher<TOwnerOutput>;
  materializePath: (
    input?: Readonly<{
      children?: ReadonlyArray<IRStep>;
    }>,
  ) => MaterializedPath;
  emitStroke: EmitStroke;
  emitHostLabels: (input: PathKindLabelInput) => ReadonlyArray<ScenePrimitive>;
  appearance: ResolvedPathKindAppearance;
  round: (value: number) => number;
};
```

</DocTab>

</DocTabs>

### PathKindCompileResult

<p>
  <ApiSourceLink
    label={'PathKindCompileResult'}
    path={'packages/kernel/core/src/contract/path-kind/types.ts'}
    startLine={14}
  >
    path kind 编译结果
  </ApiSourceLink>
</p>

path kind definition 把高层 path 形态编译成当前 Path 局部坐标系中的 Scene primitive，
并返回同一坐标系内参与 bbox / transform 计算的关键点集合

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员           | 类型                    | 默认值 | 说明                                                             |
| -------------- | ----------------------- | ------ | ---------------------------------------------------------------- |
| `boundsPoints` | `Array<IRPosition>`     | —      | 当前 Path 局部坐标系中的 layout 与路径级 rotate / scale 几何依据 |
| `primitives`   | `Array<ScenePrimitive>` | —      | 当前 Path 局部坐标系中的实际渲染输出                             |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PathKindCompileResult = {
  primitives: Array<ScenePrimitive>;
  boundsPoints: Array<IRPosition>;
};
```

</DocTab>

</DocTabs>

### PathKindDefinition

<p>
  <ApiSourceLink
    label={'PathKindDefinition'}
    path={'packages/kernel/core/src/contract/path-kind/types.ts'}
    startLine={139}
  >
    注册路径种类的解析、编译与可选所属者产物契约
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员                  | 类型                              | 默认值 | 说明                                                                                     |
| --------------------- | --------------------------------- | ------ | ---------------------------------------------------------------------------------------- |
| `TPath` (泛型)        | `extends IRPathBase = IRPathBase` | —      | 完整路径 schema 的解析结果类型，同时决定编译上下文的 path 字段；默认使用 IRPathBase      |
| `TOwnerOutput` (泛型) | `extends JsonValue = never`       | —      | 所属者产物的 JSON 类型；默认 never 时不能声明 ownerOutput，否则必须提供对应的产物 schema |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PathKindDefinition<TPath extends IRPathBase = IRPathBase, TOwnerOutput extends JsonValue = never> = {
  name: string;
  schema: ZodType<TPath>;
  compile: (context: PathKindCompileContext<TPath, TOwnerOutput>) => PathKindCompileResult | null;
} & PathKindOwnerOutputBranch<TOwnerOutput>;
```

</DocTab>

</DocTabs>

### WayDSL

<p><ApiSourceLink label={"WayDSL"} path={"packages/kernel/core/src/parse/way.ts"} startLine={141}>way DSL 数组：sugar `<Draw way={...}>` 输入形态</ApiSourceLink></p>

```ts
export type WayDSL = Array<WayItem>;
```

### WayItem

<p>
  <ApiSourceLink label={'WayItem'} path={'packages/kernel/core/src/parse/way.ts'} startLine={123}>
    Sugar 层 way 数组 DSL 元素
  </ApiSourceLink>
</p>

节点 id 字符串/笛卡尔/极坐标 → line（首项 move）；`{position,type}` 相对偏移对象 → IR relative/relativeAccumulate；horizontalTo/verticalTo → axis-line；四种裸 via 或 `{via,fraction}` 与下一项合并 fold；DrawWay.Cycle → cycle；curve/cubic/bend infix 与下一项合并；arc/circle/ellipse infix 以上一项为圆心不消耗下一项。Cycle/Relative/Accumulate 底层字符串刻意写丑避节点 id 撞结构

```ts
export type WayItem =
  | IRTarget
  | string
  | WayRelativeItem
  | WayVia
  | WayFoldOp
  | WayCycle
  | WayCurveOp
  | WayCubicOp
  | WayBendOp
  | WayArcOp
  | WayCircleOp
  | WayEllipseOp
  | WayLabelOp
  | WayAxisLineOp;
```

### definePathKind

<p>
  <ApiSourceLink label={'definePathKind'} path={'packages/kernel/core/src/contract/path-kind/define.ts'} startLine={68}>
    定义路径种类，保留路径及所属者产物的类型关联
  </ApiSourceLink>
</p>

<DocTabs defaultValue="without-owner-output">

<DocTab value="without-owner-output" label="属性 · 无所属者产物">

定义不发布所属者产物的路径种类

<ApiTable variant="parameters">

| 类别   | 名称         | 类型 / 签名                        | 说明                                                                                                       |
| ------ | ------------ | ---------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 泛型   | `TPath`      | `extends IRPathBase = IRPathBase`  | 经定义 schema 解析后的路径类型，默认使用 IRPathBase                                                        |
| 参数   | `definition` | `PathKindDefinition<TPath, never>` | 包含名称、完整路径 schema 和编译函数的定义                                                                 |
| 返回值 | —            | `PathKindDefinition<TPath, never>` | 校验后的原定义对象，保留路径类型，不复制或修改输入                                                         |
| 错误   | —            | —                                  | RetikzCoreError 当 name 为空或仅含空白字符、schema 没有 parse 函数，或提供的 ownerOutput.schema 不是对象时 |

</ApiTable>

</DocTab>

<DocTab value="with-owner-output" label="属性 · 发布所属者产物">

定义发布所属者产物的路径种类

<ApiTable variant="parameters">

| 类别   | 名称           | 类型 / 签名                               | 说明                                                                                                       |
| ------ | -------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| 泛型   | `TPath`        | `extends IRPathBase`                      | 经定义 schema 解析后的路径类型                                                                             |
|        | `TOwnerOutput` | `extends JsonValue`                       | 由 ownerOutput.schema 校验、通过编译上下文发布的 JSON 产物类型                                             |
| 参数   | `definition`   | `PathKindDefinition<TPath, TOwnerOutput>` | 包含路径定义及 ownerOutput.schema 的定义                                                                   |
| 返回值 | —              | `PathKindDefinition<TPath, TOwnerOutput>` | 校验后的原定义对象，保留路径和产物类型，不复制或修改输入                                                   |
| 错误   | —              | —                                         | RetikzCoreError 当 name 为空或仅含空白字符、schema 没有 parse 函数，或提供的 ownerOutput.schema 不是对象时 |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
<TPath extends IRPathBase = IRPathBase>(definition: PathKindDefinition<TPath, never>): PathKindDefinition<TPath, never>;
<TPath extends IRPathBase, TOwnerOutput extends JsonValue>(definition: PathKindDefinition<TPath, TOwnerOutput>): PathKindDefinition<TPath, TOwnerOutput>;
```

</DocTab>

</DocTabs>

### DrawWay

<p>
  <ApiSourceLink label={'DrawWay'} path={'packages/kernel/core/src/parse/way.ts'} startLine={33}>
    Sugar 层 way 数组的关键字常量
  </ApiSourceLink>
</p>

Cycle 闭合到 way 起点（底层字符串故意写丑避节点 id 冲突，只通过 DrawWay.Cycle 引用）；Hv/Vh/Hvh/Vhv 是两段或三段折角算子；Relative/Accumulate 相对偏移 way item 的 type 鉴别值（Relative=TikZ `(+x,+y)` 不推进 prevEnd，Accumulate=TikZ `(++x,++y)` 累积更新）。用 const + as const 而非 TS enum 避免 reverse-mapping 与字面量不互通

```ts
export const DrawWay = {
  Cycle: 'retikz-keyword_cycle',
  Hv: '-|',
  Vh: '|-',
  Hvh: '-|-',
  Vhv: '|-|',
  Relative: 'retikz-keyword_relative',
  Accumulate: 'retikz-keyword_accumulate',
} as const;
```

### defineArrow

<p>
  <ApiSourceLink label={'defineArrow'} path={'packages/kernel/core/src/contract/arrow/define.ts'} startLine={9}>
    定义 arrow 注册项
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable variant="parameters">

| 类别   | 名称  | 类型 / 签名       | 说明                                   |
| ------ | ----- | ----------------- | -------------------------------------- |
| 参数   | `def` | `ArrowDefinition` | 箭头名称、尺寸及几何生成函数组成的定义 |
| 返回值 | —     | `ArrowDefinition` | 原定义对象，不复制或修改输入           |

</ApiTable>

> **备注：** 当前是 typed identity；保留入口用于对齐 registry API，并为未来校验或归一化预留空间

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const defineArrow: (def: ArrowDefinition) => ArrowDefinition;
```

</DocTab>

</DocTabs>

### definePathGenerator

<p>
  <ApiSourceLink
    label={'definePathGenerator'}
    path={'packages/kernel/core/src/contract/path-generator/define.ts'}
    startLine={13}
  >
    定义 path generator 注册项
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable variant="parameters">

| 类别   | 名称  | 类型 / 签名               | 说明                                                 |
| ------ | ----- | ------------------------- | ---------------------------------------------------- |
| 参数   | `def` | `PathGeneratorDefinition` | 路径生成器名称、参数 schema 及命令生成函数组成的定义 |
| 返回值 | —     | `PathGeneratorDefinition` | 校验名称后的原定义对象，不复制或修改输入             |
| 错误   | —     | —                         | RetikzCoreError 当 name 为空或仅含空白字符时         |

</ApiTable>

> **备注：** generator 输出的 JSON-safe 校验仍由 compile 阶段负责

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const definePathGenerator: (def: PathGeneratorDefinition) => PathGeneratorDefinition;
```

</DocTab>

</DocTabs>
