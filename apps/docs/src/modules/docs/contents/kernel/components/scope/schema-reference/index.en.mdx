---
title: Schema reference
description: Look up public Scope, placement, transform, and clipping data contracts
---

This page selects Scope-related schemas from `@retikz/core` by name and reads their fields from source. See [API reference](/kernel/components/scope/api-reference) for React props and [Basic usage](/kernel/components/scope/usage) to get started.

`ScopeSchema` is the Tier 1 container node. It does not draw a new visual primitive; it establishes local transforms, style defaults, clipping, stacking, and reference boundaries for a child subtree. A Scope can still be referenced through its own `id`.

<span id="scope"></span>

## Scope

<ZodSchema name="ScopeSchema" />

`Scope.theme` contains only sparse `style` / `mode` environment selectors, inherited per field. Core resolves shared colors; descendant composites consume this environment according to their own contracts. `defaults.reset` does not reset Theme.

<span id="scopeposition"></span>

## Scope placement

`ScopePlacementSchema` is the closed self-anchor-to-target contract. `target` accepts only a Cartesian `[x, y]` in the parent frame or a previously completed `NodeTarget`; Polar, Offset, Between, and path-relative targets are excluded.

<ZodSchema name="ScopePlacementSchema" />

A NodeTarget offset is added once in world user units before projection into the parent frame. Forward, self, descendant, and unresolved Scope-placeholder references fail synchronously without topological sorting.

<span id="scopeselfpoint"></span>

### Scope self point

<ZodSchema name="ScopeSelfPointSchema" />

`origin` is always intrinsic local `[0, 0]`. Other anchors reuse standard names, numeric angles, and `{ side, fraction }`; an explicit local point is also accepted. For an empty Scope, origin, center, edges, corners, and numeric angles collapse to `[0, 0]`, while `{ side, fraction }` still fails on the zero-size envelope.

<span id="default-channels"></span>

### Four default channels

Scope separates defaults into four independent channels. Each fills only fields omitted by the matching host; `defaults.reset` can block parent-Scope inheritance per channel.

| Field            | Reused contract                                                 | Does not own                             |
| ---------------- | --------------------------------------------------------------- | ---------------------------------------- |
| `defaults.node`  | Visual fields from [Node](/kernel/reference/schema/entity#node) | id, position, text, labels, or metadata  |
| `defaults.path`  | Visual fields from [Path](/kernel/reference/schema/path#path)   | kind, steps, marks, or instance metadata |
| `defaults.label` | Label color, textColor, opacity, and font                       | Text, position, or host relation         |
| `defaults.arrow` | [ArrowDetail](/kernel/reference/schema/path#arrowdetail)        | Arrow existence or path placement        |

<span id="transform"></span>

## Transform

`TransformSchema` is the union accepted by `Scope.transforms`. Index 0 is outermost and the last item acts first. Translation variants resolve to Cartesian translation; a rotate pivot lowers to Scene `cx/cy`, while a non-origin scale pivot lowers to `translate(p) → scale → translate(-p)`.

<ZodSchema name="TransformSchema" />

| `kind`              | Core fields                    | Effect                                 |
| ------------------- | ------------------------------ | -------------------------------------- |
| `translate`         | `x`, `y`                       | Cartesian translation                  |
| `polar-translate`   | `origin?`, `angle`, `radius`   | Translation derived from polar input   |
| `at-translate`      | `direction`, `of`, `distance?` | Translation relative to a named entity |
| `offset-translate`  | `of`, `offset?`                | Base point plus a 2D offset            |
| `between-translate` | `between`, `fraction`          | Proportional point between endpoints   |
| `rotate`            | `degrees`, `pivot?`            | Rotate around a Scope self point       |
| `scale`             | `x`, `y?`, `pivot?`            | Scale around a Scope self point        |

`pivot` defaults to `origin` and accepts a [Scope self point](#scope-self-point). Use `pivot` to declare the rotation center.

<span id="clip"></span>

## Clip

`ClipSchema` is the Core contract accepted by `Scope.clip`. Core precisely owns only `rect`; any other non-empty `kind` is an open JSON-safe clip spec validated and resolved by a Definition contributed to `CompileOptions.clips`.

| Branch               | Discriminator            | Content                                                      |
| -------------------- | ------------------------ | ------------------------------------------------------------ |
| Core built-in        | `rect`                   | Precisely validated by `RectClipSchema`                      |
| Extension Definition | Any other non-empty kind | JSON-safe fields are validated by its contributed Definition |

<span id="rectclip"></span>

### RectClip

<ZodSchema name="RectClipSchema" />

<ZodSchema name="ClipSchema" />

Circle, path, and compound clipping come from [Standard clipping](/library/standard/clip), outside the Core schemas selected here.
