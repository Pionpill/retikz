---
title: Shadow
description: Element-level drop shadow — applied to a Node shape / Path main path, calibrated across backends and stackable; a preset string or an object.
---

`shadow` is an element-level visual-effect prop you can set directly on `<Node>` and `<Path>`. It applies to **the primitive itself** — the Node shape, the Path main path (including its endpoint arrows). A Node's text / label / pin are separate primitives and get no shadow. It is renderer-agnostic: SVG and Canvas each translate it to native capabilities and calibrate the visual contract to stay close.

## Usage

```tsx
import { Node } from '@retikz/react';
```

```tsx
<Node shape="rectangle" fill="white" shadow="md" />
```

## Examples

`shadow` adds a drop shadow to the primary geometry. Preset strings and objects are two forms of the same field.

### Presets

The simplest form is a Tailwind-style preset string: `'sm'` / `'md'` / `'lg'` / `'xl'` / `'2xl'` / `'none'`. Larger steps push the shadow farther and softer. `'none'` means an explicit no-shadow (equivalent to omitting it).

<ComponentPreview files="shadow-presets" size="sm" />

<span style={{ color: '#888' }}>The same white card at five preset steps.</span>

### Object form

For precise control, use an object `{ preset?, offsetX?, offsetY?, blur?, color?, opacity? }`:

- With a `preset`, that step is the **default base**, and any field you also set overrides it field by field — `{ preset: 'md', color: '#3b82f6' }` is "md's offset and blur, recolored".
- Without a `preset`, you must give `offsetX` + `offsetY` yourself (fully explicit).

Priority is always: **explicit field > preset > default**. The default color is translucent black `rgba(0,0,0,0.5)`; `opacity` (when given) multiplies onto the color's effective alpha.

<ComponentPreview files="shadow-object" size="sm" />

<span style={{ color: '#888' }}>
  Left: preset + recolor (md base, blue); right: a fully explicit shadow on a Path main path.
</span>

### Element-level, not group-level

`shadow` follows the primitive itself. A Node's text / label / pin are **separate primitives** and get no shadow — there is no "lift the whole card including its text" (this is an element-level effect, not a group-level one). But a Path's endpoint arrows belong to the main-path primitive and **cast a shadow together with it** (matching the native semantics of SVG's element-level filter / Canvas's single draw pass).

<ComponentPreview files="shadow-primary-only" size="sm" />

<span style={{ color: '#888' }}>
  The shadow lands on the rectangle; the adjacent text and label are separate primitives with none.
</span>

<ComponentAlert
  title="Blur is a diagnosable approximation"
  description="blur is a blur radius in user units. SVG and Canvas apply backend-specific calibration through their native shadow APIs, keeping it close under preview sizing / transforms; it is not pixel-exact. blur=0 is a hard edge."
/>

## How it works

<ComponentAlert
  type="tip"
  title="Optional for everyday use"
  description="Preset and object forms are enough for daily use. Revisit this section when diagnosing clipping, scaling, or cross-backend differences."
/>

### From input to primary geometry

The two `shadow` forms exist only at the input boundary. During compile, a preset expands into offsets, blur, and color, then explicit object fields override those values one by one. The result converges on one resolved shadow description attached to a Node shape or Path main path, so renderers never need to understand preset names.

The figure isolates shadow's local responsibility. See [Principles](/kernel/principles) for the complete JSX / IR → Scene pipeline.

<ComponentPreview files="shadow-pipeline" size="xl" hideCode />

This is also why `shadow="md"` and `shadow={{ preset: 'md' }}` produce the same output, while Node text and labels do not inherit the shadow: they are emitted later as separate primitives rather than as part of the primary shape carrying `shadow`.

<SourceLinks
  sources={[
    {
      label: 'Expand presets and merge explicit fields',
      path: 'packages/kernel/core/src/compile/style/shadow.ts',
      startLine: 8,
      endLine: 29,
    },
    {
      label: 'Attach shadow to the Node primary shape',
      path: 'packages/kernel/core/src/compile/node/emit.ts',
      startLine: 16,
      endLine: 45,
    },
    {
      label: 'Resolve Path primary-geometry style',
      path: 'packages/kernel/core/src/compile/path/host/resolve.ts',
      startLine: 19,
      endLine: 40,
    },
  ]}
/>

### Automatic layout and fixed cropping

A shadow extends beyond its original geometry. Automatic layout expands each side by `blur` plus the part of the offset pointing toward that side, then merges that result into the Scene's visible range. An explicit `viewBox`, by contrast, always remains the fixed window chosen by the user.

| Layout mode        | Compile behavior                       | Visible result                                 |
| ------------------ | -------------------------------------- | ---------------------------------------------- |
| No `viewBox`       | Merge geometry and shadow overflow     | The canvas automatically reserves shadow room  |
| Explicit `viewBox` | Fixed window replaces automatic layout | Shadow outside the window may still be clipped |

<SourceLinks
  sources={[
    {
      label: 'Merge shadow overflow into automatic bounds',
      path: 'packages/kernel/core/src/compile/orchestration/bounds.ts',
      startLine: 7,
      endLine: 33,
    },
    {
      label: 'Let an explicit viewBox replace automatic layout',
      path: 'packages/kernel/core/src/compile/compile.ts',
      startLine: 22,
      endLine: 42,
    },
  ]}
/>

### SVG and Canvas mapping

The Scene keeps renderer-agnostic values in user units. SVG and Canvas consume the same resolved shadow description but translate it to different native capabilities:

| Backend | Native mapping | Calibration and resources                         |
| ------- | -------------- | ------------------------------------------------- |
| SVG     | `feDropShadow` | Expanded `userSpaceOnUse` region<br />shared defs |
| Canvas  | `ctx.shadow*`  | Transform-aware offsets and blur<br />saved state |

SVG maps `blur` to Gaussian standard deviation and bases the filter region on the expanded Scene window, preventing thin Paths or large shadows from being clipped by the element's own bounding box. Identical shadows share one filter def. Canvas reads the current transform matrix (CTM) to calibrate offsets and blur to the actual drawing scale; after CSS color normalization, `opacity` is baked into `shadowColor`. The two backends do not promise pixel-identical output.

<SourceLinks
  sources={[
    {
      label: 'Build the SVG shadow filter and expanded region',
      path: 'packages/kernel/render/src/svg/builders/shadow-defs.ts',
      startLine: 46,
      endLine: 102,
    },
    {
      label: 'Deduplicate and register SVG shadow resources',
      path: 'packages/kernel/render/src/svg/builders/document.ts',
      startLine: 81,
      endLine: 108,
    },
    {
      label: 'Calibrate and draw Canvas shadows under transforms',
      path: 'packages/kernel/render/src/canvas/draw-scene.ts',
      startLine: 117,
      endLine: 172,
    },
    {
      label: 'Normalize CSS colors and bake shadow opacity',
      path: 'packages/kernel/render/src/canvas/draw-scene.ts',
      startLine: 189,
      endLine: 208,
    },
  ]}
/>

## API Reference

`shadow` is a shared style prop on `<Node>` and `<Path>`, written the same way on both.

| Prop     | Type                                                                                                                 | Default | Description                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------- | -------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shadow` | `'sm' \| 'md' \| 'lg' \| 'xl' \| '2xl' \| 'none'`<br />`\| { preset?, offsetX?, offsetY?, blur?, color?, opacity? }` | —       | Drop shadow on the primary geometry: a preset string, or an object (preset seeds defaults, explicit fields override; without a preset you must give offsetX + offsetY). Priority: explicit > preset > default; color defaults to `rgba(0,0,0,0.5)`, `opacity` multiplies onto the color alpha. Element-level: a Node's text / label / pin (separate primitives) get none; a Path's endpoint arrows cast a shadow with the main path |

### shadow object fields

| Prop      | Type                                              | Default           | Description                                                                                           |
| --------- | ------------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------- |
| `preset`  | `'sm' \| 'md' \| 'lg' \| 'xl' \| '2xl' \| 'none'` | —                 | Tailwind-style step seeding offsetX / offsetY / blur / color as defaults; explicit fields override it |
| `offsetX` | `number`                                          | `0` (from preset) | Horizontal offset (user units)                                                                        |
| `offsetY` | `number`                                          | from preset       | Vertical offset (user units, positive = downward)                                                     |
| `blur`    | `number` (≥0)                                     | from preset       | Blur radius (user units); `0` = hard edge                                                             |
| `color`   | `string`                                          | `rgba(0,0,0,0.5)` | Shadow color, any CSS color                                                                           |
| `opacity` | `number` (0–1)                                    | —                 | Shadow opacity, multiplied onto the color's effective alpha                                           |

## Related

- [Blend](/kernel/components/effects/blend): the element-level `blendMode`
- [Node](/kernel/components/node/overview): minimal usage and topic entry points
- [Path](/kernel/components/draw/path): path basics, common props, and topic entry points
