---
title: API reference
description: Component props, authoring input, and extension contracts for drawing
---

This focused view is generated from the public entries of `@retikz/react`, `@retikz/vanilla`, and `@retikz/core`. Start with [Basic usage](/kernel/components/path/usage); persistent fields are in the [Schema reference](/kernel/components/path/schema-reference).

[Custom paths](/kernel/components/path/custom-path#related-props) summarizes the `definePathKind` extension entry point. The generator retains the source name of this callable type; that does not imply a single call form.

The React section also includes Circle, Ellipse, Rectangle, RegularPolygon, Star, Arc, Sector and their Props.

{/* @include kernel/components/path/api-reference/generated */}
