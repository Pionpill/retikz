---
title: Scope
description: Scope is Kernel's local-context container: a group shares coordinate transforms, style defaults, namespaces, group references, and clipping without gaining drawing or automatic-layout behavior.
---

`<Scope>` places a group of children in one local context. It does not draw a new primitive or calculate plot / table layout; it only controls how the group transforms, how it can be referenced, and how it enters Scene.

Treat Scope as a _sub-figure boundary_. Use it only when the group genuinely shares geometry or runtime semantics. For JSX organization alone, extract an ordinary React component.

## Usage

```tsx
import { Scope } from '@retikz/react';
```

```tsx
<Scope id="cluster" transforms={[{ kind: 'translate', x: 80, y: 0 }]}>
  {/* Node / Path / Coordinate / nested Scope */}
</Scope>
```

### When to use Scope

| Task                  | Entry point            | Scope stops at                  |
| --------------------- | ---------------------- | ------------------------------- |
| Transform a subfigure | `transforms`           | It does not place raw children  |
| Reference a group     | `id` + `boundingShape` | Path routes do not enter bounds |
| Isolate reusable ids  | `localNamespace`       | `scope.id` stays in the parent  |
| Share defaults        | `color` / `*Default`   | Explicit element values win     |
| Limit visibility      | `clip`                 | No renderer-specific semantics  |
| Move a whole stack    | `zIndex`               | Child order stays unchanged     |

<ComponentAlert
  type="warn"
  title="Do not wrap Scope for code structure alone"
  description="Without shared transforms, references, namespaces, defaults, or clipping, Scope only adds an unnecessary context boundary."
/>

### Transform inputs

Five translation forms mirror Node's relative-position model; `rotate` and `scale` set the local axes and scale. Array order follows an SVG transform list, so the last item acts on a local point first.

| kind                | Key fields                     | Effect                             |
| ------------------- | ------------------------------ | ---------------------------------- |
| `translate`         | `x`, `y`                       | Fixed Cartesian translation        |
| `polar-translate`   | `origin?`, `angle`, `radius`   | Polar placement from an origin     |
| `at-translate`      | `direction`, `of`, `distance?` | Place in a referent direction      |
| `offset-translate`  | `of`, `offset?`                | Add a local offset to a referent   |
| `between-translate` | `between`, `fraction`          | Interpolate between two endpoints  |
| `rotate`            | `degrees`, `cx?`, `cy?`        | Rotate the local coordinate system |
| `scale`             | `x`, `y?`                      | Uniform or per-axis scaling        |

## Examples

### Local coordinates

`O` / `T` are fixed referents outside the Scope. Switch among all seven transform inputs and adjust their parameters in the panel: only the local axes and point `q` move as a group, while the two dotted guide projections preserve `q`'s position in the local coordinate system.

<ComponentPreview files="scope-translate-basic" size="sm" />

Relative directions are interpreted in the current local coordinate system, so rotating Scope also rotates the visual direction of `right` with the axes.

### Group references and namespaces

With an `id`, Scope derives a referenceable group target from the children bounds. Use the panel to switch `boundingShape` and the connection anchor: the boundary shape and attachment point change independently, while the children stay fixed.

<ComponentPreview files="scope-id-reference" size="sm" />

Use `localNamespace` only when a reusable subfigure truly carries fixed ids. The panel starts with both Nodes named `A`; disable isolation to redirect the outer arrow to the inner Node, or rename the inner id to `B` to remove the conflict.

<ComponentPreview files="scope-local-namespace-basic" size="sm" />

### Clipping and stacking

`clip` uses Scope-local coordinates and becomes a renderer-agnostic Scene resource downstream. Switch among all six built-in regions in the panel, then register a [Custom Clip](/kernel/components/layout/custom-clip) when those are insufficient.

<ComponentPreview files="scope-clip" size="xs" previewClassName="!p-3 !pt-[52px]" />

`zIndex` moves the whole Scope among siblings without reordering children inside it. Adjust all four Nodes and both Scopes in the panel to compare within-group and whole-group stacking.

<ComponentPreview files="scope-z-index" size="sm" />

### Style defaults

Scope also exposes `color`, shared graphic state, and `nodeDefault` / `pathDefault` / `labelDefault` / `arrowDefault`. Values cascade per field, explicit element values win, and `resetStyle` cuts only selected outer channels.

Style is a supporting responsibility and does not alter local coordinates, references, or clipping. Consult the API below for color, stroke, and opacity fields rather than creating extra Scopes for visual variants.

<ComponentAlert
  type="tip"
  title="Shared defaults also reduce configuration size"
  description="Putting genuinely shared local styles on Scope avoids repeating the same fields on every child, often reducing both configuration objects and persisted JSON substantially. Tier 2 packages such as Plot also centralize series or layer styles as Scope defaults during lowering to keep generated IR compact."
/>

## How it works

<ComponentAlert
  type="tip"
  title="Optional for everyday use"
  description="Return when you hit transform projection, id conflicts, envelope, or clipping edge cases."
/>

This section covers only Scope behavior visible to users. See [Architecture](/kernel/concepts/design/principles) for the complete JSX → IR → Scene pipeline.

### Transforms and relative positions

Seven inputs converge into Scene's `translate` / `rotate` / `scale`. Relative placement follows the current Scope chain: global referent → inverse-project into local space → add the relative value → project back to global space.

<ComponentPreview files="scope-coordinate-flow" size="sm" hideCode />

That is why rotate changes the visible direction of `right`, while scale changes the visible length of an offset. A Path waits until ids are available, resolves its endpoints, and backfills the original Group placeholder; deferred resolution preserves declaration order, Scope ownership, and clipping.

<SourceLinks
  sources={[
    {
      label: 'Converge transform inputs',
      path: 'packages/kernel/core/src/compile/scope.ts',
      startLine: 34,
      endLine: 112,
    },
    {
      label: 'Relative placement and Scope-chain projection',
      path: 'packages/kernel/core/src/compile/position.ts',
      startLine: 55,
      endLine: 98,
    },
    {
      label: 'Deferred Path resolution and in-place backfill',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 147,
      endLine: 161,
    },
  ]}
/>

### Namespaces and id conflicts

A default Scope reuses the current namespace frame. Only `localNamespace` creates a frame; lookup returns the first inside-out match, while registration writes only to the current frame.

<ComponentPreview files="scope-namespace-resolution" size="md" hideCode />

Equal ids in different frames are valid shadowing. A duplicate in one frame emits `DUPLICATE_NODE_ID` and keeps the later definition. `scope.id` registers in the parent before the child frame is entered, so the group handle remains externally visible.

<SourceLinks
  sources={[
    {
      label: 'Namespace registration and lookup',
      path: 'packages/kernel/core/src/compile/namespace.ts',
      startLine: 24,
      endLine: 132,
    },
    {
      label: 'Parent and child frame orchestration',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 312,
      endLine: 415,
    },
  ]}
/>

### Group handles and Scene output

`scope.id` and `clip` follow separate paths: one summarizes child layouts as a reference handle, while the other resolves a JSON clip spec into a Scene resource. Both stop at the Core compile boundary.

<ComponentPreview files="scope-boundary-clip" size="sm" hideCode />

The Scope envelope collects node, coordinate, and nested-Scope layouts, not path routes. The default is a global AABB; circle mode computes a minimum enclosing circle. A `GroupPrim` is emitted when the subtree has output or Scope carries `transforms` / `id` / `clip`; an empty Scope without those semantics is pruned.

<SourceLinks
  sources={[
    {
      label: 'Scope envelope and Group output',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 312,
      endLine: 394,
    },
    {
      label: 'Rectangle and circle synthetic layouts',
      path: 'packages/kernel/core/src/compile/node/synthetic.ts',
      startLine: 80,
      endLine: 137,
    },
    {
      label: 'Clip resolution and resource registration',
      path: 'packages/kernel/core/src/compile/resource/clip.ts',
      startLine: 156,
      endLine: 189,
    },
  ]}
/>

## API Reference

### Context and output

| Prop             | Type                         | Default       | Description                                                                       |
| ---------------- | ---------------------------- | ------------- | --------------------------------------------------------------------------------- |
| `children`       | `ReactNode`                  | —             | Node / Path / Coordinate / nested Scope                                           |
| `transforms`     | `Array<IRTransformInput>`    | —             | Seven local transforms; the last acts first                                       |
| `id`             | `string`                     | —             | Register the Scope envelope as a group handle                                     |
| `localNamespace` | `boolean`                    | `false`       | Isolate child ids; `scope.id` remains visible                                     |
| `boundingShape`  | `'rectangle' \| 'circle'`    | `'rectangle'` | Rectangle or circle envelope for `scope.id`                                       |
| `clip`           | `IRClipSpec`                 | —             | Built-in or custom clip in local coordinates                                      |
| `zIndex`         | `number` (integer)           | `0`           | Stable sibling order for the whole Scope                                          |
| `meta`           | `Record<string, JSON value>` | —             | Forwarded to GroupPrim, not layout or cascade                                     |
| `animations`     | `Array<IRAnimationTrack>`    | —             | Group animation, not inherited by children                                        |
| `on<Event>`      | `HydrationHandler`           | —             | Requires `id`; see [Hydration](/kernel/packages/render/hydration) for every event |

### Style defaults

| Prop                                                            | Type                             | Default | Description                               |
| --------------------------------------------------------------- | -------------------------------- | ------- | ----------------------------------------- |
| `color`                                                         | `string`                         | —       | Primary color cascaded across elements    |
| `stroke` / `fill`                                               | `string \| IRPaintSpec`          | —       | Per-field stroke and fill cascade         |
| `strokeWidth` / opacity fields                                  | `number`                         | —       | Stroke width and per-field opacity        |
| `nodeDefault` / `pathDefault` / `labelDefault` / `arrowDefault` | default objects                  | —       | Four independent element-default channels |
| `resetStyle`                                                    | `boolean \| Array<StyleChannel>` | `false` | Cut all or selected outer Scope channels  |

## Related

- [Layout](/kernel/components/layout/overview): Figure input, capability injection, and host boundary
- [Node](/kernel/components/node/overview): The most common referenceable child in a Scope
- [Coordinate](/kernel/components/node/coordinate): Named coordinates without visible geometry
- [Custom Clips](/kernel/components/layout/custom-clip): Extend `clip.kind` through the shared registry path
- [Hydration](/kernel/packages/render/hydration): Scope events and handler context
