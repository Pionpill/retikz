---
title: Path
description: Describe a Kernel path with ordered Steps and apply stroke, fill, arrows, marks, or whole-path transforms.
---

## Usage

```tsx
import { Path, Step } from '@retikz/react';
```

```tsx
<Path stroke="currentColor">
  <Step kind="move" to="a" />
  <Step kind="line" to="b" />
</Path>
```

`<Draw>` compiles down to `<Path>` and covers everyday step / fold / curve / arc connections; but `<Path>` additionally exposes element-level capabilities — `color` / `rotate` / `scale` / `marks` / `id` / `meta` / `animations` — that `<Draw>` does not. Reach for `<Draw>`'s single-line `way` array for everyday connections, and `<Path>` when you need an explicit Step sequence, multiple `move`s, per-segment mixing of `kind` / label / control points, or any of those Path-only capabilities:

|                                                                       | `<Draw>`                | `<Path>` + `<Step>`                              |
| --------------------------------------------------------------------- | ----------------------- | ------------------------------------------------ |
| Form                                                                  | Single-line `way` array | Container + child nodes                          |
| step / curve / fold / arc                                             | Yes                     | Yes                                              |
| `color` / `rotate` / `scale` / `marks` / `id` / `meta` / `animations` | Not exposed             | Yes                                              |
| Use when                                                              | 90% of connection cases | Explicit Step sequence, element-level capability |

### Path and Step

`<Path>` requires `<Step>` children (at least 2); if the first isn't `move`, the compiler coerces it to `move`:

```tsx
<Path>
  <Step kind="move" to={...} />     {/* First: must be move */}
  <Step kind="line" to={...} />     {/* Then any number of line / move / step */}
  ...
</Path>
```

`<Step>` supports thirteen kinds — `move` / `line` for straight segments, `fold` for the right-angle fold, `cycle` to close back, `curve` / `cubic` for Bezier curves, `bend` as the arc shorthand (with out/in angles and self-loops), `smooth` for a smooth curve through a list of points, `generator` for injected custom or Standard path generators, `arc` / `circlePath` / `ellipsePath` for arcs / full circles / full ellipses centered on the cursor, and `rectangle` for a diagonal rectangle. See [Step](/kernel/components/draw/step) and [path generators](/kernel/components/draw/path-generator) for details.

## Examples

### Basic structure

Compare a polyline, multiple subpaths, and a closed fill in one panel. Repeated `move`s lift the pen and begin independent subpaths; the fill case uses `cycle`, which maps to SVG `Z`, to close its outline. See [Way](/kernel/components/draw/way) for focused fold and closure controls; this panel stays at the Path-container composition level.

<ComponentPreview files="path-structure" size="sm" />

### Endpoints and connections

#### Arrows

`arrow` picks direction (`'->'` / `'<-'` / `'<->'` / `'none'`); `arrowDetail` is a single object covering every visual spec (shape / scale / length / width / color / fill / opacity / lineWidth) plus optional `start` / `end` sub-objects for per-end overrides. The full reference — Core's `normal` / `stealth` baseline, optional Standard arrow shapes, per-end merge semantics, color / sizing / opacity, and the complete API table — lives on the **[Arrow](/kernel/components/draw/arrow)** page.

#### Endpoint connection surface

When an endpoint references a node (the `{ id }` object form, or an id inside a way), you can add a `boundary` field to override the target node's [`boundary`](/kernel/components/node/overview#visual-shape-vs-connection-geometry) (connection surface) **for that endpoint only**. It takes the same forms as the node's `boundary`: `'shape'` (the visual shape itself) / `'circle'` (a true circle) / any registered shape name / `{ type, params }`.

```tsx
<Path arrow="->">
  <Step kind="move" to="a" />
  <Step kind="line" to={{ id: 'star', boundary: { type: 'circle', params: { fit: 'tight', gap: 8 } } }} />
</Path>
```

`circle`, `rectangle`, and `ellipse` share `fit: 'tight' | 'bounds'` plus a signed `gap`. They default to shape-aware `tight` and `gap: 0`. Both fit values are equivalent for `rectangle`; a negative gap contracts inward but may cross the visual shape.

`boundary` only applies where a connection surface is meaningful: endpoint auto-clip (no explicit `anchor`) and that endpoint's standard direction / angle anchor. In direction-less reference contexts — `between` endpoints, an `offset` `of`, a node center — it is a no-op. A node's default connection surface lives on [`<Node boundary>`](/kernel/components/node/overview#visual-shape-vs-connection-geometry); the endpoint `boundary` is the per-edge override.

The five-pointed star below has no node `boundary`. The panel starts on the circle boundary, so you can compare `tight` with `bounds` and adjust `gap` immediately; switch back to the star outline to compare the real shape surface. The light dotted outline is the current circle boundary and updates with the real endpoint.

<ComponentPreview files="path-boundary" size="sm" />

### Styling

Set `strokeWidth` directly or use the semantic `thickness` presets from `ultraThin` through `ultraThick`; an explicit `strokeWidth` wins. `dashOffset` shifts only the dash rhythm's origin. `lineCap` / `lineJoin` shape endpoints and corners, while `opacity` / `fillOpacity` / `strokeOpacity` are three composable channels.

<ComponentPreview files="path-style" size="sm" />

#### Gradient stroke

Besides a plain color string, `stroke` accepts an `IRPaintSpec`. Linear / radial gradients, patterns, and images enter the same paint resource table; `dashPattern`, `dashOffset`, `lineCap`, `strokeOpacity`, and other stroke styles still apply to the line itself. Adjust the linear-gradient angle and three color stops in the panel to inspect their distribution along one path.

<ComponentPreview files="path-stroke-paint" size="sm" />

If a gradient-stroked path also has arrows, give `arrowDetail.color` or the endpoint `color` an explicit solid color; marker geometry cannot inherit a resource-backed stroke.

#### Stacking order (zIndex)

By default stacking = declaration order. `zIndex` overrides it explicitly: higher draws on top, omitted = 0, ties keep declaration order; effective only among siblings at the same level (same scope / top level). The two filled rectangles below are declared dodgerblue-then-red; move the blue path's `zIndex` through negative, zero, and positive values to compare the overlap order.

<ComponentPreview files="path-z-index" size="sm" />

### Curves and transforms

#### Out / in angles and self-loops

Besides the symmetric `bendDirection` + `bendAngle`, `<Step kind="bend">` accepts `outAngle` / `inAngle` for the start out-angle and end in-angle to draw asymmetric curves; `looseness` tunes the control-point distance (default ≈1). **out/in take precedence over `bendDirection`.** When `from == to` (same node / same coordinate) it degenerates into a **self-loop** — the prime use case for out/in (a state-machine self transition), which a symmetric bend cannot draw. Switch between a self-loop and a two-endpoint connection in the panel, then use the dotted direction guides to tune both angles and looseness.

<ComponentPreview files="path-outin-loop" size="md" />

#### Control-point inspection

After installing `@retikz/inspect`, use `InspectLayout` and `InspectPath` to select this Path occurrence at runtime. The built-in stroke Path Inspector reads quadratic and cubic control points from final settled commands and emits ordinary Path handles and Node markers; `labels` adds control-point identifiers. Selection never enters Core IR and does not change primary bounds, resources, hit testing, or animation. See [`@retikz/inspect`](/kernel/packages/inspect) for registry and host wiring.

<ComponentPreview files="path-inspector" size="sm" />

#### Whole-path transform (rotate / scale)

`rotate` (degrees) / `scale` (a number for uniform, or `{ x, y }` for non-uniform) transform the whole path around its **bounding-box center**, sparing a wrapping `<Scope transforms>` for a single path. The order is a hard contract: path endpoints resolve first, arrow shrink is computed on the **untransformed** geometry, then everything is wrapped in a group — so arrow direction follows the transform correctly without misalignment. The dotted path preserves the original geometry while the panel's rotation and two-axis scale affect only the solid path for direct comparison.

<ComponentPreview files="path-transform" size="sm" />

#### Mid-path marks

`marks` places directional marks along the path: each `{ pos, mark }` has `pos ∈ [0, 1]` (out-of-range rejected by the schema), and `mark` is initially `{ kind: 'arrow', shape, ... }` (a registered arrow name + visual subset, see [Custom Arrows](/kernel/components/draw/custom-arrow)), **oriented by the tangent at that point** (not a fixed direction). Lines, Béziers, and arcs are all tangent-oriented. Their size **scales with `strokeWidth`**, just like endpoint arrows: the thicker the line, the larger the mark. Move both position controls to watch the marks follow the local tangent as they travel along the path.

<ComponentPreview files="path-marks" size="xs" />

#### Rounded corners (roundedCorners)

`roundedCorners` rounds every **line↔line joint** of the path with a tangent arc of the same radius — handy for flowcharts, routing, and polyline connections where you want soft corners instead of sharp ones. Omitted = sharp corners everywhere (behavior unchanged). The first / last endpoint stays sharp; a joint whose neighbor is a curve / arc / Bézier / fold stays sharp too. When the radius is too large it is **clamped per joint** to what the adjacent segment lengths allow. After a `cycle` close, the closing joint is rounded as well.

<ComponentPreview files="path-rounded-corners" size="sm" />

`roundedCorners` changes the **path geometry** — it pulls the joint vertices back and inserts a tangent arc, so it affects the bounding box, arc length, path-level `marks` normalization, and connection-point resolution. This is distinct from `lineJoin`, which only styles the **stroke render** while the path centerline stays a sharp fold. The panel shows `lineJoin` above and geometric rounding below so you can drag the radius and compare them directly. (Note: per-step edge `label`s are currently positioned along the step's own segment and are not re-fitted to the shortened, rounded geometry.)

<Comparison target="tikz" title="Path Syntax Mapping">

| retikz                            | TikZ mapping                                      |
| --------------------------------- | ------------------------------------------------- |
| `lineCap` / `lineJoin`            | `line cap` / `line join`                          |
| `ultraThin` … `ultraThick`        | `ultra thin` … `ultra thick`                      |
| `outAngle` / `inAngle`            | `out=` / `in=`                                    |
| `marks` and endpoint arrows scale | Mark and endpoint-arrow sizing follows line width |
| `roundedCorners={r}`              | `rounded corners=<r>`                             |
| `color`                           | `color=`                                          |

</Comparison>

## How it works

<ComponentAlert
  type="tip"
  title="Optional for everyday use"
  description="Revisit when endpoint references, Path kinds, rounded geometry, or arrows produce an edge case."
/>

This section focuses on the local path from a `<Path>` step sequence to Scene primitives. See [Architecture Principles](/kernel/concepts/design/principles) for the full JSX → IR → Scene pipeline.

### Why endpoints can refer forward

The compiler does not emit primitives as soon as it reaches a `<Path>`. It queues the path and leaves an ordering placeholder when output stays at the current level. It continues registering `<Node>` and `<Coordinate>` ids, then resolves `Step.to`, control points, and connection boundaries after the current scope has been scanned. Ordinary scopes backfill the placeholder; see the [`<Scope>` deep dive](/kernel/components/layout/scope#how-it-works) for transformed-scope projection.

As a result, path endpoints can refer to an id declared later in the same scope without changing paint order. This is a contract of deferred path emission only: `Node.position` and scope transforms are still resolved when the compiler reaches them, so not every id reference supports forward declarations.

<SourceLinks
  sources={[
    {
      label: 'Path placeholders and pending queue',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 256,
      endLine: 281,
    },
    {
      label: 'Endpoint resolution and primitive backfill',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 147,
      endLine: 176,
    },
  ]}
/>

### How kind selects whole-path output

`kind` is not a renderer switch. It names the provider that compiles the whole Path and defaults to `stroke`. The compiler resolves a definition from the shared registry. If that provider declares an `optionsSchema`, the compiler validates `kindOptions`; otherwise it keeps the original JSON object. The provider can then reuse standard stroke emission, reuse standard ribbon emission, or return custom Scene primitives directly.

The figure stops at the Path responsibility boundary: the input remains one `<Path>`, while every provider branch returns renderer-agnostic Scene primitives.

<ComponentPreview files="path-kind-pipeline" size="sm" hideCode />

Built-in `stroke` / [`ribbon`](/kernel/components/draw/ribbon) kinds and external kinds use the same registry and collision rules; renderers do not need to understand custom kinds. See [Custom Paths](/kernel/components/draw/custom-path) to own the whole path, or [Path Generators](/kernel/components/draw/path-generator) to extend one step.

<SourceLinks
  sources={[
    {
      label: 'Path kind lookup and dispatch',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 93,
      endLine: 145,
    },
    {
      label: 'Path kind provider contract',
      path: 'packages/kernel/core/src/contract/path-kind/types.ts',
      startLine: 18,
      endLine: 50,
    },
  ]}
/>

### How stroke materializes geometry

The default stroke path first resolves relative targets, node references, and generators into path commands (`PathCommand`: materialized move / line / arc geometry). `roundedCorners` then rewrites those commands before marks and endpoint-arrow shrinking are calculated. Finally, `rotate` / `scale` wrap the main path, labels, and marks in one group. Marks therefore follow the rounded arc length and tangent, while whole-path transforms keep decorations attached.

<SourceLinks
  sources={[
    {
      label: 'Step resolution and path commands',
      path: 'packages/kernel/core/src/compile/path/stroke/emit.ts',
      startLine: 57,
      endLine: 147,
    },
    {
      label: 'Rounded geometry, marks, and arrow order',
      path: 'packages/kernel/core/src/compile/path/stroke/emit.ts',
      startLine: 149,
      endLine: 190,
    },
  ]}
/>

## API Reference

| Prop             | Type                                                | Default        | Description                                                                                                                                                                                                                                                                                                                                                         |
| ---------------- | --------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`             | `string`                                            | —              | Optional stable id: lets other elements anchor to the path and serves as the attach point for [hydration](/kernel/packages/render/hydration) event binding (a path needs an `id` before you can bind `onClick` and friends)                                                                                                                                         |
| `on<Event>`      | `PathProps['onClick']`                              | —              | Hydration events with the same signature as `onClick`, including pointer events and wheel; requires `id`. See [Hydration](/kernel/packages/render/hydration) for the complete list                                                                                                                                                                                  |
| `meta`           | `IRPathBase['meta']`                                | —              | Provenance metadata, passed through verbatim onto the Scene primitives this path emits; ignored by renderers and never part of layout / style. Typically injected by a higher-level lowering (e.g. plot) to tag which datum / series / layer it came from, so an interaction layer can trace a hit primitive back to its source. Must be a JSON-serializable object |
| `kind`           | <ApiValues name="PathKind" /> \| `string`           | `'stroke'`     | Path kind provider. Built-ins are `'stroke'` and [`'ribbon'`](/kernel/components/draw/ribbon); custom kinds are registered through the Path kind contract                                                                                                                                                                                                           |
| `kindOptions`    | `IRPathBase['kindOptions']`                         | —              | JSON options passed to a custom Path kind provider. Built-in `stroke` / `ribbon` do not use it                                                                                                                                                                                                                                                                      |
| `color`          | `string`                                            | —              | Master color; `stroke` / arrow / step label default to it unless individually set (follows the master color, not `stroke`)                                                                                                                                                                                                                                          |
| `stroke`         | `string` \| `IRPaintSpec`                           | `currentColor` | Stroke paint: CSS color or IRPaintSpec (gradient / pattern / image); arrows that inherit stroke need an explicit solid arrow color                                                                                                                                                                                                                                  |
| `strokeWidth`    | `number`                                            | `1`            | Stroke width                                                                                                                                                                                                                                                                                                                                                        |
| `dashPattern`    | `Array<number>`                                     | —              | Dash/gap lengths in user units (e.g. `[4, 2]`)                                                                                                                                                                                                                                                                                                                      |
| `dashOffset`     | `number`                                            | —              | Dash start offset in path user units                                                                                                                                                                                                                                                                                                                                |
| `lineCap`        | <ApiValues name="PathLineCap" />                    | `'butt'`       | Stroke endpoint shape                                                                                                                                                                                                                                                                                                                                               |
| `lineJoin`       | <ApiValues name="PathLineJoin" />                   | `'miter'`      | Stroke corner shape (visual only, geometry unchanged)                                                                                                                                                                                                                                                                                                               |
| `roundedCorners` | `number` (≥0)                                       | —              | Geometric corner radius: rounds every line↔line joint with a tangent arc, changing the path geometry (distinct from `lineJoin`); non-line joints stay sharp, clamped per joint to the segment lengths; omitted = sharp corners                                                                                                                                      |
| `thickness`      | <ApiValues name="PathThickness" />                  | —              | Component / builder sugar resolved to `strokeWidth` before IR is produced; explicit `strokeWidth` always wins                                                                                                                                                                                                                                                       |
| `arrow`          | `'none'` \| `'->'` \| `'<-'` \| `'<->'`             | —              | Path-level arrow direction (see [Arrow](/kernel/components/draw/arrow))                                                                                                                                                                                                                                                                                             |
| `arrowDetail`    | `IRArrowDetail`                                     | —              | Arrow visual config; fields documented on [Arrow](/kernel/components/draw/arrow)                                                                                                                                                                                                                                                                                    |
| `fill`           | `string` \| `IRPaintSpec`                           | `'none'`       | Fill of the closed region: any CSS color, or IRPaintSpec (gradient / pattern / image)                                                                                                                                                                                                                                                                               |
| `fillRule`       | <ApiValues name="PathFillRule" />                   | `'nonzero'`    | Fill rule; use `'evenodd'` for rings / holes                                                                                                                                                                                                                                                                                                                        |
| `opacity`        | `number`                                            | —              | Whole-path opacity 0..1 (multiplies onto stroke and fill)                                                                                                                                                                                                                                                                                                           |
| `fillOpacity`    | `number`                                            | —              | Fill-only opacity 0..1                                                                                                                                                                                                                                                                                                                                              |
| `strokeOpacity`  | `number`                                            | —              | Stroke-only opacity 0..1                                                                                                                                                                                                                                                                                                                                            |
| `shadow`         | <ApiValues name="ShadowPreset" /> \| `IRDropShadow` | —              | Drop shadow on the main path: preset string or object; element-level, endpoint arrows cast a shadow together with the main path. See [Visual Effects](/kernel/components/effects)                                                                                                                                                                                   |
| `blendMode`      | <ApiValues name="BlendMode" />                      | `'normal'`     | How the main path blends with content beneath it; element-level, endpoint arrows blend with the main path. See [Visual Effects](/kernel/components/effects)                                                                                                                                                                                                         |
| `zIndex`         | `number` (integer)                                  | `0`            | Explicit stacking order: higher draws on top; ties keep declaration order; effective only among siblings at the same level                                                                                                                                                                                                                                          |
| `rotate`         | `number`                                            | —              | Whole-path rotation in degrees (around the bounding-box center)                                                                                                                                                                                                                                                                                                     |
| `scale`          | `IRPathScale`                                       | —              | Whole-path scale (number = uniform / `{ x, y }` = non-uniform, around the bounding-box center)                                                                                                                                                                                                                                                                      |
| `marks`          | `IRPathBase['marks']`                               | —              | Marks along the path: `pos∈[0,1]`, `mark.kind:'arrow'` + a registered arrow name, oriented by the tangent at that point                                                                                                                                                                                                                                             |
| `animations`     | `IRPathBase['animations']`                          | —              | Timeline animation tracks, passed through onto the primitives this path emits; renderers play them or fall back to static. See [Animation](/kernel/components/effects/animation)                                                                                                                                                                                    |
| `children`       | `PathProps['children']`                             | —              | Required by the default `stroke` kind. If the first isn't `move`, the compiler coerces it to `move`. See [Ribbon](/kernel/components/draw/ribbon) for centerline and boundary rules                                                                                                                                                                                 |

## Related

- [Draw](/kernel/components/draw/overview): Sugar preferred for everyday connections
- [Step](/kernel/components/draw/step): Complete field and kind reference for `<Path>` children
- [Ribbon](/kernel/components/draw/ribbon): Variable-width relations and precomputed boundaries
- [Arrow](/kernel/components/draw/arrow): Detailed path arrow configuration
- [Custom Paths](/kernel/components/draw/custom-path): Register a kind provider that owns the whole Path
