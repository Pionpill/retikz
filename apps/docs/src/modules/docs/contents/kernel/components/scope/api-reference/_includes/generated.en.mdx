{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/react`

### ScopeProps

Cascading styles (graphic state and four element-default channels) are shared through ScopeStyleProps with `<Layout>`; this type adds Scope-specific container, namespace, transform, barrier, stacking, and clipping fields

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member            | Type                                    | Default | Description                                                                                                                                                                                                                                                                              |
| ----------------- | --------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `animations?`     | `IRScope['animations']`                 | —       | Timeline animations for the whole Scope; rendered as animation or a static fallback, without participating in layout or propagating to children                                                                                                                                          |
| `authoring?`      | `unknown`                               | —       | Optional runtime-only authoring payload interpreted by a compile driver; excluded from Core IR                                                                                                                                                                                           |
| `boundingShape?`  | `IRScope['boundingShape']`              | —       | Synthetic reference-envelope shape for the Scope id: the closed enum 'rectangle' \| 'circle', rather than an open Node shape reference; defaults to 'rectangle' (axis-aligned bounding box)                                                                                              |
| `children?`       | `ReactNode`                             | —       | Scope children: nested Node, Path, Coordinate, or Scope                                                                                                                                                                                                                                  |
| `clip?`           | `IRScope['clip']`                       | —       | Clip region in Scope-local coordinates (rect, circle, ellipse, polygon, path, compound, or custom); clips all children when supplied                                                                                                                                                     |
| `defaults?`       | `ScopeStyleProps['defaults']`           | —       | Default styles for descendant Nodes, Paths, Labels, and Arrows; explicit element values win and `reset` blocks selected outer channels                                                                                                                                                   |
| `id?`             | `string`                                | —       | Optional Scope reference id; makes the group envelope available as a reference target<br />External paths and positions can reference these bounds using `scope.id`, `scope.id.<anchor>`, or `scope.id.<deg>`; this external handle is unaffected by `localNamespace`                    |
| `localNamespace?` | `boolean`                               | —       | Whether to create a local namespace; when true, child ids do not propagate to the parent frame and are not externally visible<br />Child IDs can only be referenced inside this Scope; they are inaccessible outside it, while `scope.id` itself remains accessible from the outer scope |
| `meta?`           | `IRScope['meta']`                       | —       | JSON-serializable user metadata available in event and hydration contexts; does not participate in layout or propagate to children                                                                                                                                                       |
| `onClick?`        | `HydrationEventProps['onClick']`        | —       | Click on this element (DOM `click`)                                                                                                                                                                                                                                                      |
| `onDoubleClick?`  | `HydrationEventProps['onDoubleClick']`  | —       | Double-click on this element (DOM `dblclick`)                                                                                                                                                                                                                                            |
| `onPointerDown?`  | `HydrationEventProps['onPointerDown']`  | —       | Pointer pressed on this element (DOM `pointerdown`)                                                                                                                                                                                                                                      |
| `onPointerEnter?` | `HydrationEventProps['onPointerEnter']` | —       | Pointer enters this element, synthesized from `pointermove` and hit-ID state; moving between children does not trigger it again                                                                                                                                                          |
| `onPointerLeave?` | `HydrationEventProps['onPointerLeave']` | —       | Pointer leaves this element, synthesized from `pointermove` and hit-ID state                                                                                                                                                                                                             |
| `onPointerMove?`  | `HydrationEventProps['onPointerMove']`  | —       | Pointer moves over this element (DOM `pointermove`)                                                                                                                                                                                                                                      |
| `onPointerUp?`    | `HydrationEventProps['onPointerUp']`    | —       | Pointer released on this element (DOM `pointerup`)                                                                                                                                                                                                                                       |
| `onRightClick?`   | `HydrationEventProps['onRightClick']`   | —       | Context menu on this element (DOM `contextmenu`); the browser menu is not suppressed by default, so the handler must call `event.preventDefault()` if needed                                                                                                                             |
| `onWheel?`        | `HydrationEventProps['onWheel']`        | —       | Wheel input over this element (DOM `wheel`)                                                                                                                                                                                                                                              |
| `placement?`      | `IRScope['placement']`                  | —       | Final anchor-aligned Scope placement<br />The target is an explicit point in parent coordinates or a previously completed named entity; selfAnchor defaults to the center of the intrinsic bounds                                                                                        |
| `style?`          | `ScopeStyleProps['style']`              | —       | Scope-level visual overrides cascade declared fields to descendant Composites                                                                                                                                                                                                            |
| `theme?`          | `IRScope['theme']`                      | —       | Local Theme overriding only declared fields, inherited by descendant Composites                                                                                                                                                                                                          |
| `transforms?`     | `Array<InputTransform>`                 | —       | Local transform list following SVG transform-list order; the last item acts on a local point first<br />Supports translate, polar-translate, at-translate, offset-translate, between-translate, rotate, and scale                                                                        |
| `zIndex?`         | `IRScope['zIndex']`                     | —       | Explicit stacking order of the whole Scope among siblings, without changing child order; defaults to 0, preserving declaration order                                                                                                                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ScopeProps = ScopeStyleProps &
  HydrationEventProps & {
    theme?: IRScope['theme'];
    id?: string;
    localNamespace?: boolean;
    transforms?: Array<InputTransform>;
    placement?: IRScope['placement'];
    zIndex?: IRScope['zIndex'];
    clip?: IRScope['clip'];
    boundingShape?: IRScope['boundingShape'];
    meta?: IRScope['meta'];
    animations?: IRScope['animations'];
    authoring?: unknown;
    children?: ReactNode;
  };
```

</DocTab>

</DocTabs>

### ScopeStyleProps

<p>
  <ApiSourceLink
    label={'ScopeStyleProps'}
    path={'packages/kernel/react/src/kernel/protocol/scope-style.ts'}
    startLine={4}
  >
    Cascading visual overrides and child-default channels for Scope; Layout carries them through rootScope
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                     | Default | Description                                                                                                                            |
| ----------- | ------------------------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `defaults?` | `InputScope['defaults']` | —       | Default styles for descendant Nodes, Paths, Labels, and Arrows; explicit element values win and `reset` blocks selected outer channels |
| `style?`    | `InputScope['style']`    | —       | Scope-level visual overrides cascade declared fields to descendant Composites                                                          |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ScopeStyleProps = {
  style?: InputScope['style'];
  defaults?: InputScope['defaults'];
};
```

</DocTab>

</DocTabs>

### Scope

<p>
  <ApiSourceLink label={'Scope'} path={'packages/kernel/react/src/kernel/components/Scope.tsx'} startLine={57}>
    Scope container, equivalent to TikZ `\begin{scope}[...]...\end{scope}`
  </ApiSourceLink>
</p>

Provides local styles, namespaces, transforms, final anchor placement, clipping, and reference bounds for a group of nodes and paths

```ts
export const Scope = FC<ScopeProps>;
```
