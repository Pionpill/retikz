---
title: Custom Patterns
description: Define a repeating fill with PatternDefinition, inject it through Layout.patterns, and reference it by name from fill.
---

Register a custom repeating pattern when the built-in lines, dots, and grid cannot express the texture you need. The `cross` definition below draws one cross in each tile and then works like any other `fill`.

<ComponentPreview files="custom-pattern-basic" size="xs" />

## Boundaries

Keep a [built-in pattern](/kernel/components/effects/pattern) when you only need to change density, color, background, line width, or rotation. Create a `PatternDefinition` only when the tile geometry itself must change.

Primitive data stores only serializable configuration such as `{ kind: 'pattern', shape: 'cross' }`. The `emit` function stays in the host-injected definition and never enters the IR (intermediate representation).

## Define the capability

Use `definePattern()` to create a named definition. `emit` receives the resolved size, base line style, directional styles, and cyclic styles, then returns structured primitives in tile-local coordinates.

```tsx
import { definePattern } from '@retikz/core';

const cross = definePattern({
  name: 'cross',
  defaultSize: 12,
  emit: ({ size, color }) => {
    const center = size / 2;
    return [
      {
        type: 'path',
        commands: [
          { kind: 'move', to: [center, 2] },
          { kind: 'line', to: [center, size - 2] },
        ],
        stroke: color,
      },
      {
        type: 'path',
        commands: [
          { kind: 'move', to: [2, center] },
          { kind: 'line', to: [size - 2, center] },
        ],
        stroke: color,
      },
    ];
  },
});
```

Built-in and custom patterns implement the same `PatternDefinition` and enter the same registry. `definePattern()` only checks the definition shape in TypeScript; it does not put functions into primitive data.

When `emit` returns an `Iterable<MarkerPrimitive>` directly, the final tile period stays at `context.size`. If the motif needs a different complete period, return `{ tileSize, motif }`; compile validates `tileSize` and writes it into the same Scene pattern resource.

## Inject the host

Pass the definition to `<Layout patterns>`, then reference its `name` from a Node or Path `fill`:

```tsx
<Layout patterns={[cross]}>
  <Node fill={{ kind: 'pattern', shape: 'cross', color: 'darkorange' }} />
</Layout>
```

When calling `compileToScene()` directly, use `CompileOptions.patterns`. Definitions belong to the current compile host, so the same IR can receive different pattern sets in different hosts.

`pattern.size` overrides the definition's `defaultSize`. These two nodes reuse the same complete definition: the left stays at `size: 6` as a reference, while the right starts at `size: 16` and lets you tune period, rotation, color, and backdrop.

<ComponentPreview
  files="custom-pattern-size"
  size="sm"
  caption={
    <>
      The controls change only serializable pattern configuration; the copyable <code>PatternDefinition</code>, host
      injection, and usage code remain complete in the example.
    </>
  }
/>

## Execution

At compile time, built-in and custom definitions merge into one registry. The compiler looks up `shape`, calls `emit`, and writes a resolved pattern resource into the Scene. SVG and Canvas materialize that resource without seeing the definition function.

| Stage    | Input                        | Output                           |
| -------- | ---------------------------- | -------------------------------- |
| Register | Built-ins + `patterns`       | One name-indexed registry        |
| Resolve  | `shape` and instance options | Inherited, expanded emit context |
| Emit     | Resolved tile                | Scene pattern resource           |
| Render   | Scene pattern resource       | SVG or Canvas pattern            |

Built-in and custom patterns therefore share name conflicts, lookup, validation, and rendering semantics.

<SourceLinks
  sources={[
    {
      label: 'Merge built-in and custom patterns',
      path: 'packages/kernel/core/src/providers/pattern/registry.ts',
      startLine: 6,
      endLine: 15,
    },
    {
      label: 'Resolve a tile into a Scene resource',
      path: 'packages/kernel/core/src/compile/resource/paint.ts',
      startLine: 39,
      endLine: 102,
    },
    {
      label: 'Pattern emit context and output contract',
      path: 'packages/kernel/core/src/contract/pattern/types.ts',
      startLine: 32,
      endLine: 86,
    },
    {
      label: 'Materialize an SVG pattern resource',
      path: 'packages/kernel/render/src/svg/builders/paint-defs.ts',
      startLine: 113,
      endLine: 126,
    },
    {
      label: 'Materialize a Canvas pattern resource',
      path: 'packages/kernel/render/src/canvas/draw-scene.ts',
      startLine: 627,
      endLine: 646,
    },
  ]}
/>

<Comparison target="tikz" title="Custom patterns">

TikZ declares patterns at the PGF layer. retikz represents the same extension with `PatternDefinition`, compiling each tile into a structured Scene resource that SVG and Canvas can share.

</Comparison>

## Errors and limits

| Case                                  | Result                           | Fix                                  |
| ------------------------------------- | -------------------------------- | ------------------------------------ |
| Empty or duplicate name               | Registration throws              | Use a non-empty unique `name`        |
| Unregistered `shape`                  | Compile lists available names    | Inject or correct the definition     |
| Invalid `size` / `lineWidth`          | Compile throws                   | Pass a finite positive number        |
| Non-finite `rotation`                 | Compile throws                   | Pass a finite angle                  |
| `emit` throws                         | Compile reports the pattern name | Fix the definition logic             |
| Non-positive or non-finite `tileSize` | Compile validation fails         | Return a finite positive period      |
| Invalid primitive or coordinate       | Compile validation fails         | Return supported structured geometry |

Tile output accepts only JSON-safe structured geometry. Do not return functions, DOM nodes, text, external resource references, or non-finite coordinates.

## API Reference

### PatternDefinition

| Prop          | Type                         | Default | Description                                   |
| ------------- | ---------------------------- | ------- | --------------------------------------------- |
| `name`        | `string`                     | —       | Registry name used by `pattern.shape`         |
| `defaultSize` | `number?`                    | `8`     | Base size used when `pattern.size` is omitted |
| `emit`        | `(ctx) => PatternEmitOutput` | —       | Produces tile-local geometry                  |

### PatternEmitContext

| Prop              | Type                              | Default        | Description                                    |
| ----------------- | --------------------------------- | -------------- | ---------------------------------------------- |
| `size`            | `number`                          | resolved       | Base size resolved from instance or definition |
| `color`           | `string`                          | `currentColor` | Primary motif color                            |
| `background`      | `string?`                         | —              | Omitted means a transparent tile               |
| `lineWidth`       | `number?`                         | —              | Omitted lets the motif choose                  |
| `dashPattern`     | `Array<number>?`                  | —              | Resolved stroke dash rhythm                    |
| `dashOffset`      | `number?`                         | —              | Starting stroke dash offset                    |
| `lineCap`         | <ApiValues name="PathLineCap" />  | —              | Stroke endpoint style                          |
| `lineJoin`        | <ApiValues name="PathLineJoin" /> | —              | Stroke corner style                            |
| `horizontalStyle` | `ResolvedPatternLineStyle?`       | —              | Horizontal style with base fields inherited    |
| `verticalStyle`   | `ResolvedPatternLineStyle?`       | —              | Vertical style with base fields inherited      |
| `lineStyleCycle`  | `ResolvedPatternLineStyleCycle?`  | —              | Complete style array expanded by index         |
| `round`           | `(n) => number`                   | —              | Applies compile precision                      |

`dashPattern`, directional styles, and cycle styles already reflect field inheritance and the `dashPattern > dashed > dotted` priority. Custom definitions can map them directly to `MarkerPathPrim` without parsing presets again. `lineStyleCycle.styles` always has the same length as `period`.

### PatternEmitResult

| Prop       | Type                        | Default | Description                             |
| ---------- | --------------------------- | ------- | --------------------------------------- |
| `tileSize` | `number`                    | —       | Final finite positive tile period       |
| `motif`    | `Iterable<MarkerPrimitive>` | —       | Structured geometry in tile coordinates |

`PatternEmitOutput` is the union of a plain motif iterable and `PatternEmitResult`. Return the result object only when the motif's complete period differs from `context.size`.

## Related

- [Patterns](/kernel/components/effects/pattern): built-in lines, dots, grids, and instance options
- [Effects](/kernel/components/effects): return to the family and reading order
- [Node](/kernel/components/node/overview): use patterns as node fills
- [Custom Arrows](/kernel/components/draw/custom-arrow): another extension returning structured `MarkerPrimitive`s
- [Extension Runtime](/kernel/reference/runtime/extensions): compare registry ownership across extension types
