---
title: Custom Arrows
description: Register new arrow-tip geometry so Draw and Path endpoints and path marks resolve one name, shorten the path correctly, and inherit stroke color.
---

A custom arrow extends **endpoint marker geometry**. IR stores only an open shape name and visual parameters. After the host injects an `ArrowDefinition`, core resolves it from the shared registry and emits renderer-neutral `MarkerPrimitive` geometry during compilation.

The example below registers a hollow `bracket` arrow through `<Layout arrows>`. Its definition, injection, and Draw usage live in one runnable file.

<ComponentPreview files="custom-arrow-basic" size="xs" />

## Applicability

Core provides the `normal`, `open`, `stealth`, `openStealth`, `circle`, and `openCircle` basic arrows by default. The remaining official diamond pair is available on demand from [Arrow Extensions](/library/standard/extension/arrow). Register a definition only when neither the Core nor the assembled Standard outline fits.

| Need                                        | Choose                                              |
| ------------------------------------------- | --------------------------------------------------- |
| Resize or recolor an existing arrow         | `arrowDetail`                                       |
| Use different registered shapes at each end | `arrowDetail.start` / `end`                         |
| Add a new endpoint outline                  | Custom Arrow                                        |
| Change the output of the whole relation     | [Custom Paths](/kernel/components/draw/custom-path) |

An ArrowDefinition serves both Path endpoints and `marks[].mark.shape`. It does not own arrow direction, mark position, path stroke, or renderer instances.

## Define the capability

A minimal definition needs a stable `name`, a visual back at `backX`, the path contact at `lineContactX`, and an `emit` function that returns marker-local geometry.

```tsx
const bracket = defineArrow({
  name: 'bracket',
  hollow: true,
  backX: 2,
  lineContactX: 2,
  emit: ({ stroke, lineWidth }) => [
    {
      type: 'path',
      commands: bracketCommands,
      stroke,
      strokeWidth: lineWidth,
    },
  ],
});
```

`emit` uses `baseSize` local coordinates. `backX` promises the base axial envelope back, `lineContactX` defines where the main stroke joins the marker, and `tipX` describes the visual tip; they must satisfy `backX <= lineContactX <= tipX`. Core lets the path overlap the contact baseline by half of its own stroke width before the marker paints over the joint. Endpoint overlap combines final `length × scale` with hollow `lineWidth` / `outerInset`, so ratio `1` aligns the final visual back with the logical endpoint.

## Inject into the host

React injects definitions through `<Layout arrows>`; direct core calls use `CompileOptions.arrows`. Endpoints and path marks then reference the registered name.

```tsx
<Layout arrows={[bracket]}>
  <Draw way={['A', 'B']} arrow="->" arrowDetail={{ shape: 'bracket' }} />
</Layout>
```

The definition never enters IR. React, Vanilla, or persisted IR can reference the same shape string as long as the compile host injects the same definition.

## Execution

Core basic arrows, explicitly assembled Standard arrows, and custom definitions all enter one registry. Compilation merges path defaults, `arrowDetail`, and per-end overrides before resolving the shape. It then uses resolved size and back / contact / tip geometry to place the endpoint and join the path before calling `emit` for marker geometry.

The `fill` / `stroke` values passed to `emit` are `MarkerFill`. Without an explicit color they use `{ kind: 'contextStroke' }`, allowing one definition to follow each Path stroke instead of freezing a color at registration time.

<ComponentPreview files="custom-arrow-inherit" size="xs" />

<SourceLinks
  sources={[
    {
      label: 'Arrow definition and emit context',
      path: 'packages/kernel/core/src/contract/arrow/types.ts',
      startLine: 4,
      endLine: 61,
    },
    {
      label: 'Unified registry for Core basic and injected arrows',
      path: 'packages/kernel/core/src/providers/arrow/registry.ts',
      startLine: 6,
      endLine: 13,
    },
    {
      label: 'Arrow lookup, visual overrides, and shrink resolution',
      path: 'packages/kernel/core/src/resolve/path/provider.ts',
      startLine: 80,
      endLine: 168,
    },
    {
      label: 'contextStroke, marker validation, and materialization',
      path: 'packages/kernel/core/src/compile/path/stroke/shrink.ts',
      startLine: 18,
      endLine: 87,
    },
    {
      label: 'Path endpoint shrink application',
      path: 'packages/kernel/core/src/compile/path/stroke/shrink.ts',
      startLine: 151,
      endLine: 204,
    },
  ]}
/>

## Errors and limits

| Case                                                    | Result                                                          |
| ------------------------------------------------------- | --------------------------------------------------------------- |
| Empty name                                              | Registry rejects the non-empty key contract                     |
| Duplicate Core, Standard, or custom name                | Registry merge reports a duplicate registration                 |
| Unknown `arrowDetail.shape`                             | Compile lists available names and `options.arrows`              |
| Invalid `baseSize`, back / contact / tip, or dimensions | Compile rejects invalid marker geometry                         |
| Unknown primitive or non-finite geometry                | Marker validation prevents invalid Scene output                 |
| Recursive arrows, clips, or text in marker              | The narrow `MarkerPrimitive` contract rejects them              |
| Path uses an IRPaint stroke                             | Marker cannot inherit a resource paint; set a solid arrow color |

MarkerPrimitive allows only `path`, `ellipse`, `rect`, and groups containing the same subset. Do not return SVG `<marker>` elements, Canvas objects, text layout, or external paint resource references.

## API Reference

### ArrowDefinition

| Field           | Type                                 | Default                           | Description                                     |
| --------------- | ------------------------------------ | --------------------------------- | ----------------------------------------------- |
| `name`          | `string`                             | —                                 | Registry key and shape reference                |
| `backX`         | `number`                             | —                                 | Axial envelope back of the marker base geometry |
| `lineContactX`  | `number`                             | —                                 | Path contact point used for body shortening     |
| `emit`          | `(ctx) => Iterable<MarkerPrimitive>` | —                                 | Generates marker-local geometry                 |
| `baseSize`      | `number`                             | `10`                              | Marker-local reference size                     |
| `tipX`          | `number`                             | `baseSize`                        | Visual tip x                                    |
| `hollow`        | `boolean`                            | `false`                           | Uses stroke for a hollow outline                |
| `defaultLength` | `number`                             | `8`                               | Fallback arrow length                           |
| `defaultWidth`  | `number`                             | `8`                               | Fallback arrow width                            |
| `outerInset`    | `number`                             | Hollow `lineWidth / 2`, solid `0` | Outer-outline compensation at node boundaries   |

### Emit context

| Field       | Type                 | Default | Description                                 |
| ----------- | -------------------- | ------- | ------------------------------------------- |
| `fill`      | `MarkerFill`         | —       | Solid-arrow fill; may be `contextStroke`    |
| `stroke`    | `MarkerFill`         | —       | Hollow-arrow stroke; may be `contextStroke` |
| `lineWidth` | `number`             | —       | Marker-local stroke width                   |
| `round`     | `(number) => number` | —       | Compile/render precision rounding function  |

## Related

- [Arrow](/kernel/components/draw/arrow): direction, sizing, color, and per-end overrides
- [Path](/kernel/components/draw/path): host for endpoint arrows and path marks
- [Custom Paths](/kernel/components/draw/custom-path): owns whole-Path output
- [Runtime extensions](/kernel/reference/runtime/extensions): cross-capability definition and registry overview
