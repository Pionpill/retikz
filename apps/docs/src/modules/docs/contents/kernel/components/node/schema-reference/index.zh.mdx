---
title: Schema 参考
description: 查询节点、标签、坐标点与连接面引用的输入契约
---

{/* Generated by pnpm generate:node-schema. Do not edit manually. */}

本页仅收录节点家族拥有的输入 Schema，来自 `@retikz/core`。共享的位置、形状、文字、字体、颜色和几何 Schema 只保留引用名称，不递归展开。React 的 Text 是行级语法糖，没有独立持久化 Schema。首次使用见[基础用法](/kernel/components/node/usage)。

Node 的 style / layout 和标签配置按需提供；字段省略时继续参与继承与编译默认值解析，不代表已写入固定值。显式字段覆盖继承值；label 的实际距离还取决于编译配置 labelDistance。Schema 表中的默认值只展示源码声明的默认。

## NodeSchema

<ZodSchema
  name="NodeSchema"
  description="带位置、可选文字和视觉形状的节点"
  descriptions={{
    type: '判别字段，固定为 node',
    id: '可选引用标识，路径或其它节点通过它引用本节点',
    aliasIds: '同一节点的额外引用 id；要求主 id，别名非空白、互不重复且不同于主 id，不生成额外图元',
    shape:
      '视觉形状名称或带参数引用；省略时继承 Scope.defaults.node.shape，无继承值时使用 rectangle，未注册名称在编译时失败',
    boundary: '连线使用的连接面；可由单个路径端点覆盖，不改变节点外观',
    position: '笛卡尔、极坐标、相对、偏移、比例或锚点对齐位置；非笛卡尔形式在编译时求值',
    rotate: '绕节点中心的旋转角度，正值顺时针',
    text: '正文字符串或行数组，支持行级样式及文字与公式混排；公式需要 lowerTex',
    cornerRadius: '仅 rectangle 使用的顶层圆角半径，单位为用户单位',
    scale: '均匀或分轴缩放；对象的 x、y 优先于 default，影响路径附着位置',
    label: '附着于节点的一个或多个标签',
    zIndex: '同层元素栈序，大者在上，同值保持声明顺序',
    style: '逐字段覆盖继承默认值的节点视觉配置',
    layout: '节点尺寸、间距与文字排布配置',
    meta: '透传至输出图元的 JSON 元数据，不参与编译计算',
    animations: '元素动画轨道，透传至输出图元，不参与布局且不跨 Scope 继承',
  }}
/>

## NodeStyleSchema

<ZodSchema
  name="NodeStyleSchema"
  description="节点视觉覆盖；每个字段独立覆盖继承值"
  descriptions={{
    color: '图元主色，未单独覆盖的描边、填充和标签可由它派生',
    fill: '填充：精确颜色、主色权重或 Paint',
    stroke: '描边：精确颜色、主色权重或 Paint',
    opacity: '整个图元的不透明度',
    fillOpacity: '填充不透明度',
    strokeOpacity: '描边不透明度',
    shadow: '投影预设或投影配置，不影响节点形状尺寸',
    blendMode: '图元与背景的混合模式',
    strokeWidth: '边框宽度，单位为用户单位；省略时默认 1',
    dashed: '虚线预设，被显式 dashPattern 覆盖',
    dotted: '点线预设，被 dashPattern 或 dashed 覆盖',
    dashPattern: '显式虚线段与间隔长度，优先于 dashed 和 dotted',
    dashOffset: '虚线起点偏移，允许正数、零或负数',
    textColor: '文字颜色或主色权重；contrast 根据静态填充选择黑白，缺省为 currentColor',
    font: '正文字体覆盖，缺省字段沿用文字默认值',
  }}
/>

## NodeLayoutSchema

<ZodSchema
  name="NodeLayoutSchema"
  description="节点的尺寸、内外间距和文字排布"
  descriptions={{
    align: '多行文字块内的水平对齐，缺省为 middle',
    lineHeight: '行高，单位为用户单位；省略时使用字号的 1.2 倍',
    maxTextWidth: '自动折行阈值，单位为用户单位；省略时不自动折行',
    width: '缩放前可见边框的精确宽度，不含 margin，不能小于有效最小宽度',
    minimumSize: '可见边框最小尺寸；数字同时指定宽高，对象的 width、height 优先于 default',
    padding: '正文到边框的内间距；数字应用于各边，对象按单边、轴、default 的顺序取值',
    margin: '连接面外间距；数字应用于各边，对象按单边、轴、default 的顺序取值',
  }}
/>

## NodeLabelSchema

<ZodSchema
  name="NodeLabelSchema"
  description="附着在节点边界或中心的额外文字，Node.label 可使用数组添加多个标签"
  descriptions={{
    text: '标签文字或混排行',
    textColor: '标签文字颜色，缺省沿继承链求值',
    opacity: '标签自身不透明度，与节点不透明度相乘',
    font: '标签字体覆盖，缺省字段继承节点字体',
    align: '标签视觉框沿附着点切线的对齐方式，缺省 middle',
    position: '命名方向、center、角度或边界比例位置，缺省 top',
    placement: '标签位于附着点的外侧或内侧，缺省 outside',
    distance: '节点边框到旋转后标签视觉框的净距；省略时使用编译配置 labelDistance',
    rotate: '标签自身旋转：none、radial、tangent 或角度；边界比例位置以所选边的外法向为准',
    keepUpright: '为 true 时将倒置文字翻转 180 度；缺省 false',
    pin: '外侧标签引线；true 使用默认值，对象覆盖引线样式，不能与 inside 同用',
  }}
/>

## NodeLabelBoundaryPositionSchema

<ZodSchema
  name="NodeLabelBoundaryPositionSchema"
  description="标签在盒状节点某条边界上的附着位置"
  descriptions={{
    boundary: '用于附着标签的标准边界方向',
    fraction: '沿所选边的 0–1 比例位置，缺省 0.5',
  }}
/>

## NodeLabelPinSchema

<ZodSchema
  name="NodeLabelPinSchema"
  description="外侧节点标签的引线样式"
  descriptions={{
    stroke: '引线颜色或由标签主色派生的权重',
    strokeWidth: '引线宽度，单位为用户单位；默认 1',
    dashPattern: '引线虚线段与间隔长度',
    dashOffset: '虚线起点偏移，允许正数、零或负数',
  }}
/>

## CoordinateSchema

<ZodSchema
  name="CoordinateSchema"
  description="不绘制可见内容的命名位置，可作为路径端点或相对定位参照"
  descriptions={{
    type: '判别字段，固定为 coordinate',
    id: '必填引用标识，供路径或节点定位使用',
    position: '笛卡尔、极坐标、相对、偏移或两点比例位置；不支持自身锚点对齐',
  }}
/>

## BoundarySchema

<ZodSchema
  name="BoundarySchema"
  description="节点或路径端点使用的连接面引用；字符串名称或带 JSON 参数的引用"
  descriptions={{}}
/>
