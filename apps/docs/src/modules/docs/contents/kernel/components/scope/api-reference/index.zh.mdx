---
title: API 参考
description: 查询 Scope 的 React 属性、Vanilla 输入与 Core 分组契约
---

本页收录 React 的 `Scope` 与属性、Vanilla 的 `scope / InputScope`，以及 Core 的作用域、外框、定位和默认值类型。共享变换与图元类型只保留引用，不展开整个包。

首次调用见[基础用法](/kernel/components/scope/usage)，持久化字段见 [Schema 参考](/kernel/components/scope/schema-reference)。

{/* @include kernel/components/scope/api-reference/generated */}

`ScopeProps` 还组合了 `HydrationEventProps`；事件名称与调用上下文见[水合](/kernel/packages/render/hydration)。`ScopeStyleProps` 的 `style` 与 `defaults` 字段由 [Scope Schema](/kernel/components/scope/schema-reference#scope) 展开。
