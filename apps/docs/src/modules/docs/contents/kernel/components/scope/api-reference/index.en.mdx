---
title: API reference
description: Look up Scope React props, Vanilla inputs, and Core group contracts
---

This page covers React Scope and its props, Vanilla `scope / InputScope`, and Core scope, frame, placement, and default types. Shared transform and drawing-element types remain references rather than expanding the whole package.

See [Basic usage](/kernel/components/scope/usage) for integration and [Schema reference](/kernel/components/scope/schema-reference) for persisted fields.

{/* @include kernel/components/scope/api-reference/generated */}

`ScopeProps` also includes `HydrationEventProps`; see [Hydration](/kernel/packages/render/hydration) for event names and context. [Scope Schema](/kernel/components/scope/schema-reference#scope) expands the `style` and `defaults` fields of `ScopeStyleProps`.
