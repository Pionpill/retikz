---
title: API reference
description: Look up the public TypeScript contract of Scope
---

This page selects `Scope`, `ScopeProps`, and `ScopeStyleProps` from the public `@retikz/react` entry. See [Basic usage](/kernel/components/scope/usage) for first use and [Schema reference](/kernel/components/scope/schema-reference) for persisted fields.

{/* @include kernel/components/scope/api-reference/generated */}

`ScopeProps` also includes `HydrationEventProps`; see [Hydration](/kernel/packages/render/hydration) for event names and context. [Scope Schema](/kernel/components/scope/schema-reference#scope) expands the `style` and `defaults` fields of `ScopeStyleProps`.
