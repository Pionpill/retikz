{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/react`

### CoordinateProps

<p>
  <ApiSourceLink
    label={'CoordinateProps'}
    path={'packages/kernel/react/src/kernel/components/Coordinate.tsx'}
    startLine={8}
  >
    命名坐标点的 React 输入，不绘制可见内容
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员         | 类型                                                                                                      | 默认值 | 说明                                                                                                                                                                                                                   |
| ------------ | --------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `authoring?` | `unknown`                                                                                                 | —      | 可选编译驱动解释的运行时载荷，不进入 Core IR                                                                                                                                                                           |
| `id`         | `string`                                                                                                  | —      | 坐标点 id；路径端点和节点定位通过它引用此位置                                                                                                                                                                          |
| `position`   | `IRPosition \| PolarPosition \| IRAtPosition \| InputAtPosition \| IROffsetPosition \| IRBetweenPosition` | —      | 坐标点位置；不支持 Node 专属的自身锚点对齐<br />笛卡尔 `[x, y]` / 极坐标 `{ angle, radius, origin? }` / 相对定位 `{ direction, of, distance? }` / 偏移定位 `{ of, offset }` / 比例位置 `{ between: [A, B], fraction }` |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CoordinateProps = {
  authoring?: unknown;
  id: string;
  position: IRPosition | PolarPosition | IRAtPosition | InputAtPosition | IROffsetPosition | IRBetweenPosition;
};
```

</DocTab>

</DocTabs>

### NodeProps

<p>
  <ApiSourceLink label={'NodeProps'} path={'packages/kernel/react/src/kernel/components/Node.tsx'} startLine={21}>
    可定位、可连接的节点输入，组合文字、视觉形状和连接面
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员              | 类型                                                                                                                                                                       | 默认值        | 说明                                                                                                                                                                                                                                                                                                                                                                         |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `animations?`     | `IRNode['animations']`                                                                                                                                                     | —             | 元素级时间轴动画；每条 track 描述一个可动画属性，渲染端播放或降级为静态，不参与布局                                                                                                                                                                                                                                                                                          |
| `authoring?`      | `InputNode['authoring']`                                                                                                                                                   | —             | 可选编译驱动解释的运行时载荷，不进入 Core IR                                                                                                                                                                                                                                                                                                                                 |
| `boundary?`       | `IRBoundary`                                                                                                                                                               | `'shape'`     | 连线接触的边界；省略时沿用视觉形状，也可选择已注册的连接面或形状，不改变节点外观                                                                                                                                                                                                                                                                                             |
| `children?`       | `ReactNode`                                                                                                                                                                | —             | children 内容：文本<br />与 `text` 二选一、`text` 优先；支持字符串内嵌 `\\n` / 模板字面量 / 字符串数组 / 混 `<Text>` 带样式行。<br />字符串里可写行内公式 `$...$`（inline）/ `$$...$$`（display），编译期在注入 `<Layout lowerTex>` 时解析；未注入则字面渲染                                                                                                                 |
| `cornerRadius?`   | `number`                                                                                                                                                                   | —             | 圆角半径（用户单位）；只对 `rectangle` shape 生效。建议用形状 params 形式 `shape={{ type: 'rectangle', params: { cornerRadius } }}`                                                                                                                                                                                                                                          |
| `id?`             | `string`                                                                                                                                                                   | —             | 节点 id；其他 Path/Draw 通过这个 id 引用本节点                                                                                                                                                                                                                                                                                                                               |
| `label?`          | `InputNodeLabel \| Array<InputNodeLabel>`                                                                                                                                  | —             | 节点附加标签，支持单对象或数组<br />标签可附着到命名方向、中心、角度或边界比例位置；支持内外侧摆放、旋转、切向对齐和外侧引线，不参与节点形状尺寸计算。缺省位置为 top，间距继承编译配置 labelDistance                                                                                                                                                                         |
| `layout?`         | `InputNode['layout']`                                                                                                                                                      | —             | 节点尺寸、间距与文本布局                                                                                                                                                                                                                                                                                                                                                     |
| `meta?`           | `IRNode['meta']`                                                                                                                                                           | —             | 用户自定义元数据；可在事件 / 水合上下文中读取，不参与布局。须为 JSON 可序列化对象                                                                                                                                                                                                                                                                                            |
| `onClick?`        | `HydrationHandler`                                                                                                                                                         | —             | 单击该图元（DOM `click`）                                                                                                                                                                                                                                                                                                                                                    |
| `onDoubleClick?`  | `HydrationHandler`                                                                                                                                                         | —             | 双击该图元（DOM `dblclick`）                                                                                                                                                                                                                                                                                                                                                 |
| `onPointerDown?`  | `HydrationHandler`                                                                                                                                                         | —             | 指针在该图元上按下（DOM `pointerdown`）                                                                                                                                                                                                                                                                                                                                      |
| `onPointerEnter?` | `HydrationHandler`                                                                                                                                                         | —             | 指针进入该图元（由 `pointermove` + 命中 id 状态机合成，跨子元素不重复触发）                                                                                                                                                                                                                                                                                                  |
| `onPointerLeave?` | `HydrationHandler`                                                                                                                                                         | —             | 指针离开该图元（由 `pointermove` + 命中 id 状态机合成）                                                                                                                                                                                                                                                                                                                      |
| `onPointerMove?`  | `HydrationHandler`                                                                                                                                                         | —             | 指针在该图元上移动（DOM `pointermove`）                                                                                                                                                                                                                                                                                                                                      |
| `onPointerUp?`    | `HydrationHandler`                                                                                                                                                         | —             | 指针在该图元上抬起（DOM `pointerup`）                                                                                                                                                                                                                                                                                                                                        |
| `onRightClick?`   | `HydrationHandler`                                                                                                                                                         | —             | 右键该图元（DOM `contextmenu`）；默认不抑制浏览器菜单，handler 自行 `event.preventDefault()`                                                                                                                                                                                                                                                                                 |
| `onWheel?`        | `HydrationHandler`                                                                                                                                                         | —             | 在该图元上滚轮（DOM `wheel`）                                                                                                                                                                                                                                                                                                                                                |
| `position`        | `\| IRPosition`<br />`\| PolarPosition`<br />`\| IRAtPosition`<br />`\| InputAtPosition`<br />`\| IROffsetPosition`<br />`\| IRBetweenPosition`<br />`\| IRAnchorPosition` | —             | 节点中心位置<br />六种形态：笛卡尔 `[x, y]` / 极坐标 `{ angle, radius, origin? }` / 相对定位 `{ direction, of, distance? }` / 偏移定位 `{ of, offset }` / 比例位置 `{ between: [A, B], fraction }` / 锚点对齐 `{ kind: 'anchor', target, selfAnchor? }`。锚点对齐会先完成当前 Node 的文本、shape、padding、margin、scale、rotate 布局，再整体平移；双方 anchor 缺省为 center |
| `rotate?`         | `number`                                                                                                                                                                   | `0`           | 绕节点中心旋转的角度，单位为度；正值顺时针                                                                                                                                                                                                                                                                                                                                   |
| `scale?`          | `number \| IRAxisScale`                                                                                                                                                    | `1`           | 均匀或分轴缩放；影响节点尺寸、字号、间距和路径附着点                                                                                                                                                                                                                                                                                                                         |
| `shape?`          | `IRNode['shape']`                                                                                                                                                          | `'rectangle'` | 视觉形状：无必填参数时可写名称，带参形状使用 `{ type, params }`；可选形状通过 Layout.extensions.shapes 注册                                                                                                                                                                                                                                                                  |
| `style?`          | `InputNode['style']`                                                                                                                                                       | —             | 实例视觉覆盖，逐字段覆盖继承默认值                                                                                                                                                                                                                                                                                                                                           |
| `text?`           | `string \| Array<IRLine>`                                                                                                                                                  | —             | 显式 text，优先级高于 children<br />`string` 单行（可含 `$...$` 公式）/ `Array<string \| IRLine>` 多行可对单行覆盖 fill / opacity / font，<br />或行内混排 `{ runs: [{ text }, { tex }] }`（每 run 可单独着色）                                                                                                                                                              |
| `zIndex?`         | `IRNode['zIndex']`                                                                                                                                                         | `0`           | 同层元素的栈序；大者在上，同值保持声明顺序                                                                                                                                                                                                                                                                                                                                   |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type NodeProps = HydrationEventProps & {
  authoring?: InputNode['authoring'];
  style?: InputNode['style'];
  layout?: InputNode['layout'];
  id?: string;
  shape?: IRNode['shape'];
  boundary?: IRBoundary;
  meta?: IRNode['meta'];
  animations?: IRNode['animations'];
  position:
    | IRPosition
    | PolarPosition
    | IRAtPosition
    | InputAtPosition
    | IROffsetPosition
    | IRBetweenPosition
    | IRAnchorPosition;
  rotate?: number;
  children?: ReactNode;
  text?: string | Array<IRLine>;
  cornerRadius?: number;
  scale?: number | IRAxisScale;
  label?: InputNodeLabel | Array<InputNodeLabel>;
  zIndex?: IRNode['zIndex'];
};
```

</DocTab>

</DocTabs>

### TextProps

<p>
  <ApiSourceLink label={'TextProps'} path={'packages/kernel/react/src/kernel/components/Text.tsx'} startLine={7}>
    Node 内单行文字的内容与样式覆盖
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型                                        | 默认值 | 说明                                             |
| ---------- | ------------------------------------------- | ------ | ------------------------------------------------ |
| `children` | `string \| number`                          | —      | 行内容（字符串或数字；数字按文本渲染）           |
| `fill?`    | `Extract<IRLine, { text: string }>['fill']` | —      | 行级覆盖颜色；不填走 Node 块级默认               |
| `font?`    | `IRFont`                                    | —      | 行级字体覆盖；缺省字段继承 Node 的 `font` 块级值 |
| `opacity?` | `number`                                    | —      | 行级透明度 0~1；不填走 Node 块级默认             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type TextProps = {
  children: string | number;
  fill?: Extract<
    IRLine,
    {
      text: string;
    }
  >['fill'];
  opacity?: number;
  font?: IRFont;
};
```

</DocTab>

</DocTabs>

### Coordinate

<p>
  <ApiSourceLink
    label={'Coordinate'}
    path={'packages/kernel/react/src/kernel/components/Coordinate.tsx'}
    startLine={24}
  >
    命名坐标点——TikZ `\coordinate (id) at (x, y);` 同义
  </ApiSourceLink>
</p>

命名一个可引用的点，供后续 path 与其它 node 的 `at.of` 使用；自身不渲染、不参与 viewBox 扩展

```ts
export declare const Coordinate: FC<CoordinateProps>;
```

### Node

<p>
  <ApiSourceLink label={'Node'} path={'packages/kernel/react/src/kernel/components/Node.tsx'} startLine={97}>
    Node 声明一个可引用的节点
  </ApiSourceLink>
</p>

声明一个带位置、文本、形状和样式的可引用节点；组件自身不渲染 DOM，最终由 `<Layout>` 输出到
SVG 或 Canvas

```ts
export declare const Node: FC<NodeProps>;
```

### Text

<p>
  <ApiSourceLink label={'Text'} path={'packages/kernel/react/src/kernel/components/Text.tsx'} startLine={22}>
    Text 是 Node 内的"行级"标记组件——本身不渲染
  </ApiSourceLink>
</p>

声明一行节点文本；与字符串内容按 JSX 顺序合并，字段只覆盖当前这一行

```ts
export declare const Text: FC<TextProps>;
```

## `@retikz/vanilla`

### InputCoordinate

<p>
  <ApiSourceLink
    label={'InputCoordinate'}
    path={'packages/kernel/vanilla/src/normalize/coordinate/types.ts'}
    startLine={4}
  >
    作者侧命名坐标输入
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员         | 类型                                                                                                                                                                                                                                                                                                                         | 默认值 | 说明                                         |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------- |
| `authoring?` | `unknown`                                                                                                                                                                                                                                                                                                                    | —      | 可选编译驱动解释的运行时载荷，不进入 Core IR |
| `id`         | `string`                                                                                                                                                                                                                                                                                                                     | —      | —                                            |
| `position`   | `SharedPolarPosition \| IRBetweenPosition \| [number, number] \| { direction: "top" \| "right" \| "bottom" \| "left" \| "top-right" \| "top-left" \| "bottom-right" \| "bottom-left"; of: string; distance?: number \| undefined; } \| { of: string \| SharedPolarPosition \| [number, number]; offset: [number, number]; }` | —      | —                                            |
| `type`       | `"coordinate"`                                                                                                                                                                                                                                                                                                               | —      | —                                            |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type InputCoordinate = IRCoordinate & {
  authoring?: unknown;
};
```

</DocTab>

</DocTabs>

### InputNode

<p>
  <ApiSourceLink label={'InputNode'} path={'packages/kernel/vanilla/src/normalize/node/types.ts'} startLine={47}>
    作者侧节点输入
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型                                                                                             | 默认值 | 说明                                         |
| --------------- | ------------------------------------------------------------------------------------------------ | ------ | -------------------------------------------- |
| `animations?`   | `IRNode['animations']`                                                                           | —      | —                                            |
| `authoring?`    | `unknown`                                                                                        | —      | 可选编译驱动解释的运行时载荷，不进入 Core IR |
| `boundary?`     | `IRNode['boundary']`                                                                             | —      | —                                            |
| `cornerRadius?` | `number`                                                                                         | —      | —                                            |
| `id?`           | `string`                                                                                         | —      | —                                            |
| `label?`        | `InputNodeLabel \| ReadonlyArray<InputNodeLabel>`                                                | —      | —                                            |
| `layout?`       | `IRNode['layout']`                                                                               | —      | —                                            |
| `meta?`         | `Record<string, JsonValue>`                                                                      | —      | —                                            |
| `position`      | `InputPosition`                                                                                  | —      | —                                            |
| `rotate?`       | `number`                                                                                         | —      | —                                            |
| `scale?`        | `number \| { default?: number \| undefined; x?: number \| undefined; y?: number \| undefined; }` | —      | —                                            |
| `shape?`        | `IRNode['shape']`                                                                                | —      | —                                            |
| `style?`        | `IRNode['style']`                                                                                | —      | —                                            |
| `text?`         | `IRNode['text']`                                                                                 | —      | —                                            |
| `type?`         | `'node'`                                                                                         | —      | —                                            |
| `zIndex?`       | `number`                                                                                         | —      | —                                            |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type InputNode = Omit<IRNode, 'type' | 'position' | 'label'> & {
  authoring?: unknown;
  type?: 'node';
  position: InputPosition;
  label?: InputNodeLabel | ReadonlyArray<InputNodeLabel>;
};
```

</DocTab>

</DocTabs>

### InputNodeLabel

<p>
  <ApiSourceLink label={'InputNodeLabel'} path={'packages/kernel/vanilla/src/normalize/node/types.ts'} startLine={42}>
    作者侧节点标签
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="直接属性">

其余属性继承自 `IRNodeLabel`，并移除 `'position'`。完整组合关系见“类型定义”

| 成员        | 类型                     | 默认值 | 说明 |
| ----------- | ------------------------ | ------ | ---- |
| `position?` | `InputNodeLabelPosition` | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type InputNodeLabel = Omit<IRNodeLabel, 'position'> & {
  position?: InputNodeLabelPosition;
};
```

</DocTab>

</DocTabs>

### InputNodeLabelBoundaryPosition

<p>
  <ApiSourceLink
    label={'InputNodeLabelBoundaryPosition'}
    path={'packages/kernel/vanilla/src/normalize/node/types.ts'}
    startLine={29}
  >
    作者侧节点标签边界位置
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型        | 默认值 | 说明 |
| ----------- | ----------- | ------ | ---- |
| `boundary`  | `SideValue` | —      | —    |
| `fraction?` | `number`    | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type InputNodeLabelBoundaryPosition = Omit<
  Extract<
    IRNodeLabel['position'],
    {
      boundary: string;
    }
  >,
  'boundary'
> & {
  boundary: SideValue;
};
```

</DocTab>

</DocTabs>

### InputNodeLabelPosition

<p>
  <ApiSourceLink
    label={'InputNodeLabelPosition'}
    path={'packages/kernel/vanilla/src/normalize/node/types.ts'}
    startLine={37}
  >
    作者侧节点标签位置
  </ApiSourceLink>
</p>

```ts
export type InputNodeLabelPosition =
  | Exclude<
      IRNodeLabel['position'],
      {
        boundary: string;
      }
    >
  | InputNodeLabelBoundaryPosition;
```

### coordinate

<p>
  <ApiSourceLink label={'coordinate'} path={'packages/kernel/vanilla/src/normalize/helpers.ts'} startLine={27}>
    创建作者侧命名坐标输入
  </ApiSourceLink>
</p>

```ts
export declare const coordinate: (id: string, config: Omit<InputCoordinate, 'type' | 'id'>) => InputCoordinate;
```

#### 参数

| 参数     | 类型                                    | 说明 |
| -------- | --------------------------------------- | ---- |
| `id`     | `string`                                | —    |
| `config` | `Omit<InputCoordinate, 'type' \| 'id'>` | —    |

### node

<p>
  <ApiSourceLink label={'node'} path={'packages/kernel/vanilla/src/normalize/helpers.ts'} startLine={10}>
    创建作者侧节点输入，支持 id 简写与完整配置
  </ApiSourceLink>
</p>

```ts
export declare const node: (
  idOrConfig?: string | Omit<InputNode, 'type' | 'id'>,
  maybeConfig?: Omit<InputNode, 'type' | 'id'>,
) => InputNode;
```

#### 参数

| 参数          | 类型                                        | 说明 |
| ------------- | ------------------------------------------- | ---- |
| `idOrConfig`  | `string \| Omit<InputNode, 'type' \| 'id'>` | —    |
| `maybeConfig` | `Omit<InputNode, 'type' \| 'id'>`           | —    |

## `@retikz/core`

### BoundaryAnchorName

<p>
  <ApiSourceLink
    label={'BoundaryAnchorName'}
    path={'packages/kernel/core/src/contract/boundary/types.ts'}
    startLine={13}
  >
    连接面命名 anchor 的名字
  </ApiSourceLink>
</p>

类型接受字符串；Node 引用解析仅将非中心的标准方位名交给 boundary，中心与形状专属名称由视觉 shape 解析

```ts
export type BoundaryAnchorName = AnchorValue | (string & {});
```

### BoundaryDefinition

<p>
  <ApiSourceLink
    label={'BoundaryDefinition'}
    path={'packages/kernel/core/src/contract/boundary/types.ts'}
    startLine={46}
  >
    Boundary 定义的擦除形态：registry 存这个
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型                                                                                                 | 默认值                                    | 说明                                                                    |
| --------------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------------- | ----------------------------------------------------------------------- |
| `anchor?`       | `(rect: Rect, name: BoundaryAnchorName, params: Record<string, JsonValue>) => Position \| undefined` | `不支持；调用方回退或报告不支持该 anchor` | 可选的标准方位 anchor 支持；Node 引用中的中心与形状专属名称不调用此回调 |
| `boundaryPoint` | `(rect: Rect, toward: Position, params: Record<string, JsonValue>) => Position`                      | —                                         | 从中心指向 toward 的射线与连接面的交点                                  |
| `name`          | `string`                                                                                             | —                                         | 注册表 key，由 IR `boundary` 引用                                       |
| `outline?`      | `(rect: Rect, params: Record<string, JsonValue>) => ReadonlyArray<PathCommand>`                      | —                                         | 返回与解析后 rect 同坐标系的精确闭合连接面轮廓；空数组表示合法空几何    |
| `paramsSchema`  | `ZodType<Record<string, JsonValue>, unknown, $ZodTypeInternals<Record<string, JsonValue>, unknown>>` | —                                         | 运行时连接面参数的 schema                                               |
| `resolveRect?`  | `(context: BoundaryFitContext, params: Record<string, JsonValue>) => Rect`                           | `直接使用视觉 rect`                       | 根据视觉 shape 和实例 params 解析本连接面使用的矩形                     |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BoundaryDefinition = BoundaryDefinitionInput<JsonObject>;
```

</DocTab>

</DocTabs>

### BoundaryDefinitionInput

<p>
  <ApiSourceLink
    label={'BoundaryDefinitionInput'}
    path={'packages/kernel/core/src/contract/boundary/types.ts'}
    startLine={24}
  >
    boundary definition 的作者侧输入形态
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型                                                                               | 默认值                                    | 说明                                                                    |
| --------------- | ---------------------------------------------------------------------------------- | ----------------------------------------- | ----------------------------------------------------------------------- |
| `anchor?`       | `(rect: Rect, name: BoundaryAnchorName, params: TParams) => Position \| undefined` | `不支持；调用方回退或报告不支持该 anchor` | 可选的标准方位 anchor 支持；Node 引用中的中心与形状专属名称不调用此回调 |
| `boundaryPoint` | `(rect: Rect, toward: Position, params: TParams) => Position`                      | —                                         | 从中心指向 toward 的射线与连接面的交点                                  |
| `name`          | `string`                                                                           | —                                         | 注册表 key，由 IR `boundary` 引用                                       |
| `outline?`      | `(rect: Rect, params: TParams) => ReadonlyArray<PathCommand>`                      | —                                         | 返回与解析后 rect 同坐标系的精确闭合连接面轮廓；空数组表示合法空几何    |
| `paramsSchema`  | `ZodType<TParams>`                                                                 | —                                         | 运行时连接面参数的 schema                                               |
| `resolveRect?`  | `(context: BoundaryFitContext, params: TParams) => Rect`                           | `直接使用视觉 rect`                       | 根据视觉 shape 和实例 params 解析本连接面使用的矩形                     |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BoundaryDefinitionInput<TParams extends JsonObject> = {
  name: string;
  paramsSchema: ZodType<TParams>;
  resolveRect?: (context: BoundaryFitContext, params: TParams) => Rect;
  outline?: (rect: Rect, params: TParams) => ReadonlyArray<PathCommand>;
  boundaryPoint: (rect: Rect, toward: Position, params: TParams) => Position;
  anchor?: (rect: Rect, name: BoundaryAnchorName, params: TParams) => Position | undefined;
};
```

</DocTab>

</DocTabs>

#### 类型参数

| 类型参数  | 说明 |
| --------- | ---- |
| `TParams` | —    |

### BoundaryFitContext

<p>
  <ApiSourceLink
    label={'BoundaryFitContext'}
    path={'packages/kernel/core/src/contract/boundary/types.ts'}
    startLine={16}
  >
    Boundary provider 解析实例连接矩形时可用的视觉几何上下文
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员                 | 类型                                     | 默认值 | 说明                                      |
| -------------------- | ---------------------------------------- | ------ | ----------------------------------------- |
| `connectionEnvelope` | `(kind: ConnectionEnvelopeKind) => Rect` | —      | 获取视觉 shape 对指定规则连接面的安全包络 |
| `visualRect`         | `Rect`                                   | —      | 节点视觉 shape 的外接矩形                 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BoundaryFitContext = {
  visualRect: Rect;
  connectionEnvelope: (kind: ConnectionEnvelopeKind) => Rect;
};
```

</DocTab>

</DocTabs>

### IRCoordinate

<p><ApiSourceLink label={"IRCoordinate"} path={"packages/kernel/core/src/schemas/coordinate/types.ts"} startLine={6}>Coordinate IR 类型 `{ type:'coordinate', id, position }`</ApiSourceLink></p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型                                                                                                                                                                                                                                                                                                                         | 默认值 | 说明 |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| `id`       | `string`                                                                                                                                                                                                                                                                                                                     | —      | —    |
| `position` | `[number, number] \| SharedPolarPosition \| IRBetweenPosition \| { direction: "top" \| "right" \| "bottom" \| "left" \| "top-right" \| "top-left" \| "bottom-right" \| "bottom-left"; of: string; distance?: number \| undefined; } \| { of: string \| [number, number] \| SharedPolarPosition; offset: [number, number]; }` | —      | —    |
| `type`     | `"coordinate"`                                                                                                                                                                                                                                                                                                               | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRCoordinate = ZodInfer<typeof CoordinateSchema>;
```

</DocTab>

</DocTabs>

### IRNode

<p>
  <ApiSourceLink label={'IRNode'} path={'packages/kernel/core/src/schemas/node/types.ts'} startLine={33}>
    节点：可定位的形状容器（矩形/圆/椭圆/菱形）+ 可选文本标签
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型                                                                                             | 默认值 | 说明 |
| --------------- | ------------------------------------------------------------------------------------------------ | ------ | ---- |
| `animations?`   | `…`                                                                                              | —      | —    |
| `boundary?`     | `…`                                                                                              | —      | —    |
| `cornerRadius?` | `number`                                                                                         | —      | —    |
| `id?`           | `string`                                                                                         | —      | —    |
| `label?`        | `…`                                                                                              | —      | —    |
| `layout?`       | `…`                                                                                              | —      | —    |
| `meta?`         | `Record<string, JsonValue>`                                                                      | —      | —    |
| `position`      | `…`                                                                                              | —      | —    |
| `rotate?`       | `number`                                                                                         | —      | —    |
| `scale?`        | `number \| { default?: number \| undefined; x?: number \| undefined; y?: number \| undefined; }` | —      | —    |
| `shape?`        | `…`                                                                                              | —      | —    |
| `style?`        | `…`                                                                                              | —      | —    |
| `text?`         | `…`                                                                                              | —      | —    |
| `type`          | `"node"`                                                                                         | —      | —    |
| `zIndex?`       | `number`                                                                                         | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRNode = ZodInfer<typeof NodeSchema>;
```

</DocTab>

</DocTabs>

### IRNodeLabel

<p>
  <ApiSourceLink label={'IRNodeLabel'} path={'packages/kernel/core/src/schemas/node/types.ts'} startLine={30}>
    Node label IR 类型
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员           | 类型                                                                                                                                                                                                                    | 默认值 | 说明 |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| `align?`       | `"start" \| "middle" \| "end"`                                                                                                                                                                                          | —      | —    |
| `distance?`    | `number`                                                                                                                                                                                                                | —      | —    |
| `font?`        | `{ family?: string \| undefined; size?: string \| number \| undefined; weight?: number \| "normal" \| "bold" \| undefined; style?: "normal" \| "italic" \| "oblique" \| undefined; }`                                   | —      | —    |
| `keepUpright?` | `false \| true`                                                                                                                                                                                                         | —      | —    |
| `opacity?`     | `number`                                                                                                                                                                                                                | —      | —    |
| `pin?`         | `false \| true \| { stroke?: string \| number \| undefined; strokeWidth?: number \| undefined; dashPattern?: Array<number> \| undefined; dashOffset?: number \| undefined; }`                                           | —      | —    |
| `placement?`   | `"outside" \| "inside"`                                                                                                                                                                                                 | —      | —    |
| `position?`    | `number \| "center" \| "top" \| "right" \| "bottom" \| "left" \| "top-right" \| "top-left" \| "bottom-right" \| "bottom-left" \| { boundary: "top" \| "right" \| "bottom" \| "left"; fraction?: number \| undefined; }` | —      | —    |
| `rotate?`      | `number \| "none" \| "radial" \| "tangent"`                                                                                                                                                                             | —      | —    |
| `text`         | `…`                                                                                                                                                                                                                     | —      | —    |
| `textColor?`   | `string \| number`                                                                                                                                                                                                      | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRNodeLabel = ZodInfer<typeof NodeLabelSchema>;
```

</DocTab>

</DocTabs>

### IRNodeLayout

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型                                                                                                                                                                                                               | 默认值 | 说明 |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ---- |
| `align?`        | `"start" \| "middle" \| "end"`                                                                                                                                                                                     | —      | —    |
| `lineHeight?`   | `number`                                                                                                                                                                                                           | —      | —    |
| `margin?`       | `number \| { default?: number \| undefined; x?: number \| undefined; y?: number \| undefined; left?: number \| undefined; right?: number \| undefined; top?: number \| undefined; bottom?: number \| undefined; }` | —      | —    |
| `maxTextWidth?` | `number`                                                                                                                                                                                                           | —      | —    |
| `minimumSize?`  | `number \| { default?: number \| undefined; width?: number \| undefined; height?: number \| undefined; }`                                                                                                          | —      | —    |
| `padding?`      | `number \| { default?: number \| undefined; x?: number \| undefined; y?: number \| undefined; left?: number \| undefined; right?: number \| undefined; top?: number \| undefined; bottom?: number \| undefined; }` | —      | —    |
| `width?`        | `number`                                                                                                                                                                                                           | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRNodeLayout = ZodInfer<typeof NodeLayoutSchema>;
```

</DocTab>

</DocTabs>

### IRNodeStyle

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员             | 类型                                                                                                                                                                                                                                                                                  | 默认值 | 说明 |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| `blendMode?`     | `"normal" \| "color" \| "multiply" \| "screen" \| "overlay" \| "darken" \| "lighten" \| "color-dodge" \| "color-burn" \| "hard-light" \| "soft-light" \| "difference" \| "exclusion" \| "hue" \| "saturation" \| "luminosity"`                                                        | —      | —    |
| `color?`         | `string`                                                                                                                                                                                                                                                                              | —      | —    |
| `dashed?`        | `false \| true`                                                                                                                                                                                                                                                                       | —      | —    |
| `dashOffset?`    | `number`                                                                                                                                                                                                                                                                              | —      | —    |
| `dashPattern?`   | `Array<number>`                                                                                                                                                                                                                                                                       | —      | —    |
| `dotted?`        | `false \| true`                                                                                                                                                                                                                                                                       | —      | —    |
| `fill?`          | `…`                                                                                                                                                                                                                                                                                   | —      | —    |
| `fillOpacity?`   | `number`                                                                                                                                                                                                                                                                              | —      | —    |
| `font?`          | `{ family?: string \| undefined; size?: string \| number \| undefined; weight?: number \| "normal" \| "bold" \| undefined; style?: "normal" \| "italic" \| "oblique" \| undefined; }`                                                                                                 | —      | —    |
| `opacity?`       | `number`                                                                                                                                                                                                                                                                              | —      | —    |
| `shadow?`        | `"none" \| "sm" \| "lg" \| "xl" \| "2xl" \| "md" \| { preset?: "none" \| "sm" \| "lg" \| "xl" \| "2xl" \| "md" \| undefined; offsetX?: number \| undefined; offsetY?: number \| undefined; blur?: number \| undefined; color?: string \| undefined; opacity?: number \| undefined; }` | —      | —    |
| `stroke?`        | `…`                                                                                                                                                                                                                                                                                   | —      | —    |
| `strokeOpacity?` | `number`                                                                                                                                                                                                                                                                              | —      | —    |
| `strokeWidth?`   | `number`                                                                                                                                                                                                                                                                              | —      | —    |
| `textColor?`     | `string \| number`                                                                                                                                                                                                                                                                    | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRNodeStyle = ZodInfer<typeof NodeStyleSchema>;
```

</DocTab>

</DocTabs>

### ShapeDefinition

<p>
  <ApiSourceLink label={'ShapeDefinition'} path={'packages/kernel/core/src/contract/shape/types.ts'} startLine={173}>
    shape 定义的擦除形态：registry 存这个
  </ApiSourceLink>
</p>

所有函数收 `JsonObject`（实际类型由 `paramsSchema.parse` 在编译期保证）；registry 同构
不泛型化（避免逆变 / 落 any）。定义点用 `defineShape<TParams>` 拿类型安全

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员                  | 类型                                                                                                                                   | 默认值                                                  | 说明                                                                                                                      |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `anchor`              | `(rect: Rect, name: ShapeAnchorName, params: Record<string, JsonValue>) => Position \| undefined`                                      | —                                                       | 解析命名 anchor 的世界坐标；不支持时返回 `undefined`                                                                      |
| `boundaryPoint`       | `(rect: Rect, toward: Position, params: Record<string, JsonValue>) => Position`                                                        | —                                                       | 返回从 rect 中心指向 `toward` 的射线与 shape 边界的交点<br />`rect` 可包含旋转；实现需要按需转换坐标                      |
| `circumscribe`        | `(innerHalfWidth: number, innerHalfHeight: number, params: Record<string, JsonValue>) => { halfWidth: number; halfHeight: number; }`   | —                                                       | 根据内容半轴和 params 计算完整 shape 的外接 AABB 半轴                                                                     |
| `circumscribeOffset?` | `(params: Record<string, JsonValue>) => Position`                                                                                      | `[0, 0]`                                                | 外接 AABB 中心相对 node `position` 的未旋转局部偏移                                                                       |
| `connectionEnvelope?` | `(rect: Rect, kind: ConnectionEnvelopeKind, params: Record<string, JsonValue>) => ConnectionEnvelope \| undefined`                     | `不支持；tight boundary 回退到 bounds 并发出 warning`   | 返回安全包含视觉几何轮廓的规则连接面半轴<br />结果与 `rect` 同心、同旋转；不含 stroke、shadow、filter 或 label 的视觉外扩 |
| `edgePoint?`          | `(rect: Rect, side: SideValue, t: number, params: Record<string, JsonValue>) => Position`                                              | `不支持`                                                | 解析标准 side 上 `t ∈ [0, 1]` 的比例点<br />`rect` 可包含旋转；未实现表示该 shape 不支持 side anchor                      |
| `emit`                | `(rect: Rect, style: ResolvedShapeStyle, round: (n: number) => number, params: Record<string, JsonValue>) => Iterable<ScenePrimitive>` | —                                                       | 生成轴对齐 rect 内的视觉 primitive                                                                                        |
| `keyPoints?`          | `(rect: Rect, params: Record<string, JsonValue>) => ReadonlyArray<Readonly<{ name: string; position: Position; }>>`                    | —                                                       | 返回 provider 命名的稳定结构关键点；名称在同一实例内必须唯一                                                              |
| `name`                | `string`                                                                                                                               | —                                                       | shape 名称，由 IR `node.shape` 引用                                                                                       |
| `outline?`            | `(rect: Rect, params: Record<string, JsonValue>) => ReadonlyArray<PathCommand>`                                                        | —                                                       | 返回与 rect 同坐标系的精确闭合视觉轮廓；空数组表示合法空几何                                                              |
| `paramsSchema`        | `ZodType<Record<string, JsonValue>, unknown, $ZodTypeInternals<Record<string, JsonValue>, unknown>>`                                   | —                                                       | 实例参数 schema<br />解析结果必须是 JSON object；无参 shape 使用 `z.strictObject({})`                                     |
| `scaleParams?`        | `(params: Record<string, JsonValue>, sx: number, sy: number) => Record<string, JsonValue>`                                             | `按 `Math.sqrt(sx \* sy)` 深度缩放 params 中的数值叶子` | 返回 node scale 后的 params<br />适用于 params 含角度等非长度字段的 shape                                                 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ShapeDefinition = ShapeDefinitionInput<JsonObject>;
```

</DocTab>

</DocTabs>

### ShapeDefinitionInput

<p>
  <ApiSourceLink
    label={'ShapeDefinitionInput'}
    path={'packages/kernel/core/src/contract/shape/types.ts'}
    startLine={105}
  >
    可注册的 shape 定义
  </ApiSourceLink>
</p>

描述第三方作者和内置 shape 共同实现的运行时能力契约；定义本身不进入 IR。
每个能力函数都以实例级 `params` 作为末位参数

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员                  | 类型                                                                                                                                                       | 默认值                                                  | 说明                                                                                                                      |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `anchor`              | `(rect: Rect, name: ShapeAnchorName, params: TParams) => Position \| undefined`                                                                            | —                                                       | 解析命名 anchor 的世界坐标；不支持时返回 `undefined`                                                                      |
| `boundaryPoint`       | `(rect: Rect, toward: Position, params: TParams) => Position`                                                                                              | —                                                       | 返回从 rect 中心指向 `toward` 的射线与 shape 边界的交点<br />`rect` 可包含旋转；实现需要按需转换坐标                      |
| `circumscribe`        | `(`<br />`innerHalfWidth: number,`<br />`innerHalfHeight: number,`<br />`params: TParams,`<br />`) => { halfWidth: number; halfHeight: number }`           | —                                                       | 根据内容半轴和 params 计算完整 shape 的外接 AABB 半轴                                                                     |
| `circumscribeOffset?` | `(params: TParams) => Position`                                                                                                                            | `[0, 0]`                                                | 外接 AABB 中心相对 node `position` 的未旋转局部偏移                                                                       |
| `connectionEnvelope?` | `(rect: Rect, kind: ConnectionEnvelopeKind, params: TParams) => ConnectionEnvelope \| undefined`                                                           | `不支持；tight boundary 回退到 bounds 并发出 warning`   | 返回安全包含视觉几何轮廓的规则连接面半轴<br />结果与 `rect` 同心、同旋转；不含 stroke、shadow、filter 或 label 的视觉外扩 |
| `edgePoint?`          | `(rect: Rect, side: SideValue, t: number, params: TParams) => Position`                                                                                    | `不支持`                                                | 解析标准 side 上 `t ∈ [0, 1]` 的比例点<br />`rect` 可包含旋转；未实现表示该 shape 不支持 side anchor                      |
| `emit`                | `(`<br />`rect: Rect,`<br />`style: ResolvedShapeStyle,`<br />`round: (n: number) => number,`<br />`params: TParams,`<br />`) => Iterable<ScenePrimitive>` | —                                                       | 生成轴对齐 rect 内的视觉 primitive                                                                                        |
| `keyPoints?`          | `(rect: Rect, params: TParams) => ReadonlyArray<GeometryKeyPoint>`                                                                                         | —                                                       | 返回 provider 命名的稳定结构关键点；名称在同一实例内必须唯一                                                              |
| `name`                | `string`                                                                                                                                                   | —                                                       | shape 名称，由 IR `node.shape` 引用                                                                                       |
| `outline?`            | `(rect: Rect, params: TParams) => ReadonlyArray<PathCommand>`                                                                                              | —                                                       | 返回与 rect 同坐标系的精确闭合视觉轮廓；空数组表示合法空几何                                                              |
| `paramsSchema`        | `ZodType<TParams>`                                                                                                                                         | —                                                       | 实例参数 schema<br />解析结果必须是 JSON object；无参 shape 使用 `z.strictObject({})`                                     |
| `scaleParams?`        | `(params: TParams, sx: number, sy: number) => TParams`                                                                                                     | `按 `Math.sqrt(sx \* sy)` 深度缩放 params 中的数值叶子` | 返回 node scale 后的 params<br />适用于 params 含角度等非长度字段的 shape                                                 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ShapeDefinitionInput<TParams extends JsonObject> = {
  name: string;
  paramsSchema: ZodType<TParams>;
  circumscribe: (
    innerHalfWidth: number,
    innerHalfHeight: number,
    params: TParams,
  ) => {
    halfWidth: number;
    halfHeight: number;
  };
  circumscribeOffset?: (params: TParams) => Position;
  boundaryPoint: (rect: Rect, toward: Position, params: TParams) => Position;
  anchor: (rect: Rect, name: ShapeAnchorName, params: TParams) => Position | undefined;
  connectionEnvelope?: (rect: Rect, kind: ConnectionEnvelopeKind, params: TParams) => ConnectionEnvelope | undefined;
  outline?: (rect: Rect, params: TParams) => ReadonlyArray<PathCommand>;
  keyPoints?: (rect: Rect, params: TParams) => ReadonlyArray<GeometryKeyPoint>;
  edgePoint?: (rect: Rect, side: SideValue, t: number, params: TParams) => Position;
  emit: (
    rect: Rect,
    style: ResolvedShapeStyle,
    round: (n: number) => number,
    params: TParams,
  ) => Iterable<ScenePrimitive>;
  scaleParams?: (params: TParams, sx: number, sy: number) => TParams;
};
```

</DocTab>

</DocTabs>

#### 类型参数

| 类型参数  | 说明 |
| --------- | ---- |
| `TParams` | —    |

### BUILTIN_BOUNDARIES

<p>
  <ApiSourceLink
    label={'BUILTIN_BOUNDARIES'}
    path={'packages/kernel/core/src/providers/boundary/definitions.ts'}
    startLine={116}
  >
    内置 boundary provider 注册项
  </ApiSourceLink>
</p>

```ts
export const BUILTIN_BOUNDARIES = defineBuiltinProviderArray<BoundaryDefinition, BuiltinBoundaryProviderName>([
  circleBoundary,
  rectangleBoundary,
  ellipseBoundary,
]);
```

### BUILTIN_SHAPES

<p>
  <ApiSourceLink
    label={'BUILTIN_SHAPES'}
    path={'packages/kernel/core/src/providers/shape/definitions.ts'}
    startLine={15}
  >
    内置 shape provider 注册项；circle / diamond 是 IR 内置 shape preset，不占独立 provider key
  </ApiSourceLink>
</p>

```ts
export const BUILTIN_SHAPES = defineBuiltinProviderArray<ShapeDefinition, BuiltinShapeProviderName>([
  rectangle,
  ellipseShape,
  polygon,
]);
```

### NodeTextColor

<p>
  <ApiSourceLink label={'NodeTextColor'} path={'packages/kernel/core/src/schemas/node/constants.ts'} startLine={4}>
    Node 文字颜色的宿主专用关键字
  </ApiSourceLink>
</p>

```ts
export const NodeTextColor = {
  Contrast: 'contrast',
} as const;
```

### defineBoundary

<p>
  <ApiSourceLink label={'defineBoundary'} path={'packages/kernel/core/src/contract/boundary/define.ts'} startLine={9}>
    定义 boundary 注册项，并把参数泛型擦除为 registry 可存储形态
  </ApiSourceLink>
</p>

```ts
export declare const defineBoundary: <TParams extends Record<string, JsonValue>>(
  def: BoundaryDefinitionInput<TParams>,
) => BoundaryDefinition;
```

#### 类型参数

| 类型参数  | 说明 |
| --------- | ---- |
| `TParams` | —    |

#### 参数

| 参数  | 类型                               | 说明 |
| ----- | ---------------------------------- | ---- |
| `def` | `BoundaryDefinitionInput<TParams>` | —    |

> **备注：** 当前只集中封装擦除边界；保留入口用于对齐 registry API，并为未来校验或归一化预留空间

### defineShape

<p>
  <ApiSourceLink label={'defineShape'} path={'packages/kernel/core/src/contract/shape/define.ts'} startLine={9}>
    定义 shape 注册项，并把参数泛型擦除为 registry 可存储形态
  </ApiSourceLink>
</p>

```ts
export declare const defineShape: <TParams extends Record<string, JsonValue>>(
  def: ShapeDefinitionInput<TParams>,
) => ShapeDefinition;
```

#### 类型参数

| 类型参数  | 说明 |
| --------- | ---- |
| `TParams` | —    |

#### 参数

| 参数  | 类型                            | 说明 |
| ----- | ------------------------------- | ---- |
| `def` | `ShapeDefinitionInput<TParams>` | —    |

> **备注：** 当前只集中封装擦除边界；保留入口用于对齐 registry API，并为未来校验或归一化预留空间
