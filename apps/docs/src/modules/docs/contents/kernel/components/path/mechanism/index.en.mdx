---
title: Implementation
description: Understand how path syntax, cursor state, endpoint references, arrow contacts, and label gaps produce a drawing
---

Read [Basic usage](/kernel/components/path/usage) first. This page explains local drawing rules: why two input forms produce the same path, why endpoints can refer to later nodes, and how arrows and labels change the visible stroke.

## Where the two forms meet

`<Draw way={['a', '-|', 'b']}>` describes the same route as a move to a followed by a fold to b. React Draw forwards input to Path; Vanilla's `normalizePath` calls `parseWay` to produce a uniform ordered sequence of steps.

- Author input: convenient `way` values, string targets, and arrow shorthand
- Source IR: JSON-serializable Path, Step, and marks, still containing node references
- Path commands: concrete move, line, curve, and arc geometry after references have been resolved

The diagram separates normalization from coordinate calculation. Creating steps neither resolves every node reference immediately nor produces SVG directly.

<ComponentPreview files="draw-compilation" size="sm" hideCode type="flow" />

At this entry, `arrow="->"` becomes an arrow mark at `pos: 1`. Per-end `arrowDetail` overrides and `arrowPlacement` overlap are included in that mark. When authoring IR directly, use `marks` rather than copying React's arrow shorthand fields.

<SourceLinks
  sources={[
    { label: 'Draw forwards to Path', path: 'packages/kernel/react/src/sugar/path/Draw.tsx', startLine: 28 },
    {
      label: 'Normalize way, steps, and arrows',
      path: 'packages/kernel/vanilla/src/normalize/path/normalize.ts',
      startLine: 174,
    },
    {
      label: 'Convert arrow shorthand to marks',
      path: 'packages/kernel/vanilla/src/normalize/path/normalize.ts',
      startLine: 42,
    },
  ]}
/>

## How steps change the cursor

The cursor is the position where the next stroke starts. `move` changes that position without drawing; `line` draws from it to a target; `cycle` returns to the start of the current subpath established by the most recent move. A fold or curve may produce several commands while remaining one step.

For example, `move [0, 0]` followed by `line [120, 0]` advances the cursor from A to B. Another move starts an independent subpath without drawing between the two move positions. In `way`, a `+` offset preserves the relative reference position while `++` advances it, so subsequent routes can differ.

A generator is also a step. It receives the local cursor and resolved target, then returns commands to the host Path. It does not own whole-path arrows, stroke, or final outer transforms. See [Step composition](/kernel/components/path/step) and [Custom generators](/kernel/components/path/custom-path-generator).

<SourceLinks
  sources={[
    {
      label: 'Generate commands step by step',
      path: 'packages/kernel/core/src/compile/path/stroke/emit.ts',
      startLine: 59,
    },
    {
      label: 'Validate generator parameters and output',
      path: 'packages/kernel/core/src/compile/path/stroke/lower.ts',
      startLine: 95,
    },
  ]}
/>

## Why later nodes can be referenced

Within the same scope, a Path can precede node B and still use B as its endpoint. Traversal queues the Path and reserves its output position. After registering nodes in the current scope, it resolves endpoints and fills the reserved position with the resulting primitives.

This provides B's connection boundary while preserving the original drawing order. It applies to deferred paths; it does not imply that Node positioning or final Scope placement supports every forward reference.

<SourceLinks
  sources={[
    {
      label: 'Queue paths and reserve drawing order',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 253,
    },
    {
      label: 'Resolve and insert deferred paths',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 124,
    },
  ]}
/>

## Logical path and visible stroke

The three states below use the same A → B line. Gray dots mark logical endpoints, and the dotted line preserves the complete route: first geometry, then arrow placement, then a gap beneath centered text. The small arrows between states describe processing and are not part of A → B.

<ComponentPreview files="draw-geometry-states" size="sm" hideCode type="illustration" />

The first stroke connects A to B. The second still points at B, but its body ends at the arrow contact so it cannot protrude through the arrowhead. The third removes stroke beneath the text while the gray route, label position, and endpoint remain unchanged.

This distinction also explains rounding: `roundedCorners` rewrites geometry, affecting length and along-path sampling; `lineJoin` changes only the stroke appearance at a corner. Rounding, arrow contact, and label interruption are different operations.

<SourceLinks
  sources={[
    {
      label: 'Geometry, rounding, and decorations',
      path: 'packages/kernel/core/src/compile/path/stroke/emit.ts',
      startLine: 300,
    },
    {
      label: 'Endpoint contact and stroke shortening',
      path: 'packages/kernel/core/src/compile/path/stroke/shrink.ts',
      startLine: 150,
    },
  ]}
/>

## How an arrow meets the path

An arrow definition supplies three distinct positions: `backX` is its back edge, `lineContactX` is where the main stroke meets it, and `tipX` is its tip. These determine placement and stroke shortening; one generic arrow length cannot replace them.

<ComponentPreview files="arrow-contact" size="lg" hideCode type="illustration" />

`overlap: 1` aligns the final visual back edge, including scaling and hollow outlines. Overlap moves the endpoint decoration and its contact without changing the node anchor or logical path sampling. An interior mark follows the local tangent without shortening the path as an endpoint would.

Without an explicit arrow color, the marker uses `contextStroke`, inheriting its host path's stroke at rendering time. Resource paints such as gradients require an explicit solid arrow color. Built-in and injected arrows share registration and resolution.

<SourceLinks
  sources={[
    { label: 'Arrow geometry contract', path: 'packages/kernel/core/src/contract/arrow/types.ts', startLine: 4 },
    {
      label: 'Inherited color and marker materialization',
      path: 'packages/kernel/core/src/compile/path/stroke/shrink.ts',
      startLine: 39,
    },
  ]}
/>

## Why labels cut only the stroke

A label first finds its position on the uninterrupted path. Its visual text bounds and `gap` then determine the interval to remove. Remaining segments preserve their original path ownership; arrows, marks, and label positions are not redistributed after cutting.

A centered label on an unfilled built-in Stroke path leaves a gap by default; `interrupt: false` preserves a continuous stroke. Filled paths and other Path kinds do not use that automatic rule, and explicit requests are subject to host restrictions. See [Path labels](/kernel/components/path/label). Final `rotate` and `scale` transform stroke, text, and decorations as one group.

<SourceLinks
  sources={[
    {
      label: 'Label gaps and final path output',
      path: 'packages/kernel/core/src/compile/path/stroke/emit.ts',
      startLine: 300,
    },
  ]}
/>

## What a custom path takes over

`kind` selects a whole-path processor. Core finds the named definition, validates the complete input with its schema, and calls compile. The processor may reuse `emitStroke` or produce Scene primitives directly. A renderer consumes those primitives without recognizing the custom kind name.

Use a generator to add a waveform segment, a Path kind to replace whole-path output, and ArrowDefinition to change endpoint geometry. They share a host but have distinct inputs and results.

<SourceLinks
  sources={[
    {
      label: 'Find and dispatch a Path kind',
      path: 'packages/kernel/core/src/compile/orchestration/traversal.ts',
      startLine: 334,
    },
    {
      label: 'Public services available to a processor',
      path: 'packages/kernel/core/src/contract/path-kind/types.ts',
      startLine: 112,
    },
  ]}
/>

## Shape shortcut expansion

### Circle and ellipse path expansion

The upper route preserves a `Circle center` target, so a node id, polar point, or `[x, y]` coordinate can resolve later during Path compilation. The lower route must compute a center and radii in the React adapter first, so it accepts literal `[x, y]` geometry only:

<ComponentPreview files="circle-ellipse-sugar-flow" size="md" hideCode type="illustration" />

`Circle` from / corner / box forms and every `Ellipse` form take the lower route. Both routes expand to an equivalent `<Path>` plus a `circlePath` / `ellipsePath` Step. With angles present, `closed` then chooses a chord (`chord`, the default), a return to the center (`sector`), or an open arc (`open`).

<SourceLinks
  sources={[
    {
      label: 'Circle input normalization and Step expansion',
      path: 'packages/kernel/react/src/sugar/shapes/Circle.tsx',
      startLine: 19,
      endLine: 93,
    },
    {
      label: 'Ellipse input normalization and Step expansion',
      path: 'packages/kernel/react/src/sugar/shapes/Ellipse.tsx',
      startLine: 18,
      endLine: 88,
    },
  ]}
/>

### Rectangle path expansion

`<Rectangle>` first converges its four prop forms on two opposite corners, `from` and `to`. The upper route in the figure passes through Targets (resolvable points, including coordinates and node ids); the lower route computes the corners from dimensions in the React layer:

<ComponentPreview files="rectangle-sugar-flow" size="lg" hideCode type="illustration" />

Only `corner1 + corner2` passes both Targets through unchanged, so it can reference node ids. Forms such as `center + width + height` need arithmetic first and accept only literal `[x, y]` coordinates. The `rectangle` step then resolves both corners and emits an independent closed subpath; `cornerRadius` replaces each corner with an arc and is clamped to half the shorter side.

If either node reference cannot be resolved, the whole Path is skipped with a warning instead of leaving a partial rectangle.

<SourceLinks
  sources={[
    {
      label: 'Rectangle prop normalization and Path expansion',
      path: 'packages/kernel/react/src/sugar/shapes/Rectangle.tsx',
      startLine: 20,
      endLine: 55,
    },
    {
      label: 'Rectangle-step point resolution and closed outline emission',
      path: 'packages/kernel/core/src/compile/path/stroke/shapes.ts',
      startLine: 171,
      endLine: 219,
    },
  ]}
/>

### Regular polygon vertex expansion

The three input groups converge on one vertex ring before the single main path expands it into a Path:

<ComponentPreview files="polygon-sugar-flow" size="md" hideCode type="illustration" />

`radius` is already the circumradius. `sideLength` first converges on the same value through `R = sideLength / (2·sin(π/sides))`. Vertex `k` then uses the direction `rotate + k·360/sides`; `rotate` is baked into the vertex coordinates instead of being forwarded as a transform on the underlying Path.

The output topology is therefore always one `move`, `sides - 1` `line` steps, and one `cycle`. A non-literal `[x, y]` center, or a `sides` value that is not an integer of at least 3, fails before the vertex ring is produced.

<SourceLinks
  sources={[
    {
      label: 'RegularPolygon input normalization and Path expansion',
      path: 'packages/kernel/react/src/sugar/shapes/RegularPolygon.tsx',
      startLine: 25,
      endLine: 43,
    },
    {
      label: 'Regular polygon vertex calculation',
      path: 'packages/kernel/react/src/sugar/shapes/shape-helpers.ts',
      startLine: 159,
      endLine: 172,
    },
  ]}
/>

### How an angle span is determined

Start with the axes and orange arrow: the span begins at `startAngle`, follows increasing screen-space angles to `endAngle`, and the difference is `sweepAngle`.

<ComponentPreview files="arc-sector-angle-model" size="md" hideCode type="illustration" />

Screen coordinates point x to the right and y downward, so `0°` points right, `90°` points down, and increasing angles look clockwise. Sugar accepts exactly two of `startAngle` / `endAngle` / `sweepAngle` and derives the third.

<SourceLinks
  sources={[
    {
      label: 'Resolve sugar angle inputs',
      path: 'packages/kernel/react/src/sugar/shapes/shape-helpers.ts',
      startLine: 191,
      endLine: 223,
    },
  ]}
/>

### Arc and sector path topology

The three outputs on the right are not rendering effects; they are three equivalent `<Path>` / `<Step>` topologies. `<Arc>` and `<Sector>` resolve angles and radii first, then choose a branch based on open, closed, or hollow geometry.

<ComponentPreview files="arc-sector-sugar-flow" size="sm" hideCode type="illustration" />

- An open `<Arc>` starts at the center, draws one `arc` Step, and leaves the cursor at the arc endpoint.
- A closed `<Arc>` and solid `<Sector>` use `circlePath` / `ellipsePath`; `closed` decides whether the endpoints form a chord or return to the center.
- A hollow `<Sector>` draws the outer arc, a radial edge, the inner arc in reverse, then the radial edge back to the start. It must calculate both arc endpoints up front, so `center` must be a literal `[x, y]`.

Stroke, fill, arrows, and whole-shape transforms all live on the outer `<Path>`; `label` belongs to the Step that draws the visible arc. Sugar stops at this boundary and introduces no parallel path or rendering semantics.

<SourceLinks
  sources={[
    {
      label: 'Expand open and closed Arc forms',
      path: 'packages/kernel/react/src/sugar/shapes/Arc.tsx',
      startLine: 29,
      endLine: 91,
    },
    {
      label: 'Expand solid and hollow Sector forms',
      path: 'packages/kernel/react/src/sugar/shapes/Sector.tsx',
      startLine: 36,
      endLine: 106,
    },
  ]}
/>

### Star vertex expansion

Star alternates inner and outer radii to produce `2 × points` vertices, then emits one move, `2 × points - 1` lines and one cycle. Rotation is baked into the vertices and is not forwarded as a Path transform, avoiding double rotation.

## Further reading

<LinkedSections
  items={[
    {
      title: 'Step composition',
      description: 'Use subpaths, transforms, and rounded corners',
      url: '/kernel/components/path/step',
    },
    {
      title: 'Custom paths',
      description: 'Define and register whole-path output',
      url: '/kernel/components/path/custom-path',
    },
    {
      title: 'API reference',
      description: 'Look up public declarations',
      url: '/kernel/components/path/api-reference',
    },
    {
      title: 'Schema reference',
      description: 'Look up persistent data fields',
      url: '/kernel/components/path/schema-reference',
    },
  ]}
/>
