---
title: Arc / Sector
description: Draw arc and sector Paths with Kernel sugar, or use the Standard Sector shape for sector, ring-wedge, and open-arc Node boundaries.
---

Arcs and sectors are both angle-based: draw an angle span with `<Arc>` / `<Sector>` (sugar), or give a node a sector, ring-wedge, or open-arc boundary with `shape={{ type: 'sector', params }}`. Node shapes keep one `sector` definition; `innerRadius === outerRadius` automatically becomes an open arc. See [Shapes](/kernel/components/shapes) for the full definition.

## As a Path shape (sugar)

`<Arc>` (open arc) / `<Sector>` (closed wedge) draw an angle span at a position, expanding to an underlying `<Path>`. Useful for angle markers, ring fragments, and highlighted sectors.

### Usage

```tsx
import { Arc, Sector } from '@retikz/react';
```

```tsx
<Arc center={[0, 0]} radius={40} startAngle={0} endAngle={90} />
<Sector center={[0, 0]} radius={40} startAngle={0} endAngle={90} fill="darkorange" />
```

### Examples

#### Arc

`<Arc>` draws an open arc by default. It is useful for curved arrows, ring markers, and decorative strokes.

| Form                                   | Best for       |
| -------------------------------------- | -------------- |
| `{ center, radius: number, angles }`   | Circular arc   |
| `{ center, radius: { x, y }, angles }` | Elliptical arc |

Angles start from +x; `90` points downward on screen. Angles take any two of `startAngle` / `endAngle` / `sweepAngle`, e.g. `{ startAngle: 30, sweepAngle: 90 }`. `center` accepts any target (node id / polar / Cartesian).

<ComponentPreview files="arc-basic" size="sm" />

**Visual props**

`<Arc>` forwards the full `<Path>` visual prop set: `stroke` / `strokeWidth` / `dashPattern` / `dashOffset` / `arrow` / `marks` / `opacity` / `rotate` / `scale`, and more. Common ones are arrowed arcs (`arrow="->"`), dashed arcs (`dashPattern` / `dashOffset`), and mid-path direction marks (`marks`, oriented along the tangent).

<ComponentPreview files="arc-visual" size="sm" />

**Closing modes**

Arcs are open by default; pass `close` to turn the arc into a fillable region:

| `close`            | Meaning                                      |
| ------------------ | -------------------------------------------- |
| omitted / `'open'` | Open arc, not closed, not filled             |
| `'chord'`          | Connect both ends with a chord (segment)     |
| `'sector'`         | Connect both ends back to the center (wedge) |

<ComponentPreview files="arc-close" size="sm" />

**Labels on the arc**

`label` forwards to the underlying arc step; the text is positioned along the arc (`position` defaults to `midway`, mapped linearly across `startAngle..endAngle`), just like an edge label. Sector supports it too — see the pie example below.

#### Sector

`<Sector>` starts from the center, draws an arc, then closes back to the center. Add `innerRadius` for a hollow sector; circles use a `number`, while ellipses use `{ x, y }`.

| Form                                   | Best for                 |
| -------------------------------------- | ------------------------ |
| `{ center, radius: number, angles }`   | Circular sector          |
| `{ center, radius: { x, y }, angles }` | Elliptical sector        |
| add `innerRadius: number`              | Hollow circular sector   |
| add `innerRadius: { x, y }`            | Hollow elliptical sector |

<ComponentPreview files="sector-basic" size="sm" />

**Center on a node, labeled slices**

A filled sector's center is the cursor, so `center` can take a node id (or polar coordinate) to pin the sector onto a node; combined with `sweepAngle` and `label`, a few slices make a labeled pie chart.

> A hollow sector (with an inner radius) needs to compute the inner/outer arc endpoints, so its `center` must still be a literal Cartesian `[x, y]`.

<ComponentPreview files="sector-label" size="sm" />

**Whole-shape transform**

`rotate` / `scale` forward to `<Path>`, transforming the whole sector around its bounding-box center (no `<Scope>` wrapper needed). `rotate` tilts an axis-aligned elliptical sector — currently the way to draw a "tilted elliptical sector".

<ComponentPreview files="sector-transform" size="sm" />

Arc and Sector share the same elliptical radii and angle model. Once the basic forms above are clear, adjust both in the playground: the left side shows Arc closure, while the right side shows how Sector's inner arc changes ring width.

<ComponentPreview files="arc-sector-playground" size="sm" />

### API Reference

| Component  | Combination                            | Meaning                                         |
| ---------- | -------------------------------------- | ----------------------------------------------- |
| `<Arc>`    | `center` + `radius: number` + angles   | Circular arc (open by default; angles required) |
|            | `center` + `radius: { x, y }` + angles | Elliptical arc                                  |
| `<Sector>` | `center` + `radius: number` + angles   | Circular sector (angles required)               |
|            | `center` + `radius: { x, y }` + angles | Elliptical sector                               |
|            | + `innerRadius?: number`               | Hollow circular sector                          |
|            | + `innerRadius?: { x, y }`             | Hollow elliptical sector                        |

Shared props (forwarded to the underlying `<Path>`):

| Prop               | Type                                                                                     | Applies to          | Notes                                                                    |
| ------------------ | ---------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------ |
| angles             | `startAngle` / `endAngle` / `sweepAngle`                                                 | Arc / Sector        | Any two, in degrees; `0`=+x, `90`=screen-down                            |
| `center`           | target                                                                                   | Arc / filled Sector | Node id / polar / Cartesian; a hollow Sector must use a literal `[x, y]` |
| `close`            | `'open'` \| `'chord'` \| `'sector'`                                                      | Arc                 | Closing mode, defaults to `'open'`                                       |
| `label`            | edge-label object                                                                        | Arc / Sector        | Positioned along the arc, `position` defaults to `midway`                |
| `rotate` / `scale` | `number` / `IRPathScale`                                                                 | Arc / Sector        | Whole-shape transform around the bounding-box center                     |
| `marks`            | `IRPathBase['marks']`                                                                    | Arc / Sector        | Mid-path direction markers along the path                                |
| visual             | `stroke` / `strokeWidth` / `dashPattern` / `dashOffset` / `arrow` / `fill` / `opacity` … | Arc / Sector        | Forwards the full `<Path>` visual prop set                               |

## As a node shape

When you need a referenceable, icon-ready **Sector Node** rather than a line, set `shape={{ type: 'sector', params }}` on `<Node>`. Its size comes from the radii and angles in `params`, **independent of text content**; when `innerRadius === outerRadius`, the same Sector definition expresses an open-arc boundary. Standard provides only this optional Sector Node shape, while `<Arc>` and `<Sector>` are Kernel React Path sugar. Inject `SectorShapeDefinition` into the current `<Layout>` before using the Sector Node.

### Usage

```tsx
import { Layout, Node } from '@retikz/react';
import { SectorShapeDefinition } from '@retikz/standard/shape';
```

```tsx
<Layout shapes={[SectorShapeDefinition]}>
  <Node shape={{ type: 'sector', params: { innerRadius: 0, outerRadius: 40, startAngle: 0, endAngle: 90 } }} />
  <Node shape={{ type: 'sector', params: { innerRadius: 40, outerRadius: 40, startAngle: 0, endAngle: 90 } }} />
</Layout>
```

### Examples

Switch between an open-arc Sector and a ring-wedge Sector in the playground. The open state sets `innerRadius === outerRadius`; the ring-wedge state also lets you adjust `cornerRadius` to compare sharp and rounded connection boundaries. Move the source with the orbit angle and distance to see automatic clipping follow the current curve or contour. All named targets use Sector anchors such as `outer-arc-mid`, `inner-arc-mid`, `start-edge-mid`, and `end-edge-mid`.

<ComponentPreview files="node-connection-playground" size="sm" />

Besides the node's center plus 8 directional anchors (`center` / `top` …), Sector provides geometry-named anchors for precise connections via `{ id, anchor }`:

- **sector**: `outer-arc-mid` / `inner-arc-mid` / `apex` / `centroid` / `start-edge-mid` / `end-edge-mid`

See [Node geometry](/kernel/components/node/overview) for Node shapes, sizes, boundaries, and anchors.

### API Reference

| `shape`                      | params                                                                      | Boundary / use                                                             |
| ---------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `{ type: 'sector', params }` | `innerRadius` / `outerRadius` / `startAngle` / `endAngle` / `cornerRadius?` | Sector or ring wedge; `innerRadius:0` is solid and equality is an open arc |

The sector's `cornerRadius` (user units) applies only to positive-thickness sectors and rounds the four wedge corners; omitted / `0` = sharp corners, clamped per corner. When `innerRadius === outerRadius`, the result is an open, unfilled arc. Named anchors such as `apex` / `*-arc-mid` do not move with it.

> Node sector constraint: `outerRadius` must be >= `innerRadius`. Equality produces an open arc, `innerRadius:0` remains a solid slice, and only `innerRadius > outerRadius` fails the compile-time refine.

## How it works

<ComponentAlert
  type="tip"
  title="Optional while getting started"
  description="Revisit when you need to understand angle direction, sugar expansion, or Node connection boundaries."
/>

This section focuses on the angle, path topology, and node geometry owned by Arc / Sector. See [Architecture Principles](/kernel/concepts/design/principles) for the complete JSX → IR (intermediate representation) → Scene pipeline.

### How an angle span is determined

Start with the axes and orange arrow: the span begins at `startAngle`, follows increasing screen-space angles to `endAngle`, and the difference is `sweepAngle`.

<ComponentPreview files="arc-sector-angle-model" size="md" hideCode />

Screen coordinates point x to the right and y downward, so `0°` points right, `90°` points down, and increasing angles look clockwise. Sugar accepts exactly two of `startAngle` / `endAngle` / `sweepAngle` and derives the third. Node shape params store `startAngle` and `endAngle` directly, but both APIs share the same visual convention.

<SourceLinks
  sources={[
    {
      label: 'Resolve sugar angle inputs',
      path: 'packages/kernel/react/src/sugar/shapes/shape-helpers.ts',
      startLine: 192,
      endLine: 224,
    },
    {
      label: 'Normalize Node angle ranges',
      path: 'packages/kernel/core/src/shared/geometry/angle.ts',
      startLine: 29,
      endLine: 37,
    },
  ]}
/>

### How sugar forms a Path

The three outputs on the right are not rendering effects; they are three equivalent `<Path>` / `<Step>` topologies. `<Arc>` and `<Sector>` resolve angles and radii first, then choose a branch based on open, closed, or hollow geometry.

<ComponentPreview files="arc-sector-sugar-flow" size="sm" hideCode />

- An open `<Arc>` starts at the center, draws one `arc` Step, and leaves the cursor at the arc endpoint.
- A closed `<Arc>` and solid `<Sector>` use `circlePath` / `ellipsePath`; `closed` decides whether the endpoints form a chord or return to the center.
- A hollow `<Sector>` draws the outer arc, a radial edge, the inner arc in reverse, then the radial edge back to the start. It must calculate both arc endpoints up front, so `center` must be a literal `[x, y]`.

Stroke, fill, arrows, and whole-shape transforms all live on the outer `<Path>`; `label` belongs to the Step that draws the visible arc. Sugar stops at this boundary and introduces no parallel path or rendering semantics.

<SourceLinks
  sources={[
    {
      label: 'Expand open and closed Arc forms',
      path: 'packages/kernel/react/src/sugar/shapes/Arc.tsx',
      startLine: 30,
      endLine: 92,
    },
    {
      label: 'Expand solid and hollow Sector forms',
      path: 'packages/kernel/react/src/sugar/shapes/Sector.tsx',
      startLine: 37,
      endLine: 107,
    },
  ]}
/>

### How a Node shares one geometry source

`<Node shape={{ type, params }}>` does not pass through the sugar component tree above. It hands the shape to its core shape definition. The middle “Shape geometry / contour” role is the key: the axis-aligned bounding box (AABB), anchors, connection points, and the final path are all derived from the same params.

<ComponentPreview files="arc-sector-node-geometry" size="sm" hideCode />

Positive-thickness Sector is a closed contour. Its connection ray starts inside the filled region and hits the real inner arc, outer arc, radial edge, or rounded contour. A zero-thickness Sector is an open arc: automatic connection projects the target direction onto the arc and clamps to the nearer endpoint when the direction falls in the gap.

As a result, `cornerRadius` changes both the visible Sector contour and automatic connection points, but it does not move the exact AABB or named anchors such as `apex` and `outer-arc-mid`. Choose an explicit anchor for a stable semantic location; use an ordinary node reference when the connection should follow the current contour.

<SourceLinks
  sources={[
    {
      label: 'Sector geometry, contour, anchors, and Scene output',
      path: 'packages/library/standard/src/shape/definitions/sector.ts',
      startLine: 25,
      endLine: 235,
    },
  ]}
/>

## Related

- [Node geometry](/kernel/components/node/overview): sizes, boundaries, and anchors when a shape is used as a node boundary
- [Circle / Ellipse](/kernel/components/shapes/circle-ellipse): half circles, segments, and partial ellipses
- [Shapes](/kernel/components/shapes): Shapes overview and full definition
