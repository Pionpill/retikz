{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## React components

### ArcStepProps

<p>
  <ApiSourceLink label={'ArcStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={157}>
    Arc action: draw a circular or elliptical arc between angles; the center defaults to the cursor and can be specified
    explicitly (TikZ `arc[start angle=…, end angle=…, radius=…]`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member       | Type             | Default | Description                                                                                    |
| ------------ | ---------------- | ------- | ---------------------------------------------------------------------------------------------- |
| `center?`    | `DslTarget`      | —       | Explicit center; defaults to the cursor (the previous step anchor)                             |
| `children?`  | `ReactNode`      | —       | Sugar form                                                                                     |
| `endAngle`   | `number`         | —       | End angle in degrees; sweep direction follows the relationship between startAngle and endAngle |
| `kind`       | `'arc'`          | —       | Discriminant for an arc step                                                                   |
| `label?`     | `InputStepLabel` | —       | Edge label                                                                                     |
| `radius`     | `IRStepRadius`   | —       | Arc radius: a number for a circle, or &#123; x, y &#125; for an ellipse                        |
| `startAngle` | `number`         | —       | Start angle in degrees: 0° is +x and 90° is screen-down +y, matching ArcStep and Node labels   |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ArcStepProps = {
  kind: 'arc';
  startAngle: number;
  endAngle: number;
  radius: IRStepRadius;
  center?: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### AxisLineStepProps

<p>
  <ApiSourceLink
    label={'AxisLineStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={42}
  >
    Axis-line action: project a target onto one local axis of the current host and draw a straight segment
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type                         | Default | Description                                                                                   |
| ----------- | ---------------------------- | ------- | --------------------------------------------------------------------------------------------- |
| `axis`      | `'horizontal' \| 'vertical'` | —       | Local host axis whose current coordinate is preserved                                         |
| `children?` | `ReactNode`                  | —       | Sugar form                                                                                    |
| `kind`      | `'axis-line'`                | —       | Discriminant for a single-axis step                                                           |
| `label?`    | `InputStepLabel`             | —       | Edge label                                                                                    |
| `to`        | `IRAxisLineTarget \| string` | —       | Projection target: Cartesian coordinates, a NodeTarget, or the corresponding string shorthand |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type AxisLineStepProps = {
  kind: 'axis-line';
  axis: 'horizontal' | 'vertical';
  to: IRAxisLineTarget | string;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### BendStepProps

<p>
  <ApiSourceLink label={'BendStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={129}>
    Bend action: curve shorthand that computes cubic control points (TikZ `to[bend left=N]` or `to[out=…, in=…]`)
  </ApiSourceLink>
</p>

Supports symmetric bends and asymmetric bends or loops. Explicit out/in angles take precedence during compilation. With all three omitted, defaults to a symmetric left bend. When `from == to`, explicit out/in angles create a loop

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member           | Type                | Default  | Description                                                                                                                                            |
| ---------------- | ------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `bendAngle?`     | `number`            | `30`     | Symmetric bend angle in degrees; defaults to 30                                                                                                        |
| `bendDirection?` | `'left' \| 'right'` | `'left'` | Symmetric bend direction: 'left' or 'right' relative to from→to; optional alongside the out/in form                                                    |
| `children?`      | `ReactNode`         | —        | Sugar form                                                                                                                                             |
| `inAngle?`       | `number`            | —        | Incoming angle in degrees for asymmetric bends or loops (TikZ `in=`); combined with outAngle into a cubic                                              |
| `kind`           | `'bend'`            | —        | Discriminant for a bend step                                                                                                                           |
| `label?`         | `InputStepLabel`    | —        | Edge label                                                                                                                                             |
| `looseness?`     | `number`            | —        | Control-point distance multiplier for asymmetric bends or loops (TikZ `looseness=`); defaults to approximately 1 and also scales the default loop size |
| `outAngle?`      | `number`            | —        | Outgoing angle in degrees for asymmetric bends or loops (TikZ `out=`); combined with inAngle into a cubic and takes precedence over bendDirection      |
| `to`             | `DslTarget`         | —        | Endpoint                                                                                                                                               |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type BendStepProps = {
  kind: 'bend';
  bendDirection?: 'left' | 'right';
  bendAngle?: number;
  outAngle?: number;
  inAngle?: number;
  looseness?: number;
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CirclePathStepProps

<p>
  <ApiSourceLink
    label={'CirclePathStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={175}
  >
    CirclePath action: draw around the cursor; without angles, draw a full circle and return to its center; with angles,
    draw a partial circle (TikZ `circle[radius=…]`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member        | Type                                        | Default | Description                                                                                                |
| ------------- | ------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------- |
| `children?`   | `ReactNode`                                 | —       | Sugar form                                                                                                 |
| `closed?`     | `'closed' \| 'chord' \| 'open' \| 'sector'` | —       | Closure: without angles, closed; with angles, chord (default), sector (back to center), or open (arc only) |
| `endAngle?`   | `number`                                    | —       | Partial-circle end angle in degrees                                                                        |
| `kind`        | `'circlePath'`                              | —       | Discriminant for a circle-path step                                                                        |
| `label?`      | `InputStepLabel`                            | —       | Edge label                                                                                                 |
| `radius`      | `number`                                    | —       | Circle radius in user units                                                                                |
| `startAngle?` | `number`                                    | —       | Partial-circle start angle in degrees; applies only together with endAngle                                 |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type CirclePathStepProps = {
  kind: 'circlePath';
  radius: number;
  startAngle?: number;
  endAngle?: number;
  closed?: 'closed' | 'chord' | 'open' | 'sector';
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CubicStepProps

<p>
  <ApiSourceLink label={'CubicStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={109}>
    Cubic action: cubic Bézier with two control points (TikZ `.. controls (B) and (C) ..`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type             | Default | Description                                      |
| ----------- | ---------------- | ------- | ------------------------------------------------ |
| `children?` | `ReactNode`      | —       | Sugar form                                       |
| `control1`  | `IRControlPoint` | —       | First control point, affecting the start tangent |
| `control2`  | `IRControlPoint` | —       | Second control point, affecting the end tangent  |
| `kind`      | `'cubic'`        | —       | Discriminant for a cubic Bézier step             |
| `label?`    | `InputStepLabel` | —       | Edge label                                       |
| `to`        | `DslTarget`      | —       | Curve endpoint                                   |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type CubicStepProps = {
  kind: 'cubic';
  control1: IRControlPoint;
  control2: IRControlPoint;
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CurveStepProps

<p>
  <ApiSourceLink label={'CurveStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={95}>
    Curve action: quadratic Bézier with one control point (TikZ `.. controls (B) ..`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type             | Default | Description                                         |
| ----------- | ---------------- | ------- | --------------------------------------------------- |
| `children?` | `ReactNode`      | —       | Sugar form                                          |
| `control`   | `IRControlPoint` | —       | Control point; only Cartesian `[x, y]` is supported |
| `kind`      | `'curve'`        | —       | Discriminant for a quadratic Bézier step            |
| `label?`    | `InputStepLabel` | —       | Edge label                                          |
| `to`        | `DslTarget`      | —       | Curve endpoint                                      |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type CurveStepProps = {
  kind: 'curve';
  control: IRControlPoint;
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### CycleStepProps

<p>
  <ApiSourceLink label={'CycleStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={89}>
    Cycle action: close the current subpath to the most recent move point (TikZ `cycle`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member | Type      | Default | Description                                                                                                   |
| ------ | --------- | ------- | ------------------------------------------------------------------------------------------------------------- |
| `kind` | `'cycle'` | —       | Closure discriminant; accepts no `to`, `label`, or `children`, attaches no edge label, and consumes no target |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type CycleStepProps = {
  kind: 'cycle';
};
```

</DocTab>

</DocTabs>

### DrawProps

<p>
  <ApiSourceLink label={'DrawProps'} path={'packages/kernel/react/src/sugar/path/Draw.tsx'} startLine={9}>
    Authoring props for Draw
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member                     | Type                          | Default | Description                                                            |
| -------------------------- | ----------------------------- | ------- | ---------------------------------------------------------------------- |
| `readonly arrow?`          | `PathProps['arrow']`          | —       | Path-level arrow direction                                             |
| `readonly arrowDetail?`    | `PathProps['arrowDetail']`    | —       | Detailed arrow configuration                                           |
| `readonly arrowPlacement?` | `PathProps['arrowPlacement']` | —       | Endpoint arrow placement                                               |
| `readonly roundedCorners?` | `IRPath['roundedCorners']`    | —       | Geometric rounding radius at polyline corners                          |
| `readonly style?`          | `InputPath['style']`          | —       | Instance visual overrides, replacing inherited defaults field by field |
| `readonly thickness?`      | `PathThicknessValue`          | —       | Semantic stroke-width shorthand                                        |
| `readonly way`             | `WayDSL`                      | —       | TikZ-style route shorthand                                             |
| `readonly zIndex?`         | `IRPath['zIndex']`            | —       | Stack order among siblings                                             |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type DrawProps = Readonly<{
  style?: InputPath['style'];
  way: WayDSL;
  roundedCorners?: IRPath['roundedCorners'];
  thickness?: PathThicknessValue;
  arrow?: PathProps['arrow'];
  arrowDetail?: PathProps['arrowDetail'];
  arrowPlacement?: PathProps['arrowPlacement'];
  zIndex?: IRPath['zIndex'];
}>;
```

</DocTab>

</DocTabs>

### EllipsePathStepProps

<p>
  <ApiSourceLink
    label={'EllipsePathStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={193}
  >
    EllipsePath action: draw around the cursor; without angles, draw a full ellipse and return to its center; with
    angles, draw a partial ellipse (TikZ `ellipse[x radius=…, y radius=…]`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member        | Type                                        | Default | Description                                                                                     |
| ------------- | ------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------- |
| `children?`   | `ReactNode`                                 | —       | Sugar form                                                                                      |
| `closed?`     | `'closed' \| 'chord' \| 'open' \| 'sector'` | —       | Closure: without angles, closed; with angles, chord (default), sector (back to center), or open |
| `endAngle?`   | `number`                                    | —       | Partial-ellipse end angle in degrees                                                            |
| `kind`        | `'ellipsePath'`                             | —       | Discriminant for an ellipse-path step                                                           |
| `label?`      | `InputStepLabel`                            | —       | Edge label                                                                                      |
| `radius`      | `IRStepAnisotropicRadius`                   | —       | Ellipse radii on the x and y axes                                                               |
| `startAngle?` | `number`                                    | —       | Partial-ellipse start angle in degrees; applies only together with endAngle                     |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type EllipsePathStepProps = {
  kind: 'ellipsePath';
  radius: IRStepAnisotropicRadius;
  startAngle?: number;
  endAngle?: number;
  closed?: 'closed' | 'chord' | 'open' | 'sector';
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### FoldStepProps

<p>
  <ApiSourceLink label={'FoldStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={71}>
    Fold action: a two- or three-segment orthogonal polyline
  </ApiSourceLink>
</p>

`-|` and `|-` use one turn; `-|-` and `|-|` use two turns, with normalized `fraction` controlling the middle leg

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<DocTabs defaultValue="two-segments">

<DocTab value="two-segments" label="Two segments">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type             | Default | Description                  |
| ----------- | ---------------- | ------- | ---------------------------- |
| `children?` | `ReactNode`      | —       | Sugar form                   |
| `kind`      | `'fold'`         | —       | Discriminant for a fold step |
| `label?`    | `InputStepLabel` | —       | Edge label                   |
| `to`        | `DslTarget`      | —       | Fold endpoint                |
| `via`       | `'-\|' \| '\|-'` | —       | Two-segment fold direction   |

</div>

</DocTab>

<DocTab value="three-segments" label="Three segments">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type                | Default | Description                           |
| ----------- | ------------------- | ------- | ------------------------------------- |
| `children?` | `ReactNode`         | —       | Sugar form                            |
| `fraction?` | `number`            | `0.5`   | Normalized position of the middle leg |
| `kind`      | `'fold'`            | —       | Discriminant for a fold step          |
| `label?`    | `InputStepLabel`    | —       | Edge label                            |
| `to`        | `DslTarget`         | —       | Fold endpoint                         |
| `via`       | `'-\|-' \| '\|-\|'` | —       | Three-segment fold direction          |

</div>

</DocTab>

</DocTabs>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type FoldStepProps = FoldStepBaseProps &
  (
    | {
        via: '-|' | '|-';
      }
    | {
        via: '-|-' | '|-|';
        fraction?: number;
      }
  );
```

</DocTab>

</DocTabs>

### GeneratorStepProps

<p>
  <ApiSourceLink
    label={'GeneratorStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={244}
  >
    Generator action: invoke a built-in or registered path generator to produce low-level path commands
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type                      | Default | Description                                                                                                       |
| ----------- | ------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------- |
| `children?` | `ReactNode`               | —       | Sugar form                                                                                                        |
| `kind`      | `'generator'`             | —       | Discriminant for a generator step                                                                                 |
| `label?`    | `InputStepLabel`          | —       | Edge label                                                                                                        |
| `name`      | `IRGeneratorStep['name']` | —       | Path generator name: a built-in name or one registered through Layout extensions.pathGenerators                   |
| `params`    | `JsonObject`              | —       | JSON-safe parameter object; target references must occupy top-level keys listed in the generator's `targetParams` |
| `to?`       | `DslTarget`               | —       | Optional endpoint passed as `to` in the generator context                                                         |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type GeneratorStepProps = {
  kind: 'generator';
  name: IRGeneratorStep['name'];
  to?: DslTarget;
  params: JsonObject;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### LineStepProps

<p>
  <ApiSourceLink label={'LineStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={30}>
    Line action: draw from the cursor to a target (TikZ `(A) -- (B)`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type             | Default | Description                                                                                |
| ----------- | ---------------- | ------- | ------------------------------------------------------------------------------------------ |
| `children?` | `ReactNode`      | —       | Sugar form: `<Step><EdgeLabel>...</EdgeLabel></Step>`; other children are silently ignored |
| `kind?`     | `'line'`         | —       | Line-step discriminant; defaults to 'line' when omitted                                    |
| `label?`    | `InputStepLabel` | —       | Edge label, equivalent to a sugar `<EdgeLabel>` child                                      |
| `to`        | `DslTarget`      | —       | Line endpoint                                                                              |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LineStepProps = {
  kind?: 'line';
  to: DslTarget;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### MoveStepProps

<p>
  <ApiSourceLink label={'MoveStepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={22}>
    Move action: move the cursor without drawing (TikZ `(A)`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member | Type        | Default | Description                  |
| ------ | ----------- | ------- | ---------------------------- |
| `kind` | `'move'`    | —       | Discriminant for a move step |
| `to`   | `DslTarget` | —       | Move target                  |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type MoveStepProps = {
  kind: 'move';
  to: DslTarget;
};
```

</DocTab>

</DocTabs>

### PathProps

<p>
  <ApiSourceLink label={'PathProps'} path={'packages/kernel/react/src/kernel/components/Path.tsx'} startLine={9}>
    Declare a path with steps or shorthand, and configure its style, arrows, and interactions
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[10%] [&_th:nth-child(2)]:w-[20%] [&_th:nth-child(3)]:w-[24%] [&_th:nth-child(4)]:w-[14%]">

| Group     | Member            | Type                          | Default    | Description                                                                                                                                                                                                                                                                                                         |
| --------- | ----------------- | ----------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Main      | `children?`       | `ReactNode`                   | —          | All children should be `<Step />`                                                                                                                                                                                                                                                                                   |
|           | `id?`             | `IRPath['id']`                | —          | Path id, used by other paths or positions to reference it and by hydration to bind event handlers                                                                                                                                                                                                                   |
|           | `kind?`           | `IRPath['kind']`              | `'stroke'` | Path kind; defaults to built-in stroke. Register custom kinds through Layout.extensions.pathKinds                                                                                                                                                                                                                   |
|           | `kindOptions?`    | `IRPath['kindOptions']`       | —          | JSON options for the selected path kind; built-in stroke does not accept this field                                                                                                                                                                                                                                 |
|           | `label?`          | `IRPath['label']`             | —          | One or more geometry labels attached to the whole path                                                                                                                                                                                                                                                              |
|           | `way?`            | `WayDSL`                      | —          | TikZ-style route shorthand, parsed into steps by Vanilla                                                                                                                                                                                                                                                            |
| Geometry  | `arrow?`          | `InputPath['arrow']`          | `'none'`   | Path-level arrow direction<br />`'->'` at the end, `'<-'` at the start, or `'<->'` at both ends; omitted or `'none'` means no arrows                                                                                                                                                                                |
|           | `arrowDetail?`    | `InputPath['arrowDetail']`    | —          | Detailed arrow configuration<br />Top-level defaults with optional `start` and `end` overrides merged field by field. On hollow shapes (open, openStealth, openDiamond, and openCircle), `fill` is a silent no-op                                                                                                   |
|           | `arrowPlacement?` | `InputPath['arrowPlacement']` | —          | Endpoint arrow placement<br />Shared `overlap` for actual endpoints, with per-end `start` and `end` overrides; interpolates from default placement to alignment of the final visual back edge with the logical endpoint                                                                                             |
|           | `marks?`          | `IRPath['marks']`             | —          | Place marks at normalized positions along the path; currently only arrows<br />Each `{ pos, mark }` uses `pos∈[0,1]` and `mark.kind:'arrow'` plus visual settings; shape is a registered arrow name and direction follows the path tangent                                                                          |
|           | `rotate?`         | `IRPath['rotate']`            | —          | Rotate the whole path in degrees about its bounding-box center; positive angles are visually clockwise in screen-down coordinates<br />Equivalent to a Scope rotating the path around its bounding-box center; endpoints resolve in the current scope before rotation                                               |
|           | `roundedCorners?` | `IRPath['roundedCorners']`    | —          | Geometric rounding radius at polyline corners (TikZ `rounded corners=`)<br />Insert a tangent arc at each line-to-line joint, changing geometry rather than only stroke appearance. Curve, arc, Bézier, and fold joints stay sharp; the radius is clamped to adjacent segment lengths. Omission keeps sharp corners |
|           | `scale?`          | `IRPath['scale']`             | —          | Scale the whole path around its bounding-box center: a number for uniform scaling or `{ x, y }` for independent axes                                                                                                                                                                                                |
| Style     | `style?`          | `InputPath['style']`          | —          | Instance visual overrides, replacing inherited defaults field by field                                                                                                                                                                                                                                              |
|           | `thickness?`      | `PathThicknessValue`          | —          | Semantic stroke-width shorthand (TikZ `ultra thin` through `ultra thick`), normalized to `strokeWidth`; explicit `strokeWidth` takes precedence                                                                                                                                                                     |
|           | `zIndex?`         | `IRPath['zIndex']`            | `0`        | Explicit stack order: larger values are above; defaults to 0, equal values preserve declaration order, and comparison is limited to siblings                                                                                                                                                                        |
| Callbacks | `onClick?`        | `HydrationHandler`            | —          | Click on this element (DOM `click`)                                                                                                                                                                                                                                                                                 |
|           | `onDoubleClick?`  | `HydrationHandler`            | —          | Double-click on this element (DOM `dblclick`)                                                                                                                                                                                                                                                                       |
|           | `onPointerDown?`  | `HydrationHandler`            | —          | Pointer pressed on this element (DOM `pointerdown`)                                                                                                                                                                                                                                                                 |
|           | `onPointerEnter?` | `HydrationHandler`            | —          | Pointer enters this element, synthesized from `pointermove` and hit-ID state; moving between children does not trigger it again                                                                                                                                                                                     |
|           | `onPointerLeave?` | `HydrationHandler`            | —          | Pointer leaves this element, synthesized from `pointermove` and hit-ID state                                                                                                                                                                                                                                        |
|           | `onPointerMove?`  | `HydrationHandler`            | —          | Pointer moves over this element (DOM `pointermove`)                                                                                                                                                                                                                                                                 |
|           | `onPointerUp?`    | `HydrationHandler`            | —          | Pointer released on this element (DOM `pointerup`)                                                                                                                                                                                                                                                                  |
|           | `onRightClick?`   | `HydrationHandler`            | —          | Context menu on this element (DOM `contextmenu`); call `event.preventDefault()` in the handler to suppress the browser menu                                                                                                                                                                                         |
|           | `onWheel?`        | `HydrationHandler`            | —          | Wheel input over this element (DOM `wheel`)                                                                                                                                                                                                                                                                         |
| Animation | `animations?`     | `IRPath['animations']`        | —          | Path-level timeline animation; rendered as animation or a static fallback, without participating in layout                                                                                                                                                                                                          |
| Runtime   | `authoring?`      | `unknown`                     | —          | Optional runtime-only authoring payload interpreted by a compile driver; excluded from Core IR                                                                                                                                                                                                                      |
|           | `meta?`           | `IRPath['meta']`              | —          | JSON-serializable user metadata available in event and hydration contexts; does not participate in layout                                                                                                                                                                                                           |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type PathProps = HydrationEventProps & {
  style?: InputPath['style'];
  authoring?: unknown;
  kind?: IRPath['kind'];
  kindOptions?: IRPath['kindOptions'];
  label?: IRPath['label'];
  id?: IRPath['id'];
  meta?: IRPath['meta'];
  animations?: IRPath['animations'];
  roundedCorners?: IRPath['roundedCorners'];
  thickness?: PathThicknessValue;
  arrow?: InputPath['arrow'];
  arrowDetail?: InputPath['arrowDetail'];
  arrowPlacement?: InputPath['arrowPlacement'];
  zIndex?: IRPath['zIndex'];
  rotate?: IRPath['rotate'];
  scale?: IRPath['scale'];
  marks?: IRPath['marks'];
  way?: WayDSL;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### RectangleStepProps

<p>
  <ApiSourceLink
    label={'RectangleStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={211}
  >
    Rectangle action: an axis-aligned rectangle from opposite corners, optionally rounded, compiled to path commands
    (TikZ `(a) rectangle (b)`)
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member          | Type          | Default | Description                                                                                          |
| --------------- | ------------- | ------- | ---------------------------------------------------------------------------------------------------- |
| `cornerRadius?` | `number`      | —       | Shared corner radius; omitted means sharp corners, and compilation clamps it to half the side length |
| `from`          | `DslTarget`   | —       | One corner                                                                                           |
| `kind`          | `'rectangle'` | —       | Discriminant for a rectangle step                                                                    |
| `to`            | `DslTarget`   | —       | Opposite corner; order is irrelevant                                                                 |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type RectangleStepProps = {
  kind: 'rectangle';
  from: DslTarget;
  to: DslTarget;
  cornerRadius?: number;
};
```

</DocTab>

</DocTabs>

### SmoothStepProps

<p>
  <ApiSourceLink
    label={'SmoothStepProps'}
    path={'packages/kernel/react/src/kernel/components/Step.tsx'}
    startLine={227}
  >
    Smooth action: a smooth curve through the cursor and `points` (TikZ `plot[smooth]` or Hobby-style input)
  </ApiSourceLink>
</p>

The cursor is the implicit first knot; the curve passes through each entry in `points`, using centripetal Catmull–Rom converted to cubics. A preceding step must set the cursor, which ends at the last point. `tension` defaults to 1; values below 1 tighten the curve and values above 1 increase the bulge

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type               | Default | Description                                                                                                         |
| ----------- | ------------------ | ------- | ------------------------------------------------------------------------------------------------------------------- |
| `children?` | `ReactNode`        | —       | Sugar form                                                                                                          |
| `kind`      | `'smooth'`         | —       | Discriminant for a smooth step                                                                                      |
| `label?`    | `InputStepLabel`   | —       | Edge label positioned by Bézier parameter along the generated cubics                                                |
| `points`    | `Array<DslTarget>` | —       | Ordered points after the cursor; one point creates one segment, and the cursor ends at the last point               |
| `tension?`  | `number`           | `1`     | Tangent-length multiplier (TikZ `tension`); defaults to 1, with lower values tighter and higher values more rounded |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type SmoothStepProps = {
  kind: 'smooth';
  points: Array<DslTarget>;
  tension?: number;
  label?: InputStepLabel;
  children?: ReactNode;
};
```

</DocTab>

</DocTabs>

### StepProps

<p>
  <ApiSourceLink label={'StepProps'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={262}>
    View source
  </ApiSourceLink>
</p>

Fourteen kinds: 'move', 'line' (default), 'axis-line' (single-axis projection), 'fold', 'cycle', 'curve' (quadratic Bézier), 'cubic', 'bend', 'arc', 'circlePath', 'ellipsePath', 'rectangle', 'smooth', and 'generator'. All except 'move', 'cycle', and 'rectangle' accept `label?: IRStepLabel`, equivalent to an `<EdgeLabel>` child with the prop taking precedence. 'smooth' uses `points` instead of `to`; 'generator' uses `name` and JSON-safe `params`. Named types for each kind support wrappers and derived props

```ts
export type StepProps =
  | MoveStepProps
  | LineStepProps
  | AxisLineStepProps
  | FoldStepProps
  | CycleStepProps
  | CurveStepProps
  | CubicStepProps
  | BendStepProps
  | ArcStepProps
  | CirclePathStepProps
  | EllipsePathStepProps
  | RectangleStepProps
  | SmoothStepProps
  | GeneratorStepProps;
```

### Draw

<p>
  <ApiSourceLink label={'Draw'} path={'packages/kernel/react/src/sugar/path/Draw.tsx'} startLine={32}>
    Sugar component forwarding path grammar to Vanilla unchanged
  </ApiSourceLink>
</p>

React only provides JSX sugar; `normalizePath` is the sole parser dispatch point for `way`

```ts
export declare const Draw: FC<DrawProps>;
```

### Path

<p><ApiSourceLink label={"Path"} path={"packages/kernel/react/src/kernel/components/Path.tsx"} startLine={82}>Path declares a route using a sequence of `<Step>` elements</ApiSourceLink></p>

This component does not render DOM itself; `<Layout>` produces the final path from steps, styles, arrows, and marks

```ts
export declare const Path: FC<PathProps>;
```

### Step

<p>
  <ApiSourceLink label={'Step'} path={'packages/kernel/react/src/kernel/components/Step.tsx'} startLine={282}>
    Step is a DSL marker component and does not render itself
  </ApiSourceLink>
</p>

Declares one path action, such as a move, line, curve, arc, closure, or generator segment

```ts
export declare const Step: FC<StepProps>;
```

## Vanilla authoring input

### InputPath

<p>
  <ApiSourceLink label={'InputPath'} path={'packages/kernel/vanilla/src/normalize/path/types.ts'} startLine={163}>
    Authoring input for a path
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[10%] [&_th:nth-child(2)]:w-[20%] [&_th:nth-child(3)]:w-[24%] [&_th:nth-child(4)]:w-[14%]">

| Group     | Member            | Type                                  | Default | Description                                                                                                                                                                  |
| --------- | ----------------- | ------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Main      | `children?`       | `ReadonlyArray<InputStep>`            | —       | Path steps; mutually exclusive with way                                                                                                                                      |
|           | `id?`             | `string`                              | —       | Optional stable id used as a reference target for the Scene primitive emitted by this drawable element.                                                                      |
|           | `kind?`           | `string & {} \| "stroke"`             | —       | Path kind provider name. Omitted means built-in `stroke`.                                                                                                                    |
|           | `kindOptions?`    | `Record<string, JsonValue>`           | —       | JSON-safe option object consumed by the selected path kind provider.                                                                                                         |
|           | `label?`          | `InputPath["label"]`                  | —       | Host label attached to this path-like relation.                                                                                                                              |
|           | `type?`           | `'path'`                              | —       | Explicit path category when authoring fields cannot identify it uniquely                                                                                                     |
|           | `way?`            | `WayDSL`                              | —       | TikZ-style route shorthand                                                                                                                                                   |
| Geometry  | `arrow?`          | `InputPathArrowDirectionValue`        | —       | Path-level arrow direction                                                                                                                                                   |
|           | `arrowDetail?`    | `IRArrowDetail`                       | —       | Top-level arrow defaults and endpoint overrides                                                                                                                              |
|           | `arrowPlacement?` | `InputPathArrowPlacement`             | —       | Shared and per-end arrow placement settings                                                                                                                                  |
|           | `marks?`          | `InputPath["marks"]`                  | —       | Marks placed along the path at normalized positions; direction follows the path tangent.                                                                                     |
|           | `rotate?`         | `number`                              | —       | Rotate the whole path around its bounding-box center. Endpoints resolve before rotation wraps the resulting geometry.                                                        |
|           | `roundedCorners?` | `number`                              | —       | Geometric corner radius applied to line-to-line joints. Distinct from `lineJoin`, which only styles stroke corners. Omitted fields keep sharp joints.                        |
|           | `scale?`          | `number \| { x: number; y: number; }` | —       | Scale the whole path around its bounding-box center. Applied with rotate around the same center.                                                                             |
| Style     | `style?`          | `InputPath["style"]`                  | —       | Path visual overrides; fields independently override inherited defaults.                                                                                                     |
|           | `thickness?`      | `PathThicknessValue`                  | —       | Path stroke-width shorthand                                                                                                                                                  |
|           | `zIndex?`         | `number`                              | —       | Explicit stacking order among sibling IR children. Higher draws on top. Omitted = 0 = source order. Sorting is stable within the same parent group.                          |
| Animation | `animations?`     | `InputPath["animations"]`             | —       | Declarative timeline animation tracks carried verbatim into the emitted Scene primitive(s). They do not affect layout or bounding boxes and are not inherited across scopes. |
| Runtime   | `authoring?`      | `unknown`                             | —       | Optional runtime payload interpreted by a compile driver; excluded from Core IR                                                                                              |
|           | `meta?`           | `Record<string, JsonValue>`           | —       | Opaque JSON metadata carried by this drawable element. Preserved into emitted Scene primitives and ignored by the compiler.                                                  |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type InputPath = InputPathBase & {
  type?: 'path';
};
```

</DocTab>

</DocTabs>

### InputStep

<p>
  <ApiSourceLink label={'InputStep'} path={'packages/kernel/vanilla/src/normalize/path/types.ts'} startLine={128}>
    Authoring path step
  </ApiSourceLink>
</p>

```ts
export type InputStep =
  | InputMoveStep
  | InputLineStep
  | InputAxisLineStep
  | InputFoldStep
  | Extract<
      IRStep,
      {
        kind: 'cycle';
      }
    >
  | InputCurveStep
  | InputCubicStep
  | InputBendStep
  | InputCirclePathStep
  | InputEllipsePathStep
  | InputArcStep
  | InputRectangleStep
  | InputSmoothStep
  | InputGeneratorStep;
```

### path

<p>
  <ApiSourceLink label={'path'} path={'packages/kernel/vanilla/src/normalize/helpers.ts'} startLine={30}>
    Create path authoring input, with identity declared by the configuration id
  </ApiSourceLink>
</p>

```ts
export declare const path: (config: Omit<InputPath, 'type'>) => InputPath;
```

#### Parameters

| Parameter | Type                      | Description                                                 |
| --------- | ------------------------- | ----------------------------------------------------------- |
| `config`  | `Omit<InputPath, 'type'>` | Path configuration; way and children are mutually exclusive |

#### Returns

Path authoring input preserving the configuration fields

## Path syntax and extension contracts

### ArrowDefinition

<p>
  <ApiSourceLink label={'ArrowDefinition'} path={'packages/kernel/core/src/contract/arrow/types.ts'} startLine={22}>
    Registerable arrow definition
  </ApiSourceLink>
</p>

Describes marker dimensions, contact points, and geometry generation; the definition itself is not IR

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member           | Type                                                   | Default                      | Description                                                                                                              |
| ---------------- | ------------------------------------------------------ | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `backX`          | `number`                                               | —                            | Promised back edge of the base marker geometry along its axis; must not lie beyond the line contact point                |
| `baseSize?`      | `number`                                               | `10`                         | Marker-local base size (viewBox `0 0 baseSize baseSize`, refY = baseSize/2); defaults to 10                              |
| `defaultLength?` | `number`                                               | `8`                          | Fallback arrow length; defaults to 8                                                                                     |
| `defaultWidth?`  | `number`                                               | `8`                          | Fallback arrow width; defaults to 8                                                                                      |
| `emit`           | `(ctx: ArrowEmitContext) => Iterable<MarkerPrimitive>` | —                            | Renderer-independent marker geometry in local coordinates; an adapter embeds the result in `<marker>`                    |
| `hollow?`        | `boolean`                                              | `false`                      | Hollow flag: when true, stroke defines the outline and lineWidth adjusts the contact point                               |
| `lineContactX`   | `number`                                               | —                            | Base line contact point, used for path shortening and marker refX                                                        |
| `name`           | `string`                                               | —                            | Arrow name referenced by IR `marks[].mark.shape`; authoring input uses `arrowDetail.shape`                               |
| `outerInset?`    | `number`                                               | `hollow ? lineWidth / 2 : 0` | Outer-outline compensation in marker-local coordinates; defaults to lineWidth/2 for hollow arrows and 0 for solid arrows |
| `tipX?`          | `number`                                               | `baseSize`                   | Tip x coordinate used for shortening; defaults to baseSize                                                               |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ArrowDefinition = {
  name: string;
  baseSize?: number;
  hollow?: boolean;
  backX: number;
  lineContactX: number;
  outerInset?: number;
  tipX?: number;
  defaultLength?: number;
  defaultWidth?: number;
  emit: (ctx: ArrowEmitContext) => Iterable<MarkerPrimitive>;
};
```

</DocTab>

</DocTabs>

### ArrowEmitContext

<p>
  <ApiSourceLink label={'ArrowEmitContext'} path={'packages/kernel/core/src/contract/arrow/types.ts'} startLine={7}>
    Runtime context for arrow emission
  </ApiSourceLink>
</p>

Resolved colors, stroke width, and rounding function used to generate marker geometry

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member      | Type                    | Default | Description                                                                                    |
| ----------- | ----------------------- | ------- | ---------------------------------------------------------------------------------------------- |
| `fill`      | `MarkerFill`            | —       | Fill color, the main color for solid arrows; processed according to `hollow` for hollow arrows |
| `lineWidth` | `number`                | —       | Stroke width in marker-local user units, used to draw hollow outlines                          |
| `round`     | `(n: number) => number` | —       | Precision rounding shared with compile and render to keep geometry consistent                  |
| `stroke`    | `MarkerFill`            | —       | Stroke color; without an override, `{ kind: 'contextStroke' }` inherits the path stroke        |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ArrowEmitContext = {
  stroke: MarkerFill;
  fill: MarkerFill;
  lineWidth: number;
  round: (n: number) => number;
};
```

</DocTab>

</DocTabs>

### PathGeneratorDefinition

<p>
  <ApiSourceLink
    label={'PathGeneratorDefinition'}
    path={'packages/kernel/core/src/contract/path-generator/types.ts'}
    startLine={31}
  >
    Registerable path generator definition
  </ApiSourceLink>
</p>

Describes JSON parameters, resolvable target parameters, and command generation; the definition itself is not IR

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member          | Type                                                        | Default | Description                                                                                                                                             |
| --------------- | ----------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `generate`      | `(ctx: PathGeneratorGenerateContext) => Array<PathCommand>` | —       | Generate low-level path commands from the context<br />Commands use the same local coordinates as the context and may contain `move` to start a subpath |
| `name`          | `string`                                                    | —       | Generator name referenced by the generator step's `name`                                                                                                |
| `paramsSchema`  | `ZodType<JsonObject>`                                       | —       | Instance parameter schema<br />Parsed output must be a JSON object                                                                                      |
| `targetParams?` | `Array<string>`                                             | `[]`    | Top-level parameter keys to resolve into the current Path's local coordinates                                                                           |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type PathGeneratorDefinition = {
  name: string;
  paramsSchema: ZodType<JsonObject>;
  targetParams?: Array<string>;
  generate: (ctx: PathGeneratorGenerateContext) => Array<PathCommand>;
};
```

</DocTab>

</DocTabs>

### PathGeneratorGenerateContext

<p>
  <ApiSourceLink
    label={'PathGeneratorGenerateContext'}
    path={'packages/kernel/core/src/contract/path-generator/types.ts'}
    startLine={11}
  >
    Runtime context for a path generator
  </ApiSourceLink>
</p>

All coordinates are local to the current Path; the owning GroupPrim applies the Scope transform to the returned commands

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member            | Type                       | Default | Description                                                                         |
| ----------------- | -------------------------- | ------- | ----------------------------------------------------------------------------------- |
| `from`            | `Position`                 | —       | Current local cursor position: the preceding segment endpoint or subpath start      |
| `params`          | `Record<string, unknown>`  | —       | Parameter object validated by paramsSchema                                          |
| `resolvedTargets` | `Record<string, Position>` | —       | Resolution of top-level targetParams keys into the current Path's local coordinates |
| `round`           | `(n: number) => number`    | —       | Precision rounding shared with compile and render                                   |
| `to?`             | `Position`                 | —       | Local coordinates after resolving step.to; absent when the step omits `to`          |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type PathGeneratorGenerateContext = {
  from: Position;
  to?: Position;
  params: Record<string, unknown>;
  resolvedTargets: Record<string, Position>;
  round: (n: number) => number;
};
```

</DocTab>

</DocTabs>

### PathKindCompileContext

<p>
  <ApiSourceLink
    label={'PathKindCompileContext'}
    path={'packages/kernel/core/src/contract/path-kind/types.ts'}
    startLine={105}
  >
    Path-kind compilation context
  </ApiSourceLink>
</p>

A custom kind can take over output completely or reuse standard stroke logic through callbacks

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member            | Type                                                                           | Default | Description                                                                                  |
| ----------------- | ------------------------------------------------------------------------------ | ------- | -------------------------------------------------------------------------------------------- |
| `appearance`      | `ResolvedPathKindAppearance`                                                   | —       | Resolved renderer-neutral host appearance                                                    |
| `emitHostLabels`  | `(input: PathKindLabelInput) => ReadonlyArray<ScenePrimitive>`                 | —       | Compile shared host labels with optional boundary offsets supplied by the kind               |
| `emitStroke`      | `EmitStroke`                                                                   | —       | Reuse Core's standard stroke compiler; omission uses the current `path`                      |
| `materializePath` | `(input?: Readonly<{ children?: ReadonlyArray<IRStep> }>) => MaterializedPath` | —       | Materialize selected steps without applying markers, dashes, fill, or kind-specific geometry |
| `ownerOutput`     | `CompileOwnerOutputPublisher<TOwnerOutput>`                                    | —       | Publisher for the final output owned by the current Path kind                                |
| `path`            | `TPath`                                                                        | —       | Path subject parsed through this definition's complete schema                                |
| `round`           | `(value: number) => number`                                                    | —       | Rounding function shared with the current compilation                                        |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type PathKindCompileContext<TPath extends IRPathBase = IRPathBase, TOwnerOutput extends JsonValue = never> = {
  path: TPath;
  ownerOutput: CompileOwnerOutputPublisher<TOwnerOutput>;
  materializePath: (
    input?: Readonly<{
      children?: ReadonlyArray<IRStep>;
    }>,
  ) => MaterializedPath;
  emitStroke: EmitStroke;
  emitHostLabels: (input: PathKindLabelInput) => ReadonlyArray<ScenePrimitive>;
  appearance: ResolvedPathKindAppearance;
  round: (value: number) => number;
};
```

</DocTab>

</DocTabs>

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TPath`        | —           |
| `TOwnerOutput` | —           |

### PathKindCompileResult

<p>
  <ApiSourceLink
    label={'PathKindCompileResult'}
    path={'packages/kernel/core/src/contract/path-kind/types.ts'}
    startLine={14}
  >
    Path-kind compilation result
  </ApiSourceLink>
</p>

A path-kind definition compiles a high-level path into Scene primitives in the current Path's local coordinates and returns points in that same space for bounds and transform calculations

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<div className="[&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| Member         | Type                    | Default | Description                                                                                  |
| -------------- | ----------------------- | ------- | -------------------------------------------------------------------------------------------- |
| `boundsPoints` | `Array<IRPosition>`     | —       | Geometry used for layout and path-level rotate/scale in the current Path's local coordinates |
| `primitives`   | `Array<ScenePrimitive>` | —       | Actual rendering output in the current Path's local coordinates                              |

</div>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type PathKindCompileResult = {
  primitives: Array<ScenePrimitive>;
  boundsPoints: Array<IRPosition>;
};
```

</DocTab>

</DocTabs>

### PathKindDefinition

<p>
  <ApiSourceLink
    label={'PathKindDefinition'}
    path={'packages/kernel/core/src/contract/path-kind/types.ts'}
    startLine={132}
  >
    View source
  </ApiSourceLink>
</p>

```ts
export type PathKindDefinition<TPath extends IRPathBase = IRPathBase, TOwnerOutput extends JsonValue = never> = {
  name: string;
  schema: ZodType<TPath>;
  compile: (context: PathKindCompileContext<TPath, TOwnerOutput>) => PathKindCompileResult | null;
} & PathKindOwnerOutputBranch<TOwnerOutput>;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TPath`        | —           |
| `TOwnerOutput` | —           |

### WayDSL

<p><ApiSourceLink label={"WayDSL"} path={"packages/kernel/core/src/parse/way.ts"} startLine={141}>Way DSL array accepted by sugar `<Draw way={...}>`</ApiSourceLink></p>

```ts
export type WayDSL = Array<WayItem>;
```

### WayItem

<p>
  <ApiSourceLink label={'WayItem'} path={'packages/kernel/core/src/parse/way.ts'} startLine={123}>
    An element of the sugar way-array DSL
  </ApiSourceLink>
</p>

Node-id strings, Cartesian points, and polar points become line steps (the first becomes move). Relative objects become IR relative/relativeAccumulate targets. horizontalTo/verticalTo become axis-line. A bare via or `{via,fraction}` combines with the next target into a fold. DrawWay.Cycle closes the subpath. curve/cubic/bend consume the next target, while arc/circle/ellipse use the previous position as center without consuming the next item. Internal Cycle/Relative/Accumulate strings avoid collisions with node ids

```ts
export type WayItem =
  | IRTarget
  | string
  | WayRelativeItem
  | WayVia
  | WayFoldOp
  | WayCycle
  | WayCurveOp
  | WayCubicOp
  | WayBendOp
  | WayArcOp
  | WayCircleOp
  | WayEllipseOp
  | WayLabelOp
  | WayAxisLineOp;
```

### definePathKind

<p>
  <ApiSourceLink label={'definePathKind'} path={'packages/kernel/core/src/contract/path-kind/define.ts'} startLine={51}>
    View source
  </ApiSourceLink>
</p>

```ts
export declare const definePathKind: {
  <TPath extends IRPathBase = IRPathBase>(
    definition: PathKindDefinition<TPath, never>,
  ): PathKindDefinition<TPath, never>;
  <TPath extends IRPathBase, TOwnerOutput extends JsonValue>(
    definition: PathKindDefinition<TPath, TOwnerOutput>,
  ): PathKindDefinition<TPath, TOwnerOutput>;
};
```

#### Overload 1

```ts
<TPath extends IRPathBase = IRPathBase>(definition: PathKindDefinition<TPath, never>): PathKindDefinition<TPath, never>;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TPath`        | —           |

#### Parameters

| Parameter    | Type                               | Description |
| ------------ | ---------------------------------- | ----------- |
| `definition` | `PathKindDefinition<TPath, never>` | —           |

#### Overload 2

```ts
<TPath extends IRPathBase, TOwnerOutput extends JsonValue>(definition: PathKindDefinition<TPath, TOwnerOutput>): PathKindDefinition<TPath, TOwnerOutput>;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TPath`        | —           |
| `TOwnerOutput` | —           |

#### Parameters

| Parameter    | Type                                      | Description |
| ------------ | ----------------------------------------- | ----------- |
| `definition` | `PathKindDefinition<TPath, TOwnerOutput>` | —           |

### DrawWay

<p>
  <ApiSourceLink label={'DrawWay'} path={'packages/kernel/core/src/parse/way.ts'} startLine={33}>
    Keyword constants for the sugar way-array DSL
  </ApiSourceLink>
</p>

Cycle closes to the way start and must be referenced through DrawWay.Cycle. Hv/Vh/Hvh/Vhv describe two- or three-segment folds. Relative/Accumulate discriminate relative-offset items: Relative preserves prevEnd, while Accumulate updates it cumulatively. A const object with as const avoids TypeScript enum reverse mappings and literal incompatibility

```ts
export const DrawWay = {
  Cycle: 'retikz-keyword_cycle',
  Hv: '-|',
  Vh: '|-',
  Hvh: '-|-',
  Vhv: '|-|',
  Relative: 'retikz-keyword_relative',
  Accumulate: 'retikz-keyword_accumulate',
} as const;
```

### defineArrow

<p>
  <ApiSourceLink label={'defineArrow'} path={'packages/kernel/core/src/contract/arrow/define.ts'} startLine={7}>
    Define an arrow registry entry
  </ApiSourceLink>
</p>

```ts
export declare const defineArrow: (def: ArrowDefinition) => ArrowDefinition;
```

#### Parameters

| Parameter | Type              | Description |
| --------- | ----------------- | ----------- |
| `def`     | `ArrowDefinition` | —           |

> **Notes:** Currently a typed identity, retained to align registry APIs and allow future validation or normalization

### definePathGenerator

<p>
  <ApiSourceLink
    label={'definePathGenerator'}
    path={'packages/kernel/core/src/contract/path-generator/define.ts'}
    startLine={11}
  >
    Define a path-generator registry entry
  </ApiSourceLink>
</p>

```ts
export declare const definePathGenerator: (def: PathGeneratorDefinition) => PathGeneratorDefinition;
```

#### Parameters

| Parameter | Type                      | Description |
| --------- | ------------------------- | ----------- |
| `def`     | `PathGeneratorDefinition` | —           |

#### Throws

- When name is empty

> **Notes:** JSON-safe validation of generator output remains the responsibility of compilation
