---
title: 复合
description: composite 让 domain 包用可序列化的 Tier 2 节点保留领域意图，再通过注册的 lowering 在 compile 期下沉为 core Tier 1 图元。
---

retikz 的 IR 子节点分两层：**Tier 1** 是 core 内置的 `node` / `path` / `coordinate` / `scope`；**Tier 2 composite** 是 domain 包提供的高层节点，例如 `@retikz/plot` 的整张统计图 spec。

Tier 2 节点用 `namespace + type` 判别，拥有完整 schema，并在 compile 开始时下沉为 Tier 1。下沉之后，布局、anchor、zIndex 与 renderer 都复用 core 的现有能力；core 不需要认识 chart、tree 等领域语义。

<ComponentAlert
  type="tip"
  title="先画图的人可以先跳过"
  description="本页只建立 Tier 2 的心智模型。直接使用现成组件时不需要手写 composite；等你要持久化高层 IR、接入 domain 包或编写扩展时再回来。"
/>

## 为什么需要 composite

直接保存几十个矩形和连线，会丢失“这是一张图表 / 一棵树”的高层意图。composite 把领域字段保留在可序列化 IR 中，再把具体几何延迟到 compile 期生成：

<ComponentPreview files="composite-pipeline" size="xs" hideCode />

图中的 `lower` 是关键边界：上游仍能存储、传输、diff 或由 AI 生成高层节点；下游只接收统一的 Tier 1，因此所有 renderer 共享同一份展开结果。

适合 composite 的能力通常同时满足三点：

- 一个高层节点会按数据或算法生成数量不固定的 Tier 1 图元；
- 领域 schema 比展开后的几何更能表达用户意图；
- 展开结果应被 React、Vanilla、SVG、Canvas 等入口共同复用。

## 最小注册与下沉

`defineComposite({ namespace, type, schema, expand })` 把 schema 和展开函数注册为同一项；`schema` 可以是单个 object，也可以是共享同一 `namespace` / `type` literal 的直接 object union。每个分支的 literal 都必须与 definition key 一致，`expand` 接收校验后的节点并返回 Tier 1。union 中出现非 object 分支、空 literal 或混合 key 时会立即报错。

Core 只按顶层 `namespace + type` 路由 composite，不读取领域 schema 内部的判别字段。例如 Table 可以在同一个 `table.table` composite 下定义多种 spec 分支，但 `structure.kind` 的解释与缩窄仍由 Table 包负责。

<ComponentPreview files="composite-basic" size="sm" />

| 字段        | 职责                                       | 约束                                  |
| ----------- | ------------------------------------------ | ------------------------------------- |
| `namespace` | 划分 domain                                | 与 schema literal 一致                |
| `type`      | 标识 domain 内的节点类型                   | 与 schema literal 一致                |
| `schema`    | 校验完整高层节点并填充默认值               | object 或共享 key 的直接 object union |
| `expand`    | 生成一个或多个 Tier 1，也可继续生成 Tier 2 | 最终必须收敛到 Tier 1                 |

编译器按 `${namespace}.${type}` 查注册表，校验节点后调用 `expand`；嵌套 composite 会继续展开，直到只剩 Tier 1。未注册节点产生 warning 并跳过，循环或超过深度限制则报错。完整字段、诊断与独立 lowering 入口见[扩展运行时](/kernel/reference/runtime/extensions)。

## 与 Sugar 的区别

|          | Sugar（如 `<Draw>`） | Tier 2 composite                |
| -------- | -------------------- | ------------------------------- |
| IR 中    | 构建期已展平         | 保留高层节点                    |
| 下沉时机 | adapter 构建期       | core compile 期                 |
| 适合     | 可直接映射的便捷写法 | 含布局、scale、聚合等算法的结构 |

<Comparison target="tikz" title="Tier 2 与 PGFPlots">

PGFPlots 位于 TikZ 基础图元之上；同样地，`@retikz/plot` 通过 composite 把 grammar-of-graphics spec 下沉为 core 图元。区别在于 retikz 把高层节点保留为可校验、可序列化的 IR，并让 lowering 成为所有后端共享的 compile 步骤。

</Comparison>

## 在 React `<Layout>` 中使用

普通用户只需要把支持嵌入的 domain 组件放进 `<Layout>`。组件所属包负责贡献高层节点与 composite definition，`<Layout>` 再把它们交给同一条 core lowering 管线；core 仍不依赖具体 domain。

如果你在实现 domain adapter、注入 `composites` / `embeddables`，或排查 datasets 汇总与宿主行为，请直接查 [Layout 高级能力与宿主接入](/kernel/components/layout/overview#高级能力与宿主接入)和[扩展运行时](/kernel/reference/runtime/extensions)，概念页不展开 React 静态遍历与聚合细节。

<SourceLinks
  sources={[
    {
      label: '校验 definition key 与 schema literal',
      path: 'packages/kernel/core/src/contract/composite/define.ts',
      startLine: 21,
      endLine: 32,
    },
    {
      label: '递归下沉 Tier 2 composite',
      path: 'packages/kernel/core/src/compile/orchestration/composite.ts',
      startLine: 23,
      endLine: 72,
    },
    {
      label: '在 compile 上下文注入 composite 与 provider',
      path: 'packages/kernel/core/src/compile/orchestration/context.ts',
      startLine: 88,
      endLine: 115,
    },
  ]}
/>

## 延伸阅读

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/kernel/concepts/design/layers">
    <span className="font-semibold">分层</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">DSL、IR、Scene 三层模型与 lowering 所在位置</span>
  </LinkedCard>
  <LinkedCard href="/kernel/reference/runtime/extensions">
    <span className="font-semibold">扩展运行时</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">查看 registry、注入入口与扩展诊断约定</span>
  </LinkedCard>
  <LinkedCard href="/viz/introduction">
    <span className="font-semibold">@retikz/plot</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">查看 composite 在图表 domain 中的实际使用</span>
  </LinkedCard>
</div>
