{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/math`

### AffineMatrix

<p>
  <ApiSourceLink label={'AffineMatrix'} path={'packages/kernel/math/src/transforms/affine.ts'} startLine={4}>
    SVG / Canvas 同序的二维仿射矩阵 `[a,b,c,d,e,f]`
  </ApiSourceLink>
</p>

```ts
export type AffineMatrix = readonly [number, number, number, number, number, number];
```

### ArcBoundingCandidatesInput

<p>
  <ApiSourceLink
    label={'ArcBoundingCandidatesInput'}
    path={'packages/kernel/math/src/primitives/arc.ts'}
    startLine={11}
  >
    圆弧外接候选点参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型       | 默认值 | 说明               |
| --------------- | ---------- | ------ | ------------------ |
| `center`        | `Position` | —      | 圆心               |
| `endAngleDeg`   | `number`   | —      | 结束角度，单位为度 |
| `radius`        | `number`   | —      | 半径               |
| `startAngleDeg` | `number`   | —      | 起始角度，单位为度 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ArcBoundingCandidatesInput = {
  center: Position;
  radius: number;
  startAngleDeg: number;
  endAngleDeg: number;
};
```

</DocTab>

</DocTabs>

### ArcSweepAngleInput

<p>
  <ApiSourceLink label={'ArcSweepAngleInput'} path={'packages/kernel/math/src/primitives/arc.ts'} startLine={23}>
    圆弧角度区间判定参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型     | 默认值 | 说明                 |
| --------------- | -------- | ------ | -------------------- |
| `angleDeg`      | `number` | —      | 待判定角度，单位为度 |
| `endAngleDeg`   | `number` | —      | 结束角度，单位为度   |
| `startAngleDeg` | `number` | —      | 起始角度，单位为度   |
| `toleranceDeg?` | `number` | —      | 角度容差，单位为度   |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ArcSweepAngleInput = {
  startAngleDeg: number;
  endAngleDeg: number;
  angleDeg: number;
  toleranceDeg?: number;
};
```

</DocTab>

</DocTabs>

### AxisAlignedBounds

<p>
  <ApiSourceLink label={'AxisAlignedBounds'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={4}>
    轴对齐外接范围
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员   | 类型     | 默认值 | 说明        |
| ------ | -------- | ------ | ----------- |
| `maxX` | `number` | —      | 最大 x 坐标 |
| `maxY` | `number` | —      | 最大 y 坐标 |
| `minX` | `number` | —      | 最小 x 坐标 |
| `minY` | `number` | —      | 最小 y 坐标 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type AxisAlignedBounds = {
  minX: number;
  minY: number;
  maxX: number;
  maxY: number;
};
```

</DocTab>

</DocTabs>

### BoundsHalfAxes

<p>
  <ApiSourceLink label={'BoundsHalfAxes'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={28}>
    轴对齐外接范围半轴
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员         | 类型     | 默认值 | 说明       |
| ------------ | -------- | ------ | ---------- |
| `halfHeight` | `number` | —      | y 方向半高 |
| `halfWidth`  | `number` | —      | x 方向半宽 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BoundsHalfAxes = {
  halfWidth: number;
  halfHeight: number;
};
```

</DocTab>

</DocTabs>

### BoundsInsets

<p>
  <ApiSourceLink label={'BoundsInsets'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={36}>
    轴对齐外接范围外扩量
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员     | 类型     | 默认值 | 说明         |
| -------- | -------- | ------ | ------------ |
| `bottom` | `number` | —      | 向下外扩距离 |
| `left`   | `number` | —      | 向左外扩距离 |
| `right`  | `number` | —      | 向右外扩距离 |
| `top`    | `number` | —      | 向上外扩距离 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BoundsInsets = {
  left: number;
  right: number;
  top: number;
  bottom: number;
};
```

</DocTab>

</DocTabs>

### BoundsRect

<p>
  <ApiSourceLink label={'BoundsRect'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={16}>
    左上角 + 尺寸表示的轴对齐外接矩形
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员     | 类型     | 默认值 | 说明          |
| -------- | -------- | ------ | ------------- |
| `height` | `number` | —      | 矩形高度      |
| `width`  | `number` | —      | 矩形宽度      |
| `x`      | `number` | —      | 左上角 x 坐标 |
| `y`      | `number` | —      | 左上角 y 坐标 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type BoundsRect = {
  x: number;
  y: number;
  width: number;
  height: number;
};
```

</DocTab>

</DocTabs>

### CenteredBox

<p>
  <ApiSourceLink label={'CenteredBox'} path={'packages/kernel/math/src/primitives/ellipse.ts'} startLine={15}>
    以中心描述的矩形盒
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员      | 类型     | 默认值 | 说明                                      |
| --------- | -------- | ------ | ----------------------------------------- |
| `height`  | `number` | —      | 矩形盒高度                                |
| `rotate?` | `number` | —      | 绕中心旋转弧度（可选，0 / 缺省 = 不旋转） |
| `width`   | `number` | —      | 矩形盒宽度                                |
| `x`       | `number` | —      | 中心横坐标                                |
| `y`       | `number` | —      | 中心纵坐标                                |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CenteredBox = CenteredShape & {
  width: number;
  height: number;
};
```

</DocTab>

</DocTabs>

### CenteredShape

<p>
  <ApiSourceLink label={'CenteredShape'} path={'packages/kernel/math/src/transforms/coordinate.ts'} startLine={4}>
    任何"中心 + 可选旋转"形状的几何契约（rect / circle / ellipse / diamond 共用）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员      | 类型     | 默认值 | 说明                                      |
| --------- | -------- | ------ | ----------------------------------------- |
| `rotate?` | `number` | —      | 绕中心旋转弧度（可选，0 / 缺省 = 不旋转） |
| `x`       | `number` | —      | 中心横坐标                                |
| `y`       | `number` | —      | 中心纵坐标                                |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CenteredShape = {
  x: number;
  y: number;
  rotate?: number;
};
```

</DocTab>

</DocTabs>

### Circle

<p>
  <ApiSourceLink label={'Circle'} path={'packages/kernel/math/src/algorithms/circle.ts'} startLine={7}>
    圆：圆心 + 半径
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员     | 类型       | 默认值 | 说明 |
| -------- | ---------- | ------ | ---- |
| `center` | `Position` | —      | —    |
| `radius` | `number`   | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type Circle = {
  center: Position;
  radius: number;
};
```

</DocTab>

</DocTabs>

### CircleCircleInput

<p>
  <ApiSourceLink label={'CircleCircleInput'} path={'packages/kernel/math/src/algorithms/intersect.ts'} startLine={30}>
    两圆求交参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员      | 类型       | 默认值 | 说明         |
| --------- | ---------- | ------ | ------------ |
| `centerA` | `Position` | —      | 第一个圆心   |
| `centerB` | `Position` | —      | 第二个圆心   |
| `radiusA` | `number`   | —      | 第一个圆半径 |
| `radiusB` | `number`   | —      | 第二个圆半径 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CircleCircleInput = {
  centerA: Position;
  radiusA: number;
  centerB: Position;
  radiusB: number;
};
```

</DocTab>

</DocTabs>

### CircularArcCurveSegment

<p>
  <ApiSourceLink label={'CircularArcCurveSegment'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={48}>
    圆弧曲线段
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员                | 类型       | 默认值 | 说明                                   |
| ------------------- | ---------- | ------ | -------------------------------------- |
| `center`            | `Position` | —      | 圆心                                   |
| `counterClockwise?` | `boolean`  | —      | 是否逆时针扫描；省略时由起止角方向决定 |
| `endAngleDeg`       | `number`   | —      | 结束参数角，单位为度                   |
| `kind`              | `'arc'`    | —      | 曲线种类                               |
| `radius`            | `number`   | —      | 半径                                   |
| `startAngleDeg`     | `number`   | —      | 起始参数角，单位为度                   |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CircularArcCurveSegment = {
  kind: 'arc';
  center: Position;
  radius: number;
  startAngleDeg: number;
  endAngleDeg: number;
  counterClockwise?: boolean;
};
```

</DocTab>

</DocTabs>

### CubicBezierCurveSegment

<p>
  <ApiSourceLink label={'CubicBezierCurveSegment'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={31}>
    三次贝塞尔曲线段
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型            | 默认值 | 说明         |
| ---------- | --------------- | ------ | ------------ |
| `control1` | `Position`      | —      | 第一个控制点 |
| `control2` | `Position`      | —      | 第二个控制点 |
| `from`     | `Position`      | —      | 起点         |
| `kind`     | `'cubicBezier'` | —      | 曲线种类     |
| `to`       | `Position`      | —      | 终点         |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CubicBezierCurveSegment = {
  kind: 'cubicBezier';
  from: Position;
  control1: Position;
  control2: Position;
  to: Position;
};
```

</DocTab>

</DocTabs>

### CubicSegment

<p>
  <ApiSourceLink label={'CubicSegment'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={45}>
    一段三次贝塞尔：两控制点 + 终点（起点为上一段终点 / 首段为第一个 knot）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型       | 默认值 | 说明 |
| ---------- | ---------- | ------ | ---- |
| `control1` | `Position` | —      | —    |
| `control2` | `Position` | —      | —    |
| `to`       | `Position` | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CubicSegment = {
  control1: Position;
  control2: Position;
  to: Position;
};
```

</DocTab>

</DocTabs>

### CurveApproximationOptions

<p>
  <ApiSourceLink
    label={'CurveApproximationOptions'}
    path={'packages/kernel/math/src/algorithms/curve.ts'}
    startLine={100}
  >
    曲线长度近似配置
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员           | 类型     | 默认值 | 说明                                |
| -------------- | -------- | ------ | ----------------------------------- |
| `sampleCount?` | `number` | —      | Bezier 与椭圆弧使用的等参数采样数量 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CurveApproximationOptions = {
  sampleCount?: number;
};
```

</DocTab>

</DocTabs>

### CurveParameterAtDistanceOptions

<p>
  <ApiSourceLink
    label={'CurveParameterAtDistanceOptions'}
    path={'packages/kernel/math/src/algorithms/curve.ts'}
    startLine={106}
  >
    曲线距离到参数反解配置
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员              | 类型     | 默认值 | 说明                                               |
| ----------------- | -------- | ------ | -------------------------------------------------- |
| `bisectionSteps?` | `number` | —      | 二分反解最多执行次数                               |
| `sampleCount?`    | `number` | —      | Bezier 与椭圆弧使用的等参数采样数量                |
| `totalLength?`    | `number` | —      | 已按同一采样预算计算的完整曲线长度，省略时即时计算 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CurveParameterAtDistanceOptions = CurveApproximationOptions & {
  totalLength?: number;
  bisectionSteps?: number;
};
```

</DocTab>

</DocTabs>

### CurveSegment

<p>
  <ApiSourceLink label={'CurveSegment'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={84}>
    可参数化、可切片的零依赖曲线段
  </ApiSourceLink>
</p>

```ts
export type CurveSegment =
  | LineCurveSegment
  | QuadraticBezierCurveSegment
  | CubicBezierCurveSegment
  | CircularArcCurveSegment
  | EllipseArcCurveSegment;
```

### CurveSegmentSample

<p>
  <ApiSourceLink label={'CurveSegmentSample'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={92}>
    曲线参数采样结果
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员      | 类型       | 默认值 | 说明                                            |
| --------- | ---------- | ------ | ----------------------------------------------- |
| `point`   | `Position` | —      | 参数位置对应的点                                |
| `tangent` | `Vector2`  | —      | 沿曲线前进方向的单位切线；零导数回退为 `[1, 0]` |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CurveSegmentSample = {
  point: Position;
  tangent: Vector2;
};
```

</DocTab>

</DocTabs>

### Ellipse

<p>
  <ApiSourceLink label={'Ellipse'} path={'packages/kernel/math/src/primitives/ellipse.ts'} startLine={7}>
    椭圆：中心 + 半轴 + 可选旋转
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员      | 类型     | 默认值 | 说明                                      |
| --------- | -------- | ------ | ----------------------------------------- |
| `rotate?` | `number` | —      | 绕中心旋转弧度（可选，0 / 缺省 = 不旋转） |
| `rx`      | `number` | —      | 沿本地 +x 的半轴长度                      |
| `ry`      | `number` | —      | 沿本地 +y 的半轴长度                      |
| `x`       | `number` | —      | 中心横坐标                                |
| `y`       | `number` | —      | 中心纵坐标                                |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type Ellipse = CenteredShape & {
  rx: number;
  ry: number;
};
```

</DocTab>

</DocTabs>

### EllipseArcAnglePointInput

<p>
  <ApiSourceLink label={'EllipseArcAnglePointInput'} path={'packages/kernel/math/src/primitives/arc.ts'} startLine={35}>
    椭圆弧参数点参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型       | 默认值 | 说明             |
| ---------- | ---------- | ------ | ---------------- |
| `angleDeg` | `number`   | —      | 参数角，单位为度 |
| `center`   | `Position` | —      | 椭圆中心         |
| `radiusX`  | `number`   | —      | x 方向半轴       |
| `radiusY`  | `number`   | —      | y 方向半轴       |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type EllipseArcAnglePointInput = {
  center: Position;
  radiusX: number;
  radiusY: number;
  angleDeg: number;
};
```

</DocTab>

</DocTabs>

### EllipseArcBoundingCandidatesInput

<p>
  <ApiSourceLink
    label={'EllipseArcBoundingCandidatesInput'}
    path={'packages/kernel/math/src/primitives/arc.ts'}
    startLine={47}
  >
    椭圆弧外接候选点参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型       | 默认值 | 说明                 |
| --------------- | ---------- | ------ | -------------------- |
| `center`        | `Position` | —      | 椭圆中心             |
| `endAngleDeg`   | `number`   | —      | 结束参数角，单位为度 |
| `radiusX`       | `number`   | —      | x 方向半轴           |
| `radiusY`       | `number`   | —      | y 方向半轴           |
| `startAngleDeg` | `number`   | —      | 起始参数角，单位为度 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type EllipseArcBoundingCandidatesInput = Omit<EllipseArcAnglePointInput, 'angleDeg'> & {
  startAngleDeg: number;
  endAngleDeg: number;
};
```

</DocTab>

</DocTabs>

### EllipseArcCurveSegment

<p>
  <ApiSourceLink label={'EllipseArcCurveSegment'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={64}>
    椭圆弧曲线段
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员                | 类型           | 默认值 | 说明                                   |
| ------------------- | -------------- | ------ | -------------------------------------- |
| `center`            | `Position`     | —      | 椭圆中心                               |
| `counterClockwise?` | `boolean`      | —      | 是否逆时针扫描；省略时由起止角方向决定 |
| `endAngleDeg`       | `number`       | —      | 结束参数角，单位为度                   |
| `kind`              | `'ellipseArc'` | —      | 曲线种类                               |
| `radiusX`           | `number`       | —      | 本地 x 半轴                            |
| `radiusY`           | `number`       | —      | 本地 y 半轴                            |
| `rotationDeg?`      | `number`       | —      | 本地椭圆相对世界坐标的旋转角，单位为度 |
| `startAngleDeg`     | `number`       | —      | 起始参数角，单位为度                   |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type EllipseArcCurveSegment = {
  kind: 'ellipseArc';
  center: Position;
  radiusX: number;
  radiusY: number;
  rotationDeg?: number;
  startAngleDeg: number;
  endAngleDeg: number;
  counterClockwise?: boolean;
};
```

</DocTab>

</DocTabs>

### EllipseCircumscribeMode

<p>
  <ApiSourceLink
    label={'EllipseCircumscribeMode'}
    path={'packages/kernel/math/src/primitives/ellipse.ts'}
    startLine={23}
  >
    椭圆外接内部盒的半轴策略
  </ApiSourceLink>
</p>

```ts
export type EllipseCircumscribeMode = 'proportional' | 'equal';
```

### LineCircleInput

<p>
  <ApiSourceLink label={'LineCircleInput'} path={'packages/kernel/math/src/algorithms/intersect.ts'} startLine={18}>
    直线与圆求交参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型       | 默认值 | 说明                   |
| ----------- | ---------- | ------ | ---------------------- |
| `center`    | `Position` | —      | 圆心                   |
| `direction` | `Position` | —      | 直线方向，不要求单位化 |
| `origin`    | `Position` | —      | 直线起点               |
| `radius`    | `number`   | —      | 圆半径                 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LineCircleInput = {
  origin: Position;
  direction: Position;
  center: Position;
  radius: number;
};
```

</DocTab>

</DocTabs>

### LineCurveSegment

<p>
  <ApiSourceLink label={'LineCurveSegment'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={9}>
    直线曲线段
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员   | 类型       | 默认值 | 说明     |
| ------ | ---------- | ------ | -------- |
| `from` | `Position` | —      | 起点     |
| `kind` | `'line'`   | —      | 曲线种类 |
| `to`   | `Position` | —      | 终点     |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LineCurveSegment = {
  kind: 'line';
  from: Position;
  to: Position;
};
```

</DocTab>

</DocTabs>

### LineLineInput

<p>
  <ApiSourceLink label={'LineLineInput'} path={'packages/kernel/math/src/algorithms/intersect.ts'} startLine={6}>
    两条无限直线求交参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员 | 类型       | 默认值 | 说明                   |
| ---- | ---------- | ------ | ---------------------- |
| `a1` | `Position` | —      | 第一条直线上的第一个点 |
| `a2` | `Position` | —      | 第一条直线上的第二个点 |
| `b1` | `Position` | —      | 第二条直线上的第一个点 |
| `b2` | `Position` | —      | 第二条直线上的第二个点 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LineLineInput = {
  a1: Position;
  a2: Position;
  b1: Position;
  b2: Position;
};
```

</DocTab>

</DocTabs>

### Position

<p>
  <ApiSourceLink label={'Position'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={7}>
    笛卡尔坐标点，格式为 `[x, y]`
  </ApiSourceLink>
</p>

```ts
export type Position = Vector2;
```

### QuadraticBezierCurveSegment

<p>
  <ApiSourceLink
    label={'QuadraticBezierCurveSegment'}
    path={'packages/kernel/math/src/algorithms/curve.ts'}
    startLine={19}
  >
    二次贝塞尔曲线段
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员      | 类型                | 默认值 | 说明     |
| --------- | ------------------- | ------ | -------- |
| `control` | `Position`          | —      | 控制点   |
| `from`    | `Position`          | —      | 起点     |
| `kind`    | `'quadraticBezier'` | —      | 曲线种类 |
| `to`      | `Position`          | —      | 终点     |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type QuadraticBezierCurveSegment = {
  kind: 'quadraticBezier';
  from: Position;
  control: Position;
  to: Position;
};
```

</DocTab>

</DocTabs>

### RayArcIntersectionInput

<p>
  <ApiSourceLink
    label={'RayArcIntersectionInput'}
    path={'packages/kernel/math/src/algorithms/arc-intersection.ts'}
    startLine={6}
  >
    射线与圆弧求交参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员            | 类型       | 默认值 | 说明                   |
| --------------- | ---------- | ------ | ---------------------- |
| `center`        | `Position` | —      | 圆心                   |
| `direction`     | `Position` | —      | 射线方向，不要求单位化 |
| `endAngleDeg`   | `number`   | —      | 结束角度，单位为度     |
| `origin`        | `Position` | —      | 射线起点               |
| `radius`        | `number`   | —      | 半径                   |
| `startAngleDeg` | `number`   | —      | 起始角度，单位为度     |
| `tolerance?`    | `number`   | —      | 正向参数容差           |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type RayArcIntersectionInput = ArcBoundingCandidatesInput & {
  origin: Position;
  direction: Position;
  tolerance?: number;
};
```

</DocTab>

</DocTabs>

### Vector2

<p>
  <ApiSourceLink label={'Vector2'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={4}>
    二维向量，格式为 `[x, y]`
  </ApiSourceLink>
</p>

```ts
export type Vector2 = [number, number];
```

### AFFINE_IDENTITY

<p>
  <ApiSourceLink label={'AFFINE_IDENTITY'} path={'packages/kernel/math/src/transforms/affine.ts'} startLine={7}>
    运行时不可变的二维仿射单位矩阵
  </ApiSourceLink>
</p>

```ts
export const AFFINE_IDENTITY: AffineMatrix = Object.freeze([1, 0, 0, 1, 0, 0]);
```

### circle

<p>
  <ApiSourceLink label={'circle'} path={'packages/kernel/math/src/algorithms/circle.ts'} startLine={41}>
    圆相关几何算法
  </ApiSourceLink>
</p>

```ts
export declare const circle: { minimalEnclosing: (points: Array<Vector2>, epsilon: number) => Circle | null };
```

### curve

<p>
  <ApiSourceLink label={'curve'} path={'packages/kernel/math/src/algorithms/curve.ts'} startLine={402}>
    可参数化曲线的纯几何运算
  </ApiSourceLink>
</p>

```ts
export declare const curve: {
  approximateLength: (segment: CurveSegment, options: CurveApproximationOptions) => number;
  catmullRomToCubic: (knots: Array<Vector2>, tension: number) => Array<CubicSegment>;
  parameterAtDistance: (segment: CurveSegment, distance: number, options: CurveParameterAtDistanceOptions) => number;
  sampleAt: (segment: CurveSegment, parameter: number) => CurveSegmentSample;
  slice: (segment: CurveSegment, fromParameter: number, toParameter: number) => CurveSegment;
};
```

### DEFAULT_EPSILON

<p>
  <ApiSourceLink label={'DEFAULT_EPSILON'} path={'packages/kernel/math/src/constants.ts'} startLine={2}>
    几何工具默认容差
  </ApiSourceLink>
</p>

```ts
export const DEFAULT_EPSILON = 1e-9;
```

### ellipse

<p>
  <ApiSourceLink label={'ellipse'} path={'packages/kernel/math/src/primitives/ellipse.ts'} startLine={26}>
    基于中心、本地半轴和可选旋转的椭圆运算
  </ApiSourceLink>
</p>

```ts
export declare const ellipse: {
  boundaryPoint: (e: Ellipse, toward: Vector2) => Vector2;
  center: (e: Ellipse) => Vector2;
  circumscribedHalfAxes: (innerHalfAxes: BoundsHalfAxes, mode: EllipseCircumscribeMode) => BoundsHalfAxes;
  contains: (e: Ellipse, p: Vector2) => boolean;
  inscribedInBox: (box: CenteredBox) => Ellipse;
};
```

### intersect

<p><ApiSourceLink label={"intersect"} path={"packages/kernel/math/src/algorithms/intersect.ts"} startLine={114}>求交原语集（line / circle / segment），统一返回点（`Position | null` / `Array<Position>`）</ApiSourceLink></p>

ray∩arc 的返回值是沿射线的标量参数 `Array<number>`，因此由 `./arc-intersection` 单独导出

```ts
export const intersect = { lineLine, lineCircle, circleCircle, segmentSegment };
```

### point

<p>
  <ApiSourceLink label={'point'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={55}>
    点的位置关系运算；所有方法都返回新 tuple，不修改输入
  </ApiSourceLink>
</p>

```ts
export declare const point: {
  against: (origin: Vector2, direction: Vector2, length: number) => Vector2;
  along: (origin: Vector2, direction: Vector2, length: number) => Vector2;
  distance: (a: Vector2, b: Vector2) => number;
  isEqual: (a: Vector2, b: Vector2) => boolean;
  shiftToward: (sourcePoint: Vector2, targetPoint: Vector2, distance: number) => Vector2;
};
```

### polygon

<p>
  <ApiSourceLink label={'polygon'} path={'packages/kernel/math/src/algorithms/polygon.ts'} startLine={4}>
    简单多边形运算
  </ApiSourceLink>
</p>

```ts
export declare const polygon: { containsPoint: (vertices: Array<Vector2>, p: Vector2) => boolean };
```

### triangle

<p>
  <ApiSourceLink label={'triangle'} path={'packages/kernel/math/src/algorithms/triangle.ts'} startLine={7}>
    三角形外接圆与内切圆构造
  </ApiSourceLink>
</p>

```ts
export declare const triangle: {
  circumCircle: (a: Vector2, b: Vector2, c: Vector2) => Circle | null;
  incircle: (a: Vector2, b: Vector2, c: Vector2) => Circle | null;
};
```

### vector2

<p>
  <ApiSourceLink label={'vector2'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={20}>
    二维向量运算；所有方法都返回新 tuple，不修改输入
  </ApiSourceLink>
</p>

```ts
export declare const vector2: {
  add: (a: Vector2, b: Vector2) => Vector2;
  cross: (a: Vector2, b: Vector2) => number;
  dot: (a: Vector2, b: Vector2) => number;
  fromAngleDegrees: (angle: number) => Vector2;
  length: (a: Vector2) => number;
  normal: (a: Vector2) => Vector2;
  normalize: (a: Vector2, fallback: Vector2, epsilon: number) => Vector2;
  normalizeOrNull: (a: Vector2, epsilon: number) => Vector2 | null;
  scale: (a: Vector2, k: number) => Vector2;
  sub: (a: Vector2, b: Vector2) => Vector2;
};
```

### applyAffine

<p>
  <ApiSourceLink label={'applyAffine'} path={'packages/kernel/math/src/transforms/affine.ts'} startLine={23}>
    按 SVG / Canvas 六元组公式把二维点映射到新坐标
  </ApiSourceLink>
</p>

```ts
export const applyAffine = (matrix: AffineMatrix, point: Vector2) => Vector2;
```

#### 参数

| 参数     | 类型           | 说明 |
| -------- | -------------- | ---- |
| `matrix` | `AffineMatrix` | —    |
| `point`  | `Vector2`      | —    |

### boundsOf

<p>
  <ApiSourceLink label={'boundsOf'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={51}>
    点集的轴对齐外接范围
  </ApiSourceLink>
</p>

输入为空时返回 undefined；调用方按自身语义决定兜底、报错或忽略

```ts
export const boundsOf = (points: readonly Array<Vector2>) => AxisAlignedBounds | undefined;
```

#### 参数

| 参数     | 类型                      | 说明 |
| -------- | ------------------------- | ---- |
| `points` | `readonly Array<Vector2>` | —    |

### boundsToRect

<p>
  <ApiSourceLink label={'boundsToRect'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={79}>
    将 min/max bounds 转成左上角 + 尺寸矩形
  </ApiSourceLink>
</p>

```ts
export const boundsToRect = (__namedParameters: AxisAlignedBounds) => BoundsRect;
```

#### 参数

| 参数                | 类型                | 说明 |
| ------------------- | ------------------- | ---- |
| `__namedParameters` | `AxisAlignedBounds` | —    |

### centerOfBounds

<p>
  <ApiSourceLink label={'centerOfBounds'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={103}>
    轴对齐外接范围中心
  </ApiSourceLink>
</p>

```ts
export const centerOfBounds = (__namedParameters: AxisAlignedBounds) => Vector2;
```

#### 参数

| 参数                | 类型                | 说明 |
| ------------------- | ------------------- | ---- |
| `__namedParameters` | `AxisAlignedBounds` | —    |

### collectArcBoundingCandidates

<p>
  <ApiSourceLink
    label={'collectArcBoundingCandidates'}
    path={'packages/kernel/math/src/primitives/arc.ts'}
    startLine={85}
  >
    弧的 bbox 极值候选：起点、终点，加 [startAngle,endAngle] 内所有 90°·k 方向的圆周点
  </ApiSourceLink>
</p>

不去重；端角恰在 90°·k 上时由调用方处理

```ts
export const collectArcBoundingCandidates = (__namedParameters: ArcBoundingCandidatesInput) => Array<Vector2>;
```

#### 参数

| 参数                | 类型                         | 说明 |
| ------------------- | ---------------------------- | ---- |
| `__namedParameters` | `ArcBoundingCandidatesInput` | —    |

### collectEllipseArcBoundingCandidates

<p>
  <ApiSourceLink
    label={'collectEllipseArcBoundingCandidates'}
    path={'packages/kernel/math/src/primitives/arc.ts'}
    startLine={140}
  >
    椭圆弧 bbox 极值候选：起点、终点，加 [start,end] 区间内所有 90°·k 参数角处的椭圆周点
  </ApiSourceLink>
</p>

只处理轴对齐椭圆弧，候选点不去重

```ts
export const collectEllipseArcBoundingCandidates = (__namedParameters: EllipseArcBoundingCandidatesInput) =>
  Array<Vector2>;
```

#### 参数

| 参数                | 类型                                | 说明 |
| ------------------- | ----------------------------------- | ---- |
| `__namedParameters` | `EllipseArcBoundingCandidatesInput` | —    |

### convexHull

<p>
  <ApiSourceLink label={'convexHull'} path={'packages/kernel/math/src/algorithms/hull.ts'} startLine={10}>
    凸包（Andrew's monotone chain）
  </ApiSourceLink>
</p>

返回 CCW 顺序顶点、不含共线中间点；点数 < 3 时返回按 (x,y) 排序去重后的点。
全部点共线时退化为两端点

```ts
export const convexHull = (points: Array<Vector2>) => Array<Vector2>;
```

#### 参数

| 参数     | 类型             | 说明 |
| -------- | ---------------- | ---- |
| `points` | `Array<Vector2>` | —    |

> **备注：** 复杂度：时间 O(n log n)，空间 O(n)，n 为输入点数

### cornersOfBounds

<p>
  <ApiSourceLink label={'cornersOfBounds'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={126}>
    轴对齐外接范围的四个角点
  </ApiSourceLink>
</p>

```ts
export const cornersOfBounds = (__namedParameters: AxisAlignedBounds) => Array<Vector2>;
```

#### 参数

| 参数                | 类型                | 说明 |
| ------------------- | ------------------- | ---- |
| `__namedParameters` | `AxisAlignedBounds` | —    |

### expandBounds

<p>
  <ApiSourceLink label={'expandBounds'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={115}>
    按四边外扩轴对齐外接范围
  </ApiSourceLink>
</p>

```ts
export const expandBounds = (__namedParameters: AxisAlignedBounds, __namedParameters: BoundsInsets) =>
  AxisAlignedBounds;
```

#### 参数

| 参数                | 类型                | 说明 |
| ------------------- | ------------------- | ---- |
| `__namedParameters` | `AxisAlignedBounds` | —    |
| `__namedParameters` | `BoundsInsets`      | —    |

### getAffineSimilarityScale

<p>
  <ApiSourceLink
    label={'getAffineSimilarityScale'}
    path={'packages/kernel/math/src/transforms/affine.ts'}
    startLine={35}
  >
    返回仿射相似变换的统一缩放因子，不符合相似变换时返回 undefined
  </ApiSourceLink>
</p>

```ts
export const getAffineSimilarityScale = (matrix: AffineMatrix) => number | undefined;
```

#### 参数

| 参数     | 类型           | 说明 |
| -------- | -------------- | ---- |
| `matrix` | `AffineMatrix` | —    |

### halfAxesOfBounds

<p>
  <ApiSourceLink label={'halfAxesOfBounds'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={109}>
    轴对齐外接范围半轴
  </ApiSourceLink>
</p>

```ts
export const halfAxesOfBounds = (__namedParameters: AxisAlignedBounds) => BoundsHalfAxes;
```

#### 参数

| 参数                | 类型                | 说明 |
| ------------------- | ------------------- | ---- |
| `__namedParameters` | `AxisAlignedBounds` | —    |

### intersectRayWithArc

<p>
  <ApiSourceLink
    label={'intersectRayWithArc'}
    path={'packages/kernel/math/src/algorithms/arc-intersection.ts'}
    startLine={19}
  >
    射线（origin + s·direction）∩ 圆弧（center, radius, [startAngle, endAngle]）
  </ApiSourceLink>
</p>

返回沿射线的正向参数 s，按升序排列；零方向或无有效交点时返回空数组

```ts
export const intersectRayWithArc = (__namedParameters: RayArcIntersectionInput) => Array<number>;
```

#### 参数

| 参数                | 类型                      | 说明 |
| ------------------- | ------------------------- | ---- |
| `__namedParameters` | `RayArcIntersectionInput` | —    |

### isAngleWithinArcSweep

<p>
  <ApiSourceLink label={'isAngleWithinArcSweep'} path={'packages/kernel/math/src/primitives/arc.ts'} startLine={112}>
    角度 a（度）是否落在弧的角度区间 [startAngle, endAngle] 内（含端点，带容差）
  </ApiSourceLink>
</p>

start 到 end 为正时按屏幕顺时针扫描，为负时按逆时针扫描

```ts
export const isAngleWithinArcSweep = (__namedParameters: ArcSweepAngleInput) => boolean;
```

#### 参数

| 参数                | 类型                 | 说明 |
| ------------------- | -------------------- | ---- |
| `__namedParameters` | `ArcSweepAngleInput` | —    |

### isFiniteBoundsRect

<p>
  <ApiSourceLink label={'isFiniteBoundsRect'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={95}>
    判断 bounds rect 四个字段是否都是 finite number
  </ApiSourceLink>
</p>

```ts
export const isFiniteBoundsRect = (__namedParameters: BoundsRect) => boolean;
```

#### 参数

| 参数                | 类型         | 说明 |
| ------------------- | ------------ | ---- |
| `__namedParameters` | `BoundsRect` | —    |

### isFiniteNonSingularAffine

<p>
  <ApiSourceLink
    label={'isFiniteNonSingularAffine'}
    path={'packages/kernel/math/src/transforms/affine.ts'}
    startLine={29}
  >
    判断二维仿射矩阵是否由有限数值组成且具有非零行列式
  </ApiSourceLink>
</p>

```ts
export const isFiniteNonSingularAffine = (matrix: AffineMatrix) => boolean;
```

#### 参数

| 参数     | 类型           | 说明 |
| -------- | -------------- | ---- |
| `matrix` | `AffineMatrix` | —    |

### isFiniteNumber

<p>
  <ApiSourceLink label={'isFiniteNumber'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={10}>
    有限数值守卫，会排除 Infinity 和 NaN
  </ApiSourceLink>
</p>

```ts
export const isFiniteNumber = (value: unknown) => predicate;
```

#### 参数

| 参数    | 类型      | 说明 |
| ------- | --------- | ---- |
| `value` | `unknown` | —    |

### isFinitePoint

<p>
  <ApiSourceLink label={'isFinitePoint'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={13}>
    有限二维点守卫
  </ApiSourceLink>
</p>

```ts
export const isFinitePoint = (value: unknown) => predicate;
```

#### 参数

| 参数    | 类型      | 说明 |
| ------- | --------- | ---- |
| `value` | `unknown` | —    |

### isInfiniteNumber

<p>
  <ApiSourceLink label={'isInfiniteNumber'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={17}>
    无限数值守卫，仅接受正负 Infinity
  </ApiSourceLink>
</p>

```ts
export const isInfiniteNumber = (value: unknown) => predicate;
```

#### 参数

| 参数    | 类型      | 说明 |
| ------- | --------- | ---- |
| `value` | `unknown` | —    |

### isPositiveBoundsRect

<p>
  <ApiSourceLink label={'isPositiveBoundsRect'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={99}>
    判断 bounds rect 是否 finite 且宽高严格大于 0
  </ApiSourceLink>
</p>

```ts
export const isPositiveBoundsRect = (rect: BoundsRect) => boolean;
```

#### 参数

| 参数   | 类型         | 说明 |
| ------ | ------------ | ---- |
| `rect` | `BoundsRect` | —    |

### lerp

<p>
  <ApiSourceLink label={'lerp'} path={'packages/kernel/math/src/primitives/point.ts'} startLine={76}>
    线性插值：a + (b - a) * t
  </ApiSourceLink>
</p>

```ts
export const lerp = (a: Vector2, b: Vector2, t: number) => Vector2;
```

#### 参数

| 参数 | 类型      | 说明 |
| ---- | --------- | ---- |
| `a`  | `Vector2` | —    |
| `b`  | `Vector2` | —    |
| `t`  | `number`  | —    |

### localToWorld

<p>
  <ApiSourceLink label={'localToWorld'} path={'packages/kernel/math/src/transforms/coordinate.ts'} startLine={17}>
    本地坐标（以中心为原点）→ 世界坐标
  </ApiSourceLink>
</p>

rotate=0 / 缺省时退化为平移，否则绕中心旋转后再平移

```ts
export const localToWorld = (shape: CenteredShape, local: Vector2) => Vector2;
```

#### 参数

| 参数    | 类型            | 说明 |
| ------- | --------------- | ---- |
| `shape` | `CenteredShape` | —    |
| `local` | `Vector2`       | —    |

### mergeBounds

<p>
  <ApiSourceLink label={'mergeBounds'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={67}>
    合并两个轴对齐外接范围
  </ApiSourceLink>
</p>

```ts
export const mergeBounds = (a?: AxisAlignedBounds, b?: AxisAlignedBounds) => AxisAlignedBounds | undefined;
```

#### 参数

| 参数 | 类型                | 说明 |
| ---- | ------------------- | ---- |
| `a`  | `AxisAlignedBounds` | —    |
| `b`  | `AxisAlignedBounds` | —    |

### multiplyAffine

<p>
  <ApiSourceLink label={'multiplyAffine'} path={'packages/kernel/math/src/transforms/affine.ts'} startLine={13}>
    复合二维仿射矩阵
  </ApiSourceLink>
</p>

返回 `outer × inner`，即对点先应用 `inner`，再应用 `outer`

```ts
export const multiplyAffine = (outer: AffineMatrix, inner: AffineMatrix) => AffineMatrix;
```

#### 参数

| 参数    | 类型           | 说明 |
| ------- | -------------- | ---- |
| `outer` | `AffineMatrix` | —    |
| `inner` | `AffineMatrix` | —    |

### pointAtArcAngle

<p>
  <ApiSourceLink label={'pointAtArcAngle'} path={'packages/kernel/math/src/primitives/arc.ts'} startLine={76}>
    圆心、半径、角度（度，与 polar.toPosition 同约定）→ 圆周上对应点
  </ApiSourceLink>
</p>

```ts
export const pointAtArcAngle = (center: Vector2, radius: number, angleDeg: number) => Vector2;
```

#### 参数

| 参数       | 类型      | 说明 |
| ---------- | --------- | ---- |
| `center`   | `Vector2` | —    |
| `radius`   | `number`  | —    |
| `angleDeg` | `number`  | —    |

### pointAtEllipseArcAngle

<p>
  <ApiSourceLink label={'pointAtEllipseArcAngle'} path={'packages/kernel/math/src/primitives/arc.ts'} startLine={131}>
    椭圆弧参数点：中心 + 半轴 rx/ry + 参数角（度）→ 椭圆周上点
  </ApiSourceLink>
</p>

与 pointAtArcAngle 同角度约定；θ 是参数角，不一定等于真实极角

```ts
export const pointAtEllipseArcAngle = (__namedParameters: EllipseArcAnglePointInput) => Vector2;
```

#### 参数

| 参数                | 类型                        | 说明 |
| ------------------- | --------------------------- | ---- |
| `__namedParameters` | `EllipseArcAnglePointInput` | —    |

### rectToBounds

<p>
  <ApiSourceLink label={'rectToBounds'} path={'packages/kernel/math/src/primitives/bounds.ts'} startLine={87}>
    将左上角 + 尺寸矩形转成 min/max bounds
  </ApiSourceLink>
</p>

```ts
export const rectToBounds = (__namedParameters: BoundsRect) => AxisAlignedBounds;
```

#### 参数

| 参数                | 类型         | 说明 |
| ------------------- | ------------ | ---- |
| `__namedParameters` | `BoundsRect` | —    |

### worldToLocal

<p>
  <ApiSourceLink label={'worldToLocal'} path={'packages/kernel/math/src/transforms/coordinate.ts'} startLine={29}>
    世界坐标 → 本地坐标（`localToWorld` 逆变换）
  </ApiSourceLink>
</p>

返回以形状中心和旋转为基准的本地坐标

```ts
export const worldToLocal = (shape: CenteredShape, world: Vector2) => Vector2;
```

#### 参数

| 参数    | 类型            | 说明 |
| ------- | --------------- | ---- |
| `shape` | `CenteredShape` | —    |
| `world` | `Vector2`       | —    |
