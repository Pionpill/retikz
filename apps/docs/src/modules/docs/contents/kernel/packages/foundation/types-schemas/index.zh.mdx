---
title: 类型与契约
description: 复用 Foundation 的编译期类型工具与开放字符串构造器，表达跨包通用契约
---

Foundation 将可在多个包之间复用的类型工具与开放字符串构造器放在一起。它们只表达通用数据契约；JSON 和数值的运行时字段、默认值与校验结构见 [Schema 参考](/kernel/packages/foundation/schema-reference)。

## 基础用法

以下能力统一从包根入口导入：

```ts
import { type WithRequiredProperties, createOpenStringSchema } from '@retikz/foundation';
```

## 类型工具

这些类型仅在编译期生效，用来描述或收窄跨包契约。

| 类型                              | 作用                                   |
| --------------------------------- | -------------------------------------- |
| `ValueOf<T>`                      | 取得对象所有 value 的联合类型          |
| `AssertEqual<TActual, TExpected>` | 双向检查两个类型是否等价               |
| `OpenString<T>`                   | 保留已知字符串提示，同时接受任意字符串 |
| `WithRequiredProperties<T, TKey>` | 将指定属性收窄为必填，同时保留其余属性 |
| `WithOptionalProperties<T, TKey>` | 将指定属性扩展为可选，同时保留其余属性 |
| `NonEmptyReadonlyArray<T>`        | 表示至少包含一个元素的只读数组         |

例如，在局部流程已经保证 `id` 存在后，可以用 `WithRequiredProperties` 将该事实传递给后续函数：

```ts
type NodeInput = { id?: string; label: string };
type IdentifiedNode = WithRequiredProperties<NodeInput, 'id'>;

const useNode = (node: IdentifiedNode) => node.id.toUpperCase();
```

反向地，作者侧契约需要省略少量已有字段时，使用 `WithOptionalProperties`，而不把整个对象变为 `Partial`：

```ts
type NodePatch = WithOptionalProperties<NodeInput, 'label'>;
```

## 开放字符串

`createOpenStringSchema(values)` 将 const object enum 的内置值与非空白自定义字符串合并。它适合协议允许扩展值、但仍希望编辑器提示内置值的字段。

```ts
const Role = {
  Participant: 'participant',
  Activity: 'activity',
} as const;

const RoleSchema = createOpenStringSchema(Role);

RoleSchema.parse('participant');
RoleSchema.parse('custom.role');
```

## 延伸阅读

<LinkedSections
  items={[
    {
      title: 'Schema 参考',
      description: '查询 JSON 与标量 schema 的字段和校验结构',
      url: '/kernel/packages/foundation/schema-reference',
    },
    { title: 'API 参考', description: '查询公开类型与完整契约', url: '/kernel/packages/foundation/api-reference' },
  ]}
/>
