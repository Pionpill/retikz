---
title: Foundation · foundation
description: Browse the foundational contracts and shared utilities provided by @retikz/foundation
---

`@retikz/foundation` provides domain-neutral contracts that every package can reuse.

## Install and use

<PackageManagerInstall packages="@retikz/foundation" />

```ts
import { cloneAndFreezeJson } from '@retikz/foundation';

const source = { id: 'diagram', settings: { grid: true } };
const snapshot = cloneAndFreezeJson(source, 'diagram config');

source.settings.grid = false;
snapshot.settings.grid; // true
Object.isFrozen(snapshot.settings); // true
```

The example above illustrates one utility. Foundation provides reusable, business-domain-neutral capabilities for the Retikz ecosystem.

<SourceLinks
  sources={[
    {
      label: 'Foundation root entry',
      path: 'packages/kernel/foundation/src/index.ts',
      startLine: 1,
      endLine: 7,
    },
    {
      label: 'Root-only and subpath contract tests',
      path: 'packages/kernel/foundation/tests/public-surface.test.ts',
      startLine: 4,
      endLine: 38,
    },
  ]}
/>

<ComponentAlert
  type="tip"
  title="For Retikz packages and extension authors"
  description="Foundation serves Retikz internal packages and community extension authors. It is not a general-purpose business utility library and makes no compatibility or maintenance commitment for use outside the Retikz ecosystem."
/>

## Responsibilities

| Document            | Responsibility                                          | Reach for it when                   |
| ------------------- | ------------------------------------------------------- | ----------------------------------- |
| Utilities           | Readonly collections, JSON snapshots, and static colors | Reusing general utilities           |
| Types & Schemas     | Compile-time types and runtime schemas                  | Reusing foundational data contracts |
| Validation & Errors | Structured errors and scalar assertions                 | Defining a failure boundary         |

## Contents

<LinkedSections
  items={[
    {
      title: 'Utilities',
      description: 'Collection, JSON, and static-color utilities',
      url: '/kernel/packages/foundation/utilities',
    },
    {
      title: 'Types & Schemas',
      description: 'Type utilities and runtime schemas',
      url: '/kernel/packages/foundation/types-schemas',
    },
    {
      title: 'Validation & Errors',
      description: 'Structured errors and scalar assertions',
      url: '/kernel/packages/foundation/validation-errors',
    },
  ]}
/>
