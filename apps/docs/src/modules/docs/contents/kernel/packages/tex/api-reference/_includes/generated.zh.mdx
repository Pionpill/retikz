{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## 根入口 `@retikz/tex`

### LowerTexOptions

<p>
  <ApiSourceLink label={'LowerTexOptions'} path={'packages/kernel/tex/src/lower/types.ts'} startLine={17}>
    TeX lowerer 配置
  </ApiSourceLink>
</p>

控制由 `createLowerTex` 创建的 lowerer 如何向外报告失败。它不改变 TeX 解析或缓存语义，成功结果仍直接返回给 Core 的文本编译流程

| 成员            | 类型                                          | 默认值 | 说明                                                         |
| --------------- | --------------------------------------------- | ------ | ------------------------------------------------------------ |
| `onDiagnostic?` | `(diagnostic: TexLoweringDiagnostic) => void` | —      | 每次公开 lowering 失败时接收一次诊断，供应用记录、提示或上报 |

### MathJaxEngineOptions

<p>
  <ApiSourceLink label={'MathJaxEngineOptions'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={34}>
    MathJax 引擎配置
  </ApiSourceLink>
</p>

指定内置配置档及其额外扩展，供 `createMathJaxEngine`、`createMathJaxLowerTex` 与 React Hook 在初始化时解析。它不包含 MathJax 实例，也不控制公式绘制样式

| 成员          | 类型                           | 默认值   | 说明                                           |
| ------------- | ------------------------------ | -------- | ---------------------------------------------- |
| `extensions?` | `Array<MathJaxExtensionValue>` | `[]`     | 在配置档之后追加的扩展，重复项会在初始化前去重 |
| `profile?`    | `MathJaxProfileValue`          | `'base'` | 选择基础或数学扩展集合的内置配置档             |

### MathJaxExtensionValue

<p>
  <ApiSourceLink label={'MathJaxExtensionValue'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={17}>
    MathJax 扩展取值
  </ApiSourceLink>
</p>

由 `MathJaxExtension` 派生的受限字符串集合，可在内置配置档之后追加。配置解析会去重并按稳定顺序加载，调用方无需自行处理 package 顺序

```ts
ValueOf<typeof MathJaxExtension>;
```

### MathJaxLowerTexOptions

<p>
  <ApiSourceLink
    label={'MathJaxLowerTexOptions'}
    path={'packages/kernel/tex/src/lower/create-mathjax.ts'}
    startLine={14}
  >
    一步创建 MathJax 引擎与 Core lowerer 的配置
  </ApiSourceLink>
</p>

合并 `MathJaxEngineOptions` 的解析配置与 `LowerTexOptions` 的诊断回调，作为 `createMathJaxLowerTex` 的单一输入。它只描述工厂参数，不保存引擎、缓存或渲染状态

```ts
MathJaxEngineOptions & LowerTexOptions;
```

### MathJaxProfileValue

<p>
  <ApiSourceLink label={'MathJaxProfileValue'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={10}>
    MathJax 配置档取值
  </ApiSourceLink>
</p>

由 `MathJaxProfile` 派生的受限字符串集合，用于选择内置扩展组合；它只描述配置值，实际扩展加载由 MathJax 工厂负责

```ts
ValueOf<typeof MathJaxProfile>;
```

### MathJaxSvgEngine

<p>
  <ApiSourceLink label={'MathJaxSvgEngine'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={24}>
    同步 TeX → SVG 引擎接口
  </ApiSourceLink>
</p>

定义 `createLowerTex` 所需的最小引擎边界：输入 TeX 字符串和 display 选项，输出可由本包 SVG lowerer 消费的标记。调用方可使用内置 MathJax 工厂，也可注入兼容实现；通用 SVG 导入与渲染不属于该接口

| 成员      | 类型                                                     | 默认值 | 说明                                              |
| --------- | -------------------------------------------------------- | ------ | ------------------------------------------------- |
| `convert` | `(tex: string, options: { display: boolean }) => string` | —      | 把一段 TeX 和 display 模式转换成受支持的 SVG 标记 |

### TexLoweringDiagnostic

<p>
  <ApiSourceLink label={'TexLoweringDiagnostic'} path={'packages/kernel/tex/src/lower/types.ts'} startLine={6}>
    TeX lowering 诊断
  </ApiSourceLink>
</p>

描述引擎执行、MathJax 解析或 SVG 降解阶段不能产出公式路径的原因。它只承载可展示或记录的失败事实，不负责错误恢复；调用方可通过 `LowerTexOptions.onDiagnostic` 决定如何提示用户

```ts
{ kind: 'engine-error'; message: string; source: string } | { kind: 'mathjax-error'; message: string; source: string } | { kind: 'unsupported-svg'; message: string; source: string } | { kind: 'malformed-svg'; message: string; source: string }
```

### MathJaxExtension

<p>
  <ApiSourceLink label={'MathJaxExtension'} path={'packages/kernel/tex/src/mathjax/constants.ts'} startLine={16}>
    可按需加载的 MathJax TeX 扩展
  </ApiSourceLink>
</p>

值与 MathJax configuration 的扩展标识保持一致，并由 profile 解析阶段统一校验、去重和加载。它提供可传给引擎配置的稳定扩展标识

```ts
{
  Ams: 'ams';
  Boldsymbol: 'boldsymbol';
  Braket: 'braket';
  Cancel: 'cancel';
  Cases: 'cases';
  Centernot: 'centernot';
  Color: 'color';
  Mathtools: 'mathtools';
  Newcommand: 'newcommand';
}
```

### MathJaxProfile

<p>
  <ApiSourceLink label={'MathJaxProfile'} path={'packages/kernel/tex/src/mathjax/constants.ts'} startLine={5}>
    内置 MathJax 配置档
  </ApiSourceLink>
</p>

`base` 仅启用基础 TeX 配置，`math` 额外启用常用数学扩展集合。它提供 `createMathJaxEngine` 与 lowerer 工厂可识别的扩展集合选择，不创建引擎，也不改变已创建实例

```ts
{
  Base: 'base';
  Math: 'math';
}
```

### createLowerTex

<p>
  <ApiSourceLink label={'createLowerTex'} path={'packages/kernel/tex/src/lower/lower-tex.ts'} startLine={24}>
    把同步 SVG engine 适配为 Core `LowerTex`，并缓存确定的解析结果
  </ApiSourceLink>
</p>

接收能输出受支持 SVG 子集的引擎与可选诊断回调，返回可直接注入 Core 文本编译流程的同步 lowerer。该函数负责 TeX → SVG → 路径的降解和结果缓存，不负责初始化引擎或渲染公式

```ts
(engine: MathJaxSvgEngine, options?: LowerTexOptions) => LowerTex;
```

#### 参数

| 参数      | 类型               | 说明                              |
| --------- | ------------------ | --------------------------------- |
| `engine`  | `MathJaxSvgEngine` | 提供同步 TeX → SVG 转换能力的引擎 |
| `options` | `LowerTexOptions`  | 控制 lowering 失败诊断的可选配置  |

#### 返回值

可注入 Core 文本编译流程的同步 `LowerTex`

#### 用法

```ts
import { createLowerTex, createMathJaxEngine, MathJaxProfile } from '@retikz/tex';

const engine = await createMathJaxEngine({ profile: MathJaxProfile.Math });
const lowerTex = createLowerTex(engine, {
  onDiagnostic: diagnostic => console.warn(diagnostic.message),
});
```

### createMathJaxEngine

<p>
  <ApiSourceLink label={'createMathJaxEngine'} path={'packages/kernel/tex/src/mathjax/engine.ts'} startLine={43}>
    创建基于可选 `mathjax-full` peer 的同步 TeX → SVG 引擎
  </ApiSourceLink>
</p>

异步加载并配置 `mathjax-full`，返回可被 `createLowerTex` 反复同步调用的引擎。输入配置只影响启用的 TeX 扩展；返回值只负责 TeX → SVG，不承担 SVG 路径降解、缓存或 Core 注入

```ts
(options?: MathJaxEngineOptions) => Promise<MathJaxSvgEngine>;
```

#### 参数

| 参数      | 类型                   | 说明                                       |
| --------- | ---------------------- | ------------------------------------------ |
| `options` | `MathJaxEngineOptions` | 控制内置 profile 与附加 TeX 扩展的可选配置 |

#### 返回值

可同步执行 TeX → SVG 转换的 `MathJaxSvgEngine`

#### 异常

- `mathjax-full` 无法加载或初始化时

> **备注：** 使用字面量 dynamic import 支持打包器分包；`fontCache: 'none'` 让字形以内联路径输出

#### 用法

```ts
import { createMathJaxEngine, MathJaxExtension } from '@retikz/tex';

const engine = await createMathJaxEngine({
  extensions: [MathJaxExtension.Ams],
});
const svg = engine.convert('\\frac{1}{2}', { display: false });
```

### createMathJaxLowerTex

<p>
  <ApiSourceLink
    label={'createMathJaxLowerTex'}
    path={'packages/kernel/tex/src/lower/create-mathjax.ts'}
    startLine={32}
  >
    创建使用内置 MathJax profile 的 Core lowerer
  </ApiSourceLink>
</p>

异步加载可选的 `mathjax-full`、建立同步 SVG 引擎，并返回可交给 Core 的 `LowerTex`。适合不需要替换引擎实现的场景；如果应用已有兼容引擎，使用 `createLowerTex` 保留引擎所有权

```ts
(options?: MathJaxLowerTexOptions) => Promise<LowerTex>;
```

#### 参数

| 参数      | 类型                     | 说明                                            |
| --------- | ------------------------ | ----------------------------------------------- |
| `options` | `MathJaxLowerTexOptions` | 同时控制 MathJax 扩展和 lowering 诊断的可选配置 |

#### 返回值

建立完成后可注入 Core 文本编译流程的 `LowerTex`

#### 异常

- `mathjax-full` 无法加载或初始化时

#### 用法

```ts
import { createMathJaxLowerTex, MathJaxProfile } from '@retikz/tex';

const lowerTex = await createMathJaxLowerTex({
  profile: MathJaxProfile.Math,
  onDiagnostic: diagnostic => console.warn(diagnostic.message),
});
```

## React 子路径 `@retikz/tex/react`

### MathJaxLowerTexState

<p>
  <ApiSourceLink label={'MathJaxLowerTexState'} path={'packages/kernel/tex/src/react/use-lower-tex.ts'} startLine={21}>
    MathJax lowerer 的异步初始化状态
  </ApiSourceLink>
</p>

表示当前 React 组件对应配置的引擎与 lowerer 是否可用：loading 时尚未完成初始化，ready 提供可传给 `@retikz/react` 的 `lowerTex`，error 提供可展示的诊断。它是 Hook 的输出快照，不保存跨组件的 UI 状态

```ts
{ status: 'loading' } | { lowerTex: LowerTex; status: 'ready' } | { diagnostic: TexLoweringDiagnostic; status: 'error' }
```

### useLowerTex

<p>
  <ApiSourceLink label={'useLowerTex'} path={'packages/kernel/tex/src/react/use-lower-tex.ts'} startLine={75}>
    按有效配置共享 MathJax engine，并异步创建当前组件的 lowerer
  </ApiSourceLink>
</p>

接收与根入口工厂一致的 MathJax 配置，复用同一有效扩展集合的引擎，并返回可判别的初始化状态。ready 状态中的 lowerer 应传入 `Layout` 等 React authoring 入口；本 Hook 不渲染公式，也不处理应用级错误界面

```ts
(options?: MathJaxLowerTexOptions) => MathJaxLowerTexState;
```

#### 参数

| 参数      | 类型                     | 说明                                        |
| --------- | ------------------------ | ------------------------------------------- |
| `options` | `MathJaxLowerTexOptions` | 控制 MathJax 扩展和 lowering 诊断的可选配置 |

#### 返回值

当前组件对应配置的 MathJax lowerer 初始化状态

#### 用法

```ts
import type { FC } from 'react';

import { Layout } from '@retikz/react';
import { MathJaxProfile } from '@retikz/tex';
import { useLowerTex } from '@retikz/tex/react';

const Formula: FC = () => {
  const lowerTexState = useLowerTex({ profile: MathJaxProfile.Math });
  if (lowerTexState.status !== 'ready') return <Layout />;

  return <Layout lowerTex={lowerTexState.lowerTex}>{'公式：$x^2$'}</Layout>;
};
```
