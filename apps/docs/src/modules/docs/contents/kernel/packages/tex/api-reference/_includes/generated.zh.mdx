{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/tex`

### LowerTexOptions

<p>
  <ApiSourceLink label={'LowerTexOptions'} path={'packages/kernel/tex/src/lower/types.ts'} startLine={45}>
    TeX lowerer 配置
  </ApiSourceLink>
</p>

控制由 `createLowerTex` 创建的 lowerer 如何向外报告失败。它不改变 TeX 解析或缓存语义，成功结果仍直接返回给 Core 的文本编译流程

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员            | 类型                                          | 默认值 | 说明                                                         |
| --------------- | --------------------------------------------- | ------ | ------------------------------------------------------------ |
| `onDiagnostic?` | `(diagnostic: TexLoweringDiagnostic) => void` | —      | 每次公开 lowering 失败时接收一次诊断，供应用记录、提示或上报 |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LowerTexOptions = {
  onDiagnostic?: (diagnostic: TexLoweringDiagnostic) => void;
};
```

</DocTab>

</DocTabs>

### MathJaxEngineOptions

<p>
  <ApiSourceLink label={'MathJaxEngineOptions'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={34}>
    MathJax 引擎配置
  </ApiSourceLink>
</p>

指定内置配置档及其额外扩展，供 `createMathJaxEngine`、`createMathJaxLowerTex` 与 React Hook 在初始化时解析。它不包含 MathJax 实例，也不控制公式绘制样式

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员          | 类型                           | 默认值   | 说明                                           |
| ------------- | ------------------------------ | -------- | ---------------------------------------------- |
| `extensions?` | `Array<MathJaxExtensionValue>` | `[]`     | 在配置档之后追加的扩展，重复项会在初始化前去重 |
| `profile?`    | `MathJaxProfileValue`          | `'base'` | 选择基础或数学扩展集合的内置配置档             |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MathJaxEngineOptions = {
  profile?: MathJaxProfileValue;
  extensions?: Array<MathJaxExtensionValue>;
};
```

</DocTab>

</DocTabs>

### MathJaxExtensionValue

<p>
  <ApiSourceLink label={'MathJaxExtensionValue'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={17}>
    MathJax 扩展取值
  </ApiSourceLink>
</p>

由 `MathJaxExtension` 派生的受限字符串集合，可在内置配置档之后追加。配置解析会去重并按稳定顺序加载，调用方无需自行处理 package 顺序

```ts
export type MathJaxExtensionValue = ValueOf<typeof MathJaxExtension>;
```

### MathJaxLowerTexOptions

<p>
  <ApiSourceLink
    label={'MathJaxLowerTexOptions'}
    path={'packages/kernel/tex/src/lower/create-mathjax.ts'}
    startLine={13}
  >
    一步创建 MathJax 引擎与 Core lowerer 的配置
  </ApiSourceLink>
</p>

合并 `MathJaxEngineOptions` 的解析配置与 `LowerTexOptions` 的诊断回调，作为 `createMathJaxLowerTex` 的单一输入。它只描述工厂参数，不保存引擎、缓存或渲染状态

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员            | 类型                                          | 默认值   | 说明                                                         |
| --------------- | --------------------------------------------- | -------- | ------------------------------------------------------------ |
| `extensions?`   | `Array<MathJaxExtensionValue>`                | `[]`     | 在配置档之后追加的扩展，重复项会在初始化前去重               |
| `onDiagnostic?` | `(diagnostic: TexLoweringDiagnostic) => void` | —        | 每次公开 lowering 失败时接收一次诊断，供应用记录、提示或上报 |
| `profile?`      | `MathJaxProfileValue`                         | `'base'` | 选择基础或数学扩展集合的内置配置档                           |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MathJaxLowerTexOptions = MathJaxEngineOptions & LowerTexOptions;
```

</DocTab>

</DocTabs>

### MathJaxProfileValue

<p>
  <ApiSourceLink label={'MathJaxProfileValue'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={10}>
    MathJax 配置档取值
  </ApiSourceLink>
</p>

由 `MathJaxProfile` 派生的受限字符串集合，用于选择内置扩展组合；它只描述配置值，实际扩展加载由 MathJax 工厂负责

<DocTabs defaultValue="expanded">

<DocTab value="expanded" label="展开类型">

```ts
export type MathJaxProfileValue = 'base' | 'math';
```

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MathJaxProfileValue = ValueOf<typeof MathJaxProfile>;
```

</DocTab>

</DocTabs>

### MathJaxSvgEngine

<p>
  <ApiSourceLink label={'MathJaxSvgEngine'} path={'packages/kernel/tex/src/mathjax/types.ts'} startLine={24}>
    同步 TeX → SVG 引擎接口
  </ApiSourceLink>
</p>

定义 `createLowerTex` 所需的最小引擎边界：输入 TeX 字符串和 display 选项，输出可由本包 SVG lowerer 消费的标记。调用方可使用内置 MathJax 工厂，也可注入兼容实现；通用 SVG 导入与渲染不属于该接口

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable>

| 成员      | 类型                                                                    | 默认值 | 说明                                              |
| --------- | ----------------------------------------------------------------------- | ------ | ------------------------------------------------- |
| `convert` | `(tex: string, options: {`<br />`display: boolean;`<br />`}) => string` | —      | 把一段 TeX 和 display 模式转换成受支持的 SVG 标记 |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MathJaxSvgEngine = {
  convert: (
    tex: string,
    options: {
      display: boolean;
    },
  ) => string;
};
```

</DocTab>

</DocTabs>

### TexLoweringDiagnostic

<p>
  <ApiSourceLink label={'TexLoweringDiagnostic'} path={'packages/kernel/tex/src/lower/types.ts'} startLine={6}>
    TeX lowering 诊断
  </ApiSourceLink>
</p>

描述引擎执行、MathJax 解析或 SVG 降解阶段不能产出公式路径的原因。它只承载可展示或记录的失败事实，不负责错误恢复；调用方可通过 `LowerTexOptions.onDiagnostic` 决定如何提示用户

<DocTabs defaultValue="engine-error">

<DocTab value="engine-error" label="属性 · 引擎失败">

<ApiTable>

| 成员      | 类型             | 默认值 | 说明                                          |
| --------- | ---------------- | ------ | --------------------------------------------- |
| `kind`    | `'engine-error'` | —      | 引擎初始化或执行失败                          |
| `message` | `string`         | —      | 可供记录或展示的失败原因                      |
| `source`  | `string`         | —      | 失败的 TeX 源文本；引擎初始化失败时为空字符串 |

</ApiTable>

</DocTab>

<DocTab value="mathjax-error" label="属性 · 公式解析失败">

<ApiTable>

| 成员      | 类型              | 默认值 | 说明                                          |
| --------- | ----------------- | ------ | --------------------------------------------- |
| `kind`    | `'mathjax-error'` | —      | MathJax 返回公式解析错误                      |
| `message` | `string`          | —      | 可供记录或展示的失败原因                      |
| `source`  | `string`          | —      | 失败的 TeX 源文本；引擎初始化失败时为空字符串 |

</ApiTable>

</DocTab>

<DocTab value="unsupported-svg" label="属性 · 不支持的 SVG">

<ApiTable>

| 成员      | 类型                | 默认值 | 说明                                          |
| --------- | ------------------- | ------ | --------------------------------------------- |
| `kind`    | `'unsupported-svg'` | —      | 引擎输出包含不支持的 SVG 结构                 |
| `message` | `string`            | —      | 可供记录或展示的失败原因                      |
| `source`  | `string`            | —      | 失败的 TeX 源文本；引擎初始化失败时为空字符串 |

</ApiTable>

</DocTab>

<DocTab value="malformed-svg" label="属性 · 无效 SVG">

<ApiTable>

| 成员      | 类型              | 默认值 | 说明                                          |
| --------- | ----------------- | ------ | --------------------------------------------- |
| `kind`    | `'malformed-svg'` | —      | 引擎输出的 SVG 结构无效                       |
| `message` | `string`          | —      | 可供记录或展示的失败原因                      |
| `source`  | `string`          | —      | 失败的 TeX 源文本；引擎初始化失败时为空字符串 |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type TexLoweringDiagnostic =
  | {
      kind: 'engine-error';
      source: string;
      message: string;
    }
  | {
      kind: 'mathjax-error';
      source: string;
      message: string;
    }
  | {
      kind: 'unsupported-svg';
      source: string;
      message: string;
    }
  | {
      kind: 'malformed-svg';
      source: string;
      message: string;
    };
```

</DocTab>

</DocTabs>

### MathJaxExtension

<p>
  <ApiSourceLink label={'MathJaxExtension'} path={'packages/kernel/tex/src/mathjax/constants.ts'} startLine={16}>
    可按需加载的 MathJax TeX 扩展
  </ApiSourceLink>
</p>

值与 MathJax configuration 的扩展标识保持一致，并由 profile 解析阶段统一校验、去重和加载。它提供可传给引擎配置的稳定扩展标识

```ts
export const MathJaxExtension = {
  Ams: 'ams',
  Newcommand: 'newcommand',
  Boldsymbol: 'boldsymbol',
  Braket: 'braket',
  Cancel: 'cancel',
  Cases: 'cases',
  Centernot: 'centernot',
  Mathtools: 'mathtools',
  Color: 'color',
} as const;
```

### MathJaxProfile

<p>
  <ApiSourceLink label={'MathJaxProfile'} path={'packages/kernel/tex/src/mathjax/constants.ts'} startLine={5}>
    内置 MathJax 配置档
  </ApiSourceLink>
</p>

`base` 仅启用基础 TeX 配置，`math` 额外启用常用数学扩展集合。它提供 `createMathJaxEngine` 与 lowerer 工厂可识别的扩展集合选择，不创建引擎，也不改变已创建实例

```ts
export const MathJaxProfile = {
  Base: 'base',
  Math: 'math',
} as const;
```

### createLowerTex

<p>
  <ApiSourceLink label={'createLowerTex'} path={'packages/kernel/tex/src/lower/lower-tex.ts'} startLine={23}>
    把同步 SVG engine 适配为 Core `LowerTex`，并缓存确定的解析结果
  </ApiSourceLink>
</p>

接收能输出受支持 SVG 子集的引擎与可选诊断回调，返回可直接注入 Core 文本编译流程的同步 lowerer。该函数负责 TeX → SVG → 路径的降解和结果缓存，不负责初始化引擎或渲染公式

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable variant="parameters">

| 类别   | 名称       | 类型 / 签名        | 说明                                                                               |
| ------ | ---------- | ------------------ | ---------------------------------------------------------------------------------- |
| 参数   | `engine`   | `MathJaxSvgEngine` | 提供同步 TeX → SVG 转换能力的引擎                                                  |
|        | `options?` | `LowerTexOptions`  | 控制 lowering 失败诊断的可选配置                                                   |
| 返回值 | —          | `LowerTex`         | 同步 LowerTex；转换失败时返回 null 并通知 onDiagnostic，缓存命中的失败也会再次通知 |

</ApiTable>

**用法**

```ts
import { createLowerTex, createMathJaxEngine, MathJaxProfile } from '@retikz/tex';

const engine = await createMathJaxEngine({ profile: MathJaxProfile.Math });
const lowerTex = createLowerTex(engine, {
  onDiagnostic: diagnostic => console.warn(diagnostic.message),
});
```

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const createLowerTex: (engine: MathJaxSvgEngine, options?: LowerTexOptions) => LowerTex;
```

</DocTab>

</DocTabs>

### createMathJaxEngine

<p>
  <ApiSourceLink label={'createMathJaxEngine'} path={'packages/kernel/tex/src/mathjax/engine.ts'} startLine={42}>
    创建基于可选 `mathjax-full` peer 的同步 TeX → SVG 引擎
  </ApiSourceLink>
</p>

异步加载并配置 `mathjax-full`，返回可被 `createLowerTex` 反复同步调用的引擎。输入配置只影响启用的 TeX 扩展；返回值只负责 TeX → SVG，不承担 SVG 路径降解、缓存或 Core 注入

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable variant="parameters">

| 类别   | 名称       | 类型 / 签名                 | 说明                                                            |
| ------ | ---------- | --------------------------- | --------------------------------------------------------------- |
| 参数   | `options?` | `MathJaxEngineOptions`      | 控制内置 profile 与附加 TeX 扩展的可选配置                      |
| 返回值 | —          | `Promise<MathJaxSvgEngine>` | Promise 在引擎初始化完成后兑现为同步 TeX → SVG 引擎             |
| 错误   | —          | —                           | 引擎加载或初始化失败时 Promise 拒绝；模块加载失败保留原始 cause |

</ApiTable>

> **备注：** 使用字面量 dynamic import 支持打包器分包；`fontCache: 'none'` 让字形以内联路径输出

**用法**

```ts
import { createMathJaxEngine, MathJaxExtension } from '@retikz/tex';

const engine = await createMathJaxEngine({
  extensions: [MathJaxExtension.Ams],
});
const svg = engine.convert('\\frac{1}{2}', { display: false });
```

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const createMathJaxEngine: (options?: MathJaxEngineOptions) => Promise<MathJaxSvgEngine>;
```

</DocTab>

</DocTabs>

### createMathJaxLowerTex

<p>
  <ApiSourceLink
    label={'createMathJaxLowerTex'}
    path={'packages/kernel/tex/src/lower/create-mathjax.ts'}
    startLine={31}
  >
    创建使用内置 MathJax profile 的 Core lowerer
  </ApiSourceLink>
</p>

异步加载可选的 `mathjax-full`、建立同步 SVG 引擎，并返回可交给 Core 的 `LowerTex`。适合不需要替换引擎实现的场景；如果应用已有兼容引擎，使用 `createLowerTex` 保留引擎所有权

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable variant="parameters">

| 类别   | 名称       | 类型 / 签名              | 说明                                                                            |
| ------ | ---------- | ------------------------ | ------------------------------------------------------------------------------- |
| 参数   | `options?` | `MathJaxLowerTexOptions` | 同时控制 MathJax 扩展和 lowering 诊断的可选配置                                 |
| 返回值 | —          | `Promise<LowerTex>`      | Promise 在引擎初始化完成后兑现为可注入 Core 的同步 LowerTex                     |
| 错误   | —          | —                        | 引擎初始化失败时先通知 onDiagnostic，再拒绝 Promise；此时诊断 source 为空字符串 |

</ApiTable>

**用法**

```ts
import { createMathJaxLowerTex, MathJaxProfile } from '@retikz/tex';

const lowerTex = await createMathJaxLowerTex({
  profile: MathJaxProfile.Math,
  onDiagnostic: diagnostic => console.warn(diagnostic.message),
});
```

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const createMathJaxLowerTex: (options?: MathJaxLowerTexOptions) => Promise<LowerTex>;
```

</DocTab>

</DocTabs>

## `@retikz/tex/react`

### MathJaxLowerTexState

<p>
  <ApiSourceLink label={'MathJaxLowerTexState'} path={'packages/kernel/tex/src/react/use-lower-tex.ts'} startLine={19}>
    MathJax lowerer 的异步初始化状态
  </ApiSourceLink>
</p>

表示当前 React 组件对应配置的引擎与 lowerer 是否可用：loading 时尚未完成初始化，ready 提供可传给 `@retikz/react` 的 `lowerTex`，error 提供可展示的诊断。它是 Hook 的输出快照，不保存跨组件的 UI 状态

<DocTabs defaultValue="loading">

<DocTab value="loading" label="属性 · 初始化中">

<ApiTable>

| 成员     | 类型        | 默认值 | 说明                         |
| -------- | ----------- | ------ | ---------------------------- |
| `status` | `'loading'` | —      | 当前配置的引擎尚未完成初始化 |

</ApiTable>

</DocTab>

<DocTab value="ready" label="属性 · 就绪">

<ApiTable>

| 成员       | 类型       | 默认值 | 说明                                                                     |
| ---------- | ---------- | ------ | ------------------------------------------------------------------------ |
| `lowerTex` | `LowerTex` | —      | 可传给 Layout 的同步公式转换函数；后续公式转换失败通过 onDiagnostic 报告 |
| `status`   | `'ready'`  | —      | 当前配置的 lowerer 已可用                                                |

</ApiTable>

</DocTab>

<DocTab value="error" label="属性 · 初始化失败">

<ApiTable>

| 成员         | 类型                    | 默认值 | 说明                                |
| ------------ | ----------------------- | ------ | ----------------------------------- |
| `diagnostic` | `TexLoweringDiagnostic` | —      | 初始化失败的诊断，source 为空字符串 |
| `status`     | `'error'`               | —      | 当前配置的引擎初始化失败            |

</ApiTable>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MathJaxLowerTexState =
  | {
      status: 'loading';
    }
  | {
      status: 'ready';
      lowerTex: LowerTex;
    }
  | {
      status: 'error';
      diagnostic: TexLoweringDiagnostic;
    };
```

</DocTab>

</DocTabs>

### useLowerTex

<p>
  <ApiSourceLink label={'useLowerTex'} path={'packages/kernel/tex/src/react/use-lower-tex.ts'} startLine={88}>
    按有效配置共享 MathJax engine，并异步创建当前组件的 lowerer
  </ApiSourceLink>
</p>

接收与根入口工厂一致的 MathJax 配置，复用同一有效扩展集合的引擎，并返回可判别的初始化状态。ready 状态中的 lowerer 应传入 `Layout` 等 React authoring 入口；本 Hook 不渲染公式，也不处理应用级错误界面

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<ApiTable variant="parameters">

| 类别   | 名称       | 类型 / 签名              | 说明                                          |
| ------ | ---------- | ------------------------ | --------------------------------------------- |
| 参数   | `options?` | `MathJaxLowerTexOptions` | 控制 MathJax 扩展和 lowering 诊断的可选配置   |
| 返回值 | —          | `MathJaxLowerTexState`   | 当前组件对应配置的 MathJax lowerer 初始化状态 |

</ApiTable>

**用法**

```tsx
import type { FC } from 'react';

import { Layout } from '@retikz/react';
import { MathJaxProfile } from '@retikz/tex';
import { useLowerTex } from '@retikz/tex/react';

const Formula: FC = () => {
  const lowerTexState = useLowerTex({ profile: MathJaxProfile.Math });
  if (lowerTexState.status !== 'ready') return <Layout />;

  return <Layout lowerTex={lowerTexState.lowerTex}>{'$x^2$'}</Layout>;
};
```

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const useLowerTex: (options?: MathJaxLowerTexOptions) => MathJaxLowerTexState;
```

</DocTab>

</DocTabs>
