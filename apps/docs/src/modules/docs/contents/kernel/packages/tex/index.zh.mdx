---
title: '@retikz/tex'
description: 把文本中的 LaTeX 公式降解为 Core 可编译、SVG 与 Canvas 可共同渲染的字形路径。
---

`@retikz/tex` 是可选的公式能力包。你仍然在 `<Node>` 等文本入口中写 `$...$`、`$$...$$` 或显式 math run；编译时注入的 `lowerTex` 会把公式转换成 `@retikz/core` 能消费的字形路径。

它不提供新的公式组件，也不拥有文本 IR 或 renderer。MathJax 接入、SVG 字形解析、lowering 缓存和 React 初始化 hook 属于本包；文本布局与 Scene 契约属于 `@retikz/core`，最终路径执行属于 `@retikz/render`。

## 安装与使用

`mathjax-full` 是 optional peer dependency，由应用安装并控制版本：

```bash
pnpm add @retikz/tex mathjax-full
```

React 应用优先使用 `useLowerTex`。hook 异步启动共享的 MathJax engine，准备完成后返回可注入 `<Layout lowerTex>` 的同步 lowerer：

```tsx
import { Layout, Node } from '@retikz/react';
import { useLowerTex } from '@retikz/tex/react';

const Diagram = () => {
  const lowerTex = useLowerTex();

  return (
    <Layout lowerTex={lowerTex}>
      <Node position={[0, 0]}>{'$\\frac{a}{b} = c$'}</Node>
    </Layout>
  );
};
```

`useLowerTex` 在 engine 就绪前返回 `undefined`，随后触发重新编译。React 通过 `<Layout lowerTex>` 注入；Vanilla 使用 `compile.lowerTex`。

## 为什么需要 @retikz/tex

### 让 Core 保持引擎无关

公式排版需要 MathJax 这类体积较大、启动方式也不同于普通文本测量的引擎。如果 Core 直接依赖 MathJax，所有用户和 renderer 都会被迫承担同一依赖与生命周期。

因此 Core 只定义 `LowerTex`：输入 TeX source、inline/display 模式和字号，输出普通的字形路径、宽高与基线深度。`@retikz/tex` 实现这个注入点，但只依赖最小的 `MathJaxSvgEngine.convert` 抽象；应用也可以提供满足该抽象的其它 engine。

### Scene 之后不再依赖 MathJax

| 阶段             | 所有者           | 产物                                             |
| ---------------- | ---------------- | ------------------------------------------------ |
| 公式写法         | Core 文本 IR     | 字符串 sugar 或 `{ tex, displayMode }` math run  |
| 公式降解         | `@retikz/tex`    | renderer-agnostic glyph commands 与度量          |
| 文本布局与 Scene | `@retikz/core`   | 按基线排版的 `PathPrim` / `GroupPrim`            |
| 后端输出         | `@retikz/render` | SVG、Canvas 或其它共享同一 Scene 语义的 renderer |

公式在进入 Scene 前已经是 Core 路径图元，SVG 与 Canvas 不需要各自理解 LaTeX，也不会出现一套 MathJax DOM 与一套 Canvas 公式语义。

<SourceLinks
  sources={[
    {
      label: 'MathJax engine 的最小抽象与 optional peer 边界',
      path: 'packages/kernel/tex/src/mathjax/engine.ts',
      startLine: 1,
      endLine: 16,
    },
    {
      label: 'Core 的 LoweredTex 与 LowerTex 契约',
      path: 'packages/kernel/core/src/compile/text/tex.ts',
      startLine: 4,
      endLine: 17,
    },
    {
      label: '公式字形进入 Scene 路径图元',
      path: 'packages/kernel/core/src/compile/text/layout.ts',
      startLine: 234,
      endLine: 248,
    },
  ]}
/>

## 公式试验场

直接编辑不带 `$` 分隔符的 TeX source，再切换 inline/display 度量、字号和 Node 容器。取景与公式中心保持不变，预设则复现独立、display、多行和带框公式四种常见任务。

<ComponentPreview
  files="tex-playground"
  size="md"
  caption="选择无边框时 padding 会隐藏；输入无效 TeX 时沿用真实 lowerer 的失败行为，不使用 demo 专属回退。"
/>

`$...$` 使用 inline 度量并贴合文字基线；`$$...$$` 使用 display 度量，让分式、求和与积分获得更舒展的上下空间。多行 LaTeX 环境仍然属于一个 Node，只有一个 bbox 和连接目标；启用 `shape` 后，容器继续使用 Node 的常规 padding 与边界计算。

## 文字与公式混排

最自然的数学叙述是文字夹公式。直接在字符串里混写即可，公式和普通文字会在同一行按基线对齐：

```tsx
<Node>{'当 $v = d/t$ 时，位移 $s = v t$'}</Node>
```

需要逐段颜色、透明度或文字字体时，使用显式 `runs`。text run 可设置 `fill`、`opacity` 与 `font`；math run 可设置 `fill`、`opacity` 与 `displayMode`：

```tsx
<Node
  text={[
    {
      runs: [{ tex: '\\sin\\alpha = \\frac{1}{2}', fill: 'red' }, { text: '（红线长度）' }],
    },
  ]}
/>
```

<ComponentAlert
  title="中文注释建议走 text run"
  description="彩色公式与中文注释同一行时，把中文写成 text run，由 measureText 度量；不要塞进 MathJax 的 \\text{}，否则结果会依赖数学字体的 CJK 覆盖。"
/>

## 注入方式

### React 注入

`useLowerTex` 复用一次共享的 engine 初始化，并在组件卸载后避免写回状态。大多数 React 应用只需要在最外层 `<Layout>` 注入一次，子级文本都会复用同一个 compile capability。

### 手动创建

非 React 初始化流程或需要控制 MathJax packages 时，直接从主入口创建 engine 与 lowerer：

```ts
import { createLowerTex, createMathJaxEngine } from '@retikz/tex';

const engine = await createMathJaxEngine({ packages: ['base'] });
const lowerTex = createLowerTex(engine);
```

`options.packages` 透传给 MathJax TeX input，默认是 `['base']`。也可以自行实现 `MathJaxSvgEngine`，只要同步 `convert(tex, { display })` 返回本包支持的 MathJax SVG。

### Vanilla 注入

Vanilla 把同一个 lowerer 放在 `compile.lowerTex`；plain spec、Core IR 与 Scene 仍沿用各自原有入口：

```ts
import { figure, node, renderToSvgString } from '@retikz/vanilla';
import { createLowerTex, createMathJaxEngine } from '@retikz/tex';

const lowerTex = createLowerTex(await createMathJaxEngine());
const spec = figure({
  children: [node('eq', { position: [0, 0], text: '$\\frac{a}{b} = c$' })],
});

const svg = renderToSvgString(spec, { compile: { lowerTex } });
```

<SourceLinks
  sources={[
    {
      label: 'React 共享初始化与重试边界',
      path: 'packages/kernel/tex/src/react/useLowerTex.ts',
      startLine: 10,
      endLine: 38,
    },
    {
      label: 'MathJax engine 创建与 packages 注入',
      path: 'packages/kernel/tex/src/mathjax/engine.ts',
      startLine: 29,
      endLine: 73,
    },
  ]}
/>

## 失败与性能边界

| 情况                            | 可观察行为                                                                    |
| ------------------------------- | ----------------------------------------------------------------------------- |
| string sugar 未注入 `lowerTex`  | `$...$` / `$$...$$` 完整保留为普通文字，不解析公式                            |
| 显式 math run 未注入 `lowerTex` | 跳过公式 segment，并发出 `TEX_LOWERER_MISSING`；同一行的 text run 继续渲染    |
| 分隔符未闭合                    | 从未闭合 `$` 开始按字面保留，并发出 `TEXT_TEX_PARSE_ERROR`                    |
| engine 输出无效公式或转换抛错   | lowerer 返回 `null`，Core 跳过该公式并发出 `TEX_INVALID`                      |
| `mathjax-full` 初始化失败       | `useLowerTex` 保持 `undefined` 并保留原始错误；失败缓存清空，后续挂载可以重试 |

`createLowerTex` 按 `fontSize | displayMode | tex source` 缓存成功或失败的 lowering 结果，避免同一个 lowerer 重复运行 MathJax 和 SVG 字形解析。这个缓存只覆盖 TeX → glyph 的纯转换，不缓存 Core IR、Scene、Node 布局或 renderer 输出，也不提供增量编译或 DOM / Canvas patch。

<SourceLinks
  sources={[
    {
      label: '字符串 sugar 的启用与未闭合分隔符处理',
      path: 'packages/kernel/core/src/compile/text/inline.ts',
      startLine: 40,
      endLine: 88,
    },
    {
      label: '缺失 lowerer 与无效公式的 warning 分支',
      path: 'packages/kernel/core/src/compile/text/layout.ts',
      startLine: 145,
      endLine: 180,
    },
    {
      label: 'TeX lowering 的缓存键与失败结果',
      path: 'packages/kernel/tex/src/lower/lower-tex.ts',
      startLine: 7,
      endLine: 23,
    },
  ]}
/>

## 语法

| 写法              | 含义                                                   |
| ----------------- | ------------------------------------------------------ |
| `$...$`           | 行内公式（inline 度量，贴文字基线）                    |
| `$$...$$`         | display 公式（block 度量；独占一个 Node 时为独立公式） |
| `\$`              | 字面美元符号，不作为分隔符                             |
| `{ runs: [...] }` | 显式 run 序列，可分别设置普通文字与公式段              |

## API 参考

| API                                  | 类型                                      | 描述                                                                    |
| ------------------------------------ | ----------------------------------------- | ----------------------------------------------------------------------- |
| `useLowerTex`（`@retikz/tex/react`） | `typeof useLowerTex`                      | 启动并共享 MathJax engine；未就绪或初始化失败时返回 `undefined`         |
| `createMathJaxEngine`                | `(options?) => Promise<MathJaxSvgEngine>` | 创建 MathJax SVG engine；`packages` 默认 `['base']`                     |
| `createLowerTex`                     | `(engine) => LowerTex`                    | 把 engine 转换成 Core lowerer，并按字号、display 模式和 TeX source 缓存 |
| `MathJaxSvgEngine`                   | `MathJaxSvgEngine`                        | 本包消费的最小同步 engine 抽象                                          |
| `LowerTex`（`@retikz/core`）         | `LowerTex`                                | Core 注入点；返回 renderer-agnostic 字形路径与度量，失败返回 `null`     |
