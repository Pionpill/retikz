---
title: math API 参考
description: 查询 @retikz/math 根入口提供的全部公开 TypeScript API
---

`@retikz/math` 提供零依赖、确定性的二维数值与计算几何工具，包括点、bounds、仿射变换、圆弧、求交和曲线算法。

本包只提供根入口。从 `@retikz/math` 导入公开 API；`algorithms`、`primitives` 和 `transforms` 目录只用于包内组织。

{/* @include kernel/packages/math/api-reference/generated */}
