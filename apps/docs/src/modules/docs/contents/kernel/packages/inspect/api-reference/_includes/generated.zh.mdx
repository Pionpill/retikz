{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/inspect`

### RetikzInspectError

<p>
  <ApiSourceLink label={'RetikzInspectError'} path={'packages/kernel/inspect/src/error.ts'} startLine={39}>
    Inspect 包统一的结构化错误
  </ApiSourceLink>
</p>

| 成员          | 类型                                                                                                                                                                                                                                                                                                                                                              | 默认值 | 说明 |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| `constructor` | `(message: string) => RetikzInspectError; (options: RetikzInspectErrorOptions) => RetikzInspectError; (code: RetikzInspectErrorCodeValue, message: string, options?: Readonly<Pick<Readonly<{ cause?: unknown; code: RetikzInspectErrorCodeValue; details?: Readonly<Record<string, unknown>>; message: string }>, 'details' \| 'cause'>>) => RetikzInspectError` | —      | —    |

### AnyInspectorDefinition

<p>
  <ApiSourceLink
    label={'AnyInspectorDefinition'}
    path={'packages/kernel/inspect/src/contract/types.ts'}
    startLine={136}
  >
    registry 内擦除具体泛型后的 Inspector 定义
  </ApiSourceLink>
</p>

```ts
Readonly<{
  inspect: (subject: never, context: never) => InspectorOutput;
  mergeOptionsInput?: (inheritedOptionsInput: never, localOptionsInput: never) => JsonObject;
  namespace: string;
  optionsSchema: Readonly<{ parse: (value: unknown) => JsonObject }>;
  owner: CompileObservationOwner;
  resolveOptions: (options: never) => JsonObject;
  subjectSchema: Readonly<{ parse: (value: unknown) => JsonValue }>;
  type: string;
}>;
```

### AnyInspectorDefinitionInput

<p>
  <ApiSourceLink
    label={'AnyInspectorDefinitionInput'}
    path={'packages/kernel/inspect/src/contract/types.ts'}
    startLine={156}
  >
    registry 接收的异构作者定义，注册时统一补齐选项 schema 与 resolver
  </ApiSourceLink>
</p>

```ts
Omit<AnyInspectorDefinition, 'optionsSchema' | 'resolveOptions'> &
  Partial<Pick<AnyInspectorDefinition, 'optionsSchema' | 'resolveOptions'>>;
```

### InspectionAppearanceContext

<p>
  <ApiSourceLink
    label={'InspectionAppearanceContext'}
    path={'packages/kernel/inspect/src/contract/types.ts'}
    startLine={38}
  >
    Inspector callback 的稳定外观上下文
  </ApiSourceLink>
</p>

```ts
Readonly<{ colorScope: number; scopeColor: CssColorValue; semanticColors: CoreSemanticColors }>;
```

### InspectionCompileResult

<p>
  <ApiSourceLink
    label={'InspectionCompileResult'}
    path={'packages/kernel/inspect/src/compile/types.ts'}
    startLine={126}
  >
    primary 与辅助结果的原子 compile 输出
  </ApiSourceLink>
</p>

```ts
Readonly<{
  diagnostics: ReadonlyArray<InspectionDiagnostic>;
  inspection: InspectionPlane | null;
  primary: CompileResult;
}>;
```

### InspectionDiagnostic

<p>
  <ApiSourceLink label={'InspectionDiagnostic'} path={'packages/kernel/inspect/src/compile/types.ts'} startLine={118}>
    一条回调或 fragment warning 的 Inspect-owned diagnostic
  </ApiSourceLink>
</p>

```ts
Readonly<{ cause: Readonly<Pick<CompileWarning, 'code' | 'message' | 'path'>>; origin: InspectionDiagnosticOrigin }>;
```

### InspectionDiagnosticOrigin

<p>
  <ApiSourceLink
    label={'InspectionDiagnosticOrigin'}
    path={'packages/kernel/inspect/src/compile/types.ts'}
    startLine={100}
  >
    Inspect 失败、回调警告及 fragment diagnostic 的结构化来源
  </ApiSourceLink>
</p>

```ts
Readonly<{ ruleIndex: number; stage: 'selection'; target: InspectionSelectionTarget | null }> |
  Readonly<{
    inspector: InspectorKey;
    occurrence: CompileOccurrenceLocator;
    owner: CompileObservationOwner;
    stage: 'subject' | 'inspect';
  }> |
  Readonly<{
    inspector: InspectorKey;
    occurrence: CompileOccurrenceLocator;
    outputIndex: number;
    owner: CompileObservationOwner;
    stage: 'output' | 'fragment';
  }> |
  Readonly<{ stage: 'complete' }>;
```

### InspectionPlane

<p>
  <ApiSourceLink label={'InspectionPlane'} path={'packages/kernel/inspect/src/compile/types.ts'} startLine={94}>
    有序、只读的 Inspector 辅助平面
  </ApiSourceLink>
</p>

```ts
Readonly<{ entries: ReadonlyArray<InspectionPlaneEntry> }>;
```

### InspectionPlaneEntry

<p>
  <ApiSourceLink label={'InspectionPlaneEntry'} path={'packages/kernel/inspect/src/compile/types.ts'} startLine={78}>
    一个辅助 Scene plane entry
  </ApiSourceLink>
</p>

```ts
Readonly<{
  colorScope: number;
  inspector: InspectorKey;
  occurrence: CompileOccurrenceLocator;
  owner: CompileObservationOwner;
  scene: Scene;
  transform: AffineMatrix;
}>;
```

### InspectionSelection

<p>
  <ApiSourceLink label={'InspectionSelection'} path={'packages/kernel/inspect/src/compile/types.ts'} startLine={56}>
    一次 compile 的 runtime-only Inspector selection
  </ApiSourceLink>
</p>

```ts
Readonly<{ rules: ReadonlyArray<InspectionSelectionRule> }>;
```

### InspectionSelectionRule

<p>
  <ApiSourceLink label={'InspectionSelectionRule'} path={'packages/kernel/inspect/src/compile/types.ts'} startLine={37}>
    Inspector selection 的单条规则：request 控制单个 Inspector，barrier 封锁一个范围内的全部 Inspector
  </ApiSourceLink>
</p>

```ts
Readonly<{
  inspector: InspectorKey;
  kind: 'request';
  options: boolean | JsonObject;
  target: InspectionSelectionTarget;
}> | Readonly<{ kind: 'barrier'; target: Extract<InspectionSelectionTarget, { kind: 'scene' | 'subtree' }> }>;
```

### InspectionSelectionTarget

<p>
  <ApiSourceLink
    label={'InspectionSelectionTarget'}
    path={'packages/kernel/inspect/src/compile/types.ts'}
    startLine={14}
  >
    Inspector selection 的目标 locator
  </ApiSourceLink>
</p>

```ts
Readonly<{ kind: 'scene' }> |
  Readonly<{ kind: 'subtree'; sourcePath: string }> |
  Readonly<{
    kind: 'self';
    locator:
      | Readonly<{ kind: 'authored'; occurrenceIndex?: number; sourcePath: string }>
      | Readonly<{ kind: 'occurrence'; occurrence: CompileOccurrenceLocator }>;
  }>;
```

### InspectorContext

<p>
  <ApiSourceLink label={'InspectorContext'} path={'packages/kernel/inspect/src/contract/types.ts'} startLine={48}>
    Inspector callback 读取的最终 occurrence 上下文
  </ApiSourceLink>
</p>

```ts
Readonly<{
  ancestors: ReadonlyArray<CompileObservationAncestor>;
  appearance: InspectionAppearanceContext;
  inspectorKey: InspectorKey;
  occurrence: CompileOccurrenceLocator;
  options: TOptions;
  owner: CompileObservationOwner;
  provenance: Readonly<{ final: CompileOccurrenceLocator; origin: CompileOccurrenceLocator }>;
  round: (value: number) => number;
  transform: AffineMatrix;
  warn: (code: string, message: string) => void;
}>;
```

#### 类型参数

| 类型参数   | 说明 |
| ---------- | ---- |
| `TOptions` | —    |

### InspectorCoordinateSpace

<p>
  <ApiSourceLink
    label={'InspectorCoordinateSpace'}
    path={'packages/kernel/inspect/src/contract/types.ts'}
    startLine={22}
  >
    Inspector callback 输出所采用的坐标约定
  </ApiSourceLink>
</p>

```ts
'local' | 'scene';
```

### InspectorDefinition

<p>
  <ApiSourceLink label={'InspectorDefinition'} path={'packages/kernel/inspect/src/contract/types.ts'} startLine={72}>
    独立于 Core owner Definition 的 Inspector 定义
  </ApiSourceLink>
</p>

```ts
Readonly<{
  inspect: (subject: TSubject, context: InspectorContext<TResolvedOptions>) => InspectorOutput;
  mergeOptionsInput?: (inheritedOptionsInput: TSourceOptions, localOptionsInput: TSourceOptions) => TSourceOptions;
  namespace: string;
  optionsSchema: ZodType<TParsedOptions, TSourceOptions>;
  owner: CompileObservationOwner;
  resolveOptions: (options: TParsedOptions) => TResolvedOptions;
  subjectSchema: ZodType<TSubject>;
  type: string;
}>;
```

#### 类型参数

| 类型参数           | 说明 |
| ------------------ | ---- |
| `TSubject`         | —    |
| `TParsedOptions`   | —    |
| `TResolvedOptions` | —    |
| `TSourceOptions`   | —    |

### InspectorDefinitionInput

<p>
  <ApiSourceLink
    label={'InspectorDefinitionInput'}
    path={'packages/kernel/inspect/src/contract/types.ts'}
    startLine={97}
  >
    作者侧 Inspector 定义；仅在对应输入输出可由默认行为满足时允许省略选项字段
  </ApiSourceLink>
</p>

```ts
(Omit<
  InspectorDefinition<TSubject, TParsedOptions, NoInfer<TResolvedOptions>, TSourceOptions>,
  'optionsSchema' | 'resolveOptions'
> &
  Readonly<{ optionsSchema: ZodType<TParsedOptions, TSourceOptions> }>) |
  (conditional & Readonly<{ resolveOptions: (options: TParsedOptions) => TResolvedOptions }>) |
  conditional;
```

#### 类型参数

| 类型参数           | 说明 |
| ------------------ | ---- |
| `TSubject`         | —    |
| `TParsedOptions`   | —    |
| `TResolvedOptions` | —    |
| `TSourceOptions`   | —    |

### InspectorFragment

<p>
  <ApiSourceLink label={'InspectorFragment'} path={'packages/kernel/inspect/src/contract/types.ts'} startLine={25}>
    带显式坐标空间的辅助片段
  </ApiSourceLink>
</p>

```ts
Readonly<{ child: IRChild; coordinateSpace: InspectorCoordinateSpace; type: 'fragment' }>;
```

### InspectorKey

<p>
  <ApiSourceLink label={'InspectorKey'} path={'packages/kernel/inspect/src/contract/types.ts'} startLine={14}>
    Inspector registry 的公开复合键
  </ApiSourceLink>
</p>

```ts
Readonly<{ namespace: string; type: string }>;
```

### InspectorOutput

<p>
  <ApiSourceLink label={'InspectorOutput'} path={'packages/kernel/inspect/src/contract/types.ts'} startLine={35}>
    裸 Core child 使用局部坐标；显式片段可逐项选择坐标空间
  </ApiSourceLink>
</p>

```ts
IRChild | InspectorFragment | ReadonlyArray<IRChild | InspectorFragment>;
```

### InspectorRegistry

<p>
  <ApiSourceLink label={'InspectorRegistry'} path={'packages/kernel/inspect/src/providers/registry.ts'} startLine={8}>
    Inspector Definition 的 immutable registry
  </ApiSourceLink>
</p>

```ts
Readonly<{
  definitions: ReadonlyArray<AnyInspectorDefinition>;
  get: (key: InspectorKey) => AnyInspectorDefinition | undefined;
  require: (key: InspectorKey) => AnyInspectorDefinition;
}>;
```

### RetikzInspectErrorCodeValue

<p>
  <ApiSourceLink label={'RetikzInspectErrorCodeValue'} path={'packages/kernel/inspect/src/error.ts'} startLine={22}>
    Inspect 包稳定错误码取值
  </ApiSourceLink>
</p>

```ts
ValueOf<typeof RetikzInspectErrorCode>;
```

### RetikzInspectErrorOptions

<p>
  <ApiSourceLink label={'RetikzInspectErrorOptions'} path={'packages/kernel/inspect/src/error.ts'} startLine={25}>
    Inspect 包错误的结构化构造参数
  </ApiSourceLink>
</p>

```ts
Readonly<{
  cause?: unknown;
  code: RetikzInspectErrorCodeValue;
  details?: Readonly<Record<string, unknown>>;
  message: string;
}>;
```

### BUILTIN_INSPECTORS

<p>
  <ApiSourceLink
    label={'BUILTIN_INSPECTORS'}
    path={'packages/kernel/inspect/src/providers/definitions.ts'}
    startLine={10}
  >
    默认 registry 使用的内置 Inspector definitions
  </ApiSourceLink>
</p>

```ts
ReadonlyArray<AnyInspectorDefinition>;
```

### CLIP_INSPECTOR

<p>
  <ApiSourceLink label={'CLIP_INSPECTOR'} path={'packages/kernel/inspect/src/providers/clip.ts'} startLine={42}>
    内置 Clip application Inspector
  </ApiSourceLink>
</p>

```ts
Readonly<{
  inspect: (
    subject: {
      path: {
        commands: Array<
          | { kind: 'move'; to: [number, number] }
          | { kind: 'line'; to: [number, number] }
          | { control: [number, number]; kind: 'quad'; to: [number, number] }
          | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] }
          | {
              center: [number, number];
              counterClockwise?: boolean;
              endAngle: number;
              kind: 'arc';
              radius: number;
              startAngle: number;
            }
          | {
              center: [number, number];
              counterClockwise?: boolean;
              endAngle: number;
              kind: 'ellipseArc';
              radiusX: number;
              radiusY: number;
              rotation?: number;
              startAngle: number;
            }
          | { kind: 'close' }
        >;
        fillRule: 'nonzero' | 'evenodd';
      };
    },
    context: InspectorContext<TResolvedOptions>,
  ) => InspectorOutput;
  mergeOptionsInput?: (
    inheritedOptionsInput: { labels?: boolean; outline?: boolean },
    localOptionsInput: { labels?: boolean; outline?: boolean },
  ) => { labels?: boolean; outline?: boolean };
  namespace: string;
  optionsSchema: ZodType<{ labels: boolean; outline: boolean }, { labels?: boolean; outline?: boolean }>;
  owner: CompileObservationOwner;
  resolveOptions: (options: { labels: boolean; outline: boolean }) => { labels: boolean; outline: boolean };
  subjectSchema: ZodType<{
    path: {
      commands: Array<
        | { kind: 'move'; to: [number, number] }
        | { kind: 'line'; to: [number, number] }
        | { control: [number, number]; kind: 'quad'; to: [number, number] }
        | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] }
        | {
            center: [number, number];
            counterClockwise?: boolean;
            endAngle: number;
            kind: 'arc';
            radius: number;
            startAngle: number;
          }
        | {
            center: [number, number];
            counterClockwise?: boolean;
            endAngle: number;
            kind: 'ellipseArc';
            radiusX: number;
            radiusY: number;
            rotation?: number;
            startAngle: number;
          }
        | { kind: 'close' }
      >;
      fillRule: 'nonzero' | 'evenodd';
    };
  }>;
  type: string;
}>;
```

### CLIP_INSPECTOR_KEY

<p>
  <ApiSourceLink label={'CLIP_INSPECTOR_KEY'} path={'packages/kernel/inspect/src/providers/clip.ts'} startLine={15}>
    内置 Core Clip Inspector key
  </ApiSourceLink>
</p>

```ts
Readonly<{ namespace: 'core'; type: 'clip' }>;
```

### ClipInspectOptionsSchema

<p>
  <ApiSourceLink label={'ClipInspectOptionsSchema'} path={'packages/kernel/inspect/src/schema/clip.ts'} startLine={6}>
    内置 Clip Inspector 选项及默认值
  </ApiSourceLink>
</p>

[Schema 参考](/kernel/packages/inspect/schema-reference#clipinspectoptionsschema)

### COORDINATE_INSPECTOR

<p>
  <ApiSourceLink
    label={'COORDINATE_INSPECTOR'}
    path={'packages/kernel/inspect/src/providers/coordinate.ts'}
    startLine={18}
  >
    内置已解析 Coordinate 点 Inspector
  </ApiSourceLink>
</p>

```ts
Readonly<{
  inspect: (
    subject: { id: string; position: [number, number] },
    context: InspectorContext<TResolvedOptions>,
  ) => InspectorOutput;
  mergeOptionsInput?: (
    inheritedOptionsInput: { labels?: boolean },
    localOptionsInput: { labels?: boolean },
  ) => { labels?: boolean };
  namespace: string;
  optionsSchema: ZodType<{ labels: boolean }, { labels?: boolean }>;
  owner: CompileObservationOwner;
  resolveOptions: (options: { labels: boolean }) => { labels: boolean };
  subjectSchema: ZodType<{ id: string; position: [number, number] }>;
  type: string;
}>;
```

### COORDINATE_INSPECTOR_KEY

<p>
  <ApiSourceLink
    label={'COORDINATE_INSPECTOR_KEY'}
    path={'packages/kernel/inspect/src/providers/coordinate.ts'}
    startLine={15}
  >
    内置 Core Coordinate Inspector key
  </ApiSourceLink>
</p>

```ts
Readonly<{ namespace: 'core'; type: 'coordinate' }>;
```

### CoordinateInspectOptionsSchema

<p>
  <ApiSourceLink
    label={'CoordinateInspectOptionsSchema'}
    path={'packages/kernel/inspect/src/schema/coordinate.ts'}
    startLine={6}
  >
    内置 Coordinate Inspector 选项及默认值
  </ApiSourceLink>
</p>

[Schema 参考](/kernel/packages/inspect/schema-reference#coordinateinspectoptionsschema)

### InspectionLabelsSchema

[Schema 参考](/kernel/packages/inspect/schema-reference#inspectionlabelsschema)

### NODE_INSPECTOR

<p>
  <ApiSourceLink label={'NODE_INSPECTOR'} path={'packages/kernel/inspect/src/providers/node.ts'} startLine={144}>
    内置 Node 几何 Inspector
  </ApiSourceLink>
</p>

```ts
Readonly<{
  inspect: (
    subject: {
      boundary: {
        name: string;
        outline: Array<
          | { kind: 'move'; to: [number, number] }
          | { kind: 'line'; to: [number, number] }
          | { control: [number, number]; kind: 'quad'; to: [number, number] }
          | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] }
          | {
              center: [number, number];
              counterClockwise?: boolean;
              endAngle: number;
              kind: 'arc';
              radius: number;
              startAngle: number;
            }
          | {
              center: [number, number];
              counterClockwise?: boolean;
              endAngle: number;
              kind: 'ellipseArc';
              radiusX: number;
              radiusY: number;
              rotation?: number;
              startAngle: number;
            }
          | { kind: 'close' }
        > | null;
      };
      content: {
        baselines: Array<{ from: [number, number]; to: [number, number] }>;
        corners: [[number, number], [number, number], [number, number], [number, number]];
      } | null;
      rect: { height: number; rotate?: number; width: number; x: number; y: number };
      shape: {
        keyPoints: Array<{ name: string; position: [number, number] }> | null;
        name: string;
        outline: Array<
          | { kind: 'move'; to: [number, number] }
          | { kind: 'line'; to: [number, number] }
          | { control: [number, number]; kind: 'quad'; to: [number, number] }
          | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] }
          | {
              center: [number, number];
              counterClockwise?: boolean;
              endAngle: number;
              kind: 'arc';
              radius: number;
              startAngle: number;
            }
          | {
              center: [number, number];
              counterClockwise?: boolean;
              endAngle: number;
              kind: 'ellipseArc';
              radiusX: number;
              radiusY: number;
              rotation?: number;
              startAngle: number;
            }
          | { kind: 'close' }
        > | null;
      };
    },
    context: InspectorContext<TResolvedOptions>,
  ) => InspectorOutput;
  mergeOptionsInput?: (
    inheritedOptionsInput: {
      baselines?: boolean;
      boundary?: boolean;
      bounds?: boolean;
      box?: boolean;
      content?: boolean;
      keyPoints?: boolean;
      labels?: boolean;
      outline?: boolean;
    },
    localOptionsInput: {
      baselines?: boolean;
      boundary?: boolean;
      bounds?: boolean;
      box?: boolean;
      content?: boolean;
      keyPoints?: boolean;
      labels?: boolean;
      outline?: boolean;
    },
  ) => {
    baselines?: boolean;
    boundary?: boolean;
    bounds?: boolean;
    box?: boolean;
    content?: boolean;
    keyPoints?: boolean;
    labels?: boolean;
    outline?: boolean;
  };
  namespace: string;
  optionsSchema: ZodType<
    {
      baselines: boolean;
      boundary: boolean;
      bounds: boolean;
      box: boolean;
      content: boolean;
      keyPoints: boolean;
      labels: boolean;
      outline: boolean;
    },
    {
      baselines?: boolean;
      boundary?: boolean;
      bounds?: boolean;
      box?: boolean;
      content?: boolean;
      keyPoints?: boolean;
      labels?: boolean;
      outline?: boolean;
    }
  >;
  owner: CompileObservationOwner;
  resolveOptions: (options: {
    baselines: boolean;
    boundary: boolean;
    bounds: boolean;
    box: boolean;
    content: boolean;
    keyPoints: boolean;
    labels: boolean;
    outline: boolean;
  }) => {
    baselines: boolean;
    boundary: boolean;
    bounds: boolean;
    box: boolean;
    content: boolean;
    keyPoints: boolean;
    labels: boolean;
    outline: boolean;
  };
  subjectSchema: ZodType<{
    boundary: {
      name: string;
      outline: Array<
        | { kind: 'move'; to: [number, number] }
        | { kind: 'line'; to: [number, number] }
        | { control: [number, number]; kind: 'quad'; to: [number, number] }
        | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] }
        | {
            center: [number, number];
            counterClockwise?: boolean;
            endAngle: number;
            kind: 'arc';
            radius: number;
            startAngle: number;
          }
        | {
            center: [number, number];
            counterClockwise?: boolean;
            endAngle: number;
            kind: 'ellipseArc';
            radiusX: number;
            radiusY: number;
            rotation?: number;
            startAngle: number;
          }
        | { kind: 'close' }
      > | null;
    };
    content: {
      baselines: Array<{ from: [number, number]; to: [number, number] }>;
      corners: [[number, number], [number, number], [number, number], [number, number]];
    } | null;
    rect: { height: number; rotate?: number; width: number; x: number; y: number };
    shape: {
      keyPoints: Array<{ name: string; position: [number, number] }> | null;
      name: string;
      outline: Array<
        | { kind: 'move'; to: [number, number] }
        | { kind: 'line'; to: [number, number] }
        | { control: [number, number]; kind: 'quad'; to: [number, number] }
        | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] }
        | {
            center: [number, number];
            counterClockwise?: boolean;
            endAngle: number;
            kind: 'arc';
            radius: number;
            startAngle: number;
          }
        | {
            center: [number, number];
            counterClockwise?: boolean;
            endAngle: number;
            kind: 'ellipseArc';
            radiusX: number;
            radiusY: number;
            rotation?: number;
            startAngle: number;
          }
        | { kind: 'close' }
      > | null;
    };
  }>;
  type: string;
}>;
```

### NODE_INSPECTOR_KEY

<p>
  <ApiSourceLink label={'NODE_INSPECTOR_KEY'} path={'packages/kernel/inspect/src/providers/node.ts'} startLine={24}>
    内置 Core Node Inspector key
  </ApiSourceLink>
</p>

```ts
Readonly<{ namespace: 'core'; type: 'node' }>;
```

### NodeInspectOptionsSchema

<p>
  <ApiSourceLink label={'NodeInspectOptionsSchema'} path={'packages/kernel/inspect/src/schema/node.ts'} startLine={6}>
    内置 Node Inspector 选项及默认值
  </ApiSourceLink>
</p>

[Schema 参考](/kernel/packages/inspect/schema-reference#nodeinspectoptionsschema)

### PATH_INSPECTOR

<p>
  <ApiSourceLink label={'PATH_INSPECTOR'} path={'packages/kernel/inspect/src/providers/path.ts'} startLine={228}>
    内置 stroke Path 控制点 Inspector
  </ApiSourceLink>
</p>

```ts
Readonly<{ inspect: (subject: { commands: Array<{ kind: 'move'; to: [number, number] } | { kind: 'line'; to: [number, number] } | { control: [number, number]; kind: 'quad'; to: [number, number] } | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] } | { center: [number, number]; counterClockwise?: boolean; endAngle: number; kind: 'arc'; radius: number; startAngle: number } | { center: [number, number]; counterClockwise?: boolean; endAngle: number; kind: 'ellipseArc'; radiusX: number; radiusY: number; rotation?: number; startAngle: number } | { kind: 'close' }>; transforms: Array<{ kind: 'translate'; x: number; y: number } | { angle: number; kind: 'polar-translate'; origin?: string | [number, number] | SharedPolarPosition; radius: number } | { direction: 'top' | 'right' | 'bottom' | 'left' | 'top-right' | 'top-left' | 'bottom-right' | 'bottom-left'; distance?: number; kind: 'at-translate'; of: string } | { kind: 'offset-translate'; of: string | [number, number] | SharedPolarPosition; offset?: [number, number] } | { between: [IRAbsoluteTarget, IRAbsoluteTarget]; fraction: number; kind: 'between-translate' } | { degrees: number; kind: 'rotate'; pivot?: number | 'origin' | [number, number] | OpenString<ValueOf<{ Bottom: ...; BottomLeft: ...; BottomRight: ...; Center: ...; Left: ...; Right: ...; Top: ...; TopLeft: ...; TopRight: ... }>> | { fraction: number; side: 'top' | 'right' | 'bottom' | 'left' } } | { kind: 'scale'; pivot?: number | 'origin' | [number, number] | OpenString<ValueOf<{ Bottom: ...; BottomLeft: ...; BottomRight: ...; Center: ...; Left: ...; Right: ...; Top: ...; TopLeft: ...; TopRight: ... }>> | { fraction: number; side: 'top' | 'right' | 'bottom' | 'left' }; x: number; y?: number }> }, context: InspectorContext<TResolvedOptions>) => InspectorOutput; mergeOptionsInput?: (inheritedOptionsInput: { arcGeometry?: boolean; controlPoints?: boolean; ellipseAxes?: boolean; labels?: boolean; vertices?: boolean }, localOptionsInput: { arcGeometry?: boolean; controlPoints?: boolean; ellipseAxes?: boolean; labels?: boolean; vertices?: boolean }) => { arcGeometry?: boolean; controlPoints?: boolean; ellipseAxes?: boolean; labels?: boolean; vertices?: boolean }; namespace: string; optionsSchema: ZodType<{ arcGeometry: boolean; controlPoints: boolean; ellipseAxes: boolean; labels: boolean; vertices: boolean }, { arcGeometry?: boolean; controlPoints?: boolean; ellipseAxes?: boolean; labels?: boolean; vertices?: boolean }>; owner: CompileObservationOwner; resolveOptions: (options: { arcGeometry: boolean; controlPoints: boolean; ellipseAxes: boolean; labels: boolean; vertices: boolean }) => { arcGeometry: boolean; controlPoints: boolean; ellipseAxes: boolean; labels: boolean; vertices: boolean }; subjectSchema: ZodType<{ commands: Array<{ kind: 'move'; to: [number, number] } | { kind: 'line'; to: [number, number] } | { control: [number, number]; kind: 'quad'; to: [number, number] } | { control1: [number, number]; control2: [number, number]; kind: 'cubic'; to: [number, number] } | { center: [number, number]; counterClockwise?: boolean; endAngle: number; kind: 'arc'; radius: number; startAngle: number } | { center: [number, number]; counterClockwise?: boolean; endAngle: number; kind: 'ellipseArc'; radiusX: number; radiusY: number; rotation?: number; startAngle: number } | { kind: 'close' }>; transforms: Array<{ kind: 'translate'; x: number; y: number } | { angle: number; kind: 'polar-translate'; origin?: string | [number, number] | SharedPolarPosition; radius: number } | { direction: 'top' | 'right' | 'bottom' | 'left' | 'top-right' | 'top-left' | 'bottom-right' | 'bottom-left'; distance?: number; kind: 'at-translate'; of: string } | { kind: 'offset-translate'; of: string | [number, number] | SharedPolarPosition; offset?: [number, number] } | { between: [IRAbsoluteTarget, IRAbsoluteTarget]; fraction: number; kind: 'between-translate' } | { degrees: number; kind: 'rotate'; pivot?: number | 'origin' | [number, number] | OpenString<ValueOf<{ Bottom: ...; BottomLeft: ...; BottomRight: ...; Center: ...; Left: ...; Right: ...; Top: ...; TopLeft: ...; TopRight: ... }>> | { fraction: number; side: 'top' | 'right' | 'bottom' | 'left' } } | { kind: 'scale'; pivot?: number | 'origin' | [number, number] | OpenString<ValueOf<{ Bottom: ...; BottomLeft: ...; BottomRight: ...; Center: ...; Left: ...; Right: ...; Top: ...; TopLeft: ...; TopRight: ... }>> | { fraction: number; side: 'top' | 'right' | 'bottom' | 'left' }; x: number; y?: number }> }>; type: string }>
```

### PATH_INSPECTOR_KEY

<p>
  <ApiSourceLink label={'PATH_INSPECTOR_KEY'} path={'packages/kernel/inspect/src/providers/path.ts'} startLine={10}>
    内置 Core stroke Path Inspector key
  </ApiSourceLink>
</p>

```ts
Readonly<{ namespace: 'core'; type: 'path' }>;
```

### PathInspectOptionsSchema

<p>
  <ApiSourceLink label={'PathInspectOptionsSchema'} path={'packages/kernel/inspect/src/schema/path.ts'} startLine={6}>
    内置 stroke Path Inspector 选项及默认值
  </ApiSourceLink>
</p>

[Schema 参考](/kernel/packages/inspect/schema-reference#pathinspectoptionsschema)

### RetikzInspectErrorCode

<p>
  <ApiSourceLink label={'RetikzInspectErrorCode'} path={'packages/kernel/inspect/src/error.ts'} startLine={6}>
    Inspect 包稳定错误码
  </ApiSourceLink>
</p>

```ts
{
  Compile: 'INSPECTION_COMPILE_ERROR';
  CompileFailed: 'INSPECTION_COMPILE_FAILED';
  Contract: 'INSPECTION_CONTRACT_ERROR';
  Default: 'INSPECTION_ERROR';
  Registry: 'INSPECTION_REGISTRY_ERROR';
  Vanilla: 'INSPECTION_VANILLA_ERROR';
}
```

### SCOPE_INSPECTOR

<p>
  <ApiSourceLink label={'SCOPE_INSPECTOR'} path={'packages/kernel/inspect/src/providers/scope.ts'} startLine={63}>
    内置 Scope 层级与坐标框 Inspector
  </ApiSourceLink>
</p>

```ts
Readonly<{
  inspect: (
    subject: {
      envelope: {
        rect: { height: number; rotate?: number; width: number; x: number; y: number };
        shape: 'rectangle' | 'circle';
      } | null;
    },
    context: InspectorContext<TResolvedOptions>,
  ) => InspectorOutput;
  mergeOptionsInput?: (
    inheritedOptionsInput: { axes?: boolean; envelope?: boolean; labels?: boolean; origin?: boolean },
    localOptionsInput: { axes?: boolean; envelope?: boolean; labels?: boolean; origin?: boolean },
  ) => { axes?: boolean; envelope?: boolean; labels?: boolean; origin?: boolean };
  namespace: string;
  optionsSchema: ZodType<
    { axes: boolean; envelope: boolean; labels: boolean; origin: boolean },
    { axes?: boolean; envelope?: boolean; labels?: boolean; origin?: boolean }
  >;
  owner: CompileObservationOwner;
  resolveOptions: (options: { axes: boolean; envelope: boolean; labels: boolean; origin: boolean }) => {
    axes: boolean;
    envelope: boolean;
    labels: boolean;
    origin: boolean;
  };
  subjectSchema: ZodType<{
    envelope: {
      rect: { height: number; rotate?: number; width: number; x: number; y: number };
      shape: 'rectangle' | 'circle';
    } | null;
  }>;
  type: string;
}>;
```

### SCOPE_INSPECTOR_KEY

<p>
  <ApiSourceLink label={'SCOPE_INSPECTOR_KEY'} path={'packages/kernel/inspect/src/providers/scope.ts'} startLine={22}>
    内置 Core Scope Inspector key
  </ApiSourceLink>
</p>

```ts
Readonly<{ namespace: 'core'; type: 'scope' }>;
```

### ScopeInspectOptionsSchema

<p>
  <ApiSourceLink label={'ScopeInspectOptionsSchema'} path={'packages/kernel/inspect/src/schema/scope.ts'} startLine={6}>
    内置 Scope Inspector 选项及默认值
  </ApiSourceLink>
</p>

[Schema 参考](/kernel/packages/inspect/schema-reference#scopeinspectoptionsschema)

### compileInspectionToScene

<p>
  <ApiSourceLink
    label={'compileInspectionToScene'}
    path={'packages/kernel/inspect/src/compile/driver.ts'}
    startLine={260}
  >
    基于 Core observed compile 执行一次原子 Inspector compile
  </ApiSourceLink>
</p>

```ts
<TComposites>(
  ir: {
    animations?: Array<{
      delay?: number;
      direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
      duration: number;
      easing?:
        | OpenString<
            ValueOf<{
              Ease: 'ease';
              EaseIn: 'ease-in';
              EaseInOut: 'ease-in-out';
              EaseOut: 'ease-out';
              Linear: 'linear';
            }>
          >
        | [number, number, number, number];
      fill?: 'none' | 'forwards' | 'backwards' | 'both';
      iterations?: number | 'infinite';
      keyframes: Array<{
        at: number;
        easing?:
          | OpenString<
              ValueOf<{
                Ease: 'ease';
                EaseIn: 'ease-in';
                EaseInOut: 'ease-in-out';
                EaseOut: 'ease-out';
                Linear: 'linear';
              }>
            >
          | [number, number, number, number];
        value: JsonValue;
      }>;
      origin?:
        | 'center'
        | [number, number]
        | 'top'
        | 'right'
        | 'bottom'
        | 'left'
        | 'top-right'
        | 'top-left'
        | 'bottom-right'
        | 'bottom-left';
      property: OpenString<
        ValueOf<{
          Fill: 'fill';
          Opacity: 'opacity';
          PathDraw: 'pathDraw';
          Rotate: 'rotate';
          Scale: 'scale';
          ScaleX: 'scaleX';
          ScaleY: 'scaleY';
          Stroke: 'stroke';
          StrokeWidth: 'strokeWidth';
          TranslateX: 'translateX';
          TranslateY: 'translateY';
          ViewBox: 'viewBox';
        }>
      >;
      trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
    }>;
    children: Array<IRChild>;
    theme?: { mode?: 'light' | 'dark'; style?: string };
    type: 'scene';
    version: 1;
    viewBox?: { height: number; width: number; x: number; y: number };
  },
  options: Readonly<{
    compileOptions?: CompileOptions<TComposites>;
    registry: InspectorRegistry;
    selection: InspectionSelection;
  }>,
) => InspectionCompileResult;
```

#### 类型参数

| 类型参数      | 说明 |
| ------------- | ---- |
| `TComposites` | —    |

#### 参数

| 参数      | 类型                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 说明 |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `ir`      | `{ animations?: Array<{ delay?: number; direction?: 'reverse' \| 'normal' \| 'alternate' \| 'alternate-reverse'; duration: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; fill?: 'none' \| 'forwards' \| 'backwards' \| 'both'; iterations?: number \| 'infinite'; keyframes: Array<{ at: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; value: JsonValue }>; origin?: 'center' \| [number, number] \| 'top' \| 'right' \| 'bottom' \| 'left' \| 'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left'; property: OpenString<ValueOf<{ Fill: 'fill'; Opacity: 'opacity'; PathDraw: 'pathDraw'; Rotate: 'rotate'; Scale: 'scale'; ScaleX: 'scaleX'; ScaleY: 'scaleY'; Stroke: 'stroke'; StrokeWidth: 'strokeWidth'; TranslateX: 'translateX'; TranslateY: 'translateY'; ViewBox: 'viewBox' }>>; trigger?: 'load' \| 'visible' \| 'manual' \| { onEvent: string } }>; children: Array<IRChild>; theme?: { mode?: 'light' \| 'dark'; style?: string }; type: 'scene'; version: 1; viewBox?: { height: number; width: number; x: number; y: number } }` | —    |
| `options` | `Readonly<{ compileOptions?: CompileOptions<TComposites>; registry: InspectorRegistry; selection: InspectionSelection }>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —    |

### createDefaultInspectorRegistry

<p>
  <ApiSourceLink
    label={'createDefaultInspectorRegistry'}
    path={'packages/kernel/inspect/src/providers/registry.ts'}
    startLine={54}
  >
    创建内置优先、第三方同路的默认 Inspector registry
  </ApiSourceLink>
</p>

```ts
(definitions: readonly Array<AnyInspectorDefinitionInput>) =>
  Readonly<{
    definitions: readonly Array<
      Readonly<{
        inspect: (subject: never, context: never) => InspectorOutput;
        mergeOptionsInput?: (inheritedOptionsInput: never, localOptionsInput: never) => JsonObject;
        namespace: string;
        optionsSchema: Readonly<{ parse: (value: unknown) => JsonObject }>;
        owner: CompileObservationOwner;
        resolveOptions: (options: never) => JsonObject;
        subjectSchema: Readonly<{ parse: (value: unknown) => JsonValue }>;
        type: string;
      }>
    >;
    get: (
      key: InspectorKey,
    ) =>
      | Readonly<{
          inspect: (subject: never, context: never) => InspectorOutput;
          mergeOptionsInput?: (inheritedOptionsInput: never, localOptionsInput: never) => JsonObject;
          namespace: string;
          optionsSchema: Readonly<{ parse: (value: unknown) => JsonObject }>;
          owner: CompileObservationOwner;
          resolveOptions: (options: never) => JsonObject;
          subjectSchema: Readonly<{ parse: (value: unknown) => JsonValue }>;
          type: string;
        }>
      | undefined;
    require: (key: InspectorKey) => AnyInspectorDefinition;
  }>;
```

#### 参数

| 参数          | 类型                                          | 说明 |
| ------------- | --------------------------------------------- | ---- |
| `definitions` | `readonly Array<AnyInspectorDefinitionInput>` | —    |

### createInspectorRegistry

<p>
  <ApiSourceLink
    label={'createInspectorRegistry'}
    path={'packages/kernel/inspect/src/providers/registry.ts'}
    startLine={21}
  >
    创建无全局状态的 Inspector registry
  </ApiSourceLink>
</p>

```ts
(definitions: readonly Array<AnyInspectorDefinitionInput>) => InspectorRegistry;
```

#### 参数

| 参数          | 类型                                          | 说明 |
| ------------- | --------------------------------------------- | ---- |
| `definitions` | `readonly Array<AnyInspectorDefinitionInput>` | —    |

### defineInspector

<p>
  <ApiSourceLink label={'defineInspector'} path={'packages/kernel/inspect/src/contract/define.ts'} startLine={66}>
    补齐默认选项契约并冻结 Inspector；省略 schema 时只接受空对象，省略 resolver 时使用 schema 输出
  </ApiSourceLink>
</p>

```ts
<TSubject, TParsedOptions, TResolvedOptions, TSourceOptions>(
  definition: InspectorDefinitionInput<TSubject, TParsedOptions, TResolvedOptions, TSourceOptions>,
) => InspectorDefinition<TSubject, TParsedOptions, TResolvedOptions, TSourceOptions>;
```

#### 类型参数

| 类型参数           | 说明 |
| ------------------ | ---- |
| `TSubject`         | —    |
| `TParsedOptions`   | —    |
| `TResolvedOptions` | —    |
| `TSourceOptions`   | —    |

#### 参数

| 参数         | 类型                                                                                   | 说明 |
| ------------ | -------------------------------------------------------------------------------------- | ---- |
| `definition` | `InspectorDefinitionInput<TSubject, TParsedOptions, TResolvedOptions, TSourceOptions>` | —    |

## `@retikz/inspect/react`

### InspectCoordinateProps

<p>
  <ApiSourceLink
    label={'InspectCoordinateProps'}
    path={'packages/kernel/inspect/src/react/components.tsx'}
    startLine={26}
  >
    只选择当前 Coordinate 的可选 Inspector wrapper props
  </ApiSourceLink>
</p>

```ts
Omit<CoordinateProps, 'authoring'> & { request: Exclude<InspectionVanillaAuthoringInput, false> };
```

### InspectLayoutProps

<p>
  <ApiSourceLink label={'InspectLayoutProps'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={64}>
    绑定 Inspect registry/selection/callback 的可选 Layout wrapper props
  </ApiSourceLink>
</p>

```ts
Omit<LayoutProps, 'authoring' | 'compileDriver'> &
  Readonly<{
    onCommit?: (result: InspectionCompileResult) => void;
    onDiagnostic?: (diagnostic: InspectionDiagnostic) => void;
    registry: InspectorRegistry;
    request?: InspectionVanillaAuthoringInput;
    selection?: InspectionSelection;
  }>;
```

### InspectNodeProps

<p>
  <ApiSourceLink label={'InspectNodeProps'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={14}>
    只选择当前 Node 的可选 Inspector wrapper props
  </ApiSourceLink>
</p>

```ts
Omit<NodeProps, 'authoring'> & { request: Exclude<InspectionVanillaAuthoringInput, false> };
```

### InspectPathProps

<p>
  <ApiSourceLink label={'InspectPathProps'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={38}>
    可选 Inspect Path wrapper props
  </ApiSourceLink>
</p>

```ts
Omit<PathProps, 'authoring'> & Readonly<{ request: Exclude<InspectionVanillaAuthoringInput, false> }>;
```

### InspectScopeProps

<p>
  <ApiSourceLink label={'InspectScopeProps'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={51}>
    可选 Inspect Scope wrapper props
  </ApiSourceLink>
</p>

```ts
Omit<ScopeProps, 'authoring'> & Readonly<{ request: InspectionVanillaAuthoringInput }>;
```

### InspectCoordinate

<p>
  <ApiSourceLink label={'InspectCoordinate'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={32}>
    复用基础 Coordinate，仅附加运行时检查请求
  </ApiSourceLink>
</p>

```ts
FC<InspectCoordinateProps>;
```

### InspectLayout

<p>
  <ApiSourceLink label={'InspectLayout'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={79}>
    复用基础 Layout/static/retained runtime 的 Inspect 可选宿主
  </ApiSourceLink>
</p>

```ts
FC<InspectLayoutProps>;
```

### InspectNode

<p>
  <ApiSourceLink label={'InspectNode'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={20}>
    复用基础 Node，仅附加运行时检查请求
  </ApiSourceLink>
</p>

```ts
FC<InspectNodeProps>;
```

### InspectPath

<p>
  <ApiSourceLink label={'InspectPath'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={45}>
    复用基础 Path lowering、只附加 runtime-only Inspector authoring 标记
  </ApiSourceLink>
</p>

```ts
FC<InspectPathProps>;
```

### InspectScope

<p>
  <ApiSourceLink label={'InspectScope'} path={'packages/kernel/inspect/src/react/components.tsx'} startLine={58}>
    复用基础 Scope lowering、只附加 runtime-only Inspector authoring 标记
  </ApiSourceLink>
</p>

```ts
FC<InspectScopeProps>;
```

## `@retikz/inspect/vanilla`

### CreateInspectionVanillaDriverOptions

<p>
  <ApiSourceLink
    label={'CreateInspectionVanillaDriverOptions'}
    path={'packages/kernel/inspect/src/vanilla/driver.ts'}
    startLine={67}
  >
    Inspect Vanilla 编译驱动的固定配置
  </ApiSourceLink>
</p>

```ts
Readonly<{
  onCommit?: (result: InspectionCompileResult) => void;
  onDiagnostic?: (diagnostic: InspectionDiagnostic) => void;
  registry: InspectorRegistry;
  selection?: InspectionSelection;
}>;
```

### InspectionVanillaAuthoringInput

<p>
  <ApiSourceLink
    label={'InspectionVanillaAuthoringInput'}
    path={'packages/kernel/inspect/src/vanilla/authoring.ts'}
    startLine={20}
  >
    Vanilla site 可声明一个 request、多个 request，或 barrier
  </ApiSourceLink>
</p>

```ts
false | InspectionVanillaRequest | ReadonlyArray<InspectionVanillaRequest>;
```

### InspectionVanillaRequest

<p>
  <ApiSourceLink
    label={'InspectionVanillaRequest'}
    path={'packages/kernel/inspect/src/vanilla/authoring.ts'}
    startLine={12}
  >
    Vanilla 可选 authoring 声明的一项 Inspector request
  </ApiSourceLink>
</p>

```ts
Readonly<{ inspector: InspectorKey; options: false | true | JsonObject }>;
```

### createInspectionVanillaAuthoring

<p>
  <ApiSourceLink
    label={'createInspectionVanillaAuthoring'}
    path={'packages/kernel/inspect/src/vanilla/authoring.ts'}
    startLine={31}
  >
    创建只由 Inspect Vanilla 驱动识别的 opaque authoring 标记
  </ApiSourceLink>
</p>

```ts
(input: InspectionVanillaAuthoringInput) => InspectionVanillaAuthoring;
```

#### 参数

| 参数    | 类型                              | 说明 |
| ------- | --------------------------------- | ---- |
| `input` | `InspectionVanillaAuthoringInput` | —    |

### createInspectionVanillaDriver

<p>
  <ApiSourceLink
    label={'createInspectionVanillaDriver'}
    path={'packages/kernel/inspect/src/vanilla/driver.ts'}
    startLine={144}
  >
    创建绑定 Inspector registry、selection 与 committed callbacks 的 Vanilla 编译驱动
  </ApiSourceLink>
</p>

```ts
(options: CreateInspectionVanillaDriverOptions) => VanillaCompileDriver;
```

#### 参数

| 参数      | 类型                                   | 说明 |
| --------- | -------------------------------------- | ---- |
| `options` | `CreateInspectionVanillaDriverOptions` | —    |
