---
title: '@retikz/react'
description: Author Kernel and Sugar components in JSX, then connect the generated Core IR to a retained or static Runtime and stable SVG or Canvas host.
---

`@retikz/react` is the primary entry for React applications. You declare figures with JSX components such as `<Layout>`, `<Node>`, `<Path>`, `<Scope>`, and `<Draw>`; its adapter turns props and children into `@retikz/core` IR, then `<Layout>` compiles a Scene and connects it to an SVG or Canvas host.

It owns React authoring and lifecycle wiring rather than new drawing semantics. IR, schemas, geometry, and compilation belong to `@retikz/core`; backend execution and cross-backend runtime belong to `@retikz/render`.

## Install and usage

`react` and `react-dom` are peer dependencies at version 18 or newer:

```bash
pnpm add @retikz/react react react-dom
```

```tsx
import { Draw, Layout, Node } from '@retikz/react';

export const Diagram = () => (
  <Layout>
    <Node id="source" position={[0, 0]}>
      Source
    </Node>
    <Node id="target" position={[160, 0]}>
      Target
    </Node>
    <Draw way={['source', 'target']} arrow="->" />
  </Layout>
);
```

`<Layout>` emits SVG by default; pass `renderer="canvas"` when you need a raster bitmap. Start with [Getting Started](/kernel/get-start), then see [Layout](/kernel/components/layout/overview) for the complete host options.

`<Layout theme>` writes the root `IRScene.theme`, while `<Scope theme>` writes a local `IRScope.theme`. Both author only sparse `style / mode` values; composites choose concrete domain tokens during compile. When `ir` and an explicit `theme` are both present, the prop overrides `ir.theme` per field without mutating either input. See [Layout: Inherited Theme](/kernel/components/layout/overview#inherited-theme) for the complete example.

## Why @retikz/react exists

### JSX reduces authoring overhead

You can construct `@retikz/core` IR directly, but figures commonly contain nested, reusable, and compositional structures. JSX expresses those relationships as a component tree, keeping declarations such as `<Layout>`, `<Scope>`, `<Node>`, and `<Path>` close to the figure hierarchy while reducing the boilerplate of assembling Core IR by hand.

retikz treats React as its officially supported first-class framework: the component API, documentation, examples, and adapter behavior in `@retikz/react` are maintained alongside the Kernel. React is the authoring layer here; the framework-neutral `@retikz/core` still owns the data contract and compilation semantics.

### Why other adapters are not official packages

This does not mean Core can only integrate with React. Any framework can reuse the retikz Kernel by converting its component or template structure into the same Core IR and connecting `compileToScene` to the renderer lifecycle.

`@retikz/react` also serves as the implementation reference for other framework adapters, demonstrating the complete boundaries for component-tree-to-IR conversion, host capability injection, and renderer lifecycle wiring. The official project focuses its maintenance resources on React and does not develop, release, or maintain packages for Vue, Svelte, or other frameworks; the community can implement those adapters independently against the same boundaries.

## Components do not render DOM directly

### Why marker components

Marker components are not used because React is incapable of drawing. They keep drawing semantics in one source of truth. If `<Node>` and `<Path>` each implemented their own DOM, browser layout, and geometry rules, the React DSL would gradually become a second system beside Core IR, and the same figure could produce different results in React, Vanilla, or another adapter.

JSX therefore organizes declarations while the builder converges them into framework-neutral Core IR. The intermediate path creates no extra DOM and does not make CSS layout an implicit input to compilation. Persistence, transport, SSR, and other framework adapters can continue through the same IR → Scene pipeline.

React still operates where it adds value. It declares a stable `<svg>` / `<canvas>` host shell and connects React commit, refs, and unmounting to the selected Runtime host. The default retained path gives a `@retikz/render` participant exclusive ownership of SVG descendants or the Canvas bitmap, while the static path directly materializes the complete Scene. Both paths prevent React reconciliation and the renderer from rewriting the same subtree. Drawing semantics are thin in this layer, not host integration.

Kernel components such as `<Node>`, `<Path>`, and `<Scope>` are **IR markers**, not DOM components. They return `null` and expose only a stable `displayName` that tells the React builder which primitive they represent. A `<Node>` therefore does not produce an intermediate `<div>` or acquire a second browser-layout model.

```tsx
export const Node: FC<NodeProps> = () => null;
Node.displayName = TIKZ_NODE;
```

Sugar components do not add another IR either. `<Draw>`, for example, parses `way` with Core's `parseWay`, then returns standard `<Path>` / `<Step>` Kernel JSX. This excerpt shows only the line branch; the source covers fold, curve, arc, cycle, and the other branches through the same mapping:

```tsx
const steps = parseWay(way);

return (
  <Path arrow={arrow}>
    {steps.map((step, index) => {
      if (step.kind === 'line') {
        return <Step key={index} kind="line" to={step.to} label={step.label} />;
      }
      return null;
    })}
  </Path>
);
```

`readSceneChildren` walks children synchronously: it recognizes Kernel markers, unwraps fragments, and directly invokes function Sugar before recursing into the result. This does not pass through the React DOM renderer, so Kernel and Sugar must be hook-free, side-effect-free function components. `buildIRWithContributions` finally assembles an ordinary JSON `IRScene`.

<SourceLinks
  sources={[
    {
      label: 'Node IR marker implementation',
      path: 'packages/kernel/react/src/kernel/components/Node.tsx',
      startLine: 112,
      endLine: 118,
    },
    {
      label: 'Draw expanding to Path / Step',
      path: 'packages/kernel/react/src/sugar/path/Draw.tsx',
      startLine: 61,
      endLine: 155,
    },
  ]}
/>

### Retained update boundaries

Marker components avoid intermediate DOM and duplicated drawing semantics, but JSX → IR still reruns when children references change. Today, every `ir` or Core-options change also calls `compileToScene` during React render to produce an SSR-safe seed; in the default retained mode, the Runtime Session compiles the same input again after the client commit for its transaction. Retained updates reduce backend commit cost, not this render-time full compile.

Frequently updated large figures can keep stable IR and Core options outside the component or in `useMemo`, then pass the IR prop to skip the builder and let the seed-compile memo hit.

In retained mode, `Layout` creates a Runtime Session after the client commit and updates the complete IR Snapshot plus renderer configuration in one transaction. When Core can prove an update locally safe it publishes a local Scene Patch; other valid changes use a `replaceScene` fallback while preserving full-oracle equivalence. The SVG / Canvas retained participant isolates candidate state during prepare, then publishes DOM / bitmap, events, animation handles, artifacts, and the inspection plane together only after the transaction succeeds.

On unmount, the Session cleans up in reverse order on a best-effort basis, then retries failed renderer cleanup once after logical resources retire. Successful entries never rerun. Persistent failure only enters Runtime diagnostics and does not block React from completing the unmount.

Definition arrays are canonicalized by ordered element identity, so recreating an equivalent container such as `[shape]` does not rebuild the Session. Changing a Definition itself, the renderer backend, `idPrefix`, or `runtime.rendererFactory` does rebuild it. Concurrent scheduling and progressive materialization remain outside the current public contract.

<SourceLinks
  sources={[
    {
      label: 'Layout Definition-container canonicalization',
      path: 'packages/kernel/react/src/kernel/runtime/Layout.tsx',
      startLine: 48,
      endLine: 73,
    },
    {
      label: 'Retained Session creation and initial publish',
      path: 'packages/kernel/react/src/render/runtime/RetainedHost.tsx',
      startLine: 244,
      endLine: 294,
    },
    {
      label: 'Retained Session update transaction',
      path: 'packages/kernel/react/src/render/runtime/RetainedHost.tsx',
      startLine: 305,
      endLine: 323,
    },
  ]}
/>

### Execution modes and update strategies

`<Layout>` uses two orthogonal `runtime` fields to select the host lifecycle and Program update behavior. Omitting the option means `retained + auto`:

```tsx
<Layout /> {/* retained + auto */}
<Layout runtime={{ mode: 'retained', updateStrategy: 'full' }} />
<Layout runtime={{ mode: 'static' }} />
```

| Configuration     | Update behavior                                           | Session, memory, and failure semantics                                                              |
| ----------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `retained + auto` | Safe changes use local Patches; others fully fall back    | Keeps Snapshots, candidates, diagnostics, atomic commits, and rollback; this is the default         |
| `retained + full` | Skips Program `update()` for affected work and runs fully | Retains the same Runtime state and transaction guarantees as auto; it is not a low-memory mode      |
| `static`          | Fully compiles and directly materializes SVG / Canvas     | Creates no Session, Snapshot, diff, transaction, or rollback; less resident state, more update work |

`mode: 'static'` cannot be combined with `updateStrategy`, `rendererFactory`, or `onDiagnostic`. The `LayoutRuntimeOptions` TypeScript union rejects those combinations, and JavaScript calls fail before a host is created. If static compilation fails before host writes, no new picture is published. Errors after materialization, animation, or hydration begins propagate without a guarantee that the old host state is restored.

Changing `updateStrategy` between `auto` and `full` on a retained host disposes the old Session and creates a new one from the current complete input. The host element keeps its identity, while the revision restarts from initial. Changing `mode` releases and replaces the complete host. Choose `retained + full`, not `static`, when you want to keep transactions, diagnostics, and rollback while excluding incremental updates.

<SourceLinks
  sources={[
    {
      label: 'Discriminated Layout runtime options',
      path: 'packages/kernel/react/src/kernel/runtime/runtime-options.ts',
      startLine: 5,
      endLine: 48,
    },
    {
      label: 'Static / retained host dispatch',
      path: 'packages/kernel/react/src/kernel/runtime/Layout.tsx',
      startLine: 539,
      endLine: 584,
    },
    {
      label: 'Retained Session recreation on strategy changes',
      path: 'packages/kernel/react/src/render/runtime/RetainedHost.tsx',
      startLine: 238,
      endLine: 306,
    },
  ]}
/>

## From JSX to Scene

`<Layout>` first calls the React builder to adapt JSX into Core IR. Whenever the input or Core options change, the render phase uses `compileToScene` for an SSR-safe seed; after the client commit, the same Core options create or update the Core Program. In the figure, blue nodes belong to `@retikz/react` authoring / adaptation, while orange nodes are the data contracts and compiler owned by `@retikz/core`.

<ComponentPreview files="react-to-scene" size="xs" hideCode />

| Stage                      | Owner           | Responsibility                                                                                 |
| -------------------------- | --------------- | ---------------------------------------------------------------------------------------------- |
| Kernel / Sugar JSX         | `@retikz/react` | Collect props and children; expand Sugar into Kernel first                                     |
| `buildIRWithContributions` | `@retikz/react` | Walk the component tree, produce `IRScene`, collect embeddable Tier 2 input                    |
| `IRScene`                  | `@retikz/core`  | JSON-serializable drawing contract with no React element, hook, or DOM                         |
| `compileToScene`           | `@retikz/core`  | Process composites; resolve registries, references, geometry, layout, resources, and animation |
| `CompileResult`            | `@retikz/core`  | Return a backend-neutral `scene`, typed `artifacts`, and isolated inspection plane together    |

`Layout` injects host capabilities such as text measurement, Definition registries, and the TeX lowerer as compile options, but those options do not put React into IR or Scene. Passing the `ir` prop skips the JSX builder. The `artifacts` prop requests opt-in outputs, while `onArtifacts` receives immutable same-compile artifacts after the selected host publishes; notification does not trigger another compile.

`<Layout inspect>` supplies the whole-figure layout policy, while `<Scope inspect>` supplies an authored-subtree policy. An inspectable Tier 2 component or `<Path inspect>` can select only its own occurrence. The builder emits an authored sidecar with `target: 'composite' | 'path'` and `policy.self`; `inspect` never enters `IRScene`, `IRScope`, or `IRPath`. `enabled: false` is an unbreakable descendant barrier for both targets.

A static host materializes the primary figure and overlay directly, while a retained host commits both in one revision. An SVG SSR seed includes the same overlay for first hydration, and later policy changes are adopted atomically through a new session. Inspectors require an authored occurrence, its Definition, and a settled subject; a Render entry that receives only a precompiled Scene cannot reconstruct them afterward.

<SourceLinks
  sources={[
    {
      label: 'React builder traversing children',
      path: 'packages/kernel/react/src/kernel/adapter/builder.ts',
      startLine: 585,
      endLine: 650,
    },
    {
      label: 'React children to IRScene',
      path: 'packages/kernel/react/src/kernel/adapter/builder.ts',
      startLine: 685,
      endLine: 701,
    },
    {
      label: 'Layout wiring builder to compile',
      path: 'packages/kernel/react/src/kernel/runtime/Layout.tsx',
      startLine: 442,
      endLine: 522,
    },
    {
      label: 'Core IR to Scene compiler',
      path: 'packages/kernel/core/src/compile/compile.ts',
      startLine: 25,
      endLine: 53,
    },
  ]}
/>

## After Scene

Scene no longer contains React component semantics. In the default retained mode, `@retikz/render` executes Snapshots / Patches against a target backend, while `@retikz/react` adds only React host lifecycle and Session wiring:

| Target | `@retikz/render`                                                        | `@retikz/react`                                           |
| ------ | ----------------------------------------------------------------------- | --------------------------------------------------------- |
| SVG    | Retained renderer owns descendants and applies a Patch or full fallback | Declares a stable `<svg>` shell, then adopts the SSR seed |
| Canvas | Retained renderer updates dirty regions or uses a full fallback         | Declares a stable `<canvas>` shell and fixes DPR at mount |

Event handlers are functions and cannot enter JSON IR. In children mode, React separately runs `collectHydrationHandlers` to collect functions by primitive id, then binds them after rendering through SVG location or Canvas hit-testing. With the `ir` prop, the `handlers` prop supplies this runtime side channel instead.

<SourceLinks
  sources={[
    {
      label: 'Layout retained-host wiring',
      path: 'packages/kernel/react/src/kernel/runtime/Layout.tsx',
      startLine: 562,
      endLine: 584,
    },
    {
      label: 'Retained host shell declaration',
      path: 'packages/kernel/react/src/render/runtime/RetainedHost.tsx',
      startLine: 329,
      endLine: 356,
    },
    {
      label: 'JSX handler runtime side channel',
      path: 'packages/kernel/react/src/kernel/runtime/collect-hydration-handlers.ts',
      startLine: 53,
      endLine: 105,
    },
  ]}
/>

## Responsibility boundaries

| Capability              | What `@retikz/react` owns                                      | Source of truth        |
| ----------------------- | -------------------------------------------------------------- | ---------------------- |
| JSX DSL                 | Kernel components construct IR; Sugar expands into Kernel      | `@retikz/core` IR      |
| Compilation host        | Collect children / IR, inject options, connect React lifecycle | `@retikz/core` compile |
| Rendering & interaction | Select SVG / Canvas, collect events, connect the host          | `@retikz/render`       |

`@retikz/react` does not include framework-free plain specs. Use [`@retikz/vanilla`](/kernel/packages/vanilla) outside React, for SSR strings, or for direct DOM mounting.

## Continue reading

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/kernel/get-start">
    <span className="font-semibold">Getting Started</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Install the package and build your first React figure
    </span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/layout">
    <span className="font-semibold">Layout</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      Understand figure input, capability injection, and the host boundary
    </span>
  </LinkedCard>
</div>
