{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/foundation`

### RetikzError

<p>
  <ApiSourceLink label={'RetikzError'} path={'packages/kernel/foundation/src/error.ts'} startLine={29}>
    Retikz 结构化领域错误的基础骨架
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&>div:last-child]:mb-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员               | 类型                                             | 默认值      | 说明                                       |
| ------------------ | ------------------------------------------------ | ----------- | ------------------------------------------ |
| `constructor`      | `(options: RetikzErrorOptions<TCode, TDetails>)` | —           | 使用结构化参数创建领域错误，保留原始 cause |
| `readonly cause?`  | `unknown`                                        | `undefined` | 导致当前错误的原始异常或值                 |
| `readonly code`    | `TCode`                                          | —           | 结构化错误的分类代码                       |
| `readonly details` | `TDetails`                                       | —           | 与错误代码关联的结构化错误详情             |

</div>

#### 类型参数

| 类型参数   | 说明                           |
| ---------- | ------------------------------ |
| `TCode`    | 错误分类代码的字符串类型       |
| `TDetails` | 与错误代码关联的结构化详情类型 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export class RetikzError<
  TCode extends string = string,
  TDetails extends Readonly<Record<string, unknown>> = Readonly<Record<string, unknown>>,
> extends Error {
  constructor(options: RetikzErrorOptions<TCode, TDetails>);
}
```

</DocTab>

</DocTabs>

### RetikzFoundationError

<p>
  <ApiSourceLink label={'RetikzFoundationError'} path={'packages/kernel/foundation/src/error.ts'} startLine={79}>
    Foundation 原子契约失败的统一结构化错误
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&>div:last-child]:mb-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[44%]">

| 成员          | 签名                                             | 说明                               |
| ------------- | ------------------------------------------------ | ---------------------------------- |
| `constructor` | `(message: string)`                              | 使用默认错误码创建 Foundation 错误 |
|               | `(options: RetikzErrorOptions<TCode, TDetails>)` | 使用结构化参数创建 Foundation 错误 |

</div>

#### 类型参数

| 类型参数   | 说明                                     |
| ---------- | ---------------------------------------- |
| `TCode`    | Foundation 错误码的具体字面量类型        |
| `TDetails` | 与 Foundation 错误码关联的结构化详情类型 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export class RetikzFoundationError<
  TCode extends RetikzFoundationErrorCodeValue = RetikzFoundationErrorCodeValue,
  TDetails extends Readonly<Record<string, unknown>> = Readonly<Record<string, unknown>>,
> extends RetikzError<TCode, TDetails> {
  constructor(message: string);
  constructor(options: RetikzErrorOptions<TCode, TDetails>);
}
```

</DocTab>

</DocTabs>

### AssertEqual

<p>
  <ApiSourceLink label={'AssertEqual'} path={'packages/kernel/foundation/src/types.ts'} startLine={24}>
    双向检查两个类型是否等价
  </ApiSourceLink>
</p>

```ts
export type AssertEqual<TActual, TExpected> = [TActual] extends [TExpected]
  ? [TExpected] extends [TActual]
    ? true
    : false
  : false;
```

#### 类型参数

| 类型参数    | 说明                   |
| ----------- | ---------------------- |
| `TActual`   | 待比较的实际类型       |
| `TExpected` | 作为比较基准的预期类型 |

### JsonObject

<p>
  <ApiSourceLink label={'JsonObject'} path={'packages/kernel/foundation/src/types.ts'} startLine={16}>
    字符串键 JSON 对象
  </ApiSourceLink>
</p>

```ts
export type JsonObject = ZodInfer<typeof JsonObjectSchema>;
```

#### 索引签名

```ts
[key: string]: JsonValue
```

### JsonValue

<p>
  <ApiSourceLink label={'JsonValue'} path={'packages/kernel/foundation/src/types.ts'} startLine={13}>
    单个递归 JSON 值；readonly 数组允许 const Source 在 parse 边界直接收窄
  </ApiSourceLink>
</p>

```ts
export type JsonValue =
  | string
  | number
  | boolean
  | null
  | ReadonlyArray<JsonValue>
  | {
      [key: string]: JsonValue;
    };
```

### MergePropertiesOptions

<p>
  <ApiSourceLink label={'MergePropertiesOptions'} path={'packages/kernel/foundation/src/objects.ts'} startLine={8}>
    属性浅合并的写入策略
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&>div:last-child]:mb-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员                       | 类型                                               | 默认值      | 说明                                                            |
| -------------------------- | -------------------------------------------------- | ----------- | --------------------------------------------------------------- |
| `readonly shouldOverride?` | `(value: T[keyof T], key: PropertyKey) => boolean` | `undefined` | 决定源属性是否参与写入；首次写入也调用，返回 false 时保留已有值 |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type MergePropertiesOptions<T extends object> = Readonly<{
  shouldOverride?: (value: T[keyof T], key: PropertyKey) => boolean;
}>;
```

</DocTab>

</DocTabs>

#### 类型参数

| 类型参数 | 说明             |
| -------- | ---------------- |
| `T`      | 被合并的对象类型 |

### NonEmptyReadonlyArray

<p>
  <ApiSourceLink label={'NonEmptyReadonlyArray'} path={'packages/kernel/foundation/src/types.ts'} startLine={58}>
    至少包含一个元素的只读数组
  </ApiSourceLink>
</p>

```ts
export type NonEmptyReadonlyArray<T> = readonly [T, ...Array<T>];
```

#### 类型参数

| 类型参数 | 说明         |
| -------- | ------------ |
| `T`      | 数组元素类型 |

### OpenString

<p>
  <ApiSourceLink label={'OpenString'} path={'packages/kernel/foundation/src/types.ts'} startLine={35}>
    保留已知字符串提示，同时接受任意字符串
  </ApiSourceLink>
</p>

```ts
export type OpenString<T extends string> = T | (string & {});
```

#### 类型参数

| 类型参数 | 说明                       |
| -------- | -------------------------- |
| `T`      | 已知字符串字面量的联合类型 |

### ParsedCssColor

<p>
  <ApiSourceLink label={'ParsedCssColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={4}>
    归一化 sRGB 颜色与 alpha
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&>div:last-child]:mb-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员 | 类型     | 默认值 | 说明               |
| ---- | -------- | ------ | ------------------ |
| `a`  | `number` | —      | 不透明度，范围 0~1 |
| `b`  | `number` | —      | 蓝色通道，范围 0~1 |
| `g`  | `number` | —      | 绿色通道，范围 0~1 |
| `r`  | `number` | —      | 红色通道，范围 0~1 |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ParsedCssColor = {
  r: number;
  g: number;
  b: number;
  a: number;
};
```

</DocTab>

</DocTabs>

### RetikzErrorOptions

<p>
  <ApiSourceLink label={'RetikzErrorOptions'} path={'packages/kernel/foundation/src/error.ts'} startLine={9}>
    Retikz 结构化领域错误的基础构造参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

<div className="[&>div:first-child]:mt-0 [&>div:last-child]:mb-0 [&_table]:table-fixed [&_th]:whitespace-normal [&_th]:px-2 [&_td]:px-2 [&_td]:align-top [&_td]:[overflow-wrap:anywhere] [&_code]:whitespace-normal [&_th:nth-child(1)]:w-[22%] [&_th:nth-child(2)]:w-[28%] [&_th:nth-child(3)]:w-[14%]">

| 成员               | 类型       | 默认值      | 说明                           |
| ------------------ | ---------- | ----------- | ------------------------------ |
| `readonly cause?`  | `unknown`  | `undefined` | 导致当前错误的原始异常或值     |
| `readonly code`    | `TCode`    | —           | 结构化错误的分类代码           |
| `readonly details` | `TDetails` | —           | 与错误代码关联的结构化错误详情 |
| `readonly message` | `string`   | —           | 面向调用方的错误消息           |

</div>

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type RetikzErrorOptions<TCode extends string, TDetails extends Readonly<Record<string, unknown>>> = Readonly<{
  code: TCode;
  message: string;
  details: TDetails;
  cause?: unknown;
}>;
```

</DocTab>

</DocTabs>

#### 类型参数

| 类型参数   | 说明                           |
| ---------- | ------------------------------ |
| `TCode`    | 错误分类代码的字符串类型       |
| `TDetails` | 与错误代码关联的结构化详情类型 |

### RetikzFoundationErrorCodeValue

<p>
  <ApiSourceLink
    label={'RetikzFoundationErrorCodeValue'}
    path={'packages/kernel/foundation/src/error.ts'}
    startLine={71}
  >
    Foundation 包稳定错误码取值
  </ApiSourceLink>
</p>

<DocTabs defaultValue="expanded">

<DocTab value="expanded" label="展开类型">

```ts
export type RetikzFoundationErrorCodeValue =
  | 'FOUNDATION_ERROR'
  | 'FOUNDATION_NON_EMPTY_STRING_REQUIRED'
  | 'FOUNDATION_POSITIVE_NUMBER_REQUIRED'
  | 'FOUNDATION_JSON_ERROR'
  | 'FOUNDATION_COLOR_ERROR';
```

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type RetikzFoundationErrorCodeValue = ValueOf<typeof RetikzFoundationErrorCode>;
```

</DocTab>

</DocTabs>

### ValueOf

<p>
  <ApiSourceLink label={'ValueOf'} path={'packages/kernel/foundation/src/types.ts'} startLine={10}>
    取得对象所有 value 的联合类型
  </ApiSourceLink>
</p>

```ts
export type ValueOf<T extends object> = T[keyof T];
```

#### 类型参数

| 类型参数 | 说明                 |
| -------- | -------------------- |
| `T`      | 提供属性值的对象类型 |

### WithOptionalProperties

<p>
  <ApiSourceLink label={'WithOptionalProperties'} path={'packages/kernel/foundation/src/types.ts'} startLine={51}>
    将指定属性扩展为可选，同时保留其余属性
  </ApiSourceLink>
</p>

```ts
export type WithOptionalProperties<T, TKey extends keyof T> = Omit<T, TKey> & Partial<Pick<T, TKey>>;
```

#### 类型参数

| 类型参数 | 说明                 |
| -------- | -------------------- |
| `T`      | 待调整属性的对象类型 |
| `TKey`   | 需要设为可选的属性键 |

### WithRequiredProperties

<p>
  <ApiSourceLink label={'WithRequiredProperties'} path={'packages/kernel/foundation/src/types.ts'} startLine={43}>
    将指定属性收窄为必填，同时保留其余属性
  </ApiSourceLink>
</p>

```ts
export type WithRequiredProperties<T, TKey extends keyof T> = T & Required<Pick<T, TKey>>;
```

#### 类型参数

| 类型参数 | 说明                 |
| -------- | -------------------- |
| `T`      | 待调整属性的对象类型 |
| `TKey`   | 需要设为必填的属性键 |

### RetikzFoundationErrorCode

<p>
  <ApiSourceLink label={'RetikzFoundationErrorCode'} path={'packages/kernel/foundation/src/error.ts'} startLine={57}>
    Foundation 包稳定错误码
  </ApiSourceLink>
</p>

```ts
export const RetikzFoundationErrorCode = {
  Default: 'FOUNDATION_ERROR',
  NonEmptyStringRequired: 'FOUNDATION_NON_EMPTY_STRING_REQUIRED',
  PositiveNumberRequired: 'FOUNDATION_POSITIVE_NUMBER_REQUIRED',
  Json: 'FOUNDATION_JSON_ERROR',
  Color: 'FOUNDATION_COLOR_ERROR',
} as const;
```

### StaticCssNamedColorHexByName

<p>
  <ApiSourceLink
    label={'StaticCssNamedColorHexByName'}
    path={'packages/kernel/foundation/src/color.ts'}
    startLine={171}
  >
    静态 CSS 命名颜色对应的小写十六进制清单
  </ApiSourceLink>
</p>

仅包含 `parseStaticCssColor()` 支持的命名颜色，不包含 transparent、函数和十六进制写法

```ts
export const StaticCssNamedColorHexByName: Readonly<Record<string, `#${string}`>> = Object.freeze(
  Object.fromEntries(
    Object.entries(CSS_NAMED_COLOR_HEX).map(([name, colorHex]): [string, `#${string}`] => [
      name,
      `#${colorHex.toString(16).padStart(6, '0')}`,
    ]),
  ),
);
```

### assertNonEmptyString

<p>
  <ApiSourceLink label={'assertNonEmptyString'} path={'packages/kernel/foundation/src/assert.ts'} startLine={12}>
    拒绝空串和全空白字符串
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 参数

| 参数         | 类型                                                                  | 说明                                                   |
| ------------ | --------------------------------------------------------------------- | ------------------------------------------------------ |
| `value`      | `string`                                                              | 待检查的字符串                                         |
| `label`      | `string`                                                              | 错误消息中的字段名称                                   |
| `ownerError` | `RetikzError<string, Readonly<Record<string, unknown>>> \| undefined` | 校验失败时原样抛出的领域错误；默认使用 Foundation 错误 |

#### 异常

- 校验失败时抛出 ownerError 或 RetikzFoundationError

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const assertNonEmptyString: (value: string, label: string, ownerError?: RetikzError | undefined) => void;
```

</DocTab>

</DocTabs>

### assertPlainDataContainers

<p>
  <ApiSourceLink label={'assertPlainDataContainers'} path={'packages/kernel/foundation/src/json.ts'} startLine={82}>
    断言一棵外部数据树只使用普通对象、普通数组与安全属性描述符
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 参数

| 参数    | 类型      | 说明                                       |
| ------- | --------- | ------------------------------------------ |
| `value` | `unknown` | 待检查的外部数据树                         |
| `path`  | `string`  | 错误消息中用于定位根值的路径，默认 'value' |

> **备注：** 叶子值域由调用方 schema 校验；本函数只在读取属性前排除原型对象、accessor、symbol、异常数组与循环引用

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const assertPlainDataContainers: (value: unknown, path?: string) => void;
```

</DocTab>

</DocTabs>

### assertPositiveNumber

<p>
  <ApiSourceLink label={'assertPositiveNumber'} path={'packages/kernel/foundation/src/assert.ts'} startLine={35}>
    拒绝非有限数值以及小于等于零的数值
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 参数

| 参数         | 类型                                                                  | 说明                                                   |
| ------------ | --------------------------------------------------------------------- | ------------------------------------------------------ |
| `value`      | `number`                                                              | 待检查的数值                                           |
| `label`      | `string`                                                              | 错误消息中的字段名称                                   |
| `ownerError` | `RetikzError<string, Readonly<Record<string, unknown>>> \| undefined` | 校验失败时原样抛出的领域错误；默认使用 Foundation 错误 |

#### 异常

- 校验失败时抛出 ownerError 或 RetikzFoundationError

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const assertPositiveNumber: (value: number, label: string, ownerError?: RetikzError | undefined) => void;
```

</DocTab>

</DocTabs>

### cloneAndFreezeJson

<p>
  <ApiSourceLink label={'cloneAndFreezeJson'} path={'packages/kernel/foundation/src/json.ts'} startLine={94}>
    校验 JSON 安全的普通数据，创建脱离原输入的深冻结副本
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 类型参数

| 类型参数 | 说明             |
| -------- | ---------------- |
| `T`      | 输入值的静态类型 |

#### 参数

| 参数    | 类型     | 说明                                         |
| ------- | -------- | -------------------------------------------- |
| `value` | `T`      | 待复制的 JSON 安全数据                       |
| `path`  | `string` | 错误消息中用于定位当前值的路径，默认 'value' |

#### 返回值

与输入结构相同但不共享可变对象的深冻结副本

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const cloneAndFreezeJson: <T>(value: T, path?: string) => T;
```

</DocTab>

</DocTabs>

### compositeOpaqueColor

<p>
  <ApiSourceLink label={'compositeOpaqueColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={367}>
    把静态 CSS 前景色按权重预合成到不透明静态底色
  </ApiSourceLink>
</p>

前景自身 alpha 与 weight 相乘，再按 source-over sRGB 得到不含透明度的确定性颜色

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 参数

| 参数         | 类型     | 说明                          |
| ------------ | -------- | ----------------------------- |
| `foreground` | `string` | 可带透明度的静态 CSS 前景色   |
| `backdrop`   | `string` | 必须完全不透明的静态 CSS 底色 |
| `weight`     | `number` | 闭区间 0..1 内的有限合成权重  |

#### 返回值

六位小写十六进制不透明颜色

#### 异常

- 颜色不支持、底色含透明度或权重越界时抛出 RetikzFoundationError

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const compositeOpaqueColor: (foreground: string, backdrop: string, weight: number) => `#${string}`;
```

</DocTab>

</DocTabs>

### createOpenStringSchema

<p>
  <ApiSourceLink label={'createOpenStringSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={30}>
    从已知 const object enum 建立保留提示的开放非空字符串 schema
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 类型参数

| 类型参数  | 说明                                  |
| --------- | ------------------------------------- |
| `TValues` | 已知字符串值的 const object enum 类型 |

#### 参数

| 参数     | 类型      | 说明                       |
| -------- | --------- | -------------------------- |
| `values` | `TValues` | 提供已知字符串值的枚举对象 |

#### 返回值

接受已知枚举值或自定义非空白字符串的 schema

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const createOpenStringSchema: <const TValues extends Readonly<Record<string, string>>>(
  values: TValues,
) => ZodType<OpenString<ValueOf<TValues>>, OpenString<ValueOf<TValues>>>;
```

</DocTab>

</DocTabs>

### createReadonlyMap

<p>
  <ApiSourceLink label={'createReadonlyMap'} path={'packages/kernel/foundation/src/collections.ts'} startLine={9}>
    创建不暴露写方法的 Map 浅快照
  </ApiSourceLink>
</p>

输入 entries 会复制到独立存储；迭代与查询保持原生 Map 语义，`forEach` 的 owner 参数返回只读快照自身

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 类型参数

| 类型参数 | 说明       |
| -------- | ---------- |
| `TKey`   | Map 键类型 |
| `TValue` | Map 值类型 |

#### 参数

| 参数      | 类型                                | 说明                 |
| --------- | ----------------------------------- | -------------------- |
| `entries` | `Iterable<readonly [TKey, TValue]>` | 用于创建快照的键值对 |

#### 返回值

独立的只读 Map 包装；键和值仍共享原引用，不进行深冻结

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const createReadonlyMap: <TKey, TValue>(
  entries: Iterable<readonly [TKey, TValue]>,
) => ReadonlyMap<TKey, TValue>;
```

</DocTab>

</DocTabs>

### isRetikzError

<p>
  <ApiSourceLink label={'isRetikzError'} path={'packages/kernel/foundation/src/error.ts'} startLine={105}>
    判断动态值是否继承自 Retikz 结构化领域错误
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 参数

| 参数    | 类型      | 说明           |
| ------- | --------- | -------------- |
| `value` | `unknown` | 待判断的动态值 |

#### 返回值

是否为 RetikzError 实例；返回 true 时收窄类型

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const isRetikzError: (value: unknown) => value is RetikzError;
```

</DocTab>

</DocTabs>

### mergeProperties

<p>
  <ApiSourceLink label={'mergeProperties'} path={'packages/kernel/foundation/src/objects.ts'} startLine={24}>
    从左到右浅合并对象的自身可枚举属性，返回新对象
  </ApiSourceLink>
</p>

包含字符串与 symbol 键，跳过 undefined 源；默认保留所有属性值，不递归合并或修改输入

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 类型参数

| 类型参数 | 说明               |
| -------- | ------------------ |
| `T`      | 被浅合并的对象类型 |

#### 参数

| 参数      | 类型                                       | 说明                         |
| --------- | ------------------------------------------ | ---------------------------- |
| `sources` | `readonly Array<Readonly<T> \| undefined>` | 按优先级从低到高排列的源对象 |
| `options` | `MergePropertiesOptions<NoInfer<T>>`       | 属性写入策略                 |

#### 返回值

可能经过过滤的新对象，因此返回类型为 `Partial<T>`

#### 异常

- 属性读取或策略回调失败时抛出 RetikzFoundationError，原始异常保留为 cause

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const mergeProperties: <T extends object>(
  sources: ReadonlyArray<Readonly<T> | undefined>,
  options?: MergePropertiesOptions<NoInfer<T>>,
) => Partial<T>;
```

</DocTab>

</DocTabs>

### parseStaticCssColor

<p>
  <ApiSourceLink label={'parseStaticCssColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={318}>
    解析无需宿主环境即可确定的静态 CSS color 子集
  </ApiSourceLink>
</p>

支持命名颜色、transparent、十六进制、逗号分隔的 rgb/rgba/hsl/hsla 和空格分隔的 rgb/hsl；不解析 currentColor 或 var 等环境相关表达式

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

#### 参数

| 参数    | 类型     | 说明                |
| ------- | -------- | ------------------- |
| `input` | `string` | 静态 CSS 颜色字符串 |

#### 返回值

归一化 sRGB 通道与 alpha；无效或不支持的颜色返回 null

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export declare const parseStaticCssColor: (input: string) => ParsedCssColor | null;
```

</DocTab>

</DocTabs>
