{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/foundation`

### RetikzError

<p>
  <ApiSourceLink label={'RetikzError'} path={'packages/kernel/foundation/src/error.ts'} startLine={29}>
    Base structure for Retikz structured domain errors
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member                      | Type                                                                            | Default     | Description                                                                                                                                             |
| --------------------------- | ------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TCode` (Type parameter)    | `extends string = string`                                                       | —           | String type of the error classification code                                                                                                            |
| `TDetails` (Type parameter) | `extends Readonly<Record<string, unknown>> = Readonly<Record<string, unknown>>` | —           | Structured detail type associated with the error code                                                                                                   |
| `constructor`               | `(options: RetikzErrorOptions<TCode, TDetails>)`                                | —           | Creates a domain error from structured options, preserving the original cause<br />`options`: Error code, message, details, and optional original cause |
| `readonly cause?`           | `unknown`                                                                       | `undefined` | Original exception or value that caused this error                                                                                                      |
| `readonly code`             | `TCode`                                                                         | —           | Classification code of the structured error                                                                                                             |
| `readonly details`          | `TDetails`                                                                      | —           | Structured error details associated with the error code                                                                                                 |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export class RetikzError<
  TCode extends string = string,
  TDetails extends Readonly<Record<string, unknown>> = Readonly<Record<string, unknown>>,
> extends Error {
  readonly code: TCode;
  readonly details: TDetails;
  readonly cause?: unknown;
  constructor(options: RetikzErrorOptions<TCode, TDetails>);
}
```

</DocTab>

</DocTabs>

### RetikzFoundationError

<p>
  <ApiSourceLink label={'RetikzFoundationError'} path={'packages/kernel/foundation/src/error.ts'} startLine={79}>
    Unified structured error for Foundation atomic-contract failures
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="methods">

| Member                      | Signature                                                                       | Description                                                    |
| --------------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| `TCode` (Type parameter)    | `extends RetikzFoundationErrorCodeValue = RetikzFoundationErrorCodeValue`       | Concrete literal type of a Foundation error code               |
| `TDetails` (Type parameter) | `extends Readonly<Record<string, unknown>> = Readonly<Record<string, unknown>>` | Structured detail type associated with a Foundation error code |
| `constructor`               | `(message: string)`                                                             | Creates a Foundation error with the default error code         |
|                             | `(options: RetikzErrorOptions<TCode, TDetails>)`                                | Creates a Foundation error from structured options             |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export class RetikzFoundationError<
  TCode extends RetikzFoundationErrorCodeValue = RetikzFoundationErrorCodeValue,
  TDetails extends Readonly<Record<string, unknown>> = Readonly<Record<string, unknown>>,
> extends RetikzError<TCode, TDetails> {
  constructor(message: string);
  constructor(options: RetikzErrorOptions<TCode, TDetails>);
}
```

</DocTab>

</DocTabs>

### AssertEqual

<p>
  <ApiSourceLink label={'AssertEqual'} path={'packages/kernel/foundation/src/types.ts'} startLine={24}>
    Bi-directionally checks whether two types are equivalent
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member                       | Type | Default | Description                                   |
| ---------------------------- | ---- | ------- | --------------------------------------------- |
| `TActual` (Type parameter)   | `—`  | —       | Actual type being compared                    |
| `TExpected` (Type parameter) | `—`  | —       | Expected type used as the comparison baseline |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type AssertEqual<TActual, TExpected> = [TActual] extends [TExpected]
  ? [TExpected] extends [TActual]
    ? true
    : false
  : false;
```

</DocTab>

</DocTabs>

### JsonObject

<p>
  <ApiSourceLink label={'JsonObject'} path={'packages/kernel/foundation/src/types.ts'} startLine={16}>
    String-keyed JSON object
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member          | Type                       | Default | Description |
| --------------- | -------------------------- | ------- | ----------- |
| Index signature | `[key: string]: JsonValue` | —       | —           |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type JsonObject = ZodInfer<typeof JsonObjectSchema>;
```

</DocTab>

</DocTabs>

### JsonValue

<p>
  <ApiSourceLink label={'JsonValue'} path={'packages/kernel/foundation/src/types.ts'} startLine={13}>
    A single recursive JSON value. Readonly arrays allow const sources to narrow directly at the parse boundary.
  </ApiSourceLink>
</p>

```ts
export type JsonValue =
  | string
  | number
  | boolean
  | null
  | ReadonlyArray<JsonValue>
  | {
      [key: string]: JsonValue;
    };
```

### MergePropertiesOptions

<p>
  <ApiSourceLink label={'MergePropertiesOptions'} path={'packages/kernel/foundation/src/objects.ts'} startLine={8}>
    Write policy for shallow property merging
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member                     | Type                                               | Default     | Description                                                                                                        |
| -------------------------- | -------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| `T` (Type parameter)       | `extends object`                                   | —           | Object type being merged                                                                                           |
| `readonly shouldOverride?` | `(value: T[keyof T], key: PropertyKey) => boolean` | `undefined` | Decides whether a source property is written; also runs for initial writes, and false preserves any existing value |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type MergePropertiesOptions<T extends object> = Readonly<{
  shouldOverride?: (value: T[keyof T], key: PropertyKey) => boolean;
}>;
```

</DocTab>

</DocTabs>

### NonEmptyReadonlyArray

<p>
  <ApiSourceLink label={'NonEmptyReadonlyArray'} path={'packages/kernel/foundation/src/types.ts'} startLine={58}>
    Readonly array with at least one element
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member               | Type | Default | Description        |
| -------------------- | ---- | ------- | ------------------ |
| `T` (Type parameter) | `—`  | —       | Array element type |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type NonEmptyReadonlyArray<T> = readonly [T, ...Array<T>];
```

</DocTab>

</DocTabs>

### OpenString

<p>
  <ApiSourceLink label={'OpenString'} path={'packages/kernel/foundation/src/types.ts'} startLine={35}>
    Preserves known-string suggestions while accepting any string
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member               | Type             | Default | Description                    |
| -------------------- | ---------------- | ------- | ------------------------------ |
| `T` (Type parameter) | `extends string` | —       | Union of known string literals |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type OpenString<T extends string> = T | (string & {});
```

</DocTab>

</DocTabs>

### ParsedCssColor

<p>
  <ApiSourceLink label={'ParsedCssColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={4}>
    Normalized sRGB color with alpha
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member | Type     | Default | Description                    |
| ------ | -------- | ------- | ------------------------------ |
| `a`    | `number` | —       | Opacity in the range 0–1       |
| `b`    | `number` | —       | Blue channel in the range 0–1  |
| `g`    | `number` | —       | Green channel in the range 0–1 |
| `r`    | `number` | —       | Red channel in the range 0–1   |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ParsedCssColor = {
  r: number;
  g: number;
  b: number;
  a: number;
};
```

</DocTab>

</DocTabs>

### RetikzErrorOptions

<p>
  <ApiSourceLink label={'RetikzErrorOptions'} path={'packages/kernel/foundation/src/error.ts'} startLine={9}>
    Base construction parameters for a Retikz structured domain error
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member                      | Type                                        | Default     | Description                                             |
| --------------------------- | ------------------------------------------- | ----------- | ------------------------------------------------------- |
| `TCode` (Type parameter)    | `extends string`                            | —           | String type of the error classification code            |
| `TDetails` (Type parameter) | `extends Readonly<Record<string, unknown>>` | —           | Structured detail type associated with the error code   |
| `readonly cause?`           | `unknown`                                   | `undefined` | Original exception or value that caused this error      |
| `readonly code`             | `TCode`                                     | —           | Classification code of the structured error             |
| `readonly details`          | `TDetails`                                  | —           | Structured error details associated with the error code |
| `readonly message`          | `string`                                    | —           | Error message for the caller                            |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type RetikzErrorOptions<TCode extends string, TDetails extends Readonly<Record<string, unknown>>> = Readonly<{
  code: TCode;
  message: string;
  details: TDetails;
  cause?: unknown;
}>;
```

</DocTab>

</DocTabs>

### RetikzFoundationErrorCodeValue

<p>
  <ApiSourceLink
    label={'RetikzFoundationErrorCodeValue'}
    path={'packages/kernel/foundation/src/error.ts'}
    startLine={71}
  >
    Stable Foundation package error-code values
  </ApiSourceLink>
</p>

<DocTabs defaultValue="expanded">

<DocTab value="expanded" label="Expanded type">

```ts
export type RetikzFoundationErrorCodeValue =
  | 'FOUNDATION_ERROR'
  | 'FOUNDATION_NON_EMPTY_STRING_REQUIRED'
  | 'FOUNDATION_POSITIVE_NUMBER_REQUIRED'
  | 'FOUNDATION_JSON_ERROR'
  | 'FOUNDATION_COLOR_ERROR';
```

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type RetikzFoundationErrorCodeValue = ValueOf<typeof RetikzFoundationErrorCode>;
```

</DocTab>

</DocTabs>

### ValueOf

<p>
  <ApiSourceLink label={'ValueOf'} path={'packages/kernel/foundation/src/types.ts'} startLine={10}>
    Obtains the union of all object values
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member               | Type             | Default | Description                                   |
| -------------------- | ---------------- | ------- | --------------------------------------------- |
| `T` (Type parameter) | `extends object` | —       | Object type that provides the property values |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ValueOf<T extends object> = T[keyof T];
```

</DocTab>

</DocTabs>

### WithOptionalProperties

<p>
  <ApiSourceLink label={'WithOptionalProperties'} path={'packages/kernel/foundation/src/types.ts'} startLine={51}>
    Makes selected properties optional while preserving the remaining properties
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member                  | Type              | Default | Description                               |
| ----------------------- | ----------------- | ------- | ----------------------------------------- |
| `T` (Type parameter)    | `—`               | —       | Object type whose properties are adjusted |
| `TKey` (Type parameter) | `extends keyof T` | —       | Property keys made optional               |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type WithOptionalProperties<T, TKey extends keyof T> = Omit<T, TKey> & Partial<Pick<T, TKey>>;
```

</DocTab>

</DocTabs>

### WithRequiredProperties

<p>
  <ApiSourceLink label={'WithRequiredProperties'} path={'packages/kernel/foundation/src/types.ts'} startLine={43}>
    Narrows selected properties to required while preserving the remaining properties
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable>

| Member                  | Type              | Default | Description                               |
| ----------------------- | ----------------- | ------- | ----------------------------------------- |
| `T` (Type parameter)    | `—`               | —       | Object type whose properties are adjusted |
| `TKey` (Type parameter) | `extends keyof T` | —       | Property keys made required               |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type WithRequiredProperties<T, TKey extends keyof T> = T & Required<Pick<T, TKey>>;
```

</DocTab>

</DocTabs>

### RetikzFoundationErrorCode

<p>
  <ApiSourceLink label={'RetikzFoundationErrorCode'} path={'packages/kernel/foundation/src/error.ts'} startLine={57}>
    Stable Foundation package error codes
  </ApiSourceLink>
</p>

```ts
export const RetikzFoundationErrorCode = {
  Default: 'FOUNDATION_ERROR',
  NonEmptyStringRequired: 'FOUNDATION_NON_EMPTY_STRING_REQUIRED',
  PositiveNumberRequired: 'FOUNDATION_POSITIVE_NUMBER_REQUIRED',
  Json: 'FOUNDATION_JSON_ERROR',
  Color: 'FOUNDATION_COLOR_ERROR',
} as const;
```

### StaticCssNamedColorHexByName

<p>
  <ApiSourceLink
    label={'StaticCssNamedColorHexByName'}
    path={'packages/kernel/foundation/src/color.ts'}
    startLine={171}
  >
    Lowercase hexadecimal values for static named CSS colors
  </ApiSourceLink>
</p>

Contains only named colors supported by `parseStaticCssColor()`; excludes transparent, functions, and hexadecimal notation.

```ts
export const StaticCssNamedColorHexByName: Readonly<Record<string, `#${string}`>> = Object.freeze(
  Object.fromEntries(
    Object.entries(CSS_NAMED_COLOR_HEX).map(([name, colorHex]): [string, `#${string}`] => [
      name,
      `#${colorHex.toString(16).padStart(6, '0')}`,
    ]),
  ),
);
```

### assertNonEmptyString

<p>
  <ApiSourceLink label={'assertNonEmptyString'} path={'packages/kernel/foundation/src/assert.ts'} startLine={12}>
    Rejects empty and all-whitespace strings
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category   | Name          | Type / signature           | Description                                                                         |
| ---------- | ------------- | -------------------------- | ----------------------------------------------------------------------------------- |
| Parameters | `value`       | `string`                   | String to validate                                                                  |
|            | `label`       | `string`                   | Field name used in error messages                                                   |
|            | `ownerError?` | `RetikzError \| undefined` | Domain error thrown unchanged on validation failure; defaults to a Foundation error |
| Returns    | —             | `void`                     | —                                                                                   |
| Throws     | —             | —                          | Throws ownerError or RetikzFoundationError on validation failure                    |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const assertNonEmptyString: (value: string, label: string, ownerError?: RetikzError | undefined) => void;
```

</DocTab>

</DocTabs>

### assertPlainDataContainers

<p>
  <ApiSourceLink label={'assertPlainDataContainers'} path={'packages/kernel/foundation/src/json.ts'} startLine={82}>
    Asserts that an external data tree uses only plain objects, plain arrays, and safe property descriptors
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category   | Name    | Type / signature | Description                                                               |
| ---------- | ------- | ---------------- | ------------------------------------------------------------------------- |
| Parameters | `value` | `unknown`        | External data tree to inspect                                             |
|            | `path?` | `string`         | Path used to locate the root value in error messages; defaults to 'value' |
| Returns    | —       | `void`           | —                                                                         |

</ApiTable>

> **Notes:** The caller schema validates leaf values. This function only excludes prototype objects, accessors, symbols, exotic arrays, and cycles before reading properties.

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const assertPlainDataContainers: (value: unknown, path?: string) => void;
```

</DocTab>

</DocTabs>

### assertPositiveNumber

<p>
  <ApiSourceLink label={'assertPositiveNumber'} path={'packages/kernel/foundation/src/assert.ts'} startLine={35}>
    Rejects non-finite numbers and numbers less than or equal to zero
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category   | Name          | Type / signature           | Description                                                                         |
| ---------- | ------------- | -------------------------- | ----------------------------------------------------------------------------------- |
| Parameters | `value`       | `number`                   | Number to validate                                                                  |
|            | `label`       | `string`                   | Field name used in error messages                                                   |
|            | `ownerError?` | `RetikzError \| undefined` | Domain error thrown unchanged on validation failure; defaults to a Foundation error |
| Returns    | —             | `void`                     | —                                                                                   |
| Throws     | —             | —                          | Throws ownerError or RetikzFoundationError on validation failure                    |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const assertPositiveNumber: (value: number, label: string, ownerError?: RetikzError | undefined) => void;
```

</DocTab>

</DocTabs>

### cloneAndFreezeJson

<p>
  <ApiSourceLink label={'cloneAndFreezeJson'} path={'packages/kernel/foundation/src/json.ts'} startLine={94}>
    Validates JSON-safe plain data and creates a deeply frozen copy detached from the original input
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category        | Name    | Type / signature | Description                                                                           |
| --------------- | ------- | ---------------- | ------------------------------------------------------------------------------------- |
| Type parameters | `T`     | —                | Static type of the input value                                                        |
| Parameters      | `value` | `T`              | JSON-safe data to copy                                                                |
|                 | `path?` | `string`         | Path used to locate the current value in error messages; defaults to 'value'          |
| Returns         | —       | `T`              | Deeply frozen copy with the same structure as the input but no shared mutable objects |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const cloneAndFreezeJson: <T>(value: T, path?: string) => T;
```

</DocTab>

</DocTabs>

### compositeOpaqueColor

<p>
  <ApiSourceLink label={'compositeOpaqueColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={367}>
    Pre-composites a static CSS foreground color onto an opaque static backdrop by weight
  </ApiSourceLink>
</p>

Multiplies the foreground alpha by weight, then uses source-over sRGB compositing to produce a deterministic opaque color.

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category   | Name         | Type / signature | Description                                                                                            |
| ---------- | ------------ | ---------------- | ------------------------------------------------------------------------------------------------------ |
| Parameters | `foreground` | `string`         | Static CSS foreground color, optionally with alpha                                                     |
|            | `backdrop`   | `string`         | Static CSS backdrop that must be fully opaque                                                          |
|            | `weight`     | `number`         | Finite compositing weight in the inclusive range 0..1                                                  |
| Returns    | —            | `#${string}`     | Opaque color in six-digit lowercase hexadecimal notation                                               |
| Throws     | —            | —                | Throws RetikzFoundationError for unsupported colors, a translucent backdrop, or an out-of-range weight |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const compositeOpaqueColor: (foreground: string, backdrop: string, weight: number) => `#${string}`;
```

</DocTab>

</DocTabs>

### createOpenStringSchema

<p>
  <ApiSourceLink label={'createOpenStringSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={30}>
    Creates an open non-blank string schema with suggestions from a known const-object enum
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category        | Name      | Type / signature                                                      | Description                                                    |
| --------------- | --------- | --------------------------------------------------------------------- | -------------------------------------------------------------- |
| Type parameters | `TValues` | `extends Readonly<Record<string, string>>`                            | Const-object enum type of known string values                  |
| Parameters      | `values`  | `TValues`                                                             | Enum object providing known string values                      |
| Returns         | —         | `ZodType<OpenString<ValueOf<TValues>>, OpenString<ValueOf<TValues>>>` | Schema accepting known enum values or custom non-blank strings |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const createOpenStringSchema: <const TValues extends Readonly<Record<string, string>>>(
  values: TValues,
) => ZodType<OpenString<ValueOf<TValues>>, OpenString<ValueOf<TValues>>>;
```

</DocTab>

</DocTabs>

### createReadonlyMap

<p>
  <ApiSourceLink label={'createReadonlyMap'} path={'packages/kernel/foundation/src/collections.ts'} startLine={9}>
    Creates a shallow Map snapshot that exposes no mutating methods
  </ApiSourceLink>
</p>

Copies input entries into independent storage while preserving native Map iteration and lookup semantics; the `forEach` owner argument is the readonly snapshot itself.

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category        | Name      | Type / signature                    | Description                                                                                                  |
| --------------- | --------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| Type parameters | `TKey`    | —                                   | Map key type                                                                                                 |
|                 | `TValue`  | —                                   | Map value type                                                                                               |
| Parameters      | `entries` | `Iterable<readonly [TKey, TValue]>` | Key-value entries used to create the snapshot                                                                |
| Returns         | —         | `ReadonlyMap<TKey, TValue>`         | Independent readonly Map wrapper; keys and values retain their original references and are not deeply frozen |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const createReadonlyMap: <TKey, TValue>(
  entries: Iterable<readonly [TKey, TValue]>,
) => ReadonlyMap<TKey, TValue>;
```

</DocTab>

</DocTabs>

### isRetikzError

<p>
  <ApiSourceLink label={'isRetikzError'} path={'packages/kernel/foundation/src/error.ts'} startLine={105}>
    Determines whether a dynamic value inherits from a Retikz structured domain error
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category   | Name    | Type / signature       | Description                                                             |
| ---------- | ------- | ---------------------- | ----------------------------------------------------------------------- |
| Parameters | `value` | `unknown`              | Dynamic value to inspect                                                |
| Returns    | —       | `value is RetikzError` | Whether the value is a RetikzError instance; narrows the type when true |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const isRetikzError: (value: unknown) => value is RetikzError;
```

</DocTab>

</DocTabs>

### mergeProperties

<p>
  <ApiSourceLink label={'mergeProperties'} path={'packages/kernel/foundation/src/objects.ts'} startLine={24}>
    Shallowly merges own enumerable properties from left to right into a new object
  </ApiSourceLink>
</p>

Includes string and symbol keys and skips undefined sources; preserves all property values by default without recursively merging or mutating inputs

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category        | Name       | Type / signature                          | Description                                                                                                                   |
| --------------- | ---------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Type parameters | `T`        | `extends object`                          | Object type being shallowly merged                                                                                            |
| Parameters      | `sources`  | `ReadonlyArray<Readonly<T> \| undefined>` | Source objects ordered from lowest to highest priority                                                                        |
|                 | `options?` | `MergePropertiesOptions<NoInfer<T>>`      | Property write strategy; defaults to &#123;&#125;, retaining all source properties                                            |
| Returns         | —          | `Partial<T>`                              | A new, potentially filtered object, hence the `Partial<T>` return type                                                        |
| Throws          | —          | —                                         | Throws RetikzFoundationError when reading a property or invoking the policy fails, preserving the original exception as cause |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const mergeProperties: <T extends object>(
  sources: ReadonlyArray<Readonly<T> | undefined>,
  options?: MergePropertiesOptions<NoInfer<T>>,
) => Partial<T>;
```

</DocTab>

</DocTabs>

### parseStaticCssColor

<p>
  <ApiSourceLink label={'parseStaticCssColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={318}>
    Parses the static CSS color subset that can be resolved without a host environment
  </ApiSourceLink>
</p>

Supports named colors, transparent, hexadecimal notation, comma-separated rgb/rgba/hsl/hsla, and space-separated rgb/hsl; does not resolve environment-dependent expressions such as currentColor or var

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

<ApiTable variant="parameters">

| Category   | Name    | Type / signature         | Description                                                                        |
| ---------- | ------- | ------------------------ | ---------------------------------------------------------------------------------- |
| Parameters | `input` | `string`                 | Static CSS color string                                                            |
| Returns    | —       | `ParsedCssColor \| null` | Normalized sRGB channels and alpha; returns null for invalid or unsupported colors |

</ApiTable>

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export declare const parseStaticCssColor: (input: string) => ParsedCssColor | null;
```

</DocTab>

</DocTabs>
