{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## `@retikz/foundation`

### RetikzError

<p>
  <ApiSourceLink label={'RetikzError'} path={'packages/kernel/foundation/src/error.ts'} startLine={16}>
    Base structure for Retikz structured domain errors
  </ApiSourceLink>
</p>

| Member        | Type       | Default | Description |
| ------------- | ---------- | ------- | ----------- |
| `constructor` | `unknown`  | —       | —           |
| `cause?`      | `unknown`  | —       | —           |
| `code`        | `TCode`    | —       | —           |
| `details`     | `TDetails` | —       | —           |

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TCode`        | —           |
| `TDetails`     | —           |

### RetikzFoundationError

<p>
  <ApiSourceLink label={'RetikzFoundationError'} path={'packages/kernel/foundation/src/error.ts'} startLine={51}>
    Unified structured error for Foundation atomic-contract failures
  </ApiSourceLink>
</p>

| Member        | Type      | Default | Description |
| ------------- | --------- | ------- | ----------- |
| `constructor` | `unknown` | —       | —           |

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TCode`        | —           |
| `TDetails`     | —           |

### AssertEqual

<p>
  <ApiSourceLink label={'AssertEqual'} path={'packages/kernel/foundation/src/types.ts'} startLine={15}>
    Bi-directionally checks whether two types are equivalent
  </ApiSourceLink>
</p>

```ts
conditional;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TActual`      | —           |
| `TExpected`    | —           |

### JsonObject

<p>
  <ApiSourceLink label={'JsonObject'} path={'packages/kernel/foundation/src/types.ts'} startLine={12}>
    String-keyed JSON object
  </ApiSourceLink>
</p>

```ts
ZodInfer<typeof JsonObjectSchema>;
```

### JsonValue

<p>
  <ApiSourceLink label={'JsonValue'} path={'packages/kernel/foundation/src/types.ts'} startLine={9}>
    A single recursive JSON value. Readonly arrays allow const sources to narrow directly at the parse boundary.
  </ApiSourceLink>
</p>

```ts
string | number | boolean | null | ReadonlyArray<JsonValue> | {};
```

### NonEmptyReadonlyArray

<p>
  <ApiSourceLink label={'NonEmptyReadonlyArray'} path={'packages/kernel/foundation/src/types.ts'} startLine={28}>
    Readonly array with at least one element
  </ApiSourceLink>
</p>

```ts
readonly [T, ...Array<T>]
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `T`            | —           |

### OpenString

<p>
  <ApiSourceLink label={'OpenString'} path={'packages/kernel/foundation/src/types.ts'} startLine={22}>
    Preserves known-string suggestions while accepting any string
  </ApiSourceLink>
</p>

```ts
T | (string & {});
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `T`            | —           |

### ParsedCssColor

<p>
  <ApiSourceLink label={'ParsedCssColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={4}>
    Normalized sRGB color with alpha
  </ApiSourceLink>
</p>

| Member | Type     | Default | Description                    |
| ------ | -------- | ------- | ------------------------------ |
| `a`    | `number` | —       | Opacity in the range 0–1       |
| `b`    | `number` | —       | Blue channel in the range 0–1  |
| `g`    | `number` | —       | Green channel in the range 0–1 |
| `r`    | `number` | —       | Red channel in the range 0–1   |

### RetikzErrorOptions

<p>
  <ApiSourceLink label={'RetikzErrorOptions'} path={'packages/kernel/foundation/src/error.ts'} startLine={4}>
    Base construction parameters for a Retikz structured domain error
  </ApiSourceLink>
</p>

```ts
Readonly<{ cause?: unknown; code: TCode; details: TDetails; message: string }>;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TCode`        | —           |
| `TDetails`     | —           |

### RetikzFoundationErrorCodeValue

<p>
  <ApiSourceLink
    label={'RetikzFoundationErrorCodeValue'}
    path={'packages/kernel/foundation/src/error.ts'}
    startLine={48}
  >
    Stable Foundation package error-code values
  </ApiSourceLink>
</p>

```ts
ValueOf<typeof RetikzFoundationErrorCode>;
```

### ValueOf

<p>
  <ApiSourceLink label={'ValueOf'} path={'packages/kernel/foundation/src/types.ts'} startLine={6}>
    Obtains the union of all object values
  </ApiSourceLink>
</p>

```ts
indexedAccess;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `T`            | —           |

### WithRequiredProperties

<p>
  <ApiSourceLink label={'WithRequiredProperties'} path={'packages/kernel/foundation/src/types.ts'} startLine={25}>
    Narrows selected properties to required while preserving the remaining properties
  </ApiSourceLink>
</p>

```ts
T & Required<Pick<T, TKey>>;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `T`            | —           |
| `TKey`         | —           |

### JsonObjectSchema

<p>
  <ApiSourceLink label={'JsonObjectSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={13}>
    Schema for a string-keyed JSON object
  </ApiSourceLink>
</p>

```ts
ZodRecord<ZodString, ZodType<JsonValue, unknown, $ZodTypeInternals<JsonValue, unknown>>>;
```

### JsonValueSchema

<p>
  <ApiSourceLink label={'JsonValueSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={8}>
    Schema for recursive JSON values
  </ApiSourceLink>
</p>

```ts
ZodType<JsonValue, unknown>;
```

### NonBlankStringSchema

<p>
  <ApiSourceLink label={'NonBlankStringSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={18}>
    Schema for non-blank strings
  </ApiSourceLink>
</p>

```ts
ZodString;
```

### NonNegativeIntegerSchema

<p>
  <ApiSourceLink label={'NonNegativeIntegerSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={42}>
    Schema for non-negative integers
  </ApiSourceLink>
</p>

```ts
ZodNumber;
```

### NonNegativeNumberSchema

<p>
  <ApiSourceLink label={'NonNegativeNumberSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={36}>
    Schema for non-negative numbers
  </ApiSourceLink>
</p>

```ts
ZodNumber;
```

### NormalizedFractionSchema

<p>
  <ApiSourceLink label={'NormalizedFractionSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={45}>
    Schema for normalized numbers in the closed interval 0..1
  </ApiSourceLink>
</p>

```ts
ZodNumber;
```

### PositiveIntegerSchema

<p>
  <ApiSourceLink label={'PositiveIntegerSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={39}>
    Schema for positive integers
  </ApiSourceLink>
</p>

```ts
ZodNumber;
```

### PositiveNumberSchema

<p>
  <ApiSourceLink label={'PositiveNumberSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={33}>
    Schema for positive numbers
  </ApiSourceLink>
</p>

```ts
ZodNumber;
```

### RetikzFoundationErrorCode

<p>
  <ApiSourceLink label={'RetikzFoundationErrorCode'} path={'packages/kernel/foundation/src/error.ts'} startLine={34}>
    Stable Foundation package error codes
  </ApiSourceLink>
</p>

```ts
{
  Color: 'FOUNDATION_COLOR_ERROR';
  Default: 'FOUNDATION_ERROR';
  Json: 'FOUNDATION_JSON_ERROR';
  NonEmptyStringRequired: 'FOUNDATION_NON_EMPTY_STRING_REQUIRED';
  PositiveNumberRequired: 'FOUNDATION_POSITIVE_NUMBER_REQUIRED';
}
```

### StaticCssNamedColorHexByName

<p>
  <ApiSourceLink
    label={'StaticCssNamedColorHexByName'}
    path={'packages/kernel/foundation/src/color.ts'}
    startLine={171}
  >
    Lowercase hexadecimal values for static named CSS colors
  </ApiSourceLink>
</p>

Contains only named colors supported by `parseStaticCssColor()`; excludes transparent, functions, and hexadecimal notation.

```ts
Readonly<Record<string, templateLiteral>>;
```

### assertNonEmptyString

<p>
  <ApiSourceLink label={'assertNonEmptyString'} path={'packages/kernel/foundation/src/assert.ts'} startLine={7}>
    Rejects empty and all-whitespace strings
  </ApiSourceLink>
</p>

```ts
(value: string, label: string, ownerError: RetikzError<string, Readonly<Record<string, unknown>>> | undefined) => void
```

#### Parameters

| Parameter    | Type                                                                  | Description |
| ------------ | --------------------------------------------------------------------- | ----------- |
| `value`      | `string`                                                              | —           |
| `label`      | `string`                                                              | —           |
| `ownerError` | `RetikzError<string, Readonly<Record<string, unknown>>> \| undefined` | —           |

### assertPlainDataContainers

<p>
  <ApiSourceLink label={'assertPlainDataContainers'} path={'packages/kernel/foundation/src/json.ts'} startLine={82}>
    Asserts that an external data tree uses only plain objects, plain arrays, and safe property descriptors
  </ApiSourceLink>
</p>

```ts
(value: unknown, path: string) => void
```

#### Parameters

| Parameter | Type      | Description                                          |
| --------- | --------- | ---------------------------------------------------- |
| `value`   | `unknown` | External data tree to inspect                        |
| `path`    | `string`  | Path used to locate the root value in error messages |

> **Notes:** The caller schema validates leaf values. This function only excludes prototype objects, accessors, symbols, exotic arrays, and cycles before reading properties.

### assertPositiveNumber

<p>
  <ApiSourceLink label={'assertPositiveNumber'} path={'packages/kernel/foundation/src/assert.ts'} startLine={24}>
    Rejects finite numbers that are not strictly greater than zero
  </ApiSourceLink>
</p>

```ts
(value: number, label: string, ownerError: RetikzError<string, Readonly<Record<string, unknown>>> | undefined) => void
```

#### Parameters

| Parameter    | Type                                                                  | Description |
| ------------ | --------------------------------------------------------------------- | ----------- |
| `value`      | `number`                                                              | —           |
| `label`      | `string`                                                              | —           |
| `ownerError` | `RetikzError<string, Readonly<Record<string, unknown>>> \| undefined` | —           |

### cloneAndFreezeJson

<p>
  <ApiSourceLink label={'cloneAndFreezeJson'} path={'packages/kernel/foundation/src/json.ts'} startLine={93}>
    Validates JSON-safe plain data and creates a deeply frozen copy detached from the original input
  </ApiSourceLink>
</p>

```ts
<T>(value: T, path: string) => T;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `T`            | —           |

#### Parameters

| Parameter | Type     | Description                                             |
| --------- | -------- | ------------------------------------------------------- |
| `value`   | `T`      | JSON-safe data to copy                                  |
| `path`    | `string` | Path used to locate the current value in error messages |

#### Returns

Deeply frozen copy with the same structure as the input but no shared mutable objects

### compositeOpaqueColor

<p>
  <ApiSourceLink label={'compositeOpaqueColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={357}>
    Pre-composites a static CSS foreground color onto an opaque static backdrop by weight
  </ApiSourceLink>
</p>

Multiplies the foreground alpha by weight, then uses source-over sRGB compositing to produce a deterministic opaque color.

```ts
(foreground: string, backdrop: string, weight: number) => templateLiteral;
```

#### Parameters

| Parameter    | Type     | Description |
| ------------ | -------- | ----------- |
| `foreground` | `string` | —           |
| `backdrop`   | `string` | —           |
| `weight`     | `number` | —           |

### createOpenStringSchema

<p>
  <ApiSourceLink label={'createOpenStringSchema'} path={'packages/kernel/foundation/src/schema.ts'} startLine={25}>
    Creates an open non-blank string schema with suggestions from a known const-object enum
  </ApiSourceLink>
</p>

```ts
<TValues>(values: TValues) => ZodType<OpenString<ValueOf<TValues>>>;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TValues`      | —           |

#### Parameters

| Parameter | Type      | Description |
| --------- | --------- | ----------- |
| `values`  | `TValues` | —           |

### createReadonlyMap

<p>
  <ApiSourceLink label={'createReadonlyMap'} path={'packages/kernel/foundation/src/collections.ts'} startLine={5}>
    Creates a shallow Map snapshot that exposes no mutating methods
  </ApiSourceLink>
</p>

Copies input entries into independent storage while preserving native Map iteration and lookup semantics; the `forEach` owner argument is the readonly snapshot itself.

```ts
<TKey, TValue>(entries: Iterable<readonly [TKey, TValue]>) => ReadonlyMap<TKey, TValue>;
```

#### Type parameters

| Type parameter | Description |
| -------------- | ----------- |
| `TKey`         | —           |
| `TValue`       | —           |

#### Parameters

| Parameter | Type                                | Description |
| --------- | ----------------------------------- | ----------- |
| `entries` | `Iterable<readonly [TKey, TValue]>` | —           |

### isRetikzError

<p>
  <ApiSourceLink label={'isRetikzError'} path={'packages/kernel/foundation/src/error.ts'} startLine={73}>
    Determines whether a dynamic value inherits from a Retikz structured domain error
  </ApiSourceLink>
</p>

```ts
(value: unknown) => predicate;
```

#### Parameters

| Parameter | Type      | Description |
| --------- | --------- | ----------- |
| `value`   | `unknown` | —           |

### parseStaticCssColor

<p>
  <ApiSourceLink label={'parseStaticCssColor'} path={'packages/kernel/foundation/src/color.ts'} startLine={313}>
    Parses the static CSS color subset that can be resolved without a host environment
  </ApiSourceLink>
</p>

```ts
(input: string) => ParsedCssColor | null;
```

#### Parameters

| Parameter | Type     | Description |
| --------- | -------- | ----------- |
| `input`   | `string` | —           |
