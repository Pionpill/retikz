---
title: 快速开始
description: 安装 retikz，并通过 4 步搭建一个最小例子，认识顶层容器、Node、Draw、动画与事件回调。
---

## 安装

retikz 的 React 适配器是 `@retikz/react`，它的对等依赖是 `react` / `react-dom`（版本不低于 18）。

如果不使用 React，也可以用 `@retikz/vanilla` 通过纯 JavaScript 组织和渲染图形。文档正文统一使用 React API；点开 demo 的 **View Code** 后，可以切换到 Vanilla API 查看等价写法。

<PackageManagerInstall packages="@retikz/react react react-dom" />

所有 retikz 发布包仅提供 ES modules，要求 Node.js 24 或更高版本。

## 如何学习

先把这页的四步例子跑通，再按当前任务选择下一站：

| 页面                                                  | 什么时候读                                                         |
| ----------------------------------------------------- | ------------------------------------------------------------------ |
| **简介**                                              | 想先理解 retikz 解决什么问题，以及 Node、路径和共享 IR 的心智模型  |
| **概念**                                              | 学定位、锚点、分层和动画等可复用规则                               |
| **组件 / 示例**                                       | 查具体 API，或看完整图形如何从想法拆成图元                         |
| **[架构](/kernel/concepts/design/principles) / 参考** | 可选；扩展 retikz、直接读写 IR、调试编译行为或查询 schema 时再深入 |

如果你想边读边问，可以点顶部的 AI 按钮打开 AI 面板，把当前页、选中的代码或 API 名称作为上下文提问。文档站是纯静态站点，不提供 LLM 服务；你需要在面板里填自己的 API Key，站点只在浏览器里把请求转发给你选择的模型供应商。

## 一个最小例子

<ComponentPreview files="get-start-final" size="xs" />

下面先用 3 步搭出这张静态图，再在第 4 步加入动画与点击回调。每一步都可以点开 **View Code** 查看源码与对应的 IR JSON。

## 一步步搭建

### 1. 容器 `<Layout />` + 一个 `<Node />`

最小图形从容器和基础图元开始。`<Layout>` 负责承载整张图，决定 SVG 的显示尺寸，并把内部图元渲染出来；`<Node>` 负责画一个有位置、有名字、可显示文字的节点。这里先放一个节点，确认画布和基础图元都能工作。

<ComponentPreview files="get-start-step-1" size="xs" />

### 2. 多个 `<Node />`

继续添加多个 `<Node>`，相当于先把图里的实体摆好。节点数量随便加，但 `id` 必须全局唯一——后面 `<Draw>` 会按 id 找到它们；`position` 是节点中心在画布里的坐标。

| Prop       | 含义                                      |
| ---------- | ----------------------------------------- |
| `id`       | 唯一标识，被 `<Path>` / `<Draw>` 用来引用 |
| `position` | `[x, y]` 用户单位坐标，表示节点中心位置   |
| children   | 节点的可见内容（label）                   |

<ComponentPreview files="get-start-step-2" size="xs" />

### 3. 用 `<Draw />` 连起来

`<Draw>` 负责画连接线。`way` 数组按顺序写要经过的节点 id，retikz 会自动把线贴到节点边界，而不是连到文字中心；`arrow="->"` 给末端加箭头。

<ComponentPreview files="get-start-final" size="xs" />

3 个节点用 2 条带箭头的线串起，静态结构已经与最上方的例子一致。

### 4. 添加动画与回调函数

图形也可以响应用户操作。给节点加 `animations` 描述动画轨道，再加 `onClick` 回调；回调第二个参数 `context` 会带上当前图元的信息和动画控制器，调用 `context.animation.restart()` 就能从事件里重播动画。

<ComponentPreview files="get-start-step-4" size="xs" controls={{ animation: true }} />

这里的动画设置为 `trigger: 'manual'`，所以它不会自动播放，而是等点击节点时由回调触发；回调里也可以同步更新你的 React 状态。预览默认跟随系统的「减少动态效果」偏好；如果动画没有播放，可在页面右上角 **… → 组件演示 → 动画播放** 中选择「开启」。

到这里先记住 4 个规则：

- 画布坐标 x 向右、y 向下；`[0, 0]` 是 retikz 内部 user units 的原点
- `<Layout width height>` 控制最终 SVG 显示尺寸，不等于强行把内部坐标改成 CSS 像素
- 节点 `id` 是连线的名字，`<Draw way={['a', 'b']}>` 引用的是节点边界，不是文本中心
- 点开 **View Code → IR** 可以看到编译后的图形数据，调试连线时优先看这里

## 接下来

<div className="my-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
  <LinkedCard href="/kernel/components/layout">
    <span className="font-semibold">Layout</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">了解顶层容器、画布尺寸与直接渲染 IR</span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/node/overview">
    <span className="font-semibold">Node</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">学习节点形状、文字、样式、定位和标签</span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/draw/overview">
    <span className="font-semibold">Draw</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">继续学习折线、曲线、箭头、填充和边标注</span>
  </LinkedCard>
  <LinkedCard href="/kernel/components/effects/animation">
    <span className="font-semibold">动画</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">理解动画轨道、preset、播放控制和静态降级</span>
  </LinkedCard>
  <LinkedCard href="/kernel/concepts/design/principles">
    <span className="font-semibold">技术原理</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">
      扩展 retikz、读写 IR 或调试编译行为时了解内部流程
    </span>
  </LinkedCard>
  <LinkedCard href="/kernel/examples/karl-circle">
    <span className="font-semibold">Karl 单位圆</span>
    <span className="mt-1 text-center text-sm text-muted-foreground">看一个更完整的几何示意图如何逐步搭出来</span>
  </LinkedCard>
</div>
