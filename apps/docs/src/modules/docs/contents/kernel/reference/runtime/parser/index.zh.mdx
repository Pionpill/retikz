---
title: 解析工具
description: 查询 adapter 可复用的路径、目标点、锚点、线宽与动画 Sugar 转换工具
---

`@retikz/core` 的解析工具把面向作者的 Sugar 收窄为可序列化的 IR 片段。它们不依赖 React、DOM 或 node index，适合 adapter、测试和自定义 DSL 复用；已经是合法 IR 的输入不需要先经过这一层。

## 公开导出概览

| 能力             | 公开导出                                                            | 用途                                                             |
| ---------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 路径与目标点     | `parseWay` / `parseTargetSugar` / `parseNodeTarget`                 | 把 Draw way 和字符串 shorthand 转成 IR                           |
| 锚点与方向别名   | `parseAnchorAlias` / `parseSideAlias` / `SupportedAnchorSugarNames` | 归一化 compass / TikZ 风格命名                                   |
| 语义线宽词汇     | `PathThickness` / `THICKNESS_TO_WIDTH`                              | 提供稳定的线宽 preset 与数值映射                                 |
| 动画 preset 工具 | `fadeIn` / `drawOn` / `scaleIn` 等                                  | 生成动画 track；详见[动画](/kernel/components/effects/animation) |

## 路径解析核心函数

| API                | Signature                        | 用途                                                                                                                                                                                                                                                             |
| ------------------ | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `parseWay`         | `(way: WayDSL) => Array<IRStep>` | 把 `<Draw way={...}>` 的 way 数组转成 path step 序列                                                                                                                                                                                                             |
| `parseTargetSugar` | `(input: unknown) => IRTarget`   | 把 React DSL 字符串 shorthand 转成 IR target：`'+dx,dy'` / `'++dx,dy'` 变相对偏移，节点 ref 字符串委托 `parseNodeTarget`；**非字符串**输入（笛卡尔 / 极坐标 / 已是对象）原样返回。注意：非法字符串会抛错而非原样返回（如 `'+x,'`、未知 anchor、空 id、纯小数串） |
| `parseNodeTarget`  | `(s: string) => IRNodeTarget`    | 节点 ref 字符串 → NodeTarget 对象：`'A'`→`{id:'A'}`、`'A.top'`→命名 anchor、`'A.30'`→角度 anchor；`north` 等 compass 写法作为别名归一到 Web 风格；按第一个 `.` 切分，含 `.` 的 id / shape 自定义 anchor 须写对象形态；空 id / 未知 anchor / 纯小数串抛错         |

## 锚点与方向别名

`parseAnchorAlias` 接受 canonical anchor，也会把 `north` / `south` / `east` / `west` 和 `above` / `below` 归一为 Web 风格方向；角点还支持 `north-east`、`above-left` 等组合。`parseSideAlias` 只处理四个边方向，无法识别时两者都返回 `undefined`。

`SupportedAnchorSugarNames` 是错误消息可展示的完整候选集合。Shape 自定义 anchor 不在这份通用集合中，应直接使用对象目标 `{ id, anchor }`。

## 语义线宽

`PathThickness` 与 `THICKNESS_TO_WIDTH` 是 Core 的稳定值词汇。作者侧 `thickness` 简写由 `@retikz/vanilla` 的路径归一化处理：显式 `strokeWidth` 优先，只有数值线宽缺省时才映射为 IR 的 `strokeWidth`。

| `PathThickness` | `strokeWidth` |
| --------------- | ------------- |
| `ultraThin`     | `0.25`        |
| `veryThin`      | `0.5`         |
| `thin`          | `1`           |
| `semithick`     | `1.5`         |
| `thick`         | `2`           |
| `veryThick`     | `3`           |
| `ultraThick`    | `4`           |

## 画线方式常量

`DrawWay` 是 way DSL 的关键字集合。不要硬编码内部字符串，使用常量引用。

| Constant             | 值 / 语义           | 说明                             |
| -------------------- | ------------------- | -------------------------------- |
| `DrawWay.Cycle`      | cycle               | 闭合回 path 起点                 |
| `DrawWay.Hv`         | `'-\|'`             | 折角：先水平后垂直               |
| `DrawWay.Vh`         | `'\|-'`             | 折角：先垂直后水平               |
| `DrawWay.Hvh`        | `'-\|-'`            | 三段折角：水平、垂直、水平       |
| `DrawWay.Vhv`        | `'\|-\|'`           | 三段折角：垂直、水平、垂直       |
| `DrawWay.Relative`   | relative 类型标记   | 对象式相对偏移，不更新 prevEnd   |
| `DrawWay.Accumulate` | accumulate 类型标记 | 对象式累积相对偏移，更新 prevEnd |

## 路径项

`WayDSL` 是 `Array<WayItem>`。常见 item 形态：

| Shape                         | 产物                                     | 说明                                                                               |
| ----------------------------- | ---------------------------------------- | ---------------------------------------------------------------------------------- |
| `IRTarget` / 字符串 shorthand | `move` / `line`                          | 第一项成为 move，后续 target 成为 line；字符串会经 `parseTargetSugar` 转成 IR 对象 |
| `{ position, type }`          | `relative` / `relativeAccumulate` target | TS-friendly 的相对偏移对象                                                         |
| 四种 `DrawWay.*v*` fold 常量  | `kind: 'fold'`                           | 与下一项 target 合并成两段或三段折角 step                                          |
| `{ via, fraction? }`          | `kind: 'fold'`                           | strict 三段折角对象；via 只接受 `-\|-` / `\|-\|`，fraction 为 `0..1`               |
| `DrawWay.Cycle`               | `kind: 'cycle'`                          | 闭合当前子路径                                                                     |
| `{ curve }`                   | `kind: 'curve'`                          | 与下一项 target 合并成二次贝塞尔                                                   |
| `{ cubic }`                   | `kind: 'cubic'`                          | 与下一项 target 合并成三次贝塞尔                                                   |
| `{ bend }`                    | `kind: 'bend'`                           | 与下一项 target 合并成弯曲段；`angle` 可选                                         |
| `{ arc }`                     | `kind: 'arc'`                            | 以上一项为圆心，不消耗下一项                                                       |
| `{ circle }`                  | `kind: 'circlePath'`                     | 以上一项为圆心画整圆                                                               |
| `{ ellipse }`                 | `kind: 'ellipsePath'`                    | 以上一项为中心画整椭圆                                                             |
| `{ label }`                   | step label                               | 修饰下一个产生绘制段的 item                                                        |

## 路径解析规则

| Rule                        | 行为                                                                                                                                                   |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 最少长度                    | `way.length < 2` 抛错                                                                                                                                  |
| 第一项                      | 必须是 target（move 起点）；`way[0]` 是 label 算子或任何其他算子都直接抛错，不降级（`[0,0]` 回退是相对 target 作首项时的基准语义，不是首项算子的降级） |
| target                      | 首项转 `move`，后续 target 转 `line`                                                                                                                   |
| horizontalTo / verticalTo   | 当前项转 `axis-line`；target 只接受笛卡尔坐标或 NodeTarget，不消费下一项                                                                               |
| fold / curve / cubic / bend | 必须跟随 target；解析后会消费下一项                                                                                                                    |
| 三段 fold 配置对象          | 只允许 `via` / `fraction`；旧 via、未知字段、非法 fraction 以 `parseWay:` 抛错                                                                         |
| arc / circle / ellipse      | 以上一项为中心，不消费下一项                                                                                                                           |
| label                       | 修饰下一个产生绘制段的 item                                                                                                                            |
| label + cycle               | 抛错；cycle 不承载 label                                                                                                                               |
| 连续 label / 末尾 label     | 抛错                                                                                                                                                   |
| 多 operator 对象            | 同一对象含 horizontalTo / verticalTo 或其它多个 Way operator 时抛错                                                                                    |

`parseWay` 的输出是 IR step 数组，可以直接放入 `PathSchema.children`。

## 相对目标点语法糖

`parseTargetSugar` 只处理 React DSL 的字符串 shorthand；非字符串输入原样返回，已经是对象的 target（含 `{ id, anchor? }`、`{ of, offset }`、`{ between, fraction }`）不会被重写。合法的节点 ref / 偏移字符串按下表转换，非法字符串（如 `'+x,'`、未知 anchor、空 id、纯小数串）抛错：

| Input                                                    | Output                                                   | 含义                                          |
| -------------------------------------------------------- | -------------------------------------------------------- | --------------------------------------------- |
| `'+10,0'`                                                | `{ relative: [10, 0] }`                                  | 相对前一终点，不更新 prevEnd                  |
| `'++10,0'`                                               | `{ relativeAccumulate: [10, 0] }`                        | 相对前一终点，并更新 prevEnd                  |
| `'node-id'`                                              | `{ id: 'node-id' }`                                      | 普通节点引用                                  |
| `'node-id.top'`                                          | `{ id: 'node-id', anchor: 'top' }`                       | 命名 anchor                                   |
| `'node-id.30'`                                           | `{ id: 'node-id', anchor: 30 }`                          | 数字角度 anchor                               |
| `[10, 20]`                                               | `[10, 20]`                                               | 非字符串输入原样返回                          |
| `{ between: [{ id: 'a' }, { id: 'b' }], fraction: 0.5 }` | `{ between: [{ id: 'a' }, { id: 'b' }], fraction: 0.5 }` | 对象 target 原样返回；后续 compile 再解析端点 |

正则允许空格和负数，例如 `'+ -4, 8'` / `'++-4,8'`。

## 错误边界

| Case                     | Message 前缀                                                                 |
| ------------------------ | ---------------------------------------------------------------------------- |
| way 太短                 | `parseWay: way must contain at least 2 items`                                |
| 第一项是 label           | `parseWay: way[0] must be a target`                                          |
| via 在末尾               | `parseWay: via operator ... at end of way`                                   |
| via 后不是 target        | `parseWay: via operator ... must be followed by a target`                    |
| curve-like 在末尾        | `parseWay: curve operator at end of way`                                     |
| curve-like 后不是 target | `parseWay: curve operator must be followed by a target`                      |
| cycle 承载 label         | `parseWay: cycle step cannot carry a label`                                  |
| 连续 label               | `parseWay: label operator ... cannot directly follow another label operator` |
| 末尾 label               | `parseWay: label operator at end of way`                                     |

这些错误属于 sugar 输入错误，应该在 adapter / 开发期尽早暴露，而不是降级成 warning。

<SourceLinks
  sources={[
    {
      label: 'Parser 公共导出面',
      path: 'packages/kernel/core/src/parse/index.ts',
      startLine: 1,
      endLine: 6,
    },
    {
      label: '锚点与方向别名归一化',
      path: 'packages/kernel/core/src/parse/anchor-alias.ts',
      startLine: 27,
      endLine: 38,
    },
    {
      label: '语义线宽映射与优先级',
      path: 'packages/kernel/core/src/parse/path-thickness.ts',
      startLine: 29,
      endLine: 61,
    },
  ]}
/>
