---
title: Entity
description: Named members placed in a scene — Node and Coordinate
---

`entity` covers IR primitives that **have an id**, are **placed at a position**, and **don't draw paths themselves**. Both register into the same node index, so a Node id and a Coordinate id share the same namespace.

## Node

`NodeSchema` describes a positioned visual entity. `style` groups visual overrides and `layout` groups size, spacing and text layout. Position, shape, transforms, text and labels remain at the root.

See the complete fields in [NodeSchema](/kernel/components/node/schema-reference#nodeschema).

<Comparison target="tikz" title="Node field mapping">

`strokeOpacity` is stroke-only opacity; `dashed` / `dotted` map to TikZ's dashed / dotted presets; the `{ text, position }` form of `label` maps to `[label=top:foo]` / `[label=30:foo]`. `placement: 'inside'` is retikz's structured extension for labels placed inside a node.

</Comparison>

<span id="node-text-color"></span>

## Node text color keyword

`NodeSchema.style.textColor` remains a regular string contract. `NodeTextColor.Contrast` is a reserved Node-host keyword that also works in the schema-derived `Scope.defaults.node.style.textColor`; it selects pure black or white from the final static opaque fill.

Core parses only deterministic named, hex, `rgb()`, and `hsl()` colors; it never reads DOM or renderer state. Translucent / missing fills, gradients, patterns, images, and dynamic CSS colors use the fixed `currentColor` fallback and emit `TEXT_AUTO_CONTRAST_UNRESOLVED`.

<span id="shaperef"></span>

## ShapeRef

`ShapeRefSchema` is the object form for parameterized shapes. Parameterless shapes can stay as strings; the object form sends a provider name and JSON parameters to the selected shape Definition.

<ZodSchema name="ShapeRefSchema" />

<span id="boundary"></span>

## Boundary

`BoundarySchema` selects the connection surface used by path endpoints and directional anchors. The string `shape` uses the visual shape itself; another string or `ShapeRef` selects a built-in / registered boundary provider.

See the complete fields in [BoundarySchema](/kernel/components/node/schema-reference#boundaryschema).

## Coordinate

`CoordinateSchema` requires an `id` (its whole purpose), never contributes to layout, and shares the node index with `Node`.

<Comparison target="tikz" title="Coordinate Mapping">

`{ type: 'coordinate', id: 'm', position: [3, 2] }` maps to TikZ `\coordinate (m) at (3,2);`.

</Comparison>

See the complete fields in [CoordinateSchema](/kernel/components/node/schema-reference#coordinateschema).

## NodeLabel

`NodeLabelSchema` describes extra text attached to a node. `Node.label` accepts either one object or an array.

See the complete fields in [NodeLabelSchema](/kernel/components/node/schema-reference#nodelabelschema).

`position: { boundary, fraction }` currently targets box-like node boundaries: `boundary` is `'top'` / `'right'` / `'bottom'` / `'left'`, and `fraction` is the 0..1 proportional position on that side, defaulting to `0.5`. For non-`center` labels, the center offset is `distance + rotated visual-box projection radius` along the placement direction. A pin ends at the exact intersection of the same ray and rotated visual box.

## TextBlock

`TextBlockSchema` is the node text block shape: a single string for one line, or a non-empty array for multiple lines. Each `IRLine` may be a plain string, a per-line style object, or a `{ runs }` mixed text / math line.

<ZodSchema name="TextBlockSchema" />

### IRLine

<ZodSchema name="LineSchema" />

`$...$` / `$$...$$` inside strings is formula sugar normalized to `MathRunSchema`; an explicit `{ tex, displayMode?, fill?, opacity? }` object may also be used as a run. Formula rendering requires the host to inject `CompileOptions.lowerTex`; see the [TeX package](/kernel/packages/tex) for integration details.

<span id="styledline"></span>

### StyledLine

<ZodSchema name="StyledLineSchema" />

<span id="mixedline"></span>

### MixedLine

<ZodSchema name="MixedLineSchema" />

<span id="textrun"></span>

#### TextRun

<ZodSchema name="TextRunSchema" />

<span id="mathrun"></span>

#### MathRun

<ZodSchema name="MathRunSchema" />

## Font

`FontSchema` is shared by node text, node labels, and per-line text overrides. All fields are optional, while family, size, weight, and style reuse Core's named value schemas. `size` accepts a positive number, a Web preset such as `sm` / `lg`, or an `em` / `rem` string; omitted sizes use `CompileOptions.fontSize`. TikZ-style sizes are handled by higher-level sugar, not core IR.

<ZodSchema name="FontSchema" />

<span id="fontfamily"></span>

### FontFamily value

<ZodSchema name="FontFamilySchema" />

<span id="fontweight"></span>

### FontWeight value

<ZodSchema name="FontWeightSchema" />

<span id="fontstyle"></span>

### FontStyle value

<ZodSchema name="FontStyleSchema" />

<span id="textalign"></span>

### TextAlign value

<ZodSchema name="TextAlignSchema" />

<span id="lineheight"></span>

### LineHeight value

<ZodSchema name="LineHeightSchema" />
