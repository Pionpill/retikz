{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## 公开契约

### IRDropShadow

<p>
  <ApiSourceLink label={'IRDropShadow'} path={'packages/kernel/core/src/schemas/effects/types.ts'} startLine={8}>
    IR shadow 对象分支（可带 preset 与显式字段覆盖）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型                                              | 默认值 | 说明 |
| ---------- | ------------------------------------------------- | ------ | ---- |
| `blur?`    | `number`                                          | —      | —    |
| `color?`   | `string`                                          | —      | —    |
| `offsetX?` | `number`                                          | —      | —    |
| `offsetY?` | `number`                                          | —      | —    |
| `opacity?` | `number`                                          | —      | —    |
| `preset?`  | `"none" \| "sm" \| "lg" \| "xl" \| "2xl" \| "md"` | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRDropShadow = ZodInfer<typeof DropShadowSchema>;
```

</DocTab>

</DocTabs>

### ShadowPresetValue

<p>
  <ApiSourceLink label={'ShadowPresetValue'} path={'packages/kernel/core/src/schemas/effects/types.ts'} startLine={15}>
    阴影预设档位值联合
  </ApiSourceLink>
</p>

```ts
export type ShadowPresetValue = ValueOf<typeof ShadowPreset>;
```

#### 展开类型

```ts
export type ShadowPresetValue = 'none' | 'sm' | 'lg' | 'xl' | '2xl' | 'md';
```

### ShadowPreset

<p>
  <ApiSourceLink label={'ShadowPreset'} path={'packages/kernel/core/src/schemas/effects/constants.ts'} startLine={8}>
    阴影预设档位关键字（Tailwind 风格刻度）
  </ApiSourceLink>
</p>

预设字符串 `shadow="md"` 等价于对象 `{ preset:'md' }`；`SHADOW_PRESETS` 表为档位展开单一真源。
`none` = 显式无阴影（当前等价省略，为将来 scope 级联预留）

```ts
export const ShadowPreset = {
  None: 'none',
  Sm: 'sm',
  Md: 'md',
  Lg: 'lg',
  Xl: 'xl',
  Xxl: '2xl',
} as const;
```
