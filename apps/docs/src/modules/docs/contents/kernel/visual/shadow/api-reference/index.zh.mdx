---
title: API 参考
description: 公开类型、函数与扩展契约
---

本页从公开入口与 JSDoc 生成当前能力的 API。组件接入方式见[组件对接](/kernel/visual/shadow/integration)。

组件通过 `style.shadow` 使用预设；`none` 可显式覆盖 Scope 的阴影默认值。下面预设声明中的简写说明不表示存在顶层 shadow prop。

{/* @include kernel/visual/shadow/api-reference/generated */}
