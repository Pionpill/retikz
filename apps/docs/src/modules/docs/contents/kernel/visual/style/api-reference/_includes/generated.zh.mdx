{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## pattern · 图案

### IRPatternLineStyle

<p>
  <ApiSourceLink label={'IRPatternLineStyle'} path={'packages/kernel/core/src/schemas/paint/types.ts'} startLine={23}>
    Pattern 单条线 motif 的可序列化样式覆盖
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员           | 类型                            | 默认值 | 说明 |
| -------------- | ------------------------------- | ------ | ---- |
| `color?`       | `string`                        | —      | —    |
| `dashed?`      | `false \| true`                 | —      | —    |
| `dashOffset?`  | `number`                        | —      | —    |
| `dashPattern?` | `Array<number>`                 | —      | —    |
| `dotted?`      | `false \| true`                 | —      | —    |
| `lineCap?`     | `"butt" \| "round" \| "square"` | —      | —    |
| `lineJoin?`    | `"round" \| "miter" \| "bevel"` | —      | —    |
| `lineWidth?`   | `number`                        | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRPatternLineStyle = ZodInfer<typeof PatternLineStyleSchema>;
```

</DocTab>

</DocTabs>

### IRPatternLineStyleCycle

<p>
  <ApiSourceLink
    label={'IRPatternLineStyleCycle'}
    path={'packages/kernel/core/src/schemas/paint/types.ts'}
    startLine={26}
  >
    Pattern 相邻线条的稀疏周期样式
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型     | 默认值 | 说明 |
| ----------- | -------- | ------ | ---- |
| `overrides` | `…`      | —      | —    |
| `period`    | `number` | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRPatternLineStyleCycle = ZodInfer<typeof PatternLineStyleCycleSchema>;
```

</DocTab>

</DocTabs>

### IRPatternPaint

<p>
  <ApiSourceLink label={'IRPatternPaint'} path={'packages/kernel/core/src/schemas/paint/types.ts'} startLine={20}>
    Pattern paint 的可序列化实例参数
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员               | 类型                                                                                                                                                                                                                                                                                                                       | 默认值 | 说明 |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| `background?`      | `string`                                                                                                                                                                                                                                                                                                                   | —      | —    |
| `color?`           | `string`                                                                                                                                                                                                                                                                                                                   | —      | —    |
| `dashed?`          | `false \| true`                                                                                                                                                                                                                                                                                                            | —      | —    |
| `dashOffset?`      | `number`                                                                                                                                                                                                                                                                                                                   | —      | —    |
| `dashPattern?`     | `Array<number>`                                                                                                                                                                                                                                                                                                            | —      | —    |
| `dotted?`          | `false \| true`                                                                                                                                                                                                                                                                                                            | —      | —    |
| `horizontalStyle?` | `{ color?: string \| undefined; dashed?: boolean \| undefined; dashOffset?: number \| undefined; dashPattern?: Array<number> \| undefined; dotted?: boolean \| undefined; lineCap?: "butt" \| "round" \| "square" \| undefined; lineJoin?: "round" \| "miter" \| "bevel" \| undefined; lineWidth?: number \| undefined; }` | —      | —    |
| `kind`             | `"pattern"`                                                                                                                                                                                                                                                                                                                | —      | —    |
| `lineCap?`         | `"butt" \| "round" \| "square"`                                                                                                                                                                                                                                                                                            | —      | —    |
| `lineJoin?`        | `"round" \| "miter" \| "bevel"`                                                                                                                                                                                                                                                                                            | —      | —    |
| `lineStyleCycle?`  | `…`                                                                                                                                                                                                                                                                                                                        | —      | —    |
| `lineWidth?`       | `number`                                                                                                                                                                                                                                                                                                                   | —      | —    |
| `rotation?`        | `number`                                                                                                                                                                                                                                                                                                                   | —      | —    |
| `shape`            | `OpenString<ValueOf<{ readonly Lines: "lines"; readonly Dots: "dots"; readonly Grid: "grid"; }>>`                                                                                                                                                                                                                          | —      | —    |
| `size?`            | `number`                                                                                                                                                                                                                                                                                                                   | —      | —    |
| `verticalStyle?`   | `{ color?: string \| undefined; dashed?: boolean \| undefined; dashOffset?: number \| undefined; dashPattern?: Array<number> \| undefined; dotted?: boolean \| undefined; lineCap?: "butt" \| "round" \| "square" \| undefined; lineJoin?: "round" \| "miter" \| "bevel" \| undefined; lineWidth?: number \| undefined; }` | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRPatternPaint = Extract<
  IRPaint,
  {
    kind: 'pattern';
  }
>;
```

</DocTab>

</DocTabs>

### PatternDefinition

<p>
  <ApiSourceLink label={'PatternDefinition'} path={'packages/kernel/core/src/contract/pattern/types.ts'} startLine={84}>
    可注册的 pattern 定义
  </ApiSourceLink>
</p>

描述默认基础尺寸和 motif 几何生成能力；定义本身不进入 IR

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员           | 类型                                             | 默认值 | 说明                                                                            |
| -------------- | ------------------------------------------------ | ------ | ------------------------------------------------------------------------------- |
| `defaultSize?` | `number`                                         | `8`    | `pattern.size` 省略时的基础尺寸（user units）；最终 tile 周期可由 emit 结果改写 |
| `emit`         | `(ctx: PatternEmitContext) => PatternEmitOutput` | —      | 局部 tile 坐标中的 motif 几何                                                   |
| `name`         | `string`                                         | —      | pattern 名称，由 IR pattern paint 的 `shape` 引用                               |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PatternDefinition = {
  name: string;
  defaultSize?: number;
  emit: (ctx: PatternEmitContext) => PatternEmitOutput;
};
```

</DocTab>

</DocTabs>

### PatternEmitContext

<p>
  <ApiSourceLink
    label={'PatternEmitContext'}
    path={'packages/kernel/core/src/contract/pattern/types.ts'}
    startLine={32}
  >
    pattern emit 的运行时上下文
  </ApiSourceLink>
</p>

提供基础尺寸、基础样式、可选方向/周期样式和取整函数，供定义生成 motif 几何

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员               | 类型                            | 默认值               | 说明                                                                                                                   |
| ------------------ | ------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `background?`      | `string`                        | `透明背景`           | tile 背景填充（CSS 串）；缺省透明（字段缺省）                                                                          |
| `color`            | `string`                        | —                    | motif 主色（CSS 串，缺省 `currentColor`）                                                                              |
| `dashOffset?`      | `IRPatternPaint['dashOffset']`  | —                    | 描边 dash offset                                                                                                       |
| `dashPattern?`     | `IRPatternPaint['dashPattern']` | —                    | 解析后的描边 dash pattern；已应用显式值与 dashed / dotted 预设优先级                                                   |
| `horizontalStyle?` | `ResolvedPatternLineStyle`      | —                    | 已继承基础字段并解析 preset 的横向线条样式                                                                             |
| `lineCap?`         | `IRPatternPaint['lineCap']`     | —                    | 描边端点线帽                                                                                                           |
| `lineJoin?`        | `IRPatternPaint['lineJoin']`    | —                    | 描边拐角连接                                                                                                           |
| `lineStyleCycle?`  | `ResolvedPatternLineStyleCycle` | —                    | 已按 index 展开并解析继承关系的线条样式周期                                                                            |
| `lineWidth?`       | `IRPatternPaint['lineWidth']`   | `motif 自定义默认值` | 线 / 网格描边宽；dots motif 用作半径<br />仅当用户在 `pattern.lineWidth` 显式给值时存在；缺省时由 motif 自行决定默认值 |
| `round`            | `(n: number) => number`         | —                    | 精度取整函数（与 compile/render 同一 round，保几何一致）                                                               |
| `size`             | `number`                        | —                    | 解析后的基础尺寸或间距（user units）；最终 tile 周期可由 PatternEmitResult.tileSize 覆盖                               |
| `verticalStyle?`   | `ResolvedPatternLineStyle`      | —                    | 已继承基础字段并解析 preset 的纵向线条样式                                                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PatternEmitContext = {
  size: number;
  color: string;
  background?: string;
  lineWidth?: IRPatternPaint['lineWidth'];
  dashPattern?: IRPatternPaint['dashPattern'];
  dashOffset?: IRPatternPaint['dashOffset'];
  lineCap?: IRPatternPaint['lineCap'];
  lineJoin?: IRPatternPaint['lineJoin'];
  horizontalStyle?: ResolvedPatternLineStyle;
  verticalStyle?: ResolvedPatternLineStyle;
  lineStyleCycle?: ResolvedPatternLineStyleCycle;
  round: (n: number) => number;
};
```

</DocTab>

</DocTabs>

### PatternEmitOutput

<p>
  <ApiSourceLink label={'PatternEmitOutput'} path={'packages/kernel/core/src/contract/pattern/types.ts'} startLine={78}>
    Pattern definition 可返回默认周期的 motif iterable，或显式 tile 周期结果
  </ApiSourceLink>
</p>

```ts
export type PatternEmitOutput = Iterable<MarkerPrimitive> | PatternEmitResult;
```

### PatternEmitResult

<p>
  <ApiSourceLink label={'PatternEmitResult'} path={'packages/kernel/core/src/contract/pattern/types.ts'} startLine={70}>
    Pattern definition 的扩展 tile 输出
  </ApiSourceLink>
</p>

用于 motif 周期不同于 `context.size` 的图案；编译器校验并写入最终 Scene tile

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型                        | 默认值 | 说明                                             |
| ---------- | --------------------------- | ------ | ------------------------------------------------ |
| `motif`    | `Iterable<MarkerPrimitive>` | —      | 局部 tile 坐标中的 motif 几何                    |
| `tileSize` | `number`                    | —      | 最终 tile 周期（user units），必须为 finite 正数 |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type PatternEmitResult = {
  tileSize: number;
  motif: Iterable<MarkerPrimitive>;
};
```

</DocTab>

</DocTabs>

### PatternShape

<p>
  <ApiSourceLink label={'PatternShape'} path={'packages/kernel/core/src/schemas/paint/constants.ts'} startLine={6}>
    内置 pattern motif 名常量（用 const + ValueOf 派生，不用 TS enum）
  </ApiSourceLink>
</p>

内置 3 motif：`lines`（横向阴影线）/ `dots`（波点）/ `grid`（横竖网格）。
各 motif 的 tile 几何由 `BUILTIN_PATTERNS` 的 `PatternDefinition.emit` 在 compile 期产出

```ts
export const PatternShape = {
  Lines: 'lines',
  Dots: 'dots',
  Grid: 'grid',
} as const;
```

### definePattern

<p>
  <ApiSourceLink label={'definePattern'} path={'packages/kernel/core/src/contract/pattern/define.ts'} startLine={7}>
    定义 pattern motif 注册项
  </ApiSourceLink>
</p>

```ts
export declare const definePattern: (def: PatternDefinition) => PatternDefinition;
```

#### 参数

| 参数  | 类型                | 说明 |
| ----- | ------------------- | ---- |
| `def` | `PatternDefinition` | —    |

> **备注：** 当前是 typed identity；保留入口用于对齐 registry API，并为未来校验或归一化预留空间

## shadow · 阴影

### IRDropShadow

<p>
  <ApiSourceLink label={'IRDropShadow'} path={'packages/kernel/core/src/schemas/effects/types.ts'} startLine={8}>
    IR shadow 对象分支（可带 preset 与显式字段覆盖）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员       | 类型                                              | 默认值 | 说明 |
| ---------- | ------------------------------------------------- | ------ | ---- |
| `blur?`    | `number`                                          | —      | —    |
| `color?`   | `string`                                          | —      | —    |
| `offsetX?` | `number`                                          | —      | —    |
| `offsetY?` | `number`                                          | —      | —    |
| `opacity?` | `number`                                          | —      | —    |
| `preset?`  | `"none" \| "sm" \| "lg" \| "xl" \| "2xl" \| "md"` | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRDropShadow = ZodInfer<typeof DropShadowSchema>;
```

</DocTab>

</DocTabs>

### ShadowPresetValue

<p>
  <ApiSourceLink label={'ShadowPresetValue'} path={'packages/kernel/core/src/schemas/effects/types.ts'} startLine={15}>
    阴影预设档位值联合
  </ApiSourceLink>
</p>

```ts
export type ShadowPresetValue = ValueOf<typeof ShadowPreset>;
```

#### 展开类型

```ts
export type ShadowPresetValue = 'none' | 'sm' | 'lg' | 'xl' | '2xl' | 'md';
```

### ShadowPreset

<p>
  <ApiSourceLink label={'ShadowPreset'} path={'packages/kernel/core/src/schemas/effects/constants.ts'} startLine={8}>
    阴影预设档位关键字（Tailwind 风格刻度）
  </ApiSourceLink>
</p>

预设字符串 `shadow="md"` 等价于对象 `{ preset:'md' }`；`SHADOW_PRESETS` 表为档位展开单一真源。
`none` = 显式无阴影（当前等价省略，为将来 scope 级联预留）

```ts
export const ShadowPreset = {
  None: 'none',
  Sm: 'sm',
  Md: 'md',
  Lg: 'lg',
  Xl: 'xl',
  Xxl: '2xl',
} as const;
```

## blend · 混合

### BlendModeValue

<p>
  <ApiSourceLink label={'BlendModeValue'} path={'packages/kernel/core/src/schemas/effects/types.ts'} startLine={18}>
    混合模式值联合
  </ApiSourceLink>
</p>

```ts
export type BlendModeValue = ValueOf<typeof BlendMode>;
```

### BlendMode

<p>
  <ApiSourceLink label={'BlendMode'} path={'packages/kernel/core/src/schemas/effects/constants.ts'} startLine={36}>
    W3C 分离式混合模式集（16 个）
  </ApiSourceLink>
</p>

三端共有交集（SVG `mix-blend-mode` / Canvas `globalCompositeOperation`）；混合数学按 W3C 规范两端逐式相同。
`normal` 保留为显式值（= 省略，便于显式覆盖）

```ts
export const BlendMode = {
  Normal: 'normal',
  Multiply: 'multiply',
  Screen: 'screen',
  Overlay: 'overlay',
  Darken: 'darken',
  Lighten: 'lighten',
  ColorDodge: 'color-dodge',
  ColorBurn: 'color-burn',
  HardLight: 'hard-light',
  SoftLight: 'soft-light',
  Difference: 'difference',
  Exclusion: 'exclusion',
  Hue: 'hue',
  Saturation: 'saturation',
  Color: 'color',
  Luminosity: 'luminosity',
} as const;
```
