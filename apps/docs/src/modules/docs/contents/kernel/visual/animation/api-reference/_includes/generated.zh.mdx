{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## 公开契约

### AnimationPresetOptions

<p>
  <ApiSourceLink label={'AnimationPresetOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={5}>
    preset 公共可调项（各 preset 在此之上加专有项；默认值由各 preset 给）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                          | 默认值 | 说明                                                              |
| ----------- | ----------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`    | `number`                      | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                      | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`  | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `trigger?`  | `IRAnimationTrack['trigger']` | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type AnimationPresetOptions = {
  duration?: number;
  delay?: number;
  easing?: IRAnimationTrack['easing'];
  trigger?: IRAnimationTrack['trigger'];
};
```

</DocTab>

</DocTabs>

### CameraToOptions

<p>
  <ApiSourceLink label={'CameraToOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={107}>
    `cameraTo` 选项：起止取景 `[x,y,w,h]`（均必填，纯工厂无法取当前 layout）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                               | 默认值 | 说明                                                              |
| ----------- | ---------------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`    | `number`                           | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                           | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`       | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `from`      | `[number, number, number, number]` | —      | 起始取景 `[x, y, w, h]`（必填）                                   |
| `to`        | `[number, number, number, number]` | —      | 终止取景 `[x, y, w, h]`（必填）                                   |
| `trigger?`  | `IRAnimationTrack['trigger']`      | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type CameraToOptions = AnimationPresetOptions & {
  from: [number, number, number, number];
  to: [number, number, number, number];
};
```

</DocTab>

</DocTabs>

### ColorShiftOptions

<p>
  <ApiSourceLink label={'ColorShiftOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={85}>
    `colorShift` 选项：通道 + 起止色（均必填，纯工厂无法取 base 色）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                          | 默认值 | 说明                                                              |
| ----------- | ----------------------------- | ------ | ----------------------------------------------------------------- |
| `channel?`  | `'fill' \| 'stroke'`          | —      | 变色通道；缺省 'fill'                                             |
| `delay?`    | `number`                      | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                      | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`  | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `from`      | `string`                      | —      | 起始颜色（必填）                                                  |
| `to`        | `string`                      | —      | 终止颜色（必填）                                                  |
| `trigger?`  | `IRAnimationTrack['trigger']` | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ColorShiftOptions = AnimationPresetOptions & {
  from: string;
  to: string;
  channel?: 'fill' | 'stroke';
};
```

</DocTab>

</DocTabs>

### IRAnimationTrack

<p>
  <ApiSourceLink label={'IRAnimationTrack'} path={'packages/kernel/core/src/schemas/animation/types.ts'} startLine={14}>
    时间轴动画 track（renderer 无关、JSON 可序列化、无函数；keyframe 给绝对展示值、末帧 = 元素 base 终态）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员          | 类型                                                                                                                                                                                                                                                                          | 默认值 | 说明 |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| `delay?`      | `number`                                                                                                                                                                                                                                                                      | —      | —    |
| `direction?`  | `"reverse" \| "normal" \| "alternate" \| "alternate-reverse"`                                                                                                                                                                                                                 | —      | —    |
| `duration`    | `number`                                                                                                                                                                                                                                                                      | —      | —    |
| `easing?`     | `string & {} \| "linear" \| "ease" \| "ease-in" \| "ease-out" \| "ease-in-out" \| [number, number, number, number]`                                                                                                                                                           | —      | —    |
| `fill?`       | `"none" \| "forwards" \| "backwards" \| "both"`                                                                                                                                                                                                                               | —      | —    |
| `iterations?` | `number \| "infinite"`                                                                                                                                                                                                                                                        | —      | —    |
| `keyframes`   | `Array<{ at: number; value: JsonValue; easing?: OpenString<ValueOf<{ readonly Linear: "linear"; readonly Ease: "ease"; readonly EaseIn: "ease-in"; readonly EaseOut: "ease-out"; readonly EaseInOut: "ease-in-out"; }>> \| [number, number, number, number] \| undefined; }>` | —      | —    |
| `origin?`     | `"center" \| "top" \| "right" \| "bottom" \| "left" \| "top-right" \| "top-left" \| "bottom-right" \| "bottom-left" \| [number, number]`                                                                                                                                      | —      | —    |
| `property`    | `…`                                                                                                                                                                                                                                                                           | —      | —    |
| `trigger?`    | `"load" \| "visible" \| "manual" \| { onEvent: string; }`                                                                                                                                                                                                                     | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRAnimationTrack = ZodInfer<typeof AnimationTrackSchema>;
```

</DocTab>

</DocTabs>

### IRKeyframe

<p>
  <ApiSourceLink label={'IRKeyframe'} path={'packages/kernel/core/src/schemas/animation/types.ts'} startLine={17}>
    单个动画关键帧
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员      | 类型                                                                                                                | 默认值 | 说明 |
| --------- | ------------------------------------------------------------------------------------------------------------------- | ------ | ---- |
| `at`      | `number`                                                                                                            | —      | —    |
| `easing?` | `string & {} \| "linear" \| "ease" \| "ease-in" \| "ease-out" \| "ease-in-out" \| [number, number, number, number]` | —      | —    |
| `value`   | `JsonValue`                                                                                                         | —      | —    |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type IRKeyframe = ZodInfer<typeof KeyframeSchema>;
```

</DocTab>

</DocTabs>

### LoopOptions

<p>
  <ApiSourceLink label={'LoopOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={127}>
    `loop` 选项：循环次数 + 方向
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员          | 类型                             | 默认值 | 说明                           |
| ------------- | -------------------------------- | ------ | ------------------------------ |
| `direction?`  | `IRAnimationTrack['direction']`  | —      | 每次迭代方向（如 'alternate'） |
| `iterations?` | `IRAnimationTrack['iterations']` | —      | 循环次数；缺省 'infinite'      |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type LoopOptions = {
  iterations?: IRAnimationTrack['iterations'];
  direction?: IRAnimationTrack['direction'];
};
```

</DocTab>

</DocTabs>

### ScaleInOptions

<p>
  <ApiSourceLink label={'ScaleInOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={48}>
    `scaleIn` 选项：起始均匀缩放 `from` + 支点 `origin`
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                          | 默认值 | 说明                                                              |
| ----------- | ----------------------------- | ------ | ----------------------------------------------------------------- |
| `delay?`    | `number`                      | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                      | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`  | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `from?`     | `number`                      | —      | 起始缩放（末帧恒为 1 = base）；缺省 0.8                           |
| `origin?`   | `IRAnimationOrigin`           | —      | 缩放支点（缺省几何中心）                                          |
| `trigger?`  | `IRAnimationTrack['trigger']` | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type ScaleInOptions = AnimationPresetOptions & {
  from?: number;
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### SlideInOptions

<p>
  <ApiSourceLink label={'SlideInOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={67}>
    `slideIn` 选项：轴向 + 起始位移
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员        | 类型                          | 默认值 | 说明                                                              |
| ----------- | ----------------------------- | ------ | ----------------------------------------------------------------- |
| `axis?`     | `'x' \| 'y'`                  | —      | 滑入轴向；缺省 'x'                                                |
| `delay?`    | `number`                      | —      | 首次迭代前延迟（毫秒）                                            |
| `duration?` | `number`                      | —      | 单次迭代时长（毫秒）；缺省由各 preset 给                          |
| `easing?`   | `IRAnimationTrack['easing']`  | —      | 缓动：具名预设 / cubic-bezier 四元组 / 注册名；缺省由各 preset 给 |
| `offset?`   | `number`                      | —      | 起始位移（末帧恒为 0 = base）；缺省 −20                           |
| `trigger?`  | `IRAnimationTrack['trigger']` | —      | 播放触发器；缺省 load                                             |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type SlideInOptions = AnimationPresetOptions & {
  axis?: 'x' | 'y';
  offset?: number;
};
```

</DocTab>

</DocTabs>

### AnimationDirection

<p>
  <ApiSourceLink
    label={'AnimationDirection'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={33}
  >
    每次迭代的播放方向（抄 WAAPI / CSS animation-direction）
  </ApiSourceLink>
</p>

```ts
export const AnimationDirection = {
  Normal: 'normal',
  Reverse: 'reverse',
  Alternate: 'alternate',
  AlternateReverse: 'alternate-reverse',
} as const;
```

### AnimationEasing

<p>
  <ApiSourceLink
    label={'AnimationEasing'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={24}
  >
    缓动具名预设（与 CSS 同名）；track / keyframe 亦可改用 cubic-bezier 四元组
  </ApiSourceLink>
</p>

```ts
export const AnimationEasing = {
  Linear: 'linear',
  Ease: 'ease',
  EaseIn: 'ease-in',
  EaseOut: 'ease-out',
  EaseInOut: 'ease-in-out',
} as const;
```

### AnimationFill

<p>
  <ApiSourceLink
    label={'AnimationFill'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={41}
  >
    活动区间外取值（抄 WAAPI / CSS animation-fill-mode）
  </ApiSourceLink>
</p>

```ts
export const AnimationFill = {
  None: 'none',
  Forwards: 'forwards',
  Backwards: 'backwards',
  Both: 'both',
} as const;
```

### AnimationProperty

<p>
  <ApiSourceLink
    label={'AnimationProperty'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={8}
  >
    可动画属性通道（renderer 无关；DrawWay 风格 const + 派生类型，裸字面量 'opacity' 仍第一形态）
  </ApiSourceLink>
</p>

`viewBox` 仅在 scene 根合法（镜头），元素级 viewBox track 由 compile / render 拒；
`pathDraw` 是 0..1 路径画出进度；`scaleX` / `scaleY` 是非均匀缩放（柱状图从基线长出等），`scale` 是均匀缩放；
transform 通道（scale / scaleX / scaleY / rotate）的支点见 track 级 `origin`，缺省几何中心。
各后端按通道翻译：SVG WAAPI/CSS、Canvas rAF 几何 lerp

```ts
export const AnimationProperty = {
  Opacity: 'opacity',
  Fill: 'fill',
  Stroke: 'stroke',
  StrokeWidth: 'strokeWidth',
  TranslateX: 'translateX',
  TranslateY: 'translateY',
  Rotate: 'rotate',
  Scale: 'scale',
  ScaleX: 'scaleX',
  ScaleY: 'scaleY',
  PathDraw: 'pathDraw',
  ViewBox: 'viewBox',
} as const;
```

### AnimationTrigger

<p>
  <ApiSourceLink
    label={'AnimationTrigger'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={49}
  >
    播放触发器关键字（runtime 落地；DrawWay 风格 const + 派生类型，与其它 Animation 枚举单一真源一致）
  </ApiSourceLink>
</p>

```ts
export const AnimationTrigger = { Load: 'load', Visible: 'visible', Manual: 'manual' } as const;
```

## 预设与编排

### cameraTo

<p><ApiSourceLink label={"cameraTo"} path={"packages/kernel/core/src/parse/animation.ts"} startLine={115}>镜头：scene 根 `viewBox` from→to（挂 `<Layout animations>` / IR 根 `animations`）</ApiSourceLink></p>

```ts
export declare const cameraTo: (opts: CameraToOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型              | 说明 |
| ------ | ----------------- | ---- |
| `opts` | `CameraToOptions` | —    |

### colorShift

<p>
  <ApiSourceLink label={'colorShift'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={95}>
    变色：`fill|stroke` from→to（oklch 插值，由 renderer 端处理）
  </ApiSourceLink>
</p>

```ts
export declare const colorShift: (opts: ColorShiftOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型                | 说明 |
| ------ | ------------------- | ---- |
| `opts` | `ColorShiftOptions` | —    |

### drawOn

<p>
  <ApiSourceLink label={'drawOn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={38}>
    描边画出：`pathDraw` 0→1（仅对有描边元素有效）
  </ApiSourceLink>
</p>

```ts
export declare const drawOn: (opts: AnimationPresetOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型                     | 说明 |
| ------ | ------------------------ | ---- |
| `opts` | `AnimationPresetOptions` | —    |

### fadeIn

<p>
  <ApiSourceLink label={'fadeIn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={28}>
    淡入：`opacity` 0→1（末帧 = base，降级见完整图）
  </ApiSourceLink>
</p>

```ts
export declare const fadeIn: (opts: AnimationPresetOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型                     | 说明 |
| ------ | ------------------------ | ---- |
| `opts` | `AnimationPresetOptions` | —    |

### loop

<p>
  <ApiSourceLink label={'loop'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={135}>
    循环包装：给任意 track 叠加无限（或指定次数）循环 + 方向
  </ApiSourceLink>
</p>

```ts
export declare const loop: (
  track: {
    delay?: number;
    direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
    duration: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    fill?: 'none' | 'forwards' | 'backwards' | 'both';
    iterations?: number | 'infinite';
    keyframes: Array<{
      at: number;
      easing?:
        | OpenString<
            ValueOf<{
              Ease: 'ease';
              EaseIn: 'ease-in';
              EaseInOut: 'ease-in-out';
              EaseOut: 'ease-out';
              Linear: 'linear';
            }>
          >
        | [number, number, number, number];
      value: JsonValue;
    }>;
    origin?:
      | 'center'
      | 'top'
      | 'right'
      | 'bottom'
      | 'left'
      | 'top-right'
      | 'top-left'
      | 'bottom-right'
      | 'bottom-left'
      | [number, number];
    property: OpenString<
      ValueOf<{
        Fill: 'fill';
        Opacity: 'opacity';
        PathDraw: 'pathDraw';
        Rotate: 'rotate';
        Scale: 'scale';
        ScaleX: 'scaleX';
        ScaleY: 'scaleY';
        Stroke: 'stroke';
        StrokeWidth: 'strokeWidth';
        TranslateX: 'translateX';
        TranslateY: 'translateY';
        ViewBox: 'viewBox';
      }>
    >;
    trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
  },
  opts: LoopOptions,
) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数    | 类型                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 说明 |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `track` | `{ delay?: number; direction?: 'reverse' \| 'normal' \| 'alternate' \| 'alternate-reverse'; duration: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; fill?: 'none' \| 'forwards' \| 'backwards' \| 'both'; iterations?: number \| 'infinite'; keyframes: Array<{ at: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; value: JsonValue }>; origin?: 'center' \| 'top' \| 'right' \| 'bottom' \| 'left' \| 'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left' \| [number, number]; property: OpenString<ValueOf<{ Fill: 'fill'; Opacity: 'opacity'; PathDraw: 'pathDraw'; Rotate: 'rotate'; Scale: 'scale'; ScaleX: 'scaleX'; ScaleY: 'scaleY'; Stroke: 'stroke'; StrokeWidth: 'strokeWidth'; TranslateX: 'translateX'; TranslateY: 'translateY'; ViewBox: 'viewBox' }>>; trigger?: 'load' \| 'visible' \| 'manual' \| { onEvent: string } }` | —    |
| `opts`  | `LoopOptions`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —    |

### scaleIn

<p>
  <ApiSourceLink label={'scaleIn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={56}>
    缩放入场：`scale` from→1（均匀，绕 origin）
  </ApiSourceLink>
</p>

```ts
export declare const scaleIn: (opts: ScaleInOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型             | 说明 |
| ------ | ---------------- | ---- |
| `opts` | `ScaleInOptions` | —    |

### slideIn

<p>
  <ApiSourceLink label={'slideIn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={75}>
    滑入：`translateX|Y` offset→0
  </ApiSourceLink>
</p>

```ts
export declare const slideIn: (opts: SlideInOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### 参数

| 参数   | 类型             | 说明 |
| ------ | ---------------- | ---- |
| `opts` | `SlideInOptions` | —    |

### stagger

<p>
  <ApiSourceLink label={'stagger'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={145}>
    错峰：给一组 track 依次叠加 delay（`startMs + i*stepMs`），实现「N 元素依次入场」
  </ApiSourceLink>
</p>

覆盖各 track 原有 delay（错峰编排以本 helper 为准）

```ts
export declare const stagger: (
  tracks: readonly Array<{
    delay?: number;
    direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
    duration: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    fill?: 'none' | 'forwards' | 'backwards' | 'both';
    iterations?: number | 'infinite';
    keyframes: Array<{
      at: number;
      easing?:
        | OpenString<
            ValueOf<{
              Ease: 'ease';
              EaseIn: 'ease-in';
              EaseInOut: 'ease-in-out';
              EaseOut: 'ease-out';
              Linear: 'linear';
            }>
          >
        | [number, number, number, number];
      value: JsonValue;
    }>;
    origin?:
      | 'center'
      | 'top'
      | 'right'
      | 'bottom'
      | 'left'
      | 'top-right'
      | 'top-left'
      | 'bottom-right'
      | 'bottom-left'
      | [number, number];
    property: OpenString<
      ValueOf<{
        Fill: 'fill';
        Opacity: 'opacity';
        PathDraw: 'pathDraw';
        Rotate: 'rotate';
        Scale: 'scale';
        ScaleX: 'scaleX';
        ScaleY: 'scaleY';
        Stroke: 'stroke';
        StrokeWidth: 'strokeWidth';
        TranslateX: 'translateX';
        TranslateY: 'translateY';
        ViewBox: 'viewBox';
      }>
    >;
    trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
  }>,
  stepMs: number,
  startMs: number,
) => Array<{
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
}>;
```

#### 参数

| 参数      | 类型                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 说明 |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| `tracks`  | `readonly Array<{ delay?: number; direction?: 'reverse' \| 'normal' \| 'alternate' \| 'alternate-reverse'; duration: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; fill?: 'none' \| 'forwards' \| 'backwards' \| 'both'; iterations?: number \| 'infinite'; keyframes: Array<{ at: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; value: JsonValue }>; origin?: 'center' \| 'top' \| 'right' \| 'bottom' \| 'left' \| 'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left' \| [number, number]; property: OpenString<ValueOf<{ Fill: 'fill'; Opacity: 'opacity'; PathDraw: 'pathDraw'; Rotate: 'rotate'; Scale: 'scale'; ScaleX: 'scaleX'; ScaleY: 'scaleY'; Stroke: 'stroke'; StrokeWidth: 'strokeWidth'; TranslateX: 'translateX'; TranslateY: 'translateY'; ViewBox: 'viewBox' }>>; trigger?: 'load' \| 'visible' \| 'manual' \| { onEvent: string } }>` | —    |
| `stepMs`  | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —    |
| `startMs` | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —    |

## 播放与扩展

### AnimationControls

<p>
  <ApiSourceLink label={'AnimationControls'} path={'packages/kernel/render/src/animation/runtime.ts'} startLine={27}>
    播放控制句柄（manual trigger / runtime 暴露给调用方）
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员               | 类型                       | 默认值 | 说明                                    |
| ------------------ | -------------------------- | ------ | --------------------------------------- |
| `dispose`          | `() => void`               | —      | 停止并释放（rAF / observer / listener） |
| `pause`            | `() => void`               | —      | 暂停（保留当前时刻）                    |
| `play`             | `() => void`               | —      | 开始 / 继续播放                         |
| `readonly running` | `boolean`                  | —      | 是否在播放                              |
| `seek`             | `(timeMs: number) => void` | —      | 跳到指定时刻（毫秒）并渲染该帧          |
| `readonly time`    | `number`                   | —      | 当前时刻（毫秒）                        |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type AnimationControls = {
  play: () => void;
  pause: () => void;
  seek: (timeMs: number) => void;
  dispose: () => void;
  readonly time: number;
  readonly running: boolean;
};
```

</DocTab>

</DocTabs>

### AnimationPropertyDefinition

<p>
  <ApiSourceLink
    label={'AnimationPropertyDefinition'}
    path={'packages/kernel/render/src/animation/registry.ts'}
    startLine={10}
  >
    单个自定义 property 的插值 + Canvas 应用定义
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="属性">

| 成员          | 类型                                                                            | 默认值 | 说明                                                                              |
| ------------- | ------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------- |
| `applyCanvas` | `(ctx: CanvasRenderingContext2D, prim: ScenePrimitive, value: unknown) => void` | —      | 把当下值应用到 Canvas context（绘制该 prim 前调用；在 ctx.save/restore 作用域内） |
| `interpolate` | `(from: unknown, to: unknown, t: number) => unknown`                            | —      | 两关键帧值 + 段进度 → 当下值（喂给 evaluateTrack 的 interpolateCustom）           |

</DocTab>

<DocTab value="definition" label="类型定义">

```ts
export type AnimationPropertyDefinition = {
  interpolate: (from: unknown, to: unknown, t: number) => unknown;
  applyCanvas: (ctx: CanvasRenderingContext2D, prim: ScenePrimitive, value: unknown) => void;
};
```

</DocTab>

</DocTabs>

### AnimationPropertyRegistry

<p>
  <ApiSourceLink
    label={'AnimationPropertyRegistry'}
    path={'packages/kernel/render/src/animation/registry.ts'}
    startLine={18}
  >
    自定义 property 注册表：property 名 → 定义
  </ApiSourceLink>
</p>

```ts
export type AnimationPropertyRegistry = Record<string, AnimationPropertyDefinition>;
```

#### 索引签名

```ts
[key: string]: AnimationPropertyDefinition
```

### EasingRegistry

<p>
  <ApiSourceLink label={'EasingRegistry'} path={'packages/kernel/render/src/animation/types.ts'} startLine={12}>
    自定义缓动注册表：名 → cubic-bezier 四元组（CSS / WAAPI 通用）或函数（仅 Canvas / JS）
  </ApiSourceLink>
</p>

```ts
export type EasingRegistry = Record<string, CubicBezier | EasingFn>;
```

#### 索引签名

```ts
[key: string]: CubicBezier | EasingFn
```
