{/* Generated by pnpm generate:api-reference. Do not edit manually. */}

## Public contracts

### AnimationPresetOptions

<p>
  <ApiSourceLink label={'AnimationPresetOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={5}>
    Shared preset options; each preset adds specific options and supplies its own defaults
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                          | Default | Description                                                                            |
| ----------- | ----------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`    | `number`                      | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                      | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`  | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `trigger?`  | `IRAnimationTrack['trigger']` | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type AnimationPresetOptions = {
  duration?: number;
  delay?: number;
  easing?: IRAnimationTrack['easing'];
  trigger?: IRAnimationTrack['trigger'];
};
```

</DocTab>

</DocTabs>

### CameraToOptions

<p>
  <ApiSourceLink label={'CameraToOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={107}>
    CameraTo options: required start and end view boxes [x,y,w,h]; the pure factory cannot read the current layout
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                               | Default | Description                                                                            |
| ----------- | ---------------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`    | `number`                           | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                           | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`       | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `from`      | `[number, number, number, number]` | —       | Required initial view box [x, y, w, h]                                                 |
| `to`        | `[number, number, number, number]` | —       | Required final view box [x, y, w, h]                                                   |
| `trigger?`  | `IRAnimationTrack['trigger']`      | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type CameraToOptions = AnimationPresetOptions & {
  from: [number, number, number, number];
  to: [number, number, number, number];
};
```

</DocTab>

</DocTabs>

### ColorShiftOptions

<p>
  <ApiSourceLink label={'ColorShiftOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={85}>
    ColorShift options: channel and required start/end colors; the pure factory cannot read the base color
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                          | Default | Description                                                                            |
| ----------- | ----------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `channel?`  | `'fill' \| 'stroke'`          | —       | Color channel; defaults to fill                                                        |
| `delay?`    | `number`                      | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                      | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`  | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `from`      | `string`                      | —       | Required initial color                                                                 |
| `to`        | `string`                      | —       | Required final color                                                                   |
| `trigger?`  | `IRAnimationTrack['trigger']` | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ColorShiftOptions = AnimationPresetOptions & {
  from: string;
  to: string;
  channel?: 'fill' | 'stroke';
};
```

</DocTab>

</DocTabs>

### IRAnimationTrack

<p>
  <ApiSourceLink label={'IRAnimationTrack'} path={'packages/kernel/core/src/schemas/animation/types.ts'} startLine={14}>
    Renderer-independent, JSON-serializable animation track without functions; keyframes contain absolute display values
    and the final frame represents the base settled state
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member        | Type                                                                                                                                                                                                                                                                          | Default | Description |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------- |
| `delay?`      | `number`                                                                                                                                                                                                                                                                      | —       | —           |
| `direction?`  | `"reverse" \| "normal" \| "alternate" \| "alternate-reverse"`                                                                                                                                                                                                                 | —       | —           |
| `duration`    | `number`                                                                                                                                                                                                                                                                      | —       | —           |
| `easing?`     | `string & {} \| "linear" \| "ease" \| "ease-in" \| "ease-out" \| "ease-in-out" \| [number, number, number, number]`                                                                                                                                                           | —       | —           |
| `fill?`       | `"none" \| "forwards" \| "backwards" \| "both"`                                                                                                                                                                                                                               | —       | —           |
| `iterations?` | `number \| "infinite"`                                                                                                                                                                                                                                                        | —       | —           |
| `keyframes`   | `Array<{ at: number; value: JsonValue; easing?: OpenString<ValueOf<{ readonly Linear: "linear"; readonly Ease: "ease"; readonly EaseIn: "ease-in"; readonly EaseOut: "ease-out"; readonly EaseInOut: "ease-in-out"; }>> \| [number, number, number, number] \| undefined; }>` | —       | —           |
| `origin?`     | `"center" \| "top" \| "right" \| "bottom" \| "left" \| "top-right" \| "top-left" \| "bottom-right" \| "bottom-left" \| [number, number]`                                                                                                                                      | —       | —           |
| `property`    | `…`                                                                                                                                                                                                                                                                           | —       | —           |
| `trigger?`    | `"load" \| "visible" \| "manual" \| { onEvent: string; }`                                                                                                                                                                                                                     | —       | —           |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type IRAnimationTrack = ZodInfer<typeof AnimationTrackSchema>;
```

</DocTab>

</DocTabs>

### IRKeyframe

<p>
  <ApiSourceLink label={'IRKeyframe'} path={'packages/kernel/core/src/schemas/animation/types.ts'} startLine={17}>
    One animation keyframe
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member    | Type                                                                                                                | Default | Description |
| --------- | ------------------------------------------------------------------------------------------------------------------- | ------- | ----------- |
| `at`      | `number`                                                                                                            | —       | —           |
| `easing?` | `string & {} \| "linear" \| "ease" \| "ease-in" \| "ease-out" \| "ease-in-out" \| [number, number, number, number]` | —       | —           |
| `value`   | `JsonValue`                                                                                                         | —       | —           |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type IRKeyframe = ZodInfer<typeof KeyframeSchema>;
```

</DocTab>

</DocTabs>

### LoopOptions

<p>
  <ApiSourceLink label={'LoopOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={127}>
    Loop options: iteration count and direction
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member        | Type                             | Default | Description                                    |
| ------------- | -------------------------------- | ------- | ---------------------------------------------- |
| `direction?`  | `IRAnimationTrack['direction']`  | —       | Direction of each iteration, such as alternate |
| `iterations?` | `IRAnimationTrack['iterations']` | —       | Iteration count; defaults to infinite          |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type LoopOptions = {
  iterations?: IRAnimationTrack['iterations'];
  direction?: IRAnimationTrack['direction'];
};
```

</DocTab>

</DocTabs>

### ScaleInOptions

<p>
  <ApiSourceLink label={'ScaleInOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={48}>
    ScaleIn options: initial uniform scale from and origin
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                          | Default | Description                                                                            |
| ----------- | ----------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `delay?`    | `number`                      | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                      | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`  | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `from?`     | `number`                      | —       | Initial scale; the final value is always 1 (base); defaults to 0.8                     |
| `origin?`   | `IRAnimationOrigin`           | —       | Scale origin; defaults to the geometry center                                          |
| `trigger?`  | `IRAnimationTrack['trigger']` | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type ScaleInOptions = AnimationPresetOptions & {
  from?: number;
  origin?: IRAnimationOrigin;
};
```

</DocTab>

</DocTabs>

### SlideInOptions

<p>
  <ApiSourceLink label={'SlideInOptions'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={67}>
    SlideIn options: axis and initial offset
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member      | Type                          | Default | Description                                                                            |
| ----------- | ----------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `axis?`     | `'x' \| 'y'`                  | —       | Slide axis; defaults to x                                                              |
| `delay?`    | `number`                      | —       | Delay before the first iteration, in milliseconds                                      |
| `duration?` | `number`                      | —       | Duration of one iteration in milliseconds; each preset supplies its default            |
| `easing?`   | `IRAnimationTrack['easing']`  | —       | Named easing, cubic-bezier tuple, or registered name; each preset supplies its default |
| `offset?`   | `number`                      | —       | Initial offset; the final value is always 0 (base); defaults to -20                    |
| `trigger?`  | `IRAnimationTrack['trigger']` | —       | Playback trigger; defaults to load                                                     |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type SlideInOptions = AnimationPresetOptions & {
  axis?: 'x' | 'y';
  offset?: number;
};
```

</DocTab>

</DocTabs>

### AnimationDirection

<p>
  <ApiSourceLink
    label={'AnimationDirection'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={33}
  >
    Playback direction per iteration, following WAAPI/CSS animation-direction
  </ApiSourceLink>
</p>

```ts
export const AnimationDirection = {
  Normal: 'normal',
  Reverse: 'reverse',
  Alternate: 'alternate',
  AlternateReverse: 'alternate-reverse',
} as const;
```

### AnimationEasing

<p>
  <ApiSourceLink
    label={'AnimationEasing'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={24}
  >
    Named easing presets matching CSS; tracks and keyframes can instead use a cubic-bezier tuple
  </ApiSourceLink>
</p>

```ts
export const AnimationEasing = {
  Linear: 'linear',
  Ease: 'ease',
  EaseIn: 'ease-in',
  EaseOut: 'ease-out',
  EaseInOut: 'ease-in-out',
} as const;
```

### AnimationFill

<p>
  <ApiSourceLink
    label={'AnimationFill'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={41}
  >
    Values outside the active interval, following WAAPI/CSS animation-fill-mode
  </ApiSourceLink>
</p>

```ts
export const AnimationFill = {
  None: 'none',
  Forwards: 'forwards',
  Backwards: 'backwards',
  Both: 'both',
} as const;
```

### AnimationProperty

<p>
  <ApiSourceLink
    label={'AnimationProperty'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={8}
  >
    Renderer-independent animation channels represented by const names and derived types; literals such as opacity
    remain supported
  </ApiSourceLink>
</p>

viewBox is valid only at the scene root for camera animation; element-level viewBox tracks are rejected. pathDraw is stroke-reveal progress from 0 to 1. scaleX/scaleY are nonuniform scale; scale is uniform. Transform origins are specified by track.origin and default to the geometry center. SVG maps channels to WAAPI/CSS; Canvas uses animation-frame interpolation.

```ts
export const AnimationProperty = {
  Opacity: 'opacity',
  Fill: 'fill',
  Stroke: 'stroke',
  StrokeWidth: 'strokeWidth',
  TranslateX: 'translateX',
  TranslateY: 'translateY',
  Rotate: 'rotate',
  Scale: 'scale',
  ScaleX: 'scaleX',
  ScaleY: 'scaleY',
  PathDraw: 'pathDraw',
  ViewBox: 'viewBox',
} as const;
```

### AnimationTrigger

<p>
  <ApiSourceLink
    label={'AnimationTrigger'}
    path={'packages/kernel/core/src/schemas/animation/constants.ts'}
    startLine={49}
  >
    Playback trigger names implemented by the runtime, using the same const-and-derived-type convention as other
    animation enums
  </ApiSourceLink>
</p>

```ts
export const AnimationTrigger = { Load: 'load', Visible: 'visible', Manual: 'manual' } as const;
```

## Presets and orchestration

### cameraTo

<p>
  <ApiSourceLink label={'cameraTo'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={115}>
    Camera animation: scene-root viewBox from start to end, attached to Layout animations or IR root animations
  </ApiSourceLink>
</p>

```ts
export declare const cameraTo: (opts: CameraToOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type              | Description |
| --------- | ----------------- | ----------- |
| `opts`    | `CameraToOptions` | —           |

### colorShift

<p>
  <ApiSourceLink label={'colorShift'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={95}>
    Color transition: fill or stroke from start to end, interpolated in OKLCH by the renderer
  </ApiSourceLink>
</p>

```ts
export declare const colorShift: (opts: ColorShiftOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type                | Description |
| --------- | ------------------- | ----------- |
| `opts`    | `ColorShiftOptions` | —           |

### drawOn

<p>
  <ApiSourceLink label={'drawOn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={38}>
    Stroke reveal: pathDraw from 0 to 1; requires a stroked element
  </ApiSourceLink>
</p>

```ts
export declare const drawOn: (opts: AnimationPresetOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type                     | Description |
| --------- | ------------------------ | ----------- |
| `opts`    | `AnimationPresetOptions` | —           |

### fadeIn

<p>
  <ApiSourceLink label={'fadeIn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={28}>
    Fade in: opacity from 0 to 1; final frame matches the base value
  </ApiSourceLink>
</p>

```ts
export declare const fadeIn: (opts: AnimationPresetOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type                     | Description |
| --------- | ------------------------ | ----------- |
| `opts`    | `AnimationPresetOptions` | —           |

### loop

<p>
  <ApiSourceLink label={'loop'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={135}>
    Loop wrapper: applies infinite or specified repetition and direction to any track
  </ApiSourceLink>
</p>

```ts
export declare const loop: (
  track: {
    delay?: number;
    direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
    duration: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    fill?: 'none' | 'forwards' | 'backwards' | 'both';
    iterations?: number | 'infinite';
    keyframes: Array<{
      at: number;
      easing?:
        | OpenString<
            ValueOf<{
              Ease: 'ease';
              EaseIn: 'ease-in';
              EaseInOut: 'ease-in-out';
              EaseOut: 'ease-out';
              Linear: 'linear';
            }>
          >
        | [number, number, number, number];
      value: JsonValue;
    }>;
    origin?:
      | 'center'
      | 'top'
      | 'right'
      | 'bottom'
      | 'left'
      | 'top-right'
      | 'top-left'
      | 'bottom-right'
      | 'bottom-left'
      | [number, number];
    property: OpenString<
      ValueOf<{
        Fill: 'fill';
        Opacity: 'opacity';
        PathDraw: 'pathDraw';
        Rotate: 'rotate';
        Scale: 'scale';
        ScaleX: 'scaleX';
        ScaleY: 'scaleY';
        Stroke: 'stroke';
        StrokeWidth: 'strokeWidth';
        TranslateX: 'translateX';
        TranslateY: 'translateY';
        ViewBox: 'viewBox';
      }>
    >;
    trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
  },
  opts: LoopOptions,
) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Description |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| `track`   | `{ delay?: number; direction?: 'reverse' \| 'normal' \| 'alternate' \| 'alternate-reverse'; duration: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; fill?: 'none' \| 'forwards' \| 'backwards' \| 'both'; iterations?: number \| 'infinite'; keyframes: Array<{ at: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; value: JsonValue }>; origin?: 'center' \| 'top' \| 'right' \| 'bottom' \| 'left' \| 'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left' \| [number, number]; property: OpenString<ValueOf<{ Fill: 'fill'; Opacity: 'opacity'; PathDraw: 'pathDraw'; Rotate: 'rotate'; Scale: 'scale'; ScaleX: 'scaleX'; ScaleY: 'scaleY'; Stroke: 'stroke'; StrokeWidth: 'strokeWidth'; TranslateX: 'translateX'; TranslateY: 'translateY'; ViewBox: 'viewBox' }>>; trigger?: 'load' \| 'visible' \| 'manual' \| { onEvent: string } }` | —           |
| `opts`    | `LoopOptions`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | —           |

### scaleIn

<p>
  <ApiSourceLink label={'scaleIn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={56}>
    Scale entrance: uniform scale from the initial value to 1 around origin
  </ApiSourceLink>
</p>

```ts
export declare const scaleIn: (opts: ScaleInOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type             | Description |
| --------- | ---------------- | ----------- |
| `opts`    | `ScaleInOptions` | —           |

### slideIn

<p>
  <ApiSourceLink label={'slideIn'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={75}>
    Slide in: translateX or translateY from offset to zero
  </ApiSourceLink>
</p>

```ts
export declare const slideIn: (opts: SlideInOptions) => {
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
};
```

#### Parameters

| Parameter | Type             | Description |
| --------- | ---------------- | ----------- |
| `opts`    | `SlideInOptions` | —           |

### stagger

<p>
  <ApiSourceLink label={'stagger'} path={'packages/kernel/core/src/parse/animation.ts'} startLine={145}>
    Stagger: assigns startMs + i * stepMs delays to tracks for sequential element entrances
  </ApiSourceLink>
</p>

Overrides existing track delays; this helper owns the staggered timing

```ts
export declare const stagger: (
  tracks: readonly Array<{
    delay?: number;
    direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
    duration: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    fill?: 'none' | 'forwards' | 'backwards' | 'both';
    iterations?: number | 'infinite';
    keyframes: Array<{
      at: number;
      easing?:
        | OpenString<
            ValueOf<{
              Ease: 'ease';
              EaseIn: 'ease-in';
              EaseInOut: 'ease-in-out';
              EaseOut: 'ease-out';
              Linear: 'linear';
            }>
          >
        | [number, number, number, number];
      value: JsonValue;
    }>;
    origin?:
      | 'center'
      | 'top'
      | 'right'
      | 'bottom'
      | 'left'
      | 'top-right'
      | 'top-left'
      | 'bottom-right'
      | 'bottom-left'
      | [number, number];
    property: OpenString<
      ValueOf<{
        Fill: 'fill';
        Opacity: 'opacity';
        PathDraw: 'pathDraw';
        Rotate: 'rotate';
        Scale: 'scale';
        ScaleX: 'scaleX';
        ScaleY: 'scaleY';
        Stroke: 'stroke';
        StrokeWidth: 'strokeWidth';
        TranslateX: 'translateX';
        TranslateY: 'translateY';
        ViewBox: 'viewBox';
      }>
    >;
    trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
  }>,
  stepMs: number,
  startMs: number,
) => Array<{
  delay?: number;
  direction?: 'reverse' | 'normal' | 'alternate' | 'alternate-reverse';
  duration: number;
  easing?:
    | OpenString<
        ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
      >
    | [number, number, number, number];
  fill?: 'none' | 'forwards' | 'backwards' | 'both';
  iterations?: number | 'infinite';
  keyframes: Array<{
    at: number;
    easing?:
      | OpenString<
          ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>
        >
      | [number, number, number, number];
    value: JsonValue;
  }>;
  origin?:
    | 'center'
    | 'top'
    | 'right'
    | 'bottom'
    | 'left'
    | 'top-right'
    | 'top-left'
    | 'bottom-right'
    | 'bottom-left'
    | [number, number];
  property: OpenString<
    ValueOf<{
      Fill: 'fill';
      Opacity: 'opacity';
      PathDraw: 'pathDraw';
      Rotate: 'rotate';
      Scale: 'scale';
      ScaleX: 'scaleX';
      ScaleY: 'scaleY';
      Stroke: 'stroke';
      StrokeWidth: 'strokeWidth';
      TranslateX: 'translateX';
      TranslateY: 'translateY';
      ViewBox: 'viewBox';
    }>
  >;
  trigger?: 'load' | 'visible' | 'manual' | { onEvent: string };
}>;
```

#### Parameters

| Parameter | Type                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Description |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| `tracks`  | `readonly Array<{ delay?: number; direction?: 'reverse' \| 'normal' \| 'alternate' \| 'alternate-reverse'; duration: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; fill?: 'none' \| 'forwards' \| 'backwards' \| 'both'; iterations?: number \| 'infinite'; keyframes: Array<{ at: number; easing?: OpenString<ValueOf<{ Ease: 'ease'; EaseIn: 'ease-in'; EaseInOut: 'ease-in-out'; EaseOut: 'ease-out'; Linear: 'linear' }>> \| [number, number, number, number]; value: JsonValue }>; origin?: 'center' \| 'top' \| 'right' \| 'bottom' \| 'left' \| 'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left' \| [number, number]; property: OpenString<ValueOf<{ Fill: 'fill'; Opacity: 'opacity'; PathDraw: 'pathDraw'; Rotate: 'rotate'; Scale: 'scale'; ScaleX: 'scaleX'; ScaleY: 'scaleY'; Stroke: 'stroke'; StrokeWidth: 'strokeWidth'; TranslateX: 'translateX'; TranslateY: 'translateY'; ViewBox: 'viewBox' }>>; trigger?: 'load' \| 'visible' \| 'manual' \| { onEvent: string } }>` | —           |
| `stepMs`  | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —           |
| `startMs` | `number`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —           |

## Playback and extensions

### AnimationControls

<p>
  <ApiSourceLink label={'AnimationControls'} path={'packages/kernel/render/src/animation/runtime.ts'} startLine={27}>
    Playback handle exposed for manual triggers and runtime control
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member             | Type                       | Default | Description                                                 |
| ------------------ | -------------------------- | ------- | ----------------------------------------------------------- |
| `dispose`          | `() => void`               | —       | Stop and release animation frames, observers, and listeners |
| `pause`            | `() => void`               | —       | Pause while retaining the current time                      |
| `play`             | `() => void`               | —       | Start or resume playback                                    |
| `readonly running` | `boolean`                  | —       | Whether playback is active                                  |
| `seek`             | `(timeMs: number) => void` | —       | Seek to a time in milliseconds and render that frame        |
| `readonly time`    | `number`                   | —       | Current time in milliseconds                                |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type AnimationControls = {
  play: () => void;
  pause: () => void;
  seek: (timeMs: number) => void;
  dispose: () => void;
  readonly time: number;
  readonly running: boolean;
};
```

</DocTab>

</DocTabs>

### AnimationPropertyDefinition

<p>
  <ApiSourceLink
    label={'AnimationPropertyDefinition'}
    path={'packages/kernel/render/src/animation/registry.ts'}
    startLine={10}
  >
    Interpolation and Canvas application definition for one custom property
  </ApiSourceLink>
</p>

<DocTabs defaultValue="members">

<DocTab value="members" label="Members">

| Member        | Type                                                                            | Default | Description                                                                                           |
| ------------- | ------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------- |
| `applyCanvas` | `(ctx: CanvasRenderingContext2D, prim: ScenePrimitive, value: unknown) => void` | —       | Apply the current value to the Canvas context before drawing the primitive, inside save/restore       |
| `interpolate` | `(from: unknown, to: unknown, t: number) => unknown`                            | —       | Map two keyframe values and segment progress to the current value for evaluateTrack.interpolateCustom |

</DocTab>

<DocTab value="definition" label="Type definition">

```ts
export type AnimationPropertyDefinition = {
  interpolate: (from: unknown, to: unknown, t: number) => unknown;
  applyCanvas: (ctx: CanvasRenderingContext2D, prim: ScenePrimitive, value: unknown) => void;
};
```

</DocTab>

</DocTabs>

### AnimationPropertyRegistry

<p>
  <ApiSourceLink
    label={'AnimationPropertyRegistry'}
    path={'packages/kernel/render/src/animation/registry.ts'}
    startLine={18}
  >
    Custom property registry mapping names to definitions
  </ApiSourceLink>
</p>

```ts
export type AnimationPropertyRegistry = Record<string, AnimationPropertyDefinition>;
```

#### Index signatures

```ts
[key: string]: AnimationPropertyDefinition
```

### EasingRegistry

<p>
  <ApiSourceLink label={'EasingRegistry'} path={'packages/kernel/render/src/animation/types.ts'} startLine={12}>
    Custom easing registry mapping names to cubic-bezier tuples (CSS/WAAPI) or functions (Canvas/JavaScript only)
  </ApiSourceLink>
</p>

```ts
export type EasingRegistry = Record<string, CubicBezier | EasingFn>;
```

#### Index signatures

```ts
[key: string]: CubicBezier | EasingFn
```
